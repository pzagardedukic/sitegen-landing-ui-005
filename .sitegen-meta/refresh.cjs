"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __esm = (fn2, res) =>
  function __init() {
    return (fn2 && (res = (0, fn2[__getOwnPropNames(fn2)[0]])((fn2 = 0))), res);
  };
var __commonJS = (cb, mod) =>
  function __require() {
    return (
      mod ||
        (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod),
      mod.exports
    );
  };
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from2, except, desc) => {
  if ((from2 && typeof from2 === "object") || typeof from2 === "function") {
    for (let key of __getOwnPropNames(from2))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, {
          get: () => from2[key],
          enumerable: !(desc = __getOwnPropDesc(from2, key)) || desc.enumerable,
        });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (
  (target = mod != null ? __create(__getProtoOf(mod)) : {}),
  __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule
      ? __defProp(target, "default", { value: mod, enumerable: true })
      : target,
    mod,
  )
);
var __toCommonJS = (mod) =>
  __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/static/path.js
function createPathHelpers(basePath) {
  const BASE_PATH2 = normalizeBasePath(basePath);
  const imgWithBasePath2 = (path3) => {
    if (!path3) return "";
    if (/^https?:\/\//i.test(path3) || BASE_PATH2 === "") return path3;
    return `${BASE_PATH2}${path3.startsWith("/") ? path3 : `/${path3}`}`;
  };
  const withBasePath2 = (path3) => {
    if (path3 === "" || BASE_PATH2 === "") return path3;
    return `${BASE_PATH2}${path3.startsWith("/") ? path3 : `/${path3}`}`;
  };
  const normalizePath3 = (path3) =>
    (BASE_PATH2 && path3.startsWith(BASE_PATH2)
      ? path3.slice(BASE_PATH2.length)
      : path3
    ).replace(/\/+$/, "") || "/";
  const isNavActive2 = (pathname2, href) => {
    if (!href) return false;
    const current2 = normalizePath3(pathname2);
    const target = normalizePath3(href);
    return (
      current2 === target ||
      (target !== "/" && current2.startsWith(`${target}/`))
    );
  };
  const isCurrentPath2 = (pathname2, href) =>
    normalizePath3(pathname2) === normalizePath3(href);
  return {
    BASE_PATH: BASE_PATH2,
    imgWithBasePath: imgWithBasePath2,
    withBasePath: withBasePath2,
    FALLBACK_IMAGE: `${BASE_PATH2}/images/image-fallback.webp`,
    normalizePath: normalizePath3,
    isNavActive: isNavActive2,
    isCurrentPath: isCurrentPath2,
  };
}
var normalizeBasePath;
var init_path = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/static/path.js"() {
    normalizeBasePath = (basePath) => {
      if (!basePath || basePath === "/") return "";
      return `/${basePath.replace(/^\/+|\/+$/g, "")}`;
    };
  },
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/static/slugs.js
var landingPageSlugs, slugs;
var init_slugs = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/static/slugs.js"() {
    landingPageSlugs = {
      EN: {
        about: "about",
        blog: "blog",
        careers: "careers",
        catalogues: "catalogues",
        contact: "contact",
        gallery: "gallery",
        legal: "legal",
        portfolio: "portfolio",
        pricing: "pricing",
        services: "services",
        schedule: "schedule",
        events: "events",
        videos: "videos",
        faq: "faq",
      },
      SL: {
        about: "o-nas",
        blog: "blog",
        careers: "zaposlitev",
        catalogues: "katalogi",
        contact: "kontakt",
        gallery: "galerija",
        legal: "pravno",
        portfolio: "projekti",
        pricing: "cenik",
        services: "storitve",
        schedule: "urnik",
        events: "dogodki",
        videos: "videi",
        faq: "pogosta-vprasanja",
      },
      DE: {
        about: "uber-uns",
        blog: "blog",
        careers: "karriere",
        catalogues: "kataloge",
        contact: "kontakt",
        gallery: "galerie",
        legal: "rechtliches",
        portfolio: "portfolio",
        pricing: "preise",
        services: "dienstleistungen",
        schedule: "zeitplan",
        events: "veranstaltungen",
        videos: "videos",
        faq: "faq",
      },
      HR: {
        about: "o-nama",
        blog: "blog",
        careers: "karijera",
        catalogues: "katalozi",
        contact: "kontakt",
        gallery: "galerija",
        legal: "pravne-informacije",
        portfolio: "portfolio",
        pricing: "cjenik",
        services: "usluge",
        schedule: "raspored",
        events: "dogadaji",
        videos: "video",
        faq: "najcesca-pitanja",
      },
      IT: {
        about: "chi-siamo",
        blog: "blog",
        careers: "carriere",
        catalogues: "cataloghi",
        contact: "contatti",
        gallery: "galleria",
        legal: "note-legali",
        portfolio: "portfolio",
        pricing: "prezzi",
        services: "servizi",
        schedule: "orari",
        events: "eventi",
        videos: "video",
        faq: "domande-frequenti",
      },
    };
    slugs = landingPageSlugs;
  },
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/static/createLandingStatic.js
function createLandingStatic({ website, slugs: slugs3 = slugs, basePath }) {
  const path3 = createPathHelpers(basePath);
  const primaryLanguage2 = website.general.primaryLanguage;
  const supportedLanguages2 = Array.from(
    /* @__PURE__ */ new Set([
      primaryLanguage2,
      ...website.general.supportedLanguages,
    ]),
  );
  const getEnabledSections2 = () =>
    Object.keys(website.optionalSections).filter(
      (section) => website.optionalSections[section],
    );
  const isSectionEnabled2 = (section) =>
    Boolean(website.optionalSections[section]);
  const getHomeMeta2 = () => ({
    name: website.home.websiteName[primaryLanguage2] ?? "",
    slogan: website.home.slogan[primaryLanguage2] ?? "",
    logo: path3.imgWithBasePath(website.home.companyLogo.file),
    primaryLanguage: primaryLanguage2,
  });
  const getAllBlogSlugs2 = () => website.blog?.slugs ?? [];
  const getAllPortfolioSlugs2 = () => website.portfolio?.slugs ?? [];
  const getAllPricingSlugs2 = () => website.pricing?.slugs ?? [];
  const getAllCareerSlugs2 = () => website.careers?.slugs ?? [];
  const getAllEventSlugs2 = () => website.events?.slugs ?? [];
  const getSlugById = (values3, id) => {
    const index = id - 1;
    if (!values3 || index < 0 || index >= values3.length) return "";
    return values3[index];
  };
  const getBlogSlugById2 = (id) => getSlugById(website.blog?.slugs, id);
  const getPortfolioSlugById2 = (id) =>
    getSlugById(website.portfolio?.slugs, id);
  const getPricingSlugById2 = (id) => getSlugById(website.pricing?.slugs, id);
  const getCareerSlugById2 = (id) => getSlugById(website.careers?.slugs, id);
  const getEventSlugById2 = (id) => getSlugById(website.events?.slugs, id);
  const getPageSlugByKey2 = (pageKey) =>
    slugs3[primaryLanguage2][pageKey] ?? "";
  const getPageSlugByKeyWithBasePath2 = (pageKey) => {
    const slug = getPageSlugByKey2(pageKey);
    return path3.withBasePath(`/${slug}`);
  };
  return {
    ...path3,
    slugs: slugs3,
    primaryLanguage: primaryLanguage2,
    supportedLanguages: supportedLanguages2,
    getEnabledSections: getEnabledSections2,
    isSectionEnabled: isSectionEnabled2,
    getHomeMeta: getHomeMeta2,
    getAllBlogSlugs: getAllBlogSlugs2,
    getBlogSlugById: getBlogSlugById2,
    getAllPortfolioSlugs: getAllPortfolioSlugs2,
    getPortfolioSlugById: getPortfolioSlugById2,
    getAllPricingSlugs: getAllPricingSlugs2,
    getPricingSlugById: getPricingSlugById2,
    getAllCareerSlugs: getAllCareerSlugs2,
    getCareerSlugById: getCareerSlugById2,
    getAllEventSlugs: getAllEventSlugs2,
    getEventSlugById: getEventSlugById2,
    getPageSlugByKey: getPageSlugByKey2,
    getPageSlugByKeyWithBasePath: getPageSlugByKeyWithBasePath2,
  };
}
var init_createLandingStatic = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/static/createLandingStatic.js"() {
    init_path();
    init_slugs();
  },
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/static/index.js
var init_static = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/static/index.js"() {
    init_createLandingStatic();
    init_path();
    init_slugs();
  },
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/eventDate.js
var EVENT_LOCALE_BY_LANGUAGE,
  DATE_ONLY_PATTERN,
  TIME_PATTERN,
  MILLISECONDS_PER_DAY,
  parseEventDate,
  getEventDateTimestamp,
  formatEventDate,
  getLocalCalendarDayNumber,
  getRelativeEventDay;
var init_eventDate = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/eventDate.js"() {
    EVENT_LOCALE_BY_LANGUAGE = {
      SL: "sl-SI",
      HR: "hr-HR",
      EN: "en-GB",
      IT: "it-IT",
      DE: "de-DE",
    };
    DATE_ONLY_PATTERN = /^(\d{4})-(\d{2})-(\d{2})$/;
    TIME_PATTERN = /(?:T|\s)\d{2}:\d{2}/;
    MILLISECONDS_PER_DAY = 864e5;
    parseEventDate = (value) => {
      const normalizedValue = value.trim();
      if (!normalizedValue) {
        return null;
      }
      const dateOnlyMatch = normalizedValue.match(DATE_ONLY_PATTERN);
      if (dateOnlyMatch) {
        const [, yearValue, monthValue, dayValue] = dateOnlyMatch;
        const year = Number(yearValue);
        const month = Number(monthValue) - 1;
        const day = Number(dayValue);
        const parsedDate2 = new Date(year, month, day);
        if (
          parsedDate2.getFullYear() !== year ||
          parsedDate2.getMonth() !== month ||
          parsedDate2.getDate() !== day
        ) {
          return null;
        }
        return parsedDate2;
      }
      const parsedDate = new Date(normalizedValue);
      return Number.isNaN(parsedDate.getTime()) ? null : parsedDate;
    };
    getEventDateTimestamp = (value) => {
      const parsedDate = parseEventDate(value);
      return parsedDate ? parsedDate.getTime() : null;
    };
    formatEventDate = (value, lang, { includeWeekday = false } = {}) => {
      if (!value) {
        return "";
      }
      const parsedDate = parseEventDate(value);
      if (!parsedDate) {
        return value;
      }
      const options = {
        day: "numeric",
        month: "long",
        year: "numeric",
      };
      if (includeWeekday) {
        options.weekday = "long";
      }
      if (TIME_PATTERN.test(value)) {
        options.hour = "2-digit";
        options.minute = "2-digit";
      }
      return new Intl.DateTimeFormat(
        EVENT_LOCALE_BY_LANGUAGE[lang ?? "EN"],
        options,
      ).format(parsedDate);
    };
    getLocalCalendarDayNumber = (date) =>
      Date.UTC(date.getFullYear(), date.getMonth(), date.getDate());
    getRelativeEventDay = (value, now = /* @__PURE__ */ new Date()) => {
      const parsedDate = parseEventDate(value);
      if (!parsedDate) {
        return null;
      }
      const dayDifference = Math.round(
        (getLocalCalendarDayNumber(parsedDate) -
          getLocalCalendarDayNumber(now)) /
          MILLISECONDS_PER_DAY,
      );
      if (dayDifference === 0) {
        return "today";
      }
      if (dayDifference === 1) {
        return "tomorrow";
      }
      return null;
    };
  },
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/file.js
var init_file = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/file.js"() {},
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/format.js
function stripRichText(
  text,
  allowStyling = {
    bold: false,
    italic: false,
    underline: false,
    newLine: false,
  },
) {
  let result = text || "";
  if (!result) return "";
  result = result
    .replace(/<br\s*\/?>/gi, "\n")
    .replace(/<\/p>\s*<p>/gi, "\n\n")
    .replace(/<\/div>\s*<div>/gi, "\n\n")
    .replace(/<\/li>\s*<li>/gi, "\n")
    .replace(/<li>/gi, "\u2022 ")
    .replace(/<\/?(p|div|ul|ol|li|blockquote)>/gi, "");
  if (!allowStyling.bold) {
    result = result.replace(/<\/?(strong|b)>/gi, "");
  }
  if (!allowStyling.italic) {
    result = result.replace(/<\/?(em|i)>/gi, "");
  }
  if (!allowStyling.underline) {
    result = result.replace(/<\/?u>/gi, "");
  }
  result = result.replace(/<\/?[^>]+(>|$)/g, "");
  result = result
    .replace(/&nbsp;/gi, " ")
    .replace(/&amp;/gi, "&")
    .replace(/&lt;/gi, "<")
    .replace(/&gt;/gi, ">")
    .replace(/&quot;/gi, '"')
    .replace(/&#39;/gi, "'");
  if (!allowStyling.newLine) {
    result = result.replace(/\r?\n+/g, " ");
  }
  result = result.replace(/[ \t]{2,}/g, " ");
  result = result
    .replace(/ *\n */g, "\n")
    .replace(/\n{3,}/g, "\n\n")
    .trim();
  return result;
}
function formatGoogleMapsUrl(placeUrl) {
  try {
    const raw = placeUrl.split("/place/")[1];
    if (!raw) return placeUrl;
    const addressPart = raw.split("/")[0].split("?")[0];
    const decoded = decodeURIComponent(addressPart.replace(/\+/g, " "));
    const encoded = encodeURIComponent(decoded);
    return `https://www.google.com/maps?q=${encoded}&output=embed`;
  } catch {
    return placeUrl;
  }
}
var truncateWordSafe;
var init_format = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/format.js"() {
    truncateWordSafe = (text, limit) => {
      if (!limit || text.length <= limit) return text;
      const sliced = text.slice(0, limit);
      const lastSpace = sliced.lastIndexOf(" ");
      if (lastSpace === -1) return sliced + "...";
      return sliced.slice(0, lastSpace) + "...";
    };
  },
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/listFilters.js
function paginate(items, page, perPage) {
  const pageCount = Math.max(1, Math.ceil(items.length / perPage));
  const currentPage = Math.min(Math.max(page, 1), pageCount);
  const pageItems = items.slice(
    (currentPage - 1) * perPage,
    currentPage * perPage,
  );
  return { pageCount, currentPage, pageItems };
}
var normalizeSearchValue;
var init_listFilters = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/listFilters.js"() {
    normalizeSearchValue = (value) =>
      value
        .normalize("NFD")
        .replace(/[\u0300-\u036f]/g, "")
        .toLocaleLowerCase()
        .trim();
  },
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/publicApi.js
var PUBLIC_API_URL,
  SITE_UUID,
  DISABLE_API_CALLS,
  endpointMap,
  sanitizePathPart,
  getRequiredEnvValue,
  getPublicApiEndpoint,
  callPublicApi;
var init_publicApi = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/publicApi.js"() {
    PUBLIC_API_URL = process.env.NEXT_PUBLIC_PUBLIC_API_URL;
    SITE_UUID = process.env.NEXT_PUBLIC_SITE_UUID;
    DISABLE_API_CALLS = process.env.NEXT_PUBLIC_DISABLE_API_CALLS === "true";
    endpointMap = {
      contact: process.env.NEXT_PUBLIC_CONTACT_ENDPOINT,
      newsletter: process.env.NEXT_PUBLIC_NEWSLETTER_ENDPOINT,
    };
    sanitizePathPart = (value) => value.trim().replace(/^\/+|\/+$/g, "");
    getRequiredEnvValue = (value, variableName) => {
      const sanitizedValue = value ? sanitizePathPart(value) : "";
      if (!sanitizedValue) {
        throw new Error(`${variableName} is not configured.`);
      }
      return sanitizedValue;
    };
    getPublicApiEndpoint = (endpoint) => {
      const baseUrl = getRequiredEnvValue(
        PUBLIC_API_URL,
        "NEXT_PUBLIC_PUBLIC_API_URL",
      );
      const siteUuid = getRequiredEnvValue(SITE_UUID, "NEXT_PUBLIC_SITE_UUID");
      const endpointPath = getRequiredEnvValue(
        endpointMap[endpoint],
        endpoint === "contact"
          ? "NEXT_PUBLIC_CONTACT_ENDPOINT"
          : "NEXT_PUBLIC_NEWSLETTER_ENDPOINT",
      );
      return `${baseUrl}/${siteUuid}/${endpointPath}`;
    };
    callPublicApi = async (endpoint, options) => {
      if (DISABLE_API_CALLS) {
        return {
          success: true,
        };
      }
      const response = await fetch(getPublicApiEndpoint(endpoint), {
        method: options.method ?? "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: options.body === void 0 ? void 0 : JSON.stringify(options.body),
      });
      if (!response.ok) {
        throw new Error(`Public API request failed with ${response.status}.`);
      }
      return response.json();
    };
  },
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/video.js
async function getVideoThumbnail(url) {
  if (!url) return null;
  const ytId = extractYouTubeId(url);
  if (ytId) {
    return `https://img.youtube.com/vi/${ytId}/hqdefault.jpg`;
  }
  const vimeoId = extractVimeoId(url);
  if (vimeoId) {
    try {
      const response = await fetch(
        `https://vimeo.com/api/v2/video/${vimeoId}.json`,
      );
      const data = await response.json();
      return (
        data?.[0]?.thumbnail_large ||
        data?.[0]?.thumbnail_medium ||
        data?.[0]?.thumbnail_small ||
        null
      );
    } catch (err) {
      console.warn("Failed to fetch Vimeo thumbnail", err);
      return null;
    }
  }
  return null;
}
function extractYouTubeId(url) {
  const match2 =
    url.match(/v=([^&]+)/) ||
    url.match(/youtu\.be\/([^?]+)/) ||
    url.match(/youtube\.com\/embed\/([^?]+)/);
  return match2 ? match2[1] : null;
}
function extractVimeoId(url) {
  const match2 = url.match(/vimeo\.com\/(\d+)/);
  return match2 ? match2[1] : null;
}
var init_video = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/video.js"() {},
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/richText.js
var init_richText = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/richText.js"() {},
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/index.js
var init_utils = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/utils/index.js"() {
    init_eventDate();
    init_file();
    init_format();
    init_listFilters();
    init_publicApi();
    init_video();
    init_richText();
  },
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/localization.js
function getLocalized(value, lang, fallback = "EN") {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    return value;
  }
  const map = value;
  const safeLang = lang ?? fallback;
  const result = map[safeLang] ?? map[fallback];
  if (result === void 0) {
    return "";
  }
  return result;
}
var init_localization = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/localization.js"() {},
});

// node_modules/.pnpm/react@19.2.8/node_modules/react/cjs/react.production.js
var require_react_production = __commonJS({
  "node_modules/.pnpm/react@19.2.8/node_modules/react/cjs/react.production.js"(
    exports2,
  ) {
    "use strict";
    var REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element");
    var REACT_PORTAL_TYPE = Symbol.for("react.portal");
    var REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
    var REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode");
    var REACT_PROFILER_TYPE = Symbol.for("react.profiler");
    var REACT_CONSUMER_TYPE = Symbol.for("react.consumer");
    var REACT_CONTEXT_TYPE = Symbol.for("react.context");
    var REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref");
    var REACT_SUSPENSE_TYPE = Symbol.for("react.suspense");
    var REACT_MEMO_TYPE = Symbol.for("react.memo");
    var REACT_LAZY_TYPE = Symbol.for("react.lazy");
    var REACT_ACTIVITY_TYPE = Symbol.for("react.activity");
    var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
    function getIteratorFn(maybeIterable) {
      if (null === maybeIterable || "object" !== typeof maybeIterable)
        return null;
      maybeIterable =
        (MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL]) ||
        maybeIterable["@@iterator"];
      return "function" === typeof maybeIterable ? maybeIterable : null;
    }
    var ReactNoopUpdateQueue = {
      isMounted: function () {
        return false;
      },
      enqueueForceUpdate: function () {},
      enqueueReplaceState: function () {},
      enqueueSetState: function () {},
    };
    var assign2 = Object.assign;
    var emptyObject = {};
    function Component(props, context, updater) {
      this.props = props;
      this.context = context;
      this.refs = emptyObject;
      this.updater = updater || ReactNoopUpdateQueue;
    }
    Component.prototype.isReactComponent = {};
    Component.prototype.setState = function (partialState, callback) {
      if (
        "object" !== typeof partialState &&
        "function" !== typeof partialState &&
        null != partialState
      )
        throw Error(
          "takes an object of state variables to update or a function which returns an object of state variables.",
        );
      this.updater.enqueueSetState(this, partialState, callback, "setState");
    };
    Component.prototype.forceUpdate = function (callback) {
      this.updater.enqueueForceUpdate(this, callback, "forceUpdate");
    };
    function ComponentDummy() {}
    ComponentDummy.prototype = Component.prototype;
    function PureComponent(props, context, updater) {
      this.props = props;
      this.context = context;
      this.refs = emptyObject;
      this.updater = updater || ReactNoopUpdateQueue;
    }
    var pureComponentPrototype = (PureComponent.prototype =
      new ComponentDummy());
    pureComponentPrototype.constructor = PureComponent;
    assign2(pureComponentPrototype, Component.prototype);
    pureComponentPrototype.isPureReactComponent = true;
    var isArrayImpl = Array.isArray;
    function noop5() {}
    var ReactSharedInternals = { H: null, A: null, T: null, S: null };
    var hasOwnProperty = Object.prototype.hasOwnProperty;
    function ReactElement(type, key, props) {
      var refProp = props.ref;
      return {
        $$typeof: REACT_ELEMENT_TYPE,
        type,
        key,
        ref: void 0 !== refProp ? refProp : null,
        props,
      };
    }
    function cloneAndReplaceKey(oldElement, newKey) {
      return ReactElement(oldElement.type, newKey, oldElement.props);
    }
    function isValidElement10(object) {
      return (
        "object" === typeof object &&
        null !== object &&
        object.$$typeof === REACT_ELEMENT_TYPE
      );
    }
    function escape(key) {
      var escaperLookup = { "=": "=0", ":": "=2" };
      return (
        "$" +
        key.replace(/[=:]/g, function (match2) {
          return escaperLookup[match2];
        })
      );
    }
    var userProvidedKeyEscapeRegex = /\/+/g;
    function getElementKey(element, index) {
      return "object" === typeof element &&
        null !== element &&
        null != element.key
        ? escape("" + element.key)
        : index.toString(36);
    }
    function resolveThenable(thenable) {
      switch (thenable.status) {
        case "fulfilled":
          return thenable.value;
        case "rejected":
          throw thenable.reason;
        default:
          switch (
            ("string" === typeof thenable.status
              ? thenable.then(noop5, noop5)
              : ((thenable.status = "pending"),
                thenable.then(
                  function (fulfilledValue) {
                    "pending" === thenable.status &&
                      ((thenable.status = "fulfilled"),
                      (thenable.value = fulfilledValue));
                  },
                  function (error) {
                    "pending" === thenable.status &&
                      ((thenable.status = "rejected"),
                      (thenable.reason = error));
                  },
                )),
            thenable.status)
          ) {
            case "fulfilled":
              return thenable.value;
            case "rejected":
              throw thenable.reason;
          }
      }
      throw thenable;
    }
    function mapIntoArray(children, array, escapedPrefix, nameSoFar, callback) {
      var type = typeof children;
      if ("undefined" === type || "boolean" === type) children = null;
      var invokeCallback = false;
      if (null === children) invokeCallback = true;
      else
        switch (type) {
          case "bigint":
          case "string":
          case "number":
            invokeCallback = true;
            break;
          case "object":
            switch (children.$$typeof) {
              case REACT_ELEMENT_TYPE:
              case REACT_PORTAL_TYPE:
                invokeCallback = true;
                break;
              case REACT_LAZY_TYPE:
                return (
                  (invokeCallback = children._init),
                  mapIntoArray(
                    invokeCallback(children._payload),
                    array,
                    escapedPrefix,
                    nameSoFar,
                    callback,
                  )
                );
            }
        }
      if (invokeCallback)
        return (
          (callback = callback(children)),
          (invokeCallback =
            "" === nameSoFar ? "." + getElementKey(children, 0) : nameSoFar),
          isArrayImpl(callback)
            ? ((escapedPrefix = ""),
              null != invokeCallback &&
                (escapedPrefix =
                  invokeCallback.replace(userProvidedKeyEscapeRegex, "$&/") +
                  "/"),
              mapIntoArray(callback, array, escapedPrefix, "", function (c) {
                return c;
              }))
            : null != callback &&
              (isValidElement10(callback) &&
                (callback = cloneAndReplaceKey(
                  callback,
                  escapedPrefix +
                    (null == callback.key ||
                    (children && children.key === callback.key)
                      ? ""
                      : ("" + callback.key).replace(
                          userProvidedKeyEscapeRegex,
                          "$&/",
                        ) + "/") +
                    invokeCallback,
                )),
              array.push(callback)),
          1
        );
      invokeCallback = 0;
      var nextNamePrefix = "" === nameSoFar ? "." : nameSoFar + ":";
      if (isArrayImpl(children))
        for (var i = 0; i < children.length; i++)
          ((nameSoFar = children[i]),
            (type = nextNamePrefix + getElementKey(nameSoFar, i)),
            (invokeCallback += mapIntoArray(
              nameSoFar,
              array,
              escapedPrefix,
              type,
              callback,
            )));
      else if (((i = getIteratorFn(children)), "function" === typeof i))
        for (
          children = i.call(children), i = 0;
          !(nameSoFar = children.next()).done;
        )
          ((nameSoFar = nameSoFar.value),
            (type = nextNamePrefix + getElementKey(nameSoFar, i++)),
            (invokeCallback += mapIntoArray(
              nameSoFar,
              array,
              escapedPrefix,
              type,
              callback,
            )));
      else if ("object" === type) {
        if ("function" === typeof children.then)
          return mapIntoArray(
            resolveThenable(children),
            array,
            escapedPrefix,
            nameSoFar,
            callback,
          );
        array = String(children);
        throw Error(
          "Objects are not valid as a React child (found: " +
            ("[object Object]" === array
              ? "object with keys {" + Object.keys(children).join(", ") + "}"
              : array) +
            "). If you meant to render a collection of children, use an array instead.",
        );
      }
      return invokeCallback;
    }
    function mapChildren(children, func, context) {
      if (null == children) return children;
      var result = [],
        count = 0;
      mapIntoArray(children, result, "", "", function (child) {
        return func.call(context, child, count++);
      });
      return result;
    }
    function lazyInitializer(payload) {
      if (-1 === payload._status) {
        var ctor = payload._result;
        ctor = ctor();
        ctor.then(
          function (moduleObject) {
            if (0 === payload._status || -1 === payload._status)
              ((payload._status = 1), (payload._result = moduleObject));
          },
          function (error) {
            if (0 === payload._status || -1 === payload._status)
              ((payload._status = 2), (payload._result = error));
          },
        );
        -1 === payload._status &&
          ((payload._status = 0), (payload._result = ctor));
      }
      if (1 === payload._status) return payload._result.default;
      throw payload._result;
    }
    var reportGlobalError =
      "function" === typeof reportError
        ? reportError
        : function (error) {
            if (
              "object" === typeof window &&
              "function" === typeof window.ErrorEvent
            ) {
              var event = new window.ErrorEvent("error", {
                bubbles: true,
                cancelable: true,
                message:
                  "object" === typeof error &&
                  null !== error &&
                  "string" === typeof error.message
                    ? String(error.message)
                    : String(error),
                error,
              });
              if (!window.dispatchEvent(event)) return;
            } else if (
              "object" === typeof process &&
              "function" === typeof process.emit
            ) {
              process.emit("uncaughtException", error);
              return;
            }
            console.error(error);
          };
    var Children8 = {
      map: mapChildren,
      forEach: function (children, forEachFunc, forEachContext) {
        mapChildren(
          children,
          function () {
            forEachFunc.apply(this, arguments);
          },
          forEachContext,
        );
      },
      count: function (children) {
        var n = 0;
        mapChildren(children, function () {
          n++;
        });
        return n;
      },
      toArray: function (children) {
        return (
          mapChildren(children, function (child) {
            return child;
          }) || []
        );
      },
      only: function (children) {
        if (!isValidElement10(children))
          throw Error(
            "React.Children.only expected to receive a single React element child.",
          );
        return children;
      },
    };
    exports2.Activity = REACT_ACTIVITY_TYPE;
    exports2.Children = Children8;
    exports2.Component = Component;
    exports2.Fragment = REACT_FRAGMENT_TYPE;
    exports2.Profiler = REACT_PROFILER_TYPE;
    exports2.PureComponent = PureComponent;
    exports2.StrictMode = REACT_STRICT_MODE_TYPE;
    exports2.Suspense = REACT_SUSPENSE_TYPE;
    exports2.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE =
      ReactSharedInternals;
    exports2.__COMPILER_RUNTIME = {
      __proto__: null,
      c: function (size) {
        return ReactSharedInternals.H.useMemoCache(size);
      },
    };
    exports2.cache = function (fn2) {
      return function () {
        return fn2.apply(null, arguments);
      };
    };
    exports2.cacheSignal = function () {
      return null;
    };
    exports2.cloneElement = function (element, config, children) {
      if (null === element || void 0 === element)
        throw Error(
          "The argument must be a React element, but you passed " +
            element +
            ".",
        );
      var props = assign2({}, element.props),
        key = element.key;
      if (null != config)
        for (propName in (void 0 !== config.key && (key = "" + config.key),
        config))
          !hasOwnProperty.call(config, propName) ||
            "key" === propName ||
            "__self" === propName ||
            "__source" === propName ||
            ("ref" === propName && void 0 === config.ref) ||
            (props[propName] = config[propName]);
      var propName = arguments.length - 2;
      if (1 === propName) props.children = children;
      else if (1 < propName) {
        for (var childArray = Array(propName), i = 0; i < propName; i++)
          childArray[i] = arguments[i + 2];
        props.children = childArray;
      }
      return ReactElement(element.type, key, props);
    };
    exports2.createContext = function (defaultValue) {
      defaultValue = {
        $$typeof: REACT_CONTEXT_TYPE,
        _currentValue: defaultValue,
        _currentValue2: defaultValue,
        _threadCount: 0,
        Provider: null,
        Consumer: null,
      };
      defaultValue.Provider = defaultValue;
      defaultValue.Consumer = {
        $$typeof: REACT_CONSUMER_TYPE,
        _context: defaultValue,
      };
      return defaultValue;
    };
    exports2.createElement = function (type, config, children) {
      var propName,
        props = {},
        key = null;
      if (null != config)
        for (propName in (void 0 !== config.key && (key = "" + config.key),
        config))
          hasOwnProperty.call(config, propName) &&
            "key" !== propName &&
            "__self" !== propName &&
            "__source" !== propName &&
            (props[propName] = config[propName]);
      var childrenLength = arguments.length - 2;
      if (1 === childrenLength) props.children = children;
      else if (1 < childrenLength) {
        for (
          var childArray = Array(childrenLength), i = 0;
          i < childrenLength;
          i++
        )
          childArray[i] = arguments[i + 2];
        props.children = childArray;
      }
      if (type && type.defaultProps)
        for (propName in ((childrenLength = type.defaultProps), childrenLength))
          void 0 === props[propName] &&
            (props[propName] = childrenLength[propName]);
      return ReactElement(type, key, props);
    };
    exports2.createRef = function () {
      return { current: null };
    };
    exports2.forwardRef = function (render) {
      return { $$typeof: REACT_FORWARD_REF_TYPE, render };
    };
    exports2.isValidElement = isValidElement10;
    exports2.lazy = function (ctor) {
      return {
        $$typeof: REACT_LAZY_TYPE,
        _payload: { _status: -1, _result: ctor },
        _init: lazyInitializer,
      };
    };
    exports2.memo = function (type, compare) {
      return {
        $$typeof: REACT_MEMO_TYPE,
        type,
        compare: void 0 === compare ? null : compare,
      };
    };
    exports2.startTransition = function (scope) {
      var prevTransition = ReactSharedInternals.T,
        currentTransition = {};
      ReactSharedInternals.T = currentTransition;
      try {
        var returnValue = scope(),
          onStartTransitionFinish = ReactSharedInternals.S;
        null !== onStartTransitionFinish &&
          onStartTransitionFinish(currentTransition, returnValue);
        "object" === typeof returnValue &&
          null !== returnValue &&
          "function" === typeof returnValue.then &&
          returnValue.then(noop5, reportGlobalError);
      } catch (error) {
        reportGlobalError(error);
      } finally {
        (null !== prevTransition &&
          null !== currentTransition.types &&
          (prevTransition.types = currentTransition.types),
          (ReactSharedInternals.T = prevTransition));
      }
    };
    exports2.unstable_useCacheRefresh = function () {
      return ReactSharedInternals.H.useCacheRefresh();
    };
    exports2.use = function (usable) {
      return ReactSharedInternals.H.use(usable);
    };
    exports2.useActionState = function (action, initialState, permalink) {
      return ReactSharedInternals.H.useActionState(
        action,
        initialState,
        permalink,
      );
    };
    exports2.useCallback = function (callback, deps) {
      return ReactSharedInternals.H.useCallback(callback, deps);
    };
    exports2.useContext = function (Context) {
      return ReactSharedInternals.H.useContext(Context);
    };
    exports2.useDebugValue = function () {};
    exports2.useDeferredValue = function (value, initialValue) {
      return ReactSharedInternals.H.useDeferredValue(value, initialValue);
    };
    exports2.useEffect = function (create, deps) {
      return ReactSharedInternals.H.useEffect(create, deps);
    };
    exports2.useEffectEvent = function (callback) {
      return ReactSharedInternals.H.useEffectEvent(callback);
    };
    exports2.useId = function () {
      return ReactSharedInternals.H.useId();
    };
    exports2.useImperativeHandle = function (ref, create, deps) {
      return ReactSharedInternals.H.useImperativeHandle(ref, create, deps);
    };
    exports2.useInsertionEffect = function (create, deps) {
      return ReactSharedInternals.H.useInsertionEffect(create, deps);
    };
    exports2.useLayoutEffect = function (create, deps) {
      return ReactSharedInternals.H.useLayoutEffect(create, deps);
    };
    exports2.useMemo = function (create, deps) {
      return ReactSharedInternals.H.useMemo(create, deps);
    };
    exports2.useOptimistic = function (passthrough, reducer) {
      return ReactSharedInternals.H.useOptimistic(passthrough, reducer);
    };
    exports2.useReducer = function (reducer, initialArg, init) {
      return ReactSharedInternals.H.useReducer(reducer, initialArg, init);
    };
    exports2.useRef = function (initialValue) {
      return ReactSharedInternals.H.useRef(initialValue);
    };
    exports2.useState = function (initialState) {
      return ReactSharedInternals.H.useState(initialState);
    };
    exports2.useSyncExternalStore = function (
      subscribe,
      getSnapshot,
      getServerSnapshot,
    ) {
      return ReactSharedInternals.H.useSyncExternalStore(
        subscribe,
        getSnapshot,
        getServerSnapshot,
      );
    };
    exports2.useTransition = function () {
      return ReactSharedInternals.H.useTransition();
    };
    exports2.version = "19.2.8";
  },
});

// node_modules/.pnpm/react@19.2.8/node_modules/react/index.js
var require_react = __commonJS({
  "node_modules/.pnpm/react@19.2.8/node_modules/react/index.js"(
    exports2,
    module2,
  ) {
    "use strict";
    if (true) {
      module2.exports = require_react_production();
    } else {
      module2.exports = null;
    }
  },
});

// node_modules/.pnpm/react-dom@19.2.8_react@19.2.8/node_modules/react-dom/cjs/react-dom.production.js
var require_react_dom_production = __commonJS({
  "node_modules/.pnpm/react-dom@19.2.8_react@19.2.8/node_modules/react-dom/cjs/react-dom.production.js"(
    exports2,
  ) {
    "use strict";
    var React102 = require_react();
    function formatProdErrorMessage(code) {
      var url = "https://react.dev/errors/" + code;
      if (1 < arguments.length) {
        url += "?args[]=" + encodeURIComponent(arguments[1]);
        for (var i = 2; i < arguments.length; i++)
          url += "&args[]=" + encodeURIComponent(arguments[i]);
      }
      return (
        "Minified React error #" +
        code +
        "; visit " +
        url +
        " for the full message or use the non-minified dev environment for full errors and additional helpful warnings."
      );
    }
    function noop5() {}
    var Internals = {
      d: {
        f: noop5,
        r: function () {
          throw Error(formatProdErrorMessage(522));
        },
        D: noop5,
        C: noop5,
        L: noop5,
        m: noop5,
        X: noop5,
        S: noop5,
        M: noop5,
      },
      p: 0,
      findDOMNode: null,
    };
    var REACT_PORTAL_TYPE = Symbol.for("react.portal");
    function createPortal$1(children, containerInfo, implementation) {
      var key =
        3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : null;
      return {
        $$typeof: REACT_PORTAL_TYPE,
        key: null == key ? null : "" + key,
        children,
        containerInfo,
        implementation,
      };
    }
    var ReactSharedInternals =
      React102.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE;
    function getCrossOriginStringAs(as, input) {
      if ("font" === as) return "";
      if ("string" === typeof input)
        return "use-credentials" === input ? input : "";
    }
    exports2.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE =
      Internals;
    exports2.createPortal = function (children, container) {
      var key =
        2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : null;
      if (
        !container ||
        (1 !== container.nodeType &&
          9 !== container.nodeType &&
          11 !== container.nodeType)
      )
        throw Error(formatProdErrorMessage(299));
      return createPortal$1(children, container, null, key);
    };
    exports2.flushSync = function (fn2) {
      var previousTransition = ReactSharedInternals.T,
        previousUpdatePriority = Internals.p;
      try {
        if (((ReactSharedInternals.T = null), (Internals.p = 2), fn2))
          return fn2();
      } finally {
        ((ReactSharedInternals.T = previousTransition),
          (Internals.p = previousUpdatePriority),
          Internals.d.f());
      }
    };
    exports2.preconnect = function (href, options) {
      "string" === typeof href &&
        (options
          ? ((options = options.crossOrigin),
            (options =
              "string" === typeof options
                ? "use-credentials" === options
                  ? options
                  : ""
                : void 0))
          : (options = null),
        Internals.d.C(href, options));
    };
    exports2.prefetchDNS = function (href) {
      "string" === typeof href && Internals.d.D(href);
    };
    exports2.preinit = function (href, options) {
      if (
        "string" === typeof href &&
        options &&
        "string" === typeof options.as
      ) {
        var as = options.as,
          crossOrigin = getCrossOriginStringAs(as, options.crossOrigin),
          integrity =
            "string" === typeof options.integrity ? options.integrity : void 0,
          fetchPriority =
            "string" === typeof options.fetchPriority
              ? options.fetchPriority
              : void 0;
        "style" === as
          ? Internals.d.S(
              href,
              "string" === typeof options.precedence
                ? options.precedence
                : void 0,
              {
                crossOrigin,
                integrity,
                fetchPriority,
              },
            )
          : "script" === as &&
            Internals.d.X(href, {
              crossOrigin,
              integrity,
              fetchPriority,
              nonce: "string" === typeof options.nonce ? options.nonce : void 0,
            });
      }
    };
    exports2.preinitModule = function (href, options) {
      if ("string" === typeof href)
        if ("object" === typeof options && null !== options) {
          if (null == options.as || "script" === options.as) {
            var crossOrigin = getCrossOriginStringAs(
              options.as,
              options.crossOrigin,
            );
            Internals.d.M(href, {
              crossOrigin,
              integrity:
                "string" === typeof options.integrity
                  ? options.integrity
                  : void 0,
              nonce: "string" === typeof options.nonce ? options.nonce : void 0,
            });
          }
        } else null == options && Internals.d.M(href);
    };
    exports2.preload = function (href, options) {
      if (
        "string" === typeof href &&
        "object" === typeof options &&
        null !== options &&
        "string" === typeof options.as
      ) {
        var as = options.as,
          crossOrigin = getCrossOriginStringAs(as, options.crossOrigin);
        Internals.d.L(href, as, {
          crossOrigin,
          integrity:
            "string" === typeof options.integrity ? options.integrity : void 0,
          nonce: "string" === typeof options.nonce ? options.nonce : void 0,
          type: "string" === typeof options.type ? options.type : void 0,
          fetchPriority:
            "string" === typeof options.fetchPriority
              ? options.fetchPriority
              : void 0,
          referrerPolicy:
            "string" === typeof options.referrerPolicy
              ? options.referrerPolicy
              : void 0,
          imageSrcSet:
            "string" === typeof options.imageSrcSet
              ? options.imageSrcSet
              : void 0,
          imageSizes:
            "string" === typeof options.imageSizes
              ? options.imageSizes
              : void 0,
          media: "string" === typeof options.media ? options.media : void 0,
        });
      }
    };
    exports2.preloadModule = function (href, options) {
      if ("string" === typeof href)
        if (options) {
          var crossOrigin = getCrossOriginStringAs(
            options.as,
            options.crossOrigin,
          );
          Internals.d.m(href, {
            as:
              "string" === typeof options.as && "script" !== options.as
                ? options.as
                : void 0,
            crossOrigin,
            integrity:
              "string" === typeof options.integrity
                ? options.integrity
                : void 0,
          });
        } else Internals.d.m(href);
    };
    exports2.requestFormReset = function (form) {
      Internals.d.r(form);
    };
    exports2.unstable_batchedUpdates = function (fn2, a) {
      return fn2(a);
    };
    exports2.useFormState = function (action, initialState, permalink) {
      return ReactSharedInternals.H.useFormState(
        action,
        initialState,
        permalink,
      );
    };
    exports2.useFormStatus = function () {
      return ReactSharedInternals.H.useHostTransitionStatus();
    };
    exports2.version = "19.2.8";
  },
});

// node_modules/.pnpm/react-dom@19.2.8_react@19.2.8/node_modules/react-dom/index.js
var require_react_dom = __commonJS({
  "node_modules/.pnpm/react-dom@19.2.8_react@19.2.8/node_modules/react-dom/index.js"(
    exports2,
    module2,
  ) {
    "use strict";
    function checkDCE() {
      if (
        typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === "undefined" ||
        typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== "function"
      ) {
        return;
      }
      if (false) {
        throw new Error("^_^");
      }
      try {
        __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(checkDCE);
      } catch (err) {
        console.error(err);
      }
    }
    if (true) {
      checkDCE();
      module2.exports = require_react_dom_production();
    } else {
      module2.exports = null;
    }
  },
});

// node_modules/.pnpm/react-dom@19.2.8_react@19.2.8/node_modules/react-dom/cjs/react-dom-server-legacy.node.production.js
var require_react_dom_server_legacy_node_production = __commonJS({
  "node_modules/.pnpm/react-dom@19.2.8_react@19.2.8/node_modules/react-dom/cjs/react-dom-server-legacy.node.production.js"(
    exports2,
  ) {
    "use strict";
    var React102 = require_react();
    var ReactDOM3 = require_react_dom();
    var REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element");
    var REACT_PORTAL_TYPE = Symbol.for("react.portal");
    var REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
    var REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode");
    var REACT_PROFILER_TYPE = Symbol.for("react.profiler");
    var REACT_CONSUMER_TYPE = Symbol.for("react.consumer");
    var REACT_CONTEXT_TYPE = Symbol.for("react.context");
    var REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref");
    var REACT_SUSPENSE_TYPE = Symbol.for("react.suspense");
    var REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list");
    var REACT_MEMO_TYPE = Symbol.for("react.memo");
    var REACT_LAZY_TYPE = Symbol.for("react.lazy");
    var REACT_SCOPE_TYPE = Symbol.for("react.scope");
    var REACT_ACTIVITY_TYPE = Symbol.for("react.activity");
    var REACT_LEGACY_HIDDEN_TYPE = Symbol.for("react.legacy_hidden");
    var REACT_MEMO_CACHE_SENTINEL = Symbol.for("react.memo_cache_sentinel");
    var REACT_VIEW_TRANSITION_TYPE = Symbol.for("react.view_transition");
    var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
    function getIteratorFn(maybeIterable) {
      if (null === maybeIterable || "object" !== typeof maybeIterable)
        return null;
      maybeIterable =
        (MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL]) ||
        maybeIterable["@@iterator"];
      return "function" === typeof maybeIterable ? maybeIterable : null;
    }
    var isArrayImpl = Array.isArray;
    function murmurhash3_32_gc(key, seed) {
      var remainder = key.length & 3;
      var bytes = key.length - remainder;
      var h1 = seed;
      for (seed = 0; seed < bytes;) {
        var k1 =
          (key.charCodeAt(seed) & 255) |
          ((key.charCodeAt(++seed) & 255) << 8) |
          ((key.charCodeAt(++seed) & 255) << 16) |
          ((key.charCodeAt(++seed) & 255) << 24);
        ++seed;
        k1 =
          (3432918353 * (k1 & 65535) +
            (((3432918353 * (k1 >>> 16)) & 65535) << 16)) &
          4294967295;
        k1 = (k1 << 15) | (k1 >>> 17);
        k1 =
          (461845907 * (k1 & 65535) +
            (((461845907 * (k1 >>> 16)) & 65535) << 16)) &
          4294967295;
        h1 ^= k1;
        h1 = (h1 << 13) | (h1 >>> 19);
        h1 =
          (5 * (h1 & 65535) + (((5 * (h1 >>> 16)) & 65535) << 16)) & 4294967295;
        h1 = (h1 & 65535) + 27492 + ((((h1 >>> 16) + 58964) & 65535) << 16);
      }
      k1 = 0;
      switch (remainder) {
        case 3:
          k1 ^= (key.charCodeAt(seed + 2) & 255) << 16;
        case 2:
          k1 ^= (key.charCodeAt(seed + 1) & 255) << 8;
        case 1:
          ((k1 ^= key.charCodeAt(seed) & 255),
            (k1 =
              (3432918353 * (k1 & 65535) +
                (((3432918353 * (k1 >>> 16)) & 65535) << 16)) &
              4294967295),
            (k1 = (k1 << 15) | (k1 >>> 17)),
            (h1 ^=
              (461845907 * (k1 & 65535) +
                (((461845907 * (k1 >>> 16)) & 65535) << 16)) &
              4294967295));
      }
      h1 ^= key.length;
      h1 ^= h1 >>> 16;
      h1 =
        (2246822507 * (h1 & 65535) +
          (((2246822507 * (h1 >>> 16)) & 65535) << 16)) &
        4294967295;
      h1 ^= h1 >>> 13;
      h1 =
        (3266489909 * (h1 & 65535) +
          (((3266489909 * (h1 >>> 16)) & 65535) << 16)) &
        4294967295;
      return (h1 ^ (h1 >>> 16)) >>> 0;
    }
    var assign2 = Object.assign;
    var hasOwnProperty = Object.prototype.hasOwnProperty;
    var VALID_ATTRIBUTE_NAME_REGEX = RegExp(
      "^[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$",
    );
    var illegalAttributeNameCache = {};
    var validatedAttributeNameCache = {};
    function isAttributeNameSafe(attributeName) {
      if (hasOwnProperty.call(validatedAttributeNameCache, attributeName))
        return true;
      if (hasOwnProperty.call(illegalAttributeNameCache, attributeName))
        return false;
      if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName))
        return (validatedAttributeNameCache[attributeName] = true);
      illegalAttributeNameCache[attributeName] = true;
      return false;
    }
    var unitlessNumbers = new Set(
      "animationIterationCount aspectRatio borderImageOutset borderImageSlice borderImageWidth boxFlex boxFlexGroup boxOrdinalGroup columnCount columns flex flexGrow flexPositive flexShrink flexNegative flexOrder gridArea gridRow gridRowEnd gridRowSpan gridRowStart gridColumn gridColumnEnd gridColumnSpan gridColumnStart fontWeight lineClamp lineHeight opacity order orphans scale tabSize widows zIndex zoom fillOpacity floodOpacity stopOpacity strokeDasharray strokeDashoffset strokeMiterlimit strokeOpacity strokeWidth MozAnimationIterationCount MozBoxFlex MozBoxFlexGroup MozLineClamp msAnimationIterationCount msFlex msZoom msFlexGrow msFlexNegative msFlexOrder msFlexPositive msFlexShrink msGridColumn msGridColumnSpan msGridRow msGridRowSpan WebkitAnimationIterationCount WebkitBoxFlex WebKitBoxFlexGroup WebkitBoxOrdinalGroup WebkitColumnCount WebkitColumns WebkitFlex WebkitFlexGrow WebkitFlexPositive WebkitFlexShrink WebkitLineClamp".split(
        " ",
      ),
    );
    var aliases2 = /* @__PURE__ */ new Map([
      ["acceptCharset", "accept-charset"],
      ["htmlFor", "for"],
      ["httpEquiv", "http-equiv"],
      ["crossOrigin", "crossorigin"],
      ["accentHeight", "accent-height"],
      ["alignmentBaseline", "alignment-baseline"],
      ["arabicForm", "arabic-form"],
      ["baselineShift", "baseline-shift"],
      ["capHeight", "cap-height"],
      ["clipPath", "clip-path"],
      ["clipRule", "clip-rule"],
      ["colorInterpolation", "color-interpolation"],
      ["colorInterpolationFilters", "color-interpolation-filters"],
      ["colorProfile", "color-profile"],
      ["colorRendering", "color-rendering"],
      ["dominantBaseline", "dominant-baseline"],
      ["enableBackground", "enable-background"],
      ["fillOpacity", "fill-opacity"],
      ["fillRule", "fill-rule"],
      ["floodColor", "flood-color"],
      ["floodOpacity", "flood-opacity"],
      ["fontFamily", "font-family"],
      ["fontSize", "font-size"],
      ["fontSizeAdjust", "font-size-adjust"],
      ["fontStretch", "font-stretch"],
      ["fontStyle", "font-style"],
      ["fontVariant", "font-variant"],
      ["fontWeight", "font-weight"],
      ["glyphName", "glyph-name"],
      ["glyphOrientationHorizontal", "glyph-orientation-horizontal"],
      ["glyphOrientationVertical", "glyph-orientation-vertical"],
      ["horizAdvX", "horiz-adv-x"],
      ["horizOriginX", "horiz-origin-x"],
      ["imageRendering", "image-rendering"],
      ["letterSpacing", "letter-spacing"],
      ["lightingColor", "lighting-color"],
      ["markerEnd", "marker-end"],
      ["markerMid", "marker-mid"],
      ["markerStart", "marker-start"],
      ["overlinePosition", "overline-position"],
      ["overlineThickness", "overline-thickness"],
      ["paintOrder", "paint-order"],
      ["panose-1", "panose-1"],
      ["pointerEvents", "pointer-events"],
      ["renderingIntent", "rendering-intent"],
      ["shapeRendering", "shape-rendering"],
      ["stopColor", "stop-color"],
      ["stopOpacity", "stop-opacity"],
      ["strikethroughPosition", "strikethrough-position"],
      ["strikethroughThickness", "strikethrough-thickness"],
      ["strokeDasharray", "stroke-dasharray"],
      ["strokeDashoffset", "stroke-dashoffset"],
      ["strokeLinecap", "stroke-linecap"],
      ["strokeLinejoin", "stroke-linejoin"],
      ["strokeMiterlimit", "stroke-miterlimit"],
      ["strokeOpacity", "stroke-opacity"],
      ["strokeWidth", "stroke-width"],
      ["textAnchor", "text-anchor"],
      ["textDecoration", "text-decoration"],
      ["textRendering", "text-rendering"],
      ["transformOrigin", "transform-origin"],
      ["underlinePosition", "underline-position"],
      ["underlineThickness", "underline-thickness"],
      ["unicodeBidi", "unicode-bidi"],
      ["unicodeRange", "unicode-range"],
      ["unitsPerEm", "units-per-em"],
      ["vAlphabetic", "v-alphabetic"],
      ["vHanging", "v-hanging"],
      ["vIdeographic", "v-ideographic"],
      ["vMathematical", "v-mathematical"],
      ["vectorEffect", "vector-effect"],
      ["vertAdvY", "vert-adv-y"],
      ["vertOriginX", "vert-origin-x"],
      ["vertOriginY", "vert-origin-y"],
      ["wordSpacing", "word-spacing"],
      ["writingMode", "writing-mode"],
      ["xmlnsXlink", "xmlns:xlink"],
      ["xHeight", "x-height"],
    ]);
    var matchHtmlRegExp = /["'&<>]/;
    function escapeTextForBrowser(text) {
      if (
        "boolean" === typeof text ||
        "number" === typeof text ||
        "bigint" === typeof text
      )
        return "" + text;
      text = "" + text;
      var match2 = matchHtmlRegExp.exec(text);
      if (match2) {
        var html2 = "",
          index,
          lastIndex = 0;
        for (index = match2.index; index < text.length; index++) {
          switch (text.charCodeAt(index)) {
            case 34:
              match2 = "&quot;";
              break;
            case 38:
              match2 = "&amp;";
              break;
            case 39:
              match2 = "&#x27;";
              break;
            case 60:
              match2 = "&lt;";
              break;
            case 62:
              match2 = "&gt;";
              break;
            default:
              continue;
          }
          lastIndex !== index && (html2 += text.slice(lastIndex, index));
          lastIndex = index + 1;
          html2 += match2;
        }
        text =
          lastIndex !== index ? html2 + text.slice(lastIndex, index) : html2;
      }
      return text;
    }
    var uppercasePattern = /([A-Z])/g;
    var msPattern = /^ms-/;
    var isJavaScriptProtocol =
      /^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*:/i;
    function sanitizeURL(url) {
      return isJavaScriptProtocol.test("" + url)
        ? "javascript:throw new Error('React has blocked a javascript: URL as a security precaution.')"
        : url;
    }
    var ReactSharedInternals =
      React102.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE;
    var ReactDOMSharedInternals =
      ReactDOM3.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE;
    var sharedNotPendingObject = {
      pending: false,
      data: null,
      method: null,
      action: null,
    };
    var previousDispatcher = ReactDOMSharedInternals.d;
    ReactDOMSharedInternals.d = {
      f: previousDispatcher.f,
      r: previousDispatcher.r,
      D: prefetchDNS,
      C: preconnect,
      L: preload,
      m: preloadModule,
      X: preinitScript,
      S: preinitStyle,
      M: preinitModuleScript,
    };
    var PRELOAD_NO_CREDS = [];
    var currentlyFlushingRenderState = null;
    var scriptRegex = /(<\/|<)(s)(cript)/gi;
    function scriptReplacer(match2, prefix3, s, suffix2) {
      return "" + prefix3 + ("s" === s ? "\\u0073" : "\\u0053") + suffix2;
    }
    function createResumableState(
      identifierPrefix,
      externalRuntimeConfig,
      bootstrapScriptContent,
      bootstrapScripts,
      bootstrapModules,
    ) {
      return {
        idPrefix: void 0 === identifierPrefix ? "" : identifierPrefix,
        nextFormID: 0,
        streamingFormat: 0,
        bootstrapScriptContent,
        bootstrapScripts,
        bootstrapModules,
        instructions: 0,
        hasBody: false,
        hasHtml: false,
        unknownResources: {},
        dnsResources: {},
        connectResources: { default: {}, anonymous: {}, credentials: {} },
        imageResources: {},
        styleResources: {},
        scriptResources: {},
        moduleUnknownResources: {},
        moduleScriptResources: {},
      };
    }
    function createFormatContext(
      insertionMode,
      selectedValue,
      tagScope,
      viewTransition,
    ) {
      return {
        insertionMode,
        selectedValue,
        tagScope,
        viewTransition,
      };
    }
    function getChildFormatContext(parentContext, type, props) {
      var subtreeScope = parentContext.tagScope & -25;
      switch (type) {
        case "noscript":
          return createFormatContext(2, null, subtreeScope | 1, null);
        case "select":
          return createFormatContext(
            2,
            null != props.value ? props.value : props.defaultValue,
            subtreeScope,
            null,
          );
        case "svg":
          return createFormatContext(4, null, subtreeScope, null);
        case "picture":
          return createFormatContext(2, null, subtreeScope | 2, null);
        case "math":
          return createFormatContext(5, null, subtreeScope, null);
        case "foreignObject":
          return createFormatContext(2, null, subtreeScope, null);
        case "table":
          return createFormatContext(6, null, subtreeScope, null);
        case "thead":
        case "tbody":
        case "tfoot":
          return createFormatContext(7, null, subtreeScope, null);
        case "colgroup":
          return createFormatContext(9, null, subtreeScope, null);
        case "tr":
          return createFormatContext(8, null, subtreeScope, null);
        case "head":
          if (2 > parentContext.insertionMode)
            return createFormatContext(3, null, subtreeScope, null);
          break;
        case "html":
          if (0 === parentContext.insertionMode)
            return createFormatContext(1, null, subtreeScope, null);
      }
      return 6 <= parentContext.insertionMode || 2 > parentContext.insertionMode
        ? createFormatContext(2, null, subtreeScope, null)
        : parentContext.tagScope !== subtreeScope
          ? createFormatContext(
              parentContext.insertionMode,
              parentContext.selectedValue,
              subtreeScope,
              null,
            )
          : parentContext;
    }
    function getSuspenseViewTransition(parentViewTransition) {
      return null === parentViewTransition
        ? null
        : {
            update: parentViewTransition.update,
            enter: "none",
            exit: "none",
            share: parentViewTransition.update,
            name: parentViewTransition.autoName,
            autoName: parentViewTransition.autoName,
            nameIdx: 0,
          };
    }
    function getSuspenseFallbackFormatContext(resumableState, parentContext) {
      parentContext.tagScope & 32 && (resumableState.instructions |= 128);
      return createFormatContext(
        parentContext.insertionMode,
        parentContext.selectedValue,
        parentContext.tagScope | 12,
        getSuspenseViewTransition(parentContext.viewTransition),
      );
    }
    function getSuspenseContentFormatContext(resumableState, parentContext) {
      resumableState = getSuspenseViewTransition(parentContext.viewTransition);
      var subtreeScope = parentContext.tagScope | 16;
      null !== resumableState &&
        "none" !== resumableState.share &&
        (subtreeScope |= 64);
      return createFormatContext(
        parentContext.insertionMode,
        parentContext.selectedValue,
        subtreeScope,
        resumableState,
      );
    }
    var styleNameCache = /* @__PURE__ */ new Map();
    function pushStyleAttribute(target, style4) {
      if ("object" !== typeof style4)
        throw Error(
          "The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + 'em'}} when using JSX.",
        );
      var isFirst = true,
        styleName;
      for (styleName in style4)
        if (hasOwnProperty.call(style4, styleName)) {
          var styleValue = style4[styleName];
          if (
            null != styleValue &&
            "boolean" !== typeof styleValue &&
            "" !== styleValue
          ) {
            if (0 === styleName.indexOf("--")) {
              var nameChunk = escapeTextForBrowser(styleName);
              styleValue = escapeTextForBrowser(("" + styleValue).trim());
            } else
              ((nameChunk = styleNameCache.get(styleName)),
                void 0 === nameChunk &&
                  ((nameChunk = escapeTextForBrowser(
                    styleName
                      .replace(uppercasePattern, "-$1")
                      .toLowerCase()
                      .replace(msPattern, "-ms-"),
                  )),
                  styleNameCache.set(styleName, nameChunk)),
                (styleValue =
                  "number" === typeof styleValue
                    ? 0 === styleValue || unitlessNumbers.has(styleName)
                      ? "" + styleValue
                      : styleValue + "px"
                    : escapeTextForBrowser(("" + styleValue).trim())));
            isFirst
              ? ((isFirst = false),
                target.push(' style="', nameChunk, ":", styleValue))
              : target.push(";", nameChunk, ":", styleValue);
          }
        }
      isFirst || target.push('"');
    }
    function pushBooleanAttribute(target, name, value) {
      value &&
        "function" !== typeof value &&
        "symbol" !== typeof value &&
        target.push(" ", name, '=""');
    }
    function pushStringAttribute(target, name, value) {
      "function" !== typeof value &&
        "symbol" !== typeof value &&
        "boolean" !== typeof value &&
        target.push(" ", name, '="', escapeTextForBrowser(value), '"');
    }
    var actionJavaScriptURL = escapeTextForBrowser(
      "javascript:throw new Error('React form unexpectedly submitted.')",
    );
    function pushAdditionalFormField(value, key) {
      this.push('<input type="hidden"');
      validateAdditionalFormField(value);
      pushStringAttribute(this, "name", key);
      pushStringAttribute(this, "value", value);
      this.push("/>");
    }
    function validateAdditionalFormField(value) {
      if ("string" !== typeof value)
        throw Error(
          "File/Blob fields are not yet supported in progressive forms. Will fallback to client hydration.",
        );
    }
    function getCustomFormFields(resumableState, formAction) {
      if ("function" === typeof formAction.$$FORM_ACTION) {
        var id = resumableState.nextFormID++;
        resumableState = resumableState.idPrefix + id;
        try {
          var customFields = formAction.$$FORM_ACTION(resumableState);
          if (customFields) {
            var formData = customFields.data;
            null != formData && formData.forEach(validateAdditionalFormField);
          }
          return customFields;
        } catch (x) {
          if (
            "object" === typeof x &&
            null !== x &&
            "function" === typeof x.then
          )
            throw x;
        }
      }
      return null;
    }
    function pushFormActionAttribute(
      target,
      resumableState,
      renderState,
      formAction,
      formEncType,
      formMethod,
      formTarget,
      name,
    ) {
      var formData = null;
      if ("function" === typeof formAction) {
        var customFields = getCustomFormFields(resumableState, formAction);
        null !== customFields
          ? ((name = customFields.name),
            (formAction = customFields.action || ""),
            (formEncType = customFields.encType),
            (formMethod = customFields.method),
            (formTarget = customFields.target),
            (formData = customFields.data))
          : (target.push(" ", "formAction", '="', actionJavaScriptURL, '"'),
            (formTarget = formMethod = formEncType = formAction = name = null),
            injectFormReplayingRuntime(resumableState, renderState));
      }
      null != name && pushAttribute(target, "name", name);
      null != formAction && pushAttribute(target, "formAction", formAction);
      null != formEncType && pushAttribute(target, "formEncType", formEncType);
      null != formMethod && pushAttribute(target, "formMethod", formMethod);
      null != formTarget && pushAttribute(target, "formTarget", formTarget);
      return formData;
    }
    function pushAttribute(target, name, value) {
      switch (name) {
        case "className":
          pushStringAttribute(target, "class", value);
          break;
        case "tabIndex":
          pushStringAttribute(target, "tabindex", value);
          break;
        case "dir":
        case "role":
        case "viewBox":
        case "width":
        case "height":
          pushStringAttribute(target, name, value);
          break;
        case "style":
          pushStyleAttribute(target, value);
          break;
        case "src":
        case "href":
          if ("" === value) break;
        case "action":
        case "formAction":
          if (
            null == value ||
            "function" === typeof value ||
            "symbol" === typeof value ||
            "boolean" === typeof value
          )
            break;
          value = sanitizeURL("" + value);
          target.push(" ", name, '="', escapeTextForBrowser(value), '"');
          break;
        case "defaultValue":
        case "defaultChecked":
        case "innerHTML":
        case "suppressContentEditableWarning":
        case "suppressHydrationWarning":
        case "ref":
          break;
        case "autoFocus":
        case "multiple":
        case "muted":
          pushBooleanAttribute(target, name.toLowerCase(), value);
          break;
        case "xlinkHref":
          if (
            "function" === typeof value ||
            "symbol" === typeof value ||
            "boolean" === typeof value
          )
            break;
          value = sanitizeURL("" + value);
          target.push(
            " ",
            "xlink:href",
            '="',
            escapeTextForBrowser(value),
            '"',
          );
          break;
        case "contentEditable":
        case "spellCheck":
        case "draggable":
        case "value":
        case "autoReverse":
        case "externalResourcesRequired":
        case "focusable":
        case "preserveAlpha":
          "function" !== typeof value &&
            "symbol" !== typeof value &&
            target.push(" ", name, '="', escapeTextForBrowser(value), '"');
          break;
        case "inert":
        case "allowFullScreen":
        case "async":
        case "autoPlay":
        case "controls":
        case "default":
        case "defer":
        case "disabled":
        case "disablePictureInPicture":
        case "disableRemotePlayback":
        case "formNoValidate":
        case "hidden":
        case "loop":
        case "noModule":
        case "noValidate":
        case "open":
        case "playsInline":
        case "readOnly":
        case "required":
        case "reversed":
        case "scoped":
        case "seamless":
        case "itemScope":
          value &&
            "function" !== typeof value &&
            "symbol" !== typeof value &&
            target.push(" ", name, '=""');
          break;
        case "capture":
        case "download":
          true === value
            ? target.push(" ", name, '=""')
            : false !== value &&
              "function" !== typeof value &&
              "symbol" !== typeof value &&
              target.push(" ", name, '="', escapeTextForBrowser(value), '"');
          break;
        case "cols":
        case "rows":
        case "size":
        case "span":
          "function" !== typeof value &&
            "symbol" !== typeof value &&
            !isNaN(value) &&
            1 <= value &&
            target.push(" ", name, '="', escapeTextForBrowser(value), '"');
          break;
        case "rowSpan":
        case "start":
          "function" === typeof value ||
            "symbol" === typeof value ||
            isNaN(value) ||
            target.push(" ", name, '="', escapeTextForBrowser(value), '"');
          break;
        case "xlinkActuate":
          pushStringAttribute(target, "xlink:actuate", value);
          break;
        case "xlinkArcrole":
          pushStringAttribute(target, "xlink:arcrole", value);
          break;
        case "xlinkRole":
          pushStringAttribute(target, "xlink:role", value);
          break;
        case "xlinkShow":
          pushStringAttribute(target, "xlink:show", value);
          break;
        case "xlinkTitle":
          pushStringAttribute(target, "xlink:title", value);
          break;
        case "xlinkType":
          pushStringAttribute(target, "xlink:type", value);
          break;
        case "xmlBase":
          pushStringAttribute(target, "xml:base", value);
          break;
        case "xmlLang":
          pushStringAttribute(target, "xml:lang", value);
          break;
        case "xmlSpace":
          pushStringAttribute(target, "xml:space", value);
          break;
        default:
          if (
            !(2 < name.length) ||
            ("o" !== name[0] && "O" !== name[0]) ||
            ("n" !== name[1] && "N" !== name[1])
          ) {
            if (
              ((name = aliases2.get(name) || name), isAttributeNameSafe(name))
            ) {
              switch (typeof value) {
                case "function":
                case "symbol":
                  return;
                case "boolean":
                  var prefix$8 = name.toLowerCase().slice(0, 5);
                  if ("data-" !== prefix$8 && "aria-" !== prefix$8) return;
              }
              target.push(" ", name, '="', escapeTextForBrowser(value), '"');
            }
          }
      }
    }
    function pushInnerHTML(target, innerHTML, children) {
      if (null != innerHTML) {
        if (null != children)
          throw Error(
            "Can only set one of `children` or `props.dangerouslySetInnerHTML`.",
          );
        if ("object" !== typeof innerHTML || !("__html" in innerHTML))
          throw Error(
            "`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://react.dev/link/dangerously-set-inner-html for more information.",
          );
        innerHTML = innerHTML.__html;
        null !== innerHTML &&
          void 0 !== innerHTML &&
          target.push("" + innerHTML);
      }
    }
    function flattenOptionChildren(children) {
      var content = "";
      React102.Children.forEach(children, function (child) {
        null != child && (content += child);
      });
      return content;
    }
    function injectFormReplayingRuntime(resumableState, renderState) {
      if (0 === (resumableState.instructions & 16)) {
        resumableState.instructions |= 16;
        var preamble = renderState.preamble,
          bootstrapChunks = renderState.bootstrapChunks;
        (preamble.htmlChunks || preamble.headChunks) &&
        0 === bootstrapChunks.length
          ? (bootstrapChunks.push(renderState.startInlineScript),
            pushCompletedShellIdAttribute(bootstrapChunks, resumableState),
            bootstrapChunks.push(
              ">",
              `addEventListener("submit",function(a){if(!a.defaultPrevented){var c=a.target,d=a.submitter,e=c.action,b=d;if(d){var f=d.getAttribute("formAction");null!=f&&(e=f,b=null)}"javascript:throw new Error('React form unexpectedly submitted.')"===e&&(a.preventDefault(),b?(a=document.createElement("input"),a.name=b.name,a.value=b.value,b.parentNode.insertBefore(a,b),b=new FormData(c),a.parentNode.removeChild(a)):b=new FormData(c),a=c.ownerDocument||c,(a.$$reactFormReplay=a.$$reactFormReplay||[]).push(c,d,b))}});`,
              "</script>",
            ))
          : bootstrapChunks.unshift(
              renderState.startInlineScript,
              ">",
              `addEventListener("submit",function(a){if(!a.defaultPrevented){var c=a.target,d=a.submitter,e=c.action,b=d;if(d){var f=d.getAttribute("formAction");null!=f&&(e=f,b=null)}"javascript:throw new Error('React form unexpectedly submitted.')"===e&&(a.preventDefault(),b?(a=document.createElement("input"),a.name=b.name,a.value=b.value,b.parentNode.insertBefore(a,b),b=new FormData(c),a.parentNode.removeChild(a)):b=new FormData(c),a=c.ownerDocument||c,(a.$$reactFormReplay=a.$$reactFormReplay||[]).push(c,d,b))}});`,
              "</script>",
            );
      }
    }
    function pushLinkImpl(target, props) {
      target.push(startChunkForTag("link"));
      for (var propKey in props)
        if (hasOwnProperty.call(props, propKey)) {
          var propValue = props[propKey];
          if (null != propValue)
            switch (propKey) {
              case "children":
              case "dangerouslySetInnerHTML":
                throw Error(
                  "link is a self-closing tag and must neither have `children` nor use `dangerouslySetInnerHTML`.",
                );
              default:
                pushAttribute(target, propKey, propValue);
            }
        }
      target.push("/>");
      return null;
    }
    var styleRegex = /(<\/|<)(s)(tyle)/gi;
    function styleReplacer(match2, prefix3, s, suffix2) {
      return "" + prefix3 + ("s" === s ? "\\73 " : "\\53 ") + suffix2;
    }
    function pushSelfClosing(target, props, tag) {
      target.push(startChunkForTag(tag));
      for (var propKey in props)
        if (hasOwnProperty.call(props, propKey)) {
          var propValue = props[propKey];
          if (null != propValue)
            switch (propKey) {
              case "children":
              case "dangerouslySetInnerHTML":
                throw Error(
                  tag +
                    " is a self-closing tag and must neither have `children` nor use `dangerouslySetInnerHTML`.",
                );
              default:
                pushAttribute(target, propKey, propValue);
            }
        }
      target.push("/>");
      return null;
    }
    function pushTitleImpl(target, props) {
      target.push(startChunkForTag("title"));
      var children = null,
        innerHTML = null,
        propKey;
      for (propKey in props)
        if (hasOwnProperty.call(props, propKey)) {
          var propValue = props[propKey];
          if (null != propValue)
            switch (propKey) {
              case "children":
                children = propValue;
                break;
              case "dangerouslySetInnerHTML":
                innerHTML = propValue;
                break;
              default:
                pushAttribute(target, propKey, propValue);
            }
        }
      target.push(">");
      props = Array.isArray(children)
        ? 2 > children.length
          ? children[0]
          : null
        : children;
      "function" !== typeof props &&
        "symbol" !== typeof props &&
        null !== props &&
        void 0 !== props &&
        target.push(escapeTextForBrowser("" + props));
      pushInnerHTML(target, innerHTML, children);
      target.push(endChunkForTag("title"));
      return null;
    }
    function pushScriptImpl(target, props) {
      target.push(startChunkForTag("script"));
      var children = null,
        innerHTML = null,
        propKey;
      for (propKey in props)
        if (hasOwnProperty.call(props, propKey)) {
          var propValue = props[propKey];
          if (null != propValue)
            switch (propKey) {
              case "children":
                children = propValue;
                break;
              case "dangerouslySetInnerHTML":
                innerHTML = propValue;
                break;
              default:
                pushAttribute(target, propKey, propValue);
            }
        }
      target.push(">");
      pushInnerHTML(target, innerHTML, children);
      "string" === typeof children &&
        target.push(("" + children).replace(scriptRegex, scriptReplacer));
      target.push(endChunkForTag("script"));
      return null;
    }
    function pushStartSingletonElement(target, props, tag) {
      target.push(startChunkForTag(tag));
      var innerHTML = (tag = null),
        propKey;
      for (propKey in props)
        if (hasOwnProperty.call(props, propKey)) {
          var propValue = props[propKey];
          if (null != propValue)
            switch (propKey) {
              case "children":
                tag = propValue;
                break;
              case "dangerouslySetInnerHTML":
                innerHTML = propValue;
                break;
              default:
                pushAttribute(target, propKey, propValue);
            }
        }
      target.push(">");
      pushInnerHTML(target, innerHTML, tag);
      return tag;
    }
    function pushStartGenericElement(target, props, tag) {
      target.push(startChunkForTag(tag));
      var innerHTML = (tag = null),
        propKey;
      for (propKey in props)
        if (hasOwnProperty.call(props, propKey)) {
          var propValue = props[propKey];
          if (null != propValue)
            switch (propKey) {
              case "children":
                tag = propValue;
                break;
              case "dangerouslySetInnerHTML":
                innerHTML = propValue;
                break;
              default:
                pushAttribute(target, propKey, propValue);
            }
        }
      target.push(">");
      pushInnerHTML(target, innerHTML, tag);
      return "string" === typeof tag
        ? (target.push(escapeTextForBrowser(tag)), null)
        : tag;
    }
    var VALID_TAG_REGEX = /^[a-zA-Z][a-zA-Z:_\.\-\d]*$/;
    var validatedTagCache = /* @__PURE__ */ new Map();
    function startChunkForTag(tag) {
      var tagStartChunk = validatedTagCache.get(tag);
      if (void 0 === tagStartChunk) {
        if (!VALID_TAG_REGEX.test(tag)) throw Error("Invalid tag: " + tag);
        tagStartChunk = "<" + tag;
        validatedTagCache.set(tag, tagStartChunk);
      }
      return tagStartChunk;
    }
    function pushStartInstance(
      target$jscomp$0,
      type,
      props,
      resumableState,
      renderState,
      preambleState,
      hoistableState,
      formatContext,
      textEmbedded,
    ) {
      switch (type) {
        case "div":
        case "span":
        case "svg":
        case "path":
          break;
        case "a":
          target$jscomp$0.push(startChunkForTag("a"));
          var children = null,
            innerHTML = null,
            propKey;
          for (propKey in props)
            if (hasOwnProperty.call(props, propKey)) {
              var propValue = props[propKey];
              if (null != propValue)
                switch (propKey) {
                  case "children":
                    children = propValue;
                    break;
                  case "dangerouslySetInnerHTML":
                    innerHTML = propValue;
                    break;
                  case "href":
                    "" === propValue
                      ? pushStringAttribute(target$jscomp$0, "href", "")
                      : pushAttribute(target$jscomp$0, propKey, propValue);
                    break;
                  default:
                    pushAttribute(target$jscomp$0, propKey, propValue);
                }
            }
          target$jscomp$0.push(">");
          pushInnerHTML(target$jscomp$0, innerHTML, children);
          if ("string" === typeof children) {
            target$jscomp$0.push(escapeTextForBrowser(children));
            var JSCompiler_inline_result = null;
          } else JSCompiler_inline_result = children;
          return JSCompiler_inline_result;
        case "g":
        case "p":
        case "li":
          break;
        case "select":
          target$jscomp$0.push(startChunkForTag("select"));
          var children$jscomp$0 = null,
            innerHTML$jscomp$0 = null,
            propKey$jscomp$0;
          for (propKey$jscomp$0 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$0)) {
              var propValue$jscomp$0 = props[propKey$jscomp$0];
              if (null != propValue$jscomp$0)
                switch (propKey$jscomp$0) {
                  case "children":
                    children$jscomp$0 = propValue$jscomp$0;
                    break;
                  case "dangerouslySetInnerHTML":
                    innerHTML$jscomp$0 = propValue$jscomp$0;
                    break;
                  case "defaultValue":
                  case "value":
                    break;
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$0,
                      propValue$jscomp$0,
                    );
                }
            }
          target$jscomp$0.push(">");
          pushInnerHTML(target$jscomp$0, innerHTML$jscomp$0, children$jscomp$0);
          return children$jscomp$0;
        case "option":
          var selectedValue = formatContext.selectedValue;
          target$jscomp$0.push(startChunkForTag("option"));
          var children$jscomp$1 = null,
            value = null,
            selected = null,
            innerHTML$jscomp$1 = null,
            propKey$jscomp$1;
          for (propKey$jscomp$1 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$1)) {
              var propValue$jscomp$1 = props[propKey$jscomp$1];
              if (null != propValue$jscomp$1)
                switch (propKey$jscomp$1) {
                  case "children":
                    children$jscomp$1 = propValue$jscomp$1;
                    break;
                  case "selected":
                    selected = propValue$jscomp$1;
                    break;
                  case "dangerouslySetInnerHTML":
                    innerHTML$jscomp$1 = propValue$jscomp$1;
                    break;
                  case "value":
                    value = propValue$jscomp$1;
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$1,
                      propValue$jscomp$1,
                    );
                }
            }
          if (null != selectedValue) {
            var stringValue =
              null !== value
                ? "" + value
                : flattenOptionChildren(children$jscomp$1);
            if (isArrayImpl(selectedValue))
              for (var i = 0; i < selectedValue.length; i++) {
                if ("" + selectedValue[i] === stringValue) {
                  target$jscomp$0.push(' selected=""');
                  break;
                }
              }
            else
              "" + selectedValue === stringValue &&
                target$jscomp$0.push(' selected=""');
          } else selected && target$jscomp$0.push(' selected=""');
          target$jscomp$0.push(">");
          pushInnerHTML(target$jscomp$0, innerHTML$jscomp$1, children$jscomp$1);
          return children$jscomp$1;
        case "textarea":
          target$jscomp$0.push(startChunkForTag("textarea"));
          var value$jscomp$0 = null,
            defaultValue = null,
            children$jscomp$2 = null,
            propKey$jscomp$2;
          for (propKey$jscomp$2 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$2)) {
              var propValue$jscomp$2 = props[propKey$jscomp$2];
              if (null != propValue$jscomp$2)
                switch (propKey$jscomp$2) {
                  case "children":
                    children$jscomp$2 = propValue$jscomp$2;
                    break;
                  case "value":
                    value$jscomp$0 = propValue$jscomp$2;
                    break;
                  case "defaultValue":
                    defaultValue = propValue$jscomp$2;
                    break;
                  case "dangerouslySetInnerHTML":
                    throw Error(
                      "`dangerouslySetInnerHTML` does not make sense on <textarea>.",
                    );
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$2,
                      propValue$jscomp$2,
                    );
                }
            }
          null === value$jscomp$0 &&
            null !== defaultValue &&
            (value$jscomp$0 = defaultValue);
          target$jscomp$0.push(">");
          if (null != children$jscomp$2) {
            if (null != value$jscomp$0)
              throw Error(
                "If you supply `defaultValue` on a <textarea>, do not pass children.",
              );
            if (isArrayImpl(children$jscomp$2)) {
              if (1 < children$jscomp$2.length)
                throw Error("<textarea> can only have at most one child.");
              value$jscomp$0 = "" + children$jscomp$2[0];
            }
            value$jscomp$0 = "" + children$jscomp$2;
          }
          "string" === typeof value$jscomp$0 &&
            "\n" === value$jscomp$0[0] &&
            target$jscomp$0.push("\n");
          null !== value$jscomp$0 &&
            target$jscomp$0.push(escapeTextForBrowser("" + value$jscomp$0));
          return null;
        case "input":
          target$jscomp$0.push(startChunkForTag("input"));
          var name = null,
            formAction = null,
            formEncType = null,
            formMethod = null,
            formTarget = null,
            value$jscomp$1 = null,
            defaultValue$jscomp$0 = null,
            checked = null,
            defaultChecked = null,
            propKey$jscomp$3;
          for (propKey$jscomp$3 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$3)) {
              var propValue$jscomp$3 = props[propKey$jscomp$3];
              if (null != propValue$jscomp$3)
                switch (propKey$jscomp$3) {
                  case "children":
                  case "dangerouslySetInnerHTML":
                    throw Error(
                      "input is a self-closing tag and must neither have `children` nor use `dangerouslySetInnerHTML`.",
                    );
                  case "name":
                    name = propValue$jscomp$3;
                    break;
                  case "formAction":
                    formAction = propValue$jscomp$3;
                    break;
                  case "formEncType":
                    formEncType = propValue$jscomp$3;
                    break;
                  case "formMethod":
                    formMethod = propValue$jscomp$3;
                    break;
                  case "formTarget":
                    formTarget = propValue$jscomp$3;
                    break;
                  case "defaultChecked":
                    defaultChecked = propValue$jscomp$3;
                    break;
                  case "defaultValue":
                    defaultValue$jscomp$0 = propValue$jscomp$3;
                    break;
                  case "checked":
                    checked = propValue$jscomp$3;
                    break;
                  case "value":
                    value$jscomp$1 = propValue$jscomp$3;
                    break;
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$3,
                      propValue$jscomp$3,
                    );
                }
            }
          var formData = pushFormActionAttribute(
            target$jscomp$0,
            resumableState,
            renderState,
            formAction,
            formEncType,
            formMethod,
            formTarget,
            name,
          );
          null !== checked
            ? pushBooleanAttribute(target$jscomp$0, "checked", checked)
            : null !== defaultChecked &&
              pushBooleanAttribute(target$jscomp$0, "checked", defaultChecked);
          null !== value$jscomp$1
            ? pushAttribute(target$jscomp$0, "value", value$jscomp$1)
            : null !== defaultValue$jscomp$0 &&
              pushAttribute(target$jscomp$0, "value", defaultValue$jscomp$0);
          target$jscomp$0.push("/>");
          null != formData &&
            formData.forEach(pushAdditionalFormField, target$jscomp$0);
          return null;
        case "button":
          target$jscomp$0.push(startChunkForTag("button"));
          var children$jscomp$3 = null,
            innerHTML$jscomp$2 = null,
            name$jscomp$0 = null,
            formAction$jscomp$0 = null,
            formEncType$jscomp$0 = null,
            formMethod$jscomp$0 = null,
            formTarget$jscomp$0 = null,
            propKey$jscomp$4;
          for (propKey$jscomp$4 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$4)) {
              var propValue$jscomp$4 = props[propKey$jscomp$4];
              if (null != propValue$jscomp$4)
                switch (propKey$jscomp$4) {
                  case "children":
                    children$jscomp$3 = propValue$jscomp$4;
                    break;
                  case "dangerouslySetInnerHTML":
                    innerHTML$jscomp$2 = propValue$jscomp$4;
                    break;
                  case "name":
                    name$jscomp$0 = propValue$jscomp$4;
                    break;
                  case "formAction":
                    formAction$jscomp$0 = propValue$jscomp$4;
                    break;
                  case "formEncType":
                    formEncType$jscomp$0 = propValue$jscomp$4;
                    break;
                  case "formMethod":
                    formMethod$jscomp$0 = propValue$jscomp$4;
                    break;
                  case "formTarget":
                    formTarget$jscomp$0 = propValue$jscomp$4;
                    break;
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$4,
                      propValue$jscomp$4,
                    );
                }
            }
          var formData$jscomp$0 = pushFormActionAttribute(
            target$jscomp$0,
            resumableState,
            renderState,
            formAction$jscomp$0,
            formEncType$jscomp$0,
            formMethod$jscomp$0,
            formTarget$jscomp$0,
            name$jscomp$0,
          );
          target$jscomp$0.push(">");
          null != formData$jscomp$0 &&
            formData$jscomp$0.forEach(pushAdditionalFormField, target$jscomp$0);
          pushInnerHTML(target$jscomp$0, innerHTML$jscomp$2, children$jscomp$3);
          if ("string" === typeof children$jscomp$3) {
            target$jscomp$0.push(escapeTextForBrowser(children$jscomp$3));
            var JSCompiler_inline_result$jscomp$0 = null;
          } else JSCompiler_inline_result$jscomp$0 = children$jscomp$3;
          return JSCompiler_inline_result$jscomp$0;
        case "form":
          target$jscomp$0.push(startChunkForTag("form"));
          var children$jscomp$4 = null,
            innerHTML$jscomp$3 = null,
            formAction$jscomp$1 = null,
            formEncType$jscomp$1 = null,
            formMethod$jscomp$1 = null,
            formTarget$jscomp$1 = null,
            propKey$jscomp$5;
          for (propKey$jscomp$5 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$5)) {
              var propValue$jscomp$5 = props[propKey$jscomp$5];
              if (null != propValue$jscomp$5)
                switch (propKey$jscomp$5) {
                  case "children":
                    children$jscomp$4 = propValue$jscomp$5;
                    break;
                  case "dangerouslySetInnerHTML":
                    innerHTML$jscomp$3 = propValue$jscomp$5;
                    break;
                  case "action":
                    formAction$jscomp$1 = propValue$jscomp$5;
                    break;
                  case "encType":
                    formEncType$jscomp$1 = propValue$jscomp$5;
                    break;
                  case "method":
                    formMethod$jscomp$1 = propValue$jscomp$5;
                    break;
                  case "target":
                    formTarget$jscomp$1 = propValue$jscomp$5;
                    break;
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$5,
                      propValue$jscomp$5,
                    );
                }
            }
          var formData$jscomp$1 = null,
            formActionName = null;
          if ("function" === typeof formAction$jscomp$1) {
            var customFields = getCustomFormFields(
              resumableState,
              formAction$jscomp$1,
            );
            null !== customFields
              ? ((formAction$jscomp$1 = customFields.action || ""),
                (formEncType$jscomp$1 = customFields.encType),
                (formMethod$jscomp$1 = customFields.method),
                (formTarget$jscomp$1 = customFields.target),
                (formData$jscomp$1 = customFields.data),
                (formActionName = customFields.name))
              : (target$jscomp$0.push(
                  " ",
                  "action",
                  '="',
                  actionJavaScriptURL,
                  '"',
                ),
                (formTarget$jscomp$1 =
                  formMethod$jscomp$1 =
                  formEncType$jscomp$1 =
                  formAction$jscomp$1 =
                    null),
                injectFormReplayingRuntime(resumableState, renderState));
          }
          null != formAction$jscomp$1 &&
            pushAttribute(target$jscomp$0, "action", formAction$jscomp$1);
          null != formEncType$jscomp$1 &&
            pushAttribute(target$jscomp$0, "encType", formEncType$jscomp$1);
          null != formMethod$jscomp$1 &&
            pushAttribute(target$jscomp$0, "method", formMethod$jscomp$1);
          null != formTarget$jscomp$1 &&
            pushAttribute(target$jscomp$0, "target", formTarget$jscomp$1);
          target$jscomp$0.push(">");
          null !== formActionName &&
            (target$jscomp$0.push('<input type="hidden"'),
            pushStringAttribute(target$jscomp$0, "name", formActionName),
            target$jscomp$0.push("/>"),
            null != formData$jscomp$1 &&
              formData$jscomp$1.forEach(
                pushAdditionalFormField,
                target$jscomp$0,
              ));
          pushInnerHTML(target$jscomp$0, innerHTML$jscomp$3, children$jscomp$4);
          if ("string" === typeof children$jscomp$4) {
            target$jscomp$0.push(escapeTextForBrowser(children$jscomp$4));
            var JSCompiler_inline_result$jscomp$1 = null;
          } else JSCompiler_inline_result$jscomp$1 = children$jscomp$4;
          return JSCompiler_inline_result$jscomp$1;
        case "menuitem":
          target$jscomp$0.push(startChunkForTag("menuitem"));
          for (var propKey$jscomp$6 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$6)) {
              var propValue$jscomp$6 = props[propKey$jscomp$6];
              if (null != propValue$jscomp$6)
                switch (propKey$jscomp$6) {
                  case "children":
                  case "dangerouslySetInnerHTML":
                    throw Error(
                      "menuitems cannot have `children` nor `dangerouslySetInnerHTML`.",
                    );
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$6,
                      propValue$jscomp$6,
                    );
                }
            }
          target$jscomp$0.push(">");
          return null;
        case "object":
          target$jscomp$0.push(startChunkForTag("object"));
          var children$jscomp$5 = null,
            innerHTML$jscomp$4 = null,
            propKey$jscomp$7;
          for (propKey$jscomp$7 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$7)) {
              var propValue$jscomp$7 = props[propKey$jscomp$7];
              if (null != propValue$jscomp$7)
                switch (propKey$jscomp$7) {
                  case "children":
                    children$jscomp$5 = propValue$jscomp$7;
                    break;
                  case "dangerouslySetInnerHTML":
                    innerHTML$jscomp$4 = propValue$jscomp$7;
                    break;
                  case "data":
                    var sanitizedValue = sanitizeURL("" + propValue$jscomp$7);
                    if ("" === sanitizedValue) break;
                    target$jscomp$0.push(
                      " ",
                      "data",
                      '="',
                      escapeTextForBrowser(sanitizedValue),
                      '"',
                    );
                    break;
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$7,
                      propValue$jscomp$7,
                    );
                }
            }
          target$jscomp$0.push(">");
          pushInnerHTML(target$jscomp$0, innerHTML$jscomp$4, children$jscomp$5);
          if ("string" === typeof children$jscomp$5) {
            target$jscomp$0.push(escapeTextForBrowser(children$jscomp$5));
            var JSCompiler_inline_result$jscomp$2 = null;
          } else JSCompiler_inline_result$jscomp$2 = children$jscomp$5;
          return JSCompiler_inline_result$jscomp$2;
        case "title":
          var noscriptTagInScope = formatContext.tagScope & 1,
            isFallback = formatContext.tagScope & 4;
          if (
            4 === formatContext.insertionMode ||
            noscriptTagInScope ||
            null != props.itemProp
          )
            var JSCompiler_inline_result$jscomp$3 = pushTitleImpl(
              target$jscomp$0,
              props,
            );
          else
            isFallback
              ? (JSCompiler_inline_result$jscomp$3 = null)
              : (pushTitleImpl(renderState.hoistableChunks, props),
                (JSCompiler_inline_result$jscomp$3 = void 0));
          return JSCompiler_inline_result$jscomp$3;
        case "link":
          var noscriptTagInScope$jscomp$0 = formatContext.tagScope & 1,
            isFallback$jscomp$0 = formatContext.tagScope & 4,
            rel = props.rel,
            href = props.href,
            precedence = props.precedence;
          if (
            4 === formatContext.insertionMode ||
            noscriptTagInScope$jscomp$0 ||
            null != props.itemProp ||
            "string" !== typeof rel ||
            "string" !== typeof href ||
            "" === href
          ) {
            pushLinkImpl(target$jscomp$0, props);
            var JSCompiler_inline_result$jscomp$4 = null;
          } else if ("stylesheet" === props.rel)
            if (
              "string" !== typeof precedence ||
              null != props.disabled ||
              props.onLoad ||
              props.onError
            )
              JSCompiler_inline_result$jscomp$4 = pushLinkImpl(
                target$jscomp$0,
                props,
              );
            else {
              var styleQueue = renderState.styles.get(precedence),
                resourceState = resumableState.styleResources.hasOwnProperty(
                  href,
                )
                  ? resumableState.styleResources[href]
                  : void 0;
              if (null !== resourceState) {
                resumableState.styleResources[href] = null;
                styleQueue ||
                  ((styleQueue = {
                    precedence: escapeTextForBrowser(precedence),
                    rules: [],
                    hrefs: [],
                    sheets: /* @__PURE__ */ new Map(),
                  }),
                  renderState.styles.set(precedence, styleQueue));
                var resource = {
                  state: 0,
                  props: assign2({}, props, {
                    "data-precedence": props.precedence,
                    precedence: null,
                  }),
                };
                if (resourceState) {
                  2 === resourceState.length &&
                    adoptPreloadCredentials(resource.props, resourceState);
                  var preloadResource =
                    renderState.preloads.stylesheets.get(href);
                  preloadResource && 0 < preloadResource.length
                    ? (preloadResource.length = 0)
                    : (resource.state = 1);
                }
                styleQueue.sheets.set(href, resource);
                hoistableState && hoistableState.stylesheets.add(resource);
              } else if (styleQueue) {
                var resource$9 = styleQueue.sheets.get(href);
                resource$9 &&
                  hoistableState &&
                  hoistableState.stylesheets.add(resource$9);
              }
              textEmbedded && target$jscomp$0.push("<!-- -->");
              JSCompiler_inline_result$jscomp$4 = null;
            }
          else
            props.onLoad || props.onError
              ? (JSCompiler_inline_result$jscomp$4 = pushLinkImpl(
                  target$jscomp$0,
                  props,
                ))
              : (textEmbedded && target$jscomp$0.push("<!-- -->"),
                (JSCompiler_inline_result$jscomp$4 = isFallback$jscomp$0
                  ? null
                  : pushLinkImpl(renderState.hoistableChunks, props)));
          return JSCompiler_inline_result$jscomp$4;
        case "script":
          var noscriptTagInScope$jscomp$1 = formatContext.tagScope & 1,
            asyncProp = props.async;
          if (
            "string" !== typeof props.src ||
            !props.src ||
            !asyncProp ||
            "function" === typeof asyncProp ||
            "symbol" === typeof asyncProp ||
            props.onLoad ||
            props.onError ||
            4 === formatContext.insertionMode ||
            noscriptTagInScope$jscomp$1 ||
            null != props.itemProp
          )
            var JSCompiler_inline_result$jscomp$5 = pushScriptImpl(
              target$jscomp$0,
              props,
            );
          else {
            var key = props.src;
            if ("module" === props.type) {
              var resources = resumableState.moduleScriptResources;
              var preloads = renderState.preloads.moduleScripts;
            } else
              ((resources = resumableState.scriptResources),
                (preloads = renderState.preloads.scripts));
            var resourceState$jscomp$0 = resources.hasOwnProperty(key)
              ? resources[key]
              : void 0;
            if (null !== resourceState$jscomp$0) {
              resources[key] = null;
              var scriptProps = props;
              if (resourceState$jscomp$0) {
                2 === resourceState$jscomp$0.length &&
                  ((scriptProps = assign2({}, props)),
                  adoptPreloadCredentials(scriptProps, resourceState$jscomp$0));
                var preloadResource$jscomp$0 = preloads.get(key);
                preloadResource$jscomp$0 &&
                  (preloadResource$jscomp$0.length = 0);
              }
              var resource$jscomp$0 = [];
              renderState.scripts.add(resource$jscomp$0);
              pushScriptImpl(resource$jscomp$0, scriptProps);
            }
            textEmbedded && target$jscomp$0.push("<!-- -->");
            JSCompiler_inline_result$jscomp$5 = null;
          }
          return JSCompiler_inline_result$jscomp$5;
        case "style":
          var noscriptTagInScope$jscomp$2 = formatContext.tagScope & 1,
            precedence$jscomp$0 = props.precedence,
            href$jscomp$0 = props.href,
            nonce = props.nonce;
          if (
            4 === formatContext.insertionMode ||
            noscriptTagInScope$jscomp$2 ||
            null != props.itemProp ||
            "string" !== typeof precedence$jscomp$0 ||
            "string" !== typeof href$jscomp$0 ||
            "" === href$jscomp$0
          ) {
            target$jscomp$0.push(startChunkForTag("style"));
            var children$jscomp$6 = null,
              innerHTML$jscomp$5 = null,
              propKey$jscomp$8;
            for (propKey$jscomp$8 in props)
              if (hasOwnProperty.call(props, propKey$jscomp$8)) {
                var propValue$jscomp$8 = props[propKey$jscomp$8];
                if (null != propValue$jscomp$8)
                  switch (propKey$jscomp$8) {
                    case "children":
                      children$jscomp$6 = propValue$jscomp$8;
                      break;
                    case "dangerouslySetInnerHTML":
                      innerHTML$jscomp$5 = propValue$jscomp$8;
                      break;
                    default:
                      pushAttribute(
                        target$jscomp$0,
                        propKey$jscomp$8,
                        propValue$jscomp$8,
                      );
                  }
              }
            target$jscomp$0.push(">");
            var child = Array.isArray(children$jscomp$6)
              ? 2 > children$jscomp$6.length
                ? children$jscomp$6[0]
                : null
              : children$jscomp$6;
            "function" !== typeof child &&
              "symbol" !== typeof child &&
              null !== child &&
              void 0 !== child &&
              target$jscomp$0.push(
                ("" + child).replace(styleRegex, styleReplacer),
              );
            pushInnerHTML(
              target$jscomp$0,
              innerHTML$jscomp$5,
              children$jscomp$6,
            );
            target$jscomp$0.push(endChunkForTag("style"));
            var JSCompiler_inline_result$jscomp$6 = null;
          } else {
            var styleQueue$jscomp$0 =
              renderState.styles.get(precedence$jscomp$0);
            if (
              null !==
              (resumableState.styleResources.hasOwnProperty(href$jscomp$0)
                ? resumableState.styleResources[href$jscomp$0]
                : void 0)
            ) {
              resumableState.styleResources[href$jscomp$0] = null;
              styleQueue$jscomp$0 ||
                ((styleQueue$jscomp$0 = {
                  precedence: escapeTextForBrowser(precedence$jscomp$0),
                  rules: [],
                  hrefs: [],
                  sheets: /* @__PURE__ */ new Map(),
                }),
                renderState.styles.set(
                  precedence$jscomp$0,
                  styleQueue$jscomp$0,
                ));
              var nonceStyle = renderState.nonce.style;
              if (!nonceStyle || nonceStyle === nonce) {
                styleQueue$jscomp$0.hrefs.push(
                  escapeTextForBrowser(href$jscomp$0),
                );
                var target = styleQueue$jscomp$0.rules,
                  children$jscomp$7 = null,
                  innerHTML$jscomp$6 = null,
                  propKey$jscomp$9;
                for (propKey$jscomp$9 in props)
                  if (hasOwnProperty.call(props, propKey$jscomp$9)) {
                    var propValue$jscomp$9 = props[propKey$jscomp$9];
                    if (null != propValue$jscomp$9)
                      switch (propKey$jscomp$9) {
                        case "children":
                          children$jscomp$7 = propValue$jscomp$9;
                          break;
                        case "dangerouslySetInnerHTML":
                          innerHTML$jscomp$6 = propValue$jscomp$9;
                      }
                  }
                var child$jscomp$0 = Array.isArray(children$jscomp$7)
                  ? 2 > children$jscomp$7.length
                    ? children$jscomp$7[0]
                    : null
                  : children$jscomp$7;
                "function" !== typeof child$jscomp$0 &&
                  "symbol" !== typeof child$jscomp$0 &&
                  null !== child$jscomp$0 &&
                  void 0 !== child$jscomp$0 &&
                  target.push(
                    ("" + child$jscomp$0).replace(styleRegex, styleReplacer),
                  );
                pushInnerHTML(target, innerHTML$jscomp$6, children$jscomp$7);
              }
            }
            styleQueue$jscomp$0 &&
              hoistableState &&
              hoistableState.styles.add(styleQueue$jscomp$0);
            textEmbedded && target$jscomp$0.push("<!-- -->");
            JSCompiler_inline_result$jscomp$6 = void 0;
          }
          return JSCompiler_inline_result$jscomp$6;
        case "meta":
          var noscriptTagInScope$jscomp$3 = formatContext.tagScope & 1,
            isFallback$jscomp$1 = formatContext.tagScope & 4;
          if (
            4 === formatContext.insertionMode ||
            noscriptTagInScope$jscomp$3 ||
            null != props.itemProp
          )
            var JSCompiler_inline_result$jscomp$7 = pushSelfClosing(
              target$jscomp$0,
              props,
              "meta",
            );
          else
            (textEmbedded && target$jscomp$0.push("<!-- -->"),
              (JSCompiler_inline_result$jscomp$7 = isFallback$jscomp$1
                ? null
                : "string" === typeof props.charSet
                  ? pushSelfClosing(renderState.charsetChunks, props, "meta")
                  : "viewport" === props.name
                    ? pushSelfClosing(renderState.viewportChunks, props, "meta")
                    : pushSelfClosing(
                        renderState.hoistableChunks,
                        props,
                        "meta",
                      )));
          return JSCompiler_inline_result$jscomp$7;
        case "listing":
        case "pre":
          target$jscomp$0.push(startChunkForTag(type));
          var children$jscomp$8 = null,
            innerHTML$jscomp$7 = null,
            propKey$jscomp$10;
          for (propKey$jscomp$10 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$10)) {
              var propValue$jscomp$10 = props[propKey$jscomp$10];
              if (null != propValue$jscomp$10)
                switch (propKey$jscomp$10) {
                  case "children":
                    children$jscomp$8 = propValue$jscomp$10;
                    break;
                  case "dangerouslySetInnerHTML":
                    innerHTML$jscomp$7 = propValue$jscomp$10;
                    break;
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$10,
                      propValue$jscomp$10,
                    );
                }
            }
          target$jscomp$0.push(">");
          if (null != innerHTML$jscomp$7) {
            if (null != children$jscomp$8)
              throw Error(
                "Can only set one of `children` or `props.dangerouslySetInnerHTML`.",
              );
            if (
              "object" !== typeof innerHTML$jscomp$7 ||
              !("__html" in innerHTML$jscomp$7)
            )
              throw Error(
                "`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://react.dev/link/dangerously-set-inner-html for more information.",
              );
            var html2 = innerHTML$jscomp$7.__html;
            null !== html2 &&
              void 0 !== html2 &&
              ("string" === typeof html2 &&
              0 < html2.length &&
              "\n" === html2[0]
                ? target$jscomp$0.push("\n", html2)
                : target$jscomp$0.push("" + html2));
          }
          "string" === typeof children$jscomp$8 &&
            "\n" === children$jscomp$8[0] &&
            target$jscomp$0.push("\n");
          return children$jscomp$8;
        case "img":
          var pictureOrNoScriptTagInScope = formatContext.tagScope & 3,
            src = props.src,
            srcSet = props.srcSet;
          if (
            !(
              "lazy" === props.loading ||
              (!src && !srcSet) ||
              ("string" !== typeof src && null != src) ||
              ("string" !== typeof srcSet && null != srcSet) ||
              "low" === props.fetchPriority ||
              pictureOrNoScriptTagInScope
            ) &&
            ("string" !== typeof src ||
              ":" !== src[4] ||
              ("d" !== src[0] && "D" !== src[0]) ||
              ("a" !== src[1] && "A" !== src[1]) ||
              ("t" !== src[2] && "T" !== src[2]) ||
              ("a" !== src[3] && "A" !== src[3])) &&
            ("string" !== typeof srcSet ||
              ":" !== srcSet[4] ||
              ("d" !== srcSet[0] && "D" !== srcSet[0]) ||
              ("a" !== srcSet[1] && "A" !== srcSet[1]) ||
              ("t" !== srcSet[2] && "T" !== srcSet[2]) ||
              ("a" !== srcSet[3] && "A" !== srcSet[3]))
          ) {
            null !== hoistableState &&
              formatContext.tagScope & 64 &&
              (hoistableState.suspenseyImages = true);
            var sizes = "string" === typeof props.sizes ? props.sizes : void 0,
              key$jscomp$0 = srcSet ? srcSet + "\n" + (sizes || "") : src,
              promotablePreloads = renderState.preloads.images,
              resource$jscomp$1 = promotablePreloads.get(key$jscomp$0);
            if (resource$jscomp$1) {
              if (
                "high" === props.fetchPriority ||
                10 > renderState.highImagePreloads.size
              )
                (promotablePreloads.delete(key$jscomp$0),
                  renderState.highImagePreloads.add(resource$jscomp$1));
            } else if (
              !resumableState.imageResources.hasOwnProperty(key$jscomp$0)
            ) {
              resumableState.imageResources[key$jscomp$0] = PRELOAD_NO_CREDS;
              var input = props.crossOrigin;
              var JSCompiler_inline_result$jscomp$8 =
                "string" === typeof input
                  ? "use-credentials" === input
                    ? input
                    : ""
                  : void 0;
              var headers = renderState.headers,
                header;
              headers &&
              0 < headers.remainingCapacity &&
              "string" !== typeof props.srcSet &&
              ("high" === props.fetchPriority ||
                500 > headers.highImagePreloads.length) &&
              ((header = getPreloadAsHeader(src, "image", {
                imageSrcSet: props.srcSet,
                imageSizes: props.sizes,
                crossOrigin: JSCompiler_inline_result$jscomp$8,
                integrity: props.integrity,
                nonce: props.nonce,
                type: props.type,
                fetchPriority: props.fetchPriority,
                referrerPolicy: props.refererPolicy,
              })),
              0 <= (headers.remainingCapacity -= header.length + 2))
                ? ((renderState.resets.image[key$jscomp$0] = PRELOAD_NO_CREDS),
                  headers.highImagePreloads &&
                    (headers.highImagePreloads += ", "),
                  (headers.highImagePreloads += header))
                : ((resource$jscomp$1 = []),
                  pushLinkImpl(resource$jscomp$1, {
                    rel: "preload",
                    as: "image",
                    href: srcSet ? void 0 : src,
                    imageSrcSet: srcSet,
                    imageSizes: sizes,
                    crossOrigin: JSCompiler_inline_result$jscomp$8,
                    integrity: props.integrity,
                    type: props.type,
                    fetchPriority: props.fetchPriority,
                    referrerPolicy: props.referrerPolicy,
                  }),
                  "high" === props.fetchPriority ||
                  10 > renderState.highImagePreloads.size
                    ? renderState.highImagePreloads.add(resource$jscomp$1)
                    : (renderState.bulkPreloads.add(resource$jscomp$1),
                      promotablePreloads.set(key$jscomp$0, resource$jscomp$1)));
            }
          }
          return pushSelfClosing(target$jscomp$0, props, "img");
        case "base":
        case "area":
        case "br":
        case "col":
        case "embed":
        case "hr":
        case "keygen":
        case "param":
        case "source":
        case "track":
        case "wbr":
          return pushSelfClosing(target$jscomp$0, props, type);
        case "annotation-xml":
        case "color-profile":
        case "font-face":
        case "font-face-src":
        case "font-face-uri":
        case "font-face-format":
        case "font-face-name":
        case "missing-glyph":
          break;
        case "head":
          if (2 > formatContext.insertionMode) {
            var preamble = preambleState || renderState.preamble;
            if (preamble.headChunks)
              throw Error("The `<head>` tag may only be rendered once.");
            null !== preambleState && target$jscomp$0.push("<!--head-->");
            preamble.headChunks = [];
            var JSCompiler_inline_result$jscomp$9 = pushStartSingletonElement(
              preamble.headChunks,
              props,
              "head",
            );
          } else
            JSCompiler_inline_result$jscomp$9 = pushStartGenericElement(
              target$jscomp$0,
              props,
              "head",
            );
          return JSCompiler_inline_result$jscomp$9;
        case "body":
          if (2 > formatContext.insertionMode) {
            var preamble$jscomp$0 = preambleState || renderState.preamble;
            if (preamble$jscomp$0.bodyChunks)
              throw Error("The `<body>` tag may only be rendered once.");
            null !== preambleState && target$jscomp$0.push("<!--body-->");
            preamble$jscomp$0.bodyChunks = [];
            var JSCompiler_inline_result$jscomp$10 = pushStartSingletonElement(
              preamble$jscomp$0.bodyChunks,
              props,
              "body",
            );
          } else
            JSCompiler_inline_result$jscomp$10 = pushStartGenericElement(
              target$jscomp$0,
              props,
              "body",
            );
          return JSCompiler_inline_result$jscomp$10;
        case "html":
          if (0 === formatContext.insertionMode) {
            var preamble$jscomp$1 = preambleState || renderState.preamble;
            if (preamble$jscomp$1.htmlChunks)
              throw Error("The `<html>` tag may only be rendered once.");
            null !== preambleState && target$jscomp$0.push("<!--html-->");
            preamble$jscomp$1.htmlChunks = [""];
            var JSCompiler_inline_result$jscomp$11 = pushStartSingletonElement(
              preamble$jscomp$1.htmlChunks,
              props,
              "html",
            );
          } else
            JSCompiler_inline_result$jscomp$11 = pushStartGenericElement(
              target$jscomp$0,
              props,
              "html",
            );
          return JSCompiler_inline_result$jscomp$11;
        default:
          if (-1 !== type.indexOf("-")) {
            target$jscomp$0.push(startChunkForTag(type));
            var children$jscomp$9 = null,
              innerHTML$jscomp$8 = null,
              propKey$jscomp$11;
            for (propKey$jscomp$11 in props)
              if (hasOwnProperty.call(props, propKey$jscomp$11)) {
                var propValue$jscomp$11 = props[propKey$jscomp$11];
                if (null != propValue$jscomp$11) {
                  var attributeName = propKey$jscomp$11;
                  switch (propKey$jscomp$11) {
                    case "children":
                      children$jscomp$9 = propValue$jscomp$11;
                      break;
                    case "dangerouslySetInnerHTML":
                      innerHTML$jscomp$8 = propValue$jscomp$11;
                      break;
                    case "style":
                      pushStyleAttribute(target$jscomp$0, propValue$jscomp$11);
                      break;
                    case "suppressContentEditableWarning":
                    case "suppressHydrationWarning":
                    case "ref":
                      break;
                    case "className":
                      attributeName = "class";
                    default:
                      if (
                        isAttributeNameSafe(propKey$jscomp$11) &&
                        "function" !== typeof propValue$jscomp$11 &&
                        "symbol" !== typeof propValue$jscomp$11 &&
                        false !== propValue$jscomp$11
                      ) {
                        if (true === propValue$jscomp$11)
                          propValue$jscomp$11 = "";
                        else if ("object" === typeof propValue$jscomp$11)
                          continue;
                        target$jscomp$0.push(
                          " ",
                          attributeName,
                          '="',
                          escapeTextForBrowser(propValue$jscomp$11),
                          '"',
                        );
                      }
                  }
                }
              }
            target$jscomp$0.push(">");
            pushInnerHTML(
              target$jscomp$0,
              innerHTML$jscomp$8,
              children$jscomp$9,
            );
            return children$jscomp$9;
          }
      }
      return pushStartGenericElement(target$jscomp$0, props, type);
    }
    var endTagCache = /* @__PURE__ */ new Map();
    function endChunkForTag(tag) {
      var chunk = endTagCache.get(tag);
      void 0 === chunk &&
        ((chunk = "</" + tag + ">"), endTagCache.set(tag, chunk));
      return chunk;
    }
    function hoistPreambleState(renderState, preambleState) {
      renderState = renderState.preamble;
      null === renderState.htmlChunks &&
        preambleState.htmlChunks &&
        (renderState.htmlChunks = preambleState.htmlChunks);
      null === renderState.headChunks &&
        preambleState.headChunks &&
        (renderState.headChunks = preambleState.headChunks);
      null === renderState.bodyChunks &&
        preambleState.bodyChunks &&
        (renderState.bodyChunks = preambleState.bodyChunks);
    }
    function writeBootstrap(destination, renderState) {
      renderState = renderState.bootstrapChunks;
      for (var i = 0; i < renderState.length - 1; i++)
        destination.push(renderState[i]);
      return i < renderState.length
        ? ((i = renderState[i]), (renderState.length = 0), destination.push(i))
        : true;
    }
    function writeStartPendingSuspenseBoundary(destination, renderState, id) {
      destination.push('<!--$?--><template id="');
      if (null === id)
        throw Error(
          "An ID must have been assigned before we can complete the boundary.",
        );
      destination.push(renderState.boundaryPrefix);
      renderState = id.toString(16);
      destination.push(renderState);
      return destination.push('"></template>');
    }
    function writeStartSegment(destination, renderState, formatContext, id) {
      switch (formatContext.insertionMode) {
        case 0:
        case 1:
        case 3:
        case 2:
          return (
            destination.push('<div hidden id="'),
            destination.push(renderState.segmentPrefix),
            (renderState = id.toString(16)),
            destination.push(renderState),
            destination.push('">')
          );
        case 4:
          return (
            destination.push(
              '<svg aria-hidden="true" style="display:none" id="',
            ),
            destination.push(renderState.segmentPrefix),
            (renderState = id.toString(16)),
            destination.push(renderState),
            destination.push('">')
          );
        case 5:
          return (
            destination.push(
              '<math aria-hidden="true" style="display:none" id="',
            ),
            destination.push(renderState.segmentPrefix),
            (renderState = id.toString(16)),
            destination.push(renderState),
            destination.push('">')
          );
        case 6:
          return (
            destination.push('<table hidden id="'),
            destination.push(renderState.segmentPrefix),
            (renderState = id.toString(16)),
            destination.push(renderState),
            destination.push('">')
          );
        case 7:
          return (
            destination.push('<table hidden><tbody id="'),
            destination.push(renderState.segmentPrefix),
            (renderState = id.toString(16)),
            destination.push(renderState),
            destination.push('">')
          );
        case 8:
          return (
            destination.push('<table hidden><tr id="'),
            destination.push(renderState.segmentPrefix),
            (renderState = id.toString(16)),
            destination.push(renderState),
            destination.push('">')
          );
        case 9:
          return (
            destination.push('<table hidden><colgroup id="'),
            destination.push(renderState.segmentPrefix),
            (renderState = id.toString(16)),
            destination.push(renderState),
            destination.push('">')
          );
        default:
          throw Error("Unknown insertion mode. This is a bug in React.");
      }
    }
    function writeEndSegment(destination, formatContext) {
      switch (formatContext.insertionMode) {
        case 0:
        case 1:
        case 3:
        case 2:
          return destination.push("</div>");
        case 4:
          return destination.push("</svg>");
        case 5:
          return destination.push("</math>");
        case 6:
          return destination.push("</table>");
        case 7:
          return destination.push("</tbody></table>");
        case 8:
          return destination.push("</tr></table>");
        case 9:
          return destination.push("</colgroup></table>");
        default:
          throw Error("Unknown insertion mode. This is a bug in React.");
      }
    }
    var regexForJSStringsInInstructionScripts = /[<\u2028\u2029]/g;
    function escapeJSStringsForInstructionScripts(input) {
      return JSON.stringify(input).replace(
        regexForJSStringsInInstructionScripts,
        function (match2) {
          switch (match2) {
            case "<":
              return "\\u003c";
            case "\u2028":
              return "\\u2028";
            case "\u2029":
              return "\\u2029";
            default:
              throw Error(
                "escapeJSStringsForInstructionScripts encountered a match it does not know how to replace. this means the match regex and the replacement characters are no longer in sync. This is a bug in React",
              );
          }
        },
      );
    }
    var regexForJSStringsInScripts = /[&><\u2028\u2029]/g;
    function escapeJSObjectForInstructionScripts(input) {
      return JSON.stringify(input).replace(
        regexForJSStringsInScripts,
        function (match2) {
          switch (match2) {
            case "&":
              return "\\u0026";
            case ">":
              return "\\u003e";
            case "<":
              return "\\u003c";
            case "\u2028":
              return "\\u2028";
            case "\u2029":
              return "\\u2029";
            default:
              throw Error(
                "escapeJSObjectForInstructionScripts encountered a match it does not know how to replace. this means the match regex and the replacement characters are no longer in sync. This is a bug in React",
              );
          }
        },
      );
    }
    var currentlyRenderingBoundaryHasStylesToHoist = false;
    var destinationHasCapacity = true;
    function flushStyleTagsLateForBoundary(styleQueue) {
      var rules = styleQueue.rules,
        hrefs = styleQueue.hrefs,
        i = 0;
      if (hrefs.length) {
        this.push(currentlyFlushingRenderState.startInlineStyle);
        this.push(' media="not all" data-precedence="');
        this.push(styleQueue.precedence);
        for (this.push('" data-href="'); i < hrefs.length - 1; i++)
          (this.push(hrefs[i]), this.push(" "));
        this.push(hrefs[i]);
        this.push('">');
        for (i = 0; i < rules.length; i++) this.push(rules[i]);
        destinationHasCapacity = this.push("</style>");
        currentlyRenderingBoundaryHasStylesToHoist = true;
        rules.length = 0;
        hrefs.length = 0;
      }
    }
    function hasStylesToHoist(stylesheet) {
      return 2 !== stylesheet.state
        ? (currentlyRenderingBoundaryHasStylesToHoist = true)
        : false;
    }
    function writeHoistablesForBoundary(
      destination,
      hoistableState,
      renderState,
    ) {
      currentlyRenderingBoundaryHasStylesToHoist = false;
      destinationHasCapacity = true;
      currentlyFlushingRenderState = renderState;
      hoistableState.styles.forEach(flushStyleTagsLateForBoundary, destination);
      currentlyFlushingRenderState = null;
      hoistableState.stylesheets.forEach(hasStylesToHoist);
      currentlyRenderingBoundaryHasStylesToHoist &&
        (renderState.stylesToHoist = true);
      return destinationHasCapacity;
    }
    function flushResource(resource) {
      for (var i = 0; i < resource.length; i++) this.push(resource[i]);
      resource.length = 0;
    }
    var stylesheetFlushingQueue = [];
    function flushStyleInPreamble(stylesheet) {
      pushLinkImpl(stylesheetFlushingQueue, stylesheet.props);
      for (var i = 0; i < stylesheetFlushingQueue.length; i++)
        this.push(stylesheetFlushingQueue[i]);
      stylesheetFlushingQueue.length = 0;
      stylesheet.state = 2;
    }
    function flushStylesInPreamble(styleQueue) {
      var hasStylesheets = 0 < styleQueue.sheets.size;
      styleQueue.sheets.forEach(flushStyleInPreamble, this);
      styleQueue.sheets.clear();
      var rules = styleQueue.rules,
        hrefs = styleQueue.hrefs;
      if (!hasStylesheets || hrefs.length) {
        this.push(currentlyFlushingRenderState.startInlineStyle);
        this.push(' data-precedence="');
        this.push(styleQueue.precedence);
        styleQueue = 0;
        if (hrefs.length) {
          for (
            this.push('" data-href="');
            styleQueue < hrefs.length - 1;
            styleQueue++
          )
            (this.push(hrefs[styleQueue]), this.push(" "));
          this.push(hrefs[styleQueue]);
        }
        this.push('">');
        for (styleQueue = 0; styleQueue < rules.length; styleQueue++)
          this.push(rules[styleQueue]);
        this.push("</style>");
        rules.length = 0;
        hrefs.length = 0;
      }
    }
    function preloadLateStyle(stylesheet) {
      if (0 === stylesheet.state) {
        stylesheet.state = 1;
        var props = stylesheet.props;
        pushLinkImpl(stylesheetFlushingQueue, {
          rel: "preload",
          as: "style",
          href: stylesheet.props.href,
          crossOrigin: props.crossOrigin,
          fetchPriority: props.fetchPriority,
          integrity: props.integrity,
          media: props.media,
          hrefLang: props.hrefLang,
          referrerPolicy: props.referrerPolicy,
        });
        for (
          stylesheet = 0;
          stylesheet < stylesheetFlushingQueue.length;
          stylesheet++
        )
          this.push(stylesheetFlushingQueue[stylesheet]);
        stylesheetFlushingQueue.length = 0;
      }
    }
    function preloadLateStyles(styleQueue) {
      styleQueue.sheets.forEach(preloadLateStyle, this);
      styleQueue.sheets.clear();
    }
    function pushCompletedShellIdAttribute(target, resumableState) {
      0 === (resumableState.instructions & 32) &&
        ((resumableState.instructions |= 32),
        target.push(
          ' id="',
          escapeTextForBrowser("_" + resumableState.idPrefix + "R_"),
          '"',
        ));
    }
    function writeStyleResourceDependenciesInJS(destination, hoistableState) {
      destination.push("[");
      var nextArrayOpenBrackChunk = "[";
      hoistableState.stylesheets.forEach(function (resource) {
        if (2 !== resource.state)
          if (3 === resource.state)
            (destination.push(nextArrayOpenBrackChunk),
              (resource = escapeJSObjectForInstructionScripts(
                "" + resource.props.href,
              )),
              destination.push(resource),
              destination.push("]"),
              (nextArrayOpenBrackChunk = ",["));
          else {
            destination.push(nextArrayOpenBrackChunk);
            var precedence = resource.props["data-precedence"],
              props = resource.props,
              coercedHref = sanitizeURL("" + resource.props.href);
            coercedHref = escapeJSObjectForInstructionScripts(coercedHref);
            destination.push(coercedHref);
            precedence = "" + precedence;
            destination.push(",");
            precedence = escapeJSObjectForInstructionScripts(precedence);
            destination.push(precedence);
            for (var propKey in props)
              if (
                hasOwnProperty.call(props, propKey) &&
                ((precedence = props[propKey]), null != precedence)
              )
                switch (propKey) {
                  case "href":
                  case "rel":
                  case "precedence":
                  case "data-precedence":
                    break;
                  case "children":
                  case "dangerouslySetInnerHTML":
                    throw Error(
                      "link is a self-closing tag and must neither have `children` nor use `dangerouslySetInnerHTML`.",
                    );
                  default:
                    writeStyleResourceAttributeInJS(
                      destination,
                      propKey,
                      precedence,
                    );
                }
            destination.push("]");
            nextArrayOpenBrackChunk = ",[";
            resource.state = 3;
          }
      });
      destination.push("]");
    }
    function writeStyleResourceAttributeInJS(destination, name, value) {
      var attributeName = name.toLowerCase();
      switch (typeof value) {
        case "function":
        case "symbol":
          return;
      }
      switch (name) {
        case "innerHTML":
        case "dangerouslySetInnerHTML":
        case "suppressContentEditableWarning":
        case "suppressHydrationWarning":
        case "style":
        case "ref":
          return;
        case "className":
          attributeName = "class";
          name = "" + value;
          break;
        case "hidden":
          if (false === value) return;
          name = "";
          break;
        case "src":
        case "href":
          value = sanitizeURL(value);
          name = "" + value;
          break;
        default:
          if (
            (2 < name.length &&
              ("o" === name[0] || "O" === name[0]) &&
              ("n" === name[1] || "N" === name[1])) ||
            !isAttributeNameSafe(name)
          )
            return;
          name = "" + value;
      }
      destination.push(",");
      attributeName = escapeJSObjectForInstructionScripts(attributeName);
      destination.push(attributeName);
      destination.push(",");
      attributeName = escapeJSObjectForInstructionScripts(name);
      destination.push(attributeName);
    }
    function createHoistableState() {
      return {
        styles: /* @__PURE__ */ new Set(),
        stylesheets: /* @__PURE__ */ new Set(),
        suspenseyImages: false,
      };
    }
    function prefetchDNS(href) {
      var request = currentRequest ? currentRequest : null;
      if (request) {
        var resumableState = request.resumableState,
          renderState = request.renderState;
        if ("string" === typeof href && href) {
          if (!resumableState.dnsResources.hasOwnProperty(href)) {
            resumableState.dnsResources[href] = null;
            resumableState = renderState.headers;
            var header, JSCompiler_temp;
            if (
              (JSCompiler_temp =
                resumableState && 0 < resumableState.remainingCapacity)
            )
              JSCompiler_temp =
                ((header =
                  "<" +
                  ("" + href).replace(
                    regexForHrefInLinkHeaderURLContext,
                    escapeHrefForLinkHeaderURLContextReplacer,
                  ) +
                  ">; rel=dns-prefetch"),
                0 <= (resumableState.remainingCapacity -= header.length + 2));
            JSCompiler_temp
              ? ((renderState.resets.dns[href] = null),
                resumableState.preconnects &&
                  (resumableState.preconnects += ", "),
                (resumableState.preconnects += header))
              : ((header = []),
                pushLinkImpl(header, { href, rel: "dns-prefetch" }),
                renderState.preconnects.add(header));
          }
          enqueueFlush(request);
        }
      } else previousDispatcher.D(href);
    }
    function preconnect(href, crossOrigin) {
      var request = currentRequest ? currentRequest : null;
      if (request) {
        var resumableState = request.resumableState,
          renderState = request.renderState;
        if ("string" === typeof href && href) {
          var bucket =
            "use-credentials" === crossOrigin
              ? "credentials"
              : "string" === typeof crossOrigin
                ? "anonymous"
                : "default";
          if (!resumableState.connectResources[bucket].hasOwnProperty(href)) {
            resumableState.connectResources[bucket][href] = null;
            resumableState = renderState.headers;
            var header, JSCompiler_temp;
            if (
              (JSCompiler_temp =
                resumableState && 0 < resumableState.remainingCapacity)
            ) {
              JSCompiler_temp =
                "<" +
                ("" + href).replace(
                  regexForHrefInLinkHeaderURLContext,
                  escapeHrefForLinkHeaderURLContextReplacer,
                ) +
                ">; rel=preconnect";
              if ("string" === typeof crossOrigin) {
                var escapedCrossOrigin = ("" + crossOrigin).replace(
                  regexForLinkHeaderQuotedParamValueContext,
                  escapeStringForLinkHeaderQuotedParamValueContextReplacer,
                );
                JSCompiler_temp += '; crossorigin="' + escapedCrossOrigin + '"';
              }
              JSCompiler_temp =
                ((header = JSCompiler_temp),
                0 <= (resumableState.remainingCapacity -= header.length + 2));
            }
            JSCompiler_temp
              ? ((renderState.resets.connect[bucket][href] = null),
                resumableState.preconnects &&
                  (resumableState.preconnects += ", "),
                (resumableState.preconnects += header))
              : ((bucket = []),
                pushLinkImpl(bucket, {
                  rel: "preconnect",
                  href,
                  crossOrigin,
                }),
                renderState.preconnects.add(bucket));
          }
          enqueueFlush(request);
        }
      } else previousDispatcher.C(href, crossOrigin);
    }
    function preload(href, as, options) {
      var request = currentRequest ? currentRequest : null;
      if (request) {
        var resumableState = request.resumableState,
          renderState = request.renderState;
        if (as && href) {
          switch (as) {
            case "image":
              if (options) {
                var imageSrcSet = options.imageSrcSet;
                var imageSizes = options.imageSizes;
                var fetchPriority = options.fetchPriority;
              }
              var key = imageSrcSet
                ? imageSrcSet + "\n" + (imageSizes || "")
                : href;
              if (resumableState.imageResources.hasOwnProperty(key)) return;
              resumableState.imageResources[key] = PRELOAD_NO_CREDS;
              resumableState = renderState.headers;
              var header;
              resumableState &&
              0 < resumableState.remainingCapacity &&
              "string" !== typeof imageSrcSet &&
              "high" === fetchPriority &&
              ((header = getPreloadAsHeader(href, as, options)),
              0 <= (resumableState.remainingCapacity -= header.length + 2))
                ? ((renderState.resets.image[key] = PRELOAD_NO_CREDS),
                  resumableState.highImagePreloads &&
                    (resumableState.highImagePreloads += ", "),
                  (resumableState.highImagePreloads += header))
                : ((resumableState = []),
                  pushLinkImpl(
                    resumableState,
                    assign2(
                      { rel: "preload", href: imageSrcSet ? void 0 : href, as },
                      options,
                    ),
                  ),
                  "high" === fetchPriority
                    ? renderState.highImagePreloads.add(resumableState)
                    : (renderState.bulkPreloads.add(resumableState),
                      renderState.preloads.images.set(key, resumableState)));
              break;
            case "style":
              if (resumableState.styleResources.hasOwnProperty(href)) return;
              imageSrcSet = [];
              pushLinkImpl(
                imageSrcSet,
                assign2({ rel: "preload", href, as }, options),
              );
              resumableState.styleResources[href] =
                !options ||
                ("string" !== typeof options.crossOrigin &&
                  "string" !== typeof options.integrity)
                  ? PRELOAD_NO_CREDS
                  : [options.crossOrigin, options.integrity];
              renderState.preloads.stylesheets.set(href, imageSrcSet);
              renderState.bulkPreloads.add(imageSrcSet);
              break;
            case "script":
              if (resumableState.scriptResources.hasOwnProperty(href)) return;
              imageSrcSet = [];
              renderState.preloads.scripts.set(href, imageSrcSet);
              renderState.bulkPreloads.add(imageSrcSet);
              pushLinkImpl(
                imageSrcSet,
                assign2({ rel: "preload", href, as }, options),
              );
              resumableState.scriptResources[href] =
                !options ||
                ("string" !== typeof options.crossOrigin &&
                  "string" !== typeof options.integrity)
                  ? PRELOAD_NO_CREDS
                  : [options.crossOrigin, options.integrity];
              break;
            default:
              if (resumableState.unknownResources.hasOwnProperty(as)) {
                if (
                  ((imageSrcSet = resumableState.unknownResources[as]),
                  imageSrcSet.hasOwnProperty(href))
                )
                  return;
              } else
                ((imageSrcSet = {}),
                  (resumableState.unknownResources[as] = imageSrcSet));
              imageSrcSet[href] = PRELOAD_NO_CREDS;
              if (
                (resumableState = renderState.headers) &&
                0 < resumableState.remainingCapacity &&
                "font" === as &&
                ((key = getPreloadAsHeader(href, as, options)),
                0 <= (resumableState.remainingCapacity -= key.length + 2))
              )
                ((renderState.resets.font[href] = PRELOAD_NO_CREDS),
                  resumableState.fontPreloads &&
                    (resumableState.fontPreloads += ", "),
                  (resumableState.fontPreloads += key));
              else
                switch (
                  ((resumableState = []),
                  (href = assign2({ rel: "preload", href, as }, options)),
                  pushLinkImpl(resumableState, href),
                  as)
                ) {
                  case "font":
                    renderState.fontPreloads.add(resumableState);
                    break;
                  default:
                    renderState.bulkPreloads.add(resumableState);
                }
          }
          enqueueFlush(request);
        }
      } else previousDispatcher.L(href, as, options);
    }
    function preloadModule(href, options) {
      var request = currentRequest ? currentRequest : null;
      if (request) {
        var resumableState = request.resumableState,
          renderState = request.renderState;
        if (href) {
          var as =
            options && "string" === typeof options.as ? options.as : "script";
          switch (as) {
            case "script":
              if (resumableState.moduleScriptResources.hasOwnProperty(href))
                return;
              as = [];
              resumableState.moduleScriptResources[href] =
                !options ||
                ("string" !== typeof options.crossOrigin &&
                  "string" !== typeof options.integrity)
                  ? PRELOAD_NO_CREDS
                  : [options.crossOrigin, options.integrity];
              renderState.preloads.moduleScripts.set(href, as);
              break;
            default:
              if (resumableState.moduleUnknownResources.hasOwnProperty(as)) {
                var resources = resumableState.unknownResources[as];
                if (resources.hasOwnProperty(href)) return;
              } else
                ((resources = {}),
                  (resumableState.moduleUnknownResources[as] = resources));
              as = [];
              resources[href] = PRELOAD_NO_CREDS;
          }
          pushLinkImpl(as, assign2({ rel: "modulepreload", href }, options));
          renderState.bulkPreloads.add(as);
          enqueueFlush(request);
        }
      } else previousDispatcher.m(href, options);
    }
    function preinitStyle(href, precedence, options) {
      var request = currentRequest ? currentRequest : null;
      if (request) {
        var resumableState = request.resumableState,
          renderState = request.renderState;
        if (href) {
          precedence = precedence || "default";
          var styleQueue = renderState.styles.get(precedence),
            resourceState = resumableState.styleResources.hasOwnProperty(href)
              ? resumableState.styleResources[href]
              : void 0;
          null !== resourceState &&
            ((resumableState.styleResources[href] = null),
            styleQueue ||
              ((styleQueue = {
                precedence: escapeTextForBrowser(precedence),
                rules: [],
                hrefs: [],
                sheets: /* @__PURE__ */ new Map(),
              }),
              renderState.styles.set(precedence, styleQueue)),
            (precedence = {
              state: 0,
              props: assign2(
                { rel: "stylesheet", href, "data-precedence": precedence },
                options,
              ),
            }),
            resourceState &&
              (2 === resourceState.length &&
                adoptPreloadCredentials(precedence.props, resourceState),
              (renderState = renderState.preloads.stylesheets.get(href)) &&
              0 < renderState.length
                ? (renderState.length = 0)
                : (precedence.state = 1)),
            styleQueue.sheets.set(href, precedence),
            enqueueFlush(request));
        }
      } else previousDispatcher.S(href, precedence, options);
    }
    function preinitScript(src, options) {
      var request = currentRequest ? currentRequest : null;
      if (request) {
        var resumableState = request.resumableState,
          renderState = request.renderState;
        if (src) {
          var resourceState = resumableState.scriptResources.hasOwnProperty(src)
            ? resumableState.scriptResources[src]
            : void 0;
          null !== resourceState &&
            ((resumableState.scriptResources[src] = null),
            (options = assign2({ src, async: true }, options)),
            resourceState &&
              (2 === resourceState.length &&
                adoptPreloadCredentials(options, resourceState),
              (src = renderState.preloads.scripts.get(src))) &&
              (src.length = 0),
            (src = []),
            renderState.scripts.add(src),
            pushScriptImpl(src, options),
            enqueueFlush(request));
        }
      } else previousDispatcher.X(src, options);
    }
    function preinitModuleScript(src, options) {
      var request = currentRequest ? currentRequest : null;
      if (request) {
        var resumableState = request.resumableState,
          renderState = request.renderState;
        if (src) {
          var resourceState =
            resumableState.moduleScriptResources.hasOwnProperty(src)
              ? resumableState.moduleScriptResources[src]
              : void 0;
          null !== resourceState &&
            ((resumableState.moduleScriptResources[src] = null),
            (options = assign2({ src, type: "module", async: true }, options)),
            resourceState &&
              (2 === resourceState.length &&
                adoptPreloadCredentials(options, resourceState),
              (src = renderState.preloads.moduleScripts.get(src))) &&
              (src.length = 0),
            (src = []),
            renderState.scripts.add(src),
            pushScriptImpl(src, options),
            enqueueFlush(request));
        }
      } else previousDispatcher.M(src, options);
    }
    function adoptPreloadCredentials(target, preloadState) {
      null == target.crossOrigin && (target.crossOrigin = preloadState[0]);
      null == target.integrity && (target.integrity = preloadState[1]);
    }
    function getPreloadAsHeader(href, as, params) {
      href = ("" + href).replace(
        regexForHrefInLinkHeaderURLContext,
        escapeHrefForLinkHeaderURLContextReplacer,
      );
      as = ("" + as).replace(
        regexForLinkHeaderQuotedParamValueContext,
        escapeStringForLinkHeaderQuotedParamValueContextReplacer,
      );
      as = "<" + href + '>; rel=preload; as="' + as + '"';
      for (var paramName in params)
        hasOwnProperty.call(params, paramName) &&
          ((href = params[paramName]),
          "string" === typeof href &&
            (as +=
              "; " +
              paramName.toLowerCase() +
              '="' +
              ("" + href).replace(
                regexForLinkHeaderQuotedParamValueContext,
                escapeStringForLinkHeaderQuotedParamValueContextReplacer,
              ) +
              '"'));
      return as;
    }
    var regexForHrefInLinkHeaderURLContext = /[<>\r\n]/g;
    function escapeHrefForLinkHeaderURLContextReplacer(match2) {
      switch (match2) {
        case "<":
          return "%3C";
        case ">":
          return "%3E";
        case "\n":
          return "%0A";
        case "\r":
          return "%0D";
        default:
          throw Error(
            "escapeLinkHrefForHeaderContextReplacer encountered a match it does not know how to replace. this means the match regex and the replacement characters are no longer in sync. This is a bug in React",
          );
      }
    }
    var regexForLinkHeaderQuotedParamValueContext = /["';,\r\n]/g;
    function escapeStringForLinkHeaderQuotedParamValueContextReplacer(match2) {
      switch (match2) {
        case '"':
          return "%22";
        case "'":
          return "%27";
        case ";":
          return "%3B";
        case ",":
          return "%2C";
        case "\n":
          return "%0A";
        case "\r":
          return "%0D";
        default:
          throw Error(
            "escapeStringForLinkHeaderQuotedParamValueContextReplacer encountered a match it does not know how to replace. this means the match regex and the replacement characters are no longer in sync. This is a bug in React",
          );
      }
    }
    function hoistStyleQueueDependency(styleQueue) {
      this.styles.add(styleQueue);
    }
    function hoistStylesheetDependency(stylesheet) {
      this.stylesheets.add(stylesheet);
    }
    function hoistHoistables(parentState, childState) {
      childState.styles.forEach(hoistStyleQueueDependency, parentState);
      childState.stylesheets.forEach(hoistStylesheetDependency, parentState);
      childState.suspenseyImages && (parentState.suspenseyImages = true);
    }
    function createRenderState(resumableState, generateStaticMarkup) {
      var idPrefix = resumableState.idPrefix,
        bootstrapChunks = [],
        bootstrapScriptContent = resumableState.bootstrapScriptContent,
        bootstrapScripts = resumableState.bootstrapScripts,
        bootstrapModules = resumableState.bootstrapModules;
      void 0 !== bootstrapScriptContent &&
        (bootstrapChunks.push("<script"),
        pushCompletedShellIdAttribute(bootstrapChunks, resumableState),
        bootstrapChunks.push(
          ">",
          ("" + bootstrapScriptContent).replace(scriptRegex, scriptReplacer),
          "</script>",
        ));
      bootstrapScriptContent = idPrefix + "P:";
      var JSCompiler_object_inline_segmentPrefix_1673 = idPrefix + "S:";
      idPrefix += "B:";
      var JSCompiler_object_inline_preconnects_1687 = /* @__PURE__ */ new Set(),
        JSCompiler_object_inline_fontPreloads_1688 = /* @__PURE__ */ new Set(),
        JSCompiler_object_inline_highImagePreloads_1689 =
          /* @__PURE__ */ new Set(),
        JSCompiler_object_inline_styles_1690 = /* @__PURE__ */ new Map(),
        JSCompiler_object_inline_bootstrapScripts_1691 =
          /* @__PURE__ */ new Set(),
        JSCompiler_object_inline_scripts_1692 = /* @__PURE__ */ new Set(),
        JSCompiler_object_inline_bulkPreloads_1693 = /* @__PURE__ */ new Set(),
        JSCompiler_object_inline_preloads_1694 = {
          images: /* @__PURE__ */ new Map(),
          stylesheets: /* @__PURE__ */ new Map(),
          scripts: /* @__PURE__ */ new Map(),
          moduleScripts: /* @__PURE__ */ new Map(),
        };
      if (void 0 !== bootstrapScripts)
        for (var i = 0; i < bootstrapScripts.length; i++) {
          var scriptConfig = bootstrapScripts[i],
            src,
            crossOrigin = void 0,
            integrity = void 0,
            props = {
              rel: "preload",
              as: "script",
              fetchPriority: "low",
              nonce: void 0,
            };
          "string" === typeof scriptConfig
            ? (props.href = src = scriptConfig)
            : ((props.href = src = scriptConfig.src),
              (props.integrity = integrity =
                "string" === typeof scriptConfig.integrity
                  ? scriptConfig.integrity
                  : void 0),
              (props.crossOrigin = crossOrigin =
                "string" === typeof scriptConfig ||
                null == scriptConfig.crossOrigin
                  ? void 0
                  : "use-credentials" === scriptConfig.crossOrigin
                    ? "use-credentials"
                    : ""));
          scriptConfig = resumableState;
          var href = src;
          scriptConfig.scriptResources[href] = null;
          scriptConfig.moduleScriptResources[href] = null;
          scriptConfig = [];
          pushLinkImpl(scriptConfig, props);
          JSCompiler_object_inline_bootstrapScripts_1691.add(scriptConfig);
          bootstrapChunks.push('<script src="', escapeTextForBrowser(src), '"');
          "string" === typeof integrity &&
            bootstrapChunks.push(
              ' integrity="',
              escapeTextForBrowser(integrity),
              '"',
            );
          "string" === typeof crossOrigin &&
            bootstrapChunks.push(
              ' crossorigin="',
              escapeTextForBrowser(crossOrigin),
              '"',
            );
          pushCompletedShellIdAttribute(bootstrapChunks, resumableState);
          bootstrapChunks.push(' async=""></script>');
        }
      if (void 0 !== bootstrapModules)
        for (
          bootstrapScripts = 0;
          bootstrapScripts < bootstrapModules.length;
          bootstrapScripts++
        )
          ((props = bootstrapModules[bootstrapScripts]),
            (crossOrigin = src = void 0),
            (integrity = {
              rel: "modulepreload",
              fetchPriority: "low",
              nonce: void 0,
            }),
            "string" === typeof props
              ? (integrity.href = i = props)
              : ((integrity.href = i = props.src),
                (integrity.integrity = crossOrigin =
                  "string" === typeof props.integrity
                    ? props.integrity
                    : void 0),
                (integrity.crossOrigin = src =
                  "string" === typeof props || null == props.crossOrigin
                    ? void 0
                    : "use-credentials" === props.crossOrigin
                      ? "use-credentials"
                      : "")),
            (props = resumableState),
            (scriptConfig = i),
            (props.scriptResources[scriptConfig] = null),
            (props.moduleScriptResources[scriptConfig] = null),
            (props = []),
            pushLinkImpl(props, integrity),
            JSCompiler_object_inline_bootstrapScripts_1691.add(props),
            bootstrapChunks.push(
              '<script type="module" src="',
              escapeTextForBrowser(i),
              '"',
            ),
            "string" === typeof crossOrigin &&
              bootstrapChunks.push(
                ' integrity="',
                escapeTextForBrowser(crossOrigin),
                '"',
              ),
            "string" === typeof src &&
              bootstrapChunks.push(
                ' crossorigin="',
                escapeTextForBrowser(src),
                '"',
              ),
            pushCompletedShellIdAttribute(bootstrapChunks, resumableState),
            bootstrapChunks.push(' async=""></script>'));
      return {
        placeholderPrefix: bootstrapScriptContent,
        segmentPrefix: JSCompiler_object_inline_segmentPrefix_1673,
        boundaryPrefix: idPrefix,
        startInlineScript: "<script",
        startInlineStyle: "<style",
        preamble: { htmlChunks: null, headChunks: null, bodyChunks: null },
        externalRuntimeScript: null,
        bootstrapChunks,
        importMapChunks: [],
        onHeaders: void 0,
        headers: null,
        resets: {
          font: {},
          dns: {},
          connect: { default: {}, anonymous: {}, credentials: {} },
          image: {},
          style: {},
        },
        charsetChunks: [],
        viewportChunks: [],
        hoistableChunks: [],
        preconnects: JSCompiler_object_inline_preconnects_1687,
        fontPreloads: JSCompiler_object_inline_fontPreloads_1688,
        highImagePreloads: JSCompiler_object_inline_highImagePreloads_1689,
        styles: JSCompiler_object_inline_styles_1690,
        bootstrapScripts: JSCompiler_object_inline_bootstrapScripts_1691,
        scripts: JSCompiler_object_inline_scripts_1692,
        bulkPreloads: JSCompiler_object_inline_bulkPreloads_1693,
        preloads: JSCompiler_object_inline_preloads_1694,
        nonce: { script: void 0, style: void 0 },
        stylesToHoist: false,
        generateStaticMarkup,
      };
    }
    function pushTextInstance(target, text, renderState, textEmbedded) {
      if (renderState.generateStaticMarkup)
        return (target.push(escapeTextForBrowser(text)), false);
      "" === text
        ? (target = textEmbedded)
        : (textEmbedded && target.push("<!-- -->"),
          target.push(escapeTextForBrowser(text)),
          (target = true));
      return target;
    }
    function pushSegmentFinale(
      target,
      renderState,
      lastPushedText,
      textEmbedded,
    ) {
      renderState.generateStaticMarkup ||
        (lastPushedText && textEmbedded && target.push("<!-- -->"));
    }
    var bind = Function.prototype.bind;
    var REACT_CLIENT_REFERENCE = Symbol.for("react.client.reference");
    function getComponentNameFromType(type) {
      if (null == type) return null;
      if ("function" === typeof type)
        return type.$$typeof === REACT_CLIENT_REFERENCE
          ? null
          : type.displayName || type.name || null;
      if ("string" === typeof type) return type;
      switch (type) {
        case REACT_FRAGMENT_TYPE:
          return "Fragment";
        case REACT_PROFILER_TYPE:
          return "Profiler";
        case REACT_STRICT_MODE_TYPE:
          return "StrictMode";
        case REACT_SUSPENSE_TYPE:
          return "Suspense";
        case REACT_SUSPENSE_LIST_TYPE:
          return "SuspenseList";
        case REACT_ACTIVITY_TYPE:
          return "Activity";
      }
      if ("object" === typeof type)
        switch (type.$$typeof) {
          case REACT_PORTAL_TYPE:
            return "Portal";
          case REACT_CONTEXT_TYPE:
            return type.displayName || "Context";
          case REACT_CONSUMER_TYPE:
            return (type._context.displayName || "Context") + ".Consumer";
          case REACT_FORWARD_REF_TYPE:
            var innerType = type.render;
            type = type.displayName;
            type ||
              ((type = innerType.displayName || innerType.name || ""),
              (type = "" !== type ? "ForwardRef(" + type + ")" : "ForwardRef"));
            return type;
          case REACT_MEMO_TYPE:
            return (
              (innerType = type.displayName || null),
              null !== innerType
                ? innerType
                : getComponentNameFromType(type.type) || "Memo"
            );
          case REACT_LAZY_TYPE:
            innerType = type._payload;
            type = type._init;
            try {
              return getComponentNameFromType(type(innerType));
            } catch (x) {}
        }
      return null;
    }
    var emptyContextObject = {};
    var currentActiveSnapshot = null;
    function popToNearestCommonAncestor(prev2, next2) {
      if (prev2 !== next2) {
        prev2.context._currentValue2 = prev2.parentValue;
        prev2 = prev2.parent;
        var parentNext = next2.parent;
        if (null === prev2) {
          if (null !== parentNext)
            throw Error(
              "The stacks must reach the root at the same time. This is a bug in React.",
            );
        } else {
          if (null === parentNext)
            throw Error(
              "The stacks must reach the root at the same time. This is a bug in React.",
            );
          popToNearestCommonAncestor(prev2, parentNext);
        }
        next2.context._currentValue2 = next2.value;
      }
    }
    function popAllPrevious(prev2) {
      prev2.context._currentValue2 = prev2.parentValue;
      prev2 = prev2.parent;
      null !== prev2 && popAllPrevious(prev2);
    }
    function pushAllNext(next2) {
      var parentNext = next2.parent;
      null !== parentNext && pushAllNext(parentNext);
      next2.context._currentValue2 = next2.value;
    }
    function popPreviousToCommonLevel(prev2, next2) {
      prev2.context._currentValue2 = prev2.parentValue;
      prev2 = prev2.parent;
      if (null === prev2)
        throw Error(
          "The depth must equal at least at zero before reaching the root. This is a bug in React.",
        );
      prev2.depth === next2.depth
        ? popToNearestCommonAncestor(prev2, next2)
        : popPreviousToCommonLevel(prev2, next2);
    }
    function popNextToCommonLevel(prev2, next2) {
      var parentNext = next2.parent;
      if (null === parentNext)
        throw Error(
          "The depth must equal at least at zero before reaching the root. This is a bug in React.",
        );
      prev2.depth === parentNext.depth
        ? popToNearestCommonAncestor(prev2, parentNext)
        : popNextToCommonLevel(prev2, parentNext);
      next2.context._currentValue2 = next2.value;
    }
    function switchContext(newSnapshot) {
      var prev2 = currentActiveSnapshot;
      prev2 !== newSnapshot &&
        (null === prev2
          ? pushAllNext(newSnapshot)
          : null === newSnapshot
            ? popAllPrevious(prev2)
            : prev2.depth === newSnapshot.depth
              ? popToNearestCommonAncestor(prev2, newSnapshot)
              : prev2.depth > newSnapshot.depth
                ? popPreviousToCommonLevel(prev2, newSnapshot)
                : popNextToCommonLevel(prev2, newSnapshot),
        (currentActiveSnapshot = newSnapshot));
    }
    var classComponentUpdater = {
      enqueueSetState: function (inst, payload) {
        inst = inst._reactInternals;
        null !== inst.queue && inst.queue.push(payload);
      },
      enqueueReplaceState: function (inst, payload) {
        inst = inst._reactInternals;
        inst.replace = true;
        inst.queue = [payload];
      },
      enqueueForceUpdate: function () {},
    };
    var emptyTreeContext = { id: 1, overflow: "" };
    function pushTreeContext(baseContext, totalChildren, index) {
      var baseIdWithLeadingBit = baseContext.id;
      baseContext = baseContext.overflow;
      var baseLength = 32 - clz32(baseIdWithLeadingBit) - 1;
      baseIdWithLeadingBit &= ~(1 << baseLength);
      index += 1;
      var length2 = 32 - clz32(totalChildren) + baseLength;
      if (30 < length2) {
        var numberOfOverflowBits = baseLength - (baseLength % 5);
        length2 = (
          baseIdWithLeadingBit &
          ((1 << numberOfOverflowBits) - 1)
        ).toString(32);
        baseIdWithLeadingBit >>= numberOfOverflowBits;
        baseLength -= numberOfOverflowBits;
        return {
          id:
            (1 << (32 - clz32(totalChildren) + baseLength)) |
            (index << baseLength) |
            baseIdWithLeadingBit,
          overflow: length2 + baseContext,
        };
      }
      return {
        id: (1 << length2) | (index << baseLength) | baseIdWithLeadingBit,
        overflow: baseContext,
      };
    }
    var clz32 = Math.clz32 ? Math.clz32 : clz32Fallback;
    var log = Math.log;
    var LN2 = Math.LN2;
    function clz32Fallback(x) {
      x >>>= 0;
      return 0 === x ? 32 : (31 - ((log(x) / LN2) | 0)) | 0;
    }
    function noop5() {}
    var SuspenseException = Error(
      "Suspense Exception: This is not a real error! It's an implementation detail of `use` to interrupt the current render. You must either rethrow it immediately, or move the `use` call outside of the `try/catch` block. Capturing without rethrowing will lead to unexpected behavior.\n\nTo handle async errors, wrap your component in an error boundary, or call the promise's `.catch` method and pass the result to `use`.",
    );
    function trackUsedThenable(thenableState2, thenable, index) {
      index = thenableState2[index];
      void 0 === index
        ? thenableState2.push(thenable)
        : index !== thenable &&
          (thenable.then(noop5, noop5), (thenable = index));
      switch (thenable.status) {
        case "fulfilled":
          return thenable.value;
        case "rejected":
          throw thenable.reason;
        default:
          "string" === typeof thenable.status
            ? thenable.then(noop5, noop5)
            : ((thenableState2 = thenable),
              (thenableState2.status = "pending"),
              thenableState2.then(
                function (fulfilledValue) {
                  if ("pending" === thenable.status) {
                    var fulfilledThenable = thenable;
                    fulfilledThenable.status = "fulfilled";
                    fulfilledThenable.value = fulfilledValue;
                  }
                },
                function (error) {
                  if ("pending" === thenable.status) {
                    var rejectedThenable = thenable;
                    rejectedThenable.status = "rejected";
                    rejectedThenable.reason = error;
                  }
                },
              ));
          switch (thenable.status) {
            case "fulfilled":
              return thenable.value;
            case "rejected":
              throw thenable.reason;
          }
          suspendedThenable = thenable;
          throw SuspenseException;
      }
    }
    var suspendedThenable = null;
    function getSuspendedThenable() {
      if (null === suspendedThenable)
        throw Error(
          "Expected a suspended thenable. This is a bug in React. Please file an issue.",
        );
      var thenable = suspendedThenable;
      suspendedThenable = null;
      return thenable;
    }
    function is(x, y) {
      return (x === y && (0 !== x || 1 / x === 1 / y)) || (x !== x && y !== y);
    }
    var objectIs = "function" === typeof Object.is ? Object.is : is;
    var currentlyRenderingComponent = null;
    var currentlyRenderingTask = null;
    var currentlyRenderingRequest = null;
    var currentlyRenderingKeyPath = null;
    var firstWorkInProgressHook = null;
    var workInProgressHook = null;
    var isReRender = false;
    var didScheduleRenderPhaseUpdate = false;
    var localIdCounter = 0;
    var actionStateCounter = 0;
    var actionStateMatchingIndex = -1;
    var thenableIndexCounter = 0;
    var thenableState = null;
    var renderPhaseUpdates = null;
    var numberOfReRenders = 0;
    function resolveCurrentlyRenderingComponent() {
      if (null === currentlyRenderingComponent)
        throw Error(
          "Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://react.dev/link/invalid-hook-call for tips about how to debug and fix this problem.",
        );
      return currentlyRenderingComponent;
    }
    function createHook() {
      if (0 < numberOfReRenders)
        throw Error("Rendered more hooks than during the previous render");
      return { memoizedState: null, queue: null, next: null };
    }
    function createWorkInProgressHook() {
      null === workInProgressHook
        ? null === firstWorkInProgressHook
          ? ((isReRender = false),
            (firstWorkInProgressHook = workInProgressHook = createHook()))
          : ((isReRender = true),
            (workInProgressHook = firstWorkInProgressHook))
        : null === workInProgressHook.next
          ? ((isReRender = false),
            (workInProgressHook = workInProgressHook.next = createHook()))
          : ((isReRender = true),
            (workInProgressHook = workInProgressHook.next));
      return workInProgressHook;
    }
    function getThenableStateAfterSuspending() {
      var state = thenableState;
      thenableState = null;
      return state;
    }
    function resetHooksState() {
      currentlyRenderingKeyPath =
        currentlyRenderingRequest =
        currentlyRenderingTask =
        currentlyRenderingComponent =
          null;
      didScheduleRenderPhaseUpdate = false;
      firstWorkInProgressHook = null;
      numberOfReRenders = 0;
      workInProgressHook = renderPhaseUpdates = null;
    }
    function basicStateReducer(state, action) {
      return "function" === typeof action ? action(state) : action;
    }
    function useReducer(reducer, initialArg, init) {
      currentlyRenderingComponent = resolveCurrentlyRenderingComponent();
      workInProgressHook = createWorkInProgressHook();
      if (isReRender) {
        var queue = workInProgressHook.queue;
        initialArg = queue.dispatch;
        if (
          null !== renderPhaseUpdates &&
          ((init = renderPhaseUpdates.get(queue)), void 0 !== init)
        ) {
          renderPhaseUpdates.delete(queue);
          queue = workInProgressHook.memoizedState;
          do ((queue = reducer(queue, init.action)), (init = init.next));
          while (null !== init);
          workInProgressHook.memoizedState = queue;
          return [queue, initialArg];
        }
        return [workInProgressHook.memoizedState, initialArg];
      }
      reducer =
        reducer === basicStateReducer
          ? "function" === typeof initialArg
            ? initialArg()
            : initialArg
          : void 0 !== init
            ? init(initialArg)
            : initialArg;
      workInProgressHook.memoizedState = reducer;
      reducer = workInProgressHook.queue = { last: null, dispatch: null };
      reducer = reducer.dispatch = dispatchAction.bind(
        null,
        currentlyRenderingComponent,
        reducer,
      );
      return [workInProgressHook.memoizedState, reducer];
    }
    function useMemo20(nextCreate, deps) {
      currentlyRenderingComponent = resolveCurrentlyRenderingComponent();
      workInProgressHook = createWorkInProgressHook();
      deps = void 0 === deps ? null : deps;
      if (null !== workInProgressHook) {
        var prevState = workInProgressHook.memoizedState;
        if (null !== prevState && null !== deps) {
          var prevDeps = prevState[1];
          a: if (null === prevDeps) prevDeps = false;
          else {
            for (var i = 0; i < prevDeps.length && i < deps.length; i++)
              if (!objectIs(deps[i], prevDeps[i])) {
                prevDeps = false;
                break a;
              }
            prevDeps = true;
          }
          if (prevDeps) return prevState[0];
        }
      }
      nextCreate = nextCreate();
      workInProgressHook.memoizedState = [nextCreate, deps];
      return nextCreate;
    }
    function dispatchAction(componentIdentity, queue, action) {
      if (25 <= numberOfReRenders)
        throw Error(
          "Too many re-renders. React limits the number of renders to prevent an infinite loop.",
        );
      if (componentIdentity === currentlyRenderingComponent)
        if (
          ((didScheduleRenderPhaseUpdate = true),
          (componentIdentity = { action, next: null }),
          null === renderPhaseUpdates &&
            (renderPhaseUpdates = /* @__PURE__ */ new Map()),
          (action = renderPhaseUpdates.get(queue)),
          void 0 === action)
        )
          renderPhaseUpdates.set(queue, componentIdentity);
        else {
          for (queue = action; null !== queue.next;) queue = queue.next;
          queue.next = componentIdentity;
        }
    }
    function throwOnUseEffectEventCall() {
      throw Error(
        "A function wrapped in useEffectEvent can't be called during rendering.",
      );
    }
    function unsupportedStartTransition() {
      throw Error("startTransition cannot be called during server rendering.");
    }
    function unsupportedSetOptimisticState() {
      throw Error("Cannot update optimistic state while rendering.");
    }
    function useActionState(action, initialState, permalink) {
      resolveCurrentlyRenderingComponent();
      var actionStateHookIndex = actionStateCounter++,
        request = currentlyRenderingRequest;
      if ("function" === typeof action.$$FORM_ACTION) {
        var nextPostbackStateKey = null,
          componentKeyPath = currentlyRenderingKeyPath;
        request = request.formState;
        var isSignatureEqual = action.$$IS_SIGNATURE_EQUAL;
        if (null !== request && "function" === typeof isSignatureEqual) {
          var postbackKey = request[1];
          isSignatureEqual.call(action, request[2], request[3]) &&
            ((nextPostbackStateKey =
              void 0 !== permalink
                ? "p" + permalink
                : "k" +
                  murmurhash3_32_gc(
                    JSON.stringify([
                      componentKeyPath,
                      null,
                      actionStateHookIndex,
                    ]),
                    0,
                  )),
            postbackKey === nextPostbackStateKey &&
              ((actionStateMatchingIndex = actionStateHookIndex),
              (initialState = request[0])));
        }
        var boundAction = action.bind(null, initialState);
        action = function (payload) {
          boundAction(payload);
        };
        "function" === typeof boundAction.$$FORM_ACTION &&
          (action.$$FORM_ACTION = function (prefix3) {
            prefix3 = boundAction.$$FORM_ACTION(prefix3);
            void 0 !== permalink &&
              ((permalink += ""), (prefix3.action = permalink));
            var formData = prefix3.data;
            formData &&
              (null === nextPostbackStateKey &&
                (nextPostbackStateKey =
                  void 0 !== permalink
                    ? "p" + permalink
                    : "k" +
                      murmurhash3_32_gc(
                        JSON.stringify([
                          componentKeyPath,
                          null,
                          actionStateHookIndex,
                        ]),
                        0,
                      )),
              formData.append("$ACTION_KEY", nextPostbackStateKey));
            return prefix3;
          });
        return [initialState, action, false];
      }
      var boundAction$22 = action.bind(null, initialState);
      return [
        initialState,
        function (payload) {
          boundAction$22(payload);
        },
        false,
      ];
    }
    function unwrapThenable(thenable) {
      var index = thenableIndexCounter;
      thenableIndexCounter += 1;
      null === thenableState && (thenableState = []);
      return trackUsedThenable(thenableState, thenable, index);
    }
    function unsupportedRefresh() {
      throw Error("Cache cannot be refreshed during server rendering.");
    }
    var HooksDispatcher = {
      readContext: function (context) {
        return context._currentValue2;
      },
      use: function (usable) {
        if (null !== usable && "object" === typeof usable) {
          if ("function" === typeof usable.then) return unwrapThenable(usable);
          if (usable.$$typeof === REACT_CONTEXT_TYPE)
            return usable._currentValue2;
        }
        throw Error(
          "An unsupported type was passed to use(): " + String(usable),
        );
      },
      useContext: function (context) {
        resolveCurrentlyRenderingComponent();
        return context._currentValue2;
      },
      useMemo: useMemo20,
      useReducer,
      useRef: function (initialValue) {
        currentlyRenderingComponent = resolveCurrentlyRenderingComponent();
        workInProgressHook = createWorkInProgressHook();
        var previousRef = workInProgressHook.memoizedState;
        return null === previousRef
          ? ((initialValue = { current: initialValue }),
            (workInProgressHook.memoizedState = initialValue))
          : previousRef;
      },
      useState: function (initialState) {
        return useReducer(basicStateReducer, initialState);
      },
      useInsertionEffect: noop5,
      useLayoutEffect: noop5,
      useCallback: function (callback, deps) {
        return useMemo20(function () {
          return callback;
        }, deps);
      },
      useImperativeHandle: noop5,
      useEffect: noop5,
      useDebugValue: noop5,
      useDeferredValue: function (value, initialValue) {
        resolveCurrentlyRenderingComponent();
        return void 0 !== initialValue ? initialValue : value;
      },
      useTransition: function () {
        resolveCurrentlyRenderingComponent();
        return [false, unsupportedStartTransition];
      },
      useId: function () {
        var JSCompiler_inline_result = currentlyRenderingTask.treeContext;
        var overflow = JSCompiler_inline_result.overflow;
        JSCompiler_inline_result = JSCompiler_inline_result.id;
        JSCompiler_inline_result =
          (
            JSCompiler_inline_result &
            ~(1 << (32 - clz32(JSCompiler_inline_result) - 1))
          ).toString(32) + overflow;
        var resumableState = currentResumableState;
        if (null === resumableState)
          throw Error(
            "Invalid hook call. Hooks can only be called inside of the body of a function component.",
          );
        overflow = localIdCounter++;
        JSCompiler_inline_result =
          "_" + resumableState.idPrefix + "R_" + JSCompiler_inline_result;
        0 < overflow &&
          (JSCompiler_inline_result += "H" + overflow.toString(32));
        return JSCompiler_inline_result + "_";
      },
      useSyncExternalStore: function (
        subscribe,
        getSnapshot,
        getServerSnapshot,
      ) {
        if (void 0 === getServerSnapshot)
          throw Error(
            "Missing getServerSnapshot, which is required for server-rendered content. Will revert to client rendering.",
          );
        return getServerSnapshot();
      },
      useOptimistic: function (passthrough) {
        resolveCurrentlyRenderingComponent();
        return [passthrough, unsupportedSetOptimisticState];
      },
      useActionState,
      useFormState: useActionState,
      useHostTransitionStatus: function () {
        resolveCurrentlyRenderingComponent();
        return sharedNotPendingObject;
      },
      useMemoCache: function (size) {
        for (var data = Array(size), i = 0; i < size; i++)
          data[i] = REACT_MEMO_CACHE_SENTINEL;
        return data;
      },
      useCacheRefresh: function () {
        return unsupportedRefresh;
      },
      useEffectEvent: function () {
        return throwOnUseEffectEventCall;
      },
    };
    var currentResumableState = null;
    var DefaultAsyncDispatcher = {
      getCacheForType: function () {
        throw Error("Not implemented.");
      },
      cacheSignal: function () {
        throw Error("Not implemented.");
      },
    };
    var prefix2;
    var suffix;
    function describeBuiltInComponentFrame(name) {
      if (void 0 === prefix2)
        try {
          throw Error();
        } catch (x) {
          var match2 = x.stack.trim().match(/\n( *(at )?)/);
          prefix2 = (match2 && match2[1]) || "";
          suffix =
            -1 < x.stack.indexOf("\n    at")
              ? " (<anonymous>)"
              : -1 < x.stack.indexOf("@")
                ? "@unknown:0:0"
                : "";
        }
      return "\n" + prefix2 + name + suffix;
    }
    var reentry = false;
    function describeNativeComponentFrame(fn2, construct) {
      if (!fn2 || reentry) return "";
      reentry = true;
      var previousPrepareStackTrace = Error.prepareStackTrace;
      Error.prepareStackTrace = void 0;
      try {
        var RunInRootFrame = {
          DetermineComponentFrameRoot: function () {
            try {
              if (construct) {
                var Fake = function () {
                  throw Error();
                };
                Object.defineProperty(Fake.prototype, "props", {
                  set: function () {
                    throw Error();
                  },
                });
                if ("object" === typeof Reflect && Reflect.construct) {
                  try {
                    Reflect.construct(Fake, []);
                  } catch (x) {
                    var control = x;
                  }
                  Reflect.construct(fn2, [], Fake);
                } else {
                  try {
                    Fake.call();
                  } catch (x$24) {
                    control = x$24;
                  }
                  fn2.call(Fake.prototype);
                }
              } else {
                try {
                  throw Error();
                } catch (x$25) {
                  control = x$25;
                }
                (Fake = fn2()) &&
                  "function" === typeof Fake.catch &&
                  Fake.catch(function () {});
              }
            } catch (sample) {
              if (sample && control && "string" === typeof sample.stack)
                return [sample.stack, control.stack];
            }
            return [null, null];
          },
        };
        RunInRootFrame.DetermineComponentFrameRoot.displayName =
          "DetermineComponentFrameRoot";
        var namePropDescriptor = Object.getOwnPropertyDescriptor(
          RunInRootFrame.DetermineComponentFrameRoot,
          "name",
        );
        namePropDescriptor &&
          namePropDescriptor.configurable &&
          Object.defineProperty(
            RunInRootFrame.DetermineComponentFrameRoot,
            "name",
            { value: "DetermineComponentFrameRoot" },
          );
        var _RunInRootFrame$Deter =
            RunInRootFrame.DetermineComponentFrameRoot(),
          sampleStack = _RunInRootFrame$Deter[0],
          controlStack = _RunInRootFrame$Deter[1];
        if (sampleStack && controlStack) {
          var sampleLines = sampleStack.split("\n"),
            controlLines = controlStack.split("\n");
          for (
            namePropDescriptor = RunInRootFrame = 0;
            RunInRootFrame < sampleLines.length &&
            !sampleLines[RunInRootFrame].includes(
              "DetermineComponentFrameRoot",
            );
          )
            RunInRootFrame++;
          for (
            ;
            namePropDescriptor < controlLines.length &&
            !controlLines[namePropDescriptor].includes(
              "DetermineComponentFrameRoot",
            );
          )
            namePropDescriptor++;
          if (
            RunInRootFrame === sampleLines.length ||
            namePropDescriptor === controlLines.length
          )
            for (
              RunInRootFrame = sampleLines.length - 1,
                namePropDescriptor = controlLines.length - 1;
              1 <= RunInRootFrame &&
              0 <= namePropDescriptor &&
              sampleLines[RunInRootFrame] !== controlLines[namePropDescriptor];
            )
              namePropDescriptor--;
          for (
            ;
            1 <= RunInRootFrame && 0 <= namePropDescriptor;
            RunInRootFrame--, namePropDescriptor--
          )
            if (
              sampleLines[RunInRootFrame] !== controlLines[namePropDescriptor]
            ) {
              if (1 !== RunInRootFrame || 1 !== namePropDescriptor) {
                do
                  if (
                    (RunInRootFrame--,
                    namePropDescriptor--,
                    0 > namePropDescriptor ||
                      sampleLines[RunInRootFrame] !==
                        controlLines[namePropDescriptor])
                  ) {
                    var frame =
                      "\n" +
                      sampleLines[RunInRootFrame].replace(" at new ", " at ");
                    fn2.displayName &&
                      frame.includes("<anonymous>") &&
                      (frame = frame.replace("<anonymous>", fn2.displayName));
                    return frame;
                  }
                while (1 <= RunInRootFrame && 0 <= namePropDescriptor);
              }
              break;
            }
        }
      } finally {
        ((reentry = false),
          (Error.prepareStackTrace = previousPrepareStackTrace));
      }
      return (previousPrepareStackTrace = fn2
        ? fn2.displayName || fn2.name
        : "")
        ? describeBuiltInComponentFrame(previousPrepareStackTrace)
        : "";
    }
    function describeComponentStackByType(type) {
      if ("string" === typeof type) return describeBuiltInComponentFrame(type);
      if ("function" === typeof type)
        return type.prototype && type.prototype.isReactComponent
          ? describeNativeComponentFrame(type, true)
          : describeNativeComponentFrame(type, false);
      if ("object" === typeof type && null !== type) {
        switch (type.$$typeof) {
          case REACT_FORWARD_REF_TYPE:
            return describeNativeComponentFrame(type.render, false);
          case REACT_MEMO_TYPE:
            return describeNativeComponentFrame(type.type, false);
          case REACT_LAZY_TYPE:
            var lazyComponent = type,
              payload = lazyComponent._payload;
            lazyComponent = lazyComponent._init;
            try {
              type = lazyComponent(payload);
            } catch (x) {
              return describeBuiltInComponentFrame("Lazy");
            }
            return describeComponentStackByType(type);
        }
        if ("string" === typeof type.name) {
          a: {
            payload = type.name;
            lazyComponent = type.env;
            var location2 = type.debugLocation;
            if (
              null != location2 &&
              ((type = Error.prepareStackTrace),
              (Error.prepareStackTrace = void 0),
              (location2 = location2.stack),
              (Error.prepareStackTrace = type),
              location2.startsWith("Error: react-stack-top-frame\n") &&
                (location2 = location2.slice(29)),
              (type = location2.indexOf("\n")),
              -1 !== type && (location2 = location2.slice(type + 1)),
              (type = location2.indexOf("react_stack_bottom_frame")),
              -1 !== type && (type = location2.lastIndexOf("\n", type)),
              (type =
                -1 !== type ? (location2 = location2.slice(0, type)) : ""),
              (location2 = type.lastIndexOf("\n")),
              (type = -1 === location2 ? type : type.slice(location2 + 1)),
              -1 !== type.indexOf(payload))
            ) {
              payload = "\n" + type;
              break a;
            }
            payload = describeBuiltInComponentFrame(
              payload + (lazyComponent ? " [" + lazyComponent + "]" : ""),
            );
          }
          return payload;
        }
      }
      switch (type) {
        case REACT_SUSPENSE_LIST_TYPE:
          return describeBuiltInComponentFrame("SuspenseList");
        case REACT_SUSPENSE_TYPE:
          return describeBuiltInComponentFrame("Suspense");
      }
      return "";
    }
    function isEligibleForOutlining(request, boundary) {
      return (
        (500 < boundary.byteSize || false) && null === boundary.contentPreamble
      );
    }
    function defaultErrorHandler(error) {
      if (
        "object" === typeof error &&
        null !== error &&
        "string" === typeof error.environmentName
      ) {
        var JSCompiler_inline_result = error.environmentName;
        error = [error].slice(0);
        "string" === typeof error[0]
          ? error.splice(
              0,
              1,
              "[%s] " + error[0],
              " " + JSCompiler_inline_result + " ",
            )
          : error.splice(0, 0, "[%s]", " " + JSCompiler_inline_result + " ");
        error.unshift(console);
        JSCompiler_inline_result = bind.apply(console.error, error);
        JSCompiler_inline_result();
      } else console.error(error);
      return null;
    }
    function RequestInstance(
      resumableState,
      renderState,
      rootFormatContext,
      progressiveChunkSize,
      onError2,
      onAllReady,
      onShellReady,
      onShellError,
      onFatalError,
      onPostpone,
      formState,
    ) {
      var abortSet = /* @__PURE__ */ new Set();
      this.destination = null;
      this.flushScheduled = false;
      this.resumableState = resumableState;
      this.renderState = renderState;
      this.rootFormatContext = rootFormatContext;
      this.progressiveChunkSize =
        void 0 === progressiveChunkSize ? 12800 : progressiveChunkSize;
      this.status = 10;
      this.fatalError = null;
      this.pendingRootTasks = this.allPendingTasks = this.nextSegmentId = 0;
      this.completedPreambleSegments = this.completedRootSegment = null;
      this.byteSize = 0;
      this.abortableTasks = abortSet;
      this.pingedTasks = [];
      this.clientRenderedBoundaries = [];
      this.completedBoundaries = [];
      this.partialBoundaries = [];
      this.trackedPostpones = null;
      this.onError = void 0 === onError2 ? defaultErrorHandler : onError2;
      this.onPostpone = void 0 === onPostpone ? noop5 : onPostpone;
      this.onAllReady = void 0 === onAllReady ? noop5 : onAllReady;
      this.onShellReady = void 0 === onShellReady ? noop5 : onShellReady;
      this.onShellError = void 0 === onShellError ? noop5 : onShellError;
      this.onFatalError = void 0 === onFatalError ? noop5 : onFatalError;
      this.formState = void 0 === formState ? null : formState;
    }
    function createRequest(
      children,
      resumableState,
      renderState,
      rootFormatContext,
      progressiveChunkSize,
      onError2,
      onAllReady,
      onShellReady,
      onShellError,
      onFatalError,
      onPostpone,
      formState,
    ) {
      resumableState = new RequestInstance(
        resumableState,
        renderState,
        rootFormatContext,
        progressiveChunkSize,
        onError2,
        onAllReady,
        onShellReady,
        onShellError,
        onFatalError,
        onPostpone,
        formState,
      );
      renderState = createPendingSegment(
        resumableState,
        0,
        null,
        rootFormatContext,
        false,
        false,
      );
      renderState.parentFlushed = true;
      children = createRenderTask(
        resumableState,
        null,
        children,
        -1,
        null,
        renderState,
        null,
        null,
        resumableState.abortableTasks,
        null,
        rootFormatContext,
        null,
        emptyTreeContext,
        null,
        null,
      );
      pushComponentStack(children);
      resumableState.pingedTasks.push(children);
      return resumableState;
    }
    var currentRequest = null;
    function pingTask(request, task) {
      request.pingedTasks.push(task);
      1 === request.pingedTasks.length &&
        ((request.flushScheduled = null !== request.destination),
        performWork(request));
    }
    function createSuspenseBoundary(
      request,
      row,
      fallbackAbortableTasks,
      contentPreamble,
      fallbackPreamble,
    ) {
      fallbackAbortableTasks = {
        status: 0,
        rootSegmentID: -1,
        parentFlushed: false,
        pendingTasks: 0,
        row,
        completedSegments: [],
        byteSize: 0,
        fallbackAbortableTasks,
        errorDigest: null,
        contentState: createHoistableState(),
        fallbackState: createHoistableState(),
        contentPreamble,
        fallbackPreamble,
        trackedContentKeyPath: null,
        trackedFallbackNode: null,
      };
      null !== row &&
        (row.pendingTasks++,
        (contentPreamble = row.boundaries),
        null !== contentPreamble &&
          (request.allPendingTasks++,
          fallbackAbortableTasks.pendingTasks++,
          contentPreamble.push(fallbackAbortableTasks)),
        (request = row.inheritedHoistables),
        null !== request &&
          hoistHoistables(fallbackAbortableTasks.contentState, request));
      return fallbackAbortableTasks;
    }
    function createRenderTask(
      request,
      thenableState2,
      node2,
      childIndex,
      blockedBoundary,
      blockedSegment,
      blockedPreamble,
      hoistableState,
      abortSet,
      keyPath,
      formatContext,
      context,
      treeContext,
      row,
      componentStack,
    ) {
      request.allPendingTasks++;
      null === blockedBoundary
        ? request.pendingRootTasks++
        : blockedBoundary.pendingTasks++;
      null !== row && row.pendingTasks++;
      var task = {
        replay: null,
        node: node2,
        childIndex,
        ping: function () {
          return pingTask(request, task);
        },
        blockedBoundary,
        blockedSegment,
        blockedPreamble,
        hoistableState,
        abortSet,
        keyPath,
        formatContext,
        context,
        treeContext,
        row,
        componentStack,
        thenableState: thenableState2,
      };
      abortSet.add(task);
      return task;
    }
    function createReplayTask(
      request,
      thenableState2,
      replay,
      node2,
      childIndex,
      blockedBoundary,
      hoistableState,
      abortSet,
      keyPath,
      formatContext,
      context,
      treeContext,
      row,
      componentStack,
    ) {
      request.allPendingTasks++;
      null === blockedBoundary
        ? request.pendingRootTasks++
        : blockedBoundary.pendingTasks++;
      null !== row && row.pendingTasks++;
      replay.pendingTasks++;
      var task = {
        replay,
        node: node2,
        childIndex,
        ping: function () {
          return pingTask(request, task);
        },
        blockedBoundary,
        blockedSegment: null,
        blockedPreamble: null,
        hoistableState,
        abortSet,
        keyPath,
        formatContext,
        context,
        treeContext,
        row,
        componentStack,
        thenableState: thenableState2,
      };
      abortSet.add(task);
      return task;
    }
    function createPendingSegment(
      request,
      index,
      boundary,
      parentFormatContext,
      lastPushedText,
      textEmbedded,
    ) {
      return {
        status: 0,
        parentFlushed: false,
        id: -1,
        index,
        chunks: [],
        children: [],
        preambleChildren: [],
        parentFormatContext,
        boundary,
        lastPushedText,
        textEmbedded,
      };
    }
    function pushComponentStack(task) {
      var node2 = task.node;
      if ("object" === typeof node2 && null !== node2)
        switch (node2.$$typeof) {
          case REACT_ELEMENT_TYPE:
            task.componentStack = {
              parent: task.componentStack,
              type: node2.type,
            };
        }
    }
    function replaceSuspenseComponentStackWithSuspenseFallbackStack(
      componentStack,
    ) {
      return null === componentStack
        ? null
        : { parent: componentStack.parent, type: "Suspense Fallback" };
    }
    function getThrownInfo(node$jscomp$0) {
      var errorInfo = {};
      node$jscomp$0 &&
        Object.defineProperty(errorInfo, "componentStack", {
          configurable: true,
          enumerable: true,
          get: function () {
            try {
              var info = "",
                node2 = node$jscomp$0;
              do
                ((info += describeComponentStackByType(node2.type)),
                  (node2 = node2.parent));
              while (node2);
              var JSCompiler_inline_result = info;
            } catch (x) {
              JSCompiler_inline_result =
                "\nError generating stack: " + x.message + "\n" + x.stack;
            }
            Object.defineProperty(errorInfo, "componentStack", {
              value: JSCompiler_inline_result,
            });
            return JSCompiler_inline_result;
          },
        });
      return errorInfo;
    }
    function logRecoverableError(request, error, errorInfo) {
      request = request.onError;
      error = request(error, errorInfo);
      if (null == error || "string" === typeof error) return error;
    }
    function fatalError(request, error) {
      var onShellError = request.onShellError,
        onFatalError = request.onFatalError;
      onShellError(error);
      onFatalError(error);
      null !== request.destination
        ? ((request.status = 14), request.destination.destroy(error))
        : ((request.status = 13), (request.fatalError = error));
    }
    function finishSuspenseListRow(request, row) {
      unblockSuspenseListRow(request, row.next, row.hoistables);
    }
    function unblockSuspenseListRow(
      request,
      unblockedRow,
      inheritedHoistables,
    ) {
      for (; null !== unblockedRow;) {
        null !== inheritedHoistables &&
          (hoistHoistables(unblockedRow.hoistables, inheritedHoistables),
          (unblockedRow.inheritedHoistables = inheritedHoistables));
        var unblockedBoundaries = unblockedRow.boundaries;
        if (null !== unblockedBoundaries) {
          unblockedRow.boundaries = null;
          for (var i = 0; i < unblockedBoundaries.length; i++) {
            var unblockedBoundary = unblockedBoundaries[i];
            null !== inheritedHoistables &&
              hoistHoistables(
                unblockedBoundary.contentState,
                inheritedHoistables,
              );
            finishedTask(request, unblockedBoundary, null, null);
          }
        }
        unblockedRow.pendingTasks--;
        if (0 < unblockedRow.pendingTasks) break;
        inheritedHoistables = unblockedRow.hoistables;
        unblockedRow = unblockedRow.next;
      }
    }
    function tryToResolveTogetherRow(request, togetherRow) {
      var boundaries = togetherRow.boundaries;
      if (
        null !== boundaries &&
        togetherRow.pendingTasks === boundaries.length
      ) {
        for (
          var allCompleteAndInlinable = true, i = 0;
          i < boundaries.length;
          i++
        ) {
          var rowBoundary = boundaries[i];
          if (
            1 !== rowBoundary.pendingTasks ||
            rowBoundary.parentFlushed ||
            isEligibleForOutlining(request, rowBoundary)
          ) {
            allCompleteAndInlinable = false;
            break;
          }
        }
        allCompleteAndInlinable &&
          unblockSuspenseListRow(request, togetherRow, togetherRow.hoistables);
      }
    }
    function createSuspenseListRow(previousRow) {
      var newRow = {
        pendingTasks: 1,
        boundaries: null,
        hoistables: createHoistableState(),
        inheritedHoistables: null,
        together: false,
        next: null,
      };
      null !== previousRow &&
        0 < previousRow.pendingTasks &&
        (newRow.pendingTasks++,
        (newRow.boundaries = []),
        (previousRow.next = newRow));
      return newRow;
    }
    function renderSuspenseListRows(request, task, keyPath, rows, revealOrder) {
      var prevKeyPath = task.keyPath,
        prevTreeContext = task.treeContext,
        prevRow = task.row;
      task.keyPath = keyPath;
      keyPath = rows.length;
      var previousSuspenseListRow = null;
      if (null !== task.replay) {
        var resumeSlots = task.replay.slots;
        if (null !== resumeSlots && "object" === typeof resumeSlots)
          for (var n = 0; n < keyPath; n++) {
            var i =
                "backwards" !== revealOrder &&
                "unstable_legacy-backwards" !== revealOrder
                  ? n
                  : keyPath - 1 - n,
              node2 = rows[i];
            task.row = previousSuspenseListRow = createSuspenseListRow(
              previousSuspenseListRow,
            );
            task.treeContext = pushTreeContext(prevTreeContext, keyPath, i);
            var resumeSegmentID = resumeSlots[i];
            "number" === typeof resumeSegmentID
              ? (resumeNode(request, task, resumeSegmentID, node2, i),
                delete resumeSlots[i])
              : renderNode(request, task, node2, i);
            0 === --previousSuspenseListRow.pendingTasks &&
              finishSuspenseListRow(request, previousSuspenseListRow);
          }
        else
          for (resumeSlots = 0; resumeSlots < keyPath; resumeSlots++)
            ((n =
              "backwards" !== revealOrder &&
              "unstable_legacy-backwards" !== revealOrder
                ? resumeSlots
                : keyPath - 1 - resumeSlots),
              (i = rows[n]),
              (task.row = previousSuspenseListRow =
                createSuspenseListRow(previousSuspenseListRow)),
              (task.treeContext = pushTreeContext(prevTreeContext, keyPath, n)),
              renderNode(request, task, i, n),
              0 === --previousSuspenseListRow.pendingTasks &&
                finishSuspenseListRow(request, previousSuspenseListRow));
      } else if (
        "backwards" !== revealOrder &&
        "unstable_legacy-backwards" !== revealOrder
      )
        for (revealOrder = 0; revealOrder < keyPath; revealOrder++)
          ((resumeSlots = rows[revealOrder]),
            (task.row = previousSuspenseListRow =
              createSuspenseListRow(previousSuspenseListRow)),
            (task.treeContext = pushTreeContext(
              prevTreeContext,
              keyPath,
              revealOrder,
            )),
            renderNode(request, task, resumeSlots, revealOrder),
            0 === --previousSuspenseListRow.pendingTasks &&
              finishSuspenseListRow(request, previousSuspenseListRow));
      else {
        revealOrder = task.blockedSegment;
        resumeSlots = revealOrder.children.length;
        n = revealOrder.chunks.length;
        for (i = keyPath - 1; 0 <= i; i--) {
          node2 = rows[i];
          task.row = previousSuspenseListRow = createSuspenseListRow(
            previousSuspenseListRow,
          );
          task.treeContext = pushTreeContext(prevTreeContext, keyPath, i);
          resumeSegmentID = createPendingSegment(
            request,
            n,
            null,
            task.formatContext,
            0 === i ? revealOrder.lastPushedText : true,
            true,
          );
          revealOrder.children.splice(resumeSlots, 0, resumeSegmentID);
          task.blockedSegment = resumeSegmentID;
          try {
            (renderNode(request, task, node2, i),
              pushSegmentFinale(
                resumeSegmentID.chunks,
                request.renderState,
                resumeSegmentID.lastPushedText,
                resumeSegmentID.textEmbedded,
              ),
              (resumeSegmentID.status = 1),
              0 === --previousSuspenseListRow.pendingTasks &&
                finishSuspenseListRow(request, previousSuspenseListRow));
          } catch (thrownValue) {
            throw (
              (resumeSegmentID.status = 12 === request.status ? 3 : 4),
              thrownValue
            );
          }
        }
        task.blockedSegment = revealOrder;
        revealOrder.lastPushedText = false;
      }
      null !== prevRow &&
        null !== previousSuspenseListRow &&
        0 < previousSuspenseListRow.pendingTasks &&
        (prevRow.pendingTasks++, (previousSuspenseListRow.next = prevRow));
      task.treeContext = prevTreeContext;
      task.row = prevRow;
      task.keyPath = prevKeyPath;
    }
    function renderWithHooks(
      request,
      task,
      keyPath,
      Component,
      props,
      secondArg,
    ) {
      var prevThenableState = task.thenableState;
      task.thenableState = null;
      currentlyRenderingComponent = {};
      currentlyRenderingTask = task;
      currentlyRenderingRequest = request;
      currentlyRenderingKeyPath = keyPath;
      actionStateCounter = localIdCounter = 0;
      actionStateMatchingIndex = -1;
      thenableIndexCounter = 0;
      thenableState = prevThenableState;
      for (request = Component(props, secondArg); didScheduleRenderPhaseUpdate;)
        ((didScheduleRenderPhaseUpdate = false),
          (actionStateCounter = localIdCounter = 0),
          (actionStateMatchingIndex = -1),
          (thenableIndexCounter = 0),
          (numberOfReRenders += 1),
          (workInProgressHook = null),
          (request = Component(props, secondArg)));
      resetHooksState();
      return request;
    }
    function finishFunctionComponent(
      request,
      task,
      keyPath,
      children,
      hasId,
      actionStateCount,
      actionStateMatchingIndex2,
    ) {
      var didEmitActionStateMarkers = false;
      if (0 !== actionStateCount && null !== request.formState) {
        var segment = task.blockedSegment;
        if (null !== segment) {
          didEmitActionStateMarkers = true;
          segment = segment.chunks;
          for (var i = 0; i < actionStateCount; i++)
            i === actionStateMatchingIndex2
              ? segment.push("<!--F!-->")
              : segment.push("<!--F-->");
        }
      }
      actionStateCount = task.keyPath;
      task.keyPath = keyPath;
      hasId
        ? ((keyPath = task.treeContext),
          (task.treeContext = pushTreeContext(keyPath, 1, 0)),
          renderNode(request, task, children, -1),
          (task.treeContext = keyPath))
        : didEmitActionStateMarkers
          ? renderNode(request, task, children, -1)
          : renderNodeDestructive(request, task, children, -1);
      task.keyPath = actionStateCount;
    }
    function renderElement(request, task, keyPath, type, props, ref) {
      if ("function" === typeof type)
        if (type.prototype && type.prototype.isReactComponent) {
          var newProps = props;
          if ("ref" in props) {
            newProps = {};
            for (var propName in props)
              "ref" !== propName && (newProps[propName] = props[propName]);
          }
          var defaultProps2 = type.defaultProps;
          if (defaultProps2) {
            newProps === props && (newProps = assign2({}, newProps, props));
            for (var propName$43 in defaultProps2)
              void 0 === newProps[propName$43] &&
                (newProps[propName$43] = defaultProps2[propName$43]);
          }
          props = newProps;
          newProps = emptyContextObject;
          defaultProps2 = type.contextType;
          "object" === typeof defaultProps2 &&
            null !== defaultProps2 &&
            (newProps = defaultProps2._currentValue2);
          newProps = new type(props, newProps);
          var initialState = void 0 !== newProps.state ? newProps.state : null;
          newProps.updater = classComponentUpdater;
          newProps.props = props;
          newProps.state = initialState;
          defaultProps2 = { queue: [], replace: false };
          newProps._reactInternals = defaultProps2;
          ref = type.contextType;
          newProps.context =
            "object" === typeof ref && null !== ref
              ? ref._currentValue2
              : emptyContextObject;
          ref = type.getDerivedStateFromProps;
          "function" === typeof ref &&
            ((ref = ref(props, initialState)),
            (initialState =
              null === ref || void 0 === ref
                ? initialState
                : assign2({}, initialState, ref)),
            (newProps.state = initialState));
          if (
            "function" !== typeof type.getDerivedStateFromProps &&
            "function" !== typeof newProps.getSnapshotBeforeUpdate &&
            ("function" === typeof newProps.UNSAFE_componentWillMount ||
              "function" === typeof newProps.componentWillMount)
          )
            if (
              ((type = newProps.state),
              "function" === typeof newProps.componentWillMount &&
                newProps.componentWillMount(),
              "function" === typeof newProps.UNSAFE_componentWillMount &&
                newProps.UNSAFE_componentWillMount(),
              type !== newProps.state &&
                classComponentUpdater.enqueueReplaceState(
                  newProps,
                  newProps.state,
                  null,
                ),
              null !== defaultProps2.queue && 0 < defaultProps2.queue.length)
            )
              if (
                ((type = defaultProps2.queue),
                (ref = defaultProps2.replace),
                (defaultProps2.queue = null),
                (defaultProps2.replace = false),
                ref && 1 === type.length)
              )
                newProps.state = type[0];
              else {
                defaultProps2 = ref ? type[0] : newProps.state;
                initialState = true;
                for (ref = ref ? 1 : 0; ref < type.length; ref++)
                  ((propName$43 = type[ref]),
                    (propName$43 =
                      "function" === typeof propName$43
                        ? propName$43.call(
                            newProps,
                            defaultProps2,
                            props,
                            void 0,
                          )
                        : propName$43),
                    null != propName$43 &&
                      (initialState
                        ? ((initialState = false),
                          (defaultProps2 = assign2(
                            {},
                            defaultProps2,
                            propName$43,
                          )))
                        : assign2(defaultProps2, propName$43)));
                newProps.state = defaultProps2;
              }
            else defaultProps2.queue = null;
          type = newProps.render();
          if (12 === request.status) throw null;
          props = task.keyPath;
          task.keyPath = keyPath;
          renderNodeDestructive(request, task, type, -1);
          task.keyPath = props;
        } else {
          type = renderWithHooks(request, task, keyPath, type, props, void 0);
          if (12 === request.status) throw null;
          finishFunctionComponent(
            request,
            task,
            keyPath,
            type,
            0 !== localIdCounter,
            actionStateCounter,
            actionStateMatchingIndex,
          );
        }
      else if ("string" === typeof type)
        if (((newProps = task.blockedSegment), null === newProps))
          ((newProps = props.children),
            (defaultProps2 = task.formatContext),
            (initialState = task.keyPath),
            (task.formatContext = getChildFormatContext(
              defaultProps2,
              type,
              props,
            )),
            (task.keyPath = keyPath),
            renderNode(request, task, newProps, -1),
            (task.formatContext = defaultProps2),
            (task.keyPath = initialState));
        else {
          initialState = pushStartInstance(
            newProps.chunks,
            type,
            props,
            request.resumableState,
            request.renderState,
            task.blockedPreamble,
            task.hoistableState,
            task.formatContext,
            newProps.lastPushedText,
          );
          newProps.lastPushedText = false;
          defaultProps2 = task.formatContext;
          ref = task.keyPath;
          task.keyPath = keyPath;
          if (
            3 ===
            (task.formatContext = getChildFormatContext(
              defaultProps2,
              type,
              props,
            )).insertionMode
          ) {
            keyPath = createPendingSegment(
              request,
              0,
              null,
              task.formatContext,
              false,
              false,
            );
            newProps.preambleChildren.push(keyPath);
            task.blockedSegment = keyPath;
            try {
              ((keyPath.status = 6),
                renderNode(request, task, initialState, -1),
                pushSegmentFinale(
                  keyPath.chunks,
                  request.renderState,
                  keyPath.lastPushedText,
                  keyPath.textEmbedded,
                ),
                (keyPath.status = 1));
            } finally {
              task.blockedSegment = newProps;
            }
          } else renderNode(request, task, initialState, -1);
          task.formatContext = defaultProps2;
          task.keyPath = ref;
          a: {
            task = newProps.chunks;
            request = request.resumableState;
            switch (type) {
              case "title":
              case "style":
              case "script":
              case "area":
              case "base":
              case "br":
              case "col":
              case "embed":
              case "hr":
              case "img":
              case "input":
              case "keygen":
              case "link":
              case "meta":
              case "param":
              case "source":
              case "track":
              case "wbr":
                break a;
              case "body":
                if (1 >= defaultProps2.insertionMode) {
                  request.hasBody = true;
                  break a;
                }
                break;
              case "html":
                if (0 === defaultProps2.insertionMode) {
                  request.hasHtml = true;
                  break a;
                }
                break;
              case "head":
                if (1 >= defaultProps2.insertionMode) break a;
            }
            task.push(endChunkForTag(type));
          }
          newProps.lastPushedText = false;
        }
      else {
        switch (type) {
          case REACT_LEGACY_HIDDEN_TYPE:
          case REACT_STRICT_MODE_TYPE:
          case REACT_PROFILER_TYPE:
          case REACT_FRAGMENT_TYPE:
            type = task.keyPath;
            task.keyPath = keyPath;
            renderNodeDestructive(request, task, props.children, -1);
            task.keyPath = type;
            return;
          case REACT_ACTIVITY_TYPE:
            type = task.blockedSegment;
            null === type
              ? "hidden" !== props.mode &&
                ((type = task.keyPath),
                (task.keyPath = keyPath),
                renderNode(request, task, props.children, -1),
                (task.keyPath = type))
              : "hidden" !== props.mode &&
                (request.renderState.generateStaticMarkup ||
                  type.chunks.push("<!--&-->"),
                (type.lastPushedText = false),
                (newProps = task.keyPath),
                (task.keyPath = keyPath),
                renderNode(request, task, props.children, -1),
                (task.keyPath = newProps),
                request.renderState.generateStaticMarkup ||
                  type.chunks.push("<!--/&-->"),
                (type.lastPushedText = false));
            return;
          case REACT_SUSPENSE_LIST_TYPE:
            a: {
              type = props.children;
              props = props.revealOrder;
              if (
                "forwards" === props ||
                "backwards" === props ||
                "unstable_legacy-backwards" === props
              ) {
                if (isArrayImpl(type)) {
                  renderSuspenseListRows(request, task, keyPath, type, props);
                  break a;
                }
                if ((newProps = getIteratorFn(type))) {
                  if ((newProps = newProps.call(type))) {
                    defaultProps2 = newProps.next();
                    if (!defaultProps2.done) {
                      do defaultProps2 = newProps.next();
                      while (!defaultProps2.done);
                      renderSuspenseListRows(
                        request,
                        task,
                        keyPath,
                        type,
                        props,
                      );
                    }
                    break a;
                  }
                }
              }
              "together" === props
                ? ((props = task.keyPath),
                  (newProps = task.row),
                  (defaultProps2 = task.row = createSuspenseListRow(null)),
                  (defaultProps2.boundaries = []),
                  (defaultProps2.together = true),
                  (task.keyPath = keyPath),
                  renderNodeDestructive(request, task, type, -1),
                  0 === --defaultProps2.pendingTasks &&
                    finishSuspenseListRow(request, defaultProps2),
                  (task.keyPath = props),
                  (task.row = newProps),
                  null !== newProps &&
                    0 < defaultProps2.pendingTasks &&
                    (newProps.pendingTasks++, (defaultProps2.next = newProps)))
                : ((props = task.keyPath),
                  (task.keyPath = keyPath),
                  renderNodeDestructive(request, task, type, -1),
                  (task.keyPath = props));
            }
            return;
          case REACT_VIEW_TRANSITION_TYPE:
          case REACT_SCOPE_TYPE:
            throw Error(
              "ReactDOMServer does not yet support scope components.",
            );
          case REACT_SUSPENSE_TYPE:
            a: if (null !== task.replay) {
              type = task.keyPath;
              newProps = task.formatContext;
              defaultProps2 = task.row;
              task.keyPath = keyPath;
              task.formatContext = getSuspenseContentFormatContext(
                request.resumableState,
                newProps,
              );
              task.row = null;
              keyPath = props.children;
              try {
                renderNode(request, task, keyPath, -1);
              } finally {
                ((task.keyPath = type),
                  (task.formatContext = newProps),
                  (task.row = defaultProps2));
              }
            } else {
              type = task.keyPath;
              ref = task.formatContext;
              var prevRow = task.row,
                parentBoundary = task.blockedBoundary;
              propName$43 = task.blockedPreamble;
              var parentHoistableState = task.hoistableState;
              propName = task.blockedSegment;
              var fallback = props.fallback;
              props = props.children;
              var fallbackAbortSet = /* @__PURE__ */ new Set();
              var newBoundary = createSuspenseBoundary(
                request,
                task.row,
                fallbackAbortSet,
                null,
                null,
              );
              null !== request.trackedPostpones &&
                (newBoundary.trackedContentKeyPath = keyPath);
              var boundarySegment = createPendingSegment(
                request,
                propName.chunks.length,
                newBoundary,
                task.formatContext,
                false,
                false,
              );
              propName.children.push(boundarySegment);
              propName.lastPushedText = false;
              var contentRootSegment = createPendingSegment(
                request,
                0,
                null,
                task.formatContext,
                false,
                false,
              );
              contentRootSegment.parentFlushed = true;
              if (null !== request.trackedPostpones) {
                newProps = task.componentStack;
                defaultProps2 = [keyPath[0], "Suspense Fallback", keyPath[2]];
                initialState = [defaultProps2[1], defaultProps2[2], [], null];
                request.trackedPostpones.workingMap.set(
                  defaultProps2,
                  initialState,
                );
                newBoundary.trackedFallbackNode = initialState;
                task.blockedSegment = boundarySegment;
                task.blockedPreamble = newBoundary.fallbackPreamble;
                task.keyPath = defaultProps2;
                task.formatContext = getSuspenseFallbackFormatContext(
                  request.resumableState,
                  ref,
                );
                task.componentStack =
                  replaceSuspenseComponentStackWithSuspenseFallbackStack(
                    newProps,
                  );
                boundarySegment.status = 6;
                try {
                  (renderNode(request, task, fallback, -1),
                    pushSegmentFinale(
                      boundarySegment.chunks,
                      request.renderState,
                      boundarySegment.lastPushedText,
                      boundarySegment.textEmbedded,
                    ),
                    (boundarySegment.status = 1));
                } catch (thrownValue) {
                  throw (
                    (boundarySegment.status = 12 === request.status ? 3 : 4),
                    thrownValue
                  );
                } finally {
                  ((task.blockedSegment = propName),
                    (task.blockedPreamble = propName$43),
                    (task.keyPath = type),
                    (task.formatContext = ref));
                }
                task = createRenderTask(
                  request,
                  null,
                  props,
                  -1,
                  newBoundary,
                  contentRootSegment,
                  newBoundary.contentPreamble,
                  newBoundary.contentState,
                  task.abortSet,
                  keyPath,
                  getSuspenseContentFormatContext(
                    request.resumableState,
                    task.formatContext,
                  ),
                  task.context,
                  task.treeContext,
                  null,
                  newProps,
                );
                pushComponentStack(task);
                request.pingedTasks.push(task);
              } else {
                task.blockedBoundary = newBoundary;
                task.blockedPreamble = newBoundary.contentPreamble;
                task.hoistableState = newBoundary.contentState;
                task.blockedSegment = contentRootSegment;
                task.keyPath = keyPath;
                task.formatContext = getSuspenseContentFormatContext(
                  request.resumableState,
                  ref,
                );
                task.row = null;
                contentRootSegment.status = 6;
                try {
                  if (
                    (renderNode(request, task, props, -1),
                    pushSegmentFinale(
                      contentRootSegment.chunks,
                      request.renderState,
                      contentRootSegment.lastPushedText,
                      contentRootSegment.textEmbedded,
                    ),
                    (contentRootSegment.status = 1),
                    queueCompletedSegment(newBoundary, contentRootSegment),
                    0 === newBoundary.pendingTasks && 0 === newBoundary.status)
                  ) {
                    if (
                      ((newBoundary.status = 1),
                      !isEligibleForOutlining(request, newBoundary))
                    ) {
                      null !== prevRow &&
                        0 === --prevRow.pendingTasks &&
                        finishSuspenseListRow(request, prevRow);
                      0 === request.pendingRootTasks &&
                        task.blockedPreamble &&
                        preparePreamble(request);
                      break a;
                    }
                  } else
                    null !== prevRow &&
                      prevRow.together &&
                      tryToResolveTogetherRow(request, prevRow);
                } catch (thrownValue$30) {
                  ((newBoundary.status = 4),
                    12 === request.status
                      ? ((contentRootSegment.status = 3),
                        (newProps = request.fatalError))
                      : ((contentRootSegment.status = 4),
                        (newProps = thrownValue$30)),
                    (defaultProps2 = getThrownInfo(task.componentStack)),
                    (initialState = logRecoverableError(
                      request,
                      newProps,
                      defaultProps2,
                    )),
                    (newBoundary.errorDigest = initialState),
                    untrackBoundary(request, newBoundary));
                } finally {
                  ((task.blockedBoundary = parentBoundary),
                    (task.blockedPreamble = propName$43),
                    (task.hoistableState = parentHoistableState),
                    (task.blockedSegment = propName),
                    (task.keyPath = type),
                    (task.formatContext = ref),
                    (task.row = prevRow));
                }
                task = createRenderTask(
                  request,
                  null,
                  fallback,
                  -1,
                  parentBoundary,
                  boundarySegment,
                  newBoundary.fallbackPreamble,
                  newBoundary.fallbackState,
                  fallbackAbortSet,
                  [keyPath[0], "Suspense Fallback", keyPath[2]],
                  getSuspenseFallbackFormatContext(
                    request.resumableState,
                    task.formatContext,
                  ),
                  task.context,
                  task.treeContext,
                  task.row,
                  replaceSuspenseComponentStackWithSuspenseFallbackStack(
                    task.componentStack,
                  ),
                );
                pushComponentStack(task);
                request.pingedTasks.push(task);
              }
            }
            return;
        }
        if ("object" === typeof type && null !== type)
          switch (type.$$typeof) {
            case REACT_FORWARD_REF_TYPE:
              if ("ref" in props)
                for (fallback in ((newProps = {}), props))
                  "ref" !== fallback && (newProps[fallback] = props[fallback]);
              else newProps = props;
              type = renderWithHooks(
                request,
                task,
                keyPath,
                type.render,
                newProps,
                ref,
              );
              finishFunctionComponent(
                request,
                task,
                keyPath,
                type,
                0 !== localIdCounter,
                actionStateCounter,
                actionStateMatchingIndex,
              );
              return;
            case REACT_MEMO_TYPE:
              renderElement(request, task, keyPath, type.type, props, ref);
              return;
            case REACT_CONTEXT_TYPE:
              defaultProps2 = props.children;
              newProps = task.keyPath;
              props = props.value;
              initialState = type._currentValue2;
              type._currentValue2 = props;
              ref = currentActiveSnapshot;
              currentActiveSnapshot = type = {
                parent: ref,
                depth: null === ref ? 0 : ref.depth + 1,
                context: type,
                parentValue: initialState,
                value: props,
              };
              task.context = type;
              task.keyPath = keyPath;
              renderNodeDestructive(request, task, defaultProps2, -1);
              request = currentActiveSnapshot;
              if (null === request)
                throw Error(
                  "Tried to pop a Context at the root of the app. This is a bug in React.",
                );
              request.context._currentValue2 = request.parentValue;
              request = currentActiveSnapshot = request.parent;
              task.context = request;
              task.keyPath = newProps;
              return;
            case REACT_CONSUMER_TYPE:
              props = props.children;
              type = props(type._context._currentValue2);
              props = task.keyPath;
              task.keyPath = keyPath;
              renderNodeDestructive(request, task, type, -1);
              task.keyPath = props;
              return;
            case REACT_LAZY_TYPE:
              newProps = type._init;
              type = newProps(type._payload);
              if (12 === request.status) throw null;
              renderElement(request, task, keyPath, type, props, ref);
              return;
          }
        throw Error(
          "Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) but got: " +
            ((null == type ? type : typeof type) + "."),
        );
      }
    }
    function resumeNode(request, task, segmentId, node2, childIndex) {
      var prevReplay = task.replay,
        blockedBoundary = task.blockedBoundary,
        resumedSegment = createPendingSegment(
          request,
          0,
          null,
          task.formatContext,
          false,
          false,
        );
      resumedSegment.id = segmentId;
      resumedSegment.parentFlushed = true;
      try {
        ((task.replay = null),
          (task.blockedSegment = resumedSegment),
          renderNode(request, task, node2, childIndex),
          (resumedSegment.status = 1),
          null === blockedBoundary
            ? (request.completedRootSegment = resumedSegment)
            : (queueCompletedSegment(blockedBoundary, resumedSegment),
              blockedBoundary.parentFlushed &&
                request.partialBoundaries.push(blockedBoundary)));
      } finally {
        ((task.replay = prevReplay), (task.blockedSegment = null));
      }
    }
    function renderNodeDestructive(request, task, node2, childIndex) {
      null !== task.replay && "number" === typeof task.replay.slots
        ? resumeNode(request, task, task.replay.slots, node2, childIndex)
        : ((task.node = node2),
          (task.childIndex = childIndex),
          (node2 = task.componentStack),
          pushComponentStack(task),
          retryNode(request, task),
          (task.componentStack = node2));
    }
    function retryNode(request, task) {
      var node2 = task.node,
        childIndex = task.childIndex;
      if (null !== node2) {
        if ("object" === typeof node2) {
          switch (node2.$$typeof) {
            case REACT_ELEMENT_TYPE:
              var type = node2.type,
                key = node2.key,
                props = node2.props;
              node2 = props.ref;
              var ref = void 0 !== node2 ? node2 : null,
                name = getComponentNameFromType(type),
                keyOrIndex =
                  null == key ? (-1 === childIndex ? 0 : childIndex) : key;
              key = [task.keyPath, name, keyOrIndex];
              if (null !== task.replay)
                a: {
                  var replay = task.replay;
                  childIndex = replay.nodes;
                  for (node2 = 0; node2 < childIndex.length; node2++) {
                    var node$jscomp$0 = childIndex[node2];
                    if (keyOrIndex === node$jscomp$0[1]) {
                      if (4 === node$jscomp$0.length) {
                        if (null !== name && name !== node$jscomp$0[0])
                          throw Error(
                            "Expected the resume to render <" +
                              node$jscomp$0[0] +
                              "> in this slot but instead it rendered <" +
                              name +
                              ">. The tree doesn't match so React will fallback to client rendering.",
                          );
                        var childNodes = node$jscomp$0[2];
                        name = node$jscomp$0[3];
                        keyOrIndex = task.node;
                        task.replay = {
                          nodes: childNodes,
                          slots: name,
                          pendingTasks: 1,
                        };
                        try {
                          renderElement(request, task, key, type, props, ref);
                          if (
                            1 === task.replay.pendingTasks &&
                            0 < task.replay.nodes.length
                          )
                            throw Error(
                              "Couldn't find all resumable slots by key/index during replaying. The tree doesn't match so React will fallback to client rendering.",
                            );
                          task.replay.pendingTasks--;
                        } catch (x) {
                          if (
                            "object" === typeof x &&
                            null !== x &&
                            (x === SuspenseException ||
                              "function" === typeof x.then)
                          )
                            throw (
                              task.node === keyOrIndex
                                ? (task.replay = replay)
                                : childIndex.splice(node2, 1),
                              x
                            );
                          task.replay.pendingTasks--;
                          props = getThrownInfo(task.componentStack);
                          key = request;
                          request = task.blockedBoundary;
                          type = x;
                          props = logRecoverableError(key, type, props);
                          abortRemainingReplayNodes(
                            key,
                            request,
                            childNodes,
                            name,
                            type,
                            props,
                          );
                        }
                        task.replay = replay;
                      } else {
                        if (type !== REACT_SUSPENSE_TYPE)
                          throw Error(
                            "Expected the resume to render <Suspense> in this slot but instead it rendered <" +
                              (getComponentNameFromType(type) || "Unknown") +
                              ">. The tree doesn't match so React will fallback to client rendering.",
                          );
                        b: {
                          replay = void 0;
                          type = node$jscomp$0[5];
                          ref = node$jscomp$0[2];
                          name = node$jscomp$0[3];
                          keyOrIndex =
                            null === node$jscomp$0[4]
                              ? []
                              : node$jscomp$0[4][2];
                          node$jscomp$0 =
                            null === node$jscomp$0[4]
                              ? null
                              : node$jscomp$0[4][3];
                          var prevKeyPath = task.keyPath,
                            prevContext = task.formatContext,
                            prevRow = task.row,
                            previousReplaySet = task.replay,
                            parentBoundary = task.blockedBoundary,
                            parentHoistableState = task.hoistableState,
                            content = props.children,
                            fallback = props.fallback,
                            fallbackAbortSet = /* @__PURE__ */ new Set();
                          props = createSuspenseBoundary(
                            request,
                            task.row,
                            fallbackAbortSet,
                            null,
                            null,
                          );
                          props.parentFlushed = true;
                          props.rootSegmentID = type;
                          task.blockedBoundary = props;
                          task.hoistableState = props.contentState;
                          task.keyPath = key;
                          task.formatContext = getSuspenseContentFormatContext(
                            request.resumableState,
                            prevContext,
                          );
                          task.row = null;
                          task.replay = {
                            nodes: ref,
                            slots: name,
                            pendingTasks: 1,
                          };
                          try {
                            renderNode(request, task, content, -1);
                            if (
                              1 === task.replay.pendingTasks &&
                              0 < task.replay.nodes.length
                            )
                              throw Error(
                                "Couldn't find all resumable slots by key/index during replaying. The tree doesn't match so React will fallback to client rendering.",
                              );
                            task.replay.pendingTasks--;
                            if (
                              0 === props.pendingTasks &&
                              0 === props.status
                            ) {
                              props.status = 1;
                              request.completedBoundaries.push(props);
                              break b;
                            }
                          } catch (error) {
                            ((props.status = 4),
                              (childNodes = getThrownInfo(task.componentStack)),
                              (replay = logRecoverableError(
                                request,
                                error,
                                childNodes,
                              )),
                              (props.errorDigest = replay),
                              task.replay.pendingTasks--,
                              request.clientRenderedBoundaries.push(props));
                          } finally {
                            ((task.blockedBoundary = parentBoundary),
                              (task.hoistableState = parentHoistableState),
                              (task.replay = previousReplaySet),
                              (task.keyPath = prevKeyPath),
                              (task.formatContext = prevContext),
                              (task.row = prevRow));
                          }
                          childNodes = createReplayTask(
                            request,
                            null,
                            {
                              nodes: keyOrIndex,
                              slots: node$jscomp$0,
                              pendingTasks: 0,
                            },
                            fallback,
                            -1,
                            parentBoundary,
                            props.fallbackState,
                            fallbackAbortSet,
                            [key[0], "Suspense Fallback", key[2]],
                            getSuspenseFallbackFormatContext(
                              request.resumableState,
                              task.formatContext,
                            ),
                            task.context,
                            task.treeContext,
                            task.row,
                            replaceSuspenseComponentStackWithSuspenseFallbackStack(
                              task.componentStack,
                            ),
                          );
                          pushComponentStack(childNodes);
                          request.pingedTasks.push(childNodes);
                        }
                      }
                      childIndex.splice(node2, 1);
                      break a;
                    }
                  }
                }
              else renderElement(request, task, key, type, props, ref);
              return;
            case REACT_PORTAL_TYPE:
              throw Error(
                "Portals are not currently supported by the server renderer. Render them conditionally so that they only appear on the client render.",
              );
            case REACT_LAZY_TYPE:
              childNodes = node2._init;
              node2 = childNodes(node2._payload);
              if (12 === request.status) throw null;
              renderNodeDestructive(request, task, node2, childIndex);
              return;
          }
          if (isArrayImpl(node2)) {
            renderChildrenArray(request, task, node2, childIndex);
            return;
          }
          if ((childNodes = getIteratorFn(node2))) {
            if ((childNodes = childNodes.call(node2))) {
              node2 = childNodes.next();
              if (!node2.done) {
                props = [];
                do (props.push(node2.value), (node2 = childNodes.next()));
                while (!node2.done);
                renderChildrenArray(request, task, props, childIndex);
              }
              return;
            }
          }
          if ("function" === typeof node2.then)
            return (
              (task.thenableState = null),
              renderNodeDestructive(
                request,
                task,
                unwrapThenable(node2),
                childIndex,
              )
            );
          if (node2.$$typeof === REACT_CONTEXT_TYPE)
            return renderNodeDestructive(
              request,
              task,
              node2._currentValue2,
              childIndex,
            );
          childIndex = Object.prototype.toString.call(node2);
          throw Error(
            "Objects are not valid as a React child (found: " +
              ("[object Object]" === childIndex
                ? "object with keys {" + Object.keys(node2).join(", ") + "}"
                : childIndex) +
              "). If you meant to render a collection of children, use an array instead.",
          );
        }
        if ("string" === typeof node2)
          ((childIndex = task.blockedSegment),
            null !== childIndex &&
              (childIndex.lastPushedText = pushTextInstance(
                childIndex.chunks,
                node2,
                request.renderState,
                childIndex.lastPushedText,
              )));
        else if ("number" === typeof node2 || "bigint" === typeof node2)
          ((childIndex = task.blockedSegment),
            null !== childIndex &&
              (childIndex.lastPushedText = pushTextInstance(
                childIndex.chunks,
                "" + node2,
                request.renderState,
                childIndex.lastPushedText,
              )));
      }
    }
    function renderChildrenArray(request, task, children, childIndex) {
      var prevKeyPath = task.keyPath;
      if (
        -1 !== childIndex &&
        ((task.keyPath = [task.keyPath, "Fragment", childIndex]),
        null !== task.replay)
      ) {
        for (
          var replay = task.replay, replayNodes = replay.nodes, j = 0;
          j < replayNodes.length;
          j++
        ) {
          var node2 = replayNodes[j];
          if (node2[1] === childIndex) {
            childIndex = node2[2];
            node2 = node2[3];
            task.replay = { nodes: childIndex, slots: node2, pendingTasks: 1 };
            try {
              renderChildrenArray(request, task, children, -1);
              if (
                1 === task.replay.pendingTasks &&
                0 < task.replay.nodes.length
              )
                throw Error(
                  "Couldn't find all resumable slots by key/index during replaying. The tree doesn't match so React will fallback to client rendering.",
                );
              task.replay.pendingTasks--;
            } catch (x) {
              if (
                "object" === typeof x &&
                null !== x &&
                (x === SuspenseException || "function" === typeof x.then)
              )
                throw x;
              task.replay.pendingTasks--;
              children = getThrownInfo(task.componentStack);
              var boundary = task.blockedBoundary,
                error = x;
              children = logRecoverableError(request, error, children);
              abortRemainingReplayNodes(
                request,
                boundary,
                childIndex,
                node2,
                error,
                children,
              );
            }
            task.replay = replay;
            replayNodes.splice(j, 1);
            break;
          }
        }
        task.keyPath = prevKeyPath;
        return;
      }
      replay = task.treeContext;
      replayNodes = children.length;
      if (
        null !== task.replay &&
        ((j = task.replay.slots), null !== j && "object" === typeof j)
      ) {
        for (childIndex = 0; childIndex < replayNodes; childIndex++)
          ((node2 = children[childIndex]),
            (task.treeContext = pushTreeContext(
              replay,
              replayNodes,
              childIndex,
            )),
            (boundary = j[childIndex]),
            "number" === typeof boundary
              ? (resumeNode(request, task, boundary, node2, childIndex),
                delete j[childIndex])
              : renderNode(request, task, node2, childIndex));
        task.treeContext = replay;
        task.keyPath = prevKeyPath;
        return;
      }
      for (j = 0; j < replayNodes; j++)
        ((childIndex = children[j]),
          (task.treeContext = pushTreeContext(replay, replayNodes, j)),
          renderNode(request, task, childIndex, j));
      task.treeContext = replay;
      task.keyPath = prevKeyPath;
    }
    function trackPostponedBoundary(request, trackedPostpones, boundary) {
      boundary.status = 5;
      boundary.rootSegmentID = request.nextSegmentId++;
      request = boundary.trackedContentKeyPath;
      if (null === request)
        throw Error(
          "It should not be possible to postpone at the root. This is a bug in React.",
        );
      var fallbackReplayNode = boundary.trackedFallbackNode,
        children = [],
        boundaryNode = trackedPostpones.workingMap.get(request);
      if (void 0 === boundaryNode)
        return (
          (boundary = [
            request[1],
            request[2],
            children,
            null,
            fallbackReplayNode,
            boundary.rootSegmentID,
          ]),
          trackedPostpones.workingMap.set(request, boundary),
          addToReplayParent(boundary, request[0], trackedPostpones),
          boundary
        );
      boundaryNode[4] = fallbackReplayNode;
      boundaryNode[5] = boundary.rootSegmentID;
      return boundaryNode;
    }
    function trackPostpone(request, trackedPostpones, task, segment) {
      segment.status = 5;
      var keyPath = task.keyPath,
        boundary = task.blockedBoundary;
      if (null === boundary)
        ((segment.id = request.nextSegmentId++),
          (trackedPostpones.rootSlots = segment.id),
          null !== request.completedRootSegment &&
            (request.completedRootSegment.status = 5));
      else {
        if (null !== boundary && 0 === boundary.status) {
          var boundaryNode = trackPostponedBoundary(
            request,
            trackedPostpones,
            boundary,
          );
          if (
            boundary.trackedContentKeyPath === keyPath &&
            -1 === task.childIndex
          ) {
            -1 === segment.id &&
              (segment.id = segment.parentFlushed
                ? boundary.rootSegmentID
                : request.nextSegmentId++);
            boundaryNode[3] = segment.id;
            return;
          }
        }
        -1 === segment.id &&
          (segment.id =
            segment.parentFlushed && null !== boundary
              ? boundary.rootSegmentID
              : request.nextSegmentId++);
        if (-1 === task.childIndex)
          null === keyPath
            ? (trackedPostpones.rootSlots = segment.id)
            : ((task = trackedPostpones.workingMap.get(keyPath)),
              void 0 === task
                ? ((task = [keyPath[1], keyPath[2], [], segment.id]),
                  addToReplayParent(task, keyPath[0], trackedPostpones))
                : (task[3] = segment.id));
        else {
          if (null === keyPath)
            if (((request = trackedPostpones.rootSlots), null === request))
              request = trackedPostpones.rootSlots = {};
            else {
              if ("number" === typeof request)
                throw Error(
                  "It should not be possible to postpone both at the root of an element as well as a slot below. This is a bug in React.",
                );
            }
          else if (
            ((boundary = trackedPostpones.workingMap),
            (boundaryNode = boundary.get(keyPath)),
            void 0 === boundaryNode)
          )
            ((request = {}),
              (boundaryNode = [keyPath[1], keyPath[2], [], request]),
              boundary.set(keyPath, boundaryNode),
              addToReplayParent(boundaryNode, keyPath[0], trackedPostpones));
          else if (((request = boundaryNode[3]), null === request))
            request = boundaryNode[3] = {};
          else if ("number" === typeof request)
            throw Error(
              "It should not be possible to postpone both at the root of an element as well as a slot below. This is a bug in React.",
            );
          request[task.childIndex] = segment.id;
        }
      }
    }
    function untrackBoundary(request, boundary) {
      request = request.trackedPostpones;
      null !== request &&
        ((boundary = boundary.trackedContentKeyPath),
        null !== boundary &&
          ((boundary = request.workingMap.get(boundary)),
          void 0 !== boundary &&
            ((boundary.length = 4), (boundary[2] = []), (boundary[3] = null))));
    }
    function spawnNewSuspendedReplayTask(request, task, thenableState2) {
      return createReplayTask(
        request,
        thenableState2,
        task.replay,
        task.node,
        task.childIndex,
        task.blockedBoundary,
        task.hoistableState,
        task.abortSet,
        task.keyPath,
        task.formatContext,
        task.context,
        task.treeContext,
        task.row,
        task.componentStack,
      );
    }
    function spawnNewSuspendedRenderTask(request, task, thenableState2) {
      var segment = task.blockedSegment,
        newSegment = createPendingSegment(
          request,
          segment.chunks.length,
          null,
          task.formatContext,
          segment.lastPushedText,
          true,
        );
      segment.children.push(newSegment);
      segment.lastPushedText = false;
      return createRenderTask(
        request,
        thenableState2,
        task.node,
        task.childIndex,
        task.blockedBoundary,
        newSegment,
        task.blockedPreamble,
        task.hoistableState,
        task.abortSet,
        task.keyPath,
        task.formatContext,
        task.context,
        task.treeContext,
        task.row,
        task.componentStack,
      );
    }
    function renderNode(request, task, node2, childIndex) {
      var previousFormatContext = task.formatContext,
        previousContext = task.context,
        previousKeyPath = task.keyPath,
        previousTreeContext = task.treeContext,
        previousComponentStack = task.componentStack,
        segment = task.blockedSegment;
      if (null === segment) {
        segment = task.replay;
        try {
          return renderNodeDestructive(request, task, node2, childIndex);
        } catch (thrownValue) {
          if (
            (resetHooksState(),
            (node2 =
              thrownValue === SuspenseException
                ? getSuspendedThenable()
                : thrownValue),
            12 !== request.status &&
              "object" === typeof node2 &&
              null !== node2)
          ) {
            if ("function" === typeof node2.then) {
              childIndex =
                thrownValue === SuspenseException
                  ? getThenableStateAfterSuspending()
                  : null;
              request = spawnNewSuspendedReplayTask(
                request,
                task,
                childIndex,
              ).ping;
              node2.then(request, request);
              task.formatContext = previousFormatContext;
              task.context = previousContext;
              task.keyPath = previousKeyPath;
              task.treeContext = previousTreeContext;
              task.componentStack = previousComponentStack;
              task.replay = segment;
              switchContext(previousContext);
              return;
            }
            if ("Maximum call stack size exceeded" === node2.message) {
              node2 =
                thrownValue === SuspenseException
                  ? getThenableStateAfterSuspending()
                  : null;
              node2 = spawnNewSuspendedReplayTask(request, task, node2);
              request.pingedTasks.push(node2);
              task.formatContext = previousFormatContext;
              task.context = previousContext;
              task.keyPath = previousKeyPath;
              task.treeContext = previousTreeContext;
              task.componentStack = previousComponentStack;
              task.replay = segment;
              switchContext(previousContext);
              return;
            }
          }
        }
      } else {
        var childrenLength = segment.children.length,
          chunkLength = segment.chunks.length;
        try {
          return renderNodeDestructive(request, task, node2, childIndex);
        } catch (thrownValue$62) {
          if (
            (resetHooksState(),
            (segment.children.length = childrenLength),
            (segment.chunks.length = chunkLength),
            (node2 =
              thrownValue$62 === SuspenseException
                ? getSuspendedThenable()
                : thrownValue$62),
            12 !== request.status &&
              "object" === typeof node2 &&
              null !== node2)
          ) {
            if ("function" === typeof node2.then) {
              segment = node2;
              node2 =
                thrownValue$62 === SuspenseException
                  ? getThenableStateAfterSuspending()
                  : null;
              request = spawnNewSuspendedRenderTask(request, task, node2).ping;
              segment.then(request, request);
              task.formatContext = previousFormatContext;
              task.context = previousContext;
              task.keyPath = previousKeyPath;
              task.treeContext = previousTreeContext;
              task.componentStack = previousComponentStack;
              switchContext(previousContext);
              return;
            }
            if ("Maximum call stack size exceeded" === node2.message) {
              segment =
                thrownValue$62 === SuspenseException
                  ? getThenableStateAfterSuspending()
                  : null;
              segment = spawnNewSuspendedRenderTask(request, task, segment);
              request.pingedTasks.push(segment);
              task.formatContext = previousFormatContext;
              task.context = previousContext;
              task.keyPath = previousKeyPath;
              task.treeContext = previousTreeContext;
              task.componentStack = previousComponentStack;
              switchContext(previousContext);
              return;
            }
          }
        }
      }
      task.formatContext = previousFormatContext;
      task.context = previousContext;
      task.keyPath = previousKeyPath;
      task.treeContext = previousTreeContext;
      switchContext(previousContext);
      throw node2;
    }
    function abortTaskSoft(task) {
      var boundary = task.blockedBoundary,
        segment = task.blockedSegment;
      null !== segment &&
        ((segment.status = 3), finishedTask(this, boundary, task.row, segment));
    }
    function abortRemainingReplayNodes(
      request$jscomp$0,
      boundary,
      nodes,
      slots,
      error,
      errorDigest$jscomp$0,
    ) {
      for (var i = 0; i < nodes.length; i++) {
        var node2 = nodes[i];
        if (4 === node2.length)
          abortRemainingReplayNodes(
            request$jscomp$0,
            boundary,
            node2[2],
            node2[3],
            error,
            errorDigest$jscomp$0,
          );
        else {
          node2 = node2[5];
          var request = request$jscomp$0,
            errorDigest = errorDigest$jscomp$0,
            resumedBoundary = createSuspenseBoundary(
              request,
              null,
              /* @__PURE__ */ new Set(),
              null,
              null,
            );
          resumedBoundary.parentFlushed = true;
          resumedBoundary.rootSegmentID = node2;
          resumedBoundary.status = 4;
          resumedBoundary.errorDigest = errorDigest;
          resumedBoundary.parentFlushed &&
            request.clientRenderedBoundaries.push(resumedBoundary);
        }
      }
      nodes.length = 0;
      if (null !== slots) {
        if (null === boundary)
          throw Error(
            "We should not have any resumable nodes in the shell. This is a bug in React.",
          );
        4 !== boundary.status &&
          ((boundary.status = 4),
          (boundary.errorDigest = errorDigest$jscomp$0),
          boundary.parentFlushed &&
            request$jscomp$0.clientRenderedBoundaries.push(boundary));
        if ("object" === typeof slots)
          for (var index in slots) delete slots[index];
      }
    }
    function abortTask(task, request, error) {
      var boundary = task.blockedBoundary,
        segment = task.blockedSegment;
      if (null !== segment) {
        if (6 === segment.status) return;
        segment.status = 3;
      }
      var errorInfo = getThrownInfo(task.componentStack);
      if (null === boundary) {
        if (13 !== request.status && 14 !== request.status) {
          boundary = task.replay;
          if (null === boundary) {
            null !== request.trackedPostpones && null !== segment
              ? ((boundary = request.trackedPostpones),
                logRecoverableError(request, error, errorInfo),
                trackPostpone(request, boundary, task, segment),
                finishedTask(request, null, task.row, segment))
              : (logRecoverableError(request, error, errorInfo),
                fatalError(request, error));
            return;
          }
          boundary.pendingTasks--;
          0 === boundary.pendingTasks &&
            0 < boundary.nodes.length &&
            ((segment = logRecoverableError(request, error, errorInfo)),
            abortRemainingReplayNodes(
              request,
              null,
              boundary.nodes,
              boundary.slots,
              error,
              segment,
            ));
          request.pendingRootTasks--;
          0 === request.pendingRootTasks && completeShell(request);
        }
      } else {
        var trackedPostpones$63 = request.trackedPostpones;
        if (4 !== boundary.status) {
          if (null !== trackedPostpones$63 && null !== segment)
            return (
              logRecoverableError(request, error, errorInfo),
              trackPostpone(request, trackedPostpones$63, task, segment),
              boundary.fallbackAbortableTasks.forEach(function (fallbackTask) {
                return abortTask(fallbackTask, request, error);
              }),
              boundary.fallbackAbortableTasks.clear(),
              finishedTask(request, boundary, task.row, segment)
            );
          boundary.status = 4;
          segment = logRecoverableError(request, error, errorInfo);
          boundary.status = 4;
          boundary.errorDigest = segment;
          untrackBoundary(request, boundary);
          boundary.parentFlushed &&
            request.clientRenderedBoundaries.push(boundary);
        }
        boundary.pendingTasks--;
        segment = boundary.row;
        null !== segment &&
          0 === --segment.pendingTasks &&
          finishSuspenseListRow(request, segment);
        boundary.fallbackAbortableTasks.forEach(function (fallbackTask) {
          return abortTask(fallbackTask, request, error);
        });
        boundary.fallbackAbortableTasks.clear();
      }
      task = task.row;
      null !== task &&
        0 === --task.pendingTasks &&
        finishSuspenseListRow(request, task);
      request.allPendingTasks--;
      0 === request.allPendingTasks && completeAll(request);
    }
    function safelyEmitEarlyPreloads(request, shellComplete) {
      try {
        var renderState = request.renderState,
          onHeaders = renderState.onHeaders;
        if (onHeaders) {
          var headers = renderState.headers;
          if (headers) {
            renderState.headers = null;
            var linkHeader = headers.preconnects;
            headers.fontPreloads &&
              (linkHeader && (linkHeader += ", "),
              (linkHeader += headers.fontPreloads));
            headers.highImagePreloads &&
              (linkHeader && (linkHeader += ", "),
              (linkHeader += headers.highImagePreloads));
            if (!shellComplete) {
              var queueIter = renderState.styles.values(),
                queueStep = queueIter.next();
              b: for (
                ;
                0 < headers.remainingCapacity && !queueStep.done;
                queueStep = queueIter.next()
              )
                for (
                  var sheetIter = queueStep.value.sheets.values(),
                    sheetStep = sheetIter.next();
                  0 < headers.remainingCapacity && !sheetStep.done;
                  sheetStep = sheetIter.next()
                ) {
                  var sheet = sheetStep.value,
                    props = sheet.props,
                    key = props.href,
                    props$jscomp$0 = sheet.props,
                    header = getPreloadAsHeader(props$jscomp$0.href, "style", {
                      crossOrigin: props$jscomp$0.crossOrigin,
                      integrity: props$jscomp$0.integrity,
                      nonce: props$jscomp$0.nonce,
                      type: props$jscomp$0.type,
                      fetchPriority: props$jscomp$0.fetchPriority,
                      referrerPolicy: props$jscomp$0.referrerPolicy,
                      media: props$jscomp$0.media,
                    });
                  if (0 <= (headers.remainingCapacity -= header.length + 2))
                    ((renderState.resets.style[key] = PRELOAD_NO_CREDS),
                      linkHeader && (linkHeader += ", "),
                      (linkHeader += header),
                      (renderState.resets.style[key] =
                        "string" === typeof props.crossOrigin ||
                        "string" === typeof props.integrity
                          ? [props.crossOrigin, props.integrity]
                          : PRELOAD_NO_CREDS));
                  else break b;
                }
            }
            linkHeader ? onHeaders({ Link: linkHeader }) : onHeaders({});
          }
        }
      } catch (error) {
        logRecoverableError(request, error, {});
      }
    }
    function completeShell(request) {
      null === request.trackedPostpones &&
        safelyEmitEarlyPreloads(request, true);
      null === request.trackedPostpones && preparePreamble(request);
      request.onShellError = noop5;
      request = request.onShellReady;
      request();
    }
    function completeAll(request) {
      safelyEmitEarlyPreloads(
        request,
        null === request.trackedPostpones
          ? true
          : null === request.completedRootSegment ||
              5 !== request.completedRootSegment.status,
      );
      preparePreamble(request);
      request = request.onAllReady;
      request();
    }
    function queueCompletedSegment(boundary, segment) {
      if (
        0 === segment.chunks.length &&
        1 === segment.children.length &&
        null === segment.children[0].boundary &&
        -1 === segment.children[0].id
      ) {
        var childSegment = segment.children[0];
        childSegment.id = segment.id;
        childSegment.parentFlushed = true;
        (1 !== childSegment.status &&
          3 !== childSegment.status &&
          4 !== childSegment.status) ||
          queueCompletedSegment(boundary, childSegment);
      } else boundary.completedSegments.push(segment);
    }
    function finishedTask(request, boundary, row, segment) {
      null !== row &&
        (0 === --row.pendingTasks
          ? finishSuspenseListRow(request, row)
          : row.together && tryToResolveTogetherRow(request, row));
      request.allPendingTasks--;
      if (null === boundary) {
        if (null !== segment && segment.parentFlushed) {
          if (null !== request.completedRootSegment)
            throw Error(
              "There can only be one root segment. This is a bug in React.",
            );
          request.completedRootSegment = segment;
        }
        request.pendingRootTasks--;
        0 === request.pendingRootTasks && completeShell(request);
      } else if ((boundary.pendingTasks--, 4 !== boundary.status))
        if (0 === boundary.pendingTasks)
          if (
            (0 === boundary.status && (boundary.status = 1),
            null !== segment &&
              segment.parentFlushed &&
              (1 === segment.status || 3 === segment.status) &&
              queueCompletedSegment(boundary, segment),
            boundary.parentFlushed &&
              request.completedBoundaries.push(boundary),
            1 === boundary.status)
          )
            ((row = boundary.row),
              null !== row &&
                hoistHoistables(row.hoistables, boundary.contentState),
              isEligibleForOutlining(request, boundary) ||
                (boundary.fallbackAbortableTasks.forEach(
                  abortTaskSoft,
                  request,
                ),
                boundary.fallbackAbortableTasks.clear(),
                null !== row &&
                  0 === --row.pendingTasks &&
                  finishSuspenseListRow(request, row)),
              0 === request.pendingRootTasks &&
                null === request.trackedPostpones &&
                null !== boundary.contentPreamble &&
                preparePreamble(request));
          else {
            if (
              5 === boundary.status &&
              ((boundary = boundary.row), null !== boundary)
            ) {
              if (null !== request.trackedPostpones) {
                row = request.trackedPostpones;
                var postponedRow = boundary.next;
                if (
                  null !== postponedRow &&
                  ((segment = postponedRow.boundaries), null !== segment)
                )
                  for (
                    postponedRow.boundaries = null, postponedRow = 0;
                    postponedRow < segment.length;
                    postponedRow++
                  ) {
                    var postponedBoundary = segment[postponedRow];
                    trackPostponedBoundary(request, row, postponedBoundary);
                    finishedTask(request, postponedBoundary, null, null);
                  }
              }
              0 === --boundary.pendingTasks &&
                finishSuspenseListRow(request, boundary);
            }
          }
        else
          (null === segment ||
            !segment.parentFlushed ||
            (1 !== segment.status && 3 !== segment.status) ||
            (queueCompletedSegment(boundary, segment),
            1 === boundary.completedSegments.length &&
              boundary.parentFlushed &&
              request.partialBoundaries.push(boundary)),
            (boundary = boundary.row),
            null !== boundary &&
              boundary.together &&
              tryToResolveTogetherRow(request, boundary));
      0 === request.allPendingTasks && completeAll(request);
    }
    function performWork(request$jscomp$2) {
      if (14 !== request$jscomp$2.status && 13 !== request$jscomp$2.status) {
        var prevContext = currentActiveSnapshot,
          prevDispatcher = ReactSharedInternals.H;
        ReactSharedInternals.H = HooksDispatcher;
        var prevAsyncDispatcher = ReactSharedInternals.A;
        ReactSharedInternals.A = DefaultAsyncDispatcher;
        var prevRequest = currentRequest;
        currentRequest = request$jscomp$2;
        var prevResumableState = currentResumableState;
        currentResumableState = request$jscomp$2.resumableState;
        try {
          var pingedTasks = request$jscomp$2.pingedTasks,
            i;
          for (i = 0; i < pingedTasks.length; i++) {
            var task = pingedTasks[i],
              request = request$jscomp$2,
              segment = task.blockedSegment;
            if (null === segment) {
              var request$jscomp$0 = request;
              if (0 !== task.replay.pendingTasks) {
                switchContext(task.context);
                try {
                  "number" === typeof task.replay.slots
                    ? resumeNode(
                        request$jscomp$0,
                        task,
                        task.replay.slots,
                        task.node,
                        task.childIndex,
                      )
                    : retryNode(request$jscomp$0, task);
                  if (
                    1 === task.replay.pendingTasks &&
                    0 < task.replay.nodes.length
                  )
                    throw Error(
                      "Couldn't find all resumable slots by key/index during replaying. The tree doesn't match so React will fallback to client rendering.",
                    );
                  task.replay.pendingTasks--;
                  task.abortSet.delete(task);
                  finishedTask(
                    request$jscomp$0,
                    task.blockedBoundary,
                    task.row,
                    null,
                  );
                } catch (thrownValue) {
                  resetHooksState();
                  var x =
                    thrownValue === SuspenseException
                      ? getSuspendedThenable()
                      : thrownValue;
                  if (
                    "object" === typeof x &&
                    null !== x &&
                    "function" === typeof x.then
                  ) {
                    var ping = task.ping;
                    x.then(ping, ping);
                    task.thenableState =
                      thrownValue === SuspenseException
                        ? getThenableStateAfterSuspending()
                        : null;
                  } else {
                    task.replay.pendingTasks--;
                    task.abortSet.delete(task);
                    var errorInfo = getThrownInfo(task.componentStack);
                    request = void 0;
                    var request$jscomp$1 = request$jscomp$0,
                      boundary = task.blockedBoundary,
                      error$jscomp$0 =
                        12 === request$jscomp$0.status
                          ? request$jscomp$0.fatalError
                          : x,
                      replayNodes = task.replay.nodes,
                      resumeSlots = task.replay.slots;
                    request = logRecoverableError(
                      request$jscomp$1,
                      error$jscomp$0,
                      errorInfo,
                    );
                    abortRemainingReplayNodes(
                      request$jscomp$1,
                      boundary,
                      replayNodes,
                      resumeSlots,
                      error$jscomp$0,
                      request,
                    );
                    request$jscomp$0.pendingRootTasks--;
                    0 === request$jscomp$0.pendingRootTasks &&
                      completeShell(request$jscomp$0);
                    request$jscomp$0.allPendingTasks--;
                    0 === request$jscomp$0.allPendingTasks &&
                      completeAll(request$jscomp$0);
                  }
                } finally {
                }
              }
            } else if (
              ((request$jscomp$0 = void 0),
              (request$jscomp$1 = segment),
              0 === request$jscomp$1.status)
            ) {
              request$jscomp$1.status = 6;
              switchContext(task.context);
              var childrenLength = request$jscomp$1.children.length,
                chunkLength = request$jscomp$1.chunks.length;
              try {
                (retryNode(request, task),
                  pushSegmentFinale(
                    request$jscomp$1.chunks,
                    request.renderState,
                    request$jscomp$1.lastPushedText,
                    request$jscomp$1.textEmbedded,
                  ),
                  task.abortSet.delete(task),
                  (request$jscomp$1.status = 1),
                  finishedTask(
                    request,
                    task.blockedBoundary,
                    task.row,
                    request$jscomp$1,
                  ));
              } catch (thrownValue) {
                resetHooksState();
                request$jscomp$1.children.length = childrenLength;
                request$jscomp$1.chunks.length = chunkLength;
                var x$jscomp$0 =
                  thrownValue === SuspenseException
                    ? getSuspendedThenable()
                    : 12 === request.status
                      ? request.fatalError
                      : thrownValue;
                if (
                  12 === request.status &&
                  null !== request.trackedPostpones
                ) {
                  var trackedPostpones = request.trackedPostpones,
                    thrownInfo = getThrownInfo(task.componentStack);
                  task.abortSet.delete(task);
                  logRecoverableError(request, x$jscomp$0, thrownInfo);
                  trackPostpone(
                    request,
                    trackedPostpones,
                    task,
                    request$jscomp$1,
                  );
                  finishedTask(
                    request,
                    task.blockedBoundary,
                    task.row,
                    request$jscomp$1,
                  );
                } else if (
                  "object" === typeof x$jscomp$0 &&
                  null !== x$jscomp$0 &&
                  "function" === typeof x$jscomp$0.then
                ) {
                  request$jscomp$1.status = 0;
                  task.thenableState =
                    thrownValue === SuspenseException
                      ? getThenableStateAfterSuspending()
                      : null;
                  var ping$jscomp$0 = task.ping;
                  x$jscomp$0.then(ping$jscomp$0, ping$jscomp$0);
                } else {
                  var errorInfo$jscomp$0 = getThrownInfo(task.componentStack);
                  task.abortSet.delete(task);
                  request$jscomp$1.status = 4;
                  var boundary$jscomp$0 = task.blockedBoundary,
                    row = task.row;
                  null !== row &&
                    0 === --row.pendingTasks &&
                    finishSuspenseListRow(request, row);
                  request.allPendingTasks--;
                  request$jscomp$0 = logRecoverableError(
                    request,
                    x$jscomp$0,
                    errorInfo$jscomp$0,
                  );
                  if (null === boundary$jscomp$0)
                    fatalError(request, x$jscomp$0);
                  else if (
                    (boundary$jscomp$0.pendingTasks--,
                    4 !== boundary$jscomp$0.status)
                  ) {
                    boundary$jscomp$0.status = 4;
                    boundary$jscomp$0.errorDigest = request$jscomp$0;
                    untrackBoundary(request, boundary$jscomp$0);
                    var boundaryRow = boundary$jscomp$0.row;
                    null !== boundaryRow &&
                      0 === --boundaryRow.pendingTasks &&
                      finishSuspenseListRow(request, boundaryRow);
                    boundary$jscomp$0.parentFlushed &&
                      request.clientRenderedBoundaries.push(boundary$jscomp$0);
                    0 === request.pendingRootTasks &&
                      null === request.trackedPostpones &&
                      null !== boundary$jscomp$0.contentPreamble &&
                      preparePreamble(request);
                  }
                  0 === request.allPendingTasks && completeAll(request);
                }
              } finally {
              }
            }
          }
          pingedTasks.splice(0, i);
          null !== request$jscomp$2.destination &&
            flushCompletedQueues(
              request$jscomp$2,
              request$jscomp$2.destination,
            );
        } catch (error) {
          (logRecoverableError(request$jscomp$2, error, {}),
            fatalError(request$jscomp$2, error));
        } finally {
          ((currentResumableState = prevResumableState),
            (ReactSharedInternals.H = prevDispatcher),
            (ReactSharedInternals.A = prevAsyncDispatcher),
            prevDispatcher === HooksDispatcher && switchContext(prevContext),
            (currentRequest = prevRequest));
        }
      }
    }
    function preparePreambleFromSubtree(
      request,
      segment,
      collectedPreambleSegments,
    ) {
      segment.preambleChildren.length &&
        collectedPreambleSegments.push(segment.preambleChildren);
      for (
        var pendingPreambles = false, i = 0;
        i < segment.children.length;
        i++
      )
        pendingPreambles =
          preparePreambleFromSegment(
            request,
            segment.children[i],
            collectedPreambleSegments,
          ) || pendingPreambles;
      return pendingPreambles;
    }
    function preparePreambleFromSegment(
      request,
      segment,
      collectedPreambleSegments,
    ) {
      var boundary = segment.boundary;
      if (null === boundary)
        return preparePreambleFromSubtree(
          request,
          segment,
          collectedPreambleSegments,
        );
      var preamble = boundary.contentPreamble,
        fallbackPreamble = boundary.fallbackPreamble;
      if (null === preamble || null === fallbackPreamble) return false;
      switch (boundary.status) {
        case 1:
          hoistPreambleState(request.renderState, preamble);
          request.byteSize += boundary.byteSize;
          segment = boundary.completedSegments[0];
          if (!segment)
            throw Error(
              "A previously unvisited boundary must have exactly one root segment. This is a bug in React.",
            );
          return preparePreambleFromSubtree(
            request,
            segment,
            collectedPreambleSegments,
          );
        case 5:
          if (null !== request.trackedPostpones) return true;
        case 4:
          if (1 === segment.status)
            return (
              hoistPreambleState(request.renderState, fallbackPreamble),
              preparePreambleFromSubtree(
                request,
                segment,
                collectedPreambleSegments,
              )
            );
        default:
          return true;
      }
    }
    function preparePreamble(request) {
      if (
        request.completedRootSegment &&
        null === request.completedPreambleSegments
      ) {
        var collectedPreambleSegments = [],
          originalRequestByteSize = request.byteSize,
          hasPendingPreambles = preparePreambleFromSegment(
            request,
            request.completedRootSegment,
            collectedPreambleSegments,
          ),
          preamble = request.renderState.preamble;
        false === hasPendingPreambles ||
        (preamble.headChunks && preamble.bodyChunks)
          ? (request.completedPreambleSegments = collectedPreambleSegments)
          : (request.byteSize = originalRequestByteSize);
      }
    }
    function flushSubtree(request, destination, segment, hoistableState) {
      segment.parentFlushed = true;
      switch (segment.status) {
        case 0:
          segment.id = request.nextSegmentId++;
        case 5:
          return (
            (hoistableState = segment.id),
            (segment.lastPushedText = false),
            (segment.textEmbedded = false),
            (request = request.renderState),
            destination.push('<template id="'),
            destination.push(request.placeholderPrefix),
            (request = hoistableState.toString(16)),
            destination.push(request),
            destination.push('"></template>')
          );
        case 1:
          segment.status = 2;
          var r2 = true,
            chunks = segment.chunks,
            chunkIdx = 0;
          segment = segment.children;
          for (var childIdx = 0; childIdx < segment.length; childIdx++) {
            for (r2 = segment[childIdx]; chunkIdx < r2.index; chunkIdx++)
              destination.push(chunks[chunkIdx]);
            r2 = flushSegment(request, destination, r2, hoistableState);
          }
          for (; chunkIdx < chunks.length - 1; chunkIdx++)
            destination.push(chunks[chunkIdx]);
          chunkIdx < chunks.length && (r2 = destination.push(chunks[chunkIdx]));
          return r2;
        case 3:
          return true;
        default:
          throw Error(
            "Aborted, errored or already flushed boundaries should not be flushed again. This is a bug in React.",
          );
      }
    }
    var flushedByteSize = 0;
    function flushSegment(request, destination, segment, hoistableState) {
      var boundary = segment.boundary;
      if (null === boundary)
        return flushSubtree(request, destination, segment, hoistableState);
      boundary.parentFlushed = true;
      if (4 === boundary.status) {
        var row = boundary.row;
        null !== row &&
          0 === --row.pendingTasks &&
          finishSuspenseListRow(request, row);
        request.renderState.generateStaticMarkup ||
          ((boundary = boundary.errorDigest),
          destination.push("<!--$!-->"),
          destination.push("<template"),
          boundary &&
            (destination.push(' data-dgst="'),
            (boundary = escapeTextForBrowser(boundary)),
            destination.push(boundary),
            destination.push('"')),
          destination.push("></template>"));
        flushSubtree(request, destination, segment, hoistableState);
        request = request.renderState.generateStaticMarkup
          ? true
          : destination.push("<!--/$-->");
        return request;
      }
      if (1 !== boundary.status)
        return (
          0 === boundary.status &&
            (boundary.rootSegmentID = request.nextSegmentId++),
          0 < boundary.completedSegments.length &&
            request.partialBoundaries.push(boundary),
          writeStartPendingSuspenseBoundary(
            destination,
            request.renderState,
            boundary.rootSegmentID,
          ),
          hoistableState &&
            hoistHoistables(hoistableState, boundary.fallbackState),
          flushSubtree(request, destination, segment, hoistableState),
          destination.push("<!--/$-->")
        );
      if (
        !flushingPartialBoundaries &&
        isEligibleForOutlining(request, boundary) &&
        flushedByteSize + boundary.byteSize > request.progressiveChunkSize
      )
        return (
          (boundary.rootSegmentID = request.nextSegmentId++),
          request.completedBoundaries.push(boundary),
          writeStartPendingSuspenseBoundary(
            destination,
            request.renderState,
            boundary.rootSegmentID,
          ),
          flushSubtree(request, destination, segment, hoistableState),
          destination.push("<!--/$-->")
        );
      flushedByteSize += boundary.byteSize;
      hoistableState && hoistHoistables(hoistableState, boundary.contentState);
      segment = boundary.row;
      null !== segment &&
        isEligibleForOutlining(request, boundary) &&
        0 === --segment.pendingTasks &&
        finishSuspenseListRow(request, segment);
      request.renderState.generateStaticMarkup || destination.push("<!--$-->");
      segment = boundary.completedSegments;
      if (1 !== segment.length)
        throw Error(
          "A previously unvisited boundary must have exactly one root segment. This is a bug in React.",
        );
      flushSegment(request, destination, segment[0], hoistableState);
      request = request.renderState.generateStaticMarkup
        ? true
        : destination.push("<!--/$-->");
      return request;
    }
    function flushSegmentContainer(
      request,
      destination,
      segment,
      hoistableState,
    ) {
      writeStartSegment(
        destination,
        request.renderState,
        segment.parentFormatContext,
        segment.id,
      );
      flushSegment(request, destination, segment, hoistableState);
      return writeEndSegment(destination, segment.parentFormatContext);
    }
    function flushCompletedBoundary(request, destination, boundary) {
      flushedByteSize = boundary.byteSize;
      for (
        var completedSegments = boundary.completedSegments, i = 0;
        i < completedSegments.length;
        i++
      )
        flushPartiallyCompletedSegment(
          request,
          destination,
          boundary,
          completedSegments[i],
        );
      completedSegments.length = 0;
      completedSegments = boundary.row;
      null !== completedSegments &&
        isEligibleForOutlining(request, boundary) &&
        0 === --completedSegments.pendingTasks &&
        finishSuspenseListRow(request, completedSegments);
      writeHoistablesForBoundary(
        destination,
        boundary.contentState,
        request.renderState,
      );
      completedSegments = request.resumableState;
      request = request.renderState;
      i = boundary.rootSegmentID;
      boundary = boundary.contentState;
      var requiresStyleInsertion = request.stylesToHoist;
      request.stylesToHoist = false;
      destination.push(request.startInlineScript);
      destination.push(">");
      requiresStyleInsertion
        ? (0 === (completedSegments.instructions & 4) &&
            ((completedSegments.instructions |= 4),
            destination.push(
              '$RX=function(b,c,d,e,f){var a=document.getElementById(b);a&&(b=a.previousSibling,b.data="$!",a=a.dataset,c&&(a.dgst=c),d&&(a.msg=d),e&&(a.stck=e),f&&(a.cstck=f),b._reactRetry&&b._reactRetry())};',
            )),
          0 === (completedSegments.instructions & 2) &&
            ((completedSegments.instructions |= 2),
            destination.push(
              '$RB=[];$RV=function(a){$RT=performance.now();for(var b=0;b<a.length;b+=2){var c=a[b],e=a[b+1];null!==e.parentNode&&e.parentNode.removeChild(e);var f=c.parentNode;if(f){var g=c.previousSibling,h=0;do{if(c&&8===c.nodeType){var d=c.data;if("/$"===d||"/&"===d)if(0===h)break;else h--;else"$"!==d&&"$?"!==d&&"$~"!==d&&"$!"!==d&&"&"!==d||h++}d=c.nextSibling;f.removeChild(c);c=d}while(c);for(;e.firstChild;)f.insertBefore(e.firstChild,c);g.data="$";g._reactRetry&&requestAnimationFrame(g._reactRetry)}}a.length=0};\n$RC=function(a,b){if(b=document.getElementById(b))(a=document.getElementById(a))?(a.previousSibling.data="$~",$RB.push(a,b),2===$RB.length&&("number"!==typeof $RT?requestAnimationFrame($RV.bind(null,$RB)):(a=performance.now(),setTimeout($RV.bind(null,$RB),2300>a&&2E3<a?2300-a:$RT+300-a)))):b.parentNode.removeChild(b)};',
            )),
          0 === (completedSegments.instructions & 8)
            ? ((completedSegments.instructions |= 8),
              destination.push(
                '$RM=new Map;$RR=function(n,w,p){function u(q){this._p=null;q()}for(var r=new Map,t=document,h,b,e=t.querySelectorAll("link[data-precedence],style[data-precedence]"),v=[],k=0;b=e[k++];)"not all"===b.getAttribute("media")?v.push(b):("LINK"===b.tagName&&$RM.set(b.getAttribute("href"),b),r.set(b.dataset.precedence,h=b));e=0;b=[];var l,a;for(k=!0;;){if(k){var f=p[e++];if(!f){k=!1;e=0;continue}var c=!1,m=0;var d=f[m++];if(a=$RM.get(d)){var g=a._p;c=!0}else{a=t.createElement("link");a.href=d;a.rel=\n"stylesheet";for(a.dataset.precedence=l=f[m++];g=f[m++];)a.setAttribute(g,f[m++]);g=a._p=new Promise(function(q,x){a.onload=u.bind(a,q);a.onerror=u.bind(a,x)});$RM.set(d,a)}d=a.getAttribute("media");!g||d&&!matchMedia(d).matches||b.push(g);if(c)continue}else{a=v[e++];if(!a)break;l=a.getAttribute("data-precedence");a.removeAttribute("media")}c=r.get(l)||h;c===h&&(h=a);r.set(l,a);c?c.parentNode.insertBefore(a,c.nextSibling):(c=t.head,c.insertBefore(a,c.firstChild))}if(p=document.getElementById(n))p.previousSibling.data=\n"$~";Promise.all(b).then($RC.bind(null,n,w),$RX.bind(null,n,"CSS failed to load"))};$RR("',
              ))
            : destination.push('$RR("'))
        : (0 === (completedSegments.instructions & 2) &&
            ((completedSegments.instructions |= 2),
            destination.push(
              '$RB=[];$RV=function(a){$RT=performance.now();for(var b=0;b<a.length;b+=2){var c=a[b],e=a[b+1];null!==e.parentNode&&e.parentNode.removeChild(e);var f=c.parentNode;if(f){var g=c.previousSibling,h=0;do{if(c&&8===c.nodeType){var d=c.data;if("/$"===d||"/&"===d)if(0===h)break;else h--;else"$"!==d&&"$?"!==d&&"$~"!==d&&"$!"!==d&&"&"!==d||h++}d=c.nextSibling;f.removeChild(c);c=d}while(c);for(;e.firstChild;)f.insertBefore(e.firstChild,c);g.data="$";g._reactRetry&&requestAnimationFrame(g._reactRetry)}}a.length=0};\n$RC=function(a,b){if(b=document.getElementById(b))(a=document.getElementById(a))?(a.previousSibling.data="$~",$RB.push(a,b),2===$RB.length&&("number"!==typeof $RT?requestAnimationFrame($RV.bind(null,$RB)):(a=performance.now(),setTimeout($RV.bind(null,$RB),2300>a&&2E3<a?2300-a:$RT+300-a)))):b.parentNode.removeChild(b)};',
            )),
          destination.push('$RC("'));
      completedSegments = i.toString(16);
      destination.push(request.boundaryPrefix);
      destination.push(completedSegments);
      destination.push('","');
      destination.push(request.segmentPrefix);
      destination.push(completedSegments);
      requiresStyleInsertion
        ? (destination.push('",'),
          writeStyleResourceDependenciesInJS(destination, boundary))
        : destination.push('"');
      boundary = destination.push(")</script>");
      return writeBootstrap(destination, request) && boundary;
    }
    function flushPartiallyCompletedSegment(
      request,
      destination,
      boundary,
      segment,
    ) {
      if (2 === segment.status) return true;
      var hoistableState = boundary.contentState,
        segmentID = segment.id;
      if (-1 === segmentID) {
        if (-1 === (segment.id = boundary.rootSegmentID))
          throw Error(
            "A root segment ID must have been assigned by now. This is a bug in React.",
          );
        return flushSegmentContainer(
          request,
          destination,
          segment,
          hoistableState,
        );
      }
      if (segmentID === boundary.rootSegmentID)
        return flushSegmentContainer(
          request,
          destination,
          segment,
          hoistableState,
        );
      flushSegmentContainer(request, destination, segment, hoistableState);
      boundary = request.resumableState;
      request = request.renderState;
      destination.push(request.startInlineScript);
      destination.push(">");
      0 === (boundary.instructions & 1)
        ? ((boundary.instructions |= 1),
          destination.push(
            '$RS=function(a,b){a=document.getElementById(a);b=document.getElementById(b);for(a.parentNode.removeChild(a);a.firstChild;)b.parentNode.insertBefore(a.firstChild,b);b.parentNode.removeChild(b)};$RS("',
          ))
        : destination.push('$RS("');
      destination.push(request.segmentPrefix);
      segmentID = segmentID.toString(16);
      destination.push(segmentID);
      destination.push('","');
      destination.push(request.placeholderPrefix);
      destination.push(segmentID);
      destination = destination.push('")</script>');
      return destination;
    }
    var flushingPartialBoundaries = false;
    function flushCompletedQueues(request, destination) {
      try {
        if (!(0 < request.pendingRootTasks)) {
          var i,
            completedRootSegment = request.completedRootSegment;
          if (null !== completedRootSegment) {
            if (5 === completedRootSegment.status) return;
            var completedPreambleSegments = request.completedPreambleSegments;
            if (null === completedPreambleSegments) return;
            flushedByteSize = request.byteSize;
            var resumableState = request.resumableState,
              renderState = request.renderState,
              preamble = renderState.preamble,
              htmlChunks = preamble.htmlChunks,
              headChunks = preamble.headChunks,
              i$jscomp$0;
            if (htmlChunks) {
              for (i$jscomp$0 = 0; i$jscomp$0 < htmlChunks.length; i$jscomp$0++)
                destination.push(htmlChunks[i$jscomp$0]);
              if (headChunks)
                for (
                  i$jscomp$0 = 0;
                  i$jscomp$0 < headChunks.length;
                  i$jscomp$0++
                )
                  destination.push(headChunks[i$jscomp$0]);
              else {
                var chunk = startChunkForTag("head");
                destination.push(chunk);
                destination.push(">");
              }
            } else if (headChunks)
              for (i$jscomp$0 = 0; i$jscomp$0 < headChunks.length; i$jscomp$0++)
                destination.push(headChunks[i$jscomp$0]);
            var charsetChunks = renderState.charsetChunks;
            for (
              i$jscomp$0 = 0;
              i$jscomp$0 < charsetChunks.length;
              i$jscomp$0++
            )
              destination.push(charsetChunks[i$jscomp$0]);
            charsetChunks.length = 0;
            renderState.preconnects.forEach(flushResource, destination);
            renderState.preconnects.clear();
            var viewportChunks = renderState.viewportChunks;
            for (
              i$jscomp$0 = 0;
              i$jscomp$0 < viewportChunks.length;
              i$jscomp$0++
            )
              destination.push(viewportChunks[i$jscomp$0]);
            viewportChunks.length = 0;
            renderState.fontPreloads.forEach(flushResource, destination);
            renderState.fontPreloads.clear();
            renderState.highImagePreloads.forEach(flushResource, destination);
            renderState.highImagePreloads.clear();
            currentlyFlushingRenderState = renderState;
            renderState.styles.forEach(flushStylesInPreamble, destination);
            currentlyFlushingRenderState = null;
            var importMapChunks = renderState.importMapChunks;
            for (
              i$jscomp$0 = 0;
              i$jscomp$0 < importMapChunks.length;
              i$jscomp$0++
            )
              destination.push(importMapChunks[i$jscomp$0]);
            importMapChunks.length = 0;
            renderState.bootstrapScripts.forEach(flushResource, destination);
            renderState.scripts.forEach(flushResource, destination);
            renderState.scripts.clear();
            renderState.bulkPreloads.forEach(flushResource, destination);
            renderState.bulkPreloads.clear();
            resumableState.instructions |= 32;
            var hoistableChunks = renderState.hoistableChunks;
            for (
              i$jscomp$0 = 0;
              i$jscomp$0 < hoistableChunks.length;
              i$jscomp$0++
            )
              destination.push(hoistableChunks[i$jscomp$0]);
            for (
              resumableState = hoistableChunks.length = 0;
              resumableState < completedPreambleSegments.length;
              resumableState++
            ) {
              var segments = completedPreambleSegments[resumableState];
              for (
                renderState = 0;
                renderState < segments.length;
                renderState++
              )
                flushSegment(request, destination, segments[renderState], null);
            }
            var preamble$jscomp$0 = request.renderState.preamble,
              headChunks$jscomp$0 = preamble$jscomp$0.headChunks;
            if (preamble$jscomp$0.htmlChunks || headChunks$jscomp$0) {
              var chunk$jscomp$0 = endChunkForTag("head");
              destination.push(chunk$jscomp$0);
            }
            var bodyChunks = preamble$jscomp$0.bodyChunks;
            if (bodyChunks)
              for (
                completedPreambleSegments = 0;
                completedPreambleSegments < bodyChunks.length;
                completedPreambleSegments++
              )
                destination.push(bodyChunks[completedPreambleSegments]);
            flushSegment(request, destination, completedRootSegment, null);
            request.completedRootSegment = null;
            var renderState$jscomp$0 = request.renderState;
            if (
              0 !== request.allPendingTasks ||
              0 !== request.clientRenderedBoundaries.length ||
              0 !== request.completedBoundaries.length ||
              (null !== request.trackedPostpones &&
                (0 !== request.trackedPostpones.rootNodes.length ||
                  null !== request.trackedPostpones.rootSlots))
            ) {
              var resumableState$jscomp$0 = request.resumableState;
              if (0 === (resumableState$jscomp$0.instructions & 64)) {
                resumableState$jscomp$0.instructions |= 64;
                destination.push(renderState$jscomp$0.startInlineScript);
                if (0 === (resumableState$jscomp$0.instructions & 32)) {
                  resumableState$jscomp$0.instructions |= 32;
                  var shellId = "_" + resumableState$jscomp$0.idPrefix + "R_";
                  destination.push(' id="');
                  var chunk$jscomp$1 = escapeTextForBrowser(shellId);
                  destination.push(chunk$jscomp$1);
                  destination.push('"');
                }
                destination.push(">");
                destination.push(
                  "requestAnimationFrame(function(){$RT=performance.now()});",
                );
                destination.push("</script>");
              }
            }
            writeBootstrap(destination, renderState$jscomp$0);
          }
          var renderState$jscomp$1 = request.renderState;
          completedRootSegment = 0;
          var viewportChunks$jscomp$0 = renderState$jscomp$1.viewportChunks;
          for (
            completedRootSegment = 0;
            completedRootSegment < viewportChunks$jscomp$0.length;
            completedRootSegment++
          )
            destination.push(viewportChunks$jscomp$0[completedRootSegment]);
          viewportChunks$jscomp$0.length = 0;
          renderState$jscomp$1.preconnects.forEach(flushResource, destination);
          renderState$jscomp$1.preconnects.clear();
          renderState$jscomp$1.fontPreloads.forEach(flushResource, destination);
          renderState$jscomp$1.fontPreloads.clear();
          renderState$jscomp$1.highImagePreloads.forEach(
            flushResource,
            destination,
          );
          renderState$jscomp$1.highImagePreloads.clear();
          renderState$jscomp$1.styles.forEach(preloadLateStyles, destination);
          renderState$jscomp$1.scripts.forEach(flushResource, destination);
          renderState$jscomp$1.scripts.clear();
          renderState$jscomp$1.bulkPreloads.forEach(flushResource, destination);
          renderState$jscomp$1.bulkPreloads.clear();
          var hoistableChunks$jscomp$0 = renderState$jscomp$1.hoistableChunks;
          for (
            completedRootSegment = 0;
            completedRootSegment < hoistableChunks$jscomp$0.length;
            completedRootSegment++
          )
            destination.push(hoistableChunks$jscomp$0[completedRootSegment]);
          hoistableChunks$jscomp$0.length = 0;
          var clientRenderedBoundaries = request.clientRenderedBoundaries;
          for (i = 0; i < clientRenderedBoundaries.length; i++) {
            var boundary = clientRenderedBoundaries[i];
            renderState$jscomp$1 = destination;
            var resumableState$jscomp$1 = request.resumableState,
              renderState$jscomp$2 = request.renderState,
              id = boundary.rootSegmentID,
              errorDigest = boundary.errorDigest;
            renderState$jscomp$1.push(renderState$jscomp$2.startInlineScript);
            renderState$jscomp$1.push(">");
            0 === (resumableState$jscomp$1.instructions & 4)
              ? ((resumableState$jscomp$1.instructions |= 4),
                renderState$jscomp$1.push(
                  '$RX=function(b,c,d,e,f){var a=document.getElementById(b);a&&(b=a.previousSibling,b.data="$!",a=a.dataset,c&&(a.dgst=c),d&&(a.msg=d),e&&(a.stck=e),f&&(a.cstck=f),b._reactRetry&&b._reactRetry())};;$RX("',
                ))
              : renderState$jscomp$1.push('$RX("');
            renderState$jscomp$1.push(renderState$jscomp$2.boundaryPrefix);
            var chunk$jscomp$2 = id.toString(16);
            renderState$jscomp$1.push(chunk$jscomp$2);
            renderState$jscomp$1.push('"');
            if (errorDigest) {
              renderState$jscomp$1.push(",");
              var chunk$jscomp$3 = escapeJSStringsForInstructionScripts(
                errorDigest || "",
              );
              renderState$jscomp$1.push(chunk$jscomp$3);
            }
            var JSCompiler_inline_result =
              renderState$jscomp$1.push(")</script>");
            if (!JSCompiler_inline_result) {
              request.destination = null;
              i++;
              clientRenderedBoundaries.splice(0, i);
              return;
            }
          }
          clientRenderedBoundaries.splice(0, i);
          var completedBoundaries = request.completedBoundaries;
          for (i = 0; i < completedBoundaries.length; i++)
            if (
              !flushCompletedBoundary(
                request,
                destination,
                completedBoundaries[i],
              )
            ) {
              request.destination = null;
              i++;
              completedBoundaries.splice(0, i);
              return;
            }
          completedBoundaries.splice(0, i);
          flushingPartialBoundaries = true;
          var partialBoundaries = request.partialBoundaries;
          for (i = 0; i < partialBoundaries.length; i++) {
            var boundary$69 = partialBoundaries[i];
            a: {
              clientRenderedBoundaries = request;
              boundary = destination;
              flushedByteSize = boundary$69.byteSize;
              var completedSegments = boundary$69.completedSegments;
              for (
                JSCompiler_inline_result = 0;
                JSCompiler_inline_result < completedSegments.length;
                JSCompiler_inline_result++
              )
                if (
                  !flushPartiallyCompletedSegment(
                    clientRenderedBoundaries,
                    boundary,
                    boundary$69,
                    completedSegments[JSCompiler_inline_result],
                  )
                ) {
                  JSCompiler_inline_result++;
                  completedSegments.splice(0, JSCompiler_inline_result);
                  var JSCompiler_inline_result$jscomp$0 = false;
                  break a;
                }
              completedSegments.splice(0, JSCompiler_inline_result);
              var row = boundary$69.row;
              null !== row &&
                row.together &&
                1 === boundary$69.pendingTasks &&
                (1 === row.pendingTasks
                  ? unblockSuspenseListRow(
                      clientRenderedBoundaries,
                      row,
                      row.hoistables,
                    )
                  : row.pendingTasks--);
              JSCompiler_inline_result$jscomp$0 = writeHoistablesForBoundary(
                boundary,
                boundary$69.contentState,
                clientRenderedBoundaries.renderState,
              );
            }
            if (!JSCompiler_inline_result$jscomp$0) {
              request.destination = null;
              i++;
              partialBoundaries.splice(0, i);
              return;
            }
          }
          partialBoundaries.splice(0, i);
          flushingPartialBoundaries = false;
          var largeBoundaries = request.completedBoundaries;
          for (i = 0; i < largeBoundaries.length; i++)
            if (
              !flushCompletedBoundary(request, destination, largeBoundaries[i])
            ) {
              request.destination = null;
              i++;
              largeBoundaries.splice(0, i);
              return;
            }
          largeBoundaries.splice(0, i);
        }
      } finally {
        ((flushingPartialBoundaries = false),
          0 === request.allPendingTasks &&
            0 === request.clientRenderedBoundaries.length &&
            0 === request.completedBoundaries.length &&
            ((request.flushScheduled = false),
            (i = request.resumableState),
            i.hasBody &&
              ((partialBoundaries = endChunkForTag("body")),
              destination.push(partialBoundaries)),
            i.hasHtml && ((i = endChunkForTag("html")), destination.push(i)),
            (request.status = 14),
            destination.push(null),
            (request.destination = null)));
      }
    }
    function enqueueFlush(request) {
      if (
        false === request.flushScheduled &&
        0 === request.pingedTasks.length &&
        null !== request.destination
      ) {
        request.flushScheduled = true;
        var destination = request.destination;
        destination
          ? flushCompletedQueues(request, destination)
          : (request.flushScheduled = false);
      }
    }
    function startFlowing(request, destination) {
      if (13 === request.status)
        ((request.status = 14), destination.destroy(request.fatalError));
      else if (14 !== request.status && null === request.destination) {
        request.destination = destination;
        try {
          flushCompletedQueues(request, destination);
        } catch (error) {
          (logRecoverableError(request, error, {}), fatalError(request, error));
        }
      }
    }
    function abort(request, reason) {
      if (11 === request.status || 10 === request.status) request.status = 12;
      try {
        var abortableTasks = request.abortableTasks;
        if (0 < abortableTasks.size) {
          var error =
            void 0 === reason
              ? Error("The render was aborted by the server without a reason.")
              : "object" === typeof reason &&
                  null !== reason &&
                  "function" === typeof reason.then
                ? Error("The render was aborted by the server with a promise.")
                : reason;
          request.fatalError = error;
          abortableTasks.forEach(function (task) {
            return abortTask(task, request, error);
          });
          abortableTasks.clear();
        }
        null !== request.destination &&
          flushCompletedQueues(request, request.destination);
      } catch (error$71) {
        (logRecoverableError(request, error$71, {}),
          fatalError(request, error$71));
      }
    }
    function addToReplayParent(node2, parentKeyPath, trackedPostpones) {
      if (null === parentKeyPath) trackedPostpones.rootNodes.push(node2);
      else {
        var workingMap = trackedPostpones.workingMap,
          parentNode = workingMap.get(parentKeyPath);
        void 0 === parentNode &&
          ((parentNode = [parentKeyPath[1], parentKeyPath[2], [], null]),
          workingMap.set(parentKeyPath, parentNode),
          addToReplayParent(parentNode, parentKeyPath[0], trackedPostpones));
        parentNode[2].push(node2);
      }
    }
    function onError() {}
    function renderToStringImpl(
      children,
      options,
      generateStaticMarkup,
      abortReason,
    ) {
      var didFatal = false,
        fatalError2 = null,
        result = "",
        readyToStream = false;
      options = createResumableState(
        options ? options.identifierPrefix : void 0,
      );
      children = createRequest(
        children,
        options,
        createRenderState(options, generateStaticMarkup),
        createFormatContext(0, null, 0, null),
        Infinity,
        onError,
        void 0,
        function () {
          readyToStream = true;
        },
        void 0,
        void 0,
        void 0,
      );
      children.flushScheduled = null !== children.destination;
      performWork(children);
      10 === children.status && (children.status = 11);
      null === children.trackedPostpones &&
        safelyEmitEarlyPreloads(children, 0 === children.pendingRootTasks);
      abort(children, abortReason);
      startFlowing(children, {
        push: function (chunk) {
          null !== chunk && (result += chunk);
          return true;
        },
        destroy: function (error) {
          didFatal = true;
          fatalError2 = error;
        },
      });
      if (didFatal && fatalError2 !== abortReason) throw fatalError2;
      if (!readyToStream)
        throw Error(
          "A component suspended while responding to synchronous input. This will cause the UI to be replaced with a loading indicator. To fix, updates that suspend should be wrapped with startTransition.",
        );
      return result;
    }
    exports2.renderToStaticMarkup = function (children, options) {
      return renderToStringImpl(
        children,
        options,
        true,
        'The server used "renderToStaticMarkup" which does not support Suspense. If you intended to have the server wait for the suspended component please switch to "renderToPipeableStream" which supports Suspense on the server',
      );
    };
    exports2.renderToString = function (children, options) {
      return renderToStringImpl(
        children,
        options,
        false,
        'The server used "renderToString" which does not support Suspense. If you intended for this Suspense boundary to render the fallback content on the server consider throwing an Error somewhere within the Suspense boundary. If you intended to have the server wait for the suspended component please switch to "renderToPipeableStream" which supports Suspense on the server',
      );
    };
    exports2.version = "19.2.8";
  },
});

// node_modules/.pnpm/react-dom@19.2.8_react@19.2.8/node_modules/react-dom/cjs/react-dom-server.node.production.js
var require_react_dom_server_node_production = __commonJS({
  "node_modules/.pnpm/react-dom@19.2.8_react@19.2.8/node_modules/react-dom/cjs/react-dom-server.node.production.js"(
    exports2,
  ) {
    "use strict";
    var util = require("util");
    var crypto = require("crypto");
    var async_hooks = require("async_hooks");
    var React102 = require_react();
    var ReactDOM3 = require_react_dom();
    var stream = require("stream");
    var REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element");
    var REACT_PORTAL_TYPE = Symbol.for("react.portal");
    var REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
    var REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode");
    var REACT_PROFILER_TYPE = Symbol.for("react.profiler");
    var REACT_CONSUMER_TYPE = Symbol.for("react.consumer");
    var REACT_CONTEXT_TYPE = Symbol.for("react.context");
    var REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref");
    var REACT_SUSPENSE_TYPE = Symbol.for("react.suspense");
    var REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list");
    var REACT_MEMO_TYPE = Symbol.for("react.memo");
    var REACT_LAZY_TYPE = Symbol.for("react.lazy");
    var REACT_SCOPE_TYPE = Symbol.for("react.scope");
    var REACT_ACTIVITY_TYPE = Symbol.for("react.activity");
    var REACT_LEGACY_HIDDEN_TYPE = Symbol.for("react.legacy_hidden");
    var REACT_MEMO_CACHE_SENTINEL = Symbol.for("react.memo_cache_sentinel");
    var REACT_VIEW_TRANSITION_TYPE = Symbol.for("react.view_transition");
    var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
    function getIteratorFn(maybeIterable) {
      if (null === maybeIterable || "object" !== typeof maybeIterable)
        return null;
      maybeIterable =
        (MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL]) ||
        maybeIterable["@@iterator"];
      return "function" === typeof maybeIterable ? maybeIterable : null;
    }
    var isArrayImpl = Array.isArray;
    var scheduleMicrotask = queueMicrotask;
    function flushBuffered(destination) {
      "function" === typeof destination.flush && destination.flush();
    }
    var currentView = null;
    var writtenBytes = 0;
    var destinationHasCapacity$1 = true;
    function writeChunk(destination, chunk) {
      if ("string" === typeof chunk) {
        if (0 !== chunk.length)
          if (2048 < 3 * chunk.length)
            (0 < writtenBytes &&
              (writeToDestination(
                destination,
                currentView.subarray(0, writtenBytes),
              ),
              (currentView = new Uint8Array(2048)),
              (writtenBytes = 0)),
              writeToDestination(destination, chunk));
          else {
            var target = currentView;
            0 < writtenBytes && (target = currentView.subarray(writtenBytes));
            target = textEncoder.encodeInto(chunk, target);
            var read2 = target.read;
            writtenBytes += target.written;
            read2 < chunk.length &&
              (writeToDestination(
                destination,
                currentView.subarray(0, writtenBytes),
              ),
              (currentView = new Uint8Array(2048)),
              (writtenBytes = textEncoder.encodeInto(
                chunk.slice(read2),
                currentView,
              ).written));
            2048 === writtenBytes &&
              (writeToDestination(destination, currentView),
              (currentView = new Uint8Array(2048)),
              (writtenBytes = 0));
          }
      } else
        0 !== chunk.byteLength &&
          (2048 < chunk.byteLength
            ? (0 < writtenBytes &&
                (writeToDestination(
                  destination,
                  currentView.subarray(0, writtenBytes),
                ),
                (currentView = new Uint8Array(2048)),
                (writtenBytes = 0)),
              writeToDestination(destination, chunk))
            : ((target = currentView.length - writtenBytes),
              target < chunk.byteLength &&
                (0 === target
                  ? writeToDestination(destination, currentView)
                  : (currentView.set(chunk.subarray(0, target), writtenBytes),
                    (writtenBytes += target),
                    writeToDestination(destination, currentView),
                    (chunk = chunk.subarray(target))),
                (currentView = new Uint8Array(2048)),
                (writtenBytes = 0)),
              currentView.set(chunk, writtenBytes),
              (writtenBytes += chunk.byteLength),
              2048 === writtenBytes &&
                (writeToDestination(destination, currentView),
                (currentView = new Uint8Array(2048)),
                (writtenBytes = 0))));
    }
    function writeToDestination(destination, view) {
      destination = destination.write(view);
      destinationHasCapacity$1 = destinationHasCapacity$1 && destination;
    }
    function writeChunkAndReturn(destination, chunk) {
      writeChunk(destination, chunk);
      return destinationHasCapacity$1;
    }
    function completeWriting(destination) {
      currentView &&
        0 < writtenBytes &&
        destination.write(currentView.subarray(0, writtenBytes));
      currentView = null;
      writtenBytes = 0;
      destinationHasCapacity$1 = true;
    }
    var textEncoder = new util.TextEncoder();
    function stringToPrecomputedChunk(content) {
      return textEncoder.encode(content);
    }
    function byteLengthOfChunk(chunk) {
      return "string" === typeof chunk
        ? Buffer.byteLength(chunk, "utf8")
        : chunk.byteLength;
    }
    var assign2 = Object.assign;
    var hasOwnProperty = Object.prototype.hasOwnProperty;
    var VALID_ATTRIBUTE_NAME_REGEX = RegExp(
      "^[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$",
    );
    var illegalAttributeNameCache = {};
    var validatedAttributeNameCache = {};
    function isAttributeNameSafe(attributeName) {
      if (hasOwnProperty.call(validatedAttributeNameCache, attributeName))
        return true;
      if (hasOwnProperty.call(illegalAttributeNameCache, attributeName))
        return false;
      if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName))
        return (validatedAttributeNameCache[attributeName] = true);
      illegalAttributeNameCache[attributeName] = true;
      return false;
    }
    var unitlessNumbers = new Set(
      "animationIterationCount aspectRatio borderImageOutset borderImageSlice borderImageWidth boxFlex boxFlexGroup boxOrdinalGroup columnCount columns flex flexGrow flexPositive flexShrink flexNegative flexOrder gridArea gridRow gridRowEnd gridRowSpan gridRowStart gridColumn gridColumnEnd gridColumnSpan gridColumnStart fontWeight lineClamp lineHeight opacity order orphans scale tabSize widows zIndex zoom fillOpacity floodOpacity stopOpacity strokeDasharray strokeDashoffset strokeMiterlimit strokeOpacity strokeWidth MozAnimationIterationCount MozBoxFlex MozBoxFlexGroup MozLineClamp msAnimationIterationCount msFlex msZoom msFlexGrow msFlexNegative msFlexOrder msFlexPositive msFlexShrink msGridColumn msGridColumnSpan msGridRow msGridRowSpan WebkitAnimationIterationCount WebkitBoxFlex WebKitBoxFlexGroup WebkitBoxOrdinalGroup WebkitColumnCount WebkitColumns WebkitFlex WebkitFlexGrow WebkitFlexPositive WebkitFlexShrink WebkitLineClamp".split(
        " ",
      ),
    );
    var aliases2 = /* @__PURE__ */ new Map([
      ["acceptCharset", "accept-charset"],
      ["htmlFor", "for"],
      ["httpEquiv", "http-equiv"],
      ["crossOrigin", "crossorigin"],
      ["accentHeight", "accent-height"],
      ["alignmentBaseline", "alignment-baseline"],
      ["arabicForm", "arabic-form"],
      ["baselineShift", "baseline-shift"],
      ["capHeight", "cap-height"],
      ["clipPath", "clip-path"],
      ["clipRule", "clip-rule"],
      ["colorInterpolation", "color-interpolation"],
      ["colorInterpolationFilters", "color-interpolation-filters"],
      ["colorProfile", "color-profile"],
      ["colorRendering", "color-rendering"],
      ["dominantBaseline", "dominant-baseline"],
      ["enableBackground", "enable-background"],
      ["fillOpacity", "fill-opacity"],
      ["fillRule", "fill-rule"],
      ["floodColor", "flood-color"],
      ["floodOpacity", "flood-opacity"],
      ["fontFamily", "font-family"],
      ["fontSize", "font-size"],
      ["fontSizeAdjust", "font-size-adjust"],
      ["fontStretch", "font-stretch"],
      ["fontStyle", "font-style"],
      ["fontVariant", "font-variant"],
      ["fontWeight", "font-weight"],
      ["glyphName", "glyph-name"],
      ["glyphOrientationHorizontal", "glyph-orientation-horizontal"],
      ["glyphOrientationVertical", "glyph-orientation-vertical"],
      ["horizAdvX", "horiz-adv-x"],
      ["horizOriginX", "horiz-origin-x"],
      ["imageRendering", "image-rendering"],
      ["letterSpacing", "letter-spacing"],
      ["lightingColor", "lighting-color"],
      ["markerEnd", "marker-end"],
      ["markerMid", "marker-mid"],
      ["markerStart", "marker-start"],
      ["overlinePosition", "overline-position"],
      ["overlineThickness", "overline-thickness"],
      ["paintOrder", "paint-order"],
      ["panose-1", "panose-1"],
      ["pointerEvents", "pointer-events"],
      ["renderingIntent", "rendering-intent"],
      ["shapeRendering", "shape-rendering"],
      ["stopColor", "stop-color"],
      ["stopOpacity", "stop-opacity"],
      ["strikethroughPosition", "strikethrough-position"],
      ["strikethroughThickness", "strikethrough-thickness"],
      ["strokeDasharray", "stroke-dasharray"],
      ["strokeDashoffset", "stroke-dashoffset"],
      ["strokeLinecap", "stroke-linecap"],
      ["strokeLinejoin", "stroke-linejoin"],
      ["strokeMiterlimit", "stroke-miterlimit"],
      ["strokeOpacity", "stroke-opacity"],
      ["strokeWidth", "stroke-width"],
      ["textAnchor", "text-anchor"],
      ["textDecoration", "text-decoration"],
      ["textRendering", "text-rendering"],
      ["transformOrigin", "transform-origin"],
      ["underlinePosition", "underline-position"],
      ["underlineThickness", "underline-thickness"],
      ["unicodeBidi", "unicode-bidi"],
      ["unicodeRange", "unicode-range"],
      ["unitsPerEm", "units-per-em"],
      ["vAlphabetic", "v-alphabetic"],
      ["vHanging", "v-hanging"],
      ["vIdeographic", "v-ideographic"],
      ["vMathematical", "v-mathematical"],
      ["vectorEffect", "vector-effect"],
      ["vertAdvY", "vert-adv-y"],
      ["vertOriginX", "vert-origin-x"],
      ["vertOriginY", "vert-origin-y"],
      ["wordSpacing", "word-spacing"],
      ["writingMode", "writing-mode"],
      ["xmlnsXlink", "xmlns:xlink"],
      ["xHeight", "x-height"],
    ]);
    var matchHtmlRegExp = /["'&<>]/;
    function escapeTextForBrowser(text) {
      if (
        "boolean" === typeof text ||
        "number" === typeof text ||
        "bigint" === typeof text
      )
        return "" + text;
      text = "" + text;
      var match2 = matchHtmlRegExp.exec(text);
      if (match2) {
        var html2 = "",
          index,
          lastIndex = 0;
        for (index = match2.index; index < text.length; index++) {
          switch (text.charCodeAt(index)) {
            case 34:
              match2 = "&quot;";
              break;
            case 38:
              match2 = "&amp;";
              break;
            case 39:
              match2 = "&#x27;";
              break;
            case 60:
              match2 = "&lt;";
              break;
            case 62:
              match2 = "&gt;";
              break;
            default:
              continue;
          }
          lastIndex !== index && (html2 += text.slice(lastIndex, index));
          lastIndex = index + 1;
          html2 += match2;
        }
        text =
          lastIndex !== index ? html2 + text.slice(lastIndex, index) : html2;
      }
      return text;
    }
    var uppercasePattern = /([A-Z])/g;
    var msPattern = /^ms-/;
    var isJavaScriptProtocol =
      /^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*:/i;
    function sanitizeURL(url) {
      return isJavaScriptProtocol.test("" + url)
        ? "javascript:throw new Error('React has blocked a javascript: URL as a security precaution.')"
        : url;
    }
    var ReactSharedInternals =
      React102.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE;
    var ReactDOMSharedInternals =
      ReactDOM3.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE;
    var sharedNotPendingObject = {
      pending: false,
      data: null,
      method: null,
      action: null,
    };
    var previousDispatcher = ReactDOMSharedInternals.d;
    ReactDOMSharedInternals.d = {
      f: previousDispatcher.f,
      r: previousDispatcher.r,
      D: prefetchDNS,
      C: preconnect,
      L: preload,
      m: preloadModule,
      X: preinitScript,
      S: preinitStyle,
      M: preinitModuleScript,
    };
    var PRELOAD_NO_CREDS = [];
    var currentlyFlushingRenderState = null;
    stringToPrecomputedChunk('"></template>');
    var startInlineScript = stringToPrecomputedChunk("<script");
    var endInlineScript = stringToPrecomputedChunk("</script>");
    var startScriptSrc = stringToPrecomputedChunk('<script src="');
    var startModuleSrc = stringToPrecomputedChunk(
      '<script type="module" src="',
    );
    var scriptNonce = stringToPrecomputedChunk(' nonce="');
    var scriptIntegirty = stringToPrecomputedChunk(' integrity="');
    var scriptCrossOrigin = stringToPrecomputedChunk(' crossorigin="');
    var endAsyncScript = stringToPrecomputedChunk(' async=""></script>');
    var startInlineStyle = stringToPrecomputedChunk("<style");
    var scriptRegex = /(<\/|<)(s)(cript)/gi;
    function scriptReplacer(match2, prefix3, s, suffix2) {
      return "" + prefix3 + ("s" === s ? "\\u0073" : "\\u0053") + suffix2;
    }
    var importMapScriptStart = stringToPrecomputedChunk(
      '<script type="importmap">',
    );
    var importMapScriptEnd = stringToPrecomputedChunk("</script>");
    function createRenderState(
      resumableState,
      nonce,
      externalRuntimeConfig,
      importMap,
      onHeaders,
      maxHeadersLength,
    ) {
      externalRuntimeConfig =
        "string" === typeof nonce ? nonce : nonce && nonce.script;
      var inlineScriptWithNonce =
          void 0 === externalRuntimeConfig
            ? startInlineScript
            : stringToPrecomputedChunk(
                '<script nonce="' +
                  escapeTextForBrowser(externalRuntimeConfig) +
                  '"',
              ),
        nonceStyle = "string" === typeof nonce ? void 0 : nonce && nonce.style,
        inlineStyleWithNonce =
          void 0 === nonceStyle
            ? startInlineStyle
            : stringToPrecomputedChunk(
                '<style nonce="' + escapeTextForBrowser(nonceStyle) + '"',
              ),
        idPrefix = resumableState.idPrefix,
        bootstrapChunks = [],
        bootstrapScriptContent = resumableState.bootstrapScriptContent,
        bootstrapScripts = resumableState.bootstrapScripts,
        bootstrapModules = resumableState.bootstrapModules;
      void 0 !== bootstrapScriptContent &&
        (bootstrapChunks.push(inlineScriptWithNonce),
        pushCompletedShellIdAttribute(bootstrapChunks, resumableState),
        bootstrapChunks.push(
          endOfStartTag,
          ("" + bootstrapScriptContent).replace(scriptRegex, scriptReplacer),
          endInlineScript,
        ));
      bootstrapScriptContent = [];
      void 0 !== importMap &&
        (bootstrapScriptContent.push(importMapScriptStart),
        bootstrapScriptContent.push(
          ("" + JSON.stringify(importMap)).replace(scriptRegex, scriptReplacer),
        ),
        bootstrapScriptContent.push(importMapScriptEnd));
      importMap = onHeaders
        ? {
            preconnects: "",
            fontPreloads: "",
            highImagePreloads: "",
            remainingCapacity:
              2 +
              ("number" === typeof maxHeadersLength ? maxHeadersLength : 2e3),
          }
        : null;
      onHeaders = {
        placeholderPrefix: stringToPrecomputedChunk(idPrefix + "P:"),
        segmentPrefix: stringToPrecomputedChunk(idPrefix + "S:"),
        boundaryPrefix: stringToPrecomputedChunk(idPrefix + "B:"),
        startInlineScript: inlineScriptWithNonce,
        startInlineStyle: inlineStyleWithNonce,
        preamble: createPreambleState(),
        externalRuntimeScript: null,
        bootstrapChunks,
        importMapChunks: bootstrapScriptContent,
        onHeaders,
        headers: importMap,
        resets: {
          font: {},
          dns: {},
          connect: { default: {}, anonymous: {}, credentials: {} },
          image: {},
          style: {},
        },
        charsetChunks: [],
        viewportChunks: [],
        hoistableChunks: [],
        preconnects: /* @__PURE__ */ new Set(),
        fontPreloads: /* @__PURE__ */ new Set(),
        highImagePreloads: /* @__PURE__ */ new Set(),
        styles: /* @__PURE__ */ new Map(),
        bootstrapScripts: /* @__PURE__ */ new Set(),
        scripts: /* @__PURE__ */ new Set(),
        bulkPreloads: /* @__PURE__ */ new Set(),
        preloads: {
          images: /* @__PURE__ */ new Map(),
          stylesheets: /* @__PURE__ */ new Map(),
          scripts: /* @__PURE__ */ new Map(),
          moduleScripts: /* @__PURE__ */ new Map(),
        },
        nonce: { script: externalRuntimeConfig, style: nonceStyle },
        hoistableState: null,
        stylesToHoist: false,
      };
      if (void 0 !== bootstrapScripts)
        for (importMap = 0; importMap < bootstrapScripts.length; importMap++)
          ((idPrefix = bootstrapScripts[importMap]),
            (nonceStyle = inlineScriptWithNonce = void 0),
            (inlineStyleWithNonce = {
              rel: "preload",
              as: "script",
              fetchPriority: "low",
              nonce,
            }),
            "string" === typeof idPrefix
              ? (inlineStyleWithNonce.href = maxHeadersLength = idPrefix)
              : ((inlineStyleWithNonce.href = maxHeadersLength = idPrefix.src),
                (inlineStyleWithNonce.integrity = nonceStyle =
                  "string" === typeof idPrefix.integrity
                    ? idPrefix.integrity
                    : void 0),
                (inlineStyleWithNonce.crossOrigin = inlineScriptWithNonce =
                  "string" === typeof idPrefix || null == idPrefix.crossOrigin
                    ? void 0
                    : "use-credentials" === idPrefix.crossOrigin
                      ? "use-credentials"
                      : "")),
            (idPrefix = resumableState),
            (bootstrapScriptContent = maxHeadersLength),
            (idPrefix.scriptResources[bootstrapScriptContent] = null),
            (idPrefix.moduleScriptResources[bootstrapScriptContent] = null),
            (idPrefix = []),
            pushLinkImpl(idPrefix, inlineStyleWithNonce),
            onHeaders.bootstrapScripts.add(idPrefix),
            bootstrapChunks.push(
              startScriptSrc,
              escapeTextForBrowser(maxHeadersLength),
              attributeEnd,
            ),
            externalRuntimeConfig &&
              bootstrapChunks.push(
                scriptNonce,
                escapeTextForBrowser(externalRuntimeConfig),
                attributeEnd,
              ),
            "string" === typeof nonceStyle &&
              bootstrapChunks.push(
                scriptIntegirty,
                escapeTextForBrowser(nonceStyle),
                attributeEnd,
              ),
            "string" === typeof inlineScriptWithNonce &&
              bootstrapChunks.push(
                scriptCrossOrigin,
                escapeTextForBrowser(inlineScriptWithNonce),
                attributeEnd,
              ),
            pushCompletedShellIdAttribute(bootstrapChunks, resumableState),
            bootstrapChunks.push(endAsyncScript));
      if (void 0 !== bootstrapModules)
        for (nonce = 0; nonce < bootstrapModules.length; nonce++)
          ((nonceStyle = bootstrapModules[nonce]),
            (maxHeadersLength = importMap = void 0),
            (inlineScriptWithNonce = {
              rel: "modulepreload",
              fetchPriority: "low",
              nonce: externalRuntimeConfig,
            }),
            "string" === typeof nonceStyle
              ? (inlineScriptWithNonce.href = bootstrapScripts = nonceStyle)
              : ((inlineScriptWithNonce.href = bootstrapScripts =
                  nonceStyle.src),
                (inlineScriptWithNonce.integrity = maxHeadersLength =
                  "string" === typeof nonceStyle.integrity
                    ? nonceStyle.integrity
                    : void 0),
                (inlineScriptWithNonce.crossOrigin = importMap =
                  "string" === typeof nonceStyle ||
                  null == nonceStyle.crossOrigin
                    ? void 0
                    : "use-credentials" === nonceStyle.crossOrigin
                      ? "use-credentials"
                      : "")),
            (nonceStyle = resumableState),
            (inlineStyleWithNonce = bootstrapScripts),
            (nonceStyle.scriptResources[inlineStyleWithNonce] = null),
            (nonceStyle.moduleScriptResources[inlineStyleWithNonce] = null),
            (nonceStyle = []),
            pushLinkImpl(nonceStyle, inlineScriptWithNonce),
            onHeaders.bootstrapScripts.add(nonceStyle),
            bootstrapChunks.push(
              startModuleSrc,
              escapeTextForBrowser(bootstrapScripts),
              attributeEnd,
            ),
            externalRuntimeConfig &&
              bootstrapChunks.push(
                scriptNonce,
                escapeTextForBrowser(externalRuntimeConfig),
                attributeEnd,
              ),
            "string" === typeof maxHeadersLength &&
              bootstrapChunks.push(
                scriptIntegirty,
                escapeTextForBrowser(maxHeadersLength),
                attributeEnd,
              ),
            "string" === typeof importMap &&
              bootstrapChunks.push(
                scriptCrossOrigin,
                escapeTextForBrowser(importMap),
                attributeEnd,
              ),
            pushCompletedShellIdAttribute(bootstrapChunks, resumableState),
            bootstrapChunks.push(endAsyncScript));
      return onHeaders;
    }
    function createResumableState(
      identifierPrefix,
      externalRuntimeConfig,
      bootstrapScriptContent,
      bootstrapScripts,
      bootstrapModules,
    ) {
      return {
        idPrefix: void 0 === identifierPrefix ? "" : identifierPrefix,
        nextFormID: 0,
        streamingFormat: 0,
        bootstrapScriptContent,
        bootstrapScripts,
        bootstrapModules,
        instructions: 0,
        hasBody: false,
        hasHtml: false,
        unknownResources: {},
        dnsResources: {},
        connectResources: { default: {}, anonymous: {}, credentials: {} },
        imageResources: {},
        styleResources: {},
        scriptResources: {},
        moduleUnknownResources: {},
        moduleScriptResources: {},
      };
    }
    function createPreambleState() {
      return { htmlChunks: null, headChunks: null, bodyChunks: null };
    }
    function createFormatContext(
      insertionMode,
      selectedValue,
      tagScope,
      viewTransition,
    ) {
      return {
        insertionMode,
        selectedValue,
        tagScope,
        viewTransition,
      };
    }
    function createRootFormatContext(namespaceURI) {
      return createFormatContext(
        "http://www.w3.org/2000/svg" === namespaceURI
          ? 4
          : "http://www.w3.org/1998/Math/MathML" === namespaceURI
            ? 5
            : 0,
        null,
        0,
        null,
      );
    }
    function getChildFormatContext(parentContext, type, props) {
      var subtreeScope = parentContext.tagScope & -25;
      switch (type) {
        case "noscript":
          return createFormatContext(2, null, subtreeScope | 1, null);
        case "select":
          return createFormatContext(
            2,
            null != props.value ? props.value : props.defaultValue,
            subtreeScope,
            null,
          );
        case "svg":
          return createFormatContext(4, null, subtreeScope, null);
        case "picture":
          return createFormatContext(2, null, subtreeScope | 2, null);
        case "math":
          return createFormatContext(5, null, subtreeScope, null);
        case "foreignObject":
          return createFormatContext(2, null, subtreeScope, null);
        case "table":
          return createFormatContext(6, null, subtreeScope, null);
        case "thead":
        case "tbody":
        case "tfoot":
          return createFormatContext(7, null, subtreeScope, null);
        case "colgroup":
          return createFormatContext(9, null, subtreeScope, null);
        case "tr":
          return createFormatContext(8, null, subtreeScope, null);
        case "head":
          if (2 > parentContext.insertionMode)
            return createFormatContext(3, null, subtreeScope, null);
          break;
        case "html":
          if (0 === parentContext.insertionMode)
            return createFormatContext(1, null, subtreeScope, null);
      }
      return 6 <= parentContext.insertionMode || 2 > parentContext.insertionMode
        ? createFormatContext(2, null, subtreeScope, null)
        : parentContext.tagScope !== subtreeScope
          ? createFormatContext(
              parentContext.insertionMode,
              parentContext.selectedValue,
              subtreeScope,
              null,
            )
          : parentContext;
    }
    function getSuspenseViewTransition(parentViewTransition) {
      return null === parentViewTransition
        ? null
        : {
            update: parentViewTransition.update,
            enter: "none",
            exit: "none",
            share: parentViewTransition.update,
            name: parentViewTransition.autoName,
            autoName: parentViewTransition.autoName,
            nameIdx: 0,
          };
    }
    function getSuspenseFallbackFormatContext(resumableState, parentContext) {
      parentContext.tagScope & 32 && (resumableState.instructions |= 128);
      return createFormatContext(
        parentContext.insertionMode,
        parentContext.selectedValue,
        parentContext.tagScope | 12,
        getSuspenseViewTransition(parentContext.viewTransition),
      );
    }
    function getSuspenseContentFormatContext(resumableState, parentContext) {
      resumableState = getSuspenseViewTransition(parentContext.viewTransition);
      var subtreeScope = parentContext.tagScope | 16;
      null !== resumableState &&
        "none" !== resumableState.share &&
        (subtreeScope |= 64);
      return createFormatContext(
        parentContext.insertionMode,
        parentContext.selectedValue,
        subtreeScope,
        resumableState,
      );
    }
    var textSeparator = stringToPrecomputedChunk("<!-- -->");
    function pushTextInstance(target, text, renderState, textEmbedded) {
      if ("" === text) return textEmbedded;
      textEmbedded && target.push(textSeparator);
      target.push(escapeTextForBrowser(text));
      return true;
    }
    var styleNameCache = /* @__PURE__ */ new Map();
    var styleAttributeStart = stringToPrecomputedChunk(' style="');
    var styleAssign = stringToPrecomputedChunk(":");
    var styleSeparator = stringToPrecomputedChunk(";");
    function pushStyleAttribute(target, style4) {
      if ("object" !== typeof style4)
        throw Error(
          "The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + 'em'}} when using JSX.",
        );
      var isFirst = true,
        styleName;
      for (styleName in style4)
        if (hasOwnProperty.call(style4, styleName)) {
          var styleValue = style4[styleName];
          if (
            null != styleValue &&
            "boolean" !== typeof styleValue &&
            "" !== styleValue
          ) {
            if (0 === styleName.indexOf("--")) {
              var nameChunk = escapeTextForBrowser(styleName);
              styleValue = escapeTextForBrowser(("" + styleValue).trim());
            } else
              ((nameChunk = styleNameCache.get(styleName)),
                void 0 === nameChunk &&
                  ((nameChunk = stringToPrecomputedChunk(
                    escapeTextForBrowser(
                      styleName
                        .replace(uppercasePattern, "-$1")
                        .toLowerCase()
                        .replace(msPattern, "-ms-"),
                    ),
                  )),
                  styleNameCache.set(styleName, nameChunk)),
                (styleValue =
                  "number" === typeof styleValue
                    ? 0 === styleValue || unitlessNumbers.has(styleName)
                      ? "" + styleValue
                      : styleValue + "px"
                    : escapeTextForBrowser(("" + styleValue).trim())));
            isFirst
              ? ((isFirst = false),
                target.push(
                  styleAttributeStart,
                  nameChunk,
                  styleAssign,
                  styleValue,
                ))
              : target.push(styleSeparator, nameChunk, styleAssign, styleValue);
          }
        }
      isFirst || target.push(attributeEnd);
    }
    var attributeSeparator = stringToPrecomputedChunk(" ");
    var attributeAssign = stringToPrecomputedChunk('="');
    var attributeEnd = stringToPrecomputedChunk('"');
    var attributeEmptyString = stringToPrecomputedChunk('=""');
    function pushBooleanAttribute(target, name, value) {
      value &&
        "function" !== typeof value &&
        "symbol" !== typeof value &&
        target.push(attributeSeparator, name, attributeEmptyString);
    }
    function pushStringAttribute(target, name, value) {
      "function" !== typeof value &&
        "symbol" !== typeof value &&
        "boolean" !== typeof value &&
        target.push(
          attributeSeparator,
          name,
          attributeAssign,
          escapeTextForBrowser(value),
          attributeEnd,
        );
    }
    var actionJavaScriptURL = stringToPrecomputedChunk(
      escapeTextForBrowser(
        "javascript:throw new Error('React form unexpectedly submitted.')",
      ),
    );
    var startHiddenInputChunk = stringToPrecomputedChunk(
      '<input type="hidden"',
    );
    function pushAdditionalFormField(value, key) {
      this.push(startHiddenInputChunk);
      validateAdditionalFormField(value);
      pushStringAttribute(this, "name", key);
      pushStringAttribute(this, "value", value);
      this.push(endOfStartTagSelfClosing);
    }
    function validateAdditionalFormField(value) {
      if ("string" !== typeof value)
        throw Error(
          "File/Blob fields are not yet supported in progressive forms. Will fallback to client hydration.",
        );
    }
    function getCustomFormFields(resumableState, formAction) {
      if ("function" === typeof formAction.$$FORM_ACTION) {
        var id = resumableState.nextFormID++;
        resumableState = resumableState.idPrefix + id;
        try {
          var customFields = formAction.$$FORM_ACTION(resumableState);
          if (customFields) {
            var formData = customFields.data;
            null != formData && formData.forEach(validateAdditionalFormField);
          }
          return customFields;
        } catch (x) {
          if (
            "object" === typeof x &&
            null !== x &&
            "function" === typeof x.then
          )
            throw x;
        }
      }
      return null;
    }
    function pushFormActionAttribute(
      target,
      resumableState,
      renderState,
      formAction,
      formEncType,
      formMethod,
      formTarget,
      name,
    ) {
      var formData = null;
      if ("function" === typeof formAction) {
        var customFields = getCustomFormFields(resumableState, formAction);
        null !== customFields
          ? ((name = customFields.name),
            (formAction = customFields.action || ""),
            (formEncType = customFields.encType),
            (formMethod = customFields.method),
            (formTarget = customFields.target),
            (formData = customFields.data))
          : (target.push(
              attributeSeparator,
              "formAction",
              attributeAssign,
              actionJavaScriptURL,
              attributeEnd,
            ),
            (formTarget = formMethod = formEncType = formAction = name = null),
            injectFormReplayingRuntime(resumableState, renderState));
      }
      null != name && pushAttribute(target, "name", name);
      null != formAction && pushAttribute(target, "formAction", formAction);
      null != formEncType && pushAttribute(target, "formEncType", formEncType);
      null != formMethod && pushAttribute(target, "formMethod", formMethod);
      null != formTarget && pushAttribute(target, "formTarget", formTarget);
      return formData;
    }
    function pushAttribute(target, name, value) {
      switch (name) {
        case "className":
          pushStringAttribute(target, "class", value);
          break;
        case "tabIndex":
          pushStringAttribute(target, "tabindex", value);
          break;
        case "dir":
        case "role":
        case "viewBox":
        case "width":
        case "height":
          pushStringAttribute(target, name, value);
          break;
        case "style":
          pushStyleAttribute(target, value);
          break;
        case "src":
        case "href":
          if ("" === value) break;
        case "action":
        case "formAction":
          if (
            null == value ||
            "function" === typeof value ||
            "symbol" === typeof value ||
            "boolean" === typeof value
          )
            break;
          value = sanitizeURL("" + value);
          target.push(
            attributeSeparator,
            name,
            attributeAssign,
            escapeTextForBrowser(value),
            attributeEnd,
          );
          break;
        case "defaultValue":
        case "defaultChecked":
        case "innerHTML":
        case "suppressContentEditableWarning":
        case "suppressHydrationWarning":
        case "ref":
          break;
        case "autoFocus":
        case "multiple":
        case "muted":
          pushBooleanAttribute(target, name.toLowerCase(), value);
          break;
        case "xlinkHref":
          if (
            "function" === typeof value ||
            "symbol" === typeof value ||
            "boolean" === typeof value
          )
            break;
          value = sanitizeURL("" + value);
          target.push(
            attributeSeparator,
            "xlink:href",
            attributeAssign,
            escapeTextForBrowser(value),
            attributeEnd,
          );
          break;
        case "contentEditable":
        case "spellCheck":
        case "draggable":
        case "value":
        case "autoReverse":
        case "externalResourcesRequired":
        case "focusable":
        case "preserveAlpha":
          "function" !== typeof value &&
            "symbol" !== typeof value &&
            target.push(
              attributeSeparator,
              name,
              attributeAssign,
              escapeTextForBrowser(value),
              attributeEnd,
            );
          break;
        case "inert":
        case "allowFullScreen":
        case "async":
        case "autoPlay":
        case "controls":
        case "default":
        case "defer":
        case "disabled":
        case "disablePictureInPicture":
        case "disableRemotePlayback":
        case "formNoValidate":
        case "hidden":
        case "loop":
        case "noModule":
        case "noValidate":
        case "open":
        case "playsInline":
        case "readOnly":
        case "required":
        case "reversed":
        case "scoped":
        case "seamless":
        case "itemScope":
          value &&
            "function" !== typeof value &&
            "symbol" !== typeof value &&
            target.push(attributeSeparator, name, attributeEmptyString);
          break;
        case "capture":
        case "download":
          true === value
            ? target.push(attributeSeparator, name, attributeEmptyString)
            : false !== value &&
              "function" !== typeof value &&
              "symbol" !== typeof value &&
              target.push(
                attributeSeparator,
                name,
                attributeAssign,
                escapeTextForBrowser(value),
                attributeEnd,
              );
          break;
        case "cols":
        case "rows":
        case "size":
        case "span":
          "function" !== typeof value &&
            "symbol" !== typeof value &&
            !isNaN(value) &&
            1 <= value &&
            target.push(
              attributeSeparator,
              name,
              attributeAssign,
              escapeTextForBrowser(value),
              attributeEnd,
            );
          break;
        case "rowSpan":
        case "start":
          "function" === typeof value ||
            "symbol" === typeof value ||
            isNaN(value) ||
            target.push(
              attributeSeparator,
              name,
              attributeAssign,
              escapeTextForBrowser(value),
              attributeEnd,
            );
          break;
        case "xlinkActuate":
          pushStringAttribute(target, "xlink:actuate", value);
          break;
        case "xlinkArcrole":
          pushStringAttribute(target, "xlink:arcrole", value);
          break;
        case "xlinkRole":
          pushStringAttribute(target, "xlink:role", value);
          break;
        case "xlinkShow":
          pushStringAttribute(target, "xlink:show", value);
          break;
        case "xlinkTitle":
          pushStringAttribute(target, "xlink:title", value);
          break;
        case "xlinkType":
          pushStringAttribute(target, "xlink:type", value);
          break;
        case "xmlBase":
          pushStringAttribute(target, "xml:base", value);
          break;
        case "xmlLang":
          pushStringAttribute(target, "xml:lang", value);
          break;
        case "xmlSpace":
          pushStringAttribute(target, "xml:space", value);
          break;
        default:
          if (
            !(2 < name.length) ||
            ("o" !== name[0] && "O" !== name[0]) ||
            ("n" !== name[1] && "N" !== name[1])
          ) {
            if (
              ((name = aliases2.get(name) || name), isAttributeNameSafe(name))
            ) {
              switch (typeof value) {
                case "function":
                case "symbol":
                  return;
                case "boolean":
                  var prefix$8 = name.toLowerCase().slice(0, 5);
                  if ("data-" !== prefix$8 && "aria-" !== prefix$8) return;
              }
              target.push(
                attributeSeparator,
                name,
                attributeAssign,
                escapeTextForBrowser(value),
                attributeEnd,
              );
            }
          }
      }
    }
    var endOfStartTag = stringToPrecomputedChunk(">");
    var endOfStartTagSelfClosing = stringToPrecomputedChunk("/>");
    function pushInnerHTML(target, innerHTML, children) {
      if (null != innerHTML) {
        if (null != children)
          throw Error(
            "Can only set one of `children` or `props.dangerouslySetInnerHTML`.",
          );
        if ("object" !== typeof innerHTML || !("__html" in innerHTML))
          throw Error(
            "`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://react.dev/link/dangerously-set-inner-html for more information.",
          );
        innerHTML = innerHTML.__html;
        null !== innerHTML &&
          void 0 !== innerHTML &&
          target.push("" + innerHTML);
      }
    }
    function flattenOptionChildren(children) {
      var content = "";
      React102.Children.forEach(children, function (child) {
        null != child && (content += child);
      });
      return content;
    }
    var selectedMarkerAttribute = stringToPrecomputedChunk(' selected=""');
    var formReplayingRuntimeScript = stringToPrecomputedChunk(
      `addEventListener("submit",function(a){if(!a.defaultPrevented){var c=a.target,d=a.submitter,e=c.action,b=d;if(d){var f=d.getAttribute("formAction");null!=f&&(e=f,b=null)}"javascript:throw new Error('React form unexpectedly submitted.')"===e&&(a.preventDefault(),b?(a=document.createElement("input"),a.name=b.name,a.value=b.value,b.parentNode.insertBefore(a,b),b=new FormData(c),a.parentNode.removeChild(a)):b=new FormData(c),a=c.ownerDocument||c,(a.$$reactFormReplay=a.$$reactFormReplay||[]).push(c,d,b))}});`,
    );
    function injectFormReplayingRuntime(resumableState, renderState) {
      if (0 === (resumableState.instructions & 16)) {
        resumableState.instructions |= 16;
        var preamble = renderState.preamble,
          bootstrapChunks = renderState.bootstrapChunks;
        (preamble.htmlChunks || preamble.headChunks) &&
        0 === bootstrapChunks.length
          ? (bootstrapChunks.push(renderState.startInlineScript),
            pushCompletedShellIdAttribute(bootstrapChunks, resumableState),
            bootstrapChunks.push(
              endOfStartTag,
              formReplayingRuntimeScript,
              endInlineScript,
            ))
          : bootstrapChunks.unshift(
              renderState.startInlineScript,
              endOfStartTag,
              formReplayingRuntimeScript,
              endInlineScript,
            );
      }
    }
    var formStateMarkerIsMatching = stringToPrecomputedChunk("<!--F!-->");
    var formStateMarkerIsNotMatching = stringToPrecomputedChunk("<!--F-->");
    function pushLinkImpl(target, props) {
      target.push(startChunkForTag("link"));
      for (var propKey in props)
        if (hasOwnProperty.call(props, propKey)) {
          var propValue = props[propKey];
          if (null != propValue)
            switch (propKey) {
              case "children":
              case "dangerouslySetInnerHTML":
                throw Error(
                  "link is a self-closing tag and must neither have `children` nor use `dangerouslySetInnerHTML`.",
                );
              default:
                pushAttribute(target, propKey, propValue);
            }
        }
      target.push(endOfStartTagSelfClosing);
      return null;
    }
    var styleRegex = /(<\/|<)(s)(tyle)/gi;
    function styleReplacer(match2, prefix3, s, suffix2) {
      return "" + prefix3 + ("s" === s ? "\\73 " : "\\53 ") + suffix2;
    }
    function pushSelfClosing(target, props, tag) {
      target.push(startChunkForTag(tag));
      for (var propKey in props)
        if (hasOwnProperty.call(props, propKey)) {
          var propValue = props[propKey];
          if (null != propValue)
            switch (propKey) {
              case "children":
              case "dangerouslySetInnerHTML":
                throw Error(
                  tag +
                    " is a self-closing tag and must neither have `children` nor use `dangerouslySetInnerHTML`.",
                );
              default:
                pushAttribute(target, propKey, propValue);
            }
        }
      target.push(endOfStartTagSelfClosing);
      return null;
    }
    function pushTitleImpl(target, props) {
      target.push(startChunkForTag("title"));
      var children = null,
        innerHTML = null,
        propKey;
      for (propKey in props)
        if (hasOwnProperty.call(props, propKey)) {
          var propValue = props[propKey];
          if (null != propValue)
            switch (propKey) {
              case "children":
                children = propValue;
                break;
              case "dangerouslySetInnerHTML":
                innerHTML = propValue;
                break;
              default:
                pushAttribute(target, propKey, propValue);
            }
        }
      target.push(endOfStartTag);
      props = Array.isArray(children)
        ? 2 > children.length
          ? children[0]
          : null
        : children;
      "function" !== typeof props &&
        "symbol" !== typeof props &&
        null !== props &&
        void 0 !== props &&
        target.push(escapeTextForBrowser("" + props));
      pushInnerHTML(target, innerHTML, children);
      target.push(endChunkForTag("title"));
      return null;
    }
    var headPreambleContributionChunk = stringToPrecomputedChunk("<!--head-->");
    var bodyPreambleContributionChunk = stringToPrecomputedChunk("<!--body-->");
    var htmlPreambleContributionChunk = stringToPrecomputedChunk("<!--html-->");
    function pushScriptImpl(target, props) {
      target.push(startChunkForTag("script"));
      var children = null,
        innerHTML = null,
        propKey;
      for (propKey in props)
        if (hasOwnProperty.call(props, propKey)) {
          var propValue = props[propKey];
          if (null != propValue)
            switch (propKey) {
              case "children":
                children = propValue;
                break;
              case "dangerouslySetInnerHTML":
                innerHTML = propValue;
                break;
              default:
                pushAttribute(target, propKey, propValue);
            }
        }
      target.push(endOfStartTag);
      pushInnerHTML(target, innerHTML, children);
      "string" === typeof children &&
        target.push(("" + children).replace(scriptRegex, scriptReplacer));
      target.push(endChunkForTag("script"));
      return null;
    }
    function pushStartSingletonElement(target, props, tag) {
      target.push(startChunkForTag(tag));
      var innerHTML = (tag = null),
        propKey;
      for (propKey in props)
        if (hasOwnProperty.call(props, propKey)) {
          var propValue = props[propKey];
          if (null != propValue)
            switch (propKey) {
              case "children":
                tag = propValue;
                break;
              case "dangerouslySetInnerHTML":
                innerHTML = propValue;
                break;
              default:
                pushAttribute(target, propKey, propValue);
            }
        }
      target.push(endOfStartTag);
      pushInnerHTML(target, innerHTML, tag);
      return tag;
    }
    function pushStartGenericElement(target, props, tag) {
      target.push(startChunkForTag(tag));
      var innerHTML = (tag = null),
        propKey;
      for (propKey in props)
        if (hasOwnProperty.call(props, propKey)) {
          var propValue = props[propKey];
          if (null != propValue)
            switch (propKey) {
              case "children":
                tag = propValue;
                break;
              case "dangerouslySetInnerHTML":
                innerHTML = propValue;
                break;
              default:
                pushAttribute(target, propKey, propValue);
            }
        }
      target.push(endOfStartTag);
      pushInnerHTML(target, innerHTML, tag);
      return "string" === typeof tag
        ? (target.push(escapeTextForBrowser(tag)), null)
        : tag;
    }
    var leadingNewline = stringToPrecomputedChunk("\n");
    var VALID_TAG_REGEX = /^[a-zA-Z][a-zA-Z:_\.\-\d]*$/;
    var validatedTagCache = /* @__PURE__ */ new Map();
    function startChunkForTag(tag) {
      var tagStartChunk = validatedTagCache.get(tag);
      if (void 0 === tagStartChunk) {
        if (!VALID_TAG_REGEX.test(tag)) throw Error("Invalid tag: " + tag);
        tagStartChunk = stringToPrecomputedChunk("<" + tag);
        validatedTagCache.set(tag, tagStartChunk);
      }
      return tagStartChunk;
    }
    var doctypeChunk = stringToPrecomputedChunk("<!DOCTYPE html>");
    function pushStartInstance(
      target$jscomp$0,
      type,
      props,
      resumableState,
      renderState,
      preambleState,
      hoistableState,
      formatContext,
      textEmbedded,
    ) {
      switch (type) {
        case "div":
        case "span":
        case "svg":
        case "path":
          break;
        case "a":
          target$jscomp$0.push(startChunkForTag("a"));
          var children = null,
            innerHTML = null,
            propKey;
          for (propKey in props)
            if (hasOwnProperty.call(props, propKey)) {
              var propValue = props[propKey];
              if (null != propValue)
                switch (propKey) {
                  case "children":
                    children = propValue;
                    break;
                  case "dangerouslySetInnerHTML":
                    innerHTML = propValue;
                    break;
                  case "href":
                    "" === propValue
                      ? pushStringAttribute(target$jscomp$0, "href", "")
                      : pushAttribute(target$jscomp$0, propKey, propValue);
                    break;
                  default:
                    pushAttribute(target$jscomp$0, propKey, propValue);
                }
            }
          target$jscomp$0.push(endOfStartTag);
          pushInnerHTML(target$jscomp$0, innerHTML, children);
          if ("string" === typeof children) {
            target$jscomp$0.push(escapeTextForBrowser(children));
            var JSCompiler_inline_result = null;
          } else JSCompiler_inline_result = children;
          return JSCompiler_inline_result;
        case "g":
        case "p":
        case "li":
          break;
        case "select":
          target$jscomp$0.push(startChunkForTag("select"));
          var children$jscomp$0 = null,
            innerHTML$jscomp$0 = null,
            propKey$jscomp$0;
          for (propKey$jscomp$0 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$0)) {
              var propValue$jscomp$0 = props[propKey$jscomp$0];
              if (null != propValue$jscomp$0)
                switch (propKey$jscomp$0) {
                  case "children":
                    children$jscomp$0 = propValue$jscomp$0;
                    break;
                  case "dangerouslySetInnerHTML":
                    innerHTML$jscomp$0 = propValue$jscomp$0;
                    break;
                  case "defaultValue":
                  case "value":
                    break;
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$0,
                      propValue$jscomp$0,
                    );
                }
            }
          target$jscomp$0.push(endOfStartTag);
          pushInnerHTML(target$jscomp$0, innerHTML$jscomp$0, children$jscomp$0);
          return children$jscomp$0;
        case "option":
          var selectedValue = formatContext.selectedValue;
          target$jscomp$0.push(startChunkForTag("option"));
          var children$jscomp$1 = null,
            value = null,
            selected = null,
            innerHTML$jscomp$1 = null,
            propKey$jscomp$1;
          for (propKey$jscomp$1 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$1)) {
              var propValue$jscomp$1 = props[propKey$jscomp$1];
              if (null != propValue$jscomp$1)
                switch (propKey$jscomp$1) {
                  case "children":
                    children$jscomp$1 = propValue$jscomp$1;
                    break;
                  case "selected":
                    selected = propValue$jscomp$1;
                    break;
                  case "dangerouslySetInnerHTML":
                    innerHTML$jscomp$1 = propValue$jscomp$1;
                    break;
                  case "value":
                    value = propValue$jscomp$1;
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$1,
                      propValue$jscomp$1,
                    );
                }
            }
          if (null != selectedValue) {
            var stringValue =
              null !== value
                ? "" + value
                : flattenOptionChildren(children$jscomp$1);
            if (isArrayImpl(selectedValue))
              for (var i = 0; i < selectedValue.length; i++) {
                if ("" + selectedValue[i] === stringValue) {
                  target$jscomp$0.push(selectedMarkerAttribute);
                  break;
                }
              }
            else
              "" + selectedValue === stringValue &&
                target$jscomp$0.push(selectedMarkerAttribute);
          } else selected && target$jscomp$0.push(selectedMarkerAttribute);
          target$jscomp$0.push(endOfStartTag);
          pushInnerHTML(target$jscomp$0, innerHTML$jscomp$1, children$jscomp$1);
          return children$jscomp$1;
        case "textarea":
          target$jscomp$0.push(startChunkForTag("textarea"));
          var value$jscomp$0 = null,
            defaultValue = null,
            children$jscomp$2 = null,
            propKey$jscomp$2;
          for (propKey$jscomp$2 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$2)) {
              var propValue$jscomp$2 = props[propKey$jscomp$2];
              if (null != propValue$jscomp$2)
                switch (propKey$jscomp$2) {
                  case "children":
                    children$jscomp$2 = propValue$jscomp$2;
                    break;
                  case "value":
                    value$jscomp$0 = propValue$jscomp$2;
                    break;
                  case "defaultValue":
                    defaultValue = propValue$jscomp$2;
                    break;
                  case "dangerouslySetInnerHTML":
                    throw Error(
                      "`dangerouslySetInnerHTML` does not make sense on <textarea>.",
                    );
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$2,
                      propValue$jscomp$2,
                    );
                }
            }
          null === value$jscomp$0 &&
            null !== defaultValue &&
            (value$jscomp$0 = defaultValue);
          target$jscomp$0.push(endOfStartTag);
          if (null != children$jscomp$2) {
            if (null != value$jscomp$0)
              throw Error(
                "If you supply `defaultValue` on a <textarea>, do not pass children.",
              );
            if (isArrayImpl(children$jscomp$2)) {
              if (1 < children$jscomp$2.length)
                throw Error("<textarea> can only have at most one child.");
              value$jscomp$0 = "" + children$jscomp$2[0];
            }
            value$jscomp$0 = "" + children$jscomp$2;
          }
          "string" === typeof value$jscomp$0 &&
            "\n" === value$jscomp$0[0] &&
            target$jscomp$0.push(leadingNewline);
          null !== value$jscomp$0 &&
            target$jscomp$0.push(escapeTextForBrowser("" + value$jscomp$0));
          return null;
        case "input":
          target$jscomp$0.push(startChunkForTag("input"));
          var name = null,
            formAction = null,
            formEncType = null,
            formMethod = null,
            formTarget = null,
            value$jscomp$1 = null,
            defaultValue$jscomp$0 = null,
            checked = null,
            defaultChecked = null,
            propKey$jscomp$3;
          for (propKey$jscomp$3 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$3)) {
              var propValue$jscomp$3 = props[propKey$jscomp$3];
              if (null != propValue$jscomp$3)
                switch (propKey$jscomp$3) {
                  case "children":
                  case "dangerouslySetInnerHTML":
                    throw Error(
                      "input is a self-closing tag and must neither have `children` nor use `dangerouslySetInnerHTML`.",
                    );
                  case "name":
                    name = propValue$jscomp$3;
                    break;
                  case "formAction":
                    formAction = propValue$jscomp$3;
                    break;
                  case "formEncType":
                    formEncType = propValue$jscomp$3;
                    break;
                  case "formMethod":
                    formMethod = propValue$jscomp$3;
                    break;
                  case "formTarget":
                    formTarget = propValue$jscomp$3;
                    break;
                  case "defaultChecked":
                    defaultChecked = propValue$jscomp$3;
                    break;
                  case "defaultValue":
                    defaultValue$jscomp$0 = propValue$jscomp$3;
                    break;
                  case "checked":
                    checked = propValue$jscomp$3;
                    break;
                  case "value":
                    value$jscomp$1 = propValue$jscomp$3;
                    break;
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$3,
                      propValue$jscomp$3,
                    );
                }
            }
          var formData = pushFormActionAttribute(
            target$jscomp$0,
            resumableState,
            renderState,
            formAction,
            formEncType,
            formMethod,
            formTarget,
            name,
          );
          null !== checked
            ? pushBooleanAttribute(target$jscomp$0, "checked", checked)
            : null !== defaultChecked &&
              pushBooleanAttribute(target$jscomp$0, "checked", defaultChecked);
          null !== value$jscomp$1
            ? pushAttribute(target$jscomp$0, "value", value$jscomp$1)
            : null !== defaultValue$jscomp$0 &&
              pushAttribute(target$jscomp$0, "value", defaultValue$jscomp$0);
          target$jscomp$0.push(endOfStartTagSelfClosing);
          null != formData &&
            formData.forEach(pushAdditionalFormField, target$jscomp$0);
          return null;
        case "button":
          target$jscomp$0.push(startChunkForTag("button"));
          var children$jscomp$3 = null,
            innerHTML$jscomp$2 = null,
            name$jscomp$0 = null,
            formAction$jscomp$0 = null,
            formEncType$jscomp$0 = null,
            formMethod$jscomp$0 = null,
            formTarget$jscomp$0 = null,
            propKey$jscomp$4;
          for (propKey$jscomp$4 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$4)) {
              var propValue$jscomp$4 = props[propKey$jscomp$4];
              if (null != propValue$jscomp$4)
                switch (propKey$jscomp$4) {
                  case "children":
                    children$jscomp$3 = propValue$jscomp$4;
                    break;
                  case "dangerouslySetInnerHTML":
                    innerHTML$jscomp$2 = propValue$jscomp$4;
                    break;
                  case "name":
                    name$jscomp$0 = propValue$jscomp$4;
                    break;
                  case "formAction":
                    formAction$jscomp$0 = propValue$jscomp$4;
                    break;
                  case "formEncType":
                    formEncType$jscomp$0 = propValue$jscomp$4;
                    break;
                  case "formMethod":
                    formMethod$jscomp$0 = propValue$jscomp$4;
                    break;
                  case "formTarget":
                    formTarget$jscomp$0 = propValue$jscomp$4;
                    break;
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$4,
                      propValue$jscomp$4,
                    );
                }
            }
          var formData$jscomp$0 = pushFormActionAttribute(
            target$jscomp$0,
            resumableState,
            renderState,
            formAction$jscomp$0,
            formEncType$jscomp$0,
            formMethod$jscomp$0,
            formTarget$jscomp$0,
            name$jscomp$0,
          );
          target$jscomp$0.push(endOfStartTag);
          null != formData$jscomp$0 &&
            formData$jscomp$0.forEach(pushAdditionalFormField, target$jscomp$0);
          pushInnerHTML(target$jscomp$0, innerHTML$jscomp$2, children$jscomp$3);
          if ("string" === typeof children$jscomp$3) {
            target$jscomp$0.push(escapeTextForBrowser(children$jscomp$3));
            var JSCompiler_inline_result$jscomp$0 = null;
          } else JSCompiler_inline_result$jscomp$0 = children$jscomp$3;
          return JSCompiler_inline_result$jscomp$0;
        case "form":
          target$jscomp$0.push(startChunkForTag("form"));
          var children$jscomp$4 = null,
            innerHTML$jscomp$3 = null,
            formAction$jscomp$1 = null,
            formEncType$jscomp$1 = null,
            formMethod$jscomp$1 = null,
            formTarget$jscomp$1 = null,
            propKey$jscomp$5;
          for (propKey$jscomp$5 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$5)) {
              var propValue$jscomp$5 = props[propKey$jscomp$5];
              if (null != propValue$jscomp$5)
                switch (propKey$jscomp$5) {
                  case "children":
                    children$jscomp$4 = propValue$jscomp$5;
                    break;
                  case "dangerouslySetInnerHTML":
                    innerHTML$jscomp$3 = propValue$jscomp$5;
                    break;
                  case "action":
                    formAction$jscomp$1 = propValue$jscomp$5;
                    break;
                  case "encType":
                    formEncType$jscomp$1 = propValue$jscomp$5;
                    break;
                  case "method":
                    formMethod$jscomp$1 = propValue$jscomp$5;
                    break;
                  case "target":
                    formTarget$jscomp$1 = propValue$jscomp$5;
                    break;
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$5,
                      propValue$jscomp$5,
                    );
                }
            }
          var formData$jscomp$1 = null,
            formActionName = null;
          if ("function" === typeof formAction$jscomp$1) {
            var customFields = getCustomFormFields(
              resumableState,
              formAction$jscomp$1,
            );
            null !== customFields
              ? ((formAction$jscomp$1 = customFields.action || ""),
                (formEncType$jscomp$1 = customFields.encType),
                (formMethod$jscomp$1 = customFields.method),
                (formTarget$jscomp$1 = customFields.target),
                (formData$jscomp$1 = customFields.data),
                (formActionName = customFields.name))
              : (target$jscomp$0.push(
                  attributeSeparator,
                  "action",
                  attributeAssign,
                  actionJavaScriptURL,
                  attributeEnd,
                ),
                (formTarget$jscomp$1 =
                  formMethod$jscomp$1 =
                  formEncType$jscomp$1 =
                  formAction$jscomp$1 =
                    null),
                injectFormReplayingRuntime(resumableState, renderState));
          }
          null != formAction$jscomp$1 &&
            pushAttribute(target$jscomp$0, "action", formAction$jscomp$1);
          null != formEncType$jscomp$1 &&
            pushAttribute(target$jscomp$0, "encType", formEncType$jscomp$1);
          null != formMethod$jscomp$1 &&
            pushAttribute(target$jscomp$0, "method", formMethod$jscomp$1);
          null != formTarget$jscomp$1 &&
            pushAttribute(target$jscomp$0, "target", formTarget$jscomp$1);
          target$jscomp$0.push(endOfStartTag);
          null !== formActionName &&
            (target$jscomp$0.push(startHiddenInputChunk),
            pushStringAttribute(target$jscomp$0, "name", formActionName),
            target$jscomp$0.push(endOfStartTagSelfClosing),
            null != formData$jscomp$1 &&
              formData$jscomp$1.forEach(
                pushAdditionalFormField,
                target$jscomp$0,
              ));
          pushInnerHTML(target$jscomp$0, innerHTML$jscomp$3, children$jscomp$4);
          if ("string" === typeof children$jscomp$4) {
            target$jscomp$0.push(escapeTextForBrowser(children$jscomp$4));
            var JSCompiler_inline_result$jscomp$1 = null;
          } else JSCompiler_inline_result$jscomp$1 = children$jscomp$4;
          return JSCompiler_inline_result$jscomp$1;
        case "menuitem":
          target$jscomp$0.push(startChunkForTag("menuitem"));
          for (var propKey$jscomp$6 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$6)) {
              var propValue$jscomp$6 = props[propKey$jscomp$6];
              if (null != propValue$jscomp$6)
                switch (propKey$jscomp$6) {
                  case "children":
                  case "dangerouslySetInnerHTML":
                    throw Error(
                      "menuitems cannot have `children` nor `dangerouslySetInnerHTML`.",
                    );
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$6,
                      propValue$jscomp$6,
                    );
                }
            }
          target$jscomp$0.push(endOfStartTag);
          return null;
        case "object":
          target$jscomp$0.push(startChunkForTag("object"));
          var children$jscomp$5 = null,
            innerHTML$jscomp$4 = null,
            propKey$jscomp$7;
          for (propKey$jscomp$7 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$7)) {
              var propValue$jscomp$7 = props[propKey$jscomp$7];
              if (null != propValue$jscomp$7)
                switch (propKey$jscomp$7) {
                  case "children":
                    children$jscomp$5 = propValue$jscomp$7;
                    break;
                  case "dangerouslySetInnerHTML":
                    innerHTML$jscomp$4 = propValue$jscomp$7;
                    break;
                  case "data":
                    var sanitizedValue = sanitizeURL("" + propValue$jscomp$7);
                    if ("" === sanitizedValue) break;
                    target$jscomp$0.push(
                      attributeSeparator,
                      "data",
                      attributeAssign,
                      escapeTextForBrowser(sanitizedValue),
                      attributeEnd,
                    );
                    break;
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$7,
                      propValue$jscomp$7,
                    );
                }
            }
          target$jscomp$0.push(endOfStartTag);
          pushInnerHTML(target$jscomp$0, innerHTML$jscomp$4, children$jscomp$5);
          if ("string" === typeof children$jscomp$5) {
            target$jscomp$0.push(escapeTextForBrowser(children$jscomp$5));
            var JSCompiler_inline_result$jscomp$2 = null;
          } else JSCompiler_inline_result$jscomp$2 = children$jscomp$5;
          return JSCompiler_inline_result$jscomp$2;
        case "title":
          var noscriptTagInScope = formatContext.tagScope & 1,
            isFallback = formatContext.tagScope & 4;
          if (
            4 === formatContext.insertionMode ||
            noscriptTagInScope ||
            null != props.itemProp
          )
            var JSCompiler_inline_result$jscomp$3 = pushTitleImpl(
              target$jscomp$0,
              props,
            );
          else
            isFallback
              ? (JSCompiler_inline_result$jscomp$3 = null)
              : (pushTitleImpl(renderState.hoistableChunks, props),
                (JSCompiler_inline_result$jscomp$3 = void 0));
          return JSCompiler_inline_result$jscomp$3;
        case "link":
          var noscriptTagInScope$jscomp$0 = formatContext.tagScope & 1,
            isFallback$jscomp$0 = formatContext.tagScope & 4,
            rel = props.rel,
            href = props.href,
            precedence = props.precedence;
          if (
            4 === formatContext.insertionMode ||
            noscriptTagInScope$jscomp$0 ||
            null != props.itemProp ||
            "string" !== typeof rel ||
            "string" !== typeof href ||
            "" === href
          ) {
            pushLinkImpl(target$jscomp$0, props);
            var JSCompiler_inline_result$jscomp$4 = null;
          } else if ("stylesheet" === props.rel)
            if (
              "string" !== typeof precedence ||
              null != props.disabled ||
              props.onLoad ||
              props.onError
            )
              JSCompiler_inline_result$jscomp$4 = pushLinkImpl(
                target$jscomp$0,
                props,
              );
            else {
              var styleQueue = renderState.styles.get(precedence),
                resourceState = resumableState.styleResources.hasOwnProperty(
                  href,
                )
                  ? resumableState.styleResources[href]
                  : void 0;
              if (null !== resourceState) {
                resumableState.styleResources[href] = null;
                styleQueue ||
                  ((styleQueue = {
                    precedence: escapeTextForBrowser(precedence),
                    rules: [],
                    hrefs: [],
                    sheets: /* @__PURE__ */ new Map(),
                  }),
                  renderState.styles.set(precedence, styleQueue));
                var resource = {
                  state: 0,
                  props: assign2({}, props, {
                    "data-precedence": props.precedence,
                    precedence: null,
                  }),
                };
                if (resourceState) {
                  2 === resourceState.length &&
                    adoptPreloadCredentials(resource.props, resourceState);
                  var preloadResource =
                    renderState.preloads.stylesheets.get(href);
                  preloadResource && 0 < preloadResource.length
                    ? (preloadResource.length = 0)
                    : (resource.state = 1);
                }
                styleQueue.sheets.set(href, resource);
                hoistableState && hoistableState.stylesheets.add(resource);
              } else if (styleQueue) {
                var resource$9 = styleQueue.sheets.get(href);
                resource$9 &&
                  hoistableState &&
                  hoistableState.stylesheets.add(resource$9);
              }
              textEmbedded && target$jscomp$0.push(textSeparator);
              JSCompiler_inline_result$jscomp$4 = null;
            }
          else
            props.onLoad || props.onError
              ? (JSCompiler_inline_result$jscomp$4 = pushLinkImpl(
                  target$jscomp$0,
                  props,
                ))
              : (textEmbedded && target$jscomp$0.push(textSeparator),
                (JSCompiler_inline_result$jscomp$4 = isFallback$jscomp$0
                  ? null
                  : pushLinkImpl(renderState.hoistableChunks, props)));
          return JSCompiler_inline_result$jscomp$4;
        case "script":
          var noscriptTagInScope$jscomp$1 = formatContext.tagScope & 1,
            asyncProp = props.async;
          if (
            "string" !== typeof props.src ||
            !props.src ||
            !asyncProp ||
            "function" === typeof asyncProp ||
            "symbol" === typeof asyncProp ||
            props.onLoad ||
            props.onError ||
            4 === formatContext.insertionMode ||
            noscriptTagInScope$jscomp$1 ||
            null != props.itemProp
          )
            var JSCompiler_inline_result$jscomp$5 = pushScriptImpl(
              target$jscomp$0,
              props,
            );
          else {
            var key = props.src;
            if ("module" === props.type) {
              var resources = resumableState.moduleScriptResources;
              var preloads = renderState.preloads.moduleScripts;
            } else
              ((resources = resumableState.scriptResources),
                (preloads = renderState.preloads.scripts));
            var resourceState$jscomp$0 = resources.hasOwnProperty(key)
              ? resources[key]
              : void 0;
            if (null !== resourceState$jscomp$0) {
              resources[key] = null;
              var scriptProps = props;
              if (resourceState$jscomp$0) {
                2 === resourceState$jscomp$0.length &&
                  ((scriptProps = assign2({}, props)),
                  adoptPreloadCredentials(scriptProps, resourceState$jscomp$0));
                var preloadResource$jscomp$0 = preloads.get(key);
                preloadResource$jscomp$0 &&
                  (preloadResource$jscomp$0.length = 0);
              }
              var resource$jscomp$0 = [];
              renderState.scripts.add(resource$jscomp$0);
              pushScriptImpl(resource$jscomp$0, scriptProps);
            }
            textEmbedded && target$jscomp$0.push(textSeparator);
            JSCompiler_inline_result$jscomp$5 = null;
          }
          return JSCompiler_inline_result$jscomp$5;
        case "style":
          var noscriptTagInScope$jscomp$2 = formatContext.tagScope & 1,
            precedence$jscomp$0 = props.precedence,
            href$jscomp$0 = props.href,
            nonce = props.nonce;
          if (
            4 === formatContext.insertionMode ||
            noscriptTagInScope$jscomp$2 ||
            null != props.itemProp ||
            "string" !== typeof precedence$jscomp$0 ||
            "string" !== typeof href$jscomp$0 ||
            "" === href$jscomp$0
          ) {
            target$jscomp$0.push(startChunkForTag("style"));
            var children$jscomp$6 = null,
              innerHTML$jscomp$5 = null,
              propKey$jscomp$8;
            for (propKey$jscomp$8 in props)
              if (hasOwnProperty.call(props, propKey$jscomp$8)) {
                var propValue$jscomp$8 = props[propKey$jscomp$8];
                if (null != propValue$jscomp$8)
                  switch (propKey$jscomp$8) {
                    case "children":
                      children$jscomp$6 = propValue$jscomp$8;
                      break;
                    case "dangerouslySetInnerHTML":
                      innerHTML$jscomp$5 = propValue$jscomp$8;
                      break;
                    default:
                      pushAttribute(
                        target$jscomp$0,
                        propKey$jscomp$8,
                        propValue$jscomp$8,
                      );
                  }
              }
            target$jscomp$0.push(endOfStartTag);
            var child = Array.isArray(children$jscomp$6)
              ? 2 > children$jscomp$6.length
                ? children$jscomp$6[0]
                : null
              : children$jscomp$6;
            "function" !== typeof child &&
              "symbol" !== typeof child &&
              null !== child &&
              void 0 !== child &&
              target$jscomp$0.push(
                ("" + child).replace(styleRegex, styleReplacer),
              );
            pushInnerHTML(
              target$jscomp$0,
              innerHTML$jscomp$5,
              children$jscomp$6,
            );
            target$jscomp$0.push(endChunkForTag("style"));
            var JSCompiler_inline_result$jscomp$6 = null;
          } else {
            var styleQueue$jscomp$0 =
              renderState.styles.get(precedence$jscomp$0);
            if (
              null !==
              (resumableState.styleResources.hasOwnProperty(href$jscomp$0)
                ? resumableState.styleResources[href$jscomp$0]
                : void 0)
            ) {
              resumableState.styleResources[href$jscomp$0] = null;
              styleQueue$jscomp$0 ||
                ((styleQueue$jscomp$0 = {
                  precedence: escapeTextForBrowser(precedence$jscomp$0),
                  rules: [],
                  hrefs: [],
                  sheets: /* @__PURE__ */ new Map(),
                }),
                renderState.styles.set(
                  precedence$jscomp$0,
                  styleQueue$jscomp$0,
                ));
              var nonceStyle = renderState.nonce.style;
              if (!nonceStyle || nonceStyle === nonce) {
                styleQueue$jscomp$0.hrefs.push(
                  escapeTextForBrowser(href$jscomp$0),
                );
                var target = styleQueue$jscomp$0.rules,
                  children$jscomp$7 = null,
                  innerHTML$jscomp$6 = null,
                  propKey$jscomp$9;
                for (propKey$jscomp$9 in props)
                  if (hasOwnProperty.call(props, propKey$jscomp$9)) {
                    var propValue$jscomp$9 = props[propKey$jscomp$9];
                    if (null != propValue$jscomp$9)
                      switch (propKey$jscomp$9) {
                        case "children":
                          children$jscomp$7 = propValue$jscomp$9;
                          break;
                        case "dangerouslySetInnerHTML":
                          innerHTML$jscomp$6 = propValue$jscomp$9;
                      }
                  }
                var child$jscomp$0 = Array.isArray(children$jscomp$7)
                  ? 2 > children$jscomp$7.length
                    ? children$jscomp$7[0]
                    : null
                  : children$jscomp$7;
                "function" !== typeof child$jscomp$0 &&
                  "symbol" !== typeof child$jscomp$0 &&
                  null !== child$jscomp$0 &&
                  void 0 !== child$jscomp$0 &&
                  target.push(
                    ("" + child$jscomp$0).replace(styleRegex, styleReplacer),
                  );
                pushInnerHTML(target, innerHTML$jscomp$6, children$jscomp$7);
              }
            }
            styleQueue$jscomp$0 &&
              hoistableState &&
              hoistableState.styles.add(styleQueue$jscomp$0);
            textEmbedded && target$jscomp$0.push(textSeparator);
            JSCompiler_inline_result$jscomp$6 = void 0;
          }
          return JSCompiler_inline_result$jscomp$6;
        case "meta":
          var noscriptTagInScope$jscomp$3 = formatContext.tagScope & 1,
            isFallback$jscomp$1 = formatContext.tagScope & 4;
          if (
            4 === formatContext.insertionMode ||
            noscriptTagInScope$jscomp$3 ||
            null != props.itemProp
          )
            var JSCompiler_inline_result$jscomp$7 = pushSelfClosing(
              target$jscomp$0,
              props,
              "meta",
            );
          else
            (textEmbedded && target$jscomp$0.push(textSeparator),
              (JSCompiler_inline_result$jscomp$7 = isFallback$jscomp$1
                ? null
                : "string" === typeof props.charSet
                  ? pushSelfClosing(renderState.charsetChunks, props, "meta")
                  : "viewport" === props.name
                    ? pushSelfClosing(renderState.viewportChunks, props, "meta")
                    : pushSelfClosing(
                        renderState.hoistableChunks,
                        props,
                        "meta",
                      )));
          return JSCompiler_inline_result$jscomp$7;
        case "listing":
        case "pre":
          target$jscomp$0.push(startChunkForTag(type));
          var children$jscomp$8 = null,
            innerHTML$jscomp$7 = null,
            propKey$jscomp$10;
          for (propKey$jscomp$10 in props)
            if (hasOwnProperty.call(props, propKey$jscomp$10)) {
              var propValue$jscomp$10 = props[propKey$jscomp$10];
              if (null != propValue$jscomp$10)
                switch (propKey$jscomp$10) {
                  case "children":
                    children$jscomp$8 = propValue$jscomp$10;
                    break;
                  case "dangerouslySetInnerHTML":
                    innerHTML$jscomp$7 = propValue$jscomp$10;
                    break;
                  default:
                    pushAttribute(
                      target$jscomp$0,
                      propKey$jscomp$10,
                      propValue$jscomp$10,
                    );
                }
            }
          target$jscomp$0.push(endOfStartTag);
          if (null != innerHTML$jscomp$7) {
            if (null != children$jscomp$8)
              throw Error(
                "Can only set one of `children` or `props.dangerouslySetInnerHTML`.",
              );
            if (
              "object" !== typeof innerHTML$jscomp$7 ||
              !("__html" in innerHTML$jscomp$7)
            )
              throw Error(
                "`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://react.dev/link/dangerously-set-inner-html for more information.",
              );
            var html2 = innerHTML$jscomp$7.__html;
            null !== html2 &&
              void 0 !== html2 &&
              ("string" === typeof html2 &&
              0 < html2.length &&
              "\n" === html2[0]
                ? target$jscomp$0.push(leadingNewline, html2)
                : target$jscomp$0.push("" + html2));
          }
          "string" === typeof children$jscomp$8 &&
            "\n" === children$jscomp$8[0] &&
            target$jscomp$0.push(leadingNewline);
          return children$jscomp$8;
        case "img":
          var pictureOrNoScriptTagInScope = formatContext.tagScope & 3,
            src = props.src,
            srcSet = props.srcSet;
          if (
            !(
              "lazy" === props.loading ||
              (!src && !srcSet) ||
              ("string" !== typeof src && null != src) ||
              ("string" !== typeof srcSet && null != srcSet) ||
              "low" === props.fetchPriority ||
              pictureOrNoScriptTagInScope
            ) &&
            ("string" !== typeof src ||
              ":" !== src[4] ||
              ("d" !== src[0] && "D" !== src[0]) ||
              ("a" !== src[1] && "A" !== src[1]) ||
              ("t" !== src[2] && "T" !== src[2]) ||
              ("a" !== src[3] && "A" !== src[3])) &&
            ("string" !== typeof srcSet ||
              ":" !== srcSet[4] ||
              ("d" !== srcSet[0] && "D" !== srcSet[0]) ||
              ("a" !== srcSet[1] && "A" !== srcSet[1]) ||
              ("t" !== srcSet[2] && "T" !== srcSet[2]) ||
              ("a" !== srcSet[3] && "A" !== srcSet[3]))
          ) {
            null !== hoistableState &&
              formatContext.tagScope & 64 &&
              (hoistableState.suspenseyImages = true);
            var sizes = "string" === typeof props.sizes ? props.sizes : void 0,
              key$jscomp$0 = srcSet ? srcSet + "\n" + (sizes || "") : src,
              promotablePreloads = renderState.preloads.images,
              resource$jscomp$1 = promotablePreloads.get(key$jscomp$0);
            if (resource$jscomp$1) {
              if (
                "high" === props.fetchPriority ||
                10 > renderState.highImagePreloads.size
              )
                (promotablePreloads.delete(key$jscomp$0),
                  renderState.highImagePreloads.add(resource$jscomp$1));
            } else if (
              !resumableState.imageResources.hasOwnProperty(key$jscomp$0)
            ) {
              resumableState.imageResources[key$jscomp$0] = PRELOAD_NO_CREDS;
              var input = props.crossOrigin;
              var JSCompiler_inline_result$jscomp$8 =
                "string" === typeof input
                  ? "use-credentials" === input
                    ? input
                    : ""
                  : void 0;
              var headers = renderState.headers,
                header;
              headers &&
              0 < headers.remainingCapacity &&
              "string" !== typeof props.srcSet &&
              ("high" === props.fetchPriority ||
                500 > headers.highImagePreloads.length) &&
              ((header = getPreloadAsHeader(src, "image", {
                imageSrcSet: props.srcSet,
                imageSizes: props.sizes,
                crossOrigin: JSCompiler_inline_result$jscomp$8,
                integrity: props.integrity,
                nonce: props.nonce,
                type: props.type,
                fetchPriority: props.fetchPriority,
                referrerPolicy: props.refererPolicy,
              })),
              0 <= (headers.remainingCapacity -= header.length + 2))
                ? ((renderState.resets.image[key$jscomp$0] = PRELOAD_NO_CREDS),
                  headers.highImagePreloads &&
                    (headers.highImagePreloads += ", "),
                  (headers.highImagePreloads += header))
                : ((resource$jscomp$1 = []),
                  pushLinkImpl(resource$jscomp$1, {
                    rel: "preload",
                    as: "image",
                    href: srcSet ? void 0 : src,
                    imageSrcSet: srcSet,
                    imageSizes: sizes,
                    crossOrigin: JSCompiler_inline_result$jscomp$8,
                    integrity: props.integrity,
                    type: props.type,
                    fetchPriority: props.fetchPriority,
                    referrerPolicy: props.referrerPolicy,
                  }),
                  "high" === props.fetchPriority ||
                  10 > renderState.highImagePreloads.size
                    ? renderState.highImagePreloads.add(resource$jscomp$1)
                    : (renderState.bulkPreloads.add(resource$jscomp$1),
                      promotablePreloads.set(key$jscomp$0, resource$jscomp$1)));
            }
          }
          return pushSelfClosing(target$jscomp$0, props, "img");
        case "base":
        case "area":
        case "br":
        case "col":
        case "embed":
        case "hr":
        case "keygen":
        case "param":
        case "source":
        case "track":
        case "wbr":
          return pushSelfClosing(target$jscomp$0, props, type);
        case "annotation-xml":
        case "color-profile":
        case "font-face":
        case "font-face-src":
        case "font-face-uri":
        case "font-face-format":
        case "font-face-name":
        case "missing-glyph":
          break;
        case "head":
          if (2 > formatContext.insertionMode) {
            var preamble = preambleState || renderState.preamble;
            if (preamble.headChunks)
              throw Error("The `<head>` tag may only be rendered once.");
            null !== preambleState &&
              target$jscomp$0.push(headPreambleContributionChunk);
            preamble.headChunks = [];
            var JSCompiler_inline_result$jscomp$9 = pushStartSingletonElement(
              preamble.headChunks,
              props,
              "head",
            );
          } else
            JSCompiler_inline_result$jscomp$9 = pushStartGenericElement(
              target$jscomp$0,
              props,
              "head",
            );
          return JSCompiler_inline_result$jscomp$9;
        case "body":
          if (2 > formatContext.insertionMode) {
            var preamble$jscomp$0 = preambleState || renderState.preamble;
            if (preamble$jscomp$0.bodyChunks)
              throw Error("The `<body>` tag may only be rendered once.");
            null !== preambleState &&
              target$jscomp$0.push(bodyPreambleContributionChunk);
            preamble$jscomp$0.bodyChunks = [];
            var JSCompiler_inline_result$jscomp$10 = pushStartSingletonElement(
              preamble$jscomp$0.bodyChunks,
              props,
              "body",
            );
          } else
            JSCompiler_inline_result$jscomp$10 = pushStartGenericElement(
              target$jscomp$0,
              props,
              "body",
            );
          return JSCompiler_inline_result$jscomp$10;
        case "html":
          if (0 === formatContext.insertionMode) {
            var preamble$jscomp$1 = preambleState || renderState.preamble;
            if (preamble$jscomp$1.htmlChunks)
              throw Error("The `<html>` tag may only be rendered once.");
            null !== preambleState &&
              target$jscomp$0.push(htmlPreambleContributionChunk);
            preamble$jscomp$1.htmlChunks = [doctypeChunk];
            var JSCompiler_inline_result$jscomp$11 = pushStartSingletonElement(
              preamble$jscomp$1.htmlChunks,
              props,
              "html",
            );
          } else
            JSCompiler_inline_result$jscomp$11 = pushStartGenericElement(
              target$jscomp$0,
              props,
              "html",
            );
          return JSCompiler_inline_result$jscomp$11;
        default:
          if (-1 !== type.indexOf("-")) {
            target$jscomp$0.push(startChunkForTag(type));
            var children$jscomp$9 = null,
              innerHTML$jscomp$8 = null,
              propKey$jscomp$11;
            for (propKey$jscomp$11 in props)
              if (hasOwnProperty.call(props, propKey$jscomp$11)) {
                var propValue$jscomp$11 = props[propKey$jscomp$11];
                if (null != propValue$jscomp$11) {
                  var attributeName = propKey$jscomp$11;
                  switch (propKey$jscomp$11) {
                    case "children":
                      children$jscomp$9 = propValue$jscomp$11;
                      break;
                    case "dangerouslySetInnerHTML":
                      innerHTML$jscomp$8 = propValue$jscomp$11;
                      break;
                    case "style":
                      pushStyleAttribute(target$jscomp$0, propValue$jscomp$11);
                      break;
                    case "suppressContentEditableWarning":
                    case "suppressHydrationWarning":
                    case "ref":
                      break;
                    case "className":
                      attributeName = "class";
                    default:
                      if (
                        isAttributeNameSafe(propKey$jscomp$11) &&
                        "function" !== typeof propValue$jscomp$11 &&
                        "symbol" !== typeof propValue$jscomp$11 &&
                        false !== propValue$jscomp$11
                      ) {
                        if (true === propValue$jscomp$11)
                          propValue$jscomp$11 = "";
                        else if ("object" === typeof propValue$jscomp$11)
                          continue;
                        target$jscomp$0.push(
                          attributeSeparator,
                          attributeName,
                          attributeAssign,
                          escapeTextForBrowser(propValue$jscomp$11),
                          attributeEnd,
                        );
                      }
                  }
                }
              }
            target$jscomp$0.push(endOfStartTag);
            pushInnerHTML(
              target$jscomp$0,
              innerHTML$jscomp$8,
              children$jscomp$9,
            );
            return children$jscomp$9;
          }
      }
      return pushStartGenericElement(target$jscomp$0, props, type);
    }
    var endTagCache = /* @__PURE__ */ new Map();
    function endChunkForTag(tag) {
      var chunk = endTagCache.get(tag);
      void 0 === chunk &&
        ((chunk = stringToPrecomputedChunk("</" + tag + ">")),
        endTagCache.set(tag, chunk));
      return chunk;
    }
    function hoistPreambleState(renderState, preambleState) {
      renderState = renderState.preamble;
      null === renderState.htmlChunks &&
        preambleState.htmlChunks &&
        (renderState.htmlChunks = preambleState.htmlChunks);
      null === renderState.headChunks &&
        preambleState.headChunks &&
        (renderState.headChunks = preambleState.headChunks);
      null === renderState.bodyChunks &&
        preambleState.bodyChunks &&
        (renderState.bodyChunks = preambleState.bodyChunks);
    }
    function writeBootstrap(destination, renderState) {
      renderState = renderState.bootstrapChunks;
      for (var i = 0; i < renderState.length - 1; i++)
        writeChunk(destination, renderState[i]);
      return i < renderState.length
        ? ((i = renderState[i]),
          (renderState.length = 0),
          writeChunkAndReturn(destination, i))
        : true;
    }
    var shellTimeRuntimeScript = stringToPrecomputedChunk(
      "requestAnimationFrame(function(){$RT=performance.now()});",
    );
    var placeholder1 = stringToPrecomputedChunk('<template id="');
    var placeholder2 = stringToPrecomputedChunk('"></template>');
    var startActivityBoundary = stringToPrecomputedChunk("<!--&-->");
    var endActivityBoundary = stringToPrecomputedChunk("<!--/&-->");
    var startCompletedSuspenseBoundary = stringToPrecomputedChunk("<!--$-->");
    var startPendingSuspenseBoundary1 = stringToPrecomputedChunk(
      '<!--$?--><template id="',
    );
    var startPendingSuspenseBoundary2 =
      stringToPrecomputedChunk('"></template>');
    var startClientRenderedSuspenseBoundary =
      stringToPrecomputedChunk("<!--$!-->");
    var endSuspenseBoundary = stringToPrecomputedChunk("<!--/$-->");
    var clientRenderedSuspenseBoundaryError1 =
      stringToPrecomputedChunk("<template");
    var clientRenderedSuspenseBoundaryErrorAttrInterstitial =
      stringToPrecomputedChunk('"');
    var clientRenderedSuspenseBoundaryError1A =
      stringToPrecomputedChunk(' data-dgst="');
    stringToPrecomputedChunk(' data-msg="');
    stringToPrecomputedChunk(' data-stck="');
    stringToPrecomputedChunk(' data-cstck="');
    var clientRenderedSuspenseBoundaryError2 =
      stringToPrecomputedChunk("></template>");
    function writeStartPendingSuspenseBoundary(destination, renderState, id) {
      writeChunk(destination, startPendingSuspenseBoundary1);
      if (null === id)
        throw Error(
          "An ID must have been assigned before we can complete the boundary.",
        );
      writeChunk(destination, renderState.boundaryPrefix);
      writeChunk(destination, id.toString(16));
      return writeChunkAndReturn(destination, startPendingSuspenseBoundary2);
    }
    var startSegmentHTML = stringToPrecomputedChunk('<div hidden id="');
    var startSegmentHTML2 = stringToPrecomputedChunk('">');
    var endSegmentHTML = stringToPrecomputedChunk("</div>");
    var startSegmentSVG = stringToPrecomputedChunk(
      '<svg aria-hidden="true" style="display:none" id="',
    );
    var startSegmentSVG2 = stringToPrecomputedChunk('">');
    var endSegmentSVG = stringToPrecomputedChunk("</svg>");
    var startSegmentMathML = stringToPrecomputedChunk(
      '<math aria-hidden="true" style="display:none" id="',
    );
    var startSegmentMathML2 = stringToPrecomputedChunk('">');
    var endSegmentMathML = stringToPrecomputedChunk("</math>");
    var startSegmentTable = stringToPrecomputedChunk('<table hidden id="');
    var startSegmentTable2 = stringToPrecomputedChunk('">');
    var endSegmentTable = stringToPrecomputedChunk("</table>");
    var startSegmentTableBody = stringToPrecomputedChunk(
      '<table hidden><tbody id="',
    );
    var startSegmentTableBody2 = stringToPrecomputedChunk('">');
    var endSegmentTableBody = stringToPrecomputedChunk("</tbody></table>");
    var startSegmentTableRow = stringToPrecomputedChunk(
      '<table hidden><tr id="',
    );
    var startSegmentTableRow2 = stringToPrecomputedChunk('">');
    var endSegmentTableRow = stringToPrecomputedChunk("</tr></table>");
    var startSegmentColGroup = stringToPrecomputedChunk(
      '<table hidden><colgroup id="',
    );
    var startSegmentColGroup2 = stringToPrecomputedChunk('">');
    var endSegmentColGroup = stringToPrecomputedChunk("</colgroup></table>");
    function writeStartSegment(destination, renderState, formatContext, id) {
      switch (formatContext.insertionMode) {
        case 0:
        case 1:
        case 3:
        case 2:
          return (
            writeChunk(destination, startSegmentHTML),
            writeChunk(destination, renderState.segmentPrefix),
            writeChunk(destination, id.toString(16)),
            writeChunkAndReturn(destination, startSegmentHTML2)
          );
        case 4:
          return (
            writeChunk(destination, startSegmentSVG),
            writeChunk(destination, renderState.segmentPrefix),
            writeChunk(destination, id.toString(16)),
            writeChunkAndReturn(destination, startSegmentSVG2)
          );
        case 5:
          return (
            writeChunk(destination, startSegmentMathML),
            writeChunk(destination, renderState.segmentPrefix),
            writeChunk(destination, id.toString(16)),
            writeChunkAndReturn(destination, startSegmentMathML2)
          );
        case 6:
          return (
            writeChunk(destination, startSegmentTable),
            writeChunk(destination, renderState.segmentPrefix),
            writeChunk(destination, id.toString(16)),
            writeChunkAndReturn(destination, startSegmentTable2)
          );
        case 7:
          return (
            writeChunk(destination, startSegmentTableBody),
            writeChunk(destination, renderState.segmentPrefix),
            writeChunk(destination, id.toString(16)),
            writeChunkAndReturn(destination, startSegmentTableBody2)
          );
        case 8:
          return (
            writeChunk(destination, startSegmentTableRow),
            writeChunk(destination, renderState.segmentPrefix),
            writeChunk(destination, id.toString(16)),
            writeChunkAndReturn(destination, startSegmentTableRow2)
          );
        case 9:
          return (
            writeChunk(destination, startSegmentColGroup),
            writeChunk(destination, renderState.segmentPrefix),
            writeChunk(destination, id.toString(16)),
            writeChunkAndReturn(destination, startSegmentColGroup2)
          );
        default:
          throw Error("Unknown insertion mode. This is a bug in React.");
      }
    }
    function writeEndSegment(destination, formatContext) {
      switch (formatContext.insertionMode) {
        case 0:
        case 1:
        case 3:
        case 2:
          return writeChunkAndReturn(destination, endSegmentHTML);
        case 4:
          return writeChunkAndReturn(destination, endSegmentSVG);
        case 5:
          return writeChunkAndReturn(destination, endSegmentMathML);
        case 6:
          return writeChunkAndReturn(destination, endSegmentTable);
        case 7:
          return writeChunkAndReturn(destination, endSegmentTableBody);
        case 8:
          return writeChunkAndReturn(destination, endSegmentTableRow);
        case 9:
          return writeChunkAndReturn(destination, endSegmentColGroup);
        default:
          throw Error("Unknown insertion mode. This is a bug in React.");
      }
    }
    var completeSegmentScript1Full = stringToPrecomputedChunk(
      '$RS=function(a,b){a=document.getElementById(a);b=document.getElementById(b);for(a.parentNode.removeChild(a);a.firstChild;)b.parentNode.insertBefore(a.firstChild,b);b.parentNode.removeChild(b)};$RS("',
    );
    var completeSegmentScript1Partial = stringToPrecomputedChunk('$RS("');
    var completeSegmentScript2 = stringToPrecomputedChunk('","');
    var completeSegmentScriptEnd = stringToPrecomputedChunk('")</script>');
    stringToPrecomputedChunk('<template data-rsi="" data-sid="');
    stringToPrecomputedChunk('" data-pid="');
    var completeBoundaryScriptFunctionOnly = stringToPrecomputedChunk(
      '$RB=[];$RV=function(a){$RT=performance.now();for(var b=0;b<a.length;b+=2){var c=a[b],e=a[b+1];null!==e.parentNode&&e.parentNode.removeChild(e);var f=c.parentNode;if(f){var g=c.previousSibling,h=0;do{if(c&&8===c.nodeType){var d=c.data;if("/$"===d||"/&"===d)if(0===h)break;else h--;else"$"!==d&&"$?"!==d&&"$~"!==d&&"$!"!==d&&"&"!==d||h++}d=c.nextSibling;f.removeChild(c);c=d}while(c);for(;e.firstChild;)f.insertBefore(e.firstChild,c);g.data="$";g._reactRetry&&requestAnimationFrame(g._reactRetry)}}a.length=0};\n$RC=function(a,b){if(b=document.getElementById(b))(a=document.getElementById(a))?(a.previousSibling.data="$~",$RB.push(a,b),2===$RB.length&&("number"!==typeof $RT?requestAnimationFrame($RV.bind(null,$RB)):(a=performance.now(),setTimeout($RV.bind(null,$RB),2300>a&&2E3<a?2300-a:$RT+300-a)))):b.parentNode.removeChild(b)};',
    );
    var completeBoundaryScript1Partial = stringToPrecomputedChunk('$RC("');
    var completeBoundaryWithStylesScript1FullPartial = stringToPrecomputedChunk(
      '$RM=new Map;$RR=function(n,w,p){function u(q){this._p=null;q()}for(var r=new Map,t=document,h,b,e=t.querySelectorAll("link[data-precedence],style[data-precedence]"),v=[],k=0;b=e[k++];)"not all"===b.getAttribute("media")?v.push(b):("LINK"===b.tagName&&$RM.set(b.getAttribute("href"),b),r.set(b.dataset.precedence,h=b));e=0;b=[];var l,a;for(k=!0;;){if(k){var f=p[e++];if(!f){k=!1;e=0;continue}var c=!1,m=0;var d=f[m++];if(a=$RM.get(d)){var g=a._p;c=!0}else{a=t.createElement("link");a.href=d;a.rel=\n"stylesheet";for(a.dataset.precedence=l=f[m++];g=f[m++];)a.setAttribute(g,f[m++]);g=a._p=new Promise(function(q,x){a.onload=u.bind(a,q);a.onerror=u.bind(a,x)});$RM.set(d,a)}d=a.getAttribute("media");!g||d&&!matchMedia(d).matches||b.push(g);if(c)continue}else{a=v[e++];if(!a)break;l=a.getAttribute("data-precedence");a.removeAttribute("media")}c=r.get(l)||h;c===h&&(h=a);r.set(l,a);c?c.parentNode.insertBefore(a,c.nextSibling):(c=t.head,c.insertBefore(a,c.firstChild))}if(p=document.getElementById(n))p.previousSibling.data=\n"$~";Promise.all(b).then($RC.bind(null,n,w),$RX.bind(null,n,"CSS failed to load"))};$RR("',
    );
    var completeBoundaryWithStylesScript1Partial =
      stringToPrecomputedChunk('$RR("');
    var completeBoundaryScript2 = stringToPrecomputedChunk('","');
    var completeBoundaryScript3a = stringToPrecomputedChunk('",');
    var completeBoundaryScript3b = stringToPrecomputedChunk('"');
    var completeBoundaryScriptEnd = stringToPrecomputedChunk(")</script>");
    stringToPrecomputedChunk('<template data-rci="" data-bid="');
    stringToPrecomputedChunk('<template data-rri="" data-bid="');
    stringToPrecomputedChunk('" data-sid="');
    stringToPrecomputedChunk('" data-sty="');
    var clientRenderScriptFunctionOnly = stringToPrecomputedChunk(
      '$RX=function(b,c,d,e,f){var a=document.getElementById(b);a&&(b=a.previousSibling,b.data="$!",a=a.dataset,c&&(a.dgst=c),d&&(a.msg=d),e&&(a.stck=e),f&&(a.cstck=f),b._reactRetry&&b._reactRetry())};',
    );
    var clientRenderScript1Full = stringToPrecomputedChunk(
      '$RX=function(b,c,d,e,f){var a=document.getElementById(b);a&&(b=a.previousSibling,b.data="$!",a=a.dataset,c&&(a.dgst=c),d&&(a.msg=d),e&&(a.stck=e),f&&(a.cstck=f),b._reactRetry&&b._reactRetry())};;$RX("',
    );
    var clientRenderScript1Partial = stringToPrecomputedChunk('$RX("');
    var clientRenderScript1A = stringToPrecomputedChunk('"');
    var clientRenderErrorScriptArgInterstitial = stringToPrecomputedChunk(",");
    var clientRenderScriptEnd = stringToPrecomputedChunk(")</script>");
    stringToPrecomputedChunk('<template data-rxi="" data-bid="');
    stringToPrecomputedChunk('" data-dgst="');
    stringToPrecomputedChunk('" data-msg="');
    stringToPrecomputedChunk('" data-stck="');
    stringToPrecomputedChunk('" data-cstck="');
    var regexForJSStringsInInstructionScripts = /[<\u2028\u2029]/g;
    function escapeJSStringsForInstructionScripts(input) {
      return JSON.stringify(input).replace(
        regexForJSStringsInInstructionScripts,
        function (match2) {
          switch (match2) {
            case "<":
              return "\\u003c";
            case "\u2028":
              return "\\u2028";
            case "\u2029":
              return "\\u2029";
            default:
              throw Error(
                "escapeJSStringsForInstructionScripts encountered a match it does not know how to replace. this means the match regex and the replacement characters are no longer in sync. This is a bug in React",
              );
          }
        },
      );
    }
    var regexForJSStringsInScripts = /[&><\u2028\u2029]/g;
    function escapeJSObjectForInstructionScripts(input) {
      return JSON.stringify(input).replace(
        regexForJSStringsInScripts,
        function (match2) {
          switch (match2) {
            case "&":
              return "\\u0026";
            case ">":
              return "\\u003e";
            case "<":
              return "\\u003c";
            case "\u2028":
              return "\\u2028";
            case "\u2029":
              return "\\u2029";
            default:
              throw Error(
                "escapeJSObjectForInstructionScripts encountered a match it does not know how to replace. this means the match regex and the replacement characters are no longer in sync. This is a bug in React",
              );
          }
        },
      );
    }
    var lateStyleTagResourceOpen1 = stringToPrecomputedChunk(
      ' media="not all" data-precedence="',
    );
    var lateStyleTagResourceOpen2 = stringToPrecomputedChunk('" data-href="');
    var lateStyleTagResourceOpen3 = stringToPrecomputedChunk('">');
    var lateStyleTagTemplateClose = stringToPrecomputedChunk("</style>");
    var currentlyRenderingBoundaryHasStylesToHoist = false;
    var destinationHasCapacity = true;
    function flushStyleTagsLateForBoundary(styleQueue) {
      var rules = styleQueue.rules,
        hrefs = styleQueue.hrefs,
        i = 0;
      if (hrefs.length) {
        writeChunk(this, currentlyFlushingRenderState.startInlineStyle);
        writeChunk(this, lateStyleTagResourceOpen1);
        writeChunk(this, styleQueue.precedence);
        for (
          writeChunk(this, lateStyleTagResourceOpen2);
          i < hrefs.length - 1;
          i++
        )
          (writeChunk(this, hrefs[i]), writeChunk(this, spaceSeparator));
        writeChunk(this, hrefs[i]);
        writeChunk(this, lateStyleTagResourceOpen3);
        for (i = 0; i < rules.length; i++) writeChunk(this, rules[i]);
        destinationHasCapacity = writeChunkAndReturn(
          this,
          lateStyleTagTemplateClose,
        );
        currentlyRenderingBoundaryHasStylesToHoist = true;
        rules.length = 0;
        hrefs.length = 0;
      }
    }
    function hasStylesToHoist(stylesheet) {
      return 2 !== stylesheet.state
        ? (currentlyRenderingBoundaryHasStylesToHoist = true)
        : false;
    }
    function writeHoistablesForBoundary(
      destination,
      hoistableState,
      renderState,
    ) {
      currentlyRenderingBoundaryHasStylesToHoist = false;
      destinationHasCapacity = true;
      currentlyFlushingRenderState = renderState;
      hoistableState.styles.forEach(flushStyleTagsLateForBoundary, destination);
      currentlyFlushingRenderState = null;
      hoistableState.stylesheets.forEach(hasStylesToHoist);
      currentlyRenderingBoundaryHasStylesToHoist &&
        (renderState.stylesToHoist = true);
      return destinationHasCapacity;
    }
    function flushResource(resource) {
      for (var i = 0; i < resource.length; i++) writeChunk(this, resource[i]);
      resource.length = 0;
    }
    var stylesheetFlushingQueue = [];
    function flushStyleInPreamble(stylesheet) {
      pushLinkImpl(stylesheetFlushingQueue, stylesheet.props);
      for (var i = 0; i < stylesheetFlushingQueue.length; i++)
        writeChunk(this, stylesheetFlushingQueue[i]);
      stylesheetFlushingQueue.length = 0;
      stylesheet.state = 2;
    }
    var styleTagResourceOpen1 = stringToPrecomputedChunk(' data-precedence="');
    var styleTagResourceOpen2 = stringToPrecomputedChunk('" data-href="');
    var spaceSeparator = stringToPrecomputedChunk(" ");
    var styleTagResourceOpen3 = stringToPrecomputedChunk('">');
    var styleTagResourceClose = stringToPrecomputedChunk("</style>");
    function flushStylesInPreamble(styleQueue) {
      var hasStylesheets = 0 < styleQueue.sheets.size;
      styleQueue.sheets.forEach(flushStyleInPreamble, this);
      styleQueue.sheets.clear();
      var rules = styleQueue.rules,
        hrefs = styleQueue.hrefs;
      if (!hasStylesheets || hrefs.length) {
        writeChunk(this, currentlyFlushingRenderState.startInlineStyle);
        writeChunk(this, styleTagResourceOpen1);
        writeChunk(this, styleQueue.precedence);
        styleQueue = 0;
        if (hrefs.length) {
          for (
            writeChunk(this, styleTagResourceOpen2);
            styleQueue < hrefs.length - 1;
            styleQueue++
          )
            (writeChunk(this, hrefs[styleQueue]),
              writeChunk(this, spaceSeparator));
          writeChunk(this, hrefs[styleQueue]);
        }
        writeChunk(this, styleTagResourceOpen3);
        for (styleQueue = 0; styleQueue < rules.length; styleQueue++)
          writeChunk(this, rules[styleQueue]);
        writeChunk(this, styleTagResourceClose);
        rules.length = 0;
        hrefs.length = 0;
      }
    }
    function preloadLateStyle(stylesheet) {
      if (0 === stylesheet.state) {
        stylesheet.state = 1;
        var props = stylesheet.props;
        pushLinkImpl(stylesheetFlushingQueue, {
          rel: "preload",
          as: "style",
          href: stylesheet.props.href,
          crossOrigin: props.crossOrigin,
          fetchPriority: props.fetchPriority,
          integrity: props.integrity,
          media: props.media,
          hrefLang: props.hrefLang,
          referrerPolicy: props.referrerPolicy,
        });
        for (
          stylesheet = 0;
          stylesheet < stylesheetFlushingQueue.length;
          stylesheet++
        )
          writeChunk(this, stylesheetFlushingQueue[stylesheet]);
        stylesheetFlushingQueue.length = 0;
      }
    }
    function preloadLateStyles(styleQueue) {
      styleQueue.sheets.forEach(preloadLateStyle, this);
      styleQueue.sheets.clear();
    }
    stringToPrecomputedChunk('<link rel="expect" href="#');
    stringToPrecomputedChunk('" blocking="render"/>');
    var completedShellIdAttributeStart = stringToPrecomputedChunk(' id="');
    function pushCompletedShellIdAttribute(target, resumableState) {
      0 === (resumableState.instructions & 32) &&
        ((resumableState.instructions |= 32),
        target.push(
          completedShellIdAttributeStart,
          escapeTextForBrowser("_" + resumableState.idPrefix + "R_"),
          attributeEnd,
        ));
    }
    var arrayFirstOpenBracket = stringToPrecomputedChunk("[");
    var arraySubsequentOpenBracket = stringToPrecomputedChunk(",[");
    var arrayInterstitial = stringToPrecomputedChunk(",");
    var arrayCloseBracket = stringToPrecomputedChunk("]");
    function writeStyleResourceDependenciesInJS(destination, hoistableState) {
      writeChunk(destination, arrayFirstOpenBracket);
      var nextArrayOpenBrackChunk = arrayFirstOpenBracket;
      hoistableState.stylesheets.forEach(function (resource) {
        if (2 !== resource.state)
          if (3 === resource.state)
            (writeChunk(destination, nextArrayOpenBrackChunk),
              writeChunk(
                destination,
                escapeJSObjectForInstructionScripts("" + resource.props.href),
              ),
              writeChunk(destination, arrayCloseBracket),
              (nextArrayOpenBrackChunk = arraySubsequentOpenBracket));
          else {
            writeChunk(destination, nextArrayOpenBrackChunk);
            var precedence = resource.props["data-precedence"],
              props = resource.props,
              coercedHref = sanitizeURL("" + resource.props.href);
            writeChunk(
              destination,
              escapeJSObjectForInstructionScripts(coercedHref),
            );
            precedence = "" + precedence;
            writeChunk(destination, arrayInterstitial);
            writeChunk(
              destination,
              escapeJSObjectForInstructionScripts(precedence),
            );
            for (var propKey in props)
              if (
                hasOwnProperty.call(props, propKey) &&
                ((precedence = props[propKey]), null != precedence)
              )
                switch (propKey) {
                  case "href":
                  case "rel":
                  case "precedence":
                  case "data-precedence":
                    break;
                  case "children":
                  case "dangerouslySetInnerHTML":
                    throw Error(
                      "link is a self-closing tag and must neither have `children` nor use `dangerouslySetInnerHTML`.",
                    );
                  default:
                    writeStyleResourceAttributeInJS(
                      destination,
                      propKey,
                      precedence,
                    );
                }
            writeChunk(destination, arrayCloseBracket);
            nextArrayOpenBrackChunk = arraySubsequentOpenBracket;
            resource.state = 3;
          }
      });
      writeChunk(destination, arrayCloseBracket);
    }
    function writeStyleResourceAttributeInJS(destination, name, value) {
      var attributeName = name.toLowerCase();
      switch (typeof value) {
        case "function":
        case "symbol":
          return;
      }
      switch (name) {
        case "innerHTML":
        case "dangerouslySetInnerHTML":
        case "suppressContentEditableWarning":
        case "suppressHydrationWarning":
        case "style":
        case "ref":
          return;
        case "className":
          attributeName = "class";
          name = "" + value;
          break;
        case "hidden":
          if (false === value) return;
          name = "";
          break;
        case "src":
        case "href":
          value = sanitizeURL(value);
          name = "" + value;
          break;
        default:
          if (
            (2 < name.length &&
              ("o" === name[0] || "O" === name[0]) &&
              ("n" === name[1] || "N" === name[1])) ||
            !isAttributeNameSafe(name)
          )
            return;
          name = "" + value;
      }
      writeChunk(destination, arrayInterstitial);
      writeChunk(
        destination,
        escapeJSObjectForInstructionScripts(attributeName),
      );
      writeChunk(destination, arrayInterstitial);
      writeChunk(destination, escapeJSObjectForInstructionScripts(name));
    }
    function createHoistableState() {
      return {
        styles: /* @__PURE__ */ new Set(),
        stylesheets: /* @__PURE__ */ new Set(),
        suspenseyImages: false,
      };
    }
    function prefetchDNS(href) {
      var request = resolveRequest();
      if (request) {
        var resumableState = request.resumableState,
          renderState = request.renderState;
        if ("string" === typeof href && href) {
          if (!resumableState.dnsResources.hasOwnProperty(href)) {
            resumableState.dnsResources[href] = null;
            resumableState = renderState.headers;
            var header, JSCompiler_temp;
            if (
              (JSCompiler_temp =
                resumableState && 0 < resumableState.remainingCapacity)
            )
              JSCompiler_temp =
                ((header =
                  "<" +
                  ("" + href).replace(
                    regexForHrefInLinkHeaderURLContext,
                    escapeHrefForLinkHeaderURLContextReplacer,
                  ) +
                  ">; rel=dns-prefetch"),
                0 <= (resumableState.remainingCapacity -= header.length + 2));
            JSCompiler_temp
              ? ((renderState.resets.dns[href] = null),
                resumableState.preconnects &&
                  (resumableState.preconnects += ", "),
                (resumableState.preconnects += header))
              : ((header = []),
                pushLinkImpl(header, { href, rel: "dns-prefetch" }),
                renderState.preconnects.add(header));
          }
          enqueueFlush(request);
        }
      } else previousDispatcher.D(href);
    }
    function preconnect(href, crossOrigin) {
      var request = resolveRequest();
      if (request) {
        var resumableState = request.resumableState,
          renderState = request.renderState;
        if ("string" === typeof href && href) {
          var bucket =
            "use-credentials" === crossOrigin
              ? "credentials"
              : "string" === typeof crossOrigin
                ? "anonymous"
                : "default";
          if (!resumableState.connectResources[bucket].hasOwnProperty(href)) {
            resumableState.connectResources[bucket][href] = null;
            resumableState = renderState.headers;
            var header, JSCompiler_temp;
            if (
              (JSCompiler_temp =
                resumableState && 0 < resumableState.remainingCapacity)
            ) {
              JSCompiler_temp =
                "<" +
                ("" + href).replace(
                  regexForHrefInLinkHeaderURLContext,
                  escapeHrefForLinkHeaderURLContextReplacer,
                ) +
                ">; rel=preconnect";
              if ("string" === typeof crossOrigin) {
                var escapedCrossOrigin = ("" + crossOrigin).replace(
                  regexForLinkHeaderQuotedParamValueContext,
                  escapeStringForLinkHeaderQuotedParamValueContextReplacer,
                );
                JSCompiler_temp += '; crossorigin="' + escapedCrossOrigin + '"';
              }
              JSCompiler_temp =
                ((header = JSCompiler_temp),
                0 <= (resumableState.remainingCapacity -= header.length + 2));
            }
            JSCompiler_temp
              ? ((renderState.resets.connect[bucket][href] = null),
                resumableState.preconnects &&
                  (resumableState.preconnects += ", "),
                (resumableState.preconnects += header))
              : ((bucket = []),
                pushLinkImpl(bucket, {
                  rel: "preconnect",
                  href,
                  crossOrigin,
                }),
                renderState.preconnects.add(bucket));
          }
          enqueueFlush(request);
        }
      } else previousDispatcher.C(href, crossOrigin);
    }
    function preload(href, as, options) {
      var request = resolveRequest();
      if (request) {
        var resumableState = request.resumableState,
          renderState = request.renderState;
        if (as && href) {
          switch (as) {
            case "image":
              if (options) {
                var imageSrcSet = options.imageSrcSet;
                var imageSizes = options.imageSizes;
                var fetchPriority = options.fetchPriority;
              }
              var key = imageSrcSet
                ? imageSrcSet + "\n" + (imageSizes || "")
                : href;
              if (resumableState.imageResources.hasOwnProperty(key)) return;
              resumableState.imageResources[key] = PRELOAD_NO_CREDS;
              resumableState = renderState.headers;
              var header;
              resumableState &&
              0 < resumableState.remainingCapacity &&
              "string" !== typeof imageSrcSet &&
              "high" === fetchPriority &&
              ((header = getPreloadAsHeader(href, as, options)),
              0 <= (resumableState.remainingCapacity -= header.length + 2))
                ? ((renderState.resets.image[key] = PRELOAD_NO_CREDS),
                  resumableState.highImagePreloads &&
                    (resumableState.highImagePreloads += ", "),
                  (resumableState.highImagePreloads += header))
                : ((resumableState = []),
                  pushLinkImpl(
                    resumableState,
                    assign2(
                      { rel: "preload", href: imageSrcSet ? void 0 : href, as },
                      options,
                    ),
                  ),
                  "high" === fetchPriority
                    ? renderState.highImagePreloads.add(resumableState)
                    : (renderState.bulkPreloads.add(resumableState),
                      renderState.preloads.images.set(key, resumableState)));
              break;
            case "style":
              if (resumableState.styleResources.hasOwnProperty(href)) return;
              imageSrcSet = [];
              pushLinkImpl(
                imageSrcSet,
                assign2({ rel: "preload", href, as }, options),
              );
              resumableState.styleResources[href] =
                !options ||
                ("string" !== typeof options.crossOrigin &&
                  "string" !== typeof options.integrity)
                  ? PRELOAD_NO_CREDS
                  : [options.crossOrigin, options.integrity];
              renderState.preloads.stylesheets.set(href, imageSrcSet);
              renderState.bulkPreloads.add(imageSrcSet);
              break;
            case "script":
              if (resumableState.scriptResources.hasOwnProperty(href)) return;
              imageSrcSet = [];
              renderState.preloads.scripts.set(href, imageSrcSet);
              renderState.bulkPreloads.add(imageSrcSet);
              pushLinkImpl(
                imageSrcSet,
                assign2({ rel: "preload", href, as }, options),
              );
              resumableState.scriptResources[href] =
                !options ||
                ("string" !== typeof options.crossOrigin &&
                  "string" !== typeof options.integrity)
                  ? PRELOAD_NO_CREDS
                  : [options.crossOrigin, options.integrity];
              break;
            default:
              if (resumableState.unknownResources.hasOwnProperty(as)) {
                if (
                  ((imageSrcSet = resumableState.unknownResources[as]),
                  imageSrcSet.hasOwnProperty(href))
                )
                  return;
              } else
                ((imageSrcSet = {}),
                  (resumableState.unknownResources[as] = imageSrcSet));
              imageSrcSet[href] = PRELOAD_NO_CREDS;
              if (
                (resumableState = renderState.headers) &&
                0 < resumableState.remainingCapacity &&
                "font" === as &&
                ((key = getPreloadAsHeader(href, as, options)),
                0 <= (resumableState.remainingCapacity -= key.length + 2))
              )
                ((renderState.resets.font[href] = PRELOAD_NO_CREDS),
                  resumableState.fontPreloads &&
                    (resumableState.fontPreloads += ", "),
                  (resumableState.fontPreloads += key));
              else
                switch (
                  ((resumableState = []),
                  (href = assign2({ rel: "preload", href, as }, options)),
                  pushLinkImpl(resumableState, href),
                  as)
                ) {
                  case "font":
                    renderState.fontPreloads.add(resumableState);
                    break;
                  default:
                    renderState.bulkPreloads.add(resumableState);
                }
          }
          enqueueFlush(request);
        }
      } else previousDispatcher.L(href, as, options);
    }
    function preloadModule(href, options) {
      var request = resolveRequest();
      if (request) {
        var resumableState = request.resumableState,
          renderState = request.renderState;
        if (href) {
          var as =
            options && "string" === typeof options.as ? options.as : "script";
          switch (as) {
            case "script":
              if (resumableState.moduleScriptResources.hasOwnProperty(href))
                return;
              as = [];
              resumableState.moduleScriptResources[href] =
                !options ||
                ("string" !== typeof options.crossOrigin &&
                  "string" !== typeof options.integrity)
                  ? PRELOAD_NO_CREDS
                  : [options.crossOrigin, options.integrity];
              renderState.preloads.moduleScripts.set(href, as);
              break;
            default:
              if (resumableState.moduleUnknownResources.hasOwnProperty(as)) {
                var resources = resumableState.unknownResources[as];
                if (resources.hasOwnProperty(href)) return;
              } else
                ((resources = {}),
                  (resumableState.moduleUnknownResources[as] = resources));
              as = [];
              resources[href] = PRELOAD_NO_CREDS;
          }
          pushLinkImpl(as, assign2({ rel: "modulepreload", href }, options));
          renderState.bulkPreloads.add(as);
          enqueueFlush(request);
        }
      } else previousDispatcher.m(href, options);
    }
    function preinitStyle(href, precedence, options) {
      var request = resolveRequest();
      if (request) {
        var resumableState = request.resumableState,
          renderState = request.renderState;
        if (href) {
          precedence = precedence || "default";
          var styleQueue = renderState.styles.get(precedence),
            resourceState = resumableState.styleResources.hasOwnProperty(href)
              ? resumableState.styleResources[href]
              : void 0;
          null !== resourceState &&
            ((resumableState.styleResources[href] = null),
            styleQueue ||
              ((styleQueue = {
                precedence: escapeTextForBrowser(precedence),
                rules: [],
                hrefs: [],
                sheets: /* @__PURE__ */ new Map(),
              }),
              renderState.styles.set(precedence, styleQueue)),
            (precedence = {
              state: 0,
              props: assign2(
                { rel: "stylesheet", href, "data-precedence": precedence },
                options,
              ),
            }),
            resourceState &&
              (2 === resourceState.length &&
                adoptPreloadCredentials(precedence.props, resourceState),
              (renderState = renderState.preloads.stylesheets.get(href)) &&
              0 < renderState.length
                ? (renderState.length = 0)
                : (precedence.state = 1)),
            styleQueue.sheets.set(href, precedence),
            enqueueFlush(request));
        }
      } else previousDispatcher.S(href, precedence, options);
    }
    function preinitScript(src, options) {
      var request = resolveRequest();
      if (request) {
        var resumableState = request.resumableState,
          renderState = request.renderState;
        if (src) {
          var resourceState = resumableState.scriptResources.hasOwnProperty(src)
            ? resumableState.scriptResources[src]
            : void 0;
          null !== resourceState &&
            ((resumableState.scriptResources[src] = null),
            (options = assign2({ src, async: true }, options)),
            resourceState &&
              (2 === resourceState.length &&
                adoptPreloadCredentials(options, resourceState),
              (src = renderState.preloads.scripts.get(src))) &&
              (src.length = 0),
            (src = []),
            renderState.scripts.add(src),
            pushScriptImpl(src, options),
            enqueueFlush(request));
        }
      } else previousDispatcher.X(src, options);
    }
    function preinitModuleScript(src, options) {
      var request = resolveRequest();
      if (request) {
        var resumableState = request.resumableState,
          renderState = request.renderState;
        if (src) {
          var resourceState =
            resumableState.moduleScriptResources.hasOwnProperty(src)
              ? resumableState.moduleScriptResources[src]
              : void 0;
          null !== resourceState &&
            ((resumableState.moduleScriptResources[src] = null),
            (options = assign2({ src, type: "module", async: true }, options)),
            resourceState &&
              (2 === resourceState.length &&
                adoptPreloadCredentials(options, resourceState),
              (src = renderState.preloads.moduleScripts.get(src))) &&
              (src.length = 0),
            (src = []),
            renderState.scripts.add(src),
            pushScriptImpl(src, options),
            enqueueFlush(request));
        }
      } else previousDispatcher.M(src, options);
    }
    function adoptPreloadCredentials(target, preloadState) {
      null == target.crossOrigin && (target.crossOrigin = preloadState[0]);
      null == target.integrity && (target.integrity = preloadState[1]);
    }
    function getPreloadAsHeader(href, as, params) {
      href = ("" + href).replace(
        regexForHrefInLinkHeaderURLContext,
        escapeHrefForLinkHeaderURLContextReplacer,
      );
      as = ("" + as).replace(
        regexForLinkHeaderQuotedParamValueContext,
        escapeStringForLinkHeaderQuotedParamValueContextReplacer,
      );
      as = "<" + href + '>; rel=preload; as="' + as + '"';
      for (var paramName in params)
        hasOwnProperty.call(params, paramName) &&
          ((href = params[paramName]),
          "string" === typeof href &&
            (as +=
              "; " +
              paramName.toLowerCase() +
              '="' +
              ("" + href).replace(
                regexForLinkHeaderQuotedParamValueContext,
                escapeStringForLinkHeaderQuotedParamValueContextReplacer,
              ) +
              '"'));
      return as;
    }
    var regexForHrefInLinkHeaderURLContext = /[<>\r\n]/g;
    function escapeHrefForLinkHeaderURLContextReplacer(match2) {
      switch (match2) {
        case "<":
          return "%3C";
        case ">":
          return "%3E";
        case "\n":
          return "%0A";
        case "\r":
          return "%0D";
        default:
          throw Error(
            "escapeLinkHrefForHeaderContextReplacer encountered a match it does not know how to replace. this means the match regex and the replacement characters are no longer in sync. This is a bug in React",
          );
      }
    }
    var regexForLinkHeaderQuotedParamValueContext = /["';,\r\n]/g;
    function escapeStringForLinkHeaderQuotedParamValueContextReplacer(match2) {
      switch (match2) {
        case '"':
          return "%22";
        case "'":
          return "%27";
        case ";":
          return "%3B";
        case ",":
          return "%2C";
        case "\n":
          return "%0A";
        case "\r":
          return "%0D";
        default:
          throw Error(
            "escapeStringForLinkHeaderQuotedParamValueContextReplacer encountered a match it does not know how to replace. this means the match regex and the replacement characters are no longer in sync. This is a bug in React",
          );
      }
    }
    function hoistStyleQueueDependency(styleQueue) {
      this.styles.add(styleQueue);
    }
    function hoistStylesheetDependency(stylesheet) {
      this.stylesheets.add(stylesheet);
    }
    function hoistHoistables(parentState, childState) {
      childState.styles.forEach(hoistStyleQueueDependency, parentState);
      childState.stylesheets.forEach(hoistStylesheetDependency, parentState);
      childState.suspenseyImages && (parentState.suspenseyImages = true);
    }
    function hasSuspenseyContent(hoistableState) {
      return (
        0 < hoistableState.stylesheets.size || hoistableState.suspenseyImages
      );
    }
    var bind = Function.prototype.bind;
    var requestStorage = new async_hooks.AsyncLocalStorage();
    var REACT_CLIENT_REFERENCE = Symbol.for("react.client.reference");
    function getComponentNameFromType(type) {
      if (null == type) return null;
      if ("function" === typeof type)
        return type.$$typeof === REACT_CLIENT_REFERENCE
          ? null
          : type.displayName || type.name || null;
      if ("string" === typeof type) return type;
      switch (type) {
        case REACT_FRAGMENT_TYPE:
          return "Fragment";
        case REACT_PROFILER_TYPE:
          return "Profiler";
        case REACT_STRICT_MODE_TYPE:
          return "StrictMode";
        case REACT_SUSPENSE_TYPE:
          return "Suspense";
        case REACT_SUSPENSE_LIST_TYPE:
          return "SuspenseList";
        case REACT_ACTIVITY_TYPE:
          return "Activity";
      }
      if ("object" === typeof type)
        switch (type.$$typeof) {
          case REACT_PORTAL_TYPE:
            return "Portal";
          case REACT_CONTEXT_TYPE:
            return type.displayName || "Context";
          case REACT_CONSUMER_TYPE:
            return (type._context.displayName || "Context") + ".Consumer";
          case REACT_FORWARD_REF_TYPE:
            var innerType = type.render;
            type = type.displayName;
            type ||
              ((type = innerType.displayName || innerType.name || ""),
              (type = "" !== type ? "ForwardRef(" + type + ")" : "ForwardRef"));
            return type;
          case REACT_MEMO_TYPE:
            return (
              (innerType = type.displayName || null),
              null !== innerType
                ? innerType
                : getComponentNameFromType(type.type) || "Memo"
            );
          case REACT_LAZY_TYPE:
            innerType = type._payload;
            type = type._init;
            try {
              return getComponentNameFromType(type(innerType));
            } catch (x) {}
        }
      return null;
    }
    var emptyContextObject = {};
    var currentActiveSnapshot = null;
    function popToNearestCommonAncestor(prev2, next2) {
      if (prev2 !== next2) {
        prev2.context._currentValue = prev2.parentValue;
        prev2 = prev2.parent;
        var parentNext = next2.parent;
        if (null === prev2) {
          if (null !== parentNext)
            throw Error(
              "The stacks must reach the root at the same time. This is a bug in React.",
            );
        } else {
          if (null === parentNext)
            throw Error(
              "The stacks must reach the root at the same time. This is a bug in React.",
            );
          popToNearestCommonAncestor(prev2, parentNext);
        }
        next2.context._currentValue = next2.value;
      }
    }
    function popAllPrevious(prev2) {
      prev2.context._currentValue = prev2.parentValue;
      prev2 = prev2.parent;
      null !== prev2 && popAllPrevious(prev2);
    }
    function pushAllNext(next2) {
      var parentNext = next2.parent;
      null !== parentNext && pushAllNext(parentNext);
      next2.context._currentValue = next2.value;
    }
    function popPreviousToCommonLevel(prev2, next2) {
      prev2.context._currentValue = prev2.parentValue;
      prev2 = prev2.parent;
      if (null === prev2)
        throw Error(
          "The depth must equal at least at zero before reaching the root. This is a bug in React.",
        );
      prev2.depth === next2.depth
        ? popToNearestCommonAncestor(prev2, next2)
        : popPreviousToCommonLevel(prev2, next2);
    }
    function popNextToCommonLevel(prev2, next2) {
      var parentNext = next2.parent;
      if (null === parentNext)
        throw Error(
          "The depth must equal at least at zero before reaching the root. This is a bug in React.",
        );
      prev2.depth === parentNext.depth
        ? popToNearestCommonAncestor(prev2, parentNext)
        : popNextToCommonLevel(prev2, parentNext);
      next2.context._currentValue = next2.value;
    }
    function switchContext(newSnapshot) {
      var prev2 = currentActiveSnapshot;
      prev2 !== newSnapshot &&
        (null === prev2
          ? pushAllNext(newSnapshot)
          : null === newSnapshot
            ? popAllPrevious(prev2)
            : prev2.depth === newSnapshot.depth
              ? popToNearestCommonAncestor(prev2, newSnapshot)
              : prev2.depth > newSnapshot.depth
                ? popPreviousToCommonLevel(prev2, newSnapshot)
                : popNextToCommonLevel(prev2, newSnapshot),
        (currentActiveSnapshot = newSnapshot));
    }
    var classComponentUpdater = {
      enqueueSetState: function (inst, payload) {
        inst = inst._reactInternals;
        null !== inst.queue && inst.queue.push(payload);
      },
      enqueueReplaceState: function (inst, payload) {
        inst = inst._reactInternals;
        inst.replace = true;
        inst.queue = [payload];
      },
      enqueueForceUpdate: function () {},
    };
    var emptyTreeContext = { id: 1, overflow: "" };
    function pushTreeContext(baseContext, totalChildren, index) {
      var baseIdWithLeadingBit = baseContext.id;
      baseContext = baseContext.overflow;
      var baseLength = 32 - clz32(baseIdWithLeadingBit) - 1;
      baseIdWithLeadingBit &= ~(1 << baseLength);
      index += 1;
      var length2 = 32 - clz32(totalChildren) + baseLength;
      if (30 < length2) {
        var numberOfOverflowBits = baseLength - (baseLength % 5);
        length2 = (
          baseIdWithLeadingBit &
          ((1 << numberOfOverflowBits) - 1)
        ).toString(32);
        baseIdWithLeadingBit >>= numberOfOverflowBits;
        baseLength -= numberOfOverflowBits;
        return {
          id:
            (1 << (32 - clz32(totalChildren) + baseLength)) |
            (index << baseLength) |
            baseIdWithLeadingBit,
          overflow: length2 + baseContext,
        };
      }
      return {
        id: (1 << length2) | (index << baseLength) | baseIdWithLeadingBit,
        overflow: baseContext,
      };
    }
    var clz32 = Math.clz32 ? Math.clz32 : clz32Fallback;
    var log = Math.log;
    var LN2 = Math.LN2;
    function clz32Fallback(x) {
      x >>>= 0;
      return 0 === x ? 32 : (31 - ((log(x) / LN2) | 0)) | 0;
    }
    function noop5() {}
    var SuspenseException = Error(
      "Suspense Exception: This is not a real error! It's an implementation detail of `use` to interrupt the current render. You must either rethrow it immediately, or move the `use` call outside of the `try/catch` block. Capturing without rethrowing will lead to unexpected behavior.\n\nTo handle async errors, wrap your component in an error boundary, or call the promise's `.catch` method and pass the result to `use`.",
    );
    function trackUsedThenable(thenableState2, thenable, index) {
      index = thenableState2[index];
      void 0 === index
        ? thenableState2.push(thenable)
        : index !== thenable &&
          (thenable.then(noop5, noop5), (thenable = index));
      switch (thenable.status) {
        case "fulfilled":
          return thenable.value;
        case "rejected":
          throw thenable.reason;
        default:
          "string" === typeof thenable.status
            ? thenable.then(noop5, noop5)
            : ((thenableState2 = thenable),
              (thenableState2.status = "pending"),
              thenableState2.then(
                function (fulfilledValue) {
                  if ("pending" === thenable.status) {
                    var fulfilledThenable = thenable;
                    fulfilledThenable.status = "fulfilled";
                    fulfilledThenable.value = fulfilledValue;
                  }
                },
                function (error) {
                  if ("pending" === thenable.status) {
                    var rejectedThenable = thenable;
                    rejectedThenable.status = "rejected";
                    rejectedThenable.reason = error;
                  }
                },
              ));
          switch (thenable.status) {
            case "fulfilled":
              return thenable.value;
            case "rejected":
              throw thenable.reason;
          }
          suspendedThenable = thenable;
          throw SuspenseException;
      }
    }
    var suspendedThenable = null;
    function getSuspendedThenable() {
      if (null === suspendedThenable)
        throw Error(
          "Expected a suspended thenable. This is a bug in React. Please file an issue.",
        );
      var thenable = suspendedThenable;
      suspendedThenable = null;
      return thenable;
    }
    function is(x, y) {
      return (x === y && (0 !== x || 1 / x === 1 / y)) || (x !== x && y !== y);
    }
    var objectIs = "function" === typeof Object.is ? Object.is : is;
    var currentlyRenderingComponent = null;
    var currentlyRenderingTask = null;
    var currentlyRenderingRequest = null;
    var currentlyRenderingKeyPath = null;
    var firstWorkInProgressHook = null;
    var workInProgressHook = null;
    var isReRender = false;
    var didScheduleRenderPhaseUpdate = false;
    var localIdCounter = 0;
    var actionStateCounter = 0;
    var actionStateMatchingIndex = -1;
    var thenableIndexCounter = 0;
    var thenableState = null;
    var renderPhaseUpdates = null;
    var numberOfReRenders = 0;
    function resolveCurrentlyRenderingComponent() {
      if (null === currentlyRenderingComponent)
        throw Error(
          "Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://react.dev/link/invalid-hook-call for tips about how to debug and fix this problem.",
        );
      return currentlyRenderingComponent;
    }
    function createHook() {
      if (0 < numberOfReRenders)
        throw Error("Rendered more hooks than during the previous render");
      return { memoizedState: null, queue: null, next: null };
    }
    function createWorkInProgressHook() {
      null === workInProgressHook
        ? null === firstWorkInProgressHook
          ? ((isReRender = false),
            (firstWorkInProgressHook = workInProgressHook = createHook()))
          : ((isReRender = true),
            (workInProgressHook = firstWorkInProgressHook))
        : null === workInProgressHook.next
          ? ((isReRender = false),
            (workInProgressHook = workInProgressHook.next = createHook()))
          : ((isReRender = true),
            (workInProgressHook = workInProgressHook.next));
      return workInProgressHook;
    }
    function getThenableStateAfterSuspending() {
      var state = thenableState;
      thenableState = null;
      return state;
    }
    function resetHooksState() {
      currentlyRenderingKeyPath =
        currentlyRenderingRequest =
        currentlyRenderingTask =
        currentlyRenderingComponent =
          null;
      didScheduleRenderPhaseUpdate = false;
      firstWorkInProgressHook = null;
      numberOfReRenders = 0;
      workInProgressHook = renderPhaseUpdates = null;
    }
    function basicStateReducer(state, action) {
      return "function" === typeof action ? action(state) : action;
    }
    function useReducer(reducer, initialArg, init) {
      currentlyRenderingComponent = resolveCurrentlyRenderingComponent();
      workInProgressHook = createWorkInProgressHook();
      if (isReRender) {
        var queue = workInProgressHook.queue;
        initialArg = queue.dispatch;
        if (
          null !== renderPhaseUpdates &&
          ((init = renderPhaseUpdates.get(queue)), void 0 !== init)
        ) {
          renderPhaseUpdates.delete(queue);
          queue = workInProgressHook.memoizedState;
          do ((queue = reducer(queue, init.action)), (init = init.next));
          while (null !== init);
          workInProgressHook.memoizedState = queue;
          return [queue, initialArg];
        }
        return [workInProgressHook.memoizedState, initialArg];
      }
      reducer =
        reducer === basicStateReducer
          ? "function" === typeof initialArg
            ? initialArg()
            : initialArg
          : void 0 !== init
            ? init(initialArg)
            : initialArg;
      workInProgressHook.memoizedState = reducer;
      reducer = workInProgressHook.queue = { last: null, dispatch: null };
      reducer = reducer.dispatch = dispatchAction.bind(
        null,
        currentlyRenderingComponent,
        reducer,
      );
      return [workInProgressHook.memoizedState, reducer];
    }
    function useMemo20(nextCreate, deps) {
      currentlyRenderingComponent = resolveCurrentlyRenderingComponent();
      workInProgressHook = createWorkInProgressHook();
      deps = void 0 === deps ? null : deps;
      if (null !== workInProgressHook) {
        var prevState = workInProgressHook.memoizedState;
        if (null !== prevState && null !== deps) {
          var prevDeps = prevState[1];
          a: if (null === prevDeps) prevDeps = false;
          else {
            for (var i = 0; i < prevDeps.length && i < deps.length; i++)
              if (!objectIs(deps[i], prevDeps[i])) {
                prevDeps = false;
                break a;
              }
            prevDeps = true;
          }
          if (prevDeps) return prevState[0];
        }
      }
      nextCreate = nextCreate();
      workInProgressHook.memoizedState = [nextCreate, deps];
      return nextCreate;
    }
    function dispatchAction(componentIdentity, queue, action) {
      if (25 <= numberOfReRenders)
        throw Error(
          "Too many re-renders. React limits the number of renders to prevent an infinite loop.",
        );
      if (componentIdentity === currentlyRenderingComponent)
        if (
          ((didScheduleRenderPhaseUpdate = true),
          (componentIdentity = { action, next: null }),
          null === renderPhaseUpdates &&
            (renderPhaseUpdates = /* @__PURE__ */ new Map()),
          (action = renderPhaseUpdates.get(queue)),
          void 0 === action)
        )
          renderPhaseUpdates.set(queue, componentIdentity);
        else {
          for (queue = action; null !== queue.next;) queue = queue.next;
          queue.next = componentIdentity;
        }
    }
    function throwOnUseEffectEventCall() {
      throw Error(
        "A function wrapped in useEffectEvent can't be called during rendering.",
      );
    }
    function unsupportedStartTransition() {
      throw Error("startTransition cannot be called during server rendering.");
    }
    function unsupportedSetOptimisticState() {
      throw Error("Cannot update optimistic state while rendering.");
    }
    function createPostbackActionStateKey(
      permalink,
      componentKeyPath,
      hookIndex,
    ) {
      if (void 0 !== permalink) return "p" + permalink;
      permalink = JSON.stringify([componentKeyPath, null, hookIndex]);
      componentKeyPath = crypto.createHash("md5");
      componentKeyPath.update(permalink);
      return "k" + componentKeyPath.digest("hex");
    }
    function useActionState(action, initialState, permalink) {
      resolveCurrentlyRenderingComponent();
      var actionStateHookIndex = actionStateCounter++,
        request = currentlyRenderingRequest;
      if ("function" === typeof action.$$FORM_ACTION) {
        var nextPostbackStateKey = null,
          componentKeyPath = currentlyRenderingKeyPath;
        request = request.formState;
        var isSignatureEqual = action.$$IS_SIGNATURE_EQUAL;
        if (null !== request && "function" === typeof isSignatureEqual) {
          var postbackKey = request[1];
          isSignatureEqual.call(action, request[2], request[3]) &&
            ((nextPostbackStateKey = createPostbackActionStateKey(
              permalink,
              componentKeyPath,
              actionStateHookIndex,
            )),
            postbackKey === nextPostbackStateKey &&
              ((actionStateMatchingIndex = actionStateHookIndex),
              (initialState = request[0])));
        }
        var boundAction = action.bind(null, initialState);
        action = function (payload) {
          boundAction(payload);
        };
        "function" === typeof boundAction.$$FORM_ACTION &&
          (action.$$FORM_ACTION = function (prefix3) {
            prefix3 = boundAction.$$FORM_ACTION(prefix3);
            void 0 !== permalink &&
              ((permalink += ""), (prefix3.action = permalink));
            var formData = prefix3.data;
            formData &&
              (null === nextPostbackStateKey &&
                (nextPostbackStateKey = createPostbackActionStateKey(
                  permalink,
                  componentKeyPath,
                  actionStateHookIndex,
                )),
              formData.append("$ACTION_KEY", nextPostbackStateKey));
            return prefix3;
          });
        return [initialState, action, false];
      }
      var boundAction$22 = action.bind(null, initialState);
      return [
        initialState,
        function (payload) {
          boundAction$22(payload);
        },
        false,
      ];
    }
    function unwrapThenable(thenable) {
      var index = thenableIndexCounter;
      thenableIndexCounter += 1;
      null === thenableState && (thenableState = []);
      return trackUsedThenable(thenableState, thenable, index);
    }
    function unsupportedRefresh() {
      throw Error("Cache cannot be refreshed during server rendering.");
    }
    var HooksDispatcher = {
      readContext: function (context) {
        return context._currentValue;
      },
      use: function (usable) {
        if (null !== usable && "object" === typeof usable) {
          if ("function" === typeof usable.then) return unwrapThenable(usable);
          if (usable.$$typeof === REACT_CONTEXT_TYPE)
            return usable._currentValue;
        }
        throw Error(
          "An unsupported type was passed to use(): " + String(usable),
        );
      },
      useContext: function (context) {
        resolveCurrentlyRenderingComponent();
        return context._currentValue;
      },
      useMemo: useMemo20,
      useReducer,
      useRef: function (initialValue) {
        currentlyRenderingComponent = resolveCurrentlyRenderingComponent();
        workInProgressHook = createWorkInProgressHook();
        var previousRef = workInProgressHook.memoizedState;
        return null === previousRef
          ? ((initialValue = { current: initialValue }),
            (workInProgressHook.memoizedState = initialValue))
          : previousRef;
      },
      useState: function (initialState) {
        return useReducer(basicStateReducer, initialState);
      },
      useInsertionEffect: noop5,
      useLayoutEffect: noop5,
      useCallback: function (callback, deps) {
        return useMemo20(function () {
          return callback;
        }, deps);
      },
      useImperativeHandle: noop5,
      useEffect: noop5,
      useDebugValue: noop5,
      useDeferredValue: function (value, initialValue) {
        resolveCurrentlyRenderingComponent();
        return void 0 !== initialValue ? initialValue : value;
      },
      useTransition: function () {
        resolveCurrentlyRenderingComponent();
        return [false, unsupportedStartTransition];
      },
      useId: function () {
        var JSCompiler_inline_result = currentlyRenderingTask.treeContext;
        var overflow = JSCompiler_inline_result.overflow;
        JSCompiler_inline_result = JSCompiler_inline_result.id;
        JSCompiler_inline_result =
          (
            JSCompiler_inline_result &
            ~(1 << (32 - clz32(JSCompiler_inline_result) - 1))
          ).toString(32) + overflow;
        var resumableState = currentResumableState;
        if (null === resumableState)
          throw Error(
            "Invalid hook call. Hooks can only be called inside of the body of a function component.",
          );
        overflow = localIdCounter++;
        JSCompiler_inline_result =
          "_" + resumableState.idPrefix + "R_" + JSCompiler_inline_result;
        0 < overflow &&
          (JSCompiler_inline_result += "H" + overflow.toString(32));
        return JSCompiler_inline_result + "_";
      },
      useSyncExternalStore: function (
        subscribe,
        getSnapshot,
        getServerSnapshot,
      ) {
        if (void 0 === getServerSnapshot)
          throw Error(
            "Missing getServerSnapshot, which is required for server-rendered content. Will revert to client rendering.",
          );
        return getServerSnapshot();
      },
      useOptimistic: function (passthrough) {
        resolveCurrentlyRenderingComponent();
        return [passthrough, unsupportedSetOptimisticState];
      },
      useActionState,
      useFormState: useActionState,
      useHostTransitionStatus: function () {
        resolveCurrentlyRenderingComponent();
        return sharedNotPendingObject;
      },
      useMemoCache: function (size) {
        for (var data = Array(size), i = 0; i < size; i++)
          data[i] = REACT_MEMO_CACHE_SENTINEL;
        return data;
      },
      useCacheRefresh: function () {
        return unsupportedRefresh;
      },
      useEffectEvent: function () {
        return throwOnUseEffectEventCall;
      },
    };
    var currentResumableState = null;
    var DefaultAsyncDispatcher = {
      getCacheForType: function () {
        throw Error("Not implemented.");
      },
      cacheSignal: function () {
        throw Error("Not implemented.");
      },
    };
    function prepareStackTrace(error, structuredStackTrace) {
      error = (error.name || "Error") + ": " + (error.message || "");
      for (var i = 0; i < structuredStackTrace.length; i++)
        error += "\n    at " + structuredStackTrace[i].toString();
      return error;
    }
    var prefix2;
    var suffix;
    function describeBuiltInComponentFrame(name) {
      if (void 0 === prefix2)
        try {
          throw Error();
        } catch (x) {
          var match2 = x.stack.trim().match(/\n( *(at )?)/);
          prefix2 = (match2 && match2[1]) || "";
          suffix =
            -1 < x.stack.indexOf("\n    at")
              ? " (<anonymous>)"
              : -1 < x.stack.indexOf("@")
                ? "@unknown:0:0"
                : "";
        }
      return "\n" + prefix2 + name + suffix;
    }
    var reentry = false;
    function describeNativeComponentFrame(fn2, construct) {
      if (!fn2 || reentry) return "";
      reentry = true;
      var previousPrepareStackTrace = Error.prepareStackTrace;
      Error.prepareStackTrace = prepareStackTrace;
      try {
        var RunInRootFrame = {
          DetermineComponentFrameRoot: function () {
            try {
              if (construct) {
                var Fake = function () {
                  throw Error();
                };
                Object.defineProperty(Fake.prototype, "props", {
                  set: function () {
                    throw Error();
                  },
                });
                if ("object" === typeof Reflect && Reflect.construct) {
                  try {
                    Reflect.construct(Fake, []);
                  } catch (x) {
                    var control = x;
                  }
                  Reflect.construct(fn2, [], Fake);
                } else {
                  try {
                    Fake.call();
                  } catch (x$24) {
                    control = x$24;
                  }
                  fn2.call(Fake.prototype);
                }
              } else {
                try {
                  throw Error();
                } catch (x$25) {
                  control = x$25;
                }
                (Fake = fn2()) &&
                  "function" === typeof Fake.catch &&
                  Fake.catch(function () {});
              }
            } catch (sample) {
              if (sample && control && "string" === typeof sample.stack)
                return [sample.stack, control.stack];
            }
            return [null, null];
          },
        };
        RunInRootFrame.DetermineComponentFrameRoot.displayName =
          "DetermineComponentFrameRoot";
        var namePropDescriptor = Object.getOwnPropertyDescriptor(
          RunInRootFrame.DetermineComponentFrameRoot,
          "name",
        );
        namePropDescriptor &&
          namePropDescriptor.configurable &&
          Object.defineProperty(
            RunInRootFrame.DetermineComponentFrameRoot,
            "name",
            { value: "DetermineComponentFrameRoot" },
          );
        var _RunInRootFrame$Deter =
            RunInRootFrame.DetermineComponentFrameRoot(),
          sampleStack = _RunInRootFrame$Deter[0],
          controlStack = _RunInRootFrame$Deter[1];
        if (sampleStack && controlStack) {
          var sampleLines = sampleStack.split("\n"),
            controlLines = controlStack.split("\n");
          for (
            namePropDescriptor = RunInRootFrame = 0;
            RunInRootFrame < sampleLines.length &&
            !sampleLines[RunInRootFrame].includes(
              "DetermineComponentFrameRoot",
            );
          )
            RunInRootFrame++;
          for (
            ;
            namePropDescriptor < controlLines.length &&
            !controlLines[namePropDescriptor].includes(
              "DetermineComponentFrameRoot",
            );
          )
            namePropDescriptor++;
          if (
            RunInRootFrame === sampleLines.length ||
            namePropDescriptor === controlLines.length
          )
            for (
              RunInRootFrame = sampleLines.length - 1,
                namePropDescriptor = controlLines.length - 1;
              1 <= RunInRootFrame &&
              0 <= namePropDescriptor &&
              sampleLines[RunInRootFrame] !== controlLines[namePropDescriptor];
            )
              namePropDescriptor--;
          for (
            ;
            1 <= RunInRootFrame && 0 <= namePropDescriptor;
            RunInRootFrame--, namePropDescriptor--
          )
            if (
              sampleLines[RunInRootFrame] !== controlLines[namePropDescriptor]
            ) {
              if (1 !== RunInRootFrame || 1 !== namePropDescriptor) {
                do
                  if (
                    (RunInRootFrame--,
                    namePropDescriptor--,
                    0 > namePropDescriptor ||
                      sampleLines[RunInRootFrame] !==
                        controlLines[namePropDescriptor])
                  ) {
                    var frame =
                      "\n" +
                      sampleLines[RunInRootFrame].replace(" at new ", " at ");
                    fn2.displayName &&
                      frame.includes("<anonymous>") &&
                      (frame = frame.replace("<anonymous>", fn2.displayName));
                    return frame;
                  }
                while (1 <= RunInRootFrame && 0 <= namePropDescriptor);
              }
              break;
            }
        }
      } finally {
        ((reentry = false),
          (Error.prepareStackTrace = previousPrepareStackTrace));
      }
      return (previousPrepareStackTrace = fn2
        ? fn2.displayName || fn2.name
        : "")
        ? describeBuiltInComponentFrame(previousPrepareStackTrace)
        : "";
    }
    function describeComponentStackByType(type) {
      if ("string" === typeof type) return describeBuiltInComponentFrame(type);
      if ("function" === typeof type)
        return type.prototype && type.prototype.isReactComponent
          ? describeNativeComponentFrame(type, true)
          : describeNativeComponentFrame(type, false);
      if ("object" === typeof type && null !== type) {
        switch (type.$$typeof) {
          case REACT_FORWARD_REF_TYPE:
            return describeNativeComponentFrame(type.render, false);
          case REACT_MEMO_TYPE:
            return describeNativeComponentFrame(type.type, false);
          case REACT_LAZY_TYPE:
            var lazyComponent = type,
              payload = lazyComponent._payload;
            lazyComponent = lazyComponent._init;
            try {
              type = lazyComponent(payload);
            } catch (x) {
              return describeBuiltInComponentFrame("Lazy");
            }
            return describeComponentStackByType(type);
        }
        if ("string" === typeof type.name) {
          a: {
            payload = type.name;
            lazyComponent = type.env;
            var location2 = type.debugLocation;
            if (
              null != location2 &&
              ((type = Error.prepareStackTrace),
              (Error.prepareStackTrace = prepareStackTrace),
              (location2 = location2.stack),
              (Error.prepareStackTrace = type),
              location2.startsWith("Error: react-stack-top-frame\n") &&
                (location2 = location2.slice(29)),
              (type = location2.indexOf("\n")),
              -1 !== type && (location2 = location2.slice(type + 1)),
              (type = location2.indexOf("react_stack_bottom_frame")),
              -1 !== type && (type = location2.lastIndexOf("\n", type)),
              (type =
                -1 !== type ? (location2 = location2.slice(0, type)) : ""),
              (location2 = type.lastIndexOf("\n")),
              (type = -1 === location2 ? type : type.slice(location2 + 1)),
              -1 !== type.indexOf(payload))
            ) {
              payload = "\n" + type;
              break a;
            }
            payload = describeBuiltInComponentFrame(
              payload + (lazyComponent ? " [" + lazyComponent + "]" : ""),
            );
          }
          return payload;
        }
      }
      switch (type) {
        case REACT_SUSPENSE_LIST_TYPE:
          return describeBuiltInComponentFrame("SuspenseList");
        case REACT_SUSPENSE_TYPE:
          return describeBuiltInComponentFrame("Suspense");
      }
      return "";
    }
    function isEligibleForOutlining(request, boundary) {
      return (
        (500 < boundary.byteSize ||
          hasSuspenseyContent(boundary.contentState)) &&
        null === boundary.contentPreamble
      );
    }
    function defaultErrorHandler(error) {
      if (
        "object" === typeof error &&
        null !== error &&
        "string" === typeof error.environmentName
      ) {
        var JSCompiler_inline_result = error.environmentName;
        error = [error].slice(0);
        "string" === typeof error[0]
          ? error.splice(
              0,
              1,
              "\x1B[0m\x1B[7m%c%s\x1B[0m%c " + error[0],
              "background: #e6e6e6;background: light-dark(rgba(0,0,0,0.1), rgba(255,255,255,0.25));color: #000000;color: light-dark(#000000, #ffffff);border-radius: 2px",
              " " + JSCompiler_inline_result + " ",
              "",
            )
          : error.splice(
              0,
              0,
              "\x1B[0m\x1B[7m%c%s\x1B[0m%c",
              "background: #e6e6e6;background: light-dark(rgba(0,0,0,0.1), rgba(255,255,255,0.25));color: #000000;color: light-dark(#000000, #ffffff);border-radius: 2px",
              " " + JSCompiler_inline_result + " ",
              "",
            );
        error.unshift(console);
        JSCompiler_inline_result = bind.apply(console.error, error);
        JSCompiler_inline_result();
      } else console.error(error);
      return null;
    }
    function RequestInstance(
      resumableState,
      renderState,
      rootFormatContext,
      progressiveChunkSize,
      onError,
      onAllReady,
      onShellReady,
      onShellError,
      onFatalError,
      onPostpone,
      formState,
    ) {
      var abortSet = /* @__PURE__ */ new Set();
      this.destination = null;
      this.flushScheduled = false;
      this.resumableState = resumableState;
      this.renderState = renderState;
      this.rootFormatContext = rootFormatContext;
      this.progressiveChunkSize =
        void 0 === progressiveChunkSize ? 12800 : progressiveChunkSize;
      this.status = 10;
      this.fatalError = null;
      this.pendingRootTasks = this.allPendingTasks = this.nextSegmentId = 0;
      this.completedPreambleSegments = this.completedRootSegment = null;
      this.byteSize = 0;
      this.abortableTasks = abortSet;
      this.pingedTasks = [];
      this.clientRenderedBoundaries = [];
      this.completedBoundaries = [];
      this.partialBoundaries = [];
      this.trackedPostpones = null;
      this.onError = void 0 === onError ? defaultErrorHandler : onError;
      this.onPostpone = void 0 === onPostpone ? noop5 : onPostpone;
      this.onAllReady = void 0 === onAllReady ? noop5 : onAllReady;
      this.onShellReady = void 0 === onShellReady ? noop5 : onShellReady;
      this.onShellError = void 0 === onShellError ? noop5 : onShellError;
      this.onFatalError = void 0 === onFatalError ? noop5 : onFatalError;
      this.formState = void 0 === formState ? null : formState;
    }
    function createRequest(
      children,
      resumableState,
      renderState,
      rootFormatContext,
      progressiveChunkSize,
      onError,
      onAllReady,
      onShellReady,
      onShellError,
      onFatalError,
      onPostpone,
      formState,
    ) {
      resumableState = new RequestInstance(
        resumableState,
        renderState,
        rootFormatContext,
        progressiveChunkSize,
        onError,
        onAllReady,
        onShellReady,
        onShellError,
        onFatalError,
        onPostpone,
        formState,
      );
      renderState = createPendingSegment(
        resumableState,
        0,
        null,
        rootFormatContext,
        false,
        false,
      );
      renderState.parentFlushed = true;
      children = createRenderTask(
        resumableState,
        null,
        children,
        -1,
        null,
        renderState,
        null,
        null,
        resumableState.abortableTasks,
        null,
        rootFormatContext,
        null,
        emptyTreeContext,
        null,
        null,
      );
      pushComponentStack(children);
      resumableState.pingedTasks.push(children);
      return resumableState;
    }
    function createPrerenderRequest(
      children,
      resumableState,
      renderState,
      rootFormatContext,
      progressiveChunkSize,
      onError,
      onAllReady,
      onShellReady,
      onShellError,
      onFatalError,
      onPostpone,
    ) {
      children = createRequest(
        children,
        resumableState,
        renderState,
        rootFormatContext,
        progressiveChunkSize,
        onError,
        onAllReady,
        onShellReady,
        onShellError,
        onFatalError,
        onPostpone,
        void 0,
      );
      children.trackedPostpones = {
        workingMap: /* @__PURE__ */ new Map(),
        rootNodes: [],
        rootSlots: null,
      };
      return children;
    }
    function resumeRequest(
      children,
      postponedState,
      renderState,
      onError,
      onAllReady,
      onShellReady,
      onShellError,
      onFatalError,
      onPostpone,
    ) {
      renderState = new RequestInstance(
        postponedState.resumableState,
        renderState,
        postponedState.rootFormatContext,
        postponedState.progressiveChunkSize,
        onError,
        onAllReady,
        onShellReady,
        onShellError,
        onFatalError,
        onPostpone,
        null,
      );
      renderState.nextSegmentId = postponedState.nextSegmentId;
      if ("number" === typeof postponedState.replaySlots)
        return (
          (onError = createPendingSegment(
            renderState,
            0,
            null,
            postponedState.rootFormatContext,
            false,
            false,
          )),
          (onError.parentFlushed = true),
          (children = createRenderTask(
            renderState,
            null,
            children,
            -1,
            null,
            onError,
            null,
            null,
            renderState.abortableTasks,
            null,
            postponedState.rootFormatContext,
            null,
            emptyTreeContext,
            null,
            null,
          )),
          pushComponentStack(children),
          renderState.pingedTasks.push(children),
          renderState
        );
      children = createReplayTask(
        renderState,
        null,
        {
          nodes: postponedState.replayNodes,
          slots: postponedState.replaySlots,
          pendingTasks: 0,
        },
        children,
        -1,
        null,
        null,
        renderState.abortableTasks,
        null,
        postponedState.rootFormatContext,
        null,
        emptyTreeContext,
        null,
        null,
      );
      pushComponentStack(children);
      renderState.pingedTasks.push(children);
      return renderState;
    }
    function resumeAndPrerenderRequest(
      children,
      postponedState,
      renderState,
      onError,
      onAllReady,
      onShellReady,
      onShellError,
      onFatalError,
      onPostpone,
    ) {
      children = resumeRequest(
        children,
        postponedState,
        renderState,
        onError,
        onAllReady,
        onShellReady,
        onShellError,
        onFatalError,
        onPostpone,
      );
      children.trackedPostpones = {
        workingMap: /* @__PURE__ */ new Map(),
        rootNodes: [],
        rootSlots: null,
      };
      return children;
    }
    var currentRequest = null;
    function resolveRequest() {
      if (currentRequest) return currentRequest;
      var store = requestStorage.getStore();
      return store ? store : null;
    }
    function pingTask(request, task) {
      request.pingedTasks.push(task);
      1 === request.pingedTasks.length &&
        ((request.flushScheduled = null !== request.destination),
        null !== request.trackedPostpones || 10 === request.status
          ? scheduleMicrotask(function () {
              return performWork(request);
            })
          : setImmediate(function () {
              return performWork(request);
            }));
    }
    function createSuspenseBoundary(
      request,
      row,
      fallbackAbortableTasks,
      contentPreamble,
      fallbackPreamble,
    ) {
      fallbackAbortableTasks = {
        status: 0,
        rootSegmentID: -1,
        parentFlushed: false,
        pendingTasks: 0,
        row,
        completedSegments: [],
        byteSize: 0,
        fallbackAbortableTasks,
        errorDigest: null,
        contentState: createHoistableState(),
        fallbackState: createHoistableState(),
        contentPreamble,
        fallbackPreamble,
        trackedContentKeyPath: null,
        trackedFallbackNode: null,
      };
      null !== row &&
        (row.pendingTasks++,
        (contentPreamble = row.boundaries),
        null !== contentPreamble &&
          (request.allPendingTasks++,
          fallbackAbortableTasks.pendingTasks++,
          contentPreamble.push(fallbackAbortableTasks)),
        (request = row.inheritedHoistables),
        null !== request &&
          hoistHoistables(fallbackAbortableTasks.contentState, request));
      return fallbackAbortableTasks;
    }
    function createRenderTask(
      request,
      thenableState2,
      node2,
      childIndex,
      blockedBoundary,
      blockedSegment,
      blockedPreamble,
      hoistableState,
      abortSet,
      keyPath,
      formatContext,
      context,
      treeContext,
      row,
      componentStack,
    ) {
      request.allPendingTasks++;
      null === blockedBoundary
        ? request.pendingRootTasks++
        : blockedBoundary.pendingTasks++;
      null !== row && row.pendingTasks++;
      var task = {
        replay: null,
        node: node2,
        childIndex,
        ping: function () {
          return pingTask(request, task);
        },
        blockedBoundary,
        blockedSegment,
        blockedPreamble,
        hoistableState,
        abortSet,
        keyPath,
        formatContext,
        context,
        treeContext,
        row,
        componentStack,
        thenableState: thenableState2,
      };
      abortSet.add(task);
      return task;
    }
    function createReplayTask(
      request,
      thenableState2,
      replay,
      node2,
      childIndex,
      blockedBoundary,
      hoistableState,
      abortSet,
      keyPath,
      formatContext,
      context,
      treeContext,
      row,
      componentStack,
    ) {
      request.allPendingTasks++;
      null === blockedBoundary
        ? request.pendingRootTasks++
        : blockedBoundary.pendingTasks++;
      null !== row && row.pendingTasks++;
      replay.pendingTasks++;
      var task = {
        replay,
        node: node2,
        childIndex,
        ping: function () {
          return pingTask(request, task);
        },
        blockedBoundary,
        blockedSegment: null,
        blockedPreamble: null,
        hoistableState,
        abortSet,
        keyPath,
        formatContext,
        context,
        treeContext,
        row,
        componentStack,
        thenableState: thenableState2,
      };
      abortSet.add(task);
      return task;
    }
    function createPendingSegment(
      request,
      index,
      boundary,
      parentFormatContext,
      lastPushedText,
      textEmbedded,
    ) {
      return {
        status: 0,
        parentFlushed: false,
        id: -1,
        index,
        chunks: [],
        children: [],
        preambleChildren: [],
        parentFormatContext,
        boundary,
        lastPushedText,
        textEmbedded,
      };
    }
    function pushComponentStack(task) {
      var node2 = task.node;
      if ("object" === typeof node2 && null !== node2)
        switch (node2.$$typeof) {
          case REACT_ELEMENT_TYPE:
            task.componentStack = {
              parent: task.componentStack,
              type: node2.type,
            };
        }
    }
    function replaceSuspenseComponentStackWithSuspenseFallbackStack(
      componentStack,
    ) {
      return null === componentStack
        ? null
        : { parent: componentStack.parent, type: "Suspense Fallback" };
    }
    function getThrownInfo(node$jscomp$0) {
      var errorInfo = {};
      node$jscomp$0 &&
        Object.defineProperty(errorInfo, "componentStack", {
          configurable: true,
          enumerable: true,
          get: function () {
            try {
              var info = "",
                node2 = node$jscomp$0;
              do
                ((info += describeComponentStackByType(node2.type)),
                  (node2 = node2.parent));
              while (node2);
              var JSCompiler_inline_result = info;
            } catch (x) {
              JSCompiler_inline_result =
                "\nError generating stack: " + x.message + "\n" + x.stack;
            }
            Object.defineProperty(errorInfo, "componentStack", {
              value: JSCompiler_inline_result,
            });
            return JSCompiler_inline_result;
          },
        });
      return errorInfo;
    }
    function logRecoverableError(request, error, errorInfo) {
      request = request.onError;
      error = request(error, errorInfo);
      if (null == error || "string" === typeof error) return error;
    }
    function fatalError(request, error) {
      var onShellError = request.onShellError,
        onFatalError = request.onFatalError;
      onShellError(error);
      onFatalError(error);
      null !== request.destination
        ? ((request.status = 14), request.destination.destroy(error))
        : ((request.status = 13), (request.fatalError = error));
    }
    function finishSuspenseListRow(request, row) {
      unblockSuspenseListRow(request, row.next, row.hoistables);
    }
    function unblockSuspenseListRow(
      request,
      unblockedRow,
      inheritedHoistables,
    ) {
      for (; null !== unblockedRow;) {
        null !== inheritedHoistables &&
          (hoistHoistables(unblockedRow.hoistables, inheritedHoistables),
          (unblockedRow.inheritedHoistables = inheritedHoistables));
        var unblockedBoundaries = unblockedRow.boundaries;
        if (null !== unblockedBoundaries) {
          unblockedRow.boundaries = null;
          for (var i = 0; i < unblockedBoundaries.length; i++) {
            var unblockedBoundary = unblockedBoundaries[i];
            null !== inheritedHoistables &&
              hoistHoistables(
                unblockedBoundary.contentState,
                inheritedHoistables,
              );
            finishedTask(request, unblockedBoundary, null, null);
          }
        }
        unblockedRow.pendingTasks--;
        if (0 < unblockedRow.pendingTasks) break;
        inheritedHoistables = unblockedRow.hoistables;
        unblockedRow = unblockedRow.next;
      }
    }
    function tryToResolveTogetherRow(request, togetherRow) {
      var boundaries = togetherRow.boundaries;
      if (
        null !== boundaries &&
        togetherRow.pendingTasks === boundaries.length
      ) {
        for (
          var allCompleteAndInlinable = true, i = 0;
          i < boundaries.length;
          i++
        ) {
          var rowBoundary = boundaries[i];
          if (
            1 !== rowBoundary.pendingTasks ||
            rowBoundary.parentFlushed ||
            isEligibleForOutlining(request, rowBoundary)
          ) {
            allCompleteAndInlinable = false;
            break;
          }
        }
        allCompleteAndInlinable &&
          unblockSuspenseListRow(request, togetherRow, togetherRow.hoistables);
      }
    }
    function createSuspenseListRow(previousRow) {
      var newRow = {
        pendingTasks: 1,
        boundaries: null,
        hoistables: createHoistableState(),
        inheritedHoistables: null,
        together: false,
        next: null,
      };
      null !== previousRow &&
        0 < previousRow.pendingTasks &&
        (newRow.pendingTasks++,
        (newRow.boundaries = []),
        (previousRow.next = newRow));
      return newRow;
    }
    function renderSuspenseListRows(request, task, keyPath, rows, revealOrder) {
      var prevKeyPath = task.keyPath,
        prevTreeContext = task.treeContext,
        prevRow = task.row;
      task.keyPath = keyPath;
      keyPath = rows.length;
      var previousSuspenseListRow = null;
      if (null !== task.replay) {
        var resumeSlots = task.replay.slots;
        if (null !== resumeSlots && "object" === typeof resumeSlots)
          for (var n = 0; n < keyPath; n++) {
            var i =
                "backwards" !== revealOrder &&
                "unstable_legacy-backwards" !== revealOrder
                  ? n
                  : keyPath - 1 - n,
              node2 = rows[i];
            task.row = previousSuspenseListRow = createSuspenseListRow(
              previousSuspenseListRow,
            );
            task.treeContext = pushTreeContext(prevTreeContext, keyPath, i);
            var resumeSegmentID = resumeSlots[i];
            "number" === typeof resumeSegmentID
              ? (resumeNode(request, task, resumeSegmentID, node2, i),
                delete resumeSlots[i])
              : renderNode(request, task, node2, i);
            0 === --previousSuspenseListRow.pendingTasks &&
              finishSuspenseListRow(request, previousSuspenseListRow);
          }
        else
          for (resumeSlots = 0; resumeSlots < keyPath; resumeSlots++)
            ((n =
              "backwards" !== revealOrder &&
              "unstable_legacy-backwards" !== revealOrder
                ? resumeSlots
                : keyPath - 1 - resumeSlots),
              (i = rows[n]),
              (task.row = previousSuspenseListRow =
                createSuspenseListRow(previousSuspenseListRow)),
              (task.treeContext = pushTreeContext(prevTreeContext, keyPath, n)),
              renderNode(request, task, i, n),
              0 === --previousSuspenseListRow.pendingTasks &&
                finishSuspenseListRow(request, previousSuspenseListRow));
      } else if (
        "backwards" !== revealOrder &&
        "unstable_legacy-backwards" !== revealOrder
      )
        for (revealOrder = 0; revealOrder < keyPath; revealOrder++)
          ((resumeSlots = rows[revealOrder]),
            (task.row = previousSuspenseListRow =
              createSuspenseListRow(previousSuspenseListRow)),
            (task.treeContext = pushTreeContext(
              prevTreeContext,
              keyPath,
              revealOrder,
            )),
            renderNode(request, task, resumeSlots, revealOrder),
            0 === --previousSuspenseListRow.pendingTasks &&
              finishSuspenseListRow(request, previousSuspenseListRow));
      else {
        revealOrder = task.blockedSegment;
        resumeSlots = revealOrder.children.length;
        n = revealOrder.chunks.length;
        for (i = keyPath - 1; 0 <= i; i--) {
          node2 = rows[i];
          task.row = previousSuspenseListRow = createSuspenseListRow(
            previousSuspenseListRow,
          );
          task.treeContext = pushTreeContext(prevTreeContext, keyPath, i);
          resumeSegmentID = createPendingSegment(
            request,
            n,
            null,
            task.formatContext,
            0 === i ? revealOrder.lastPushedText : true,
            true,
          );
          revealOrder.children.splice(resumeSlots, 0, resumeSegmentID);
          task.blockedSegment = resumeSegmentID;
          try {
            (renderNode(request, task, node2, i),
              resumeSegmentID.lastPushedText &&
                resumeSegmentID.textEmbedded &&
                resumeSegmentID.chunks.push(textSeparator),
              (resumeSegmentID.status = 1),
              finishedSegment(request, task.blockedBoundary, resumeSegmentID),
              0 === --previousSuspenseListRow.pendingTasks &&
                finishSuspenseListRow(request, previousSuspenseListRow));
          } catch (thrownValue) {
            throw (
              (resumeSegmentID.status = 12 === request.status ? 3 : 4),
              thrownValue
            );
          }
        }
        task.blockedSegment = revealOrder;
        revealOrder.lastPushedText = false;
      }
      null !== prevRow &&
        null !== previousSuspenseListRow &&
        0 < previousSuspenseListRow.pendingTasks &&
        (prevRow.pendingTasks++, (previousSuspenseListRow.next = prevRow));
      task.treeContext = prevTreeContext;
      task.row = prevRow;
      task.keyPath = prevKeyPath;
    }
    function renderWithHooks(
      request,
      task,
      keyPath,
      Component,
      props,
      secondArg,
    ) {
      var prevThenableState = task.thenableState;
      task.thenableState = null;
      currentlyRenderingComponent = {};
      currentlyRenderingTask = task;
      currentlyRenderingRequest = request;
      currentlyRenderingKeyPath = keyPath;
      actionStateCounter = localIdCounter = 0;
      actionStateMatchingIndex = -1;
      thenableIndexCounter = 0;
      thenableState = prevThenableState;
      for (request = Component(props, secondArg); didScheduleRenderPhaseUpdate;)
        ((didScheduleRenderPhaseUpdate = false),
          (actionStateCounter = localIdCounter = 0),
          (actionStateMatchingIndex = -1),
          (thenableIndexCounter = 0),
          (numberOfReRenders += 1),
          (workInProgressHook = null),
          (request = Component(props, secondArg)));
      resetHooksState();
      return request;
    }
    function finishFunctionComponent(
      request,
      task,
      keyPath,
      children,
      hasId,
      actionStateCount,
      actionStateMatchingIndex2,
    ) {
      var didEmitActionStateMarkers = false;
      if (0 !== actionStateCount && null !== request.formState) {
        var segment = task.blockedSegment;
        if (null !== segment) {
          didEmitActionStateMarkers = true;
          segment = segment.chunks;
          for (var i = 0; i < actionStateCount; i++)
            i === actionStateMatchingIndex2
              ? segment.push(formStateMarkerIsMatching)
              : segment.push(formStateMarkerIsNotMatching);
        }
      }
      actionStateCount = task.keyPath;
      task.keyPath = keyPath;
      hasId
        ? ((keyPath = task.treeContext),
          (task.treeContext = pushTreeContext(keyPath, 1, 0)),
          renderNode(request, task, children, -1),
          (task.treeContext = keyPath))
        : didEmitActionStateMarkers
          ? renderNode(request, task, children, -1)
          : renderNodeDestructive(request, task, children, -1);
      task.keyPath = actionStateCount;
    }
    function renderElement(request, task, keyPath, type, props, ref) {
      if ("function" === typeof type)
        if (type.prototype && type.prototype.isReactComponent) {
          var newProps = props;
          if ("ref" in props) {
            newProps = {};
            for (var propName in props)
              "ref" !== propName && (newProps[propName] = props[propName]);
          }
          var defaultProps2 = type.defaultProps;
          if (defaultProps2) {
            newProps === props && (newProps = assign2({}, newProps, props));
            for (var propName$44 in defaultProps2)
              void 0 === newProps[propName$44] &&
                (newProps[propName$44] = defaultProps2[propName$44]);
          }
          props = newProps;
          newProps = emptyContextObject;
          defaultProps2 = type.contextType;
          "object" === typeof defaultProps2 &&
            null !== defaultProps2 &&
            (newProps = defaultProps2._currentValue);
          newProps = new type(props, newProps);
          var initialState = void 0 !== newProps.state ? newProps.state : null;
          newProps.updater = classComponentUpdater;
          newProps.props = props;
          newProps.state = initialState;
          defaultProps2 = { queue: [], replace: false };
          newProps._reactInternals = defaultProps2;
          ref = type.contextType;
          newProps.context =
            "object" === typeof ref && null !== ref
              ? ref._currentValue
              : emptyContextObject;
          ref = type.getDerivedStateFromProps;
          "function" === typeof ref &&
            ((ref = ref(props, initialState)),
            (initialState =
              null === ref || void 0 === ref
                ? initialState
                : assign2({}, initialState, ref)),
            (newProps.state = initialState));
          if (
            "function" !== typeof type.getDerivedStateFromProps &&
            "function" !== typeof newProps.getSnapshotBeforeUpdate &&
            ("function" === typeof newProps.UNSAFE_componentWillMount ||
              "function" === typeof newProps.componentWillMount)
          )
            if (
              ((type = newProps.state),
              "function" === typeof newProps.componentWillMount &&
                newProps.componentWillMount(),
              "function" === typeof newProps.UNSAFE_componentWillMount &&
                newProps.UNSAFE_componentWillMount(),
              type !== newProps.state &&
                classComponentUpdater.enqueueReplaceState(
                  newProps,
                  newProps.state,
                  null,
                ),
              null !== defaultProps2.queue && 0 < defaultProps2.queue.length)
            )
              if (
                ((type = defaultProps2.queue),
                (ref = defaultProps2.replace),
                (defaultProps2.queue = null),
                (defaultProps2.replace = false),
                ref && 1 === type.length)
              )
                newProps.state = type[0];
              else {
                defaultProps2 = ref ? type[0] : newProps.state;
                initialState = true;
                for (ref = ref ? 1 : 0; ref < type.length; ref++)
                  ((propName$44 = type[ref]),
                    (propName$44 =
                      "function" === typeof propName$44
                        ? propName$44.call(
                            newProps,
                            defaultProps2,
                            props,
                            void 0,
                          )
                        : propName$44),
                    null != propName$44 &&
                      (initialState
                        ? ((initialState = false),
                          (defaultProps2 = assign2(
                            {},
                            defaultProps2,
                            propName$44,
                          )))
                        : assign2(defaultProps2, propName$44)));
                newProps.state = defaultProps2;
              }
            else defaultProps2.queue = null;
          type = newProps.render();
          if (12 === request.status) throw null;
          props = task.keyPath;
          task.keyPath = keyPath;
          renderNodeDestructive(request, task, type, -1);
          task.keyPath = props;
        } else {
          type = renderWithHooks(request, task, keyPath, type, props, void 0);
          if (12 === request.status) throw null;
          finishFunctionComponent(
            request,
            task,
            keyPath,
            type,
            0 !== localIdCounter,
            actionStateCounter,
            actionStateMatchingIndex,
          );
        }
      else if ("string" === typeof type)
        if (((newProps = task.blockedSegment), null === newProps))
          ((newProps = props.children),
            (defaultProps2 = task.formatContext),
            (initialState = task.keyPath),
            (task.formatContext = getChildFormatContext(
              defaultProps2,
              type,
              props,
            )),
            (task.keyPath = keyPath),
            renderNode(request, task, newProps, -1),
            (task.formatContext = defaultProps2),
            (task.keyPath = initialState));
        else {
          initialState = pushStartInstance(
            newProps.chunks,
            type,
            props,
            request.resumableState,
            request.renderState,
            task.blockedPreamble,
            task.hoistableState,
            task.formatContext,
            newProps.lastPushedText,
          );
          newProps.lastPushedText = false;
          defaultProps2 = task.formatContext;
          ref = task.keyPath;
          task.keyPath = keyPath;
          if (
            3 ===
            (task.formatContext = getChildFormatContext(
              defaultProps2,
              type,
              props,
            )).insertionMode
          ) {
            keyPath = createPendingSegment(
              request,
              0,
              null,
              task.formatContext,
              false,
              false,
            );
            newProps.preambleChildren.push(keyPath);
            task.blockedSegment = keyPath;
            try {
              ((keyPath.status = 6),
                renderNode(request, task, initialState, -1),
                keyPath.lastPushedText &&
                  keyPath.textEmbedded &&
                  keyPath.chunks.push(textSeparator),
                (keyPath.status = 1),
                finishedSegment(request, task.blockedBoundary, keyPath));
            } finally {
              task.blockedSegment = newProps;
            }
          } else renderNode(request, task, initialState, -1);
          task.formatContext = defaultProps2;
          task.keyPath = ref;
          a: {
            task = newProps.chunks;
            request = request.resumableState;
            switch (type) {
              case "title":
              case "style":
              case "script":
              case "area":
              case "base":
              case "br":
              case "col":
              case "embed":
              case "hr":
              case "img":
              case "input":
              case "keygen":
              case "link":
              case "meta":
              case "param":
              case "source":
              case "track":
              case "wbr":
                break a;
              case "body":
                if (1 >= defaultProps2.insertionMode) {
                  request.hasBody = true;
                  break a;
                }
                break;
              case "html":
                if (0 === defaultProps2.insertionMode) {
                  request.hasHtml = true;
                  break a;
                }
                break;
              case "head":
                if (1 >= defaultProps2.insertionMode) break a;
            }
            task.push(endChunkForTag(type));
          }
          newProps.lastPushedText = false;
        }
      else {
        switch (type) {
          case REACT_LEGACY_HIDDEN_TYPE:
          case REACT_STRICT_MODE_TYPE:
          case REACT_PROFILER_TYPE:
          case REACT_FRAGMENT_TYPE:
            type = task.keyPath;
            task.keyPath = keyPath;
            renderNodeDestructive(request, task, props.children, -1);
            task.keyPath = type;
            return;
          case REACT_ACTIVITY_TYPE:
            type = task.blockedSegment;
            null === type
              ? "hidden" !== props.mode &&
                ((type = task.keyPath),
                (task.keyPath = keyPath),
                renderNode(request, task, props.children, -1),
                (task.keyPath = type))
              : "hidden" !== props.mode &&
                (type.chunks.push(startActivityBoundary),
                (type.lastPushedText = false),
                (newProps = task.keyPath),
                (task.keyPath = keyPath),
                renderNode(request, task, props.children, -1),
                (task.keyPath = newProps),
                type.chunks.push(endActivityBoundary),
                (type.lastPushedText = false));
            return;
          case REACT_SUSPENSE_LIST_TYPE:
            a: {
              type = props.children;
              props = props.revealOrder;
              if (
                "forwards" === props ||
                "backwards" === props ||
                "unstable_legacy-backwards" === props
              ) {
                if (isArrayImpl(type)) {
                  renderSuspenseListRows(request, task, keyPath, type, props);
                  break a;
                }
                if ((newProps = getIteratorFn(type))) {
                  if ((newProps = newProps.call(type))) {
                    defaultProps2 = newProps.next();
                    if (!defaultProps2.done) {
                      do defaultProps2 = newProps.next();
                      while (!defaultProps2.done);
                      renderSuspenseListRows(
                        request,
                        task,
                        keyPath,
                        type,
                        props,
                      );
                    }
                    break a;
                  }
                }
              }
              "together" === props
                ? ((props = task.keyPath),
                  (newProps = task.row),
                  (defaultProps2 = task.row = createSuspenseListRow(null)),
                  (defaultProps2.boundaries = []),
                  (defaultProps2.together = true),
                  (task.keyPath = keyPath),
                  renderNodeDestructive(request, task, type, -1),
                  0 === --defaultProps2.pendingTasks &&
                    finishSuspenseListRow(request, defaultProps2),
                  (task.keyPath = props),
                  (task.row = newProps),
                  null !== newProps &&
                    0 < defaultProps2.pendingTasks &&
                    (newProps.pendingTasks++, (defaultProps2.next = newProps)))
                : ((props = task.keyPath),
                  (task.keyPath = keyPath),
                  renderNodeDestructive(request, task, type, -1),
                  (task.keyPath = props));
            }
            return;
          case REACT_VIEW_TRANSITION_TYPE:
          case REACT_SCOPE_TYPE:
            throw Error(
              "ReactDOMServer does not yet support scope components.",
            );
          case REACT_SUSPENSE_TYPE:
            a: if (null !== task.replay) {
              type = task.keyPath;
              newProps = task.formatContext;
              defaultProps2 = task.row;
              task.keyPath = keyPath;
              task.formatContext = getSuspenseContentFormatContext(
                request.resumableState,
                newProps,
              );
              task.row = null;
              keyPath = props.children;
              try {
                renderNode(request, task, keyPath, -1);
              } finally {
                ((task.keyPath = type),
                  (task.formatContext = newProps),
                  (task.row = defaultProps2));
              }
            } else {
              type = task.keyPath;
              ref = task.formatContext;
              var prevRow = task.row;
              propName$44 = task.blockedBoundary;
              propName = task.blockedPreamble;
              var parentHoistableState = task.hoistableState,
                parentSegment = task.blockedSegment,
                fallback = props.fallback;
              props = props.children;
              var fallbackAbortSet = /* @__PURE__ */ new Set();
              var newBoundary =
                2 > task.formatContext.insertionMode
                  ? createSuspenseBoundary(
                      request,
                      task.row,
                      fallbackAbortSet,
                      createPreambleState(),
                      createPreambleState(),
                    )
                  : createSuspenseBoundary(
                      request,
                      task.row,
                      fallbackAbortSet,
                      null,
                      null,
                    );
              null !== request.trackedPostpones &&
                (newBoundary.trackedContentKeyPath = keyPath);
              var boundarySegment = createPendingSegment(
                request,
                parentSegment.chunks.length,
                newBoundary,
                task.formatContext,
                false,
                false,
              );
              parentSegment.children.push(boundarySegment);
              parentSegment.lastPushedText = false;
              var contentRootSegment = createPendingSegment(
                request,
                0,
                null,
                task.formatContext,
                false,
                false,
              );
              contentRootSegment.parentFlushed = true;
              if (null !== request.trackedPostpones) {
                newProps = task.componentStack;
                defaultProps2 = [keyPath[0], "Suspense Fallback", keyPath[2]];
                initialState = [defaultProps2[1], defaultProps2[2], [], null];
                request.trackedPostpones.workingMap.set(
                  defaultProps2,
                  initialState,
                );
                newBoundary.trackedFallbackNode = initialState;
                task.blockedSegment = boundarySegment;
                task.blockedPreamble = newBoundary.fallbackPreamble;
                task.keyPath = defaultProps2;
                task.formatContext = getSuspenseFallbackFormatContext(
                  request.resumableState,
                  ref,
                );
                task.componentStack =
                  replaceSuspenseComponentStackWithSuspenseFallbackStack(
                    newProps,
                  );
                boundarySegment.status = 6;
                try {
                  (renderNode(request, task, fallback, -1),
                    boundarySegment.lastPushedText &&
                      boundarySegment.textEmbedded &&
                      boundarySegment.chunks.push(textSeparator),
                    (boundarySegment.status = 1),
                    finishedSegment(request, propName$44, boundarySegment));
                } catch (thrownValue) {
                  throw (
                    (boundarySegment.status = 12 === request.status ? 3 : 4),
                    thrownValue
                  );
                } finally {
                  ((task.blockedSegment = parentSegment),
                    (task.blockedPreamble = propName),
                    (task.keyPath = type),
                    (task.formatContext = ref));
                }
                task = createRenderTask(
                  request,
                  null,
                  props,
                  -1,
                  newBoundary,
                  contentRootSegment,
                  newBoundary.contentPreamble,
                  newBoundary.contentState,
                  task.abortSet,
                  keyPath,
                  getSuspenseContentFormatContext(
                    request.resumableState,
                    task.formatContext,
                  ),
                  task.context,
                  task.treeContext,
                  null,
                  newProps,
                );
                pushComponentStack(task);
                request.pingedTasks.push(task);
              } else {
                task.blockedBoundary = newBoundary;
                task.blockedPreamble = newBoundary.contentPreamble;
                task.hoistableState = newBoundary.contentState;
                task.blockedSegment = contentRootSegment;
                task.keyPath = keyPath;
                task.formatContext = getSuspenseContentFormatContext(
                  request.resumableState,
                  ref,
                );
                task.row = null;
                contentRootSegment.status = 6;
                try {
                  if (
                    (renderNode(request, task, props, -1),
                    contentRootSegment.lastPushedText &&
                      contentRootSegment.textEmbedded &&
                      contentRootSegment.chunks.push(textSeparator),
                    (contentRootSegment.status = 1),
                    finishedSegment(request, newBoundary, contentRootSegment),
                    queueCompletedSegment(newBoundary, contentRootSegment),
                    0 === newBoundary.pendingTasks && 0 === newBoundary.status)
                  ) {
                    if (
                      ((newBoundary.status = 1),
                      !isEligibleForOutlining(request, newBoundary))
                    ) {
                      null !== prevRow &&
                        0 === --prevRow.pendingTasks &&
                        finishSuspenseListRow(request, prevRow);
                      0 === request.pendingRootTasks &&
                        task.blockedPreamble &&
                        preparePreamble(request);
                      break a;
                    }
                  } else
                    null !== prevRow &&
                      prevRow.together &&
                      tryToResolveTogetherRow(request, prevRow);
                } catch (thrownValue$31) {
                  ((newBoundary.status = 4),
                    12 === request.status
                      ? ((contentRootSegment.status = 3),
                        (newProps = request.fatalError))
                      : ((contentRootSegment.status = 4),
                        (newProps = thrownValue$31)),
                    (defaultProps2 = getThrownInfo(task.componentStack)),
                    (initialState = logRecoverableError(
                      request,
                      newProps,
                      defaultProps2,
                    )),
                    (newBoundary.errorDigest = initialState),
                    untrackBoundary(request, newBoundary));
                } finally {
                  ((task.blockedBoundary = propName$44),
                    (task.blockedPreamble = propName),
                    (task.hoistableState = parentHoistableState),
                    (task.blockedSegment = parentSegment),
                    (task.keyPath = type),
                    (task.formatContext = ref),
                    (task.row = prevRow));
                }
                task = createRenderTask(
                  request,
                  null,
                  fallback,
                  -1,
                  propName$44,
                  boundarySegment,
                  newBoundary.fallbackPreamble,
                  newBoundary.fallbackState,
                  fallbackAbortSet,
                  [keyPath[0], "Suspense Fallback", keyPath[2]],
                  getSuspenseFallbackFormatContext(
                    request.resumableState,
                    task.formatContext,
                  ),
                  task.context,
                  task.treeContext,
                  task.row,
                  replaceSuspenseComponentStackWithSuspenseFallbackStack(
                    task.componentStack,
                  ),
                );
                pushComponentStack(task);
                request.pingedTasks.push(task);
              }
            }
            return;
        }
        if ("object" === typeof type && null !== type)
          switch (type.$$typeof) {
            case REACT_FORWARD_REF_TYPE:
              if ("ref" in props)
                for (parentSegment in ((newProps = {}), props))
                  "ref" !== parentSegment &&
                    (newProps[parentSegment] = props[parentSegment]);
              else newProps = props;
              type = renderWithHooks(
                request,
                task,
                keyPath,
                type.render,
                newProps,
                ref,
              );
              finishFunctionComponent(
                request,
                task,
                keyPath,
                type,
                0 !== localIdCounter,
                actionStateCounter,
                actionStateMatchingIndex,
              );
              return;
            case REACT_MEMO_TYPE:
              renderElement(request, task, keyPath, type.type, props, ref);
              return;
            case REACT_CONTEXT_TYPE:
              defaultProps2 = props.children;
              newProps = task.keyPath;
              props = props.value;
              initialState = type._currentValue;
              type._currentValue = props;
              ref = currentActiveSnapshot;
              currentActiveSnapshot = type = {
                parent: ref,
                depth: null === ref ? 0 : ref.depth + 1,
                context: type,
                parentValue: initialState,
                value: props,
              };
              task.context = type;
              task.keyPath = keyPath;
              renderNodeDestructive(request, task, defaultProps2, -1);
              request = currentActiveSnapshot;
              if (null === request)
                throw Error(
                  "Tried to pop a Context at the root of the app. This is a bug in React.",
                );
              request.context._currentValue = request.parentValue;
              request = currentActiveSnapshot = request.parent;
              task.context = request;
              task.keyPath = newProps;
              return;
            case REACT_CONSUMER_TYPE:
              props = props.children;
              type = props(type._context._currentValue);
              props = task.keyPath;
              task.keyPath = keyPath;
              renderNodeDestructive(request, task, type, -1);
              task.keyPath = props;
              return;
            case REACT_LAZY_TYPE:
              newProps = type._init;
              type = newProps(type._payload);
              if (12 === request.status) throw null;
              renderElement(request, task, keyPath, type, props, ref);
              return;
          }
        throw Error(
          "Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) but got: " +
            ((null == type ? type : typeof type) + "."),
        );
      }
    }
    function resumeNode(request, task, segmentId, node2, childIndex) {
      var prevReplay = task.replay,
        blockedBoundary = task.blockedBoundary,
        resumedSegment = createPendingSegment(
          request,
          0,
          null,
          task.formatContext,
          false,
          false,
        );
      resumedSegment.id = segmentId;
      resumedSegment.parentFlushed = true;
      try {
        ((task.replay = null),
          (task.blockedSegment = resumedSegment),
          renderNode(request, task, node2, childIndex),
          (resumedSegment.status = 1),
          finishedSegment(request, blockedBoundary, resumedSegment),
          null === blockedBoundary
            ? (request.completedRootSegment = resumedSegment)
            : (queueCompletedSegment(blockedBoundary, resumedSegment),
              blockedBoundary.parentFlushed &&
                request.partialBoundaries.push(blockedBoundary)));
      } finally {
        ((task.replay = prevReplay), (task.blockedSegment = null));
      }
    }
    function renderNodeDestructive(request, task, node2, childIndex) {
      null !== task.replay && "number" === typeof task.replay.slots
        ? resumeNode(request, task, task.replay.slots, node2, childIndex)
        : ((task.node = node2),
          (task.childIndex = childIndex),
          (node2 = task.componentStack),
          pushComponentStack(task),
          retryNode(request, task),
          (task.componentStack = node2));
    }
    function retryNode(request, task) {
      var node2 = task.node,
        childIndex = task.childIndex;
      if (null !== node2) {
        if ("object" === typeof node2) {
          switch (node2.$$typeof) {
            case REACT_ELEMENT_TYPE:
              var type = node2.type,
                key = node2.key,
                props = node2.props;
              node2 = props.ref;
              var ref = void 0 !== node2 ? node2 : null,
                name = getComponentNameFromType(type),
                keyOrIndex =
                  null == key ? (-1 === childIndex ? 0 : childIndex) : key;
              key = [task.keyPath, name, keyOrIndex];
              if (null !== task.replay)
                a: {
                  var replay = task.replay;
                  childIndex = replay.nodes;
                  for (node2 = 0; node2 < childIndex.length; node2++) {
                    var node$jscomp$0 = childIndex[node2];
                    if (keyOrIndex === node$jscomp$0[1]) {
                      if (4 === node$jscomp$0.length) {
                        if (null !== name && name !== node$jscomp$0[0])
                          throw Error(
                            "Expected the resume to render <" +
                              node$jscomp$0[0] +
                              "> in this slot but instead it rendered <" +
                              name +
                              ">. The tree doesn't match so React will fallback to client rendering.",
                          );
                        var childNodes = node$jscomp$0[2];
                        name = node$jscomp$0[3];
                        keyOrIndex = task.node;
                        task.replay = {
                          nodes: childNodes,
                          slots: name,
                          pendingTasks: 1,
                        };
                        try {
                          renderElement(request, task, key, type, props, ref);
                          if (
                            1 === task.replay.pendingTasks &&
                            0 < task.replay.nodes.length
                          )
                            throw Error(
                              "Couldn't find all resumable slots by key/index during replaying. The tree doesn't match so React will fallback to client rendering.",
                            );
                          task.replay.pendingTasks--;
                        } catch (x) {
                          if (
                            "object" === typeof x &&
                            null !== x &&
                            (x === SuspenseException ||
                              "function" === typeof x.then)
                          )
                            throw (
                              task.node === keyOrIndex
                                ? (task.replay = replay)
                                : childIndex.splice(node2, 1),
                              x
                            );
                          task.replay.pendingTasks--;
                          props = getThrownInfo(task.componentStack);
                          key = request;
                          request = task.blockedBoundary;
                          type = x;
                          props = logRecoverableError(key, type, props);
                          abortRemainingReplayNodes(
                            key,
                            request,
                            childNodes,
                            name,
                            type,
                            props,
                          );
                        }
                        task.replay = replay;
                      } else {
                        if (type !== REACT_SUSPENSE_TYPE)
                          throw Error(
                            "Expected the resume to render <Suspense> in this slot but instead it rendered <" +
                              (getComponentNameFromType(type) || "Unknown") +
                              ">. The tree doesn't match so React will fallback to client rendering.",
                          );
                        b: {
                          replay = void 0;
                          type = node$jscomp$0[5];
                          ref = node$jscomp$0[2];
                          name = node$jscomp$0[3];
                          keyOrIndex =
                            null === node$jscomp$0[4]
                              ? []
                              : node$jscomp$0[4][2];
                          node$jscomp$0 =
                            null === node$jscomp$0[4]
                              ? null
                              : node$jscomp$0[4][3];
                          var prevKeyPath = task.keyPath,
                            prevContext = task.formatContext,
                            prevRow = task.row,
                            previousReplaySet = task.replay,
                            parentBoundary = task.blockedBoundary,
                            parentHoistableState = task.hoistableState,
                            content = props.children,
                            fallback = props.fallback,
                            fallbackAbortSet = /* @__PURE__ */ new Set();
                          props =
                            2 > task.formatContext.insertionMode
                              ? createSuspenseBoundary(
                                  request,
                                  task.row,
                                  fallbackAbortSet,
                                  createPreambleState(),
                                  createPreambleState(),
                                )
                              : createSuspenseBoundary(
                                  request,
                                  task.row,
                                  fallbackAbortSet,
                                  null,
                                  null,
                                );
                          props.parentFlushed = true;
                          props.rootSegmentID = type;
                          task.blockedBoundary = props;
                          task.hoistableState = props.contentState;
                          task.keyPath = key;
                          task.formatContext = getSuspenseContentFormatContext(
                            request.resumableState,
                            prevContext,
                          );
                          task.row = null;
                          task.replay = {
                            nodes: ref,
                            slots: name,
                            pendingTasks: 1,
                          };
                          try {
                            renderNode(request, task, content, -1);
                            if (
                              1 === task.replay.pendingTasks &&
                              0 < task.replay.nodes.length
                            )
                              throw Error(
                                "Couldn't find all resumable slots by key/index during replaying. The tree doesn't match so React will fallback to client rendering.",
                              );
                            task.replay.pendingTasks--;
                            if (
                              0 === props.pendingTasks &&
                              0 === props.status
                            ) {
                              props.status = 1;
                              request.completedBoundaries.push(props);
                              break b;
                            }
                          } catch (error) {
                            ((props.status = 4),
                              (childNodes = getThrownInfo(task.componentStack)),
                              (replay = logRecoverableError(
                                request,
                                error,
                                childNodes,
                              )),
                              (props.errorDigest = replay),
                              task.replay.pendingTasks--,
                              request.clientRenderedBoundaries.push(props));
                          } finally {
                            ((task.blockedBoundary = parentBoundary),
                              (task.hoistableState = parentHoistableState),
                              (task.replay = previousReplaySet),
                              (task.keyPath = prevKeyPath),
                              (task.formatContext = prevContext),
                              (task.row = prevRow));
                          }
                          childNodes = createReplayTask(
                            request,
                            null,
                            {
                              nodes: keyOrIndex,
                              slots: node$jscomp$0,
                              pendingTasks: 0,
                            },
                            fallback,
                            -1,
                            parentBoundary,
                            props.fallbackState,
                            fallbackAbortSet,
                            [key[0], "Suspense Fallback", key[2]],
                            getSuspenseFallbackFormatContext(
                              request.resumableState,
                              task.formatContext,
                            ),
                            task.context,
                            task.treeContext,
                            task.row,
                            replaceSuspenseComponentStackWithSuspenseFallbackStack(
                              task.componentStack,
                            ),
                          );
                          pushComponentStack(childNodes);
                          request.pingedTasks.push(childNodes);
                        }
                      }
                      childIndex.splice(node2, 1);
                      break a;
                    }
                  }
                }
              else renderElement(request, task, key, type, props, ref);
              return;
            case REACT_PORTAL_TYPE:
              throw Error(
                "Portals are not currently supported by the server renderer. Render them conditionally so that they only appear on the client render.",
              );
            case REACT_LAZY_TYPE:
              childNodes = node2._init;
              node2 = childNodes(node2._payload);
              if (12 === request.status) throw null;
              renderNodeDestructive(request, task, node2, childIndex);
              return;
          }
          if (isArrayImpl(node2)) {
            renderChildrenArray(request, task, node2, childIndex);
            return;
          }
          if ((childNodes = getIteratorFn(node2))) {
            if ((childNodes = childNodes.call(node2))) {
              node2 = childNodes.next();
              if (!node2.done) {
                props = [];
                do (props.push(node2.value), (node2 = childNodes.next()));
                while (!node2.done);
                renderChildrenArray(request, task, props, childIndex);
              }
              return;
            }
          }
          if ("function" === typeof node2.then)
            return (
              (task.thenableState = null),
              renderNodeDestructive(
                request,
                task,
                unwrapThenable(node2),
                childIndex,
              )
            );
          if (node2.$$typeof === REACT_CONTEXT_TYPE)
            return renderNodeDestructive(
              request,
              task,
              node2._currentValue,
              childIndex,
            );
          childIndex = Object.prototype.toString.call(node2);
          throw Error(
            "Objects are not valid as a React child (found: " +
              ("[object Object]" === childIndex
                ? "object with keys {" + Object.keys(node2).join(", ") + "}"
                : childIndex) +
              "). If you meant to render a collection of children, use an array instead.",
          );
        }
        if ("string" === typeof node2)
          ((childIndex = task.blockedSegment),
            null !== childIndex &&
              (childIndex.lastPushedText = pushTextInstance(
                childIndex.chunks,
                node2,
                request.renderState,
                childIndex.lastPushedText,
              )));
        else if ("number" === typeof node2 || "bigint" === typeof node2)
          ((childIndex = task.blockedSegment),
            null !== childIndex &&
              (childIndex.lastPushedText = pushTextInstance(
                childIndex.chunks,
                "" + node2,
                request.renderState,
                childIndex.lastPushedText,
              )));
      }
    }
    function renderChildrenArray(request, task, children, childIndex) {
      var prevKeyPath = task.keyPath;
      if (
        -1 !== childIndex &&
        ((task.keyPath = [task.keyPath, "Fragment", childIndex]),
        null !== task.replay)
      ) {
        for (
          var replay = task.replay, replayNodes = replay.nodes, j = 0;
          j < replayNodes.length;
          j++
        ) {
          var node2 = replayNodes[j];
          if (node2[1] === childIndex) {
            childIndex = node2[2];
            node2 = node2[3];
            task.replay = { nodes: childIndex, slots: node2, pendingTasks: 1 };
            try {
              renderChildrenArray(request, task, children, -1);
              if (
                1 === task.replay.pendingTasks &&
                0 < task.replay.nodes.length
              )
                throw Error(
                  "Couldn't find all resumable slots by key/index during replaying. The tree doesn't match so React will fallback to client rendering.",
                );
              task.replay.pendingTasks--;
            } catch (x) {
              if (
                "object" === typeof x &&
                null !== x &&
                (x === SuspenseException || "function" === typeof x.then)
              )
                throw x;
              task.replay.pendingTasks--;
              children = getThrownInfo(task.componentStack);
              var boundary = task.blockedBoundary,
                error = x;
              children = logRecoverableError(request, error, children);
              abortRemainingReplayNodes(
                request,
                boundary,
                childIndex,
                node2,
                error,
                children,
              );
            }
            task.replay = replay;
            replayNodes.splice(j, 1);
            break;
          }
        }
        task.keyPath = prevKeyPath;
        return;
      }
      replay = task.treeContext;
      replayNodes = children.length;
      if (
        null !== task.replay &&
        ((j = task.replay.slots), null !== j && "object" === typeof j)
      ) {
        for (childIndex = 0; childIndex < replayNodes; childIndex++)
          ((node2 = children[childIndex]),
            (task.treeContext = pushTreeContext(
              replay,
              replayNodes,
              childIndex,
            )),
            (boundary = j[childIndex]),
            "number" === typeof boundary
              ? (resumeNode(request, task, boundary, node2, childIndex),
                delete j[childIndex])
              : renderNode(request, task, node2, childIndex));
        task.treeContext = replay;
        task.keyPath = prevKeyPath;
        return;
      }
      for (j = 0; j < replayNodes; j++)
        ((childIndex = children[j]),
          (task.treeContext = pushTreeContext(replay, replayNodes, j)),
          renderNode(request, task, childIndex, j));
      task.treeContext = replay;
      task.keyPath = prevKeyPath;
    }
    function trackPostponedBoundary(request, trackedPostpones, boundary) {
      boundary.status = 5;
      boundary.rootSegmentID = request.nextSegmentId++;
      request = boundary.trackedContentKeyPath;
      if (null === request)
        throw Error(
          "It should not be possible to postpone at the root. This is a bug in React.",
        );
      var fallbackReplayNode = boundary.trackedFallbackNode,
        children = [],
        boundaryNode = trackedPostpones.workingMap.get(request);
      if (void 0 === boundaryNode)
        return (
          (boundary = [
            request[1],
            request[2],
            children,
            null,
            fallbackReplayNode,
            boundary.rootSegmentID,
          ]),
          trackedPostpones.workingMap.set(request, boundary),
          addToReplayParent(boundary, request[0], trackedPostpones),
          boundary
        );
      boundaryNode[4] = fallbackReplayNode;
      boundaryNode[5] = boundary.rootSegmentID;
      return boundaryNode;
    }
    function trackPostpone(request, trackedPostpones, task, segment) {
      segment.status = 5;
      var keyPath = task.keyPath,
        boundary = task.blockedBoundary;
      if (null === boundary)
        ((segment.id = request.nextSegmentId++),
          (trackedPostpones.rootSlots = segment.id),
          null !== request.completedRootSegment &&
            (request.completedRootSegment.status = 5));
      else {
        if (null !== boundary && 0 === boundary.status) {
          var boundaryNode = trackPostponedBoundary(
            request,
            trackedPostpones,
            boundary,
          );
          if (
            boundary.trackedContentKeyPath === keyPath &&
            -1 === task.childIndex
          ) {
            -1 === segment.id &&
              (segment.id = segment.parentFlushed
                ? boundary.rootSegmentID
                : request.nextSegmentId++);
            boundaryNode[3] = segment.id;
            return;
          }
        }
        -1 === segment.id &&
          (segment.id =
            segment.parentFlushed && null !== boundary
              ? boundary.rootSegmentID
              : request.nextSegmentId++);
        if (-1 === task.childIndex)
          null === keyPath
            ? (trackedPostpones.rootSlots = segment.id)
            : ((task = trackedPostpones.workingMap.get(keyPath)),
              void 0 === task
                ? ((task = [keyPath[1], keyPath[2], [], segment.id]),
                  addToReplayParent(task, keyPath[0], trackedPostpones))
                : (task[3] = segment.id));
        else {
          if (null === keyPath)
            if (((request = trackedPostpones.rootSlots), null === request))
              request = trackedPostpones.rootSlots = {};
            else {
              if ("number" === typeof request)
                throw Error(
                  "It should not be possible to postpone both at the root of an element as well as a slot below. This is a bug in React.",
                );
            }
          else if (
            ((boundary = trackedPostpones.workingMap),
            (boundaryNode = boundary.get(keyPath)),
            void 0 === boundaryNode)
          )
            ((request = {}),
              (boundaryNode = [keyPath[1], keyPath[2], [], request]),
              boundary.set(keyPath, boundaryNode),
              addToReplayParent(boundaryNode, keyPath[0], trackedPostpones));
          else if (((request = boundaryNode[3]), null === request))
            request = boundaryNode[3] = {};
          else if ("number" === typeof request)
            throw Error(
              "It should not be possible to postpone both at the root of an element as well as a slot below. This is a bug in React.",
            );
          request[task.childIndex] = segment.id;
        }
      }
    }
    function untrackBoundary(request, boundary) {
      request = request.trackedPostpones;
      null !== request &&
        ((boundary = boundary.trackedContentKeyPath),
        null !== boundary &&
          ((boundary = request.workingMap.get(boundary)),
          void 0 !== boundary &&
            ((boundary.length = 4), (boundary[2] = []), (boundary[3] = null))));
    }
    function spawnNewSuspendedReplayTask(request, task, thenableState2) {
      return createReplayTask(
        request,
        thenableState2,
        task.replay,
        task.node,
        task.childIndex,
        task.blockedBoundary,
        task.hoistableState,
        task.abortSet,
        task.keyPath,
        task.formatContext,
        task.context,
        task.treeContext,
        task.row,
        task.componentStack,
      );
    }
    function spawnNewSuspendedRenderTask(request, task, thenableState2) {
      var segment = task.blockedSegment,
        newSegment = createPendingSegment(
          request,
          segment.chunks.length,
          null,
          task.formatContext,
          segment.lastPushedText,
          true,
        );
      segment.children.push(newSegment);
      segment.lastPushedText = false;
      return createRenderTask(
        request,
        thenableState2,
        task.node,
        task.childIndex,
        task.blockedBoundary,
        newSegment,
        task.blockedPreamble,
        task.hoistableState,
        task.abortSet,
        task.keyPath,
        task.formatContext,
        task.context,
        task.treeContext,
        task.row,
        task.componentStack,
      );
    }
    function renderNode(request, task, node2, childIndex) {
      var previousFormatContext = task.formatContext,
        previousContext = task.context,
        previousKeyPath = task.keyPath,
        previousTreeContext = task.treeContext,
        previousComponentStack = task.componentStack,
        segment = task.blockedSegment;
      if (null === segment) {
        segment = task.replay;
        try {
          return renderNodeDestructive(request, task, node2, childIndex);
        } catch (thrownValue) {
          if (
            (resetHooksState(),
            (node2 =
              thrownValue === SuspenseException
                ? getSuspendedThenable()
                : thrownValue),
            12 !== request.status &&
              "object" === typeof node2 &&
              null !== node2)
          ) {
            if ("function" === typeof node2.then) {
              childIndex =
                thrownValue === SuspenseException
                  ? getThenableStateAfterSuspending()
                  : null;
              request = spawnNewSuspendedReplayTask(
                request,
                task,
                childIndex,
              ).ping;
              node2.then(request, request);
              task.formatContext = previousFormatContext;
              task.context = previousContext;
              task.keyPath = previousKeyPath;
              task.treeContext = previousTreeContext;
              task.componentStack = previousComponentStack;
              task.replay = segment;
              switchContext(previousContext);
              return;
            }
            if ("Maximum call stack size exceeded" === node2.message) {
              node2 =
                thrownValue === SuspenseException
                  ? getThenableStateAfterSuspending()
                  : null;
              node2 = spawnNewSuspendedReplayTask(request, task, node2);
              request.pingedTasks.push(node2);
              task.formatContext = previousFormatContext;
              task.context = previousContext;
              task.keyPath = previousKeyPath;
              task.treeContext = previousTreeContext;
              task.componentStack = previousComponentStack;
              task.replay = segment;
              switchContext(previousContext);
              return;
            }
          }
        }
      } else {
        var childrenLength = segment.children.length,
          chunkLength = segment.chunks.length;
        try {
          return renderNodeDestructive(request, task, node2, childIndex);
        } catch (thrownValue$63) {
          if (
            (resetHooksState(),
            (segment.children.length = childrenLength),
            (segment.chunks.length = chunkLength),
            (node2 =
              thrownValue$63 === SuspenseException
                ? getSuspendedThenable()
                : thrownValue$63),
            12 !== request.status &&
              "object" === typeof node2 &&
              null !== node2)
          ) {
            if ("function" === typeof node2.then) {
              segment = node2;
              node2 =
                thrownValue$63 === SuspenseException
                  ? getThenableStateAfterSuspending()
                  : null;
              request = spawnNewSuspendedRenderTask(request, task, node2).ping;
              segment.then(request, request);
              task.formatContext = previousFormatContext;
              task.context = previousContext;
              task.keyPath = previousKeyPath;
              task.treeContext = previousTreeContext;
              task.componentStack = previousComponentStack;
              switchContext(previousContext);
              return;
            }
            if ("Maximum call stack size exceeded" === node2.message) {
              segment =
                thrownValue$63 === SuspenseException
                  ? getThenableStateAfterSuspending()
                  : null;
              segment = spawnNewSuspendedRenderTask(request, task, segment);
              request.pingedTasks.push(segment);
              task.formatContext = previousFormatContext;
              task.context = previousContext;
              task.keyPath = previousKeyPath;
              task.treeContext = previousTreeContext;
              task.componentStack = previousComponentStack;
              switchContext(previousContext);
              return;
            }
          }
        }
      }
      task.formatContext = previousFormatContext;
      task.context = previousContext;
      task.keyPath = previousKeyPath;
      task.treeContext = previousTreeContext;
      switchContext(previousContext);
      throw node2;
    }
    function abortTaskSoft(task) {
      var boundary = task.blockedBoundary,
        segment = task.blockedSegment;
      null !== segment &&
        ((segment.status = 3), finishedTask(this, boundary, task.row, segment));
    }
    function abortRemainingReplayNodes(
      request$jscomp$0,
      boundary,
      nodes,
      slots,
      error,
      errorDigest$jscomp$0,
    ) {
      for (var i = 0; i < nodes.length; i++) {
        var node2 = nodes[i];
        if (4 === node2.length)
          abortRemainingReplayNodes(
            request$jscomp$0,
            boundary,
            node2[2],
            node2[3],
            error,
            errorDigest$jscomp$0,
          );
        else {
          node2 = node2[5];
          var request = request$jscomp$0,
            errorDigest = errorDigest$jscomp$0,
            resumedBoundary = createSuspenseBoundary(
              request,
              null,
              /* @__PURE__ */ new Set(),
              null,
              null,
            );
          resumedBoundary.parentFlushed = true;
          resumedBoundary.rootSegmentID = node2;
          resumedBoundary.status = 4;
          resumedBoundary.errorDigest = errorDigest;
          resumedBoundary.parentFlushed &&
            request.clientRenderedBoundaries.push(resumedBoundary);
        }
      }
      nodes.length = 0;
      if (null !== slots) {
        if (null === boundary)
          throw Error(
            "We should not have any resumable nodes in the shell. This is a bug in React.",
          );
        4 !== boundary.status &&
          ((boundary.status = 4),
          (boundary.errorDigest = errorDigest$jscomp$0),
          boundary.parentFlushed &&
            request$jscomp$0.clientRenderedBoundaries.push(boundary));
        if ("object" === typeof slots)
          for (var index in slots) delete slots[index];
      }
    }
    function abortTask(task, request, error) {
      var boundary = task.blockedBoundary,
        segment = task.blockedSegment;
      if (null !== segment) {
        if (6 === segment.status) return;
        segment.status = 3;
      }
      var errorInfo = getThrownInfo(task.componentStack);
      if (null === boundary) {
        if (13 !== request.status && 14 !== request.status) {
          boundary = task.replay;
          if (null === boundary) {
            null !== request.trackedPostpones && null !== segment
              ? ((boundary = request.trackedPostpones),
                logRecoverableError(request, error, errorInfo),
                trackPostpone(request, boundary, task, segment),
                finishedTask(request, null, task.row, segment))
              : (logRecoverableError(request, error, errorInfo),
                fatalError(request, error));
            return;
          }
          boundary.pendingTasks--;
          0 === boundary.pendingTasks &&
            0 < boundary.nodes.length &&
            ((segment = logRecoverableError(request, error, errorInfo)),
            abortRemainingReplayNodes(
              request,
              null,
              boundary.nodes,
              boundary.slots,
              error,
              segment,
            ));
          request.pendingRootTasks--;
          0 === request.pendingRootTasks && completeShell(request);
        }
      } else {
        var trackedPostpones$64 = request.trackedPostpones;
        if (4 !== boundary.status) {
          if (null !== trackedPostpones$64 && null !== segment)
            return (
              logRecoverableError(request, error, errorInfo),
              trackPostpone(request, trackedPostpones$64, task, segment),
              boundary.fallbackAbortableTasks.forEach(function (fallbackTask) {
                return abortTask(fallbackTask, request, error);
              }),
              boundary.fallbackAbortableTasks.clear(),
              finishedTask(request, boundary, task.row, segment)
            );
          boundary.status = 4;
          segment = logRecoverableError(request, error, errorInfo);
          boundary.status = 4;
          boundary.errorDigest = segment;
          untrackBoundary(request, boundary);
          boundary.parentFlushed &&
            request.clientRenderedBoundaries.push(boundary);
        }
        boundary.pendingTasks--;
        segment = boundary.row;
        null !== segment &&
          0 === --segment.pendingTasks &&
          finishSuspenseListRow(request, segment);
        boundary.fallbackAbortableTasks.forEach(function (fallbackTask) {
          return abortTask(fallbackTask, request, error);
        });
        boundary.fallbackAbortableTasks.clear();
      }
      task = task.row;
      null !== task &&
        0 === --task.pendingTasks &&
        finishSuspenseListRow(request, task);
      request.allPendingTasks--;
      0 === request.allPendingTasks && completeAll(request);
    }
    function safelyEmitEarlyPreloads(request, shellComplete) {
      try {
        var renderState = request.renderState,
          onHeaders = renderState.onHeaders;
        if (onHeaders) {
          var headers = renderState.headers;
          if (headers) {
            renderState.headers = null;
            var linkHeader = headers.preconnects;
            headers.fontPreloads &&
              (linkHeader && (linkHeader += ", "),
              (linkHeader += headers.fontPreloads));
            headers.highImagePreloads &&
              (linkHeader && (linkHeader += ", "),
              (linkHeader += headers.highImagePreloads));
            if (!shellComplete) {
              var queueIter = renderState.styles.values(),
                queueStep = queueIter.next();
              b: for (
                ;
                0 < headers.remainingCapacity && !queueStep.done;
                queueStep = queueIter.next()
              )
                for (
                  var sheetIter = queueStep.value.sheets.values(),
                    sheetStep = sheetIter.next();
                  0 < headers.remainingCapacity && !sheetStep.done;
                  sheetStep = sheetIter.next()
                ) {
                  var sheet = sheetStep.value,
                    props = sheet.props,
                    key = props.href,
                    props$jscomp$0 = sheet.props,
                    header = getPreloadAsHeader(props$jscomp$0.href, "style", {
                      crossOrigin: props$jscomp$0.crossOrigin,
                      integrity: props$jscomp$0.integrity,
                      nonce: props$jscomp$0.nonce,
                      type: props$jscomp$0.type,
                      fetchPriority: props$jscomp$0.fetchPriority,
                      referrerPolicy: props$jscomp$0.referrerPolicy,
                      media: props$jscomp$0.media,
                    });
                  if (0 <= (headers.remainingCapacity -= header.length + 2))
                    ((renderState.resets.style[key] = PRELOAD_NO_CREDS),
                      linkHeader && (linkHeader += ", "),
                      (linkHeader += header),
                      (renderState.resets.style[key] =
                        "string" === typeof props.crossOrigin ||
                        "string" === typeof props.integrity
                          ? [props.crossOrigin, props.integrity]
                          : PRELOAD_NO_CREDS));
                  else break b;
                }
            }
            linkHeader ? onHeaders({ Link: linkHeader }) : onHeaders({});
          }
        }
      } catch (error) {
        logRecoverableError(request, error, {});
      }
    }
    function completeShell(request) {
      null === request.trackedPostpones &&
        safelyEmitEarlyPreloads(request, true);
      null === request.trackedPostpones && preparePreamble(request);
      request.onShellError = noop5;
      request = request.onShellReady;
      request();
    }
    function completeAll(request) {
      safelyEmitEarlyPreloads(
        request,
        null === request.trackedPostpones
          ? true
          : null === request.completedRootSegment ||
              5 !== request.completedRootSegment.status,
      );
      preparePreamble(request);
      request = request.onAllReady;
      request();
    }
    function queueCompletedSegment(boundary, segment) {
      if (
        0 === segment.chunks.length &&
        1 === segment.children.length &&
        null === segment.children[0].boundary &&
        -1 === segment.children[0].id
      ) {
        var childSegment = segment.children[0];
        childSegment.id = segment.id;
        childSegment.parentFlushed = true;
        (1 !== childSegment.status &&
          3 !== childSegment.status &&
          4 !== childSegment.status) ||
          queueCompletedSegment(boundary, childSegment);
      } else boundary.completedSegments.push(segment);
    }
    function finishedSegment(request, boundary, segment) {
      if (null !== byteLengthOfChunk) {
        segment = segment.chunks;
        for (var segmentByteSize = 0, i = 0; i < segment.length; i++)
          segmentByteSize += byteLengthOfChunk(segment[i]);
        null === boundary
          ? (request.byteSize += segmentByteSize)
          : (boundary.byteSize += segmentByteSize);
      }
    }
    function finishedTask(request, boundary, row, segment) {
      null !== row &&
        (0 === --row.pendingTasks
          ? finishSuspenseListRow(request, row)
          : row.together && tryToResolveTogetherRow(request, row));
      request.allPendingTasks--;
      if (null === boundary) {
        if (null !== segment && segment.parentFlushed) {
          if (null !== request.completedRootSegment)
            throw Error(
              "There can only be one root segment. This is a bug in React.",
            );
          request.completedRootSegment = segment;
        }
        request.pendingRootTasks--;
        0 === request.pendingRootTasks && completeShell(request);
      } else if ((boundary.pendingTasks--, 4 !== boundary.status))
        if (0 === boundary.pendingTasks)
          if (
            (0 === boundary.status && (boundary.status = 1),
            null !== segment &&
              segment.parentFlushed &&
              (1 === segment.status || 3 === segment.status) &&
              queueCompletedSegment(boundary, segment),
            boundary.parentFlushed &&
              request.completedBoundaries.push(boundary),
            1 === boundary.status)
          )
            ((row = boundary.row),
              null !== row &&
                hoistHoistables(row.hoistables, boundary.contentState),
              isEligibleForOutlining(request, boundary) ||
                (boundary.fallbackAbortableTasks.forEach(
                  abortTaskSoft,
                  request,
                ),
                boundary.fallbackAbortableTasks.clear(),
                null !== row &&
                  0 === --row.pendingTasks &&
                  finishSuspenseListRow(request, row)),
              0 === request.pendingRootTasks &&
                null === request.trackedPostpones &&
                null !== boundary.contentPreamble &&
                preparePreamble(request));
          else {
            if (
              5 === boundary.status &&
              ((boundary = boundary.row), null !== boundary)
            ) {
              if (null !== request.trackedPostpones) {
                row = request.trackedPostpones;
                var postponedRow = boundary.next;
                if (
                  null !== postponedRow &&
                  ((segment = postponedRow.boundaries), null !== segment)
                )
                  for (
                    postponedRow.boundaries = null, postponedRow = 0;
                    postponedRow < segment.length;
                    postponedRow++
                  ) {
                    var postponedBoundary = segment[postponedRow];
                    trackPostponedBoundary(request, row, postponedBoundary);
                    finishedTask(request, postponedBoundary, null, null);
                  }
              }
              0 === --boundary.pendingTasks &&
                finishSuspenseListRow(request, boundary);
            }
          }
        else
          (null === segment ||
            !segment.parentFlushed ||
            (1 !== segment.status && 3 !== segment.status) ||
            (queueCompletedSegment(boundary, segment),
            1 === boundary.completedSegments.length &&
              boundary.parentFlushed &&
              request.partialBoundaries.push(boundary)),
            (boundary = boundary.row),
            null !== boundary &&
              boundary.together &&
              tryToResolveTogetherRow(request, boundary));
      0 === request.allPendingTasks && completeAll(request);
    }
    function performWork(request$jscomp$2) {
      if (14 !== request$jscomp$2.status && 13 !== request$jscomp$2.status) {
        var prevContext = currentActiveSnapshot,
          prevDispatcher = ReactSharedInternals.H;
        ReactSharedInternals.H = HooksDispatcher;
        var prevAsyncDispatcher = ReactSharedInternals.A;
        ReactSharedInternals.A = DefaultAsyncDispatcher;
        var prevRequest = currentRequest;
        currentRequest = request$jscomp$2;
        var prevResumableState = currentResumableState;
        currentResumableState = request$jscomp$2.resumableState;
        try {
          var pingedTasks = request$jscomp$2.pingedTasks,
            i;
          for (i = 0; i < pingedTasks.length; i++) {
            var task = pingedTasks[i],
              request = request$jscomp$2,
              segment = task.blockedSegment;
            if (null === segment) {
              var request$jscomp$0 = request;
              if (0 !== task.replay.pendingTasks) {
                switchContext(task.context);
                try {
                  "number" === typeof task.replay.slots
                    ? resumeNode(
                        request$jscomp$0,
                        task,
                        task.replay.slots,
                        task.node,
                        task.childIndex,
                      )
                    : retryNode(request$jscomp$0, task);
                  if (
                    1 === task.replay.pendingTasks &&
                    0 < task.replay.nodes.length
                  )
                    throw Error(
                      "Couldn't find all resumable slots by key/index during replaying. The tree doesn't match so React will fallback to client rendering.",
                    );
                  task.replay.pendingTasks--;
                  task.abortSet.delete(task);
                  finishedTask(
                    request$jscomp$0,
                    task.blockedBoundary,
                    task.row,
                    null,
                  );
                } catch (thrownValue) {
                  resetHooksState();
                  var x =
                    thrownValue === SuspenseException
                      ? getSuspendedThenable()
                      : thrownValue;
                  if (
                    "object" === typeof x &&
                    null !== x &&
                    "function" === typeof x.then
                  ) {
                    var ping = task.ping;
                    x.then(ping, ping);
                    task.thenableState =
                      thrownValue === SuspenseException
                        ? getThenableStateAfterSuspending()
                        : null;
                  } else {
                    task.replay.pendingTasks--;
                    task.abortSet.delete(task);
                    var errorInfo = getThrownInfo(task.componentStack);
                    request = void 0;
                    var request$jscomp$1 = request$jscomp$0,
                      boundary = task.blockedBoundary,
                      error$jscomp$0 =
                        12 === request$jscomp$0.status
                          ? request$jscomp$0.fatalError
                          : x,
                      replayNodes = task.replay.nodes,
                      resumeSlots = task.replay.slots;
                    request = logRecoverableError(
                      request$jscomp$1,
                      error$jscomp$0,
                      errorInfo,
                    );
                    abortRemainingReplayNodes(
                      request$jscomp$1,
                      boundary,
                      replayNodes,
                      resumeSlots,
                      error$jscomp$0,
                      request,
                    );
                    request$jscomp$0.pendingRootTasks--;
                    0 === request$jscomp$0.pendingRootTasks &&
                      completeShell(request$jscomp$0);
                    request$jscomp$0.allPendingTasks--;
                    0 === request$jscomp$0.allPendingTasks &&
                      completeAll(request$jscomp$0);
                  }
                } finally {
                }
              }
            } else if (
              ((request$jscomp$0 = void 0),
              (request$jscomp$1 = segment),
              0 === request$jscomp$1.status)
            ) {
              request$jscomp$1.status = 6;
              switchContext(task.context);
              var childrenLength = request$jscomp$1.children.length,
                chunkLength = request$jscomp$1.chunks.length;
              try {
                (retryNode(request, task),
                  request$jscomp$1.lastPushedText &&
                    request$jscomp$1.textEmbedded &&
                    request$jscomp$1.chunks.push(textSeparator),
                  task.abortSet.delete(task),
                  (request$jscomp$1.status = 1),
                  finishedSegment(
                    request,
                    task.blockedBoundary,
                    request$jscomp$1,
                  ),
                  finishedTask(
                    request,
                    task.blockedBoundary,
                    task.row,
                    request$jscomp$1,
                  ));
              } catch (thrownValue) {
                resetHooksState();
                request$jscomp$1.children.length = childrenLength;
                request$jscomp$1.chunks.length = chunkLength;
                var x$jscomp$0 =
                  thrownValue === SuspenseException
                    ? getSuspendedThenable()
                    : 12 === request.status
                      ? request.fatalError
                      : thrownValue;
                if (
                  12 === request.status &&
                  null !== request.trackedPostpones
                ) {
                  var trackedPostpones = request.trackedPostpones,
                    thrownInfo = getThrownInfo(task.componentStack);
                  task.abortSet.delete(task);
                  logRecoverableError(request, x$jscomp$0, thrownInfo);
                  trackPostpone(
                    request,
                    trackedPostpones,
                    task,
                    request$jscomp$1,
                  );
                  finishedTask(
                    request,
                    task.blockedBoundary,
                    task.row,
                    request$jscomp$1,
                  );
                } else if (
                  "object" === typeof x$jscomp$0 &&
                  null !== x$jscomp$0 &&
                  "function" === typeof x$jscomp$0.then
                ) {
                  request$jscomp$1.status = 0;
                  task.thenableState =
                    thrownValue === SuspenseException
                      ? getThenableStateAfterSuspending()
                      : null;
                  var ping$jscomp$0 = task.ping;
                  x$jscomp$0.then(ping$jscomp$0, ping$jscomp$0);
                } else {
                  var errorInfo$jscomp$0 = getThrownInfo(task.componentStack);
                  task.abortSet.delete(task);
                  request$jscomp$1.status = 4;
                  var boundary$jscomp$0 = task.blockedBoundary,
                    row = task.row;
                  null !== row &&
                    0 === --row.pendingTasks &&
                    finishSuspenseListRow(request, row);
                  request.allPendingTasks--;
                  request$jscomp$0 = logRecoverableError(
                    request,
                    x$jscomp$0,
                    errorInfo$jscomp$0,
                  );
                  if (null === boundary$jscomp$0)
                    fatalError(request, x$jscomp$0);
                  else if (
                    (boundary$jscomp$0.pendingTasks--,
                    4 !== boundary$jscomp$0.status)
                  ) {
                    boundary$jscomp$0.status = 4;
                    boundary$jscomp$0.errorDigest = request$jscomp$0;
                    untrackBoundary(request, boundary$jscomp$0);
                    var boundaryRow = boundary$jscomp$0.row;
                    null !== boundaryRow &&
                      0 === --boundaryRow.pendingTasks &&
                      finishSuspenseListRow(request, boundaryRow);
                    boundary$jscomp$0.parentFlushed &&
                      request.clientRenderedBoundaries.push(boundary$jscomp$0);
                    0 === request.pendingRootTasks &&
                      null === request.trackedPostpones &&
                      null !== boundary$jscomp$0.contentPreamble &&
                      preparePreamble(request);
                  }
                  0 === request.allPendingTasks && completeAll(request);
                }
              } finally {
              }
            }
          }
          pingedTasks.splice(0, i);
          null !== request$jscomp$2.destination &&
            flushCompletedQueues(
              request$jscomp$2,
              request$jscomp$2.destination,
            );
        } catch (error) {
          (logRecoverableError(request$jscomp$2, error, {}),
            fatalError(request$jscomp$2, error));
        } finally {
          ((currentResumableState = prevResumableState),
            (ReactSharedInternals.H = prevDispatcher),
            (ReactSharedInternals.A = prevAsyncDispatcher),
            prevDispatcher === HooksDispatcher && switchContext(prevContext),
            (currentRequest = prevRequest));
        }
      }
    }
    function preparePreambleFromSubtree(
      request,
      segment,
      collectedPreambleSegments,
    ) {
      segment.preambleChildren.length &&
        collectedPreambleSegments.push(segment.preambleChildren);
      for (
        var pendingPreambles = false, i = 0;
        i < segment.children.length;
        i++
      )
        pendingPreambles =
          preparePreambleFromSegment(
            request,
            segment.children[i],
            collectedPreambleSegments,
          ) || pendingPreambles;
      return pendingPreambles;
    }
    function preparePreambleFromSegment(
      request,
      segment,
      collectedPreambleSegments,
    ) {
      var boundary = segment.boundary;
      if (null === boundary)
        return preparePreambleFromSubtree(
          request,
          segment,
          collectedPreambleSegments,
        );
      var preamble = boundary.contentPreamble,
        fallbackPreamble = boundary.fallbackPreamble;
      if (null === preamble || null === fallbackPreamble) return false;
      switch (boundary.status) {
        case 1:
          hoistPreambleState(request.renderState, preamble);
          request.byteSize += boundary.byteSize;
          segment = boundary.completedSegments[0];
          if (!segment)
            throw Error(
              "A previously unvisited boundary must have exactly one root segment. This is a bug in React.",
            );
          return preparePreambleFromSubtree(
            request,
            segment,
            collectedPreambleSegments,
          );
        case 5:
          if (null !== request.trackedPostpones) return true;
        case 4:
          if (1 === segment.status)
            return (
              hoistPreambleState(request.renderState, fallbackPreamble),
              preparePreambleFromSubtree(
                request,
                segment,
                collectedPreambleSegments,
              )
            );
        default:
          return true;
      }
    }
    function preparePreamble(request) {
      if (
        request.completedRootSegment &&
        null === request.completedPreambleSegments
      ) {
        var collectedPreambleSegments = [],
          originalRequestByteSize = request.byteSize,
          hasPendingPreambles = preparePreambleFromSegment(
            request,
            request.completedRootSegment,
            collectedPreambleSegments,
          ),
          preamble = request.renderState.preamble;
        false === hasPendingPreambles ||
        (preamble.headChunks && preamble.bodyChunks)
          ? (request.completedPreambleSegments = collectedPreambleSegments)
          : (request.byteSize = originalRequestByteSize);
      }
    }
    function flushSubtree(request, destination, segment, hoistableState) {
      segment.parentFlushed = true;
      switch (segment.status) {
        case 0:
          segment.id = request.nextSegmentId++;
        case 5:
          return (
            (hoistableState = segment.id),
            (segment.lastPushedText = false),
            (segment.textEmbedded = false),
            (request = request.renderState),
            writeChunk(destination, placeholder1),
            writeChunk(destination, request.placeholderPrefix),
            (request = hoistableState.toString(16)),
            writeChunk(destination, request),
            writeChunkAndReturn(destination, placeholder2)
          );
        case 1:
          segment.status = 2;
          var r2 = true,
            chunks = segment.chunks,
            chunkIdx = 0;
          segment = segment.children;
          for (var childIdx = 0; childIdx < segment.length; childIdx++) {
            for (r2 = segment[childIdx]; chunkIdx < r2.index; chunkIdx++)
              writeChunk(destination, chunks[chunkIdx]);
            r2 = flushSegment(request, destination, r2, hoistableState);
          }
          for (; chunkIdx < chunks.length - 1; chunkIdx++)
            writeChunk(destination, chunks[chunkIdx]);
          chunkIdx < chunks.length &&
            (r2 = writeChunkAndReturn(destination, chunks[chunkIdx]));
          return r2;
        case 3:
          return true;
        default:
          throw Error(
            "Aborted, errored or already flushed boundaries should not be flushed again. This is a bug in React.",
          );
      }
    }
    var flushedByteSize = 0;
    function flushSegment(request, destination, segment, hoistableState) {
      var boundary = segment.boundary;
      if (null === boundary)
        return flushSubtree(request, destination, segment, hoistableState);
      boundary.parentFlushed = true;
      if (4 === boundary.status) {
        var row = boundary.row;
        null !== row &&
          0 === --row.pendingTasks &&
          finishSuspenseListRow(request, row);
        boundary = boundary.errorDigest;
        writeChunkAndReturn(destination, startClientRenderedSuspenseBoundary);
        writeChunk(destination, clientRenderedSuspenseBoundaryError1);
        boundary &&
          (writeChunk(destination, clientRenderedSuspenseBoundaryError1A),
          writeChunk(destination, escapeTextForBrowser(boundary)),
          writeChunk(
            destination,
            clientRenderedSuspenseBoundaryErrorAttrInterstitial,
          ));
        writeChunkAndReturn(destination, clientRenderedSuspenseBoundaryError2);
        flushSubtree(request, destination, segment, hoistableState);
      } else if (1 !== boundary.status)
        (0 === boundary.status &&
          (boundary.rootSegmentID = request.nextSegmentId++),
          0 < boundary.completedSegments.length &&
            request.partialBoundaries.push(boundary),
          writeStartPendingSuspenseBoundary(
            destination,
            request.renderState,
            boundary.rootSegmentID,
          ),
          hoistableState &&
            hoistHoistables(hoistableState, boundary.fallbackState),
          flushSubtree(request, destination, segment, hoistableState));
      else if (
        !flushingPartialBoundaries &&
        isEligibleForOutlining(request, boundary) &&
        (flushedByteSize + boundary.byteSize > request.progressiveChunkSize ||
          hasSuspenseyContent(boundary.contentState))
      )
        ((boundary.rootSegmentID = request.nextSegmentId++),
          request.completedBoundaries.push(boundary),
          writeStartPendingSuspenseBoundary(
            destination,
            request.renderState,
            boundary.rootSegmentID,
          ),
          flushSubtree(request, destination, segment, hoistableState));
      else {
        flushedByteSize += boundary.byteSize;
        hoistableState &&
          hoistHoistables(hoistableState, boundary.contentState);
        segment = boundary.row;
        null !== segment &&
          isEligibleForOutlining(request, boundary) &&
          0 === --segment.pendingTasks &&
          finishSuspenseListRow(request, segment);
        writeChunkAndReturn(destination, startCompletedSuspenseBoundary);
        segment = boundary.completedSegments;
        if (1 !== segment.length)
          throw Error(
            "A previously unvisited boundary must have exactly one root segment. This is a bug in React.",
          );
        flushSegment(request, destination, segment[0], hoistableState);
      }
      return writeChunkAndReturn(destination, endSuspenseBoundary);
    }
    function flushSegmentContainer(
      request,
      destination,
      segment,
      hoistableState,
    ) {
      writeStartSegment(
        destination,
        request.renderState,
        segment.parentFormatContext,
        segment.id,
      );
      flushSegment(request, destination, segment, hoistableState);
      return writeEndSegment(destination, segment.parentFormatContext);
    }
    function flushCompletedBoundary(request, destination, boundary) {
      flushedByteSize = boundary.byteSize;
      for (
        var completedSegments = boundary.completedSegments, i = 0;
        i < completedSegments.length;
        i++
      )
        flushPartiallyCompletedSegment(
          request,
          destination,
          boundary,
          completedSegments[i],
        );
      completedSegments.length = 0;
      completedSegments = boundary.row;
      null !== completedSegments &&
        isEligibleForOutlining(request, boundary) &&
        0 === --completedSegments.pendingTasks &&
        finishSuspenseListRow(request, completedSegments);
      writeHoistablesForBoundary(
        destination,
        boundary.contentState,
        request.renderState,
      );
      completedSegments = request.resumableState;
      request = request.renderState;
      i = boundary.rootSegmentID;
      boundary = boundary.contentState;
      var requiresStyleInsertion = request.stylesToHoist;
      request.stylesToHoist = false;
      writeChunk(destination, request.startInlineScript);
      writeChunk(destination, endOfStartTag);
      requiresStyleInsertion
        ? (0 === (completedSegments.instructions & 4) &&
            ((completedSegments.instructions |= 4),
            writeChunk(destination, clientRenderScriptFunctionOnly)),
          0 === (completedSegments.instructions & 2) &&
            ((completedSegments.instructions |= 2),
            writeChunk(destination, completeBoundaryScriptFunctionOnly)),
          0 === (completedSegments.instructions & 8)
            ? ((completedSegments.instructions |= 8),
              writeChunk(
                destination,
                completeBoundaryWithStylesScript1FullPartial,
              ))
            : writeChunk(destination, completeBoundaryWithStylesScript1Partial))
        : (0 === (completedSegments.instructions & 2) &&
            ((completedSegments.instructions |= 2),
            writeChunk(destination, completeBoundaryScriptFunctionOnly)),
          writeChunk(destination, completeBoundaryScript1Partial));
      completedSegments = i.toString(16);
      writeChunk(destination, request.boundaryPrefix);
      writeChunk(destination, completedSegments);
      writeChunk(destination, completeBoundaryScript2);
      writeChunk(destination, request.segmentPrefix);
      writeChunk(destination, completedSegments);
      requiresStyleInsertion
        ? (writeChunk(destination, completeBoundaryScript3a),
          writeStyleResourceDependenciesInJS(destination, boundary))
        : writeChunk(destination, completeBoundaryScript3b);
      boundary = writeChunkAndReturn(destination, completeBoundaryScriptEnd);
      return writeBootstrap(destination, request) && boundary;
    }
    function flushPartiallyCompletedSegment(
      request,
      destination,
      boundary,
      segment,
    ) {
      if (2 === segment.status) return true;
      var hoistableState = boundary.contentState,
        segmentID = segment.id;
      if (-1 === segmentID) {
        if (-1 === (segment.id = boundary.rootSegmentID))
          throw Error(
            "A root segment ID must have been assigned by now. This is a bug in React.",
          );
        return flushSegmentContainer(
          request,
          destination,
          segment,
          hoistableState,
        );
      }
      if (segmentID === boundary.rootSegmentID)
        return flushSegmentContainer(
          request,
          destination,
          segment,
          hoistableState,
        );
      flushSegmentContainer(request, destination, segment, hoistableState);
      boundary = request.resumableState;
      request = request.renderState;
      writeChunk(destination, request.startInlineScript);
      writeChunk(destination, endOfStartTag);
      0 === (boundary.instructions & 1)
        ? ((boundary.instructions |= 1),
          writeChunk(destination, completeSegmentScript1Full))
        : writeChunk(destination, completeSegmentScript1Partial);
      writeChunk(destination, request.segmentPrefix);
      segmentID = segmentID.toString(16);
      writeChunk(destination, segmentID);
      writeChunk(destination, completeSegmentScript2);
      writeChunk(destination, request.placeholderPrefix);
      writeChunk(destination, segmentID);
      destination = writeChunkAndReturn(destination, completeSegmentScriptEnd);
      return destination;
    }
    var flushingPartialBoundaries = false;
    function flushCompletedQueues(request, destination) {
      currentView = new Uint8Array(2048);
      writtenBytes = 0;
      destinationHasCapacity$1 = true;
      try {
        if (!(0 < request.pendingRootTasks)) {
          var i,
            completedRootSegment = request.completedRootSegment;
          if (null !== completedRootSegment) {
            if (5 === completedRootSegment.status) return;
            var completedPreambleSegments = request.completedPreambleSegments;
            if (null === completedPreambleSegments) return;
            flushedByteSize = request.byteSize;
            var resumableState = request.resumableState,
              renderState = request.renderState,
              preamble = renderState.preamble,
              htmlChunks = preamble.htmlChunks,
              headChunks = preamble.headChunks,
              i$jscomp$0;
            if (htmlChunks) {
              for (i$jscomp$0 = 0; i$jscomp$0 < htmlChunks.length; i$jscomp$0++)
                writeChunk(destination, htmlChunks[i$jscomp$0]);
              if (headChunks)
                for (
                  i$jscomp$0 = 0;
                  i$jscomp$0 < headChunks.length;
                  i$jscomp$0++
                )
                  writeChunk(destination, headChunks[i$jscomp$0]);
              else
                (writeChunk(destination, startChunkForTag("head")),
                  writeChunk(destination, endOfStartTag));
            } else if (headChunks)
              for (i$jscomp$0 = 0; i$jscomp$0 < headChunks.length; i$jscomp$0++)
                writeChunk(destination, headChunks[i$jscomp$0]);
            var charsetChunks = renderState.charsetChunks;
            for (
              i$jscomp$0 = 0;
              i$jscomp$0 < charsetChunks.length;
              i$jscomp$0++
            )
              writeChunk(destination, charsetChunks[i$jscomp$0]);
            charsetChunks.length = 0;
            renderState.preconnects.forEach(flushResource, destination);
            renderState.preconnects.clear();
            var viewportChunks = renderState.viewportChunks;
            for (
              i$jscomp$0 = 0;
              i$jscomp$0 < viewportChunks.length;
              i$jscomp$0++
            )
              writeChunk(destination, viewportChunks[i$jscomp$0]);
            viewportChunks.length = 0;
            renderState.fontPreloads.forEach(flushResource, destination);
            renderState.fontPreloads.clear();
            renderState.highImagePreloads.forEach(flushResource, destination);
            renderState.highImagePreloads.clear();
            currentlyFlushingRenderState = renderState;
            renderState.styles.forEach(flushStylesInPreamble, destination);
            currentlyFlushingRenderState = null;
            var importMapChunks = renderState.importMapChunks;
            for (
              i$jscomp$0 = 0;
              i$jscomp$0 < importMapChunks.length;
              i$jscomp$0++
            )
              writeChunk(destination, importMapChunks[i$jscomp$0]);
            importMapChunks.length = 0;
            renderState.bootstrapScripts.forEach(flushResource, destination);
            renderState.scripts.forEach(flushResource, destination);
            renderState.scripts.clear();
            renderState.bulkPreloads.forEach(flushResource, destination);
            renderState.bulkPreloads.clear();
            htmlChunks || headChunks || (resumableState.instructions |= 32);
            var hoistableChunks = renderState.hoistableChunks;
            for (
              i$jscomp$0 = 0;
              i$jscomp$0 < hoistableChunks.length;
              i$jscomp$0++
            )
              writeChunk(destination, hoistableChunks[i$jscomp$0]);
            for (
              resumableState = hoistableChunks.length = 0;
              resumableState < completedPreambleSegments.length;
              resumableState++
            ) {
              var segments = completedPreambleSegments[resumableState];
              for (
                renderState = 0;
                renderState < segments.length;
                renderState++
              )
                flushSegment(request, destination, segments[renderState], null);
            }
            var preamble$jscomp$0 = request.renderState.preamble,
              headChunks$jscomp$0 = preamble$jscomp$0.headChunks;
            (preamble$jscomp$0.htmlChunks || headChunks$jscomp$0) &&
              writeChunk(destination, endChunkForTag("head"));
            var bodyChunks = preamble$jscomp$0.bodyChunks;
            if (bodyChunks)
              for (
                completedPreambleSegments = 0;
                completedPreambleSegments < bodyChunks.length;
                completedPreambleSegments++
              )
                writeChunk(destination, bodyChunks[completedPreambleSegments]);
            flushSegment(request, destination, completedRootSegment, null);
            request.completedRootSegment = null;
            var renderState$jscomp$0 = request.renderState;
            if (
              0 !== request.allPendingTasks ||
              0 !== request.clientRenderedBoundaries.length ||
              0 !== request.completedBoundaries.length ||
              (null !== request.trackedPostpones &&
                (0 !== request.trackedPostpones.rootNodes.length ||
                  null !== request.trackedPostpones.rootSlots))
            ) {
              var resumableState$jscomp$0 = request.resumableState;
              if (0 === (resumableState$jscomp$0.instructions & 64)) {
                resumableState$jscomp$0.instructions |= 64;
                writeChunk(destination, renderState$jscomp$0.startInlineScript);
                if (0 === (resumableState$jscomp$0.instructions & 32)) {
                  resumableState$jscomp$0.instructions |= 32;
                  var shellId = "_" + resumableState$jscomp$0.idPrefix + "R_";
                  writeChunk(destination, completedShellIdAttributeStart);
                  writeChunk(destination, escapeTextForBrowser(shellId));
                  writeChunk(destination, attributeEnd);
                }
                writeChunk(destination, endOfStartTag);
                writeChunk(destination, shellTimeRuntimeScript);
                writeChunkAndReturn(destination, endInlineScript);
              }
            }
            writeBootstrap(destination, renderState$jscomp$0);
          }
          var renderState$jscomp$1 = request.renderState;
          completedRootSegment = 0;
          var viewportChunks$jscomp$0 = renderState$jscomp$1.viewportChunks;
          for (
            completedRootSegment = 0;
            completedRootSegment < viewportChunks$jscomp$0.length;
            completedRootSegment++
          )
            writeChunk(
              destination,
              viewportChunks$jscomp$0[completedRootSegment],
            );
          viewportChunks$jscomp$0.length = 0;
          renderState$jscomp$1.preconnects.forEach(flushResource, destination);
          renderState$jscomp$1.preconnects.clear();
          renderState$jscomp$1.fontPreloads.forEach(flushResource, destination);
          renderState$jscomp$1.fontPreloads.clear();
          renderState$jscomp$1.highImagePreloads.forEach(
            flushResource,
            destination,
          );
          renderState$jscomp$1.highImagePreloads.clear();
          renderState$jscomp$1.styles.forEach(preloadLateStyles, destination);
          renderState$jscomp$1.scripts.forEach(flushResource, destination);
          renderState$jscomp$1.scripts.clear();
          renderState$jscomp$1.bulkPreloads.forEach(flushResource, destination);
          renderState$jscomp$1.bulkPreloads.clear();
          var hoistableChunks$jscomp$0 = renderState$jscomp$1.hoistableChunks;
          for (
            completedRootSegment = 0;
            completedRootSegment < hoistableChunks$jscomp$0.length;
            completedRootSegment++
          )
            writeChunk(
              destination,
              hoistableChunks$jscomp$0[completedRootSegment],
            );
          hoistableChunks$jscomp$0.length = 0;
          var clientRenderedBoundaries = request.clientRenderedBoundaries;
          for (i = 0; i < clientRenderedBoundaries.length; i++) {
            var boundary = clientRenderedBoundaries[i];
            renderState$jscomp$1 = destination;
            var resumableState$jscomp$1 = request.resumableState,
              renderState$jscomp$2 = request.renderState,
              id = boundary.rootSegmentID,
              errorDigest = boundary.errorDigest;
            writeChunk(
              renderState$jscomp$1,
              renderState$jscomp$2.startInlineScript,
            );
            writeChunk(renderState$jscomp$1, endOfStartTag);
            0 === (resumableState$jscomp$1.instructions & 4)
              ? ((resumableState$jscomp$1.instructions |= 4),
                writeChunk(renderState$jscomp$1, clientRenderScript1Full))
              : writeChunk(renderState$jscomp$1, clientRenderScript1Partial);
            writeChunk(
              renderState$jscomp$1,
              renderState$jscomp$2.boundaryPrefix,
            );
            writeChunk(renderState$jscomp$1, id.toString(16));
            writeChunk(renderState$jscomp$1, clientRenderScript1A);
            errorDigest &&
              (writeChunk(
                renderState$jscomp$1,
                clientRenderErrorScriptArgInterstitial,
              ),
              writeChunk(
                renderState$jscomp$1,
                escapeJSStringsForInstructionScripts(errorDigest || ""),
              ));
            var JSCompiler_inline_result = writeChunkAndReturn(
              renderState$jscomp$1,
              clientRenderScriptEnd,
            );
            if (!JSCompiler_inline_result) {
              request.destination = null;
              i++;
              clientRenderedBoundaries.splice(0, i);
              return;
            }
          }
          clientRenderedBoundaries.splice(0, i);
          var completedBoundaries = request.completedBoundaries;
          for (i = 0; i < completedBoundaries.length; i++)
            if (
              !flushCompletedBoundary(
                request,
                destination,
                completedBoundaries[i],
              )
            ) {
              request.destination = null;
              i++;
              completedBoundaries.splice(0, i);
              return;
            }
          completedBoundaries.splice(0, i);
          completeWriting(destination);
          currentView = new Uint8Array(2048);
          writtenBytes = 0;
          flushingPartialBoundaries = destinationHasCapacity$1 = true;
          var partialBoundaries = request.partialBoundaries;
          for (i = 0; i < partialBoundaries.length; i++) {
            var boundary$70 = partialBoundaries[i];
            a: {
              clientRenderedBoundaries = request;
              boundary = destination;
              flushedByteSize = boundary$70.byteSize;
              var completedSegments = boundary$70.completedSegments;
              for (
                JSCompiler_inline_result = 0;
                JSCompiler_inline_result < completedSegments.length;
                JSCompiler_inline_result++
              )
                if (
                  !flushPartiallyCompletedSegment(
                    clientRenderedBoundaries,
                    boundary,
                    boundary$70,
                    completedSegments[JSCompiler_inline_result],
                  )
                ) {
                  JSCompiler_inline_result++;
                  completedSegments.splice(0, JSCompiler_inline_result);
                  var JSCompiler_inline_result$jscomp$0 = false;
                  break a;
                }
              completedSegments.splice(0, JSCompiler_inline_result);
              var row = boundary$70.row;
              null !== row &&
                row.together &&
                1 === boundary$70.pendingTasks &&
                (1 === row.pendingTasks
                  ? unblockSuspenseListRow(
                      clientRenderedBoundaries,
                      row,
                      row.hoistables,
                    )
                  : row.pendingTasks--);
              JSCompiler_inline_result$jscomp$0 = writeHoistablesForBoundary(
                boundary,
                boundary$70.contentState,
                clientRenderedBoundaries.renderState,
              );
            }
            if (!JSCompiler_inline_result$jscomp$0) {
              request.destination = null;
              i++;
              partialBoundaries.splice(0, i);
              return;
            }
          }
          partialBoundaries.splice(0, i);
          flushingPartialBoundaries = false;
          var largeBoundaries = request.completedBoundaries;
          for (i = 0; i < largeBoundaries.length; i++)
            if (
              !flushCompletedBoundary(request, destination, largeBoundaries[i])
            ) {
              request.destination = null;
              i++;
              largeBoundaries.splice(0, i);
              return;
            }
          largeBoundaries.splice(0, i);
        }
      } finally {
        ((flushingPartialBoundaries = false),
          0 === request.allPendingTasks &&
          0 === request.clientRenderedBoundaries.length &&
          0 === request.completedBoundaries.length
            ? ((request.flushScheduled = false),
              (i = request.resumableState),
              i.hasBody && writeChunk(destination, endChunkForTag("body")),
              i.hasHtml && writeChunk(destination, endChunkForTag("html")),
              completeWriting(destination),
              flushBuffered(destination),
              (request.status = 14),
              destination.end(),
              (request.destination = null))
            : (completeWriting(destination), flushBuffered(destination)));
      }
    }
    function startWork(request) {
      request.flushScheduled = null !== request.destination;
      scheduleMicrotask(function () {
        return requestStorage.run(request, performWork, request);
      });
      setImmediate(function () {
        10 === request.status && (request.status = 11);
        null === request.trackedPostpones &&
          requestStorage.run(
            request,
            enqueueEarlyPreloadsAfterInitialWork,
            request,
          );
      });
    }
    function enqueueEarlyPreloadsAfterInitialWork(request) {
      safelyEmitEarlyPreloads(request, 0 === request.pendingRootTasks);
    }
    function enqueueFlush(request) {
      false === request.flushScheduled &&
        0 === request.pingedTasks.length &&
        null !== request.destination &&
        ((request.flushScheduled = true),
        setImmediate(function () {
          var destination = request.destination;
          destination
            ? flushCompletedQueues(request, destination)
            : (request.flushScheduled = false);
        }));
    }
    function startFlowing(request, destination) {
      if (13 === request.status)
        ((request.status = 14), destination.destroy(request.fatalError));
      else if (14 !== request.status && null === request.destination) {
        request.destination = destination;
        try {
          flushCompletedQueues(request, destination);
        } catch (error) {
          (logRecoverableError(request, error, {}), fatalError(request, error));
        }
      }
    }
    function abort(request, reason) {
      if (11 === request.status || 10 === request.status) request.status = 12;
      try {
        var abortableTasks = request.abortableTasks;
        if (0 < abortableTasks.size) {
          var error =
            void 0 === reason
              ? Error("The render was aborted by the server without a reason.")
              : "object" === typeof reason &&
                  null !== reason &&
                  "function" === typeof reason.then
                ? Error("The render was aborted by the server with a promise.")
                : reason;
          request.fatalError = error;
          abortableTasks.forEach(function (task) {
            return abortTask(task, request, error);
          });
          abortableTasks.clear();
        }
        null !== request.destination &&
          flushCompletedQueues(request, request.destination);
      } catch (error$72) {
        (logRecoverableError(request, error$72, {}),
          fatalError(request, error$72));
      }
    }
    function addToReplayParent(node2, parentKeyPath, trackedPostpones) {
      if (null === parentKeyPath) trackedPostpones.rootNodes.push(node2);
      else {
        var workingMap = trackedPostpones.workingMap,
          parentNode = workingMap.get(parentKeyPath);
        void 0 === parentNode &&
          ((parentNode = [parentKeyPath[1], parentKeyPath[2], [], null]),
          workingMap.set(parentKeyPath, parentNode),
          addToReplayParent(parentNode, parentKeyPath[0], trackedPostpones));
        parentNode[2].push(node2);
      }
    }
    function getPostponedState(request) {
      var trackedPostpones = request.trackedPostpones;
      if (
        null === trackedPostpones ||
        (0 === trackedPostpones.rootNodes.length &&
          null === trackedPostpones.rootSlots)
      )
        return (request.trackedPostpones = null);
      if (
        null === request.completedRootSegment ||
        (5 !== request.completedRootSegment.status &&
          null !== request.completedPreambleSegments)
      ) {
        var nextSegmentId = request.nextSegmentId;
        var replaySlots = trackedPostpones.rootSlots;
        var resumableState = request.resumableState;
        resumableState.bootstrapScriptContent = void 0;
        resumableState.bootstrapScripts = void 0;
        resumableState.bootstrapModules = void 0;
      } else {
        nextSegmentId = 0;
        replaySlots = -1;
        resumableState = request.resumableState;
        var renderState = request.renderState;
        resumableState.nextFormID = 0;
        resumableState.hasBody = false;
        resumableState.hasHtml = false;
        resumableState.unknownResources = { font: renderState.resets.font };
        resumableState.dnsResources = renderState.resets.dns;
        resumableState.connectResources = renderState.resets.connect;
        resumableState.imageResources = renderState.resets.image;
        resumableState.styleResources = renderState.resets.style;
        resumableState.scriptResources = {};
        resumableState.moduleUnknownResources = {};
        resumableState.moduleScriptResources = {};
        resumableState.instructions = 0;
      }
      return {
        nextSegmentId,
        rootFormatContext: request.rootFormatContext,
        progressiveChunkSize: request.progressiveChunkSize,
        resumableState: request.resumableState,
        replayNodes: trackedPostpones.rootNodes,
        replaySlots,
      };
    }
    function ensureCorrectIsomorphicReactVersion() {
      var isomorphicReactPackageVersion = React102.version;
      if ("19.2.8" !== isomorphicReactPackageVersion)
        throw Error(
          'Incompatible React versions: The "react" and "react-dom" packages must have the exact same version. Instead got:\n  - react:      ' +
            (isomorphicReactPackageVersion +
              "\n  - react-dom:  19.2.8\nLearn more: https://react.dev/warnings/version-mismatch"),
        );
    }
    ensureCorrectIsomorphicReactVersion();
    function createDrainHandler(destination, request) {
      return function () {
        return startFlowing(request, destination);
      };
    }
    function createCancelHandler(request, reason) {
      return function () {
        request.destination = null;
        abort(request, Error(reason));
      };
    }
    function createRequestImpl(children, options) {
      var resumableState = createResumableState(
        options ? options.identifierPrefix : void 0,
        options ? options.unstable_externalRuntimeSrc : void 0,
        options ? options.bootstrapScriptContent : void 0,
        options ? options.bootstrapScripts : void 0,
        options ? options.bootstrapModules : void 0,
      );
      return createRequest(
        children,
        resumableState,
        createRenderState(
          resumableState,
          options ? options.nonce : void 0,
          options ? options.unstable_externalRuntimeSrc : void 0,
          options ? options.importMap : void 0,
          options ? options.onHeaders : void 0,
          options ? options.maxHeadersLength : void 0,
        ),
        createRootFormatContext(options ? options.namespaceURI : void 0),
        options ? options.progressiveChunkSize : void 0,
        options ? options.onError : void 0,
        options ? options.onAllReady : void 0,
        options ? options.onShellReady : void 0,
        options ? options.onShellError : void 0,
        void 0,
        options ? options.onPostpone : void 0,
        options ? options.formState : void 0,
      );
    }
    function createFakeWritableFromReadableStreamController$1(controller) {
      return {
        write: function (chunk) {
          "string" === typeof chunk && (chunk = textEncoder.encode(chunk));
          controller.enqueue(chunk);
          return true;
        },
        end: function () {
          controller.close();
        },
        destroy: function (error) {
          "function" === typeof controller.error
            ? controller.error(error)
            : controller.close();
        },
      };
    }
    function resumeRequestImpl(children, postponedState, options) {
      return resumeRequest(
        children,
        postponedState,
        createRenderState(
          postponedState.resumableState,
          options ? options.nonce : void 0,
          void 0,
          void 0,
          void 0,
          void 0,
        ),
        options ? options.onError : void 0,
        options ? options.onAllReady : void 0,
        options ? options.onShellReady : void 0,
        options ? options.onShellError : void 0,
        void 0,
        options ? options.onPostpone : void 0,
      );
    }
    ensureCorrectIsomorphicReactVersion();
    function createFakeWritableFromReadableStreamController(controller) {
      return {
        write: function (chunk) {
          "string" === typeof chunk && (chunk = textEncoder.encode(chunk));
          controller.enqueue(chunk);
          return true;
        },
        end: function () {
          controller.close();
        },
        destroy: function (error) {
          "function" === typeof controller.error
            ? controller.error(error)
            : controller.close();
        },
      };
    }
    function createFakeWritableFromReadable(readable) {
      return {
        write: function (chunk) {
          return readable.push(chunk);
        },
        end: function () {
          readable.push(null);
        },
        destroy: function (error) {
          readable.destroy(error);
        },
      };
    }
    exports2.prerender = function (children, options) {
      return new Promise(function (resolve, reject) {
        var onHeaders = options ? options.onHeaders : void 0,
          onHeadersImpl;
        onHeaders &&
          (onHeadersImpl = function (headersDescriptor) {
            onHeaders(new Headers(headersDescriptor));
          });
        var resources = createResumableState(
            options ? options.identifierPrefix : void 0,
            options ? options.unstable_externalRuntimeSrc : void 0,
            options ? options.bootstrapScriptContent : void 0,
            options ? options.bootstrapScripts : void 0,
            options ? options.bootstrapModules : void 0,
          ),
          request = createPrerenderRequest(
            children,
            resources,
            createRenderState(
              resources,
              void 0,
              options ? options.unstable_externalRuntimeSrc : void 0,
              options ? options.importMap : void 0,
              onHeadersImpl,
              options ? options.maxHeadersLength : void 0,
            ),
            createRootFormatContext(options ? options.namespaceURI : void 0),
            options ? options.progressiveChunkSize : void 0,
            options ? options.onError : void 0,
            function () {
              var writable,
                stream2 = new ReadableStream(
                  {
                    type: "bytes",
                    start: function (controller) {
                      writable =
                        createFakeWritableFromReadableStreamController(
                          controller,
                        );
                    },
                    pull: function () {
                      startFlowing(request, writable);
                    },
                    cancel: function (reason) {
                      request.destination = null;
                      abort(request, reason);
                    },
                  },
                  { highWaterMark: 0 },
                );
              stream2 = {
                postponed: getPostponedState(request),
                prelude: stream2,
              };
              resolve(stream2);
            },
            void 0,
            void 0,
            reject,
            options ? options.onPostpone : void 0,
          );
        if (options && options.signal) {
          var signal = options.signal;
          if (signal.aborted) abort(request, signal.reason);
          else {
            var listener = function () {
              abort(request, signal.reason);
              signal.removeEventListener("abort", listener);
            };
            signal.addEventListener("abort", listener);
          }
        }
        startWork(request);
      });
    };
    exports2.prerenderToNodeStream = function (children, options) {
      return new Promise(function (resolve, reject) {
        var resumableState = createResumableState(
            options ? options.identifierPrefix : void 0,
            options ? options.unstable_externalRuntimeSrc : void 0,
            options ? options.bootstrapScriptContent : void 0,
            options ? options.bootstrapScripts : void 0,
            options ? options.bootstrapModules : void 0,
          ),
          request = createPrerenderRequest(
            children,
            resumableState,
            createRenderState(
              resumableState,
              void 0,
              options ? options.unstable_externalRuntimeSrc : void 0,
              options ? options.importMap : void 0,
              options ? options.onHeaders : void 0,
              options ? options.maxHeadersLength : void 0,
            ),
            createRootFormatContext(options ? options.namespaceURI : void 0),
            options ? options.progressiveChunkSize : void 0,
            options ? options.onError : void 0,
            function () {
              var readable = new stream.Readable({
                  read: function () {
                    startFlowing(request, writable);
                  },
                }),
                writable = createFakeWritableFromReadable(readable);
              readable = {
                postponed: getPostponedState(request),
                prelude: readable,
              };
              resolve(readable);
            },
            void 0,
            void 0,
            reject,
            options ? options.onPostpone : void 0,
          );
        if (options && options.signal) {
          var signal = options.signal;
          if (signal.aborted) abort(request, signal.reason);
          else {
            var listener = function () {
              abort(request, signal.reason);
              signal.removeEventListener("abort", listener);
            };
            signal.addEventListener("abort", listener);
          }
        }
        startWork(request);
      });
    };
    exports2.renderToPipeableStream = function (children, options) {
      var request = createRequestImpl(children, options),
        hasStartedFlowing = false;
      startWork(request);
      return {
        pipe: function (destination) {
          if (hasStartedFlowing)
            throw Error(
              "React currently only supports piping to one writable stream.",
            );
          hasStartedFlowing = true;
          safelyEmitEarlyPreloads(
            request,
            null === request.trackedPostpones
              ? 0 === request.pendingRootTasks
              : null === request.completedRootSegment
                ? 0 === request.pendingRootTasks
                : 5 !== request.completedRootSegment.status,
          );
          startFlowing(request, destination);
          destination.on("drain", createDrainHandler(destination, request));
          destination.on(
            "error",
            createCancelHandler(
              request,
              "The destination stream errored while writing data.",
            ),
          );
          destination.on(
            "close",
            createCancelHandler(
              request,
              "The destination stream closed early.",
            ),
          );
          return destination;
        },
        abort: function (reason) {
          abort(request, reason);
        },
      };
    };
    exports2.renderToReadableStream = function (children, options) {
      return new Promise(function (resolve, reject) {
        var onFatalError,
          onAllReady,
          allReady = new Promise(function (res, rej) {
            onAllReady = res;
            onFatalError = rej;
          }),
          onHeaders = options ? options.onHeaders : void 0,
          onHeadersImpl;
        onHeaders &&
          (onHeadersImpl = function (headersDescriptor) {
            onHeaders(new Headers(headersDescriptor));
          });
        var resumableState = createResumableState(
            options ? options.identifierPrefix : void 0,
            options ? options.unstable_externalRuntimeSrc : void 0,
            options ? options.bootstrapScriptContent : void 0,
            options ? options.bootstrapScripts : void 0,
            options ? options.bootstrapModules : void 0,
          ),
          request = createRequest(
            children,
            resumableState,
            createRenderState(
              resumableState,
              options ? options.nonce : void 0,
              options ? options.unstable_externalRuntimeSrc : void 0,
              options ? options.importMap : void 0,
              onHeadersImpl,
              options ? options.maxHeadersLength : void 0,
            ),
            createRootFormatContext(options ? options.namespaceURI : void 0),
            options ? options.progressiveChunkSize : void 0,
            options ? options.onError : void 0,
            onAllReady,
            function () {
              var writable,
                stream2 = new ReadableStream(
                  {
                    type: "bytes",
                    start: function (controller) {
                      writable =
                        createFakeWritableFromReadableStreamController$1(
                          controller,
                        );
                    },
                    pull: function () {
                      startFlowing(request, writable);
                    },
                    cancel: function (reason) {
                      request.destination = null;
                      abort(request, reason);
                    },
                  },
                  { highWaterMark: 0 },
                );
              stream2.allReady = allReady;
              resolve(stream2);
            },
            function (error) {
              allReady.catch(function () {});
              reject(error);
            },
            onFatalError,
            options ? options.onPostpone : void 0,
            options ? options.formState : void 0,
          );
        if (options && options.signal) {
          var signal = options.signal;
          if (signal.aborted) abort(request, signal.reason);
          else {
            var listener = function () {
              abort(request, signal.reason);
              signal.removeEventListener("abort", listener);
            };
            signal.addEventListener("abort", listener);
          }
        }
        startWork(request);
      });
    };
    exports2.resume = function (children, postponedState, options) {
      return new Promise(function (resolve, reject) {
        var onFatalError,
          onAllReady,
          allReady = new Promise(function (res, rej) {
            onAllReady = res;
            onFatalError = rej;
          }),
          request = resumeRequest(
            children,
            postponedState,
            createRenderState(
              postponedState.resumableState,
              options ? options.nonce : void 0,
              void 0,
              void 0,
              void 0,
              void 0,
            ),
            options ? options.onError : void 0,
            onAllReady,
            function () {
              var writable,
                stream2 = new ReadableStream(
                  {
                    type: "bytes",
                    start: function (controller) {
                      writable =
                        createFakeWritableFromReadableStreamController$1(
                          controller,
                        );
                    },
                    pull: function () {
                      startFlowing(request, writable);
                    },
                    cancel: function (reason) {
                      request.destination = null;
                      abort(request, reason);
                    },
                  },
                  { highWaterMark: 0 },
                );
              stream2.allReady = allReady;
              resolve(stream2);
            },
            function (error) {
              allReady.catch(function () {});
              reject(error);
            },
            onFatalError,
            options ? options.onPostpone : void 0,
          );
        if (options && options.signal) {
          var signal = options.signal;
          if (signal.aborted) abort(request, signal.reason);
          else {
            var listener = function () {
              abort(request, signal.reason);
              signal.removeEventListener("abort", listener);
            };
            signal.addEventListener("abort", listener);
          }
        }
        startWork(request);
      });
    };
    exports2.resumeAndPrerender = function (children, postponedState, options) {
      return new Promise(function (resolve, reject) {
        var request = resumeAndPrerenderRequest(
          children,
          postponedState,
          createRenderState(
            postponedState.resumableState,
            void 0,
            void 0,
            void 0,
            void 0,
            void 0,
          ),
          options ? options.onError : void 0,
          function () {
            var writable,
              stream2 = new ReadableStream(
                {
                  type: "bytes",
                  start: function (controller) {
                    writable =
                      createFakeWritableFromReadableStreamController(
                        controller,
                      );
                  },
                  pull: function () {
                    startFlowing(request, writable);
                  },
                  cancel: function (reason) {
                    request.destination = null;
                    abort(request, reason);
                  },
                },
                { highWaterMark: 0 },
              );
            stream2 = {
              postponed: getPostponedState(request),
              prelude: stream2,
            };
            resolve(stream2);
          },
          void 0,
          void 0,
          reject,
          options ? options.onPostpone : void 0,
        );
        if (options && options.signal) {
          var signal = options.signal;
          if (signal.aborted) abort(request, signal.reason);
          else {
            var listener = function () {
              abort(request, signal.reason);
              signal.removeEventListener("abort", listener);
            };
            signal.addEventListener("abort", listener);
          }
        }
        startWork(request);
      });
    };
    exports2.resumeAndPrerenderToNodeStream = function (
      children,
      postponedState,
      options,
    ) {
      return new Promise(function (resolve, reject) {
        var request = resumeAndPrerenderRequest(
          children,
          postponedState,
          createRenderState(
            postponedState.resumableState,
            void 0,
            void 0,
            void 0,
            void 0,
            void 0,
          ),
          options ? options.onError : void 0,
          function () {
            var readable = new stream.Readable({
                read: function () {
                  startFlowing(request, writable);
                },
              }),
              writable = createFakeWritableFromReadable(readable);
            readable = {
              postponed: getPostponedState(request),
              prelude: readable,
            };
            resolve(readable);
          },
          void 0,
          void 0,
          reject,
          options ? options.onPostpone : void 0,
        );
        if (options && options.signal) {
          var signal = options.signal;
          if (signal.aborted) abort(request, signal.reason);
          else {
            var listener = function () {
              abort(request, signal.reason);
              signal.removeEventListener("abort", listener);
            };
            signal.addEventListener("abort", listener);
          }
        }
        startWork(request);
      });
    };
    exports2.resumeToPipeableStream = function (
      children,
      postponedState,
      options,
    ) {
      var request = resumeRequestImpl(children, postponedState, options),
        hasStartedFlowing = false;
      startWork(request);
      return {
        pipe: function (destination) {
          if (hasStartedFlowing)
            throw Error(
              "React currently only supports piping to one writable stream.",
            );
          hasStartedFlowing = true;
          startFlowing(request, destination);
          destination.on("drain", createDrainHandler(destination, request));
          destination.on(
            "error",
            createCancelHandler(
              request,
              "The destination stream errored while writing data.",
            ),
          );
          destination.on(
            "close",
            createCancelHandler(
              request,
              "The destination stream closed early.",
            ),
          );
          return destination;
        },
        abort: function (reason) {
          abort(request, reason);
        },
      };
    };
    exports2.version = "19.2.8";
  },
});

// node_modules/.pnpm/react-dom@19.2.8_react@19.2.8/node_modules/react-dom/server.node.js
var require_server_node = __commonJS({
  "node_modules/.pnpm/react-dom@19.2.8_react@19.2.8/node_modules/react-dom/server.node.js"(
    exports2,
  ) {
    "use strict";
    var l;
    var s;
    if (true) {
      l = require_react_dom_server_legacy_node_production();
      s = require_react_dom_server_node_production();
    } else {
      l = null;
      s = null;
    }
    exports2.version = l.version;
    exports2.renderToString = l.renderToString;
    exports2.renderToStaticMarkup = l.renderToStaticMarkup;
    exports2.renderToPipeableStream = s.renderToPipeableStream;
    exports2.renderToReadableStream = s.renderToReadableStream;
    exports2.resumeToPipeableStream = s.resumeToPipeableStream;
    exports2.resume = s.resume;
  },
});

// node_modules/.pnpm/react-is@16.13.1/node_modules/react-is/cjs/react-is.production.min.js
var require_react_is_production_min = __commonJS({
  "node_modules/.pnpm/react-is@16.13.1/node_modules/react-is/cjs/react-is.production.min.js"(
    exports2,
  ) {
    "use strict";
    var b = "function" === typeof Symbol && Symbol.for;
    var c = b ? Symbol.for("react.element") : 60103;
    var d = b ? Symbol.for("react.portal") : 60106;
    var e = b ? Symbol.for("react.fragment") : 60107;
    var f = b ? Symbol.for("react.strict_mode") : 60108;
    var g = b ? Symbol.for("react.profiler") : 60114;
    var h = b ? Symbol.for("react.provider") : 60109;
    var k = b ? Symbol.for("react.context") : 60110;
    var l = b ? Symbol.for("react.async_mode") : 60111;
    var m = b ? Symbol.for("react.concurrent_mode") : 60111;
    var n = b ? Symbol.for("react.forward_ref") : 60112;
    var p = b ? Symbol.for("react.suspense") : 60113;
    var q = b ? Symbol.for("react.suspense_list") : 60120;
    var r2 = b ? Symbol.for("react.memo") : 60115;
    var t = b ? Symbol.for("react.lazy") : 60116;
    var v = b ? Symbol.for("react.block") : 60121;
    var w = b ? Symbol.for("react.fundamental") : 60117;
    var x = b ? Symbol.for("react.responder") : 60118;
    var y = b ? Symbol.for("react.scope") : 60119;
    function z(a) {
      if ("object" === typeof a && null !== a) {
        var u = a.$$typeof;
        switch (u) {
          case c:
            switch (((a = a.type), a)) {
              case l:
              case m:
              case e:
              case g:
              case f:
              case p:
                return a;
              default:
                switch (((a = a && a.$$typeof), a)) {
                  case k:
                  case n:
                  case t:
                  case r2:
                  case h:
                    return a;
                  default:
                    return u;
                }
            }
          case d:
            return u;
        }
      }
    }
    function A(a) {
      return z(a) === m;
    }
    exports2.AsyncMode = l;
    exports2.ConcurrentMode = m;
    exports2.ContextConsumer = k;
    exports2.ContextProvider = h;
    exports2.Element = c;
    exports2.ForwardRef = n;
    exports2.Fragment = e;
    exports2.Lazy = t;
    exports2.Memo = r2;
    exports2.Portal = d;
    exports2.Profiler = g;
    exports2.StrictMode = f;
    exports2.Suspense = p;
    exports2.isAsyncMode = function (a) {
      return A(a) || z(a) === l;
    };
    exports2.isConcurrentMode = A;
    exports2.isContextConsumer = function (a) {
      return z(a) === k;
    };
    exports2.isContextProvider = function (a) {
      return z(a) === h;
    };
    exports2.isElement = function (a) {
      return "object" === typeof a && null !== a && a.$$typeof === c;
    };
    exports2.isForwardRef = function (a) {
      return z(a) === n;
    };
    exports2.isFragment = function (a) {
      return z(a) === e;
    };
    exports2.isLazy = function (a) {
      return z(a) === t;
    };
    exports2.isMemo = function (a) {
      return z(a) === r2;
    };
    exports2.isPortal = function (a) {
      return z(a) === d;
    };
    exports2.isProfiler = function (a) {
      return z(a) === g;
    };
    exports2.isStrictMode = function (a) {
      return z(a) === f;
    };
    exports2.isSuspense = function (a) {
      return z(a) === p;
    };
    exports2.isValidElementType = function (a) {
      return (
        "string" === typeof a ||
        "function" === typeof a ||
        a === e ||
        a === m ||
        a === g ||
        a === f ||
        a === p ||
        a === q ||
        ("object" === typeof a &&
          null !== a &&
          (a.$$typeof === t ||
            a.$$typeof === r2 ||
            a.$$typeof === h ||
            a.$$typeof === k ||
            a.$$typeof === n ||
            a.$$typeof === w ||
            a.$$typeof === x ||
            a.$$typeof === y ||
            a.$$typeof === v))
      );
    };
    exports2.typeOf = z;
  },
});

// node_modules/.pnpm/react-is@16.13.1/node_modules/react-is/index.js
var require_react_is = __commonJS({
  "node_modules/.pnpm/react-is@16.13.1/node_modules/react-is/index.js"(
    exports2,
    module2,
  ) {
    "use strict";
    if (true) {
      module2.exports = require_react_is_production_min();
    } else {
      module2.exports = null;
    }
  },
});

// node_modules/.pnpm/hoist-non-react-statics@3.3.2/node_modules/hoist-non-react-statics/dist/hoist-non-react-statics.cjs.js
var require_hoist_non_react_statics_cjs = __commonJS({
  "node_modules/.pnpm/hoist-non-react-statics@3.3.2/node_modules/hoist-non-react-statics/dist/hoist-non-react-statics.cjs.js"(
    exports2,
    module2,
  ) {
    "use strict";
    var reactIs = require_react_is();
    var REACT_STATICS = {
      childContextTypes: true,
      contextType: true,
      contextTypes: true,
      defaultProps: true,
      displayName: true,
      getDefaultProps: true,
      getDerivedStateFromError: true,
      getDerivedStateFromProps: true,
      mixins: true,
      propTypes: true,
      type: true,
    };
    var KNOWN_STATICS = {
      name: true,
      length: true,
      prototype: true,
      caller: true,
      callee: true,
      arguments: true,
      arity: true,
    };
    var FORWARD_REF_STATICS = {
      $$typeof: true,
      render: true,
      defaultProps: true,
      displayName: true,
      propTypes: true,
    };
    var MEMO_STATICS = {
      $$typeof: true,
      compare: true,
      defaultProps: true,
      displayName: true,
      propTypes: true,
      type: true,
    };
    var TYPE_STATICS = {};
    TYPE_STATICS[reactIs.ForwardRef] = FORWARD_REF_STATICS;
    TYPE_STATICS[reactIs.Memo] = MEMO_STATICS;
    function getStatics(component) {
      if (reactIs.isMemo(component)) {
        return MEMO_STATICS;
      }
      return TYPE_STATICS[component["$$typeof"]] || REACT_STATICS;
    }
    var defineProperty = Object.defineProperty;
    var getOwnPropertyNames = Object.getOwnPropertyNames;
    var getOwnPropertySymbols = Object.getOwnPropertySymbols;
    var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
    var getPrototypeOf = Object.getPrototypeOf;
    var objectPrototype = Object.prototype;
    function hoistNonReactStatics(targetComponent, sourceComponent, blacklist) {
      if (typeof sourceComponent !== "string") {
        if (objectPrototype) {
          var inheritedComponent = getPrototypeOf(sourceComponent);
          if (inheritedComponent && inheritedComponent !== objectPrototype) {
            hoistNonReactStatics(
              targetComponent,
              inheritedComponent,
              blacklist,
            );
          }
        }
        var keys = getOwnPropertyNames(sourceComponent);
        if (getOwnPropertySymbols) {
          keys = keys.concat(getOwnPropertySymbols(sourceComponent));
        }
        var targetStatics = getStatics(targetComponent);
        var sourceStatics = getStatics(sourceComponent);
        for (var i = 0; i < keys.length; ++i) {
          var key = keys[i];
          if (
            !KNOWN_STATICS[key] &&
            !(blacklist && blacklist[key]) &&
            !(sourceStatics && sourceStatics[key]) &&
            !(targetStatics && targetStatics[key])
          ) {
            var descriptor = getOwnPropertyDescriptor(sourceComponent, key);
            try {
              defineProperty(targetComponent, key, descriptor);
            } catch (e) {}
          }
        }
      }
      return targetComponent;
    }
    module2.exports = hoistNonReactStatics;
  },
});

// node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/extends.js
var require_extends = __commonJS({
  "node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/extends.js"(
    exports2,
    module2,
  ) {
    function _extends2() {
      return (
        (module2.exports = _extends2 =
          Object.assign
            ? Object.assign.bind()
            : function (n) {
                for (var e = 1; e < arguments.length; e++) {
                  var t = arguments[e];
                  for (var r2 in t)
                    ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
                }
                return n;
              }),
        (module2.exports.__esModule = true),
        (module2.exports["default"] = module2.exports),
        _extends2.apply(null, arguments)
      );
    }
    ((module2.exports = _extends2),
      (module2.exports.__esModule = true),
      (module2.exports["default"] = module2.exports));
  },
});

// node_modules/.pnpm/react@19.2.8/node_modules/react/cjs/react-jsx-runtime.production.js
var require_react_jsx_runtime_production = __commonJS({
  "node_modules/.pnpm/react@19.2.8/node_modules/react/cjs/react-jsx-runtime.production.js"(
    exports2,
  ) {
    "use strict";
    var REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element");
    var REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
    function jsxProd(type, config, maybeKey) {
      var key = null;
      void 0 !== maybeKey && (key = "" + maybeKey);
      void 0 !== config.key && (key = "" + config.key);
      if ("key" in config) {
        maybeKey = {};
        for (var propName in config)
          "key" !== propName && (maybeKey[propName] = config[propName]);
      } else maybeKey = config;
      config = maybeKey.ref;
      return {
        $$typeof: REACT_ELEMENT_TYPE,
        type,
        key,
        ref: void 0 !== config ? config : null,
        props: maybeKey,
      };
    }
    exports2.Fragment = REACT_FRAGMENT_TYPE;
    exports2.jsx = jsxProd;
    exports2.jsxs = jsxProd;
  },
});

// node_modules/.pnpm/react@19.2.8/node_modules/react/jsx-runtime.js
var require_jsx_runtime = __commonJS({
  "node_modules/.pnpm/react@19.2.8/node_modules/react/jsx-runtime.js"(
    exports2,
    module2,
  ) {
    "use strict";
    if (true) {
      module2.exports = require_react_jsx_runtime_production();
    } else {
      module2.exports = null;
    }
  },
});

// node_modules/.pnpm/react-is@19.2.8/node_modules/react-is/cjs/react-is.production.js
var require_react_is_production = __commonJS({
  "node_modules/.pnpm/react-is@19.2.8/node_modules/react-is/cjs/react-is.production.js"(
    exports2,
  ) {
    "use strict";
    var REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element");
    var REACT_PORTAL_TYPE = Symbol.for("react.portal");
    var REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
    var REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode");
    var REACT_PROFILER_TYPE = Symbol.for("react.profiler");
    var REACT_CONSUMER_TYPE = Symbol.for("react.consumer");
    var REACT_CONTEXT_TYPE = Symbol.for("react.context");
    var REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref");
    var REACT_SUSPENSE_TYPE = Symbol.for("react.suspense");
    var REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list");
    var REACT_MEMO_TYPE = Symbol.for("react.memo");
    var REACT_LAZY_TYPE = Symbol.for("react.lazy");
    var REACT_VIEW_TRANSITION_TYPE = Symbol.for("react.view_transition");
    var REACT_CLIENT_REFERENCE = Symbol.for("react.client.reference");
    function typeOf(object) {
      if ("object" === typeof object && null !== object) {
        var $$typeof = object.$$typeof;
        switch ($$typeof) {
          case REACT_ELEMENT_TYPE:
            switch (((object = object.type), object)) {
              case REACT_FRAGMENT_TYPE:
              case REACT_PROFILER_TYPE:
              case REACT_STRICT_MODE_TYPE:
              case REACT_SUSPENSE_TYPE:
              case REACT_SUSPENSE_LIST_TYPE:
              case REACT_VIEW_TRANSITION_TYPE:
                return object;
              default:
                switch (((object = object && object.$$typeof), object)) {
                  case REACT_CONTEXT_TYPE:
                  case REACT_FORWARD_REF_TYPE:
                  case REACT_LAZY_TYPE:
                  case REACT_MEMO_TYPE:
                    return object;
                  case REACT_CONSUMER_TYPE:
                    return object;
                  default:
                    return $$typeof;
                }
            }
          case REACT_PORTAL_TYPE:
            return $$typeof;
        }
      }
    }
    exports2.ContextConsumer = REACT_CONSUMER_TYPE;
    exports2.ContextProvider = REACT_CONTEXT_TYPE;
    exports2.Element = REACT_ELEMENT_TYPE;
    exports2.ForwardRef = REACT_FORWARD_REF_TYPE;
    exports2.Fragment = REACT_FRAGMENT_TYPE;
    exports2.Lazy = REACT_LAZY_TYPE;
    exports2.Memo = REACT_MEMO_TYPE;
    exports2.Portal = REACT_PORTAL_TYPE;
    exports2.Profiler = REACT_PROFILER_TYPE;
    exports2.StrictMode = REACT_STRICT_MODE_TYPE;
    exports2.Suspense = REACT_SUSPENSE_TYPE;
    exports2.SuspenseList = REACT_SUSPENSE_LIST_TYPE;
    exports2.isContextConsumer = function (object) {
      return typeOf(object) === REACT_CONSUMER_TYPE;
    };
    exports2.isContextProvider = function (object) {
      return typeOf(object) === REACT_CONTEXT_TYPE;
    };
    exports2.isElement = function (object) {
      return (
        "object" === typeof object &&
        null !== object &&
        object.$$typeof === REACT_ELEMENT_TYPE
      );
    };
    exports2.isForwardRef = function (object) {
      return typeOf(object) === REACT_FORWARD_REF_TYPE;
    };
    exports2.isFragment = function (object) {
      return typeOf(object) === REACT_FRAGMENT_TYPE;
    };
    exports2.isLazy = function (object) {
      return typeOf(object) === REACT_LAZY_TYPE;
    };
    exports2.isMemo = function (object) {
      return typeOf(object) === REACT_MEMO_TYPE;
    };
    exports2.isPortal = function (object) {
      return typeOf(object) === REACT_PORTAL_TYPE;
    };
    exports2.isProfiler = function (object) {
      return typeOf(object) === REACT_PROFILER_TYPE;
    };
    exports2.isStrictMode = function (object) {
      return typeOf(object) === REACT_STRICT_MODE_TYPE;
    };
    exports2.isSuspense = function (object) {
      return typeOf(object) === REACT_SUSPENSE_TYPE;
    };
    exports2.isSuspenseList = function (object) {
      return typeOf(object) === REACT_SUSPENSE_LIST_TYPE;
    };
    exports2.isValidElementType = function (type) {
      return "string" === typeof type ||
        "function" === typeof type ||
        type === REACT_FRAGMENT_TYPE ||
        type === REACT_PROFILER_TYPE ||
        type === REACT_STRICT_MODE_TYPE ||
        type === REACT_SUSPENSE_TYPE ||
        type === REACT_SUSPENSE_LIST_TYPE ||
        ("object" === typeof type &&
          null !== type &&
          (type.$$typeof === REACT_LAZY_TYPE ||
            type.$$typeof === REACT_MEMO_TYPE ||
            type.$$typeof === REACT_CONTEXT_TYPE ||
            type.$$typeof === REACT_CONSUMER_TYPE ||
            type.$$typeof === REACT_FORWARD_REF_TYPE ||
            type.$$typeof === REACT_CLIENT_REFERENCE ||
            void 0 !== type.getModuleId))
        ? true
        : false;
    };
    exports2.typeOf = typeOf;
  },
});

// node_modules/.pnpm/react-is@19.2.8/node_modules/react-is/index.js
var require_react_is2 = __commonJS({
  "node_modules/.pnpm/react-is@19.2.8/node_modules/react-is/index.js"(
    exports2,
    module2,
  ) {
    "use strict";
    if (true) {
      module2.exports = require_react_is_production();
    } else {
      module2.exports = null;
    }
  },
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/language/index.js
function createLanguageStorageKey(basePath = "") {
  const normalized = basePath.trim().replace(/^\/+|\/+$/g, "");
  return `site_language:${normalized ? `/${normalized}` : "/"}`;
}
function markContentReady() {
  if (typeof document === "undefined") return;
  document.documentElement.dataset.sitegenReady = "true";
  document.documentElement.removeAttribute("data-sitegen-pending-language");
  window.dispatchEvent(new Event("sitegen:content-ready"));
}
function createLanguageBootstrapScript(config) {
  const json = JSON.stringify(config).replace(/</g, "\\u003c");
  return `(${bootstrap.toString()})(${json});`;
}
function bootstrap(config) {
  const root = document.documentElement;
  let selected = config.primaryLanguage;
  try {
    const stored =
      localStorage.getItem(config.storageKey || "site_language") ??
      (config.legacyStorageKey
        ? localStorage.getItem(config.legacyStorageKey)
        : null);
    const value = stored?.toUpperCase();
    if (value && config.supportedLanguages.includes(value)) selected = value;
  } catch {}
  if (selected !== config.primaryLanguage) {
    root.setAttribute("data-sitegen-pending-language", selected);
  }
  const messages = {
    EN: ["The page could not finish loading. Please try again.", "Reload"],
    SL: [
      "Strani ni bilo mogo\u010De nalo\u017Eiti. Poskusite znova.",
      "Osve\u017Ei",
    ],
    DE: [
      "Die Seite konnte nicht geladen werden. Bitte erneut versuchen.",
      "Neu laden",
    ],
    IT: ["Impossibile caricare la pagina. Riprova.", "Ricarica"],
    HR: [
      "Stranica se nije mogla u\u010Ditati. Poku\u0161ajte ponovo.",
      "Osvje\u017Ei",
    ],
  };
  const showError = () => {
    if (root.dataset.sitegenReady === "true") return;
    const host = document.getElementById("sitegen-language-recovery");
    if (!host) return;
    const [message, label] = messages[selected] || messages.EN;
    const panel = document.createElement("div");
    panel.setAttribute("role", "alert");
    panel.setAttribute("lang", selected.toLowerCase());
    const text = document.createElement("p");
    text.textContent = message;
    const button = document.createElement("button");
    button.type = "button";
    button.textContent = label;
    button.onclick = () => location.reload();
    panel.append(text, button);
    host.replaceChildren(panel);
  };
  const timer = window.setTimeout(showError, 15e3);
  window.addEventListener("sitegen:language-error", showError);
  window.addEventListener(
    "sitegen:content-ready",
    () => {
      window.clearTimeout(timer);
      window.removeEventListener("sitegen:language-error", showError);
      document.getElementById("sitegen-language-recovery")?.replaceChildren();
    },
    { once: true },
  );
}
var init_language = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/language/index.js"() {},
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/runtime/createLanguageRuntime.js
var import_jsx_runtime70, import_react10, languageMap;
var init_createLanguageRuntime = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/runtime/createLanguageRuntime.js"() {
    "use client";
    import_jsx_runtime70 = __toESM(require_jsx_runtime(), 1);
    import_react10 = __toESM(require_react(), 1);
    languageMap = {
      SL: "Sloven\u0161\u010Dina",
      EN: "English",
      IT: "Italiano",
      DE: "Deutsch",
      HR: "Hrvatski",
    };
  },
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/runtime/createWebsiteRuntime.js
function createWebsiteRuntime({
  primaryLanguage: primaryLanguage2,
  basePath,
  fetchImpl = fetch,
  initialWebsiteJson,
}) {
  const { BASE_PATH: BASE_PATH2, imgWithBasePath: imgWithBasePath2 } =
    createPathHelpers(basePath);
  const localize = (value, lang) => getLocalized(value, lang, primaryLanguage2);
  let metaCache = null;
  const websiteCache = /* @__PURE__ */ new Map();
  let websiteJson = initialWebsiteJson;
  let loadSequence = 0;
  let metaPending;
  const pending = /* @__PURE__ */ new Map();
  async function getWebsiteMeta() {
    if (metaCache) return metaCache;
    if (!metaPending) {
      metaPending = (async () => {
        const response = await fetchImpl(`${BASE_PATH2}/data/meta.json`, {
          cache: "no-store",
        });
        if (!response.ok) throw new Error("Failed to load meta.json");
        const meta = await response.json();
        const version2 = String(meta.version ?? "");
        if (!/^[a-zA-Z0-9_-]+$/.test(version2))
          throw new Error("Invalid meta.json");
        metaCache = { version: version2 };
        return metaCache;
      })().finally(() => {
        metaPending = void 0;
      });
    }
    return metaPending;
  }
  async function getWebsiteJson(lang) {
    const meta = await getWebsiteMeta();
    const normalizedLang = (lang ?? primaryLanguage2).toLowerCase();
    const cacheKey = `${normalizedLang}_${meta.version}`;
    if (websiteCache.has(cacheKey)) return websiteCache.get(cacheKey);
    if (!pending.has(cacheKey)) {
      const request = (async () => {
        const response = await fetchImpl(
          `${BASE_PATH2}/data/website_${cacheKey}.json`,
          {
            cache: "force-cache",
          },
        );
        if (!response.ok)
          throw new Error(`Failed to load website_${cacheKey}.json`);
        const data = await response.json();
        websiteCache.set(cacheKey, data);
        return data;
      })().finally(() => {
        pending.delete(cacheKey);
      });
      pending.set(cacheKey, request);
    }
    return pending.get(cacheKey);
  }
  async function loadWebsiteJsonForLang(lang) {
    const request = ++loadSequence;
    const data = await getWebsiteJson(lang ?? primaryLanguage2);
    if (request === loadSequence) websiteJson = data;
    return data;
  }
  const getLocalizedCategories = (categories, lang) => {
    if (!categories) {
      return [];
    }
    return categories.map((category) => localize(category.name, lang));
  };
  const getLocalizedCategoryOptions = (categories, lang) => {
    if (!categories) {
      return [];
    }
    return categories.map((category) => ({
      id: category.id,
      name: localize(category.name, lang),
    }));
  };
  const getLocalizedCategoryById = (categories, categoryId, lang) => {
    if (!categories || !categoryId) {
      return "";
    }
    const category = categories.find((item) => item.id === categoryId);
    return category ? localize(category.name, lang) : "";
  };
  const getCurrencySymbol = (currency) => {
    switch (currency) {
      case "USD":
        return "$";
      case "EUR":
        return "\u20AC";
      case "GBP":
        return "\xA3";
      default:
        return "";
    }
  };
  const getLocalizedFile = (value, lang) => {
    if (!value || typeof value !== "object") {
      return void 0;
    }
    const resource = "file" in value ? value : localize(value, lang);
    if (!resource || typeof resource.file !== "string") {
      return void 0;
    }
    const file = resource.file.trim();
    return file ? imgWithBasePath2(file) : void 0;
  };
  function getThemeSettings2() {
    return (
      websiteJson.theme ?? {
        colors: {},
        fonts: {},
        images: {},
      }
    );
  }
  const getHome2 = (lang) => {
    return {
      name: localize(websiteJson.home.websiteName, lang),
      slogan: localize(websiteJson.home.slogan, lang),
      logo: {
        text: websiteJson.home.companyLogo.text,
        image: imgWithBasePath2(websiteJson.home.companyLogo.image?.file),
      },
    };
  };
  const getLegalSection2 = (lang) => {
    if (!websiteJson.legal) {
      return void 0;
    }
    const privacyPolicy = getLocalizedFile(
      websiteJson.legal.privacyPolicy,
      lang,
    );
    const termsOfService = getLocalizedFile(
      websiteJson.legal.termsOfService,
      lang,
    );
    if (!privacyPolicy && !termsOfService) {
      return void 0;
    }
    return {
      privacyPolicy,
      termsOfService,
    };
  };
  const getAboutSection2 = (lang) => {
    return {
      aboutType: websiteJson.about.aboutType,
      sectionName: localize(websiteJson.about.sectionName, lang),
      text: localize(websiteJson.about.text, lang),
      establishedYear: websiteJson.about.establishedYear,
    };
  };
  const getAboutItems2 = (lang) => {
    return websiteJson.about.items.map((item) => ({
      text: localize(item.text, lang),
      image: imgWithBasePath2(item.image.file),
    }));
  };
  const getExperienceSection2 = (lang) => {
    if (!websiteJson.experience) {
      return void 0;
    }
    return {
      text: localize(websiteJson.experience.text, lang),
      clientCount: websiteJson.experience.clientCount,
      projectCount: websiteJson.experience.projectCount,
    };
  };
  const getExperienceItems2 = () => {
    if (!websiteJson.experience) {
      return [];
    }
    return websiteJson.experience.items.map((item) => ({
      url: item.url,
      image: imgWithBasePath2(item.image.file),
    }));
  };
  const getTeamSection2 = (lang) => {
    if (!websiteJson.team) {
      return void 0;
    }
    return {
      text: localize(websiteJson.team.text, lang),
    };
  };
  const getTeamItems2 = (lang) => {
    if (!websiteJson.team) {
      return [];
    }
    return websiteJson.team.items.map((member) => ({
      name: localize(member.name, lang),
      text: localize(member.text, lang),
      contact: member.contact.map((c) => ({
        type: c.type,
        value: c.value,
      })),
      image: imgWithBasePath2(member.image.file),
    }));
  };
  const getPricingSection2 = (lang) => {
    if (!websiteJson.pricing) {
      return void 0;
    }
    return {
      sectionName: localize(websiteJson.pricing.sectionName, lang),
      type: websiteJson.pricing.type,
      text: localize(websiteJson.pricing.text, lang),
      categories: getLocalizedCategories(websiteJson.pricing.categories, lang),
      categoryOptions: getLocalizedCategoryOptions(
        websiteJson.pricing.categories,
        lang,
      ),
      note: localize(websiteJson.pricing.note, lang),
    };
  };
  const getPricingItems4 = (lang) => {
    if (!websiteJson.pricing) {
      return [];
    }
    const categories = websiteJson.pricing.categories;
    return websiteJson.pricing.items
      .map((item, index) => ({
        id: index + 1,
        title: localize(item.title, lang),
        text: localize(item.text, lang),
        categoryId: item.categoryId ?? null,
        category: getLocalizedCategoryById(categories, item.categoryId, lang),
        price: {
          value: item.price.value,
          discountedValue: item.price.discountedValue ?? "",
          currency: getCurrencySymbol(item.price.currency),
          onAgreement: item.price.onAgreement,
          unit: item.price.unit,
        },
        images: item.images.map((img) => imgWithBasePath2(img.file)),
        features: item.features.map((feature) => ({
          label: localize(feature.label, lang),
          value: localize(feature.value, lang),
        })),
        recommended: item.recommended,
        status: item.status ?? "AVAILABLE",
      }))
      .filter((item) => item.status !== "DISABLED");
  };
  const getCareersItems2 = (lang) => {
    if (!websiteJson.careers) {
      return [];
    }
    return websiteJson.careers.items.map((item, index) => ({
      id: index + 1,
      title: localize(item.title, lang),
      text: localize(item.text, lang),
      requirements: item.requirements.map((req) => localize(req, lang)),
      note: localize(item.note, lang),
    }));
  };
  const getCareersSection2 = (lang) => {
    if (!websiteJson.careers) {
      return void 0;
    }
    return {
      text: localize(websiteJson.careers.text, lang),
    };
  };
  const getServicesSection2 = (lang) => {
    if (!websiteJson.services) {
      return void 0;
    }
    return {
      text: localize(websiteJson.services.text, lang),
    };
  };
  const getServiceItems2 = (lang) => {
    if (!websiteJson.services) {
      return [];
    }
    return websiteJson.services.items.map((item) => ({
      title: localize(item.title, lang),
      text: localize(item.text, lang),
      image: imgWithBasePath2(item.image.file),
      features: item.features?.map((feature) => localize(feature, lang)),
    }));
  };
  const getPortfolioSection2 = (lang) => {
    if (!websiteJson.portfolio) {
      return void 0;
    }
    const categories = getLocalizedCategories(
      websiteJson.portfolio.categories,
      lang,
    );
    return {
      name: localize(websiteJson.portfolio.sectionName, lang),
      text: localize(websiteJson.portfolio.text, lang),
      categories,
    };
  };
  const getPortfolioItems3 = (lang) => {
    if (!websiteJson.portfolio) {
      return [];
    }
    const categories = websiteJson.portfolio.categories;
    return websiteJson.portfolio.items.map((item, index) => ({
      id: index + 1,
      title: localize(item.title, lang),
      text: localize(item.text, lang),
      client: localize(item.client, lang),
      date: item.date,
      category: getLocalizedCategoryById(categories, item.categoryId, lang),
      images: item.images.map((img) => imgWithBasePath2(img.file)),
    }));
  };
  const getReviewSection2 = (lang) => {
    if (!websiteJson.reviews) {
      return void 0;
    }
    return {
      text: localize(websiteJson.reviews.text, lang),
    };
  };
  const getReviewItems2 = (lang) => {
    if (!websiteJson.reviews) {
      return [];
    }
    return websiteJson.reviews.items.map((item) => ({
      title: localize(item.title, lang),
      text: localize(item.text, lang),
      url: item.url,
      image: imgWithBasePath2(item.image.file),
    }));
  };
  const getBlogSection2 = (lang) => {
    if (!websiteJson.blog) {
      return void 0;
    }
    return {
      text: localize(websiteJson.blog.text, lang),
    };
  };
  const getBlogItems2 = (lang) => {
    if (!websiteJson.blog) {
      return [];
    }
    return websiteJson.blog.items.map((item, index) => ({
      id: index + 1,
      title: localize(item.title, lang),
      description: localize(item.description, lang),
      author: item.author,
      date: item.date,
      image: imgWithBasePath2(item.image.file),
    }));
  };
  const getGalleryItems2 = () => {
    if (!websiteJson.gallery) {
      return [];
    }
    return websiteJson.gallery.items.map((item) => imgWithBasePath2(item.file));
  };
  const getCompany2 = () => {
    return websiteJson.contactInfo.company;
  };
  const getMap2 = () => {
    return websiteJson.contactInfo.map;
  };
  const getContacts2 = () => {
    return websiteJson.contactInfo.contact.map((item) => ({
      type: item.type,
      value: item.value,
    }));
  };
  const getWorkingHours2 = (lang = null) => {
    return {
      enabled: websiteJson.contactInfo.workingHours.enabled,
      items: websiteJson.contactInfo.workingHours.items,
      note: localize(websiteJson.contactInfo.workingHours.note, lang),
    };
  };
  const getWorkingHoursNote = (lang) => {
    return localize(websiteJson.contactInfo.workingHours.note, lang);
  };
  const getClients2 = () => {
    if (!websiteJson.clients) {
      return [];
    }
    return websiteJson.clients.items.map((item) => ({
      src: imgWithBasePath2(item.image.file),
      href: item.url,
    }));
  };
  const getCatalogueSection2 = () => {
    if (!websiteJson.catalogues) {
      return void 0;
    }
    return {
      items: websiteJson.catalogues.items.map((item) => ({
        name: item.displayName,
        file: imgWithBasePath2(item.file.file),
      })),
    };
  };
  const getVideoSection2 = () => {
    if (!websiteJson.videos) {
      return void 0;
    }
    return {
      items: websiteJson.videos.items,
    };
  };
  const getFaqSection2 = (lang) => {
    if (!websiteJson.faq) {
      return void 0;
    }
    return {
      text: localize(websiteJson.faq.text, lang),
      items: websiteJson.faq.items.map((item) => ({
        question: localize(item.question, lang),
        answer: localize(item.answer, lang),
      })),
    };
  };
  const getEventsSection2 = (lang) => {
    if (!websiteJson.events) {
      return void 0;
    }
    return {
      text: localize(websiteJson.events.text, lang),
      categories: getLocalizedCategoryOptions(
        websiteJson.events.categories,
        lang,
      ),
      showApplyButton: Boolean(websiteJson.events.showApplyButton),
    };
  };
  const getEventItems3 = (lang) => {
    if (!websiteJson.events) {
      return [];
    }
    const categories = websiteJson.events.categories;
    return websiteJson.events.items.map((item, index) => ({
      id: index + 1,
      title: localize(item.title, lang),
      text: localize(item.text, lang),
      date: item.date,
      location: localize(item.location, lang),
      categoryId: item.categoryId ?? null,
      category: getLocalizedCategoryById(categories, item.categoryId, lang),
      isCancelled: Boolean(item.isCancelled),
      image: imgWithBasePath2(item.image?.file),
    }));
  };
  const getScheduleSection2 = (lang) => {
    if (!websiteJson.schedule) {
      return void 0;
    }
    return {
      title: localize(websiteJson.schedule.title, lang),
      text: localize(websiteJson.schedule.text, lang),
      categories: getLocalizedCategoryOptions(
        websiteJson.schedule.categories,
        lang,
      ),
    };
  };
  const getScheduleTables2 = (lang) => {
    if (!websiteJson.schedule) {
      return [];
    }
    return websiteJson.schedule.items.map((table, index) => ({
      id: index + 1,
      title: localize(table.title, lang),
      text: localize(table.text, lang),
      categoryId: table.categoryId ?? null,
      rows: (table.rows ?? []).map((row) =>
        (row.cells ?? []).map((cell) => localize(cell, lang)),
      ),
    }));
  };
  return {
    getWebsiteJson,
    loadWebsiteJsonForLang,
    getThemeSettings: getThemeSettings2,
    getHome: getHome2,
    getLegalSection: getLegalSection2,
    getAboutSection: getAboutSection2,
    getAboutItems: getAboutItems2,
    getExperienceSection: getExperienceSection2,
    getExperienceItems: getExperienceItems2,
    getTeamSection: getTeamSection2,
    getTeamItems: getTeamItems2,
    getPricingSection: getPricingSection2,
    getPricingItems: getPricingItems4,
    getCareersItems: getCareersItems2,
    getCareersSection: getCareersSection2,
    getServicesSection: getServicesSection2,
    getServiceItems: getServiceItems2,
    getPortfolioSection: getPortfolioSection2,
    getPortfolioItems: getPortfolioItems3,
    getReviewSection: getReviewSection2,
    getReviewItems: getReviewItems2,
    getBlogSection: getBlogSection2,
    getBlogItems: getBlogItems2,
    getGalleryItems: getGalleryItems2,
    getCompany: getCompany2,
    getMap: getMap2,
    getContacts: getContacts2,
    getWorkingHours: getWorkingHours2,
    getWorkingHoursNote,
    getClients: getClients2,
    getCatalogueSection: getCatalogueSection2,
    getVideoSection: getVideoSection2,
    getFaqSection: getFaqSection2,
    getEventsSection: getEventsSection2,
    getEventItems: getEventItems3,
    getScheduleSection: getScheduleSection2,
    getScheduleTables: getScheduleTables2,
  };
}
var BUILD_YEAR;
var init_createWebsiteRuntime = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/runtime/createWebsiteRuntime.js"() {
    "use client";
    init_localization();
    init_path();
    BUILD_YEAR = /* @__PURE__ */ new Date().getFullYear();
  },
});

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/runtime/index.js
var init_runtime = __esm({
  "node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/runtime/index.js"() {
    "use client";
    init_createLanguageRuntime();
    init_createWebsiteRuntime();
  },
});

// src/data/website.json
var website_default;
var init_website = __esm({
  "src/data/website.json"() {
    website_default = {
      general: {
        primaryLanguage: "SL",
        supportedLanguages: ["EN"],
      },
      home: {
        websiteName: {
          SL: "Mirna Spa & Beauty",
          EN: "Mirna Spa & Beauty",
        },
        slogan: {
          SL: "Masa\u017Ee, nega obraza in manikura ob Ljubljanici. Dovolj \u010Dasa za vsak obisk, naravna kozmetika in terapevtke, ki vedo, kaj delajo.",
          EN: "Massage, facials and manicures by the Ljubljanica. Enough time for every visit, natural skincare, and therapists who know what they are doing.",
        },
        companyLogo: {
          text: "MIRNA",
        },
      },
      contactInfo: {
        company: {
          name: "Mirna Spa & Beauty demo d.o.o.",
          country: "Slovenia",
          address: "Trnovski pristan 14",
          postalCode: "1000",
          postalOffice: "Ljubljana",
        },
        contact: [
          {
            type: "PHONE",
            value: "+386 1 555 02 14",
          },
          {
            type: "EMAIL",
            value: "pozdravljeni@mirna-spa.example",
          },
          {
            type: "WEBSITE",
            value: "https://mirna-spa.example",
          },
          {
            type: "INSTAGRAM",
            value: "https://instagram.com/mirna.spa.example",
          },
          {
            type: "FACEBOOK",
            value: "https://facebook.com/mirna.spa.example",
          },
          {
            type: "LINKEDIN",
            value: "https://www.linkedin.com/company/example",
          },
        ],
      },
      optionalSections: {
        team: true,
        experience: true,
        reviews: true,
        portfolio: true,
        gallery: true,
        clients: true,
        services: true,
        blog: true,
        videos: true,
        catalogues: true,
        pricing: true,
        careers: true,
        subscribe: true,
        faq: true,
        schedule: true,
        events: true,
      },
      blog: {
        slugs: [
          "1-maska-iz-gline-kolikokrat-na-teden-je-dovolj",
          "2-katero-olje-za-masazo-in-zakaj-ne-vsako",
          "3-zakaj-se-po-masazi-pocutite-utrujeni",
          "4-limfna-drenaza-obraza-ki-jo-zmorete-sami",
          "5-zascita-pred-soncem-tudi-pozimi",
        ],
      },
      portfolio: {
        slugs: [
          "1-ritual-globinske-nege-obraza",
          "2-masaza-s-toplimi-kamni",
          "3-manikura-z-masazo-rok",
          "4-spa-pedikura",
          "5-nega-las-in-lasisca",
          "6-lifting-obraza-brez-igel",
        ],
      },
      pricing: {
        slugs: [
          "1-paket-umiritev",
          "2-paket-mirna",
          "3-paket-za-dva",
          "4-klasicna-masaza",
          "5-masaza-s-toplimi-kamni",
          "6-nosecniska-masaza",
          "7-ritual-obraza",
          "8-serija-liftinga-obraza",
          "9-manikura-s-trajnim-lakom",
          "10-porocni-paket",
        ],
      },
      careers: {
        slugs: [
          "1-masazni-terapevt-oziroma-terapevtka",
          "2-kozmeticarka",
          "3-receptor-oziroma-receptorka",
        ],
      },
      events: {
        slugs: [
          "1-vecer-aromaterapije",
          "2-masaza-za-nosecnice-in-partnerje",
          "3-manikura-doma-ki-zdrzi",
          "4-dan-odprtih-vrat",
          "5-priceske-za-praznike",
        ],
      },
    };
  },
});

// src/core/static.ts
var landingStatic,
  BASE_PATH,
  slugs2,
  FALLBACK_IMAGE,
  primaryLanguage,
  supportedLanguages,
  imgWithBasePath,
  withBasePath,
  normalizePath,
  isNavActive,
  isCurrentPath,
  getEnabledSections,
  isSectionEnabled,
  getHomeMeta,
  getAllBlogSlugs,
  getBlogSlugById,
  getAllPortfolioSlugs,
  getPortfolioSlugById,
  getAllPricingSlugs,
  getPricingSlugById,
  getAllCareerSlugs,
  getCareerSlugById,
  getAllEventSlugs,
  getEventSlugById,
  getPageSlugByKey,
  getPageSlugByKeyWithBasePath;
var init_static2 = __esm({
  "src/core/static.ts"() {
    "use strict";
    init_static();
    init_website();
    landingStatic = createLandingStatic({
      website: website_default,
      basePath: "/sitegen-landing-ui-005",
    });
    ({
      BASE_PATH,
      slugs: slugs2,
      FALLBACK_IMAGE,
      primaryLanguage,
      supportedLanguages,
      imgWithBasePath,
      withBasePath,
      normalizePath,
      isNavActive,
      isCurrentPath,
      getEnabledSections,
      isSectionEnabled,
      getHomeMeta,
      getAllBlogSlugs,
      getBlogSlugById,
      getAllPortfolioSlugs,
      getPortfolioSlugById,
      getAllPricingSlugs,
      getPricingSlugById,
      getAllCareerSlugs,
      getCareerSlugById,
      getAllEventSlugs,
      getEventSlugById,
      getPageSlugByKey,
      getPageSlugByKeyWithBasePath,
    } = landingStatic);
  },
});

// scripts/snapshot/runtime.tsx
function configureSnapshotRuntime(website) {
  current = createWebsiteRuntime({
    primaryLanguage,
    basePath: BASE_PATH,
    initialWebsiteJson: website,
    fetchImpl: async () => {
      throw new Error("Snapshot rendering must not fetch runtime JSON");
    },
  });
}
function useLanguage() {
  return {
    lang: primaryLanguage,
    defaultLanguage: primaryLanguage,
    languageList: supportedLanguages.map((code) => ({
      code,
      label: languageMap[code],
    })),
    setLang: async () => {},
  };
}
var current,
  getThemeSettings,
  getHome,
  getLegalSection,
  getAboutSection,
  getAboutItems,
  getExperienceSection,
  getExperienceItems,
  getTeamSection,
  getTeamItems,
  getPricingSection,
  getPricingItems,
  getCareersItems,
  getCareersSection,
  getServicesSection,
  getServiceItems,
  getPortfolioSection,
  getPortfolioItems,
  getReviewSection,
  getReviewItems,
  getBlogSection,
  getBlogItems,
  getGalleryItems,
  getCompany,
  getMap,
  getContacts,
  getWorkingHours,
  getClients,
  getCatalogueSection,
  getVideoSection,
  getFaqSection,
  getEventsSection,
  getEventItems,
  getScheduleSection,
  getScheduleTables;
var init_runtime2 = __esm({
  "scripts/snapshot/runtime.tsx"() {
    "use strict";
    init_runtime();
    init_static2();
    getThemeSettings = (...args) => current.getThemeSettings(...args);
    getHome = (...args) => current.getHome(...args);
    getLegalSection = (...args) => current.getLegalSection(...args);
    getAboutSection = (...args) => current.getAboutSection(...args);
    getAboutItems = (...args) => current.getAboutItems(...args);
    getExperienceSection = (...args) => current.getExperienceSection(...args);
    getExperienceItems = (...args) => current.getExperienceItems(...args);
    getTeamSection = (...args) => current.getTeamSection(...args);
    getTeamItems = (...args) => current.getTeamItems(...args);
    getPricingSection = (...args) => current.getPricingSection(...args);
    getPricingItems = (...args) => current.getPricingItems(...args);
    getCareersItems = (...args) => current.getCareersItems(...args);
    getCareersSection = (...args) => current.getCareersSection(...args);
    getServicesSection = (...args) => current.getServicesSection(...args);
    getServiceItems = (...args) => current.getServiceItems(...args);
    getPortfolioSection = (...args) => current.getPortfolioSection(...args);
    getPortfolioItems = (...args) => current.getPortfolioItems(...args);
    getReviewSection = (...args) => current.getReviewSection(...args);
    getReviewItems = (...args) => current.getReviewItems(...args);
    getBlogSection = (...args) => current.getBlogSection(...args);
    getBlogItems = (...args) => current.getBlogItems(...args);
    getGalleryItems = (...args) => current.getGalleryItems(...args);
    getCompany = (...args) => current.getCompany(...args);
    getMap = (...args) => current.getMap(...args);
    getContacts = (...args) => current.getContacts(...args);
    getWorkingHours = (...args) => current.getWorkingHours(...args);
    getClients = (...args) => current.getClients(...args);
    getCatalogueSection = (...args) => current.getCatalogueSection(...args);
    getVideoSection = (...args) => current.getVideoSection(...args);
    getFaqSection = (...args) => current.getFaqSection(...args);
    getEventsSection = (...args) => current.getEventsSection(...args);
    getEventItems = (...args) => current.getEventItems(...args);
    getScheduleSection = (...args) => current.getScheduleSection(...args);
    getScheduleTables = (...args) => current.getScheduleTables(...args);
  },
});

// src/app/theme/utils/getBannerImage.ts
function getBannerFromTheme() {
  const banner = getThemeSettings().images?.banner;
  return banner?.file ? imgWithBasePath(banner.file) : DEFAULT_HEADER_IMAGE;
}
var DEFAULT_HEADER_IMAGE;
var init_getBannerImage = __esm({
  "src/app/theme/utils/getBannerImage.ts"() {
    "use strict";
    init_runtime2();
    init_static2();
    DEFAULT_HEADER_IMAGE = `${BASE_PATH}/images/main-banner.webp`;
  },
});

// src/app/theme/utils/useBannerImage.default.ts
var useBannerImage_default_exports = {};
__export(useBannerImage_default_exports, {
  useBannerImage: () => useBannerImage,
});
function useBannerImage() {
  return getBannerFromTheme();
}
var init_useBannerImage_default = __esm({
  "src/app/theme/utils/useBannerImage.default.ts"() {
    "use strict";
    init_getBannerImage();
  },
});

// node_modules/.pnpm/photoswipe@5.4.4/node_modules/photoswipe/dist/photoswipe-lightbox.esm.js
var photoswipe_lightbox_esm_exports = {};
__export(photoswipe_lightbox_esm_exports, {
  default: () => PhotoSwipeLightbox,
});
function createElement4(className, tagName, appendToEl) {
  const el = document.createElement(tagName);
  if (className) {
    el.className = className;
  }
  if (appendToEl) {
    appendToEl.appendChild(el);
  }
  return el;
}
function toTransformString(x, y, scale) {
  let propValue = `translate3d(${x}px,${y || 0}px,0)`;
  if (scale !== void 0) {
    propValue += ` scale3d(${scale},${scale},1)`;
  }
  return propValue;
}
function setWidthHeight(el, w, h) {
  el.style.width = typeof w === "number" ? `${w}px` : w;
  el.style.height = typeof h === "number" ? `${h}px` : h;
}
function specialKeyUsed(e) {
  return (
    ("button" in e && e.button === 1) ||
    e.ctrlKey ||
    e.metaKey ||
    e.altKey ||
    e.shiftKey
  );
}
function getElementsFromOption(option, legacySelector, parent = document) {
  let elements = [];
  if (option instanceof Element) {
    elements = [option];
  } else if (option instanceof NodeList || Array.isArray(option)) {
    elements = Array.from(option);
  } else {
    const selector = typeof option === "string" ? option : legacySelector;
    if (selector) {
      elements = Array.from(parent.querySelectorAll(selector));
    }
  }
  return elements;
}
function isPswpClass(fn2) {
  return typeof fn2 === "function" && fn2.prototype && fn2.prototype.goTo;
}
function isSafari() {
  return !!(navigator.vendor && navigator.vendor.match(/apple/i));
}
function getViewportSize(options, pswp) {
  if (options.getViewportSizeFn) {
    const newViewportSize = options.getViewportSizeFn(options, pswp);
    if (newViewportSize) {
      return newViewportSize;
    }
  }
  return {
    x: document.documentElement.clientWidth,
    // TODO: height on mobile is very incosistent due to toolbar
    // find a way to improve this
    //
    // document.documentElement.clientHeight - doesn't seem to work well
    y: window.innerHeight,
  };
}
function parsePaddingOption(prop, options, viewportSize, itemData, index) {
  let paddingValue = 0;
  if (options.paddingFn) {
    paddingValue = options.paddingFn(viewportSize, itemData, index)[prop];
  } else if (options.padding) {
    paddingValue = options.padding[prop];
  } else {
    const legacyPropName = "padding" + prop[0].toUpperCase() + prop.slice(1);
    if (options[legacyPropName]) {
      paddingValue = options[legacyPropName];
    }
  }
  return Number(paddingValue) || 0;
}
function getPanAreaSize(options, viewportSize, itemData, index) {
  return {
    x:
      viewportSize.x -
      parsePaddingOption("left", options, viewportSize, itemData, index) -
      parsePaddingOption("right", options, viewportSize, itemData, index),
    y:
      viewportSize.y -
      parsePaddingOption("top", options, viewportSize, itemData, index) -
      parsePaddingOption("bottom", options, viewportSize, itemData, index),
  };
}
function lazyLoadData(itemData, instance, index) {
  const content = instance.createContentFromData(itemData, index);
  let zoomLevel;
  const { options } = instance;
  if (options) {
    zoomLevel = new ZoomLevel(options, itemData, -1);
    let viewportSize;
    if (instance.pswp) {
      viewportSize = instance.pswp.viewportSize;
    } else {
      viewportSize = getViewportSize(options, instance);
    }
    const panAreaSize = getPanAreaSize(options, viewportSize, itemData, index);
    zoomLevel.update(content.width, content.height, panAreaSize);
  }
  content.lazyLoad();
  if (zoomLevel) {
    content.setDisplayedSize(
      Math.ceil(content.width * zoomLevel.initial),
      Math.ceil(content.height * zoomLevel.initial),
    );
  }
  return content;
}
function lazyLoadSlide(index, instance) {
  const itemData = instance.getItemData(index);
  if (
    instance.dispatch("lazyLoadSlide", {
      index,
      itemData,
    }).defaultPrevented
  ) {
    return;
  }
  return lazyLoadData(itemData, instance, index);
}
var LOAD_STATE,
  PhotoSwipeEvent,
  Eventable,
  Placeholder,
  Content,
  MAX_IMAGE_WIDTH,
  ZoomLevel,
  PhotoSwipeBase,
  PhotoSwipeLightbox;
var init_photoswipe_lightbox_esm = __esm({
  "node_modules/.pnpm/photoswipe@5.4.4/node_modules/photoswipe/dist/photoswipe-lightbox.esm.js"() {
    LOAD_STATE = {
      IDLE: "idle",
      LOADING: "loading",
      LOADED: "loaded",
      ERROR: "error",
    };
    PhotoSwipeEvent = class {
      /**
       * @param {T} type
       * @param {PhotoSwipeEventsMap[T]} [details]
       */
      constructor(type, details2) {
        this.type = type;
        this.defaultPrevented = false;
        if (details2) {
          Object.assign(this, details2);
        }
      }
      preventDefault() {
        this.defaultPrevented = true;
      }
    };
    Eventable = class {
      constructor() {
        this._listeners = {};
        this._filters = {};
        this.pswp = void 0;
        this.options = void 0;
      }
      /**
       * @template {keyof PhotoSwipeFiltersMap} T
       * @param {T} name
       * @param {PhotoSwipeFiltersMap[T]} fn
       * @param {number} priority
       */
      addFilter(name, fn2, priority = 100) {
        var _this$_filters$name, _this$_filters$name2, _this$pswp;
        if (!this._filters[name]) {
          this._filters[name] = [];
        }
        (_this$_filters$name = this._filters[name]) === null ||
          _this$_filters$name === void 0 ||
          _this$_filters$name.push({
            fn: fn2,
            priority,
          });
        (_this$_filters$name2 = this._filters[name]) === null ||
          _this$_filters$name2 === void 0 ||
          _this$_filters$name2.sort((f1, f2) => f1.priority - f2.priority);
        (_this$pswp = this.pswp) === null ||
          _this$pswp === void 0 ||
          _this$pswp.addFilter(name, fn2, priority);
      }
      /**
       * @template {keyof PhotoSwipeFiltersMap} T
       * @param {T} name
       * @param {PhotoSwipeFiltersMap[T]} fn
       */
      removeFilter(name, fn2) {
        if (this._filters[name]) {
          this._filters[name] = this._filters[name].filter(
            (filter) => filter.fn !== fn2,
          );
        }
        if (this.pswp) {
          this.pswp.removeFilter(name, fn2);
        }
      }
      /**
       * @template {keyof PhotoSwipeFiltersMap} T
       * @param {T} name
       * @param {Parameters<PhotoSwipeFiltersMap[T]>} args
       * @returns {Parameters<PhotoSwipeFiltersMap[T]>[0]}
       */
      applyFilters(name, ...args) {
        var _this$_filters$name3;
        (_this$_filters$name3 = this._filters[name]) === null ||
          _this$_filters$name3 === void 0 ||
          _this$_filters$name3.forEach((filter) => {
            args[0] = filter.fn.apply(this, args);
          });
        return args[0];
      }
      /**
       * @template {keyof PhotoSwipeEventsMap} T
       * @param {T} name
       * @param {EventCallback<T>} fn
       */
      on(name, fn2) {
        var _this$_listeners$name, _this$pswp2;
        if (!this._listeners[name]) {
          this._listeners[name] = [];
        }
        (_this$_listeners$name = this._listeners[name]) === null ||
          _this$_listeners$name === void 0 ||
          _this$_listeners$name.push(fn2);
        (_this$pswp2 = this.pswp) === null ||
          _this$pswp2 === void 0 ||
          _this$pswp2.on(name, fn2);
      }
      /**
       * @template {keyof PhotoSwipeEventsMap} T
       * @param {T} name
       * @param {EventCallback<T>} fn
       */
      off(name, fn2) {
        var _this$pswp3;
        if (this._listeners[name]) {
          this._listeners[name] = this._listeners[name].filter(
            (listener) => fn2 !== listener,
          );
        }
        (_this$pswp3 = this.pswp) === null ||
          _this$pswp3 === void 0 ||
          _this$pswp3.off(name, fn2);
      }
      /**
       * @template {keyof PhotoSwipeEventsMap} T
       * @param {T} name
       * @param {PhotoSwipeEventsMap[T]} [details]
       * @returns {AugmentedEvent<T>}
       */
      dispatch(name, details2) {
        var _this$_listeners$name2;
        if (this.pswp) {
          return this.pswp.dispatch(name, details2);
        }
        const event =
          /** @type {AugmentedEvent<T>} */
          new PhotoSwipeEvent(name, details2);
        (_this$_listeners$name2 = this._listeners[name]) === null ||
          _this$_listeners$name2 === void 0 ||
          _this$_listeners$name2.forEach((listener) => {
            listener.call(this, event);
          });
        return event;
      }
    };
    Placeholder = class {
      /**
       * @param {string | false} imageSrc
       * @param {HTMLElement} container
       */
      constructor(imageSrc, container) {
        this.element = createElement4(
          "pswp__img pswp__img--placeholder",
          imageSrc ? "img" : "div",
          container,
        );
        if (imageSrc) {
          const imgEl =
            /** @type {HTMLImageElement} */
            this.element;
          imgEl.decoding = "async";
          imgEl.alt = "";
          imgEl.src = imageSrc;
          imgEl.setAttribute("role", "presentation");
        }
        this.element.setAttribute("aria-hidden", "true");
      }
      /**
       * @param {number} width
       * @param {number} height
       */
      setDisplayedSize(width2, height2) {
        if (!this.element) {
          return;
        }
        if (this.element.tagName === "IMG") {
          setWidthHeight(this.element, 250, "auto");
          this.element.style.transformOrigin = "0 0";
          this.element.style.transform = toTransformString(0, 0, width2 / 250);
        } else {
          setWidthHeight(this.element, width2, height2);
        }
      }
      destroy() {
        var _this$element;
        if (
          (_this$element = this.element) !== null &&
          _this$element !== void 0 &&
          _this$element.parentNode
        ) {
          this.element.remove();
        }
        this.element = null;
      }
    };
    Content = class {
      /**
       * @param {SlideData} itemData Slide data
       * @param {PhotoSwipeBase} instance PhotoSwipe or PhotoSwipeLightbox instance
       * @param {number} index
       */
      constructor(itemData, instance, index) {
        this.instance = instance;
        this.data = itemData;
        this.index = index;
        this.element = void 0;
        this.placeholder = void 0;
        this.slide = void 0;
        this.displayedImageWidth = 0;
        this.displayedImageHeight = 0;
        this.width = Number(this.data.w) || Number(this.data.width) || 0;
        this.height = Number(this.data.h) || Number(this.data.height) || 0;
        this.isAttached = false;
        this.hasSlide = false;
        this.isDecoding = false;
        this.state = LOAD_STATE.IDLE;
        if (this.data.type) {
          this.type = this.data.type;
        } else if (this.data.src) {
          this.type = "image";
        } else {
          this.type = "html";
        }
        this.instance.dispatch("contentInit", {
          content: this,
        });
      }
      removePlaceholder() {
        if (this.placeholder && !this.keepPlaceholder()) {
          setTimeout(() => {
            if (this.placeholder) {
              this.placeholder.destroy();
              this.placeholder = void 0;
            }
          }, 1e3);
        }
      }
      /**
       * Preload content
       *
       * @param {boolean} isLazy
       * @param {boolean} [reload]
       */
      load(isLazy, reload) {
        if (this.slide && this.usePlaceholder()) {
          if (!this.placeholder) {
            const placeholderSrc = this.instance.applyFilters(
              "placeholderSrc",
              // use  image-based placeholder only for the first slide,
              // as rendering (even small stretched thumbnail) is an expensive operation
              this.data.msrc && this.slide.isFirstSlide
                ? this.data.msrc
                : false,
              this,
            );
            this.placeholder = new Placeholder(
              placeholderSrc,
              this.slide.container,
            );
          } else {
            const placeholderEl = this.placeholder.element;
            if (placeholderEl && !placeholderEl.parentElement) {
              this.slide.container.prepend(placeholderEl);
            }
          }
        }
        if (this.element && !reload) {
          return;
        }
        if (
          this.instance.dispatch("contentLoad", {
            content: this,
            isLazy,
          }).defaultPrevented
        ) {
          return;
        }
        if (this.isImageContent()) {
          this.element = createElement4("pswp__img", "img");
          if (this.displayedImageWidth) {
            this.loadImage(isLazy);
          }
        } else {
          this.element = createElement4("pswp__content", "div");
          this.element.innerHTML = this.data.html || "";
        }
        if (reload && this.slide) {
          this.slide.updateContentSize(true);
        }
      }
      /**
       * Preload image
       *
       * @param {boolean} isLazy
       */
      loadImage(isLazy) {
        var _this$data$src, _this$data$alt;
        if (
          !this.isImageContent() ||
          !this.element ||
          this.instance.dispatch("contentLoadImage", {
            content: this,
            isLazy,
          }).defaultPrevented
        ) {
          return;
        }
        const imageElement =
          /** @type HTMLImageElement */
          this.element;
        this.updateSrcsetSizes();
        if (this.data.srcset) {
          imageElement.srcset = this.data.srcset;
        }
        imageElement.src =
          (_this$data$src = this.data.src) !== null && _this$data$src !== void 0
            ? _this$data$src
            : "";
        imageElement.alt =
          (_this$data$alt = this.data.alt) !== null && _this$data$alt !== void 0
            ? _this$data$alt
            : "";
        this.state = LOAD_STATE.LOADING;
        if (imageElement.complete) {
          this.onLoaded();
        } else {
          imageElement.onload = () => {
            this.onLoaded();
          };
          imageElement.onerror = () => {
            this.onError();
          };
        }
      }
      /**
       * Assign slide to content
       *
       * @param {Slide} slide
       */
      setSlide(slide) {
        this.slide = slide;
        this.hasSlide = true;
        this.instance = slide.pswp;
      }
      /**
       * Content load success handler
       */
      onLoaded() {
        this.state = LOAD_STATE.LOADED;
        if (this.slide && this.element) {
          this.instance.dispatch("loadComplete", {
            slide: this.slide,
            content: this,
          });
          if (
            this.slide.isActive &&
            this.slide.heavyAppended &&
            !this.element.parentNode
          ) {
            this.append();
            this.slide.updateContentSize(true);
          }
          if (
            this.state === LOAD_STATE.LOADED ||
            this.state === LOAD_STATE.ERROR
          ) {
            this.removePlaceholder();
          }
        }
      }
      /**
       * Content load error handler
       */
      onError() {
        this.state = LOAD_STATE.ERROR;
        if (this.slide) {
          this.displayError();
          this.instance.dispatch("loadComplete", {
            slide: this.slide,
            isError: true,
            content: this,
          });
          this.instance.dispatch("loadError", {
            slide: this.slide,
            content: this,
          });
        }
      }
      /**
       * @returns {Boolean} If the content is currently loading
       */
      isLoading() {
        return this.instance.applyFilters(
          "isContentLoading",
          this.state === LOAD_STATE.LOADING,
          this,
        );
      }
      /**
       * @returns {Boolean} If the content is in error state
       */
      isError() {
        return this.state === LOAD_STATE.ERROR;
      }
      /**
       * @returns {boolean} If the content is image
       */
      isImageContent() {
        return this.type === "image";
      }
      /**
       * Update content size
       *
       * @param {Number} width
       * @param {Number} height
       */
      setDisplayedSize(width2, height2) {
        if (!this.element) {
          return;
        }
        if (this.placeholder) {
          this.placeholder.setDisplayedSize(width2, height2);
        }
        if (
          this.instance.dispatch("contentResize", {
            content: this,
            width: width2,
            height: height2,
          }).defaultPrevented
        ) {
          return;
        }
        setWidthHeight(this.element, width2, height2);
        if (this.isImageContent() && !this.isError()) {
          const isInitialSizeUpdate = !this.displayedImageWidth && width2;
          this.displayedImageWidth = width2;
          this.displayedImageHeight = height2;
          if (isInitialSizeUpdate) {
            this.loadImage(false);
          } else {
            this.updateSrcsetSizes();
          }
          if (this.slide) {
            this.instance.dispatch("imageSizeChange", {
              slide: this.slide,
              width: width2,
              height: height2,
              content: this,
            });
          }
        }
      }
      /**
       * @returns {boolean} If the content can be zoomed
       */
      isZoomable() {
        return this.instance.applyFilters(
          "isContentZoomable",
          this.isImageContent() && this.state !== LOAD_STATE.ERROR,
          this,
        );
      }
      /**
       * Update image srcset sizes attribute based on width and height
       */
      updateSrcsetSizes() {
        if (!this.isImageContent() || !this.element || !this.data.srcset) {
          return;
        }
        const image =
          /** @type HTMLImageElement */
          this.element;
        const sizesWidth = this.instance.applyFilters(
          "srcsetSizesWidth",
          this.displayedImageWidth,
          this,
        );
        if (
          !image.dataset.largestUsedSize ||
          sizesWidth > parseInt(image.dataset.largestUsedSize, 10)
        ) {
          image.sizes = sizesWidth + "px";
          image.dataset.largestUsedSize = String(sizesWidth);
        }
      }
      /**
       * @returns {boolean} If content should use a placeholder (from msrc by default)
       */
      usePlaceholder() {
        return this.instance.applyFilters(
          "useContentPlaceholder",
          this.isImageContent(),
          this,
        );
      }
      /**
       * Preload content with lazy-loading param
       */
      lazyLoad() {
        if (
          this.instance.dispatch("contentLazyLoad", {
            content: this,
          }).defaultPrevented
        ) {
          return;
        }
        this.load(true);
      }
      /**
       * @returns {boolean} If placeholder should be kept after content is loaded
       */
      keepPlaceholder() {
        return this.instance.applyFilters(
          "isKeepingPlaceholder",
          this.isLoading(),
          this,
        );
      }
      /**
       * Destroy the content
       */
      destroy() {
        this.hasSlide = false;
        this.slide = void 0;
        if (
          this.instance.dispatch("contentDestroy", {
            content: this,
          }).defaultPrevented
        ) {
          return;
        }
        this.remove();
        if (this.placeholder) {
          this.placeholder.destroy();
          this.placeholder = void 0;
        }
        if (this.isImageContent() && this.element) {
          this.element.onload = null;
          this.element.onerror = null;
          this.element = void 0;
        }
      }
      /**
       * Display error message
       */
      displayError() {
        if (this.slide) {
          var _this$instance$option, _this$instance$option2;
          let errorMsgEl = createElement4("pswp__error-msg", "div");
          errorMsgEl.innerText =
            (_this$instance$option =
              (_this$instance$option2 = this.instance.options) === null ||
              _this$instance$option2 === void 0
                ? void 0
                : _this$instance$option2.errorMsg) !== null &&
            _this$instance$option !== void 0
              ? _this$instance$option
              : "";
          errorMsgEl =
            /** @type {HTMLDivElement} */
            this.instance.applyFilters("contentErrorElement", errorMsgEl, this);
          this.element = createElement4(
            "pswp__content pswp__error-msg-container",
            "div",
          );
          this.element.appendChild(errorMsgEl);
          this.slide.container.innerText = "";
          this.slide.container.appendChild(this.element);
          this.slide.updateContentSize(true);
          this.removePlaceholder();
        }
      }
      /**
       * Append the content
       */
      append() {
        if (this.isAttached || !this.element) {
          return;
        }
        this.isAttached = true;
        if (this.state === LOAD_STATE.ERROR) {
          this.displayError();
          return;
        }
        if (
          this.instance.dispatch("contentAppend", {
            content: this,
          }).defaultPrevented
        ) {
          return;
        }
        const supportsDecode = "decode" in this.element;
        if (this.isImageContent()) {
          if (
            supportsDecode &&
            this.slide &&
            (!this.slide.isActive || isSafari())
          ) {
            this.isDecoding = true;
            this.element
              .decode()
              .catch(() => {})
              .finally(() => {
                this.isDecoding = false;
                this.appendImage();
              });
          } else {
            this.appendImage();
          }
        } else if (this.slide && !this.element.parentNode) {
          this.slide.container.appendChild(this.element);
        }
      }
      /**
       * Activate the slide,
       * active slide is generally the current one,
       * meaning the user can see it.
       */
      activate() {
        if (
          this.instance.dispatch("contentActivate", {
            content: this,
          }).defaultPrevented ||
          !this.slide
        ) {
          return;
        }
        if (this.isImageContent() && this.isDecoding && !isSafari()) {
          this.appendImage();
        } else if (this.isError()) {
          this.load(false, true);
        }
        if (this.slide.holderElement) {
          this.slide.holderElement.setAttribute("aria-hidden", "false");
        }
      }
      /**
       * Deactivate the content
       */
      deactivate() {
        this.instance.dispatch("contentDeactivate", {
          content: this,
        });
        if (this.slide && this.slide.holderElement) {
          this.slide.holderElement.setAttribute("aria-hidden", "true");
        }
      }
      /**
       * Remove the content from DOM
       */
      remove() {
        this.isAttached = false;
        if (
          this.instance.dispatch("contentRemove", {
            content: this,
          }).defaultPrevented
        ) {
          return;
        }
        if (this.element && this.element.parentNode) {
          this.element.remove();
        }
        if (this.placeholder && this.placeholder.element) {
          this.placeholder.element.remove();
        }
      }
      /**
       * Append the image content to slide container
       */
      appendImage() {
        if (!this.isAttached) {
          return;
        }
        if (
          this.instance.dispatch("contentAppendImage", {
            content: this,
          }).defaultPrevented
        ) {
          return;
        }
        if (this.slide && this.element && !this.element.parentNode) {
          this.slide.container.appendChild(this.element);
        }
        if (
          this.state === LOAD_STATE.LOADED ||
          this.state === LOAD_STATE.ERROR
        ) {
          this.removePlaceholder();
        }
      }
    };
    MAX_IMAGE_WIDTH = 4e3;
    ZoomLevel = class {
      /**
       * @param {PhotoSwipeOptions} options PhotoSwipe options
       * @param {SlideData} itemData Slide data
       * @param {number} index Slide index
       * @param {PhotoSwipe} [pswp] PhotoSwipe instance, can be undefined if not initialized yet
       */
      constructor(options, itemData, index, pswp) {
        this.pswp = pswp;
        this.options = options;
        this.itemData = itemData;
        this.index = index;
        this.panAreaSize = null;
        this.elementSize = null;
        this.fit = 1;
        this.fill = 1;
        this.vFill = 1;
        this.initial = 1;
        this.secondary = 1;
        this.max = 1;
        this.min = 1;
      }
      /**
       * Calculate initial, secondary and maximum zoom level for the specified slide.
       *
       * It should be called when either image or viewport size changes.
       *
       * @param {number} maxWidth
       * @param {number} maxHeight
       * @param {Point} panAreaSize
       */
      update(maxWidth2, maxHeight2, panAreaSize) {
        const elementSize = {
          x: maxWidth2,
          y: maxHeight2,
        };
        this.elementSize = elementSize;
        this.panAreaSize = panAreaSize;
        const hRatio = panAreaSize.x / elementSize.x;
        const vRatio = panAreaSize.y / elementSize.y;
        this.fit = Math.min(1, hRatio < vRatio ? hRatio : vRatio);
        this.fill = Math.min(1, hRatio > vRatio ? hRatio : vRatio);
        this.vFill = Math.min(1, vRatio);
        this.initial = this._getInitial();
        this.secondary = this._getSecondary();
        this.max = Math.max(this.initial, this.secondary, this._getMax());
        this.min = Math.min(this.fit, this.initial, this.secondary);
        if (this.pswp) {
          this.pswp.dispatch("zoomLevelsUpdate", {
            zoomLevels: this,
            slideData: this.itemData,
          });
        }
      }
      /**
       * Parses user-defined zoom option.
       *
       * @private
       * @param {'initial' | 'secondary' | 'max'} optionPrefix Zoom level option prefix (initial, secondary, max)
       * @returns { number | undefined }
       */
      _parseZoomLevelOption(optionPrefix) {
        const optionName =
          /** @type {'initialZoomLevel' | 'secondaryZoomLevel' | 'maxZoomLevel'} */
          optionPrefix + "ZoomLevel";
        const optionValue = this.options[optionName];
        if (!optionValue) {
          return;
        }
        if (typeof optionValue === "function") {
          return optionValue(this);
        }
        if (optionValue === "fill") {
          return this.fill;
        }
        if (optionValue === "fit") {
          return this.fit;
        }
        return Number(optionValue);
      }
      /**
       * Get zoom level to which image will be zoomed after double-tap gesture,
       * or when user clicks on zoom icon,
       * or mouse-click on image itself.
       * If you return 1 image will be zoomed to its original size.
       *
       * @private
       * @return {number}
       */
      _getSecondary() {
        let currZoomLevel = this._parseZoomLevelOption("secondary");
        if (currZoomLevel) {
          return currZoomLevel;
        }
        currZoomLevel = Math.min(1, this.fit * 3);
        if (
          this.elementSize &&
          currZoomLevel * this.elementSize.x > MAX_IMAGE_WIDTH
        ) {
          currZoomLevel = MAX_IMAGE_WIDTH / this.elementSize.x;
        }
        return currZoomLevel;
      }
      /**
       * Get initial image zoom level.
       *
       * @private
       * @return {number}
       */
      _getInitial() {
        return this._parseZoomLevelOption("initial") || this.fit;
      }
      /**
       * Maximum zoom level when user zooms
       * via zoom/pinch gesture,
       * via cmd/ctrl-wheel or via trackpad.
       *
       * @private
       * @return {number}
       */
      _getMax() {
        return this._parseZoomLevelOption("max") || Math.max(1, this.fit * 4);
      }
    };
    PhotoSwipeBase = class extends Eventable {
      /**
       * Get total number of slides
       *
       * @returns {number}
       */
      getNumItems() {
        var _this$options;
        let numItems = 0;
        const dataSource =
          (_this$options = this.options) === null || _this$options === void 0
            ? void 0
            : _this$options.dataSource;
        if (dataSource && "length" in dataSource) {
          numItems = dataSource.length;
        } else if (dataSource && "gallery" in dataSource) {
          if (!dataSource.items) {
            dataSource.items = this._getGalleryDOMElements(dataSource.gallery);
          }
          if (dataSource.items) {
            numItems = dataSource.items.length;
          }
        }
        const event = this.dispatch("numItems", {
          dataSource,
          numItems,
        });
        return this.applyFilters("numItems", event.numItems, dataSource);
      }
      /**
       * @param {SlideData} slideData
       * @param {number} index
       * @returns {Content}
       */
      createContentFromData(slideData, index) {
        return new Content(slideData, this, index);
      }
      /**
       * Get item data by index.
       *
       * "item data" should contain normalized information that PhotoSwipe needs to generate a slide.
       * For example, it may contain properties like
       * `src`, `srcset`, `w`, `h`, which will be used to generate a slide with image.
       *
       * @param {number} index
       * @returns {SlideData}
       */
      getItemData(index) {
        var _this$options2;
        const dataSource =
          (_this$options2 = this.options) === null || _this$options2 === void 0
            ? void 0
            : _this$options2.dataSource;
        let dataSourceItem = {};
        if (Array.isArray(dataSource)) {
          dataSourceItem = dataSource[index];
        } else if (dataSource && "gallery" in dataSource) {
          if (!dataSource.items) {
            dataSource.items = this._getGalleryDOMElements(dataSource.gallery);
          }
          dataSourceItem = dataSource.items[index];
        }
        let itemData = dataSourceItem;
        if (itemData instanceof Element) {
          itemData = this._domElementToItemData(itemData);
        }
        const event = this.dispatch("itemData", {
          itemData: itemData || {},
          index,
        });
        return this.applyFilters("itemData", event.itemData, index);
      }
      /**
       * Get array of gallery DOM elements,
       * based on childSelector and gallery element.
       *
       * @param {HTMLElement} galleryElement
       * @returns {HTMLElement[]}
       */
      _getGalleryDOMElements(galleryElement) {
        var _this$options3, _this$options4;
        if (
          ((_this$options3 = this.options) !== null &&
            _this$options3 !== void 0 &&
            _this$options3.children) ||
          ((_this$options4 = this.options) !== null &&
            _this$options4 !== void 0 &&
            _this$options4.childSelector)
        ) {
          return (
            getElementsFromOption(
              this.options.children,
              this.options.childSelector,
              galleryElement,
            ) || []
          );
        }
        return [galleryElement];
      }
      /**
       * Converts DOM element to item data object.
       *
       * @param {HTMLElement} element DOM element
       * @returns {SlideData}
       */
      _domElementToItemData(element) {
        const itemData = {
          element,
        };
        const linkEl =
          /** @type {HTMLAnchorElement} */
          element.tagName === "A" ? element : element.querySelector("a");
        if (linkEl) {
          itemData.src = linkEl.dataset.pswpSrc || linkEl.href;
          if (linkEl.dataset.pswpSrcset) {
            itemData.srcset = linkEl.dataset.pswpSrcset;
          }
          itemData.width = linkEl.dataset.pswpWidth
            ? parseInt(linkEl.dataset.pswpWidth, 10)
            : 0;
          itemData.height = linkEl.dataset.pswpHeight
            ? parseInt(linkEl.dataset.pswpHeight, 10)
            : 0;
          itemData.w = itemData.width;
          itemData.h = itemData.height;
          if (linkEl.dataset.pswpType) {
            itemData.type = linkEl.dataset.pswpType;
          }
          const thumbnailEl = element.querySelector("img");
          if (thumbnailEl) {
            var _thumbnailEl$getAttri;
            itemData.msrc = thumbnailEl.currentSrc || thumbnailEl.src;
            itemData.alt =
              (_thumbnailEl$getAttri = thumbnailEl.getAttribute("alt")) !==
                null && _thumbnailEl$getAttri !== void 0
                ? _thumbnailEl$getAttri
                : "";
          }
          if (linkEl.dataset.pswpCropped || linkEl.dataset.cropped) {
            itemData.thumbCropped = true;
          }
        }
        return this.applyFilters("domItemData", itemData, element, linkEl);
      }
      /**
       * Lazy-load by slide data
       *
       * @param {SlideData} itemData Data about the slide
       * @param {number} index
       * @returns {Content} Image that is being decoded or false.
       */
      lazyLoadData(itemData, index) {
        return lazyLoadData(itemData, this, index);
      }
    };
    PhotoSwipeLightbox = class extends PhotoSwipeBase {
      /**
       * @param {PhotoSwipeOptions} [options]
       */
      constructor(options) {
        super();
        this.options = options || {};
        this._uid = 0;
        this.shouldOpen = false;
        this._preloadedContent = void 0;
        this.onThumbnailsClick = this.onThumbnailsClick.bind(this);
      }
      /**
       * Initialize lightbox, should be called only once.
       * It's not included in the main constructor, so you may bind events before it.
       */
      init() {
        getElementsFromOption(
          this.options.gallery,
          this.options.gallerySelector,
        ).forEach((galleryElement) => {
          galleryElement.addEventListener(
            "click",
            this.onThumbnailsClick,
            false,
          );
        });
      }
      /**
       * @param {MouseEvent} e
       */
      onThumbnailsClick(e) {
        if (specialKeyUsed(e) || window.pswp) {
          return;
        }
        let initialPoint = {
          x: e.clientX,
          y: e.clientY,
        };
        if (!initialPoint.x && !initialPoint.y) {
          initialPoint = null;
        }
        let clickedIndex = this.getClickedIndex(e);
        clickedIndex = this.applyFilters("clickedIndex", clickedIndex, e, this);
        const dataSource = {
          gallery:
            /** @type {HTMLElement} */
            e.currentTarget,
        };
        if (clickedIndex >= 0) {
          e.preventDefault();
          this.loadAndOpen(clickedIndex, dataSource, initialPoint);
        }
      }
      /**
       * Get index of gallery item that was clicked.
       *
       * @param {MouseEvent} e click event
       * @returns {number}
       */
      getClickedIndex(e) {
        if (this.options.getClickedIndexFn) {
          return this.options.getClickedIndexFn.call(this, e);
        }
        const clickedTarget =
          /** @type {HTMLElement} */
          e.target;
        const childElements = getElementsFromOption(
          this.options.children,
          this.options.childSelector,
          /** @type {HTMLElement} */
          e.currentTarget,
        );
        const clickedChildIndex = childElements.findIndex(
          (child) => child === clickedTarget || child.contains(clickedTarget),
        );
        if (clickedChildIndex !== -1) {
          return clickedChildIndex;
        } else if (this.options.children || this.options.childSelector) {
          return -1;
        }
        return 0;
      }
      /**
       * Load and open PhotoSwipe
       *
       * @param {number} index
       * @param {DataSource} [dataSource]
       * @param {Point | null} [initialPoint]
       * @returns {boolean}
       */
      loadAndOpen(index, dataSource, initialPoint) {
        if (window.pswp || !this.options) {
          return false;
        }
        if (!dataSource && this.options.gallery && this.options.children) {
          const galleryElements = getElementsFromOption(this.options.gallery);
          if (galleryElements[0]) {
            dataSource = {
              gallery: galleryElements[0],
            };
          }
        }
        this.options.index = index;
        this.options.initialPointerPos = initialPoint;
        this.shouldOpen = true;
        this.preload(index, dataSource);
        return true;
      }
      /**
       * Load the main module and the slide content by index
       *
       * @param {number} index
       * @param {DataSource} [dataSource]
       */
      preload(index, dataSource) {
        const { options } = this;
        if (dataSource) {
          options.dataSource = dataSource;
        }
        const promiseArray = [];
        const pswpModuleType = typeof options.pswpModule;
        if (isPswpClass(options.pswpModule)) {
          promiseArray.push(
            Promise.resolve(
              /** @type {Type<PhotoSwipe>} */
              options.pswpModule,
            ),
          );
        } else if (pswpModuleType === "string") {
          throw new Error("pswpModule as string is no longer supported");
        } else if (pswpModuleType === "function") {
          promiseArray.push(
            /** @type {() => Promise<Type<PhotoSwipe>>} */
            options.pswpModule(),
          );
        } else {
          throw new Error("pswpModule is not valid");
        }
        if (typeof options.openPromise === "function") {
          promiseArray.push(options.openPromise());
        }
        if (options.preloadFirstSlide !== false && index >= 0) {
          this._preloadedContent = lazyLoadSlide(index, this);
        }
        const uid = ++this._uid;
        Promise.all(promiseArray).then((iterableModules) => {
          if (this.shouldOpen) {
            const mainModule = iterableModules[0];
            this._openPhotoswipe(mainModule, uid);
          }
        });
      }
      /**
       * @private
       * @param {Type<PhotoSwipe> | { default: Type<PhotoSwipe> }} module
       * @param {number} uid
       */
      _openPhotoswipe(module2, uid) {
        if (uid !== this._uid && this.shouldOpen) {
          return;
        }
        this.shouldOpen = false;
        if (window.pswp) {
          return;
        }
        const pswp =
          typeof module2 === "object"
            ? new module2.default(this.options)
            : new module2(this.options);
        this.pswp = pswp;
        window.pswp = pswp;
        Object.keys(this._listeners).forEach((name) => {
          var _this$_listeners$name;
          (_this$_listeners$name = this._listeners[name]) === null ||
            _this$_listeners$name === void 0 ||
            _this$_listeners$name.forEach((fn2) => {
              pswp.on(
                name,
                /** @type {EventCallback<typeof name>} */
                fn2,
              );
            });
        });
        Object.keys(this._filters).forEach((name) => {
          var _this$_filters$name;
          (_this$_filters$name = this._filters[name]) === null ||
            _this$_filters$name === void 0 ||
            _this$_filters$name.forEach((filter) => {
              pswp.addFilter(name, filter.fn, filter.priority);
            });
        });
        if (this._preloadedContent) {
          pswp.contentLoader.addToCache(this._preloadedContent);
          this._preloadedContent = void 0;
        }
        pswp.on("destroy", () => {
          this.pswp = void 0;
          delete window.pswp;
        });
        pswp.init();
      }
      /**
       * Unbinds all events, closes PhotoSwipe if it's open.
       */
      destroy() {
        var _this$pswp;
        (_this$pswp = this.pswp) === null ||
          _this$pswp === void 0 ||
          _this$pswp.destroy();
        this.shouldOpen = false;
        this._listeners = {};
        getElementsFromOption(
          this.options.gallery,
          this.options.gallerySelector,
        ).forEach((galleryElement) => {
          galleryElement.removeEventListener(
            "click",
            this.onThumbnailsClick,
            false,
          );
        });
      }
    };
  },
});

// node_modules/.pnpm/photoswipe@5.4.4/node_modules/photoswipe/dist/photoswipe.esm.js
var photoswipe_esm_exports = {};
__export(photoswipe_esm_exports, {
  default: () => PhotoSwipe,
});
function createElement5(className, tagName, appendToEl) {
  const el = document.createElement(tagName);
  if (className) {
    el.className = className;
  }
  if (appendToEl) {
    appendToEl.appendChild(el);
  }
  return el;
}
function equalizePoints(p1, p2) {
  p1.x = p2.x;
  p1.y = p2.y;
  if (p2.id !== void 0) {
    p1.id = p2.id;
  }
  return p1;
}
function roundPoint(p) {
  p.x = Math.round(p.x);
  p.y = Math.round(p.y);
}
function getDistanceBetween(p1, p2) {
  const x = Math.abs(p1.x - p2.x);
  const y = Math.abs(p1.y - p2.y);
  return Math.sqrt(x * x + y * y);
}
function pointsEqual(p1, p2) {
  return p1.x === p2.x && p1.y === p2.y;
}
function clamp2(val, min2, max2) {
  return Math.min(Math.max(val, min2), max2);
}
function toTransformString2(x, y, scale) {
  let propValue = `translate3d(${x}px,${y || 0}px,0)`;
  if (scale !== void 0) {
    propValue += ` scale3d(${scale},${scale},1)`;
  }
  return propValue;
}
function setTransform(el, x, y, scale) {
  el.style.transform = toTransformString2(x, y, scale);
}
function setTransitionStyle(el, prop, duration2, ease) {
  el.style.transition = prop
    ? `${prop} ${duration2}ms ${ease || defaultCSSEasing}`
    : "none";
}
function setWidthHeight2(el, w, h) {
  el.style.width = typeof w === "number" ? `${w}px` : w;
  el.style.height = typeof h === "number" ? `${h}px` : h;
}
function removeTransitionStyle(el) {
  setTransitionStyle(el);
}
function decodeImage(img) {
  if ("decode" in img) {
    return img.decode().catch(() => {});
  }
  if (img.complete) {
    return Promise.resolve(img);
  }
  return new Promise((resolve, reject) => {
    img.onload = () => resolve(img);
    img.onerror = reject;
  });
}
function specialKeyUsed2(e) {
  return (
    ("button" in e && e.button === 1) ||
    e.ctrlKey ||
    e.metaKey ||
    e.altKey ||
    e.shiftKey
  );
}
function getElementsFromOption2(option, legacySelector, parent = document) {
  let elements = [];
  if (option instanceof Element) {
    elements = [option];
  } else if (option instanceof NodeList || Array.isArray(option)) {
    elements = Array.from(option);
  } else {
    const selector = typeof option === "string" ? option : legacySelector;
    if (selector) {
      elements = Array.from(parent.querySelectorAll(selector));
    }
  }
  return elements;
}
function isSafari2() {
  return !!(navigator.vendor && navigator.vendor.match(/apple/i));
}
function getViewportSize2(options, pswp) {
  if (options.getViewportSizeFn) {
    const newViewportSize = options.getViewportSizeFn(options, pswp);
    if (newViewportSize) {
      return newViewportSize;
    }
  }
  return {
    x: document.documentElement.clientWidth,
    // TODO: height on mobile is very incosistent due to toolbar
    // find a way to improve this
    //
    // document.documentElement.clientHeight - doesn't seem to work well
    y: window.innerHeight,
  };
}
function parsePaddingOption2(prop, options, viewportSize, itemData, index) {
  let paddingValue = 0;
  if (options.paddingFn) {
    paddingValue = options.paddingFn(viewportSize, itemData, index)[prop];
  } else if (options.padding) {
    paddingValue = options.padding[prop];
  } else {
    const legacyPropName = "padding" + prop[0].toUpperCase() + prop.slice(1);
    if (options[legacyPropName]) {
      paddingValue = options[legacyPropName];
    }
  }
  return Number(paddingValue) || 0;
}
function getPanAreaSize2(options, viewportSize, itemData, index) {
  return {
    x:
      viewportSize.x -
      parsePaddingOption2("left", options, viewportSize, itemData, index) -
      parsePaddingOption2("right", options, viewportSize, itemData, index),
    y:
      viewportSize.y -
      parsePaddingOption2("top", options, viewportSize, itemData, index) -
      parsePaddingOption2("bottom", options, viewportSize, itemData, index),
  };
}
function project(initialVelocity, decelerationRate) {
  return (initialVelocity * decelerationRate) / (1 - decelerationRate);
}
function getZoomPointsCenter(p, p1, p2) {
  p.x = (p1.x + p2.x) / 2;
  p.y = (p1.y + p2.y) / 2;
  return p;
}
function didTapOnMainContent(event) {
  return !!(
    /** @type {HTMLElement} */
    event.target.closest(".pswp__container")
  );
}
function addElementHTML(htmlData) {
  if (typeof htmlData === "string") {
    return htmlData;
  }
  if (!htmlData || !htmlData.isCustomSVG) {
    return "";
  }
  const svgData = htmlData;
  let out =
    '<svg aria-hidden="true" class="pswp__icn" viewBox="0 0 %d %d" width="%d" height="%d">';
  out = out.split("%d").join(
    /** @type {string} */
    svgData.size || 32,
  );
  if (svgData.outlineID) {
    out +=
      '<use class="pswp__icn-shadow" xlink:href="#' + svgData.outlineID + '"/>';
  }
  out += svgData.inner;
  out += "</svg>";
  return out;
}
function initArrowButton(element, pswp, isNextButton) {
  element.classList.add("pswp__button--arrow");
  element.setAttribute("aria-controls", "pswp__items");
  pswp.on("change", () => {
    if (!pswp.options.loop) {
      if (isNextButton) {
        element.disabled = !(pswp.currIndex < pswp.getNumItems() - 1);
      } else {
        element.disabled = !(pswp.currIndex > 0);
      }
    }
  });
}
function setZoomedIn(el, isZoomedIn) {
  el.classList.toggle("pswp--zoomed-in", isZoomedIn);
}
function getBoundsByElement(el) {
  const thumbAreaRect = el.getBoundingClientRect();
  return {
    x: thumbAreaRect.left,
    y: thumbAreaRect.top,
    w: thumbAreaRect.width,
  };
}
function getCroppedBoundsByElement(el, imageWidth, imageHeight) {
  const thumbAreaRect = el.getBoundingClientRect();
  const hRatio = thumbAreaRect.width / imageWidth;
  const vRatio = thumbAreaRect.height / imageHeight;
  const fillZoomLevel = hRatio > vRatio ? hRatio : vRatio;
  const offsetX = (thumbAreaRect.width - imageWidth * fillZoomLevel) / 2;
  const offsetY = (thumbAreaRect.height - imageHeight * fillZoomLevel) / 2;
  const bounds = {
    x: thumbAreaRect.left + offsetX,
    y: thumbAreaRect.top + offsetY,
    w: imageWidth * fillZoomLevel,
  };
  bounds.innerRect = {
    w: thumbAreaRect.width,
    h: thumbAreaRect.height,
    x: offsetX,
    y: offsetY,
  };
  return bounds;
}
function getThumbBounds(index, itemData, instance) {
  const event = instance.dispatch("thumbBounds", {
    index,
    itemData,
    instance,
  });
  if (event.thumbBounds) {
    return event.thumbBounds;
  }
  const { element } = itemData;
  let thumbBounds;
  let thumbnail;
  if (element && instance.options.thumbSelector !== false) {
    const thumbSelector = instance.options.thumbSelector || "img";
    thumbnail = element.matches(thumbSelector)
      ? element
      : /** @type {HTMLElement | null} */
        element.querySelector(thumbSelector);
  }
  thumbnail = instance.applyFilters("thumbEl", thumbnail, itemData, index);
  if (thumbnail) {
    if (!itemData.thumbCropped) {
      thumbBounds = getBoundsByElement(thumbnail);
    } else {
      thumbBounds = getCroppedBoundsByElement(
        thumbnail,
        itemData.width || itemData.w || 0,
        itemData.height || itemData.h || 0,
      );
    }
  }
  return instance.applyFilters("thumbBounds", thumbBounds, itemData, index);
}
function lazyLoadData2(itemData, instance, index) {
  const content = instance.createContentFromData(itemData, index);
  let zoomLevel;
  const { options } = instance;
  if (options) {
    zoomLevel = new ZoomLevel2(options, itemData, -1);
    let viewportSize;
    if (instance.pswp) {
      viewportSize = instance.pswp.viewportSize;
    } else {
      viewportSize = getViewportSize2(options, instance);
    }
    const panAreaSize = getPanAreaSize2(options, viewportSize, itemData, index);
    zoomLevel.update(content.width, content.height, panAreaSize);
  }
  content.lazyLoad();
  if (zoomLevel) {
    content.setDisplayedSize(
      Math.ceil(content.width * zoomLevel.initial),
      Math.ceil(content.height * zoomLevel.initial),
    );
  }
  return content;
}
function lazyLoadSlide2(index, instance) {
  const itemData = instance.getItemData(index);
  if (
    instance.dispatch("lazyLoadSlide", {
      index,
      itemData,
    }).defaultPrevented
  ) {
    return;
  }
  return lazyLoadData2(itemData, instance, index);
}
var defaultCSSEasing,
  LOAD_STATE2,
  supportsPassive,
  DOMEvents,
  PanBounds,
  MAX_IMAGE_WIDTH2,
  ZoomLevel2,
  Slide3,
  PAN_END_FRICTION,
  VERTICAL_DRAG_FRICTION,
  MIN_RATIO_TO_CLOSE,
  MIN_NEXT_SLIDE_SPEED,
  DragHandler,
  UPPER_ZOOM_FRICTION,
  LOWER_ZOOM_FRICTION,
  ZoomHandler,
  TapHandler,
  AXIS_SWIPE_HYSTERISIS,
  DOUBLE_TAP_DELAY,
  MIN_TAP_DISTANCE,
  Gestures,
  MAIN_SCROLL_END_FRICTION,
  MainScroll,
  KeyboardKeyCodesMap,
  getKeyboardEventKey,
  Keyboard,
  DEFAULT_EASING,
  CSSAnimation,
  DEFAULT_NATURAL_FREQUENCY,
  DEFAULT_DAMPING_RATIO,
  SpringEaser,
  SpringAnimation,
  Animations,
  ScrollWheel,
  UIElement,
  arrowPrev,
  arrowNext,
  closeButton,
  zoomButton,
  loadingIndicator,
  counterIndicator,
  UI,
  PhotoSwipeEvent2,
  Eventable2,
  Placeholder2,
  Content2,
  MIN_SLIDES_TO_CACHE,
  ContentLoader,
  PhotoSwipeBase2,
  MIN_OPACITY,
  Opener,
  defaultOptions,
  PhotoSwipe;
var init_photoswipe_esm = __esm({
  "node_modules/.pnpm/photoswipe@5.4.4/node_modules/photoswipe/dist/photoswipe.esm.js"() {
    defaultCSSEasing = "cubic-bezier(.4,0,.22,1)";
    LOAD_STATE2 = {
      IDLE: "idle",
      LOADING: "loading",
      LOADED: "loaded",
      ERROR: "error",
    };
    supportsPassive = false;
    try {
      window.addEventListener(
        "test",
        null,
        Object.defineProperty({}, "passive", {
          get: () => {
            supportsPassive = true;
          },
        }),
      );
    } catch (e) {}
    DOMEvents = class {
      constructor() {
        this._pool = [];
      }
      /**
       * Adds event listeners
       *
       * @param {PoolItem['target']} target
       * @param {PoolItem['type']} type Can be multiple, separated by space.
       * @param {PoolItem['listener']} listener
       * @param {PoolItem['passive']} [passive]
       */
      add(target, type, listener, passive2) {
        this._toggleListener(target, type, listener, passive2);
      }
      /**
       * Removes event listeners
       *
       * @param {PoolItem['target']} target
       * @param {PoolItem['type']} type
       * @param {PoolItem['listener']} listener
       * @param {PoolItem['passive']} [passive]
       */
      remove(target, type, listener, passive2) {
        this._toggleListener(target, type, listener, passive2, true);
      }
      /**
       * Removes all bound events
       */
      removeAll() {
        this._pool.forEach((poolItem) => {
          this._toggleListener(
            poolItem.target,
            poolItem.type,
            poolItem.listener,
            poolItem.passive,
            true,
            true,
          );
        });
        this._pool = [];
      }
      /**
       * Adds or removes event
       *
       * @private
       * @param {PoolItem['target']} target
       * @param {PoolItem['type']} type
       * @param {PoolItem['listener']} listener
       * @param {PoolItem['passive']} [passive]
       * @param {boolean} [unbind] Whether the event should be added or removed
       * @param {boolean} [skipPool] Whether events pool should be skipped
       */
      _toggleListener(target, type, listener, passive2, unbind, skipPool) {
        if (!target) {
          return;
        }
        const methodName = unbind ? "removeEventListener" : "addEventListener";
        const types = type.split(" ");
        types.forEach((eType) => {
          if (eType) {
            if (!skipPool) {
              if (unbind) {
                this._pool = this._pool.filter((poolItem) => {
                  return (
                    poolItem.type !== eType ||
                    poolItem.listener !== listener ||
                    poolItem.target !== target
                  );
                });
              } else {
                this._pool.push({
                  target,
                  type: eType,
                  listener,
                  passive: passive2,
                });
              }
            }
            const eventOptions = supportsPassive
              ? {
                  passive: passive2 || false,
                }
              : false;
            target[methodName](eType, listener, eventOptions);
          }
        });
      }
    };
    PanBounds = class {
      /**
       * @param {Slide} slide
       */
      constructor(slide) {
        this.slide = slide;
        this.currZoomLevel = 1;
        this.center =
          /** @type {Point} */
          {
            x: 0,
            y: 0,
          };
        this.max =
          /** @type {Point} */
          {
            x: 0,
            y: 0,
          };
        this.min =
          /** @type {Point} */
          {
            x: 0,
            y: 0,
          };
      }
      /**
       * _getItemBounds
       *
       * @param {number} currZoomLevel
       */
      update(currZoomLevel) {
        this.currZoomLevel = currZoomLevel;
        if (!this.slide.width) {
          this.reset();
        } else {
          this._updateAxis("x");
          this._updateAxis("y");
          this.slide.pswp.dispatch("calcBounds", {
            slide: this.slide,
          });
        }
      }
      /**
       * _calculateItemBoundsForAxis
       *
       * @param {Axis} axis
       */
      _updateAxis(axis) {
        const { pswp } = this.slide;
        const elSize =
          this.slide[axis === "x" ? "width" : "height"] * this.currZoomLevel;
        const paddingProp = axis === "x" ? "left" : "top";
        const padding2 = parsePaddingOption2(
          paddingProp,
          pswp.options,
          pswp.viewportSize,
          this.slide.data,
          this.slide.index,
        );
        const panAreaSize = this.slide.panAreaSize[axis];
        this.center[axis] = Math.round((panAreaSize - elSize) / 2) + padding2;
        this.max[axis] =
          elSize > panAreaSize
            ? Math.round(panAreaSize - elSize) + padding2
            : this.center[axis];
        this.min[axis] = elSize > panAreaSize ? padding2 : this.center[axis];
      }
      // _getZeroBounds
      reset() {
        this.center.x = 0;
        this.center.y = 0;
        this.max.x = 0;
        this.max.y = 0;
        this.min.x = 0;
        this.min.y = 0;
      }
      /**
       * Correct pan position if it's beyond the bounds
       *
       * @param {Axis} axis x or y
       * @param {number} panOffset
       * @returns {number}
       */
      correctPan(axis, panOffset) {
        return clamp2(panOffset, this.max[axis], this.min[axis]);
      }
    };
    MAX_IMAGE_WIDTH2 = 4e3;
    ZoomLevel2 = class {
      /**
       * @param {PhotoSwipeOptions} options PhotoSwipe options
       * @param {SlideData} itemData Slide data
       * @param {number} index Slide index
       * @param {PhotoSwipe} [pswp] PhotoSwipe instance, can be undefined if not initialized yet
       */
      constructor(options, itemData, index, pswp) {
        this.pswp = pswp;
        this.options = options;
        this.itemData = itemData;
        this.index = index;
        this.panAreaSize = null;
        this.elementSize = null;
        this.fit = 1;
        this.fill = 1;
        this.vFill = 1;
        this.initial = 1;
        this.secondary = 1;
        this.max = 1;
        this.min = 1;
      }
      /**
       * Calculate initial, secondary and maximum zoom level for the specified slide.
       *
       * It should be called when either image or viewport size changes.
       *
       * @param {number} maxWidth
       * @param {number} maxHeight
       * @param {Point} panAreaSize
       */
      update(maxWidth2, maxHeight2, panAreaSize) {
        const elementSize = {
          x: maxWidth2,
          y: maxHeight2,
        };
        this.elementSize = elementSize;
        this.panAreaSize = panAreaSize;
        const hRatio = panAreaSize.x / elementSize.x;
        const vRatio = panAreaSize.y / elementSize.y;
        this.fit = Math.min(1, hRatio < vRatio ? hRatio : vRatio);
        this.fill = Math.min(1, hRatio > vRatio ? hRatio : vRatio);
        this.vFill = Math.min(1, vRatio);
        this.initial = this._getInitial();
        this.secondary = this._getSecondary();
        this.max = Math.max(this.initial, this.secondary, this._getMax());
        this.min = Math.min(this.fit, this.initial, this.secondary);
        if (this.pswp) {
          this.pswp.dispatch("zoomLevelsUpdate", {
            zoomLevels: this,
            slideData: this.itemData,
          });
        }
      }
      /**
       * Parses user-defined zoom option.
       *
       * @private
       * @param {'initial' | 'secondary' | 'max'} optionPrefix Zoom level option prefix (initial, secondary, max)
       * @returns { number | undefined }
       */
      _parseZoomLevelOption(optionPrefix) {
        const optionName =
          /** @type {'initialZoomLevel' | 'secondaryZoomLevel' | 'maxZoomLevel'} */
          optionPrefix + "ZoomLevel";
        const optionValue = this.options[optionName];
        if (!optionValue) {
          return;
        }
        if (typeof optionValue === "function") {
          return optionValue(this);
        }
        if (optionValue === "fill") {
          return this.fill;
        }
        if (optionValue === "fit") {
          return this.fit;
        }
        return Number(optionValue);
      }
      /**
       * Get zoom level to which image will be zoomed after double-tap gesture,
       * or when user clicks on zoom icon,
       * or mouse-click on image itself.
       * If you return 1 image will be zoomed to its original size.
       *
       * @private
       * @return {number}
       */
      _getSecondary() {
        let currZoomLevel = this._parseZoomLevelOption("secondary");
        if (currZoomLevel) {
          return currZoomLevel;
        }
        currZoomLevel = Math.min(1, this.fit * 3);
        if (
          this.elementSize &&
          currZoomLevel * this.elementSize.x > MAX_IMAGE_WIDTH2
        ) {
          currZoomLevel = MAX_IMAGE_WIDTH2 / this.elementSize.x;
        }
        return currZoomLevel;
      }
      /**
       * Get initial image zoom level.
       *
       * @private
       * @return {number}
       */
      _getInitial() {
        return this._parseZoomLevelOption("initial") || this.fit;
      }
      /**
       * Maximum zoom level when user zooms
       * via zoom/pinch gesture,
       * via cmd/ctrl-wheel or via trackpad.
       *
       * @private
       * @return {number}
       */
      _getMax() {
        return this._parseZoomLevelOption("max") || Math.max(1, this.fit * 4);
      }
    };
    Slide3 = class {
      /**
       * @param {SlideData} data
       * @param {number} index
       * @param {PhotoSwipe} pswp
       */
      constructor(data, index, pswp) {
        this.data = data;
        this.index = index;
        this.pswp = pswp;
        this.isActive = index === pswp.currIndex;
        this.currentResolution = 0;
        this.panAreaSize = {
          x: 0,
          y: 0,
        };
        this.pan = {
          x: 0,
          y: 0,
        };
        this.isFirstSlide = this.isActive && !pswp.opener.isOpen;
        this.zoomLevels = new ZoomLevel2(pswp.options, data, index, pswp);
        this.pswp.dispatch("gettingData", {
          slide: this,
          data: this.data,
          index,
        });
        this.content = this.pswp.contentLoader.getContentBySlide(this);
        this.container = createElement5("pswp__zoom-wrap", "div");
        this.holderElement = null;
        this.currZoomLevel = 1;
        this.width = this.content.width;
        this.height = this.content.height;
        this.heavyAppended = false;
        this.bounds = new PanBounds(this);
        this.prevDisplayedWidth = -1;
        this.prevDisplayedHeight = -1;
        this.pswp.dispatch("slideInit", {
          slide: this,
        });
      }
      /**
       * If this slide is active/current/visible
       *
       * @param {boolean} isActive
       */
      setIsActive(isActive) {
        if (isActive && !this.isActive) {
          this.activate();
        } else if (!isActive && this.isActive) {
          this.deactivate();
        }
      }
      /**
       * Appends slide content to DOM
       *
       * @param {HTMLElement} holderElement
       */
      append(holderElement) {
        this.holderElement = holderElement;
        this.container.style.transformOrigin = "0 0";
        if (!this.data) {
          return;
        }
        this.calculateSize();
        this.load();
        this.updateContentSize();
        this.appendHeavy();
        this.holderElement.appendChild(this.container);
        this.zoomAndPanToInitial();
        this.pswp.dispatch("firstZoomPan", {
          slide: this,
        });
        this.applyCurrentZoomPan();
        this.pswp.dispatch("afterSetContent", {
          slide: this,
        });
        if (this.isActive) {
          this.activate();
        }
      }
      load() {
        this.content.load(false);
        this.pswp.dispatch("slideLoad", {
          slide: this,
        });
      }
      /**
       * Append "heavy" DOM elements
       *
       * This may depend on a type of slide,
       * but generally these are large images.
       */
      appendHeavy() {
        const { pswp } = this;
        const appendHeavyNearby = true;
        if (
          this.heavyAppended ||
          !pswp.opener.isOpen ||
          pswp.mainScroll.isShifted() ||
          (!this.isActive && !appendHeavyNearby)
        ) {
          return;
        }
        if (
          this.pswp.dispatch("appendHeavy", {
            slide: this,
          }).defaultPrevented
        ) {
          return;
        }
        this.heavyAppended = true;
        this.content.append();
        this.pswp.dispatch("appendHeavyContent", {
          slide: this,
        });
      }
      /**
       * Triggered when this slide is active (selected).
       *
       * If it's part of opening/closing transition -
       * activate() will trigger after the transition is ended.
       */
      activate() {
        this.isActive = true;
        this.appendHeavy();
        this.content.activate();
        this.pswp.dispatch("slideActivate", {
          slide: this,
        });
      }
      /**
       * Triggered when this slide becomes inactive.
       *
       * Slide can become inactive only after it was active.
       */
      deactivate() {
        this.isActive = false;
        this.content.deactivate();
        if (this.currZoomLevel !== this.zoomLevels.initial) {
          this.calculateSize();
        }
        this.currentResolution = 0;
        this.zoomAndPanToInitial();
        this.applyCurrentZoomPan();
        this.updateContentSize();
        this.pswp.dispatch("slideDeactivate", {
          slide: this,
        });
      }
      /**
       * The slide should destroy itself, it will never be used again.
       * (unbind all events and destroy internal components)
       */
      destroy() {
        this.content.hasSlide = false;
        this.content.remove();
        this.container.remove();
        this.pswp.dispatch("slideDestroy", {
          slide: this,
        });
      }
      resize() {
        if (this.currZoomLevel === this.zoomLevels.initial || !this.isActive) {
          this.calculateSize();
          this.currentResolution = 0;
          this.zoomAndPanToInitial();
          this.applyCurrentZoomPan();
          this.updateContentSize();
        } else {
          this.calculateSize();
          this.bounds.update(this.currZoomLevel);
          this.panTo(this.pan.x, this.pan.y);
        }
      }
      /**
       * Apply size to current slide content,
       * based on the current resolution and scale.
       *
       * @param {boolean} [force] if size should be updated even if dimensions weren't changed
       */
      updateContentSize(force) {
        const scaleMultiplier =
          this.currentResolution || this.zoomLevels.initial;
        if (!scaleMultiplier) {
          return;
        }
        const width2 =
          Math.round(this.width * scaleMultiplier) || this.pswp.viewportSize.x;
        const height2 =
          Math.round(this.height * scaleMultiplier) || this.pswp.viewportSize.y;
        if (!this.sizeChanged(width2, height2) && !force) {
          return;
        }
        this.content.setDisplayedSize(width2, height2);
      }
      /**
       * @param {number} width
       * @param {number} height
       */
      sizeChanged(width2, height2) {
        if (
          width2 !== this.prevDisplayedWidth ||
          height2 !== this.prevDisplayedHeight
        ) {
          this.prevDisplayedWidth = width2;
          this.prevDisplayedHeight = height2;
          return true;
        }
        return false;
      }
      /** @returns {HTMLImageElement | HTMLDivElement | null | undefined} */
      getPlaceholderElement() {
        var _this$content$placeho;
        return (_this$content$placeho = this.content.placeholder) === null ||
          _this$content$placeho === void 0
          ? void 0
          : _this$content$placeho.element;
      }
      /**
       * Zoom current slide image to...
       *
       * @param {number} destZoomLevel Destination zoom level.
       * @param {Point} [centerPoint]
       * Transform origin center point, or false if viewport center should be used.
       * @param {number | false} [transitionDuration] Transition duration, may be set to 0.
       * @param {boolean} [ignoreBounds] Minimum and maximum zoom levels will be ignored.
       */
      zoomTo(destZoomLevel, centerPoint, transitionDuration, ignoreBounds) {
        const { pswp } = this;
        if (!this.isZoomable() || pswp.mainScroll.isShifted()) {
          return;
        }
        pswp.dispatch("beforeZoomTo", {
          destZoomLevel,
          centerPoint,
          transitionDuration,
        });
        pswp.animations.stopAllPan();
        const prevZoomLevel = this.currZoomLevel;
        if (!ignoreBounds) {
          destZoomLevel = clamp2(
            destZoomLevel,
            this.zoomLevels.min,
            this.zoomLevels.max,
          );
        }
        this.setZoomLevel(destZoomLevel);
        this.pan.x = this.calculateZoomToPanOffset(
          "x",
          centerPoint,
          prevZoomLevel,
        );
        this.pan.y = this.calculateZoomToPanOffset(
          "y",
          centerPoint,
          prevZoomLevel,
        );
        roundPoint(this.pan);
        const finishTransition = () => {
          this._setResolution(destZoomLevel);
          this.applyCurrentZoomPan();
        };
        if (!transitionDuration) {
          finishTransition();
        } else {
          pswp.animations.startTransition({
            isPan: true,
            name: "zoomTo",
            target: this.container,
            transform: this.getCurrentTransform(),
            onComplete: finishTransition,
            duration: transitionDuration,
            easing: pswp.options.easing,
          });
        }
      }
      /**
       * @param {Point} [centerPoint]
       */
      toggleZoom(centerPoint) {
        this.zoomTo(
          this.currZoomLevel === this.zoomLevels.initial
            ? this.zoomLevels.secondary
            : this.zoomLevels.initial,
          centerPoint,
          this.pswp.options.zoomAnimationDuration,
        );
      }
      /**
       * Updates zoom level property and recalculates new pan bounds,
       * unlike zoomTo it does not apply transform (use applyCurrentZoomPan)
       *
       * @param {number} currZoomLevel
       */
      setZoomLevel(currZoomLevel) {
        this.currZoomLevel = currZoomLevel;
        this.bounds.update(this.currZoomLevel);
      }
      /**
       * Get pan position after zoom at a given `point`.
       *
       * Always call setZoomLevel(newZoomLevel) beforehand to recalculate
       * pan bounds according to the new zoom level.
       *
       * @param {'x' | 'y'} axis
       * @param {Point} [point]
       * point based on which zoom is performed, usually refers to the current mouse position,
       * if false - viewport center will be used.
       * @param {number} [prevZoomLevel] Zoom level before new zoom was applied.
       * @returns {number}
       */
      calculateZoomToPanOffset(axis, point, prevZoomLevel) {
        const totalPanDistance = this.bounds.max[axis] - this.bounds.min[axis];
        if (totalPanDistance === 0) {
          return this.bounds.center[axis];
        }
        if (!point) {
          point = this.pswp.getViewportCenterPoint();
        }
        if (!prevZoomLevel) {
          prevZoomLevel = this.zoomLevels.initial;
        }
        const zoomFactor = this.currZoomLevel / prevZoomLevel;
        return this.bounds.correctPan(
          axis,
          (this.pan[axis] - point[axis]) * zoomFactor + point[axis],
        );
      }
      /**
       * Apply pan and keep it within bounds.
       *
       * @param {number} panX
       * @param {number} panY
       */
      panTo(panX, panY) {
        this.pan.x = this.bounds.correctPan("x", panX);
        this.pan.y = this.bounds.correctPan("y", panY);
        this.applyCurrentZoomPan();
      }
      /**
       * If the slide in the current state can be panned by the user
       * @returns {boolean}
       */
      isPannable() {
        return Boolean(this.width) && this.currZoomLevel > this.zoomLevels.fit;
      }
      /**
       * If the slide can be zoomed
       * @returns {boolean}
       */
      isZoomable() {
        return Boolean(this.width) && this.content.isZoomable();
      }
      /**
       * Apply transform and scale based on
       * the current pan position (this.pan) and zoom level (this.currZoomLevel)
       */
      applyCurrentZoomPan() {
        this._applyZoomTransform(this.pan.x, this.pan.y, this.currZoomLevel);
        if (this === this.pswp.currSlide) {
          this.pswp.dispatch("zoomPanUpdate", {
            slide: this,
          });
        }
      }
      zoomAndPanToInitial() {
        this.currZoomLevel = this.zoomLevels.initial;
        this.bounds.update(this.currZoomLevel);
        equalizePoints(this.pan, this.bounds.center);
        this.pswp.dispatch("initialZoomPan", {
          slide: this,
        });
      }
      /**
       * Set translate and scale based on current resolution
       *
       * @param {number} x
       * @param {number} y
       * @param {number} zoom
       * @private
       */
      _applyZoomTransform(x, y, zoom) {
        zoom /= this.currentResolution || this.zoomLevels.initial;
        setTransform(this.container, x, y, zoom);
      }
      calculateSize() {
        const { pswp } = this;
        equalizePoints(
          this.panAreaSize,
          getPanAreaSize2(
            pswp.options,
            pswp.viewportSize,
            this.data,
            this.index,
          ),
        );
        this.zoomLevels.update(this.width, this.height, this.panAreaSize);
        pswp.dispatch("calcSlideSize", {
          slide: this,
        });
      }
      /** @returns {string} */
      getCurrentTransform() {
        const scale =
          this.currZoomLevel /
          (this.currentResolution || this.zoomLevels.initial);
        return toTransformString2(this.pan.x, this.pan.y, scale);
      }
      /**
       * Set resolution and re-render the image.
       *
       * For example, if the real image size is 2000x1500,
       * and resolution is 0.5 - it will be rendered as 1000x750.
       *
       * Image with zoom level 2 and resolution 0.5 is
       * the same as image with zoom level 1 and resolution 1.
       *
       * Used to optimize animations and make
       * sure that browser renders image in the highest quality.
       * Also used by responsive images to load the correct one.
       *
       * @param {number} newResolution
       */
      _setResolution(newResolution) {
        if (newResolution === this.currentResolution) {
          return;
        }
        this.currentResolution = newResolution;
        this.updateContentSize();
        this.pswp.dispatch("resolutionChanged");
      }
    };
    PAN_END_FRICTION = 0.35;
    VERTICAL_DRAG_FRICTION = 0.6;
    MIN_RATIO_TO_CLOSE = 0.4;
    MIN_NEXT_SLIDE_SPEED = 0.5;
    DragHandler = class {
      /**
       * @param {Gestures} gestures
       */
      constructor(gestures) {
        this.gestures = gestures;
        this.pswp = gestures.pswp;
        this.startPan = {
          x: 0,
          y: 0,
        };
      }
      start() {
        if (this.pswp.currSlide) {
          equalizePoints(this.startPan, this.pswp.currSlide.pan);
        }
        this.pswp.animations.stopAll();
      }
      change() {
        const { p1, prevP1, dragAxis } = this.gestures;
        const { currSlide } = this.pswp;
        if (
          dragAxis === "y" &&
          this.pswp.options.closeOnVerticalDrag &&
          currSlide &&
          currSlide.currZoomLevel <= currSlide.zoomLevels.fit &&
          !this.gestures.isMultitouch
        ) {
          const panY = currSlide.pan.y + (p1.y - prevP1.y);
          if (
            !this.pswp.dispatch("verticalDrag", {
              panY,
            }).defaultPrevented
          ) {
            this._setPanWithFriction("y", panY, VERTICAL_DRAG_FRICTION);
            const bgOpacity =
              1 - Math.abs(this._getVerticalDragRatio(currSlide.pan.y));
            this.pswp.applyBgOpacity(bgOpacity);
            currSlide.applyCurrentZoomPan();
          }
        } else {
          const mainScrollChanged = this._panOrMoveMainScroll("x");
          if (!mainScrollChanged) {
            this._panOrMoveMainScroll("y");
            if (currSlide) {
              roundPoint(currSlide.pan);
              currSlide.applyCurrentZoomPan();
            }
          }
        }
      }
      end() {
        const { velocity } = this.gestures;
        const { mainScroll, currSlide } = this.pswp;
        let indexDiff = 0;
        this.pswp.animations.stopAll();
        if (mainScroll.isShifted()) {
          const mainScrollShiftDiff = mainScroll.x - mainScroll.getCurrSlideX();
          const currentSlideVisibilityRatio =
            mainScrollShiftDiff / this.pswp.viewportSize.x;
          if (
            (velocity.x < -MIN_NEXT_SLIDE_SPEED &&
              currentSlideVisibilityRatio < 0) ||
            (velocity.x < 0.1 && currentSlideVisibilityRatio < -0.5)
          ) {
            indexDiff = 1;
            velocity.x = Math.min(velocity.x, 0);
          } else if (
            (velocity.x > MIN_NEXT_SLIDE_SPEED &&
              currentSlideVisibilityRatio > 0) ||
            (velocity.x > -0.1 && currentSlideVisibilityRatio > 0.5)
          ) {
            indexDiff = -1;
            velocity.x = Math.max(velocity.x, 0);
          }
          mainScroll.moveIndexBy(indexDiff, true, velocity.x);
        }
        if (
          (currSlide && currSlide.currZoomLevel > currSlide.zoomLevels.max) ||
          this.gestures.isMultitouch
        ) {
          this.gestures.zoomLevels.correctZoomPan(true);
        } else {
          this._finishPanGestureForAxis("x");
          this._finishPanGestureForAxis("y");
        }
      }
      /**
       * @private
       * @param {'x' | 'y'} axis
       */
      _finishPanGestureForAxis(axis) {
        const { velocity } = this.gestures;
        const { currSlide } = this.pswp;
        if (!currSlide) {
          return;
        }
        const { pan, bounds } = currSlide;
        const panPos = pan[axis];
        const restoreBgOpacity = this.pswp.bgOpacity < 1 && axis === "y";
        const decelerationRate = 0.995;
        const projectedPosition =
          panPos + project(velocity[axis], decelerationRate);
        if (restoreBgOpacity) {
          const vDragRatio = this._getVerticalDragRatio(panPos);
          const projectedVDragRatio =
            this._getVerticalDragRatio(projectedPosition);
          if (
            (vDragRatio < 0 && projectedVDragRatio < -MIN_RATIO_TO_CLOSE) ||
            (vDragRatio > 0 && projectedVDragRatio > MIN_RATIO_TO_CLOSE)
          ) {
            this.pswp.close();
            return;
          }
        }
        const correctedPanPosition = bounds.correctPan(axis, projectedPosition);
        if (panPos === correctedPanPosition) {
          return;
        }
        const dampingRatio =
          correctedPanPosition === projectedPosition ? 1 : 0.82;
        const initialBgOpacity = this.pswp.bgOpacity;
        const totalPanDist = correctedPanPosition - panPos;
        this.pswp.animations.startSpring({
          name: "panGesture" + axis,
          isPan: true,
          start: panPos,
          end: correctedPanPosition,
          velocity: velocity[axis],
          dampingRatio,
          onUpdate: (pos) => {
            if (restoreBgOpacity && this.pswp.bgOpacity < 1) {
              const animationProgressRatio =
                1 - (correctedPanPosition - pos) / totalPanDist;
              this.pswp.applyBgOpacity(
                clamp2(
                  initialBgOpacity +
                    (1 - initialBgOpacity) * animationProgressRatio,
                  0,
                  1,
                ),
              );
            }
            pan[axis] = Math.floor(pos);
            currSlide.applyCurrentZoomPan();
          },
        });
      }
      /**
       * Update position of the main scroll,
       * or/and update pan position of the current slide.
       *
       * Should return true if it changes (or can change) main scroll.
       *
       * @private
       * @param {'x' | 'y'} axis
       * @returns {boolean}
       */
      _panOrMoveMainScroll(axis) {
        const { p1, dragAxis, prevP1, isMultitouch } = this.gestures;
        const { currSlide, mainScroll } = this.pswp;
        const delta = p1[axis] - prevP1[axis];
        const newMainScrollX = mainScroll.x + delta;
        if (!delta || !currSlide) {
          return false;
        }
        if (axis === "x" && !currSlide.isPannable() && !isMultitouch) {
          mainScroll.moveTo(newMainScrollX, true);
          return true;
        }
        const { bounds } = currSlide;
        const newPan = currSlide.pan[axis] + delta;
        if (
          this.pswp.options.allowPanToNext &&
          dragAxis === "x" &&
          axis === "x" &&
          !isMultitouch
        ) {
          const currSlideMainScrollX = mainScroll.getCurrSlideX();
          const mainScrollShiftDiff = mainScroll.x - currSlideMainScrollX;
          const isLeftToRight = delta > 0;
          const isRightToLeft = !isLeftToRight;
          if (newPan > bounds.min[axis] && isLeftToRight) {
            const wasAtMinPanPosition = bounds.min[axis] <= this.startPan[axis];
            if (wasAtMinPanPosition) {
              mainScroll.moveTo(newMainScrollX, true);
              return true;
            } else {
              this._setPanWithFriction(axis, newPan);
            }
          } else if (newPan < bounds.max[axis] && isRightToLeft) {
            const wasAtMaxPanPosition = this.startPan[axis] <= bounds.max[axis];
            if (wasAtMaxPanPosition) {
              mainScroll.moveTo(newMainScrollX, true);
              return true;
            } else {
              this._setPanWithFriction(axis, newPan);
            }
          } else {
            if (mainScrollShiftDiff !== 0) {
              if (mainScrollShiftDiff > 0) {
                mainScroll.moveTo(
                  Math.max(newMainScrollX, currSlideMainScrollX),
                  true,
                );
                return true;
              } else if (mainScrollShiftDiff < 0) {
                mainScroll.moveTo(
                  Math.min(newMainScrollX, currSlideMainScrollX),
                  true,
                );
                return true;
              }
            } else {
              this._setPanWithFriction(axis, newPan);
            }
          }
        } else {
          if (axis === "y") {
            if (!mainScroll.isShifted() && bounds.min.y !== bounds.max.y) {
              this._setPanWithFriction(axis, newPan);
            }
          } else {
            this._setPanWithFriction(axis, newPan);
          }
        }
        return false;
      }
      // If we move above - the ratio is negative
      // If we move below the ratio is positive
      /**
       * Relation between pan Y position and third of viewport height.
       *
       * When we are at initial position (center bounds) - the ratio is 0,
       * if position is shifted upwards - the ratio is negative,
       * if position is shifted downwards - the ratio is positive.
       *
       * @private
       * @param {number} panY The current pan Y position.
       * @returns {number}
       */
      _getVerticalDragRatio(panY) {
        var _this$pswp$currSlide$, _this$pswp$currSlide;
        return (
          (panY -
            ((_this$pswp$currSlide$ =
              (_this$pswp$currSlide = this.pswp.currSlide) === null ||
              _this$pswp$currSlide === void 0
                ? void 0
                : _this$pswp$currSlide.bounds.center.y) !== null &&
            _this$pswp$currSlide$ !== void 0
              ? _this$pswp$currSlide$
              : 0)) /
          (this.pswp.viewportSize.y / 3)
        );
      }
      /**
       * Set pan position of the current slide.
       * Apply friction if the position is beyond the pan bounds,
       * or if custom friction is defined.
       *
       * @private
       * @param {'x' | 'y'} axis
       * @param {number} potentialPan
       * @param {number} [customFriction] (0.1 - 1)
       */
      _setPanWithFriction(axis, potentialPan, customFriction) {
        const { currSlide } = this.pswp;
        if (!currSlide) {
          return;
        }
        const { pan, bounds } = currSlide;
        const correctedPan = bounds.correctPan(axis, potentialPan);
        if (correctedPan !== potentialPan || customFriction) {
          const delta = Math.round(potentialPan - pan[axis]);
          pan[axis] += delta * (customFriction || PAN_END_FRICTION);
        } else {
          pan[axis] = potentialPan;
        }
      }
    };
    UPPER_ZOOM_FRICTION = 0.05;
    LOWER_ZOOM_FRICTION = 0.15;
    ZoomHandler = class {
      /**
       * @param {Gestures} gestures
       */
      constructor(gestures) {
        this.gestures = gestures;
        this._startPan = {
          x: 0,
          y: 0,
        };
        this._startZoomPoint = {
          x: 0,
          y: 0,
        };
        this._zoomPoint = {
          x: 0,
          y: 0,
        };
        this._wasOverFitZoomLevel = false;
        this._startZoomLevel = 1;
      }
      start() {
        const { currSlide } = this.gestures.pswp;
        if (currSlide) {
          this._startZoomLevel = currSlide.currZoomLevel;
          equalizePoints(this._startPan, currSlide.pan);
        }
        this.gestures.pswp.animations.stopAllPan();
        this._wasOverFitZoomLevel = false;
      }
      change() {
        const { p1, startP1, p2, startP2, pswp } = this.gestures;
        const { currSlide } = pswp;
        if (!currSlide) {
          return;
        }
        const minZoomLevel = currSlide.zoomLevels.min;
        const maxZoomLevel = currSlide.zoomLevels.max;
        if (!currSlide.isZoomable() || pswp.mainScroll.isShifted()) {
          return;
        }
        getZoomPointsCenter(this._startZoomPoint, startP1, startP2);
        getZoomPointsCenter(this._zoomPoint, p1, p2);
        let currZoomLevel =
          (1 / getDistanceBetween(startP1, startP2)) *
          getDistanceBetween(p1, p2) *
          this._startZoomLevel;
        if (
          currZoomLevel >
          currSlide.zoomLevels.initial + currSlide.zoomLevels.initial / 15
        ) {
          this._wasOverFitZoomLevel = true;
        }
        if (currZoomLevel < minZoomLevel) {
          if (
            pswp.options.pinchToClose &&
            !this._wasOverFitZoomLevel &&
            this._startZoomLevel <= currSlide.zoomLevels.initial
          ) {
            const bgOpacity =
              1 - (minZoomLevel - currZoomLevel) / (minZoomLevel / 1.2);
            if (
              !pswp.dispatch("pinchClose", {
                bgOpacity,
              }).defaultPrevented
            ) {
              pswp.applyBgOpacity(bgOpacity);
            }
          } else {
            currZoomLevel =
              minZoomLevel -
              (minZoomLevel - currZoomLevel) * LOWER_ZOOM_FRICTION;
          }
        } else if (currZoomLevel > maxZoomLevel) {
          currZoomLevel =
            maxZoomLevel + (currZoomLevel - maxZoomLevel) * UPPER_ZOOM_FRICTION;
        }
        currSlide.pan.x = this._calculatePanForZoomLevel("x", currZoomLevel);
        currSlide.pan.y = this._calculatePanForZoomLevel("y", currZoomLevel);
        currSlide.setZoomLevel(currZoomLevel);
        currSlide.applyCurrentZoomPan();
      }
      end() {
        const { pswp } = this.gestures;
        const { currSlide } = pswp;
        if (
          (!currSlide ||
            currSlide.currZoomLevel < currSlide.zoomLevels.initial) &&
          !this._wasOverFitZoomLevel &&
          pswp.options.pinchToClose
        ) {
          pswp.close();
        } else {
          this.correctZoomPan();
        }
      }
      /**
       * @private
       * @param {'x' | 'y'} axis
       * @param {number} currZoomLevel
       * @returns {number}
       */
      _calculatePanForZoomLevel(axis, currZoomLevel) {
        const zoomFactor = currZoomLevel / this._startZoomLevel;
        return (
          this._zoomPoint[axis] -
          (this._startZoomPoint[axis] - this._startPan[axis]) * zoomFactor
        );
      }
      /**
       * Correct currZoomLevel and pan if they are
       * beyond minimum or maximum values.
       * With animation.
       *
       * @param {boolean} [ignoreGesture]
       * Wether gesture coordinates should be ignored when calculating destination pan position.
       */
      correctZoomPan(ignoreGesture) {
        const { pswp } = this.gestures;
        const { currSlide } = pswp;
        if (!(
          currSlide !== null &&
          currSlide !== void 0 &&
          currSlide.isZoomable()
        )) {
          return;
        }
        if (this._zoomPoint.x === 0) {
          ignoreGesture = true;
        }
        const prevZoomLevel = currSlide.currZoomLevel;
        let destinationZoomLevel;
        let currZoomLevelNeedsChange = true;
        if (prevZoomLevel < currSlide.zoomLevels.initial) {
          destinationZoomLevel = currSlide.zoomLevels.initial;
        } else if (prevZoomLevel > currSlide.zoomLevels.max) {
          destinationZoomLevel = currSlide.zoomLevels.max;
        } else {
          currZoomLevelNeedsChange = false;
          destinationZoomLevel = prevZoomLevel;
        }
        const initialBgOpacity = pswp.bgOpacity;
        const restoreBgOpacity = pswp.bgOpacity < 1;
        const initialPan = equalizePoints(
          {
            x: 0,
            y: 0,
          },
          currSlide.pan,
        );
        let destinationPan = equalizePoints(
          {
            x: 0,
            y: 0,
          },
          initialPan,
        );
        if (ignoreGesture) {
          this._zoomPoint.x = 0;
          this._zoomPoint.y = 0;
          this._startZoomPoint.x = 0;
          this._startZoomPoint.y = 0;
          this._startZoomLevel = prevZoomLevel;
          equalizePoints(this._startPan, initialPan);
        }
        if (currZoomLevelNeedsChange) {
          destinationPan = {
            x: this._calculatePanForZoomLevel("x", destinationZoomLevel),
            y: this._calculatePanForZoomLevel("y", destinationZoomLevel),
          };
        }
        currSlide.setZoomLevel(destinationZoomLevel);
        destinationPan = {
          x: currSlide.bounds.correctPan("x", destinationPan.x),
          y: currSlide.bounds.correctPan("y", destinationPan.y),
        };
        currSlide.setZoomLevel(prevZoomLevel);
        const panNeedsChange = !pointsEqual(destinationPan, initialPan);
        if (!panNeedsChange && !currZoomLevelNeedsChange && !restoreBgOpacity) {
          currSlide._setResolution(destinationZoomLevel);
          currSlide.applyCurrentZoomPan();
          return;
        }
        pswp.animations.stopAllPan();
        pswp.animations.startSpring({
          isPan: true,
          start: 0,
          end: 1e3,
          velocity: 0,
          dampingRatio: 1,
          naturalFrequency: 40,
          onUpdate: (now) => {
            now /= 1e3;
            if (panNeedsChange || currZoomLevelNeedsChange) {
              if (panNeedsChange) {
                currSlide.pan.x =
                  initialPan.x + (destinationPan.x - initialPan.x) * now;
                currSlide.pan.y =
                  initialPan.y + (destinationPan.y - initialPan.y) * now;
              }
              if (currZoomLevelNeedsChange) {
                const newZoomLevel =
                  prevZoomLevel + (destinationZoomLevel - prevZoomLevel) * now;
                currSlide.setZoomLevel(newZoomLevel);
              }
              currSlide.applyCurrentZoomPan();
            }
            if (restoreBgOpacity && pswp.bgOpacity < 1) {
              pswp.applyBgOpacity(
                clamp2(initialBgOpacity + (1 - initialBgOpacity) * now, 0, 1),
              );
            }
          },
          onComplete: () => {
            currSlide._setResolution(destinationZoomLevel);
            currSlide.applyCurrentZoomPan();
          },
        });
      }
    };
    TapHandler = class {
      /**
       * @param {Gestures} gestures
       */
      constructor(gestures) {
        this.gestures = gestures;
      }
      /**
       * @param {Point} point
       * @param {PointerEvent} originalEvent
       */
      click(point, originalEvent) {
        const targetClassList =
          /** @type {HTMLElement} */
          originalEvent.target.classList;
        const isImageClick = targetClassList.contains("pswp__img");
        const isBackgroundClick =
          targetClassList.contains("pswp__item") ||
          targetClassList.contains("pswp__zoom-wrap");
        if (isImageClick) {
          this._doClickOrTapAction("imageClick", point, originalEvent);
        } else if (isBackgroundClick) {
          this._doClickOrTapAction("bgClick", point, originalEvent);
        }
      }
      /**
       * @param {Point} point
       * @param {PointerEvent} originalEvent
       */
      tap(point, originalEvent) {
        if (didTapOnMainContent(originalEvent)) {
          this._doClickOrTapAction("tap", point, originalEvent);
        }
      }
      /**
       * @param {Point} point
       * @param {PointerEvent} originalEvent
       */
      doubleTap(point, originalEvent) {
        if (didTapOnMainContent(originalEvent)) {
          this._doClickOrTapAction("doubleTap", point, originalEvent);
        }
      }
      /**
       * @private
       * @param {Actions} actionName
       * @param {Point} point
       * @param {PointerEvent} originalEvent
       */
      _doClickOrTapAction(actionName, point, originalEvent) {
        var _this$gestures$pswp$e;
        const { pswp } = this.gestures;
        const { currSlide } = pswp;
        const actionFullName =
          /** @type {AddPostfix<Actions, 'Action'>} */
          actionName + "Action";
        const optionValue = pswp.options[actionFullName];
        if (
          pswp.dispatch(actionFullName, {
            point,
            originalEvent,
          }).defaultPrevented
        ) {
          return;
        }
        if (typeof optionValue === "function") {
          optionValue.call(pswp, point, originalEvent);
          return;
        }
        switch (optionValue) {
          case "close":
          case "next":
            pswp[optionValue]();
            break;
          case "zoom":
            currSlide === null ||
              currSlide === void 0 ||
              currSlide.toggleZoom(point);
            break;
          case "zoom-or-close":
            if (
              currSlide !== null &&
              currSlide !== void 0 &&
              currSlide.isZoomable() &&
              currSlide.zoomLevels.secondary !== currSlide.zoomLevels.initial
            ) {
              currSlide.toggleZoom(point);
            } else if (pswp.options.clickToCloseNonZoomable) {
              pswp.close();
            }
            break;
          case "toggle-controls":
            (_this$gestures$pswp$e = this.gestures.pswp.element) === null ||
              _this$gestures$pswp$e === void 0 ||
              _this$gestures$pswp$e.classList.toggle("pswp--ui-visible");
            break;
        }
      }
    };
    AXIS_SWIPE_HYSTERISIS = 10;
    DOUBLE_TAP_DELAY = 300;
    MIN_TAP_DISTANCE = 25;
    Gestures = class {
      /**
       * @param {PhotoSwipe} pswp
       */
      constructor(pswp) {
        this.pswp = pswp;
        this.dragAxis = null;
        this.p1 = {
          x: 0,
          y: 0,
        };
        this.p2 = {
          x: 0,
          y: 0,
        };
        this.prevP1 = {
          x: 0,
          y: 0,
        };
        this.prevP2 = {
          x: 0,
          y: 0,
        };
        this.startP1 = {
          x: 0,
          y: 0,
        };
        this.startP2 = {
          x: 0,
          y: 0,
        };
        this.velocity = {
          x: 0,
          y: 0,
        };
        this._lastStartP1 = {
          x: 0,
          y: 0,
        };
        this._intervalP1 = {
          x: 0,
          y: 0,
        };
        this._numActivePoints = 0;
        this._ongoingPointers = [];
        this._touchEventEnabled = "ontouchstart" in window;
        this._pointerEventEnabled = !!window.PointerEvent;
        this.supportsTouch =
          this._touchEventEnabled ||
          (this._pointerEventEnabled && navigator.maxTouchPoints > 1);
        this._numActivePoints = 0;
        this._intervalTime = 0;
        this._velocityCalculated = false;
        this.isMultitouch = false;
        this.isDragging = false;
        this.isZooming = false;
        this.raf = null;
        this._tapTimer = null;
        if (!this.supportsTouch) {
          pswp.options.allowPanToNext = false;
        }
        this.drag = new DragHandler(this);
        this.zoomLevels = new ZoomHandler(this);
        this.tapHandler = new TapHandler(this);
        pswp.on("bindEvents", () => {
          pswp.events.add(
            pswp.scrollWrap,
            "click",
            /** @type EventListener */
            this._onClick.bind(this),
          );
          if (this._pointerEventEnabled) {
            this._bindEvents("pointer", "down", "up", "cancel");
          } else if (this._touchEventEnabled) {
            this._bindEvents("touch", "start", "end", "cancel");
            if (pswp.scrollWrap) {
              pswp.scrollWrap.ontouchmove = () => {};
              pswp.scrollWrap.ontouchend = () => {};
            }
          } else {
            this._bindEvents("mouse", "down", "up");
          }
        });
      }
      /**
       * @private
       * @param {'mouse' | 'touch' | 'pointer'} pref
       * @param {'down' | 'start'} down
       * @param {'up' | 'end'} up
       * @param {'cancel'} [cancel]
       */
      _bindEvents(pref, down, up, cancel) {
        const { pswp } = this;
        const { events } = pswp;
        const cancelEvent = cancel ? pref + cancel : "";
        events.add(
          pswp.scrollWrap,
          pref + down,
          /** @type EventListener */
          this.onPointerDown.bind(this),
        );
        events.add(
          window,
          pref + "move",
          /** @type EventListener */
          this.onPointerMove.bind(this),
        );
        events.add(
          window,
          pref + up,
          /** @type EventListener */
          this.onPointerUp.bind(this),
        );
        if (cancelEvent) {
          events.add(
            pswp.scrollWrap,
            cancelEvent,
            /** @type EventListener */
            this.onPointerUp.bind(this),
          );
        }
      }
      /**
       * @param {PointerEvent} e
       */
      onPointerDown(e) {
        const isMousePointer =
          e.type === "mousedown" || e.pointerType === "mouse";
        if (isMousePointer && e.button > 0) {
          return;
        }
        const { pswp } = this;
        if (!pswp.opener.isOpen) {
          e.preventDefault();
          return;
        }
        if (
          pswp.dispatch("pointerDown", {
            originalEvent: e,
          }).defaultPrevented
        ) {
          return;
        }
        if (isMousePointer) {
          pswp.mouseDetected();
          this._preventPointerEventBehaviour(e, "down");
        }
        pswp.animations.stopAll();
        this._updatePoints(e, "down");
        if (this._numActivePoints === 1) {
          this.dragAxis = null;
          equalizePoints(this.startP1, this.p1);
        }
        if (this._numActivePoints > 1) {
          this._clearTapTimer();
          this.isMultitouch = true;
        } else {
          this.isMultitouch = false;
        }
      }
      /**
       * @param {PointerEvent} e
       */
      onPointerMove(e) {
        this._preventPointerEventBehaviour(e, "move");
        if (!this._numActivePoints) {
          return;
        }
        this._updatePoints(e, "move");
        if (
          this.pswp.dispatch("pointerMove", {
            originalEvent: e,
          }).defaultPrevented
        ) {
          return;
        }
        if (this._numActivePoints === 1 && !this.isDragging) {
          if (!this.dragAxis) {
            this._calculateDragDirection();
          }
          if (this.dragAxis && !this.isDragging) {
            if (this.isZooming) {
              this.isZooming = false;
              this.zoomLevels.end();
            }
            this.isDragging = true;
            this._clearTapTimer();
            this._updateStartPoints();
            this._intervalTime = Date.now();
            this._velocityCalculated = false;
            equalizePoints(this._intervalP1, this.p1);
            this.velocity.x = 0;
            this.velocity.y = 0;
            this.drag.start();
            this._rafStopLoop();
            this._rafRenderLoop();
          }
        } else if (this._numActivePoints > 1 && !this.isZooming) {
          this._finishDrag();
          this.isZooming = true;
          this._updateStartPoints();
          this.zoomLevels.start();
          this._rafStopLoop();
          this._rafRenderLoop();
        }
      }
      /**
       * @private
       */
      _finishDrag() {
        if (this.isDragging) {
          this.isDragging = false;
          if (!this._velocityCalculated) {
            this._updateVelocity(true);
          }
          this.drag.end();
          this.dragAxis = null;
        }
      }
      /**
       * @param {PointerEvent} e
       */
      onPointerUp(e) {
        if (!this._numActivePoints) {
          return;
        }
        this._updatePoints(e, "up");
        if (
          this.pswp.dispatch("pointerUp", {
            originalEvent: e,
          }).defaultPrevented
        ) {
          return;
        }
        if (this._numActivePoints === 0) {
          this._rafStopLoop();
          if (this.isDragging) {
            this._finishDrag();
          } else if (!this.isZooming && !this.isMultitouch) {
            this._finishTap(e);
          }
        }
        if (this._numActivePoints < 2 && this.isZooming) {
          this.isZooming = false;
          this.zoomLevels.end();
          if (this._numActivePoints === 1) {
            this.dragAxis = null;
            this._updateStartPoints();
          }
        }
      }
      /**
       * @private
       */
      _rafRenderLoop() {
        if (this.isDragging || this.isZooming) {
          this._updateVelocity();
          if (this.isDragging) {
            if (!pointsEqual(this.p1, this.prevP1)) {
              this.drag.change();
            }
          } else {
            if (
              !pointsEqual(this.p1, this.prevP1) ||
              !pointsEqual(this.p2, this.prevP2)
            ) {
              this.zoomLevels.change();
            }
          }
          this._updatePrevPoints();
          this.raf = requestAnimationFrame(this._rafRenderLoop.bind(this));
        }
      }
      /**
       * Update velocity at 50ms interval
       *
       * @private
       * @param {boolean} [force]
       */
      _updateVelocity(force) {
        const time = Date.now();
        const duration2 = time - this._intervalTime;
        if (duration2 < 50 && !force) {
          return;
        }
        this.velocity.x = this._getVelocity("x", duration2);
        this.velocity.y = this._getVelocity("y", duration2);
        this._intervalTime = time;
        equalizePoints(this._intervalP1, this.p1);
        this._velocityCalculated = true;
      }
      /**
       * @private
       * @param {PointerEvent} e
       */
      _finishTap(e) {
        const { mainScroll } = this.pswp;
        if (mainScroll.isShifted()) {
          mainScroll.moveIndexBy(0, true);
          return;
        }
        if (e.type.indexOf("cancel") > 0) {
          return;
        }
        if (e.type === "mouseup" || e.pointerType === "mouse") {
          this.tapHandler.click(this.startP1, e);
          return;
        }
        const tapDelay = this.pswp.options.doubleTapAction
          ? DOUBLE_TAP_DELAY
          : 0;
        if (this._tapTimer) {
          this._clearTapTimer();
          if (
            getDistanceBetween(this._lastStartP1, this.startP1) <
            MIN_TAP_DISTANCE
          ) {
            this.tapHandler.doubleTap(this.startP1, e);
          }
        } else {
          equalizePoints(this._lastStartP1, this.startP1);
          this._tapTimer = setTimeout(() => {
            this.tapHandler.tap(this.startP1, e);
            this._clearTapTimer();
          }, tapDelay);
        }
      }
      /**
       * @private
       */
      _clearTapTimer() {
        if (this._tapTimer) {
          clearTimeout(this._tapTimer);
          this._tapTimer = null;
        }
      }
      /**
       * Get velocity for axis
       *
       * @private
       * @param {'x' | 'y'} axis
       * @param {number} duration
       * @returns {number}
       */
      _getVelocity(axis, duration2) {
        const displacement = this.p1[axis] - this._intervalP1[axis];
        if (Math.abs(displacement) > 1 && duration2 > 5) {
          return displacement / duration2;
        }
        return 0;
      }
      /**
       * @private
       */
      _rafStopLoop() {
        if (this.raf) {
          cancelAnimationFrame(this.raf);
          this.raf = null;
        }
      }
      /**
       * @private
       * @param {PointerEvent} e
       * @param {'up' | 'down' | 'move'} pointerType Normalized pointer type
       */
      _preventPointerEventBehaviour(e, pointerType) {
        const preventPointerEvent = this.pswp.applyFilters(
          "preventPointerEvent",
          true,
          e,
          pointerType,
        );
        if (preventPointerEvent) {
          e.preventDefault();
        }
      }
      /**
       * Parses and normalizes points from the touch, mouse or pointer event.
       * Updates p1 and p2.
       *
       * @private
       * @param {PointerEvent | TouchEvent} e
       * @param {'up' | 'down' | 'move'} pointerType Normalized pointer type
       */
      _updatePoints(e, pointerType) {
        if (this._pointerEventEnabled) {
          const pointerEvent =
            /** @type {PointerEvent} */
            e;
          const pointerIndex = this._ongoingPointers.findIndex(
            (ongoingPointer) => {
              return ongoingPointer.id === pointerEvent.pointerId;
            },
          );
          if (pointerType === "up" && pointerIndex > -1) {
            this._ongoingPointers.splice(pointerIndex, 1);
          } else if (pointerType === "down" && pointerIndex === -1) {
            this._ongoingPointers.push(
              this._convertEventPosToPoint(pointerEvent, {
                x: 0,
                y: 0,
              }),
            );
          } else if (pointerIndex > -1) {
            this._convertEventPosToPoint(
              pointerEvent,
              this._ongoingPointers[pointerIndex],
            );
          }
          this._numActivePoints = this._ongoingPointers.length;
          if (this._numActivePoints > 0) {
            equalizePoints(this.p1, this._ongoingPointers[0]);
          }
          if (this._numActivePoints > 1) {
            equalizePoints(this.p2, this._ongoingPointers[1]);
          }
        } else {
          const touchEvent =
            /** @type {TouchEvent} */
            e;
          this._numActivePoints = 0;
          if (touchEvent.type.indexOf("touch") > -1) {
            if (touchEvent.touches && touchEvent.touches.length > 0) {
              this._convertEventPosToPoint(touchEvent.touches[0], this.p1);
              this._numActivePoints++;
              if (touchEvent.touches.length > 1) {
                this._convertEventPosToPoint(touchEvent.touches[1], this.p2);
                this._numActivePoints++;
              }
            }
          } else {
            this._convertEventPosToPoint(
              /** @type {PointerEvent} */
              e,
              this.p1,
            );
            if (pointerType === "up") {
              this._numActivePoints = 0;
            } else {
              this._numActivePoints++;
            }
          }
        }
      }
      /** update points that were used during previous rAF tick
       * @private
       */
      _updatePrevPoints() {
        equalizePoints(this.prevP1, this.p1);
        equalizePoints(this.prevP2, this.p2);
      }
      /** update points at the start of gesture
       * @private
       */
      _updateStartPoints() {
        equalizePoints(this.startP1, this.p1);
        equalizePoints(this.startP2, this.p2);
        this._updatePrevPoints();
      }
      /** @private */
      _calculateDragDirection() {
        if (this.pswp.mainScroll.isShifted()) {
          this.dragAxis = "x";
        } else {
          const diff =
            Math.abs(this.p1.x - this.startP1.x) -
            Math.abs(this.p1.y - this.startP1.y);
          if (diff !== 0) {
            const axisToCheck = diff > 0 ? "x" : "y";
            if (
              Math.abs(this.p1[axisToCheck] - this.startP1[axisToCheck]) >=
              AXIS_SWIPE_HYSTERISIS
            ) {
              this.dragAxis = axisToCheck;
            }
          }
        }
      }
      /**
       * Converts touch, pointer or mouse event
       * to PhotoSwipe point.
       *
       * @private
       * @param {Touch | PointerEvent} e
       * @param {Point} p
       * @returns {Point}
       */
      _convertEventPosToPoint(e, p) {
        p.x = e.pageX - this.pswp.offset.x;
        p.y = e.pageY - this.pswp.offset.y;
        if ("pointerId" in e) {
          p.id = e.pointerId;
        } else if (e.identifier !== void 0) {
          p.id = e.identifier;
        }
        return p;
      }
      /**
       * @private
       * @param {PointerEvent} e
       */
      _onClick(e) {
        if (this.pswp.mainScroll.isShifted()) {
          e.preventDefault();
          e.stopPropagation();
        }
      }
    };
    MAIN_SCROLL_END_FRICTION = 0.35;
    MainScroll = class {
      /**
       * @param {PhotoSwipe} pswp
       */
      constructor(pswp) {
        this.pswp = pswp;
        this.x = 0;
        this.slideWidth = 0;
        this._currPositionIndex = 0;
        this._prevPositionIndex = 0;
        this._containerShiftIndex = -1;
        this.itemHolders = [];
      }
      /**
       * Position the scroller and slide containers
       * according to viewport size.
       *
       * @param {boolean} [resizeSlides] Whether slides content should resized
       */
      resize(resizeSlides) {
        const { pswp } = this;
        const newSlideWidth = Math.round(
          pswp.viewportSize.x + pswp.viewportSize.x * pswp.options.spacing,
        );
        const slideWidthChanged = newSlideWidth !== this.slideWidth;
        if (slideWidthChanged) {
          this.slideWidth = newSlideWidth;
          this.moveTo(this.getCurrSlideX());
        }
        this.itemHolders.forEach((itemHolder, index) => {
          if (slideWidthChanged) {
            setTransform(
              itemHolder.el,
              (index + this._containerShiftIndex) * this.slideWidth,
            );
          }
          if (resizeSlides && itemHolder.slide) {
            itemHolder.slide.resize();
          }
        });
      }
      /**
       * Reset X position of the main scroller to zero
       */
      resetPosition() {
        this._currPositionIndex = 0;
        this._prevPositionIndex = 0;
        this.slideWidth = 0;
        this._containerShiftIndex = -1;
      }
      /**
       * Create and append array of three items
       * that hold data about slides in DOM
       */
      appendHolders() {
        this.itemHolders = [];
        for (let i = 0; i < 3; i++) {
          const el = createElement5("pswp__item", "div", this.pswp.container);
          el.setAttribute("role", "group");
          el.setAttribute("aria-roledescription", "slide");
          el.setAttribute("aria-hidden", "true");
          el.style.display = i === 1 ? "block" : "none";
          this.itemHolders.push({
            el,
            //index: -1
          });
        }
      }
      /**
       * Whether the main scroll can be horizontally swiped to the next or previous slide.
       * @returns {boolean}
       */
      canBeSwiped() {
        return this.pswp.getNumItems() > 1;
      }
      /**
       * Move main scroll by X amount of slides.
       * For example:
       *   `-1` will move to the previous slide,
       *    `0` will reset the scroll position of the current slide,
       *    `3` will move three slides forward
       *
       * If loop option is enabled - index will be automatically looped too,
       * (for example `-1` will move to the last slide of the gallery).
       *
       * @param {number} diff
       * @param {boolean} [animate]
       * @param {number} [velocityX]
       * @returns {boolean} whether index was changed or not
       */
      moveIndexBy(diff, animate, velocityX) {
        const { pswp } = this;
        let newIndex = pswp.potentialIndex + diff;
        const numSlides = pswp.getNumItems();
        if (pswp.canLoop()) {
          newIndex = pswp.getLoopedIndex(newIndex);
          const distance = (diff + numSlides) % numSlides;
          if (distance <= numSlides / 2) {
            diff = distance;
          } else {
            diff = distance - numSlides;
          }
        } else {
          if (newIndex < 0) {
            newIndex = 0;
          } else if (newIndex >= numSlides) {
            newIndex = numSlides - 1;
          }
          diff = newIndex - pswp.potentialIndex;
        }
        pswp.potentialIndex = newIndex;
        this._currPositionIndex -= diff;
        pswp.animations.stopMainScroll();
        const destinationX = this.getCurrSlideX();
        if (!animate) {
          this.moveTo(destinationX);
          this.updateCurrItem();
        } else {
          pswp.animations.startSpring({
            isMainScroll: true,
            start: this.x,
            end: destinationX,
            velocity: velocityX || 0,
            naturalFrequency: 30,
            dampingRatio: 1,
            //0.7,
            onUpdate: (x) => {
              this.moveTo(x);
            },
            onComplete: () => {
              this.updateCurrItem();
              pswp.appendHeavy();
            },
          });
          let currDiff = pswp.potentialIndex - pswp.currIndex;
          if (pswp.canLoop()) {
            const currDistance = (currDiff + numSlides) % numSlides;
            if (currDistance <= numSlides / 2) {
              currDiff = currDistance;
            } else {
              currDiff = currDistance - numSlides;
            }
          }
          if (Math.abs(currDiff) > 1) {
            this.updateCurrItem();
          }
        }
        return Boolean(diff);
      }
      /**
       * X position of the main scroll for the current slide
       * (ignores position during dragging)
       * @returns {number}
       */
      getCurrSlideX() {
        return this.slideWidth * this._currPositionIndex;
      }
      /**
       * Whether scroll position is shifted.
       * For example, it will return true if the scroll is being dragged or animated.
       * @returns {boolean}
       */
      isShifted() {
        return this.x !== this.getCurrSlideX();
      }
      /**
       * Update slides X positions and set their content
       */
      updateCurrItem() {
        var _this$itemHolders$;
        const { pswp } = this;
        const positionDifference =
          this._prevPositionIndex - this._currPositionIndex;
        if (!positionDifference) {
          return;
        }
        this._prevPositionIndex = this._currPositionIndex;
        pswp.currIndex = pswp.potentialIndex;
        let diffAbs = Math.abs(positionDifference);
        let tempHolder;
        if (diffAbs >= 3) {
          this._containerShiftIndex +=
            positionDifference + (positionDifference > 0 ? -3 : 3);
          diffAbs = 3;
          this.itemHolders.forEach((itemHolder) => {
            var _itemHolder$slide;
            (_itemHolder$slide = itemHolder.slide) === null ||
              _itemHolder$slide === void 0 ||
              _itemHolder$slide.destroy();
            itemHolder.slide = void 0;
          });
        }
        for (let i = 0; i < diffAbs; i++) {
          if (positionDifference > 0) {
            tempHolder = this.itemHolders.shift();
            if (tempHolder) {
              this.itemHolders[2] = tempHolder;
              this._containerShiftIndex++;
              setTransform(
                tempHolder.el,
                (this._containerShiftIndex + 2) * this.slideWidth,
              );
              pswp.setContent(tempHolder, pswp.currIndex - diffAbs + i + 2);
            }
          } else {
            tempHolder = this.itemHolders.pop();
            if (tempHolder) {
              this.itemHolders.unshift(tempHolder);
              this._containerShiftIndex--;
              setTransform(
                tempHolder.el,
                this._containerShiftIndex * this.slideWidth,
              );
              pswp.setContent(tempHolder, pswp.currIndex + diffAbs - i - 2);
            }
          }
        }
        if (Math.abs(this._containerShiftIndex) > 50 && !this.isShifted()) {
          this.resetPosition();
          this.resize();
        }
        pswp.animations.stopAllPan();
        this.itemHolders.forEach((itemHolder, i) => {
          if (itemHolder.slide) {
            itemHolder.slide.setIsActive(i === 1);
          }
        });
        pswp.currSlide =
          (_this$itemHolders$ = this.itemHolders[1]) === null ||
          _this$itemHolders$ === void 0
            ? void 0
            : _this$itemHolders$.slide;
        pswp.contentLoader.updateLazy(positionDifference);
        if (pswp.currSlide) {
          pswp.currSlide.applyCurrentZoomPan();
        }
        pswp.dispatch("change");
      }
      /**
       * Move the X position of the main scroll container
       *
       * @param {number} x
       * @param {boolean} [dragging]
       */
      moveTo(x, dragging) {
        if (!this.pswp.canLoop() && dragging) {
          let newSlideIndexOffset =
            (this.slideWidth * this._currPositionIndex - x) / this.slideWidth;
          newSlideIndexOffset += this.pswp.currIndex;
          const delta = Math.round(x - this.x);
          if (
            (newSlideIndexOffset < 0 && delta > 0) ||
            (newSlideIndexOffset >= this.pswp.getNumItems() - 1 && delta < 0)
          ) {
            x = this.x + delta * MAIN_SCROLL_END_FRICTION;
          }
        }
        this.x = x;
        if (this.pswp.container) {
          setTransform(this.pswp.container, x);
        }
        this.pswp.dispatch("moveMainScroll", {
          x,
          dragging: dragging !== null && dragging !== void 0 ? dragging : false,
        });
      }
    };
    KeyboardKeyCodesMap = {
      Escape: 27,
      z: 90,
      ArrowLeft: 37,
      ArrowUp: 38,
      ArrowRight: 39,
      ArrowDown: 40,
      Tab: 9,
    };
    getKeyboardEventKey = (key, isKeySupported) => {
      return isKeySupported ? key : KeyboardKeyCodesMap[key];
    };
    Keyboard = class {
      /**
       * @param {PhotoSwipe} pswp
       */
      constructor(pswp) {
        this.pswp = pswp;
        this._wasFocused = false;
        pswp.on("bindEvents", () => {
          if (pswp.options.trapFocus) {
            if (!pswp.options.initialPointerPos) {
              this._focusRoot();
            }
            pswp.events.add(
              document,
              "focusin",
              /** @type EventListener */
              this._onFocusIn.bind(this),
            );
          }
          pswp.events.add(
            document,
            "keydown",
            /** @type EventListener */
            this._onKeyDown.bind(this),
          );
        });
        const lastActiveElement =
          /** @type {HTMLElement} */
          document.activeElement;
        pswp.on("destroy", () => {
          if (
            pswp.options.returnFocus &&
            lastActiveElement &&
            this._wasFocused
          ) {
            lastActiveElement.focus();
          }
        });
      }
      /** @private */
      _focusRoot() {
        if (!this._wasFocused && this.pswp.element) {
          this.pswp.element.focus();
          this._wasFocused = true;
        }
      }
      /**
       * @private
       * @param {KeyboardEvent} e
       */
      _onKeyDown(e) {
        const { pswp } = this;
        if (
          pswp.dispatch("keydown", {
            originalEvent: e,
          }).defaultPrevented
        ) {
          return;
        }
        if (specialKeyUsed2(e)) {
          return;
        }
        let keydownAction;
        let axis;
        let isForward = false;
        const isKeySupported = "key" in e;
        switch (isKeySupported ? e.key : e.keyCode) {
          case getKeyboardEventKey("Escape", isKeySupported):
            if (pswp.options.escKey) {
              keydownAction = "close";
            }
            break;
          case getKeyboardEventKey("z", isKeySupported):
            keydownAction = "toggleZoom";
            break;
          case getKeyboardEventKey("ArrowLeft", isKeySupported):
            axis = "x";
            break;
          case getKeyboardEventKey("ArrowUp", isKeySupported):
            axis = "y";
            break;
          case getKeyboardEventKey("ArrowRight", isKeySupported):
            axis = "x";
            isForward = true;
            break;
          case getKeyboardEventKey("ArrowDown", isKeySupported):
            isForward = true;
            axis = "y";
            break;
          case getKeyboardEventKey("Tab", isKeySupported):
            this._focusRoot();
            break;
        }
        if (axis) {
          e.preventDefault();
          const { currSlide } = pswp;
          if (
            pswp.options.arrowKeys &&
            axis === "x" &&
            pswp.getNumItems() > 1
          ) {
            keydownAction = isForward ? "next" : "prev";
          } else if (
            currSlide &&
            currSlide.currZoomLevel > currSlide.zoomLevels.fit
          ) {
            currSlide.pan[axis] += isForward ? -80 : 80;
            currSlide.panTo(currSlide.pan.x, currSlide.pan.y);
          }
        }
        if (keydownAction) {
          e.preventDefault();
          pswp[keydownAction]();
        }
      }
      /**
       * Trap focus inside photoswipe
       *
       * @private
       * @param {FocusEvent} e
       */
      _onFocusIn(e) {
        const { template } = this.pswp;
        if (
          template &&
          document !== e.target &&
          template !== e.target &&
          !template.contains(
            /** @type {Node} */
            e.target,
          )
        ) {
          template.focus();
        }
      }
    };
    DEFAULT_EASING = "cubic-bezier(.4,0,.22,1)";
    CSSAnimation = class {
      /**
       * onComplete can be unpredictable, be careful about current state
       *
       * @param {CssAnimationProps} props
       */
      constructor(props) {
        var _props$prop;
        this.props = props;
        const {
          target,
          onComplete,
          transform,
          onFinish = () => {},
          duration: duration2 = 333,
          easing: easing2 = DEFAULT_EASING,
        } = props;
        this.onFinish = onFinish;
        const prop = transform ? "transform" : "opacity";
        const propValue =
          (_props$prop = props[prop]) !== null && _props$prop !== void 0
            ? _props$prop
            : "";
        this._target = target;
        this._onComplete = onComplete;
        this._finished = false;
        this._onTransitionEnd = this._onTransitionEnd.bind(this);
        this._helperTimeout = setTimeout(() => {
          setTransitionStyle(target, prop, duration2, easing2);
          this._helperTimeout = setTimeout(() => {
            target.addEventListener(
              "transitionend",
              this._onTransitionEnd,
              false,
            );
            target.addEventListener(
              "transitioncancel",
              this._onTransitionEnd,
              false,
            );
            this._helperTimeout = setTimeout(() => {
              this._finalizeAnimation();
            }, duration2 + 500);
            target.style[prop] = propValue;
          }, 30);
        }, 0);
      }
      /**
       * @private
       * @param {TransitionEvent} e
       */
      _onTransitionEnd(e) {
        if (e.target === this._target) {
          this._finalizeAnimation();
        }
      }
      /**
       * @private
       */
      _finalizeAnimation() {
        if (!this._finished) {
          this._finished = true;
          this.onFinish();
          if (this._onComplete) {
            this._onComplete();
          }
        }
      }
      // Destroy is called automatically onFinish
      destroy() {
        if (this._helperTimeout) {
          clearTimeout(this._helperTimeout);
        }
        removeTransitionStyle(this._target);
        this._target.removeEventListener(
          "transitionend",
          this._onTransitionEnd,
          false,
        );
        this._target.removeEventListener(
          "transitioncancel",
          this._onTransitionEnd,
          false,
        );
        if (!this._finished) {
          this._finalizeAnimation();
        }
      }
    };
    DEFAULT_NATURAL_FREQUENCY = 12;
    DEFAULT_DAMPING_RATIO = 0.75;
    SpringEaser = class {
      /**
       * @param {number} initialVelocity Initial velocity, px per ms.
       *
       * @param {number} [dampingRatio]
       * Determines how bouncy animation will be.
       * From 0 to 1, 0 - always overshoot, 1 - do not overshoot.
       * "overshoot" refers to part of animation that
       * goes beyond the final value.
       *
       * @param {number} [naturalFrequency]
       * Determines how fast animation will slow down.
       * The higher value - the stiffer the transition will be,
       * and the faster it will slow down.
       * Recommended value from 10 to 50
       */
      constructor(initialVelocity, dampingRatio, naturalFrequency) {
        this.velocity = initialVelocity * 1e3;
        this._dampingRatio = dampingRatio || DEFAULT_DAMPING_RATIO;
        this._naturalFrequency = naturalFrequency || DEFAULT_NATURAL_FREQUENCY;
        this._dampedFrequency = this._naturalFrequency;
        if (this._dampingRatio < 1) {
          this._dampedFrequency *= Math.sqrt(
            1 - this._dampingRatio * this._dampingRatio,
          );
        }
      }
      /**
       * @param {number} deltaPosition Difference between current and end position of the animation
       * @param {number} deltaTime Frame duration in milliseconds
       *
       * @returns {number} Displacement, relative to the end position.
       */
      easeFrame(deltaPosition, deltaTime) {
        let displacement = 0;
        let coeff;
        deltaTime /= 1e3;
        const naturalDumpingPow =
          Math.E ** (-this._dampingRatio * this._naturalFrequency * deltaTime);
        if (this._dampingRatio === 1) {
          coeff = this.velocity + this._naturalFrequency * deltaPosition;
          displacement =
            (deltaPosition + coeff * deltaTime) * naturalDumpingPow;
          this.velocity =
            displacement * -this._naturalFrequency + coeff * naturalDumpingPow;
        } else if (this._dampingRatio < 1) {
          coeff =
            (1 / this._dampedFrequency) *
            (this._dampingRatio * this._naturalFrequency * deltaPosition +
              this.velocity);
          const dumpedFCos = Math.cos(this._dampedFrequency * deltaTime);
          const dumpedFSin = Math.sin(this._dampedFrequency * deltaTime);
          displacement =
            naturalDumpingPow *
            (deltaPosition * dumpedFCos + coeff * dumpedFSin);
          this.velocity =
            displacement * -this._naturalFrequency * this._dampingRatio +
            naturalDumpingPow *
              (-this._dampedFrequency * deltaPosition * dumpedFSin +
                this._dampedFrequency * coeff * dumpedFCos);
        }
        return displacement;
      }
    };
    SpringAnimation = class {
      /**
       * @param {SpringAnimationProps} props
       */
      constructor(props) {
        this.props = props;
        this._raf = 0;
        const {
          start: start2,
          end: end2,
          velocity,
          onUpdate,
          onComplete,
          onFinish = () => {},
          dampingRatio,
          naturalFrequency,
        } = props;
        this.onFinish = onFinish;
        const easer = new SpringEaser(velocity, dampingRatio, naturalFrequency);
        let prevTime = Date.now();
        let deltaPosition = start2 - end2;
        const animationLoop = () => {
          if (this._raf) {
            deltaPosition = easer.easeFrame(
              deltaPosition,
              Date.now() - prevTime,
            );
            if (Math.abs(deltaPosition) < 1 && Math.abs(easer.velocity) < 50) {
              onUpdate(end2);
              if (onComplete) {
                onComplete();
              }
              this.onFinish();
            } else {
              prevTime = Date.now();
              onUpdate(deltaPosition + end2);
              this._raf = requestAnimationFrame(animationLoop);
            }
          }
        };
        this._raf = requestAnimationFrame(animationLoop);
      }
      // Destroy is called automatically onFinish
      destroy() {
        if (this._raf >= 0) {
          cancelAnimationFrame(this._raf);
        }
        this._raf = 0;
      }
    };
    Animations = class {
      constructor() {
        this.activeAnimations = [];
      }
      /**
       * @param {SpringAnimationProps} props
       */
      startSpring(props) {
        this._start(props, true);
      }
      /**
       * @param {CssAnimationProps} props
       */
      startTransition(props) {
        this._start(props);
      }
      /**
       * @private
       * @param {AnimationProps} props
       * @param {boolean} [isSpring]
       * @returns {Animation}
       */
      _start(props, isSpring) {
        const animation = isSpring
          ? new SpringAnimation(
              /** @type SpringAnimationProps */
              props,
            )
          : new CSSAnimation(
              /** @type CssAnimationProps */
              props,
            );
        this.activeAnimations.push(animation);
        animation.onFinish = () => this.stop(animation);
        return animation;
      }
      /**
       * @param {Animation} animation
       */
      stop(animation) {
        animation.destroy();
        const index = this.activeAnimations.indexOf(animation);
        if (index > -1) {
          this.activeAnimations.splice(index, 1);
        }
      }
      stopAll() {
        this.activeAnimations.forEach((animation) => {
          animation.destroy();
        });
        this.activeAnimations = [];
      }
      /**
       * Stop all pan or zoom transitions
       */
      stopAllPan() {
        this.activeAnimations = this.activeAnimations.filter((animation) => {
          if (animation.props.isPan) {
            animation.destroy();
            return false;
          }
          return true;
        });
      }
      stopMainScroll() {
        this.activeAnimations = this.activeAnimations.filter((animation) => {
          if (animation.props.isMainScroll) {
            animation.destroy();
            return false;
          }
          return true;
        });
      }
      /**
       * Returns true if main scroll transition is running
       */
      // isMainScrollRunning() {
      //   return this.activeAnimations.some((animation) => {
      //     return animation.props.isMainScroll;
      //   });
      // }
      /**
       * Returns true if any pan or zoom transition is running
       */
      isPanRunning() {
        return this.activeAnimations.some((animation) => {
          return animation.props.isPan;
        });
      }
    };
    ScrollWheel = class {
      /**
       * @param {PhotoSwipe} pswp
       */
      constructor(pswp) {
        this.pswp = pswp;
        pswp.events.add(
          pswp.element,
          "wheel",
          /** @type EventListener */
          this._onWheel.bind(this),
        );
      }
      /**
       * @private
       * @param {WheelEvent} e
       */
      _onWheel(e) {
        e.preventDefault();
        const { currSlide } = this.pswp;
        let { deltaX, deltaY } = e;
        if (!currSlide) {
          return;
        }
        if (
          this.pswp.dispatch("wheel", {
            originalEvent: e,
          }).defaultPrevented
        ) {
          return;
        }
        if (e.ctrlKey || this.pswp.options.wheelToZoom) {
          if (currSlide.isZoomable()) {
            let zoomFactor = -deltaY;
            if (e.deltaMode === 1) {
              zoomFactor *= 0.05;
            } else {
              zoomFactor *= e.deltaMode ? 1 : 2e-3;
            }
            zoomFactor = 2 ** zoomFactor;
            const destZoomLevel = currSlide.currZoomLevel * zoomFactor;
            currSlide.zoomTo(destZoomLevel, {
              x: e.clientX,
              y: e.clientY,
            });
          }
        } else {
          if (currSlide.isPannable()) {
            if (e.deltaMode === 1) {
              deltaX *= 18;
              deltaY *= 18;
            }
            currSlide.panTo(currSlide.pan.x - deltaX, currSlide.pan.y - deltaY);
          }
        }
      }
    };
    UIElement = class {
      /**
       * @param {PhotoSwipe} pswp
       * @param {UIElementData} data
       */
      constructor(pswp, data) {
        var _container;
        const name = data.name || data.className;
        let elementHTML = data.html;
        if (pswp.options[name] === false) {
          return;
        }
        if (typeof pswp.options[name + "SVG"] === "string") {
          elementHTML = pswp.options[name + "SVG"];
        }
        pswp.dispatch("uiElementCreate", {
          data,
        });
        let className = "";
        if (data.isButton) {
          className += "pswp__button ";
          className += data.className || `pswp__button--${data.name}`;
        } else {
          className += data.className || `pswp__${data.name}`;
        }
        let tagName = data.isButton
          ? data.tagName || "button"
          : data.tagName || "div";
        tagName =
          /** @type {keyof HTMLElementTagNameMap} */
          tagName.toLowerCase();
        const element = createElement5(className, tagName);
        if (data.isButton) {
          if (tagName === "button") {
            element.type = "button";
          }
          let { title } = data;
          const { ariaLabel } = data;
          if (typeof pswp.options[name + "Title"] === "string") {
            title = pswp.options[name + "Title"];
          }
          if (title) {
            element.title = title;
          }
          const ariaText = ariaLabel || title;
          if (ariaText) {
            element.setAttribute("aria-label", ariaText);
          }
        }
        element.innerHTML = addElementHTML(elementHTML);
        if (data.onInit) {
          data.onInit(element, pswp);
        }
        if (data.onClick) {
          element.onclick = (e) => {
            if (typeof data.onClick === "string") {
              pswp[data.onClick]();
            } else if (typeof data.onClick === "function") {
              data.onClick(e, element, pswp);
            }
          };
        }
        const appendTo = data.appendTo || "bar";
        let container = pswp.element;
        if (appendTo === "bar") {
          if (!pswp.topBar) {
            pswp.topBar = createElement5(
              "pswp__top-bar pswp__hide-on-close",
              "div",
              pswp.scrollWrap,
            );
          }
          container = pswp.topBar;
        } else {
          element.classList.add("pswp__hide-on-close");
          if (appendTo === "wrapper") {
            container = pswp.scrollWrap;
          }
        }
        (_container = container) === null ||
          _container === void 0 ||
          _container.appendChild(pswp.applyFilters("uiElement", element, data));
      }
    };
    arrowPrev = {
      name: "arrowPrev",
      className: "pswp__button--arrow--prev",
      title: "Previous",
      order: 10,
      isButton: true,
      appendTo: "wrapper",
      html: {
        isCustomSVG: true,
        size: 60,
        inner:
          '<path d="M29 43l-3 3-16-16 16-16 3 3-13 13 13 13z" id="pswp__icn-arrow"/>',
        outlineID: "pswp__icn-arrow",
      },
      onClick: "prev",
      onInit: initArrowButton,
    };
    arrowNext = {
      name: "arrowNext",
      className: "pswp__button--arrow--next",
      title: "Next",
      order: 11,
      isButton: true,
      appendTo: "wrapper",
      html: {
        isCustomSVG: true,
        size: 60,
        inner: '<use xlink:href="#pswp__icn-arrow"/>',
        outlineID: "pswp__icn-arrow",
      },
      onClick: "next",
      onInit: (el, pswp) => {
        initArrowButton(el, pswp, true);
      },
    };
    closeButton = {
      name: "close",
      title: "Close",
      order: 20,
      isButton: true,
      html: {
        isCustomSVG: true,
        inner:
          '<path d="M24 10l-2-2-6 6-6-6-2 2 6 6-6 6 2 2 6-6 6 6 2-2-6-6z" id="pswp__icn-close"/>',
        outlineID: "pswp__icn-close",
      },
      onClick: "close",
    };
    zoomButton = {
      name: "zoom",
      title: "Zoom",
      order: 10,
      isButton: true,
      html: {
        isCustomSVG: true,
        // eslint-disable-next-line max-len
        inner:
          '<path d="M17.426 19.926a6 6 0 1 1 1.5-1.5L23 22.5 21.5 24l-4.074-4.074z" id="pswp__icn-zoom"/><path fill="currentColor" class="pswp__zoom-icn-bar-h" d="M11 16v-2h6v2z"/><path fill="currentColor" class="pswp__zoom-icn-bar-v" d="M13 12h2v6h-2z"/>',
        outlineID: "pswp__icn-zoom",
      },
      onClick: "toggleZoom",
    };
    loadingIndicator = {
      name: "preloader",
      appendTo: "bar",
      order: 7,
      html: {
        isCustomSVG: true,
        // eslint-disable-next-line max-len
        inner:
          '<path fill-rule="evenodd" clip-rule="evenodd" d="M21.2 16a5.2 5.2 0 1 1-5.2-5.2V8a8 8 0 1 0 8 8h-2.8Z" id="pswp__icn-loading"/>',
        outlineID: "pswp__icn-loading",
      },
      onInit: (indicatorElement, pswp) => {
        let isVisible;
        let delayTimeout = null;
        const toggleIndicatorClass = (className, add) => {
          indicatorElement.classList.toggle(
            "pswp__preloader--" + className,
            add,
          );
        };
        const setIndicatorVisibility = (visible) => {
          if (isVisible !== visible) {
            isVisible = visible;
            toggleIndicatorClass("active", visible);
          }
        };
        const updatePreloaderVisibility = () => {
          var _pswp$currSlide;
          if (!(
            (_pswp$currSlide = pswp.currSlide) !== null &&
            _pswp$currSlide !== void 0 &&
            _pswp$currSlide.content.isLoading()
          )) {
            setIndicatorVisibility(false);
            if (delayTimeout) {
              clearTimeout(delayTimeout);
              delayTimeout = null;
            }
            return;
          }
          if (!delayTimeout) {
            delayTimeout = setTimeout(() => {
              var _pswp$currSlide2;
              setIndicatorVisibility(
                Boolean(
                  (_pswp$currSlide2 = pswp.currSlide) === null ||
                    _pswp$currSlide2 === void 0
                    ? void 0
                    : _pswp$currSlide2.content.isLoading(),
                ),
              );
              delayTimeout = null;
            }, pswp.options.preloaderDelay);
          }
        };
        pswp.on("change", updatePreloaderVisibility);
        pswp.on("loadComplete", (e) => {
          if (pswp.currSlide === e.slide) {
            updatePreloaderVisibility();
          }
        });
        if (pswp.ui) {
          pswp.ui.updatePreloaderVisibility = updatePreloaderVisibility;
        }
      },
    };
    counterIndicator = {
      name: "counter",
      order: 5,
      onInit: (counterElement, pswp) => {
        pswp.on("change", () => {
          counterElement.innerText =
            pswp.currIndex +
            1 +
            pswp.options.indexIndicatorSep +
            pswp.getNumItems();
        });
      },
    };
    UI = class {
      /**
       * @param {PhotoSwipe} pswp
       */
      constructor(pswp) {
        this.pswp = pswp;
        this.isRegistered = false;
        this.uiElementsData = [];
        this.items = [];
        this.updatePreloaderVisibility = () => {};
        this._lastUpdatedZoomLevel = void 0;
      }
      init() {
        const { pswp } = this;
        this.isRegistered = false;
        this.uiElementsData = [
          closeButton,
          arrowPrev,
          arrowNext,
          zoomButton,
          loadingIndicator,
          counterIndicator,
        ];
        pswp.dispatch("uiRegister");
        this.uiElementsData.sort((a, b) => {
          return (a.order || 0) - (b.order || 0);
        });
        this.items = [];
        this.isRegistered = true;
        this.uiElementsData.forEach((uiElementData) => {
          this.registerElement(uiElementData);
        });
        pswp.on("change", () => {
          var _pswp$element;
          (_pswp$element = pswp.element) === null ||
            _pswp$element === void 0 ||
            _pswp$element.classList.toggle(
              "pswp--one-slide",
              pswp.getNumItems() === 1,
            );
        });
        pswp.on("zoomPanUpdate", () => this._onZoomPanUpdate());
      }
      /**
       * @param {UIElementData} elementData
       */
      registerElement(elementData) {
        if (this.isRegistered) {
          this.items.push(new UIElement(this.pswp, elementData));
        } else {
          this.uiElementsData.push(elementData);
        }
      }
      /**
       * Fired each time zoom or pan position is changed.
       * Update classes that control visibility of zoom button and cursor icon.
       *
       * @private
       */
      _onZoomPanUpdate() {
        const { template, currSlide, options } = this.pswp;
        if (this.pswp.opener.isClosing || !template || !currSlide) {
          return;
        }
        let { currZoomLevel } = currSlide;
        if (!this.pswp.opener.isOpen) {
          currZoomLevel = currSlide.zoomLevels.initial;
        }
        if (currZoomLevel === this._lastUpdatedZoomLevel) {
          return;
        }
        this._lastUpdatedZoomLevel = currZoomLevel;
        const currZoomLevelDiff =
          currSlide.zoomLevels.initial - currSlide.zoomLevels.secondary;
        if (Math.abs(currZoomLevelDiff) < 0.01 || !currSlide.isZoomable()) {
          setZoomedIn(template, false);
          template.classList.remove("pswp--zoom-allowed");
          return;
        }
        template.classList.add("pswp--zoom-allowed");
        const potentialZoomLevel =
          currZoomLevel === currSlide.zoomLevels.initial
            ? currSlide.zoomLevels.secondary
            : currSlide.zoomLevels.initial;
        setZoomedIn(template, potentialZoomLevel <= currZoomLevel);
        if (
          options.imageClickAction === "zoom" ||
          options.imageClickAction === "zoom-or-close"
        ) {
          template.classList.add("pswp--click-to-zoom");
        }
      }
    };
    PhotoSwipeEvent2 = class {
      /**
       * @param {T} type
       * @param {PhotoSwipeEventsMap[T]} [details]
       */
      constructor(type, details2) {
        this.type = type;
        this.defaultPrevented = false;
        if (details2) {
          Object.assign(this, details2);
        }
      }
      preventDefault() {
        this.defaultPrevented = true;
      }
    };
    Eventable2 = class {
      constructor() {
        this._listeners = {};
        this._filters = {};
        this.pswp = void 0;
        this.options = void 0;
      }
      /**
       * @template {keyof PhotoSwipeFiltersMap} T
       * @param {T} name
       * @param {PhotoSwipeFiltersMap[T]} fn
       * @param {number} priority
       */
      addFilter(name, fn2, priority = 100) {
        var _this$_filters$name, _this$_filters$name2, _this$pswp;
        if (!this._filters[name]) {
          this._filters[name] = [];
        }
        (_this$_filters$name = this._filters[name]) === null ||
          _this$_filters$name === void 0 ||
          _this$_filters$name.push({
            fn: fn2,
            priority,
          });
        (_this$_filters$name2 = this._filters[name]) === null ||
          _this$_filters$name2 === void 0 ||
          _this$_filters$name2.sort((f1, f2) => f1.priority - f2.priority);
        (_this$pswp = this.pswp) === null ||
          _this$pswp === void 0 ||
          _this$pswp.addFilter(name, fn2, priority);
      }
      /**
       * @template {keyof PhotoSwipeFiltersMap} T
       * @param {T} name
       * @param {PhotoSwipeFiltersMap[T]} fn
       */
      removeFilter(name, fn2) {
        if (this._filters[name]) {
          this._filters[name] = this._filters[name].filter(
            (filter) => filter.fn !== fn2,
          );
        }
        if (this.pswp) {
          this.pswp.removeFilter(name, fn2);
        }
      }
      /**
       * @template {keyof PhotoSwipeFiltersMap} T
       * @param {T} name
       * @param {Parameters<PhotoSwipeFiltersMap[T]>} args
       * @returns {Parameters<PhotoSwipeFiltersMap[T]>[0]}
       */
      applyFilters(name, ...args) {
        var _this$_filters$name3;
        (_this$_filters$name3 = this._filters[name]) === null ||
          _this$_filters$name3 === void 0 ||
          _this$_filters$name3.forEach((filter) => {
            args[0] = filter.fn.apply(this, args);
          });
        return args[0];
      }
      /**
       * @template {keyof PhotoSwipeEventsMap} T
       * @param {T} name
       * @param {EventCallback<T>} fn
       */
      on(name, fn2) {
        var _this$_listeners$name, _this$pswp2;
        if (!this._listeners[name]) {
          this._listeners[name] = [];
        }
        (_this$_listeners$name = this._listeners[name]) === null ||
          _this$_listeners$name === void 0 ||
          _this$_listeners$name.push(fn2);
        (_this$pswp2 = this.pswp) === null ||
          _this$pswp2 === void 0 ||
          _this$pswp2.on(name, fn2);
      }
      /**
       * @template {keyof PhotoSwipeEventsMap} T
       * @param {T} name
       * @param {EventCallback<T>} fn
       */
      off(name, fn2) {
        var _this$pswp3;
        if (this._listeners[name]) {
          this._listeners[name] = this._listeners[name].filter(
            (listener) => fn2 !== listener,
          );
        }
        (_this$pswp3 = this.pswp) === null ||
          _this$pswp3 === void 0 ||
          _this$pswp3.off(name, fn2);
      }
      /**
       * @template {keyof PhotoSwipeEventsMap} T
       * @param {T} name
       * @param {PhotoSwipeEventsMap[T]} [details]
       * @returns {AugmentedEvent<T>}
       */
      dispatch(name, details2) {
        var _this$_listeners$name2;
        if (this.pswp) {
          return this.pswp.dispatch(name, details2);
        }
        const event =
          /** @type {AugmentedEvent<T>} */
          new PhotoSwipeEvent2(name, details2);
        (_this$_listeners$name2 = this._listeners[name]) === null ||
          _this$_listeners$name2 === void 0 ||
          _this$_listeners$name2.forEach((listener) => {
            listener.call(this, event);
          });
        return event;
      }
    };
    Placeholder2 = class {
      /**
       * @param {string | false} imageSrc
       * @param {HTMLElement} container
       */
      constructor(imageSrc, container) {
        this.element = createElement5(
          "pswp__img pswp__img--placeholder",
          imageSrc ? "img" : "div",
          container,
        );
        if (imageSrc) {
          const imgEl =
            /** @type {HTMLImageElement} */
            this.element;
          imgEl.decoding = "async";
          imgEl.alt = "";
          imgEl.src = imageSrc;
          imgEl.setAttribute("role", "presentation");
        }
        this.element.setAttribute("aria-hidden", "true");
      }
      /**
       * @param {number} width
       * @param {number} height
       */
      setDisplayedSize(width2, height2) {
        if (!this.element) {
          return;
        }
        if (this.element.tagName === "IMG") {
          setWidthHeight2(this.element, 250, "auto");
          this.element.style.transformOrigin = "0 0";
          this.element.style.transform = toTransformString2(0, 0, width2 / 250);
        } else {
          setWidthHeight2(this.element, width2, height2);
        }
      }
      destroy() {
        var _this$element;
        if (
          (_this$element = this.element) !== null &&
          _this$element !== void 0 &&
          _this$element.parentNode
        ) {
          this.element.remove();
        }
        this.element = null;
      }
    };
    Content2 = class {
      /**
       * @param {SlideData} itemData Slide data
       * @param {PhotoSwipeBase} instance PhotoSwipe or PhotoSwipeLightbox instance
       * @param {number} index
       */
      constructor(itemData, instance, index) {
        this.instance = instance;
        this.data = itemData;
        this.index = index;
        this.element = void 0;
        this.placeholder = void 0;
        this.slide = void 0;
        this.displayedImageWidth = 0;
        this.displayedImageHeight = 0;
        this.width = Number(this.data.w) || Number(this.data.width) || 0;
        this.height = Number(this.data.h) || Number(this.data.height) || 0;
        this.isAttached = false;
        this.hasSlide = false;
        this.isDecoding = false;
        this.state = LOAD_STATE2.IDLE;
        if (this.data.type) {
          this.type = this.data.type;
        } else if (this.data.src) {
          this.type = "image";
        } else {
          this.type = "html";
        }
        this.instance.dispatch("contentInit", {
          content: this,
        });
      }
      removePlaceholder() {
        if (this.placeholder && !this.keepPlaceholder()) {
          setTimeout(() => {
            if (this.placeholder) {
              this.placeholder.destroy();
              this.placeholder = void 0;
            }
          }, 1e3);
        }
      }
      /**
       * Preload content
       *
       * @param {boolean} isLazy
       * @param {boolean} [reload]
       */
      load(isLazy, reload) {
        if (this.slide && this.usePlaceholder()) {
          if (!this.placeholder) {
            const placeholderSrc = this.instance.applyFilters(
              "placeholderSrc",
              // use  image-based placeholder only for the first slide,
              // as rendering (even small stretched thumbnail) is an expensive operation
              this.data.msrc && this.slide.isFirstSlide
                ? this.data.msrc
                : false,
              this,
            );
            this.placeholder = new Placeholder2(
              placeholderSrc,
              this.slide.container,
            );
          } else {
            const placeholderEl = this.placeholder.element;
            if (placeholderEl && !placeholderEl.parentElement) {
              this.slide.container.prepend(placeholderEl);
            }
          }
        }
        if (this.element && !reload) {
          return;
        }
        if (
          this.instance.dispatch("contentLoad", {
            content: this,
            isLazy,
          }).defaultPrevented
        ) {
          return;
        }
        if (this.isImageContent()) {
          this.element = createElement5("pswp__img", "img");
          if (this.displayedImageWidth) {
            this.loadImage(isLazy);
          }
        } else {
          this.element = createElement5("pswp__content", "div");
          this.element.innerHTML = this.data.html || "";
        }
        if (reload && this.slide) {
          this.slide.updateContentSize(true);
        }
      }
      /**
       * Preload image
       *
       * @param {boolean} isLazy
       */
      loadImage(isLazy) {
        var _this$data$src, _this$data$alt;
        if (
          !this.isImageContent() ||
          !this.element ||
          this.instance.dispatch("contentLoadImage", {
            content: this,
            isLazy,
          }).defaultPrevented
        ) {
          return;
        }
        const imageElement =
          /** @type HTMLImageElement */
          this.element;
        this.updateSrcsetSizes();
        if (this.data.srcset) {
          imageElement.srcset = this.data.srcset;
        }
        imageElement.src =
          (_this$data$src = this.data.src) !== null && _this$data$src !== void 0
            ? _this$data$src
            : "";
        imageElement.alt =
          (_this$data$alt = this.data.alt) !== null && _this$data$alt !== void 0
            ? _this$data$alt
            : "";
        this.state = LOAD_STATE2.LOADING;
        if (imageElement.complete) {
          this.onLoaded();
        } else {
          imageElement.onload = () => {
            this.onLoaded();
          };
          imageElement.onerror = () => {
            this.onError();
          };
        }
      }
      /**
       * Assign slide to content
       *
       * @param {Slide} slide
       */
      setSlide(slide) {
        this.slide = slide;
        this.hasSlide = true;
        this.instance = slide.pswp;
      }
      /**
       * Content load success handler
       */
      onLoaded() {
        this.state = LOAD_STATE2.LOADED;
        if (this.slide && this.element) {
          this.instance.dispatch("loadComplete", {
            slide: this.slide,
            content: this,
          });
          if (
            this.slide.isActive &&
            this.slide.heavyAppended &&
            !this.element.parentNode
          ) {
            this.append();
            this.slide.updateContentSize(true);
          }
          if (
            this.state === LOAD_STATE2.LOADED ||
            this.state === LOAD_STATE2.ERROR
          ) {
            this.removePlaceholder();
          }
        }
      }
      /**
       * Content load error handler
       */
      onError() {
        this.state = LOAD_STATE2.ERROR;
        if (this.slide) {
          this.displayError();
          this.instance.dispatch("loadComplete", {
            slide: this.slide,
            isError: true,
            content: this,
          });
          this.instance.dispatch("loadError", {
            slide: this.slide,
            content: this,
          });
        }
      }
      /**
       * @returns {Boolean} If the content is currently loading
       */
      isLoading() {
        return this.instance.applyFilters(
          "isContentLoading",
          this.state === LOAD_STATE2.LOADING,
          this,
        );
      }
      /**
       * @returns {Boolean} If the content is in error state
       */
      isError() {
        return this.state === LOAD_STATE2.ERROR;
      }
      /**
       * @returns {boolean} If the content is image
       */
      isImageContent() {
        return this.type === "image";
      }
      /**
       * Update content size
       *
       * @param {Number} width
       * @param {Number} height
       */
      setDisplayedSize(width2, height2) {
        if (!this.element) {
          return;
        }
        if (this.placeholder) {
          this.placeholder.setDisplayedSize(width2, height2);
        }
        if (
          this.instance.dispatch("contentResize", {
            content: this,
            width: width2,
            height: height2,
          }).defaultPrevented
        ) {
          return;
        }
        setWidthHeight2(this.element, width2, height2);
        if (this.isImageContent() && !this.isError()) {
          const isInitialSizeUpdate = !this.displayedImageWidth && width2;
          this.displayedImageWidth = width2;
          this.displayedImageHeight = height2;
          if (isInitialSizeUpdate) {
            this.loadImage(false);
          } else {
            this.updateSrcsetSizes();
          }
          if (this.slide) {
            this.instance.dispatch("imageSizeChange", {
              slide: this.slide,
              width: width2,
              height: height2,
              content: this,
            });
          }
        }
      }
      /**
       * @returns {boolean} If the content can be zoomed
       */
      isZoomable() {
        return this.instance.applyFilters(
          "isContentZoomable",
          this.isImageContent() && this.state !== LOAD_STATE2.ERROR,
          this,
        );
      }
      /**
       * Update image srcset sizes attribute based on width and height
       */
      updateSrcsetSizes() {
        if (!this.isImageContent() || !this.element || !this.data.srcset) {
          return;
        }
        const image =
          /** @type HTMLImageElement */
          this.element;
        const sizesWidth = this.instance.applyFilters(
          "srcsetSizesWidth",
          this.displayedImageWidth,
          this,
        );
        if (
          !image.dataset.largestUsedSize ||
          sizesWidth > parseInt(image.dataset.largestUsedSize, 10)
        ) {
          image.sizes = sizesWidth + "px";
          image.dataset.largestUsedSize = String(sizesWidth);
        }
      }
      /**
       * @returns {boolean} If content should use a placeholder (from msrc by default)
       */
      usePlaceholder() {
        return this.instance.applyFilters(
          "useContentPlaceholder",
          this.isImageContent(),
          this,
        );
      }
      /**
       * Preload content with lazy-loading param
       */
      lazyLoad() {
        if (
          this.instance.dispatch("contentLazyLoad", {
            content: this,
          }).defaultPrevented
        ) {
          return;
        }
        this.load(true);
      }
      /**
       * @returns {boolean} If placeholder should be kept after content is loaded
       */
      keepPlaceholder() {
        return this.instance.applyFilters(
          "isKeepingPlaceholder",
          this.isLoading(),
          this,
        );
      }
      /**
       * Destroy the content
       */
      destroy() {
        this.hasSlide = false;
        this.slide = void 0;
        if (
          this.instance.dispatch("contentDestroy", {
            content: this,
          }).defaultPrevented
        ) {
          return;
        }
        this.remove();
        if (this.placeholder) {
          this.placeholder.destroy();
          this.placeholder = void 0;
        }
        if (this.isImageContent() && this.element) {
          this.element.onload = null;
          this.element.onerror = null;
          this.element = void 0;
        }
      }
      /**
       * Display error message
       */
      displayError() {
        if (this.slide) {
          var _this$instance$option, _this$instance$option2;
          let errorMsgEl = createElement5("pswp__error-msg", "div");
          errorMsgEl.innerText =
            (_this$instance$option =
              (_this$instance$option2 = this.instance.options) === null ||
              _this$instance$option2 === void 0
                ? void 0
                : _this$instance$option2.errorMsg) !== null &&
            _this$instance$option !== void 0
              ? _this$instance$option
              : "";
          errorMsgEl =
            /** @type {HTMLDivElement} */
            this.instance.applyFilters("contentErrorElement", errorMsgEl, this);
          this.element = createElement5(
            "pswp__content pswp__error-msg-container",
            "div",
          );
          this.element.appendChild(errorMsgEl);
          this.slide.container.innerText = "";
          this.slide.container.appendChild(this.element);
          this.slide.updateContentSize(true);
          this.removePlaceholder();
        }
      }
      /**
       * Append the content
       */
      append() {
        if (this.isAttached || !this.element) {
          return;
        }
        this.isAttached = true;
        if (this.state === LOAD_STATE2.ERROR) {
          this.displayError();
          return;
        }
        if (
          this.instance.dispatch("contentAppend", {
            content: this,
          }).defaultPrevented
        ) {
          return;
        }
        const supportsDecode = "decode" in this.element;
        if (this.isImageContent()) {
          if (
            supportsDecode &&
            this.slide &&
            (!this.slide.isActive || isSafari2())
          ) {
            this.isDecoding = true;
            this.element
              .decode()
              .catch(() => {})
              .finally(() => {
                this.isDecoding = false;
                this.appendImage();
              });
          } else {
            this.appendImage();
          }
        } else if (this.slide && !this.element.parentNode) {
          this.slide.container.appendChild(this.element);
        }
      }
      /**
       * Activate the slide,
       * active slide is generally the current one,
       * meaning the user can see it.
       */
      activate() {
        if (
          this.instance.dispatch("contentActivate", {
            content: this,
          }).defaultPrevented ||
          !this.slide
        ) {
          return;
        }
        if (this.isImageContent() && this.isDecoding && !isSafari2()) {
          this.appendImage();
        } else if (this.isError()) {
          this.load(false, true);
        }
        if (this.slide.holderElement) {
          this.slide.holderElement.setAttribute("aria-hidden", "false");
        }
      }
      /**
       * Deactivate the content
       */
      deactivate() {
        this.instance.dispatch("contentDeactivate", {
          content: this,
        });
        if (this.slide && this.slide.holderElement) {
          this.slide.holderElement.setAttribute("aria-hidden", "true");
        }
      }
      /**
       * Remove the content from DOM
       */
      remove() {
        this.isAttached = false;
        if (
          this.instance.dispatch("contentRemove", {
            content: this,
          }).defaultPrevented
        ) {
          return;
        }
        if (this.element && this.element.parentNode) {
          this.element.remove();
        }
        if (this.placeholder && this.placeholder.element) {
          this.placeholder.element.remove();
        }
      }
      /**
       * Append the image content to slide container
       */
      appendImage() {
        if (!this.isAttached) {
          return;
        }
        if (
          this.instance.dispatch("contentAppendImage", {
            content: this,
          }).defaultPrevented
        ) {
          return;
        }
        if (this.slide && this.element && !this.element.parentNode) {
          this.slide.container.appendChild(this.element);
        }
        if (
          this.state === LOAD_STATE2.LOADED ||
          this.state === LOAD_STATE2.ERROR
        ) {
          this.removePlaceholder();
        }
      }
    };
    MIN_SLIDES_TO_CACHE = 5;
    ContentLoader = class {
      /**
       * @param {PhotoSwipe} pswp
       */
      constructor(pswp) {
        this.pswp = pswp;
        this.limit = Math.max(
          pswp.options.preload[0] + pswp.options.preload[1] + 1,
          MIN_SLIDES_TO_CACHE,
        );
        this._cachedItems = [];
      }
      /**
       * Lazy load nearby slides based on `preload` option.
       *
       * @param {number} [diff] Difference between slide indexes that was changed recently, or 0.
       */
      updateLazy(diff) {
        const { pswp } = this;
        if (pswp.dispatch("lazyLoad").defaultPrevented) {
          return;
        }
        const { preload } = pswp.options;
        const isForward = diff === void 0 ? true : diff >= 0;
        let i;
        for (i = 0; i <= preload[1]; i++) {
          this.loadSlideByIndex(pswp.currIndex + (isForward ? i : -i));
        }
        for (i = 1; i <= preload[0]; i++) {
          this.loadSlideByIndex(pswp.currIndex + (isForward ? -i : i));
        }
      }
      /**
       * @param {number} initialIndex
       */
      loadSlideByIndex(initialIndex) {
        const index = this.pswp.getLoopedIndex(initialIndex);
        let content = this.getContentByIndex(index);
        if (!content) {
          content = lazyLoadSlide2(index, this.pswp);
          if (content) {
            this.addToCache(content);
          }
        }
      }
      /**
       * @param {Slide} slide
       * @returns {Content}
       */
      getContentBySlide(slide) {
        let content = this.getContentByIndex(slide.index);
        if (!content) {
          content = this.pswp.createContentFromData(slide.data, slide.index);
          this.addToCache(content);
        }
        content.setSlide(slide);
        return content;
      }
      /**
       * @param {Content} content
       */
      addToCache(content) {
        this.removeByIndex(content.index);
        this._cachedItems.push(content);
        if (this._cachedItems.length > this.limit) {
          const indexToRemove = this._cachedItems.findIndex((item) => {
            return !item.isAttached && !item.hasSlide;
          });
          if (indexToRemove !== -1) {
            const removedItem = this._cachedItems.splice(indexToRemove, 1)[0];
            removedItem.destroy();
          }
        }
      }
      /**
       * Removes an image from cache, does not destroy() it, just removes.
       *
       * @param {number} index
       */
      removeByIndex(index) {
        const indexToRemove = this._cachedItems.findIndex(
          (item) => item.index === index,
        );
        if (indexToRemove !== -1) {
          this._cachedItems.splice(indexToRemove, 1);
        }
      }
      /**
       * @param {number} index
       * @returns {Content | undefined}
       */
      getContentByIndex(index) {
        return this._cachedItems.find((content) => content.index === index);
      }
      destroy() {
        this._cachedItems.forEach((content) => content.destroy());
        this._cachedItems = [];
      }
    };
    PhotoSwipeBase2 = class extends Eventable2 {
      /**
       * Get total number of slides
       *
       * @returns {number}
       */
      getNumItems() {
        var _this$options;
        let numItems = 0;
        const dataSource =
          (_this$options = this.options) === null || _this$options === void 0
            ? void 0
            : _this$options.dataSource;
        if (dataSource && "length" in dataSource) {
          numItems = dataSource.length;
        } else if (dataSource && "gallery" in dataSource) {
          if (!dataSource.items) {
            dataSource.items = this._getGalleryDOMElements(dataSource.gallery);
          }
          if (dataSource.items) {
            numItems = dataSource.items.length;
          }
        }
        const event = this.dispatch("numItems", {
          dataSource,
          numItems,
        });
        return this.applyFilters("numItems", event.numItems, dataSource);
      }
      /**
       * @param {SlideData} slideData
       * @param {number} index
       * @returns {Content}
       */
      createContentFromData(slideData, index) {
        return new Content2(slideData, this, index);
      }
      /**
       * Get item data by index.
       *
       * "item data" should contain normalized information that PhotoSwipe needs to generate a slide.
       * For example, it may contain properties like
       * `src`, `srcset`, `w`, `h`, which will be used to generate a slide with image.
       *
       * @param {number} index
       * @returns {SlideData}
       */
      getItemData(index) {
        var _this$options2;
        const dataSource =
          (_this$options2 = this.options) === null || _this$options2 === void 0
            ? void 0
            : _this$options2.dataSource;
        let dataSourceItem = {};
        if (Array.isArray(dataSource)) {
          dataSourceItem = dataSource[index];
        } else if (dataSource && "gallery" in dataSource) {
          if (!dataSource.items) {
            dataSource.items = this._getGalleryDOMElements(dataSource.gallery);
          }
          dataSourceItem = dataSource.items[index];
        }
        let itemData = dataSourceItem;
        if (itemData instanceof Element) {
          itemData = this._domElementToItemData(itemData);
        }
        const event = this.dispatch("itemData", {
          itemData: itemData || {},
          index,
        });
        return this.applyFilters("itemData", event.itemData, index);
      }
      /**
       * Get array of gallery DOM elements,
       * based on childSelector and gallery element.
       *
       * @param {HTMLElement} galleryElement
       * @returns {HTMLElement[]}
       */
      _getGalleryDOMElements(galleryElement) {
        var _this$options3, _this$options4;
        if (
          ((_this$options3 = this.options) !== null &&
            _this$options3 !== void 0 &&
            _this$options3.children) ||
          ((_this$options4 = this.options) !== null &&
            _this$options4 !== void 0 &&
            _this$options4.childSelector)
        ) {
          return (
            getElementsFromOption2(
              this.options.children,
              this.options.childSelector,
              galleryElement,
            ) || []
          );
        }
        return [galleryElement];
      }
      /**
       * Converts DOM element to item data object.
       *
       * @param {HTMLElement} element DOM element
       * @returns {SlideData}
       */
      _domElementToItemData(element) {
        const itemData = {
          element,
        };
        const linkEl =
          /** @type {HTMLAnchorElement} */
          element.tagName === "A" ? element : element.querySelector("a");
        if (linkEl) {
          itemData.src = linkEl.dataset.pswpSrc || linkEl.href;
          if (linkEl.dataset.pswpSrcset) {
            itemData.srcset = linkEl.dataset.pswpSrcset;
          }
          itemData.width = linkEl.dataset.pswpWidth
            ? parseInt(linkEl.dataset.pswpWidth, 10)
            : 0;
          itemData.height = linkEl.dataset.pswpHeight
            ? parseInt(linkEl.dataset.pswpHeight, 10)
            : 0;
          itemData.w = itemData.width;
          itemData.h = itemData.height;
          if (linkEl.dataset.pswpType) {
            itemData.type = linkEl.dataset.pswpType;
          }
          const thumbnailEl = element.querySelector("img");
          if (thumbnailEl) {
            var _thumbnailEl$getAttri;
            itemData.msrc = thumbnailEl.currentSrc || thumbnailEl.src;
            itemData.alt =
              (_thumbnailEl$getAttri = thumbnailEl.getAttribute("alt")) !==
                null && _thumbnailEl$getAttri !== void 0
                ? _thumbnailEl$getAttri
                : "";
          }
          if (linkEl.dataset.pswpCropped || linkEl.dataset.cropped) {
            itemData.thumbCropped = true;
          }
        }
        return this.applyFilters("domItemData", itemData, element, linkEl);
      }
      /**
       * Lazy-load by slide data
       *
       * @param {SlideData} itemData Data about the slide
       * @param {number} index
       * @returns {Content} Image that is being decoded or false.
       */
      lazyLoadData(itemData, index) {
        return lazyLoadData2(itemData, this, index);
      }
    };
    MIN_OPACITY = 3e-3;
    Opener = class {
      /**
       * @param {PhotoSwipe} pswp
       */
      constructor(pswp) {
        this.pswp = pswp;
        this.isClosed = true;
        this.isOpen = false;
        this.isClosing = false;
        this.isOpening = false;
        this._duration = void 0;
        this._useAnimation = false;
        this._croppedZoom = false;
        this._animateRootOpacity = false;
        this._animateBgOpacity = false;
        this._placeholder = void 0;
        this._opacityElement = void 0;
        this._cropContainer1 = void 0;
        this._cropContainer2 = void 0;
        this._thumbBounds = void 0;
        this._prepareOpen = this._prepareOpen.bind(this);
        pswp.on("firstZoomPan", this._prepareOpen);
      }
      open() {
        this._prepareOpen();
        this._start();
      }
      close() {
        if (this.isClosed || this.isClosing || this.isOpening) {
          return;
        }
        const slide = this.pswp.currSlide;
        this.isOpen = false;
        this.isOpening = false;
        this.isClosing = true;
        this._duration = this.pswp.options.hideAnimationDuration;
        if (
          slide &&
          slide.currZoomLevel * slide.width >=
            this.pswp.options.maxWidthToAnimate
        ) {
          this._duration = 0;
        }
        this._applyStartProps();
        setTimeout(
          () => {
            this._start();
          },
          this._croppedZoom ? 30 : 0,
        );
      }
      /** @private */
      _prepareOpen() {
        this.pswp.off("firstZoomPan", this._prepareOpen);
        if (!this.isOpening) {
          const slide = this.pswp.currSlide;
          this.isOpening = true;
          this.isClosing = false;
          this._duration = this.pswp.options.showAnimationDuration;
          if (
            slide &&
            slide.zoomLevels.initial * slide.width >=
              this.pswp.options.maxWidthToAnimate
          ) {
            this._duration = 0;
          }
          this._applyStartProps();
        }
      }
      /** @private */
      _applyStartProps() {
        const { pswp } = this;
        const slide = this.pswp.currSlide;
        const { options } = pswp;
        if (options.showHideAnimationType === "fade") {
          options.showHideOpacity = true;
          this._thumbBounds = void 0;
        } else if (options.showHideAnimationType === "none") {
          options.showHideOpacity = false;
          this._duration = 0;
          this._thumbBounds = void 0;
        } else if (this.isOpening && pswp._initialThumbBounds) {
          this._thumbBounds = pswp._initialThumbBounds;
        } else {
          this._thumbBounds = this.pswp.getThumbBounds();
        }
        this._placeholder =
          slide === null || slide === void 0
            ? void 0
            : slide.getPlaceholderElement();
        pswp.animations.stopAll();
        this._useAnimation = Boolean(this._duration && this._duration > 50);
        this._animateZoom =
          Boolean(this._thumbBounds) &&
          (slide === null || slide === void 0
            ? void 0
            : slide.content.usePlaceholder()) &&
          (!this.isClosing || !pswp.mainScroll.isShifted());
        if (!this._animateZoom) {
          this._animateRootOpacity = true;
          if (this.isOpening && slide) {
            slide.zoomAndPanToInitial();
            slide.applyCurrentZoomPan();
          }
        } else {
          var _options$showHideOpac;
          this._animateRootOpacity =
            (_options$showHideOpac = options.showHideOpacity) !== null &&
            _options$showHideOpac !== void 0
              ? _options$showHideOpac
              : false;
        }
        this._animateBgOpacity =
          !this._animateRootOpacity &&
          this.pswp.options.bgOpacity > MIN_OPACITY;
        this._opacityElement = this._animateRootOpacity
          ? pswp.element
          : pswp.bg;
        if (!this._useAnimation) {
          this._duration = 0;
          this._animateZoom = false;
          this._animateBgOpacity = false;
          this._animateRootOpacity = true;
          if (this.isOpening) {
            if (pswp.element) {
              pswp.element.style.opacity = String(MIN_OPACITY);
            }
            pswp.applyBgOpacity(1);
          }
          return;
        }
        if (
          this._animateZoom &&
          this._thumbBounds &&
          this._thumbBounds.innerRect
        ) {
          var _this$pswp$currSlide;
          this._croppedZoom = true;
          this._cropContainer1 = this.pswp.container;
          this._cropContainer2 =
            (_this$pswp$currSlide = this.pswp.currSlide) === null ||
            _this$pswp$currSlide === void 0
              ? void 0
              : _this$pswp$currSlide.holderElement;
          if (pswp.container) {
            pswp.container.style.overflow = "hidden";
            pswp.container.style.width = pswp.viewportSize.x + "px";
          }
        } else {
          this._croppedZoom = false;
        }
        if (this.isOpening) {
          if (this._animateRootOpacity) {
            if (pswp.element) {
              pswp.element.style.opacity = String(MIN_OPACITY);
            }
            pswp.applyBgOpacity(1);
          } else {
            if (this._animateBgOpacity && pswp.bg) {
              pswp.bg.style.opacity = String(MIN_OPACITY);
            }
            if (pswp.element) {
              pswp.element.style.opacity = "1";
            }
          }
          if (this._animateZoom) {
            this._setClosedStateZoomPan();
            if (this._placeholder) {
              this._placeholder.style.willChange = "transform";
              this._placeholder.style.opacity = String(MIN_OPACITY);
            }
          }
        } else if (this.isClosing) {
          if (pswp.mainScroll.itemHolders[0]) {
            pswp.mainScroll.itemHolders[0].el.style.display = "none";
          }
          if (pswp.mainScroll.itemHolders[2]) {
            pswp.mainScroll.itemHolders[2].el.style.display = "none";
          }
          if (this._croppedZoom) {
            if (pswp.mainScroll.x !== 0) {
              pswp.mainScroll.resetPosition();
              pswp.mainScroll.resize();
            }
          }
        }
      }
      /** @private */
      _start() {
        if (
          this.isOpening &&
          this._useAnimation &&
          this._placeholder &&
          this._placeholder.tagName === "IMG"
        ) {
          new Promise((resolve) => {
            let decoded = false;
            let isDelaying = true;
            decodeImage(
              /** @type {HTMLImageElement} */
              this._placeholder,
            ).finally(() => {
              decoded = true;
              if (!isDelaying) {
                resolve(true);
              }
            });
            setTimeout(() => {
              isDelaying = false;
              if (decoded) {
                resolve(true);
              }
            }, 50);
            setTimeout(resolve, 250);
          }).finally(() => this._initiate());
        } else {
          this._initiate();
        }
      }
      /** @private */
      _initiate() {
        var _this$pswp$element, _this$pswp$element2;
        (_this$pswp$element = this.pswp.element) === null ||
          _this$pswp$element === void 0 ||
          _this$pswp$element.style.setProperty(
            "--pswp-transition-duration",
            this._duration + "ms",
          );
        this.pswp.dispatch(
          this.isOpening ? "openingAnimationStart" : "closingAnimationStart",
        );
        this.pswp.dispatch(
          /** @type {'initialZoomIn' | 'initialZoomOut'} */
          "initialZoom" + (this.isOpening ? "In" : "Out"),
        );
        (_this$pswp$element2 = this.pswp.element) === null ||
          _this$pswp$element2 === void 0 ||
          _this$pswp$element2.classList.toggle(
            "pswp--ui-visible",
            this.isOpening,
          );
        if (this.isOpening) {
          if (this._placeholder) {
            this._placeholder.style.opacity = "1";
          }
          this._animateToOpenState();
        } else if (this.isClosing) {
          this._animateToClosedState();
        }
        if (!this._useAnimation) {
          this._onAnimationComplete();
        }
      }
      /** @private */
      _onAnimationComplete() {
        const { pswp } = this;
        this.isOpen = this.isOpening;
        this.isClosed = this.isClosing;
        this.isOpening = false;
        this.isClosing = false;
        pswp.dispatch(
          this.isOpen ? "openingAnimationEnd" : "closingAnimationEnd",
        );
        pswp.dispatch(
          /** @type {'initialZoomInEnd' | 'initialZoomOutEnd'} */
          "initialZoom" + (this.isOpen ? "InEnd" : "OutEnd"),
        );
        if (this.isClosed) {
          pswp.destroy();
        } else if (this.isOpen) {
          var _pswp$currSlide;
          if (this._animateZoom && pswp.container) {
            pswp.container.style.overflow = "visible";
            pswp.container.style.width = "100%";
          }
          (_pswp$currSlide = pswp.currSlide) === null ||
            _pswp$currSlide === void 0 ||
            _pswp$currSlide.applyCurrentZoomPan();
        }
      }
      /** @private */
      _animateToOpenState() {
        const { pswp } = this;
        if (this._animateZoom) {
          if (
            this._croppedZoom &&
            this._cropContainer1 &&
            this._cropContainer2
          ) {
            this._animateTo(
              this._cropContainer1,
              "transform",
              "translate3d(0,0,0)",
            );
            this._animateTo(this._cropContainer2, "transform", "none");
          }
          if (pswp.currSlide) {
            pswp.currSlide.zoomAndPanToInitial();
            this._animateTo(
              pswp.currSlide.container,
              "transform",
              pswp.currSlide.getCurrentTransform(),
            );
          }
        }
        if (this._animateBgOpacity && pswp.bg) {
          this._animateTo(pswp.bg, "opacity", String(pswp.options.bgOpacity));
        }
        if (this._animateRootOpacity && pswp.element) {
          this._animateTo(pswp.element, "opacity", "1");
        }
      }
      /** @private */
      _animateToClosedState() {
        const { pswp } = this;
        if (this._animateZoom) {
          this._setClosedStateZoomPan(true);
        }
        if (this._animateBgOpacity && pswp.bgOpacity > 0.01 && pswp.bg) {
          this._animateTo(pswp.bg, "opacity", "0");
        }
        if (this._animateRootOpacity && pswp.element) {
          this._animateTo(pswp.element, "opacity", "0");
        }
      }
      /**
       * @private
       * @param {boolean} [animate]
       */
      _setClosedStateZoomPan(animate) {
        if (!this._thumbBounds) return;
        const { pswp } = this;
        const { innerRect } = this._thumbBounds;
        const { currSlide, viewportSize } = pswp;
        if (
          this._croppedZoom &&
          innerRect &&
          this._cropContainer1 &&
          this._cropContainer2
        ) {
          const containerOnePanX =
            -viewportSize.x + (this._thumbBounds.x - innerRect.x) + innerRect.w;
          const containerOnePanY =
            -viewportSize.y + (this._thumbBounds.y - innerRect.y) + innerRect.h;
          const containerTwoPanX = viewportSize.x - innerRect.w;
          const containerTwoPanY = viewportSize.y - innerRect.h;
          if (animate) {
            this._animateTo(
              this._cropContainer1,
              "transform",
              toTransformString2(containerOnePanX, containerOnePanY),
            );
            this._animateTo(
              this._cropContainer2,
              "transform",
              toTransformString2(containerTwoPanX, containerTwoPanY),
            );
          } else {
            setTransform(
              this._cropContainer1,
              containerOnePanX,
              containerOnePanY,
            );
            setTransform(
              this._cropContainer2,
              containerTwoPanX,
              containerTwoPanY,
            );
          }
        }
        if (currSlide) {
          equalizePoints(currSlide.pan, innerRect || this._thumbBounds);
          currSlide.currZoomLevel = this._thumbBounds.w / currSlide.width;
          if (animate) {
            this._animateTo(
              currSlide.container,
              "transform",
              currSlide.getCurrentTransform(),
            );
          } else {
            currSlide.applyCurrentZoomPan();
          }
        }
      }
      /**
       * @private
       * @param {HTMLElement} target
       * @param {'transform' | 'opacity'} prop
       * @param {string} propValue
       */
      _animateTo(target, prop, propValue) {
        if (!this._duration) {
          target.style[prop] = propValue;
          return;
        }
        const { animations } = this.pswp;
        const animProps = {
          duration: this._duration,
          easing: this.pswp.options.easing,
          onComplete: () => {
            if (!animations.activeAnimations.length) {
              this._onAnimationComplete();
            }
          },
          target,
        };
        animProps[prop] = propValue;
        animations.startTransition(animProps);
      }
    };
    defaultOptions = {
      allowPanToNext: true,
      spacing: 0.1,
      loop: true,
      pinchToClose: true,
      closeOnVerticalDrag: true,
      hideAnimationDuration: 333,
      showAnimationDuration: 333,
      zoomAnimationDuration: 333,
      escKey: true,
      arrowKeys: true,
      trapFocus: true,
      returnFocus: true,
      maxWidthToAnimate: 4e3,
      clickToCloseNonZoomable: true,
      imageClickAction: "zoom-or-close",
      bgClickAction: "close",
      tapAction: "toggle-controls",
      doubleTapAction: "zoom",
      indexIndicatorSep: " / ",
      preloaderDelay: 2e3,
      bgOpacity: 0.8,
      index: 0,
      errorMsg: "The image cannot be loaded",
      preload: [1, 2],
      easing: "cubic-bezier(.4,0,.22,1)",
    };
    PhotoSwipe = class extends PhotoSwipeBase2 {
      /**
       * @param {PhotoSwipeOptions} [options]
       */
      constructor(options) {
        super();
        this.options = this._prepareOptions(options || {});
        this.offset = {
          x: 0,
          y: 0,
        };
        this._prevViewportSize = {
          x: 0,
          y: 0,
        };
        this.viewportSize = {
          x: 0,
          y: 0,
        };
        this.bgOpacity = 1;
        this.currIndex = 0;
        this.potentialIndex = 0;
        this.isOpen = false;
        this.isDestroying = false;
        this.hasMouse = false;
        this._initialItemData = {};
        this._initialThumbBounds = void 0;
        this.topBar = void 0;
        this.element = void 0;
        this.template = void 0;
        this.container = void 0;
        this.scrollWrap = void 0;
        this.currSlide = void 0;
        this.events = new DOMEvents();
        this.animations = new Animations();
        this.mainScroll = new MainScroll(this);
        this.gestures = new Gestures(this);
        this.opener = new Opener(this);
        this.keyboard = new Keyboard(this);
        this.contentLoader = new ContentLoader(this);
      }
      /** @returns {boolean} */
      init() {
        if (this.isOpen || this.isDestroying) {
          return false;
        }
        this.isOpen = true;
        this.dispatch("init");
        this.dispatch("beforeOpen");
        this._createMainStructure();
        let rootClasses = "pswp--open";
        if (this.gestures.supportsTouch) {
          rootClasses += " pswp--touch";
        }
        if (this.options.mainClass) {
          rootClasses += " " + this.options.mainClass;
        }
        if (this.element) {
          this.element.className += " " + rootClasses;
        }
        this.currIndex = this.options.index || 0;
        this.potentialIndex = this.currIndex;
        this.dispatch("firstUpdate");
        this.scrollWheel = new ScrollWheel(this);
        if (
          Number.isNaN(this.currIndex) ||
          this.currIndex < 0 ||
          this.currIndex >= this.getNumItems()
        ) {
          this.currIndex = 0;
        }
        if (!this.gestures.supportsTouch) {
          this.mouseDetected();
        }
        this.updateSize();
        this.offset.y = window.pageYOffset;
        this._initialItemData = this.getItemData(this.currIndex);
        this.dispatch("gettingData", {
          index: this.currIndex,
          data: this._initialItemData,
          slide: void 0,
        });
        this._initialThumbBounds = this.getThumbBounds();
        this.dispatch("initialLayout");
        this.on("openingAnimationEnd", () => {
          const { itemHolders } = this.mainScroll;
          if (itemHolders[0]) {
            itemHolders[0].el.style.display = "block";
            this.setContent(itemHolders[0], this.currIndex - 1);
          }
          if (itemHolders[2]) {
            itemHolders[2].el.style.display = "block";
            this.setContent(itemHolders[2], this.currIndex + 1);
          }
          this.appendHeavy();
          this.contentLoader.updateLazy();
          this.events.add(window, "resize", this._handlePageResize.bind(this));
          this.events.add(
            window,
            "scroll",
            this._updatePageScrollOffset.bind(this),
          );
          this.dispatch("bindEvents");
        });
        if (this.mainScroll.itemHolders[1]) {
          this.setContent(this.mainScroll.itemHolders[1], this.currIndex);
        }
        this.dispatch("change");
        this.opener.open();
        this.dispatch("afterInit");
        return true;
      }
      /**
       * Get looped slide index
       * (for example, -1 will return the last slide)
       *
       * @param {number} index
       * @returns {number}
       */
      getLoopedIndex(index) {
        const numSlides = this.getNumItems();
        if (this.options.loop) {
          if (index > numSlides - 1) {
            index -= numSlides;
          }
          if (index < 0) {
            index += numSlides;
          }
        }
        return clamp2(index, 0, numSlides - 1);
      }
      appendHeavy() {
        this.mainScroll.itemHolders.forEach((itemHolder) => {
          var _itemHolder$slide;
          (_itemHolder$slide = itemHolder.slide) === null ||
            _itemHolder$slide === void 0 ||
            _itemHolder$slide.appendHeavy();
        });
      }
      /**
       * Change the slide
       * @param {number} index New index
       */
      goTo(index) {
        this.mainScroll.moveIndexBy(
          this.getLoopedIndex(index) - this.potentialIndex,
        );
      }
      /**
       * Go to the next slide.
       */
      next() {
        this.goTo(this.potentialIndex + 1);
      }
      /**
       * Go to the previous slide.
       */
      prev() {
        this.goTo(this.potentialIndex - 1);
      }
      /**
       * @see slide/slide.js zoomTo
       *
       * @param {Parameters<Slide['zoomTo']>} args
       */
      zoomTo(...args) {
        var _this$currSlide;
        (_this$currSlide = this.currSlide) === null ||
          _this$currSlide === void 0 ||
          _this$currSlide.zoomTo(...args);
      }
      /**
       * @see slide/slide.js toggleZoom
       */
      toggleZoom() {
        var _this$currSlide2;
        (_this$currSlide2 = this.currSlide) === null ||
          _this$currSlide2 === void 0 ||
          _this$currSlide2.toggleZoom();
      }
      /**
       * Close the gallery.
       * After closing transition ends - destroy it
       */
      close() {
        if (!this.opener.isOpen || this.isDestroying) {
          return;
        }
        this.isDestroying = true;
        this.dispatch("close");
        this.events.removeAll();
        this.opener.close();
      }
      /**
       * Destroys the gallery:
       * - instantly closes the gallery
       * - unbinds events,
       * - cleans intervals and timeouts
       * - removes elements from DOM
       */
      destroy() {
        var _this$element;
        if (!this.isDestroying) {
          this.options.showHideAnimationType = "none";
          this.close();
          return;
        }
        this.dispatch("destroy");
        this._listeners = {};
        if (this.scrollWrap) {
          this.scrollWrap.ontouchmove = null;
          this.scrollWrap.ontouchend = null;
        }
        (_this$element = this.element) === null ||
          _this$element === void 0 ||
          _this$element.remove();
        this.mainScroll.itemHolders.forEach((itemHolder) => {
          var _itemHolder$slide2;
          (_itemHolder$slide2 = itemHolder.slide) === null ||
            _itemHolder$slide2 === void 0 ||
            _itemHolder$slide2.destroy();
        });
        this.contentLoader.destroy();
        this.events.removeAll();
      }
      /**
       * Refresh/reload content of a slide by its index
       *
       * @param {number} slideIndex
       */
      refreshSlideContent(slideIndex) {
        this.contentLoader.removeByIndex(slideIndex);
        this.mainScroll.itemHolders.forEach((itemHolder, i) => {
          var _this$currSlide$index, _this$currSlide3;
          let potentialHolderIndex =
            ((_this$currSlide$index =
              (_this$currSlide3 = this.currSlide) === null ||
              _this$currSlide3 === void 0
                ? void 0
                : _this$currSlide3.index) !== null &&
            _this$currSlide$index !== void 0
              ? _this$currSlide$index
              : 0) -
            1 +
            i;
          if (this.canLoop()) {
            potentialHolderIndex = this.getLoopedIndex(potentialHolderIndex);
          }
          if (potentialHolderIndex === slideIndex) {
            this.setContent(itemHolder, slideIndex, true);
            if (i === 1) {
              var _itemHolder$slide3;
              this.currSlide = itemHolder.slide;
              (_itemHolder$slide3 = itemHolder.slide) === null ||
                _itemHolder$slide3 === void 0 ||
                _itemHolder$slide3.setIsActive(true);
            }
          }
        });
        this.dispatch("change");
      }
      /**
       * Set slide content
       *
       * @param {ItemHolder} holder mainScroll.itemHolders array item
       * @param {number} index Slide index
       * @param {boolean} [force] If content should be set even if index wasn't changed
       */
      setContent(holder, index, force) {
        if (this.canLoop()) {
          index = this.getLoopedIndex(index);
        }
        if (holder.slide) {
          if (holder.slide.index === index && !force) {
            return;
          }
          holder.slide.destroy();
          holder.slide = void 0;
        }
        if (!this.canLoop() && (index < 0 || index >= this.getNumItems())) {
          return;
        }
        const itemData = this.getItemData(index);
        holder.slide = new Slide3(itemData, index, this);
        if (index === this.currIndex) {
          this.currSlide = holder.slide;
        }
        holder.slide.append(holder.el);
      }
      /** @returns {Point} */
      getViewportCenterPoint() {
        return {
          x: this.viewportSize.x / 2,
          y: this.viewportSize.y / 2,
        };
      }
      /**
       * Update size of all elements.
       * Executed on init and on page resize.
       *
       * @param {boolean} [force] Update size even if size of viewport was not changed.
       */
      updateSize(force) {
        if (this.isDestroying) {
          return;
        }
        const newViewportSize = getViewportSize2(this.options, this);
        if (!force && pointsEqual(newViewportSize, this._prevViewportSize)) {
          return;
        }
        equalizePoints(this._prevViewportSize, newViewportSize);
        this.dispatch("beforeResize");
        equalizePoints(this.viewportSize, this._prevViewportSize);
        this._updatePageScrollOffset();
        this.dispatch("viewportSize");
        this.mainScroll.resize(this.opener.isOpen);
        if (!this.hasMouse && window.matchMedia("(any-hover: hover)").matches) {
          this.mouseDetected();
        }
        this.dispatch("resize");
      }
      /**
       * @param {number} opacity
       */
      applyBgOpacity(opacity) {
        this.bgOpacity = Math.max(opacity, 0);
        if (this.bg) {
          this.bg.style.opacity = String(
            this.bgOpacity * this.options.bgOpacity,
          );
        }
      }
      /**
       * Whether mouse is detected
       */
      mouseDetected() {
        if (!this.hasMouse) {
          var _this$element2;
          this.hasMouse = true;
          (_this$element2 = this.element) === null ||
            _this$element2 === void 0 ||
            _this$element2.classList.add("pswp--has_mouse");
        }
      }
      /**
       * Page resize event handler
       *
       * @private
       */
      _handlePageResize() {
        this.updateSize();
        if (/iPhone|iPad|iPod/i.test(window.navigator.userAgent)) {
          setTimeout(() => {
            this.updateSize();
          }, 500);
        }
      }
      /**
       * Page scroll offset is used
       * to get correct coordinates
       * relative to PhotoSwipe viewport.
       *
       * @private
       */
      _updatePageScrollOffset() {
        this.setScrollOffset(0, window.pageYOffset);
      }
      /**
       * @param {number} x
       * @param {number} y
       */
      setScrollOffset(x, y) {
        this.offset.x = x;
        this.offset.y = y;
        this.dispatch("updateScrollOffset");
      }
      /**
       * Create main HTML structure of PhotoSwipe,
       * and add it to DOM
       *
       * @private
       */
      _createMainStructure() {
        this.element = createElement5("pswp", "div");
        this.element.setAttribute("tabindex", "-1");
        this.element.setAttribute("role", "dialog");
        this.template = this.element;
        this.bg = createElement5("pswp__bg", "div", this.element);
        this.scrollWrap = createElement5(
          "pswp__scroll-wrap",
          "section",
          this.element,
        );
        this.container = createElement5(
          "pswp__container",
          "div",
          this.scrollWrap,
        );
        this.scrollWrap.setAttribute("aria-roledescription", "carousel");
        this.container.setAttribute("aria-live", "off");
        this.container.setAttribute("id", "pswp__items");
        this.mainScroll.appendHolders();
        this.ui = new UI(this);
        this.ui.init();
        (this.options.appendToEl || document.body).appendChild(this.element);
      }
      /**
       * Get position and dimensions of small thumbnail
       *   {x:,y:,w:}
       *
       * Height is optional (calculated based on the large image)
       *
       * @returns {Bounds | undefined}
       */
      getThumbBounds() {
        return getThumbBounds(
          this.currIndex,
          this.currSlide ? this.currSlide.data : this._initialItemData,
          this,
        );
      }
      /**
       * If the PhotoSwipe can have continuous loop
       * @returns Boolean
       */
      canLoop() {
        return this.options.loop && this.getNumItems() > 2;
      }
      /**
       * @private
       * @param {PhotoSwipeOptions} options
       * @returns {PreparedPhotoSwipeOptions}
       */
      _prepareOptions(options) {
        if (
          window.matchMedia("(prefers-reduced-motion), (update: slow)").matches
        ) {
          options.showHideAnimationType = "none";
          options.zoomAnimationDuration = 0;
        }
        return {
          ...defaultOptions,
          ...options,
        };
      }
    };
  },
});

// scripts/snapshot/refresh.ts
var refresh_exports = {};
__export(refresh_exports, {
  refreshExport: () => refreshExport,
});
module.exports = __toCommonJS(refresh_exports);
var import_node_fs2 = __toESM(require("node:fs"), 1);
var import_node_path2 = __toESM(require("node:path"), 1);
var import_node_perf_hooks = require("node:perf_hooks");

// src/core/snapshot-model.ts
init_static();
init_utils();

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/translations/translations.json
var translations_default = {
  about: {
    title: {
      EN: "About Us",
      SL: "O Nas",
      HR: "O Nama",
      DE: "\xDCber Uns",
      IT: "Chi Siamo",
    },
    subtitle: {
      EN: "Who We Are",
      SL: "Kdo Smo",
      HR: "Tko Smo",
      DE: "Wer Wir Sind",
      IT: "Chi Siamo",
    },
    establishedIn: {
      EN: "Established in",
      SL: "Ustanovljeno leta",
      HR: "Osnovano godine",
      DE: "Gegr\xFCndet im Jahr",
      IT: "Fondata nel",
    },
    satisfiedClients: {
      EN: "Satisfied Clients",
      SL: "Zadovoljnih Strank",
      HR: "Zadovoljnih Klijenata",
      DE: "Zufriedene Kunden",
      IT: "Clienti Soddisfatti",
    },
  },
  contact: {
    title: {
      EN: "Our Contact",
      SL: "Na\u0161 Kontakt",
      HR: "Na\u0161 Kontakt",
      DE: "Unser Kontakt",
      IT: "Il Nostro Contatto",
    },
    contactInfo: {
      phone: {
        title: {
          EN: "Phone",
          SL: "Telefon",
          HR: "Telefon",
          DE: "Telefon",
          IT: "Telefono",
        },
        callToAction: {
          EN: "Call Us",
          SL: "Pokli\u010Di Nas",
          HR: "Pozovi Nas",
          DE: "Rufen Sie Uns An",
          IT: "Chiamaci",
        },
      },
      email: {
        title: {
          EN: "Email",
          SL: "E-po\u0161ta",
          HR: "E-po\u0161ta",
          DE: "E-Mail",
          IT: "Email",
        },
        callToAction: {
          EN: "Email Us",
          SL: "Po\u0161lji Nam E-po\u0161to",
          HR: "Po\u0161alji Nam E-po\u0161tu",
          DE: "Schreiben Sie Uns Eine E-Mail",
          IT: "Inviaci Una Email",
        },
      },
      address: {
        title: {
          EN: "Address",
          SL: "Naslov",
          HR: "Adresa",
          DE: "Adresse",
          IT: "Indirizzo",
        },
        callToAction: {
          EN: "See Map",
          SL: "Poglej Zemljevid",
          HR: "Pogledaj Kartu",
          DE: "Karte Ansehen",
          IT: "Vedi Mappa",
        },
      },
    },
    workingHours: {
      title: {
        EN: "Working Hours",
        SL: "Delovni \u010Cas",
        HR: "Radno Vrijeme",
        DE: "Arbeitszeiten",
        IT: "Orari di Lavoro",
      },
      days: {
        MONDAY: {
          EN: "Monday",
          SL: "Ponedeljek",
          HR: "Ponedjeljak",
          DE: "Montag",
          IT: "Luned\xEC",
        },
        TUESDAY: {
          EN: "Tuesday",
          SL: "Torek",
          HR: "Utorak",
          DE: "Dienstag",
          IT: "Marted\xEC",
        },
        WEDNESDAY: {
          EN: "Wednesday",
          SL: "Sreda",
          HR: "Srijeda",
          DE: "Mittwoch",
          IT: "Mercoled\xEC",
        },
        THURSDAY: {
          EN: "Thursday",
          SL: "\u010Cetrtek",
          HR: "\u010Cetvrtak",
          DE: "Donnerstag",
          IT: "Gioved\xEC",
        },
        FRIDAY: {
          EN: "Friday",
          SL: "Petek",
          HR: "Petak",
          DE: "Freitag",
          IT: "Venerd\xEC",
        },
        SATURDAY: {
          EN: "Saturday",
          SL: "Sobota",
          HR: "Subota",
          DE: "Samstag",
          IT: "Sabato",
        },
        SUNDAY: {
          EN: "Sunday",
          SL: "Nedelja",
          HR: "Nedjelja",
          DE: "Sonntag",
          IT: "Domenica",
        },
      },
      closedLabel: {
        EN: "Closed",
        SL: "Zaprto",
        HR: "Zatvoreno",
        DE: "Geschlossen",
        IT: "Chiuso",
      },
      noteLabel: {
        EN: "Note",
        SL: "Opomba",
        HR: "Napomena",
        DE: "Hinweis",
        IT: "Nota",
      },
    },
  },
  services: {
    title: {
      EN: "Our Services",
      SL: "Na\u0161e Storitve",
      HR: "Na\u0161e Usluge",
      DE: "Unsere Dienstleistungen",
      IT: "I Nostri Servizi",
    },
    callToAction: {
      EN: "Discover More",
      SL: "Odkrij Ve\u010D",
      HR: "Otkrij Vi\u0161e",
      DE: "Entdecken Sie Mehr",
      IT: "Scopri di Pi\xF9",
    },
  },
  reviews: {
    title: {
      EN: "Customer Reviews",
      SL: "Mnenja Strank",
      HR: "Recenzije Kupaca",
      DE: "Kundenbewertungen",
      IT: "Recensioni dei Clienti",
    },
  },
  team: {
    title: {
      EN: "Meet the Team",
      SL: "Spoznajte Ekipo",
      HR: "Upoznajte Tim",
      DE: "Lernen Sie das Team kennen",
      IT: "Incontra il Team",
    },
  },
  experience: {
    title: {
      EN: "Our Experience",
      SL: "Na\u0161e Izku\u0161nje",
      HR: "Na\u0161e Iskustvo",
      DE: "Unsere Erfahrung",
      IT: "La Nostra Esperienza",
    },
  },
  gallery: {
    title: {
      EN: "Our Gallery",
      SL: "Na\u0161a Galerija",
      HR: "Na\u0161a Galerija",
      DE: "Unsere Galerie",
      IT: "La Nostra Galleria",
    },
    callToAction: {
      EN: "See More",
      SL: "Oglej Si Ve\u010D",
      HR: "Pogledaj Vi\u0161e",
      DE: "Mehr Sehen",
      IT: "Vedi di Pi\xF9",
    },
  },
  blog: {
    title: {
      EN: "Our Blog",
      SL: "Na\u0161 Blog",
      HR: "Na\u0161 Blog",
      DE: "Unser Blog",
      IT: "Il Nostro Blog",
    },
    callToAction: {
      EN: "Read More",
      SL: "Preberi Ve\u010D",
      HR: "Pro\u010Ditaj Vi\u0161e",
      DE: "Mehr Lesen",
      IT: "Leggi di Pi\xF9",
    },
    posts: {
      latestPosts: {
        EN: "Latest Posts",
        SL: "Najnovej\u0161i Prispevki",
        HR: "Najnoviji Postovi",
        DE: "Neueste Beitr\xE4ge",
        IT: "Ultimi Articoli",
      },
      backToBlogs: {
        EN: "Back to Blogs",
        SL: "Nazaj na Bloge",
        HR: "Natrag na Blogove",
        DE: "Zur\xFCck zu den Blogs",
        IT: "Torna ai Blog",
      },
    },
  },
  portfolio: {
    title: {
      EN: "Our Portfolio",
      SL: "Na\u0161 Portfolio",
      HR: "Na\u0161 Portfolio",
      DE: "Unser Portfolio",
      IT: "Il Nostro Portfolio",
    },
    callToAction: {
      EN: "See More",
      SL: "Oglej Si Ve\u010D",
      HR: "Pogledaj Vi\u0161e",
      DE: "Mehr Sehen",
      IT: "Vedi di Pi\xF9",
    },
    project: {
      details: {
        label: {
          EN: "Project Details",
          SL: "Podrobnosti Projekta",
          HR: "Detalji Projekta",
          DE: "Projektdetails",
          IT: "Dettagli del Progetto",
        },
        client: {
          EN: "Client",
          SL: "Stranka",
          HR: "Klijent",
          DE: "Kunde",
          IT: "Cliente",
        },
        date: {
          EN: "Date",
          SL: "Datum",
          HR: "Datum",
          DE: "Datum",
          IT: "Data",
        },
        category: {
          EN: "Category",
          SL: "Kategorija",
          HR: "Kategorija",
          DE: "Kategorie",
          IT: "Categoria",
        },
      },
      descriptionLabel: {
        EN: "Description",
        SL: "Opis",
        HR: "Opis",
        DE: "Beschreibung",
        IT: "Descrizione",
      },
      backToPortfolio: {
        EN: "Back to Portfolio",
        SL: "Nazaj na Portfolio",
        HR: "Natrag na Portfolio",
        DE: "Zur\xFCck zum Portfolio",
        IT: "Torna al Portfolio",
      },
      relatedProjects: {
        EN: "Related Projects",
        SL: "Sorodni Projekti",
        HR: "Povezani Projekti",
        DE: "Verwandte Projekte",
        IT: "Progetti Correlati",
      },
    },
  },
  pricing: {
    onAgreement: {
      EN: "On Agreement",
      SL: "Po Dogovoru",
      HR: "Po Dogovoru",
      DE: "Nach Vereinbarung",
      IT: "Su Accordo",
    },
    comingSoon: {
      EN: "Coming soon",
      SL: "Kmalu",
      HR: "Uskoro",
      DE: "Demn\xE4chst",
      IT: "In arrivo",
    },
    unavailable: {
      EN: "Unavailable",
      SL: "Ni na voljo",
      HR: "Nedostupno",
      DE: "Nicht verf\xFCgbar",
      IT: "Non disponibile",
    },
    store: {
      search: {
        EN: "Search products",
        SL: "I\u0161\u010Di izdelke",
        HR: "Pretra\u017Ei proizvode",
        DE: "Produkte suchen",
        IT: "Cerca prodotti",
      },
      category: {
        EN: "Category",
        SL: "Kategorija",
        HR: "Kategorija",
        DE: "Kategorie",
        IT: "Categoria",
      },
      sortByPrice: {
        EN: "Sort by price",
        SL: "Razvrsti po ceni",
        HR: "Poredaj po cijeni",
        DE: "Nach Preis sortieren",
        IT: "Ordina per prezzo",
      },
      priceAscending: {
        EN: "Lowest price first",
        SL: "Najni\u017Eja cena najprej",
        HR: "Najni\u017Ea cijena prvo",
        DE: "Niedrigster Preis zuerst",
        IT: "Prezzo pi\xF9 basso prima",
      },
      priceDescending: {
        EN: "Highest price first",
        SL: "Najvi\u0161ja cena najprej",
        HR: "Najvi\u0161a cijena prvo",
        DE: "H\xF6chster Preis zuerst",
        IT: "Prezzo pi\xF9 alto prima",
      },
      noResults: {
        EN: "No products found.",
        SL: "Ni najdenih izdelkov.",
        HR: "Nema prona\u0111enih proizvoda.",
        DE: "Keine Produkte gefunden.",
        IT: "Nessun prodotto trovato.",
      },
    },
    priceListWithImages: {
      title: {
        EN: "Our Products",
        SL: "Na\u0161i Izdelki",
        HR: "Na\u0161i Proizvodi",
        DE: "Unsere Produkte",
        IT: "I Nostri Prodotti",
      },
      items: {
        description: {
          EN: "Description",
          SL: "Opis",
          HR: "Opis",
          DE: "Beschreibung",
          IT: "Descrizione",
        },
        price: {
          EN: "Price",
          SL: "Cena",
          HR: "Cijena",
          DE: "Preis",
          IT: "Prezzo",
        },
        goBackButton: {
          EN: "Go Back",
          SL: "Nazaj",
          HR: "Natrag",
          DE: "Zur\xFCck",
          IT: "Torna Indietro",
        },
      },
      relatedItemsTitle: {
        EN: "You May Also Like",
        SL: "Morda Vam Bo Tudi V\u0161e\u010D",
        HR: "Mo\u017Eda \u0106e Vam Se Svidjeti",
        DE: "Das K\xF6nnte Ihnen Auch Gefallen",
        IT: "Potrebbe Interessarti Anche",
      },
    },
    priceListNoImages: {
      title: {
        EN: "Pricing",
        SL: "Cenik",
        HR: "Cjenik",
        DE: "Preise",
        IT: "Prezzi",
      },
    },
    packagesNoImages: {
      title: {
        EN: "Our Packages",
        SL: "Na\u0161i Paketi",
        HR: "Na\u0161i Paketi",
        DE: "Unsere Pakete",
        IT: "I Nostri Pacchetti",
      },
      items: {
        recommended: {
          EN: "Recommended",
          SL: "Priporo\u010Deno",
          HR: "Preporu\u010Deno",
          DE: "Empfohlen",
          IT: "Raccomandato",
        },
        callToAction: {
          EN: "Select",
          SL: "Izberi",
          HR: "Odaberi",
          DE: "Ausw\xE4hlen",
          IT: "Seleziona",
        },
      },
      acquirementSubject: {
        EN: "Acquirement for",
        SL: "Povpra\u0161evanje za",
        HR: "Upit za",
        DE: "Anfrage f\xFCr",
        IT: "Richiesta per",
      },
    },
    PriceUnit: {
      ITEM: {
        EN: "item",
        SL: "kos",
        HR: "komad",
        DE: "St\xFCck",
        IT: "articolo",
      },
      PERSON: {
        EN: "person",
        SL: "oseba",
        HR: "osoba",
        DE: "Person",
        IT: "persona",
      },
      NIGHT: {
        EN: "night",
        SL: "no\u010D",
        HR: "no\u0107",
        DE: "Nacht",
        IT: "notte",
      },
      HOUR: {
        EN: "hour",
        SL: "ura",
        HR: "sat",
        DE: "Stunde",
        IT: "ora",
      },
      DAY: {
        EN: "day",
        SL: "dan",
        HR: "dan",
        DE: "Tag",
        IT: "giorno",
      },
      WEEK: {
        EN: "week",
        SL: "teden",
        HR: "tjedan",
        DE: "Woche",
        IT: "settimana",
      },
      MONTH: {
        EN: "month",
        SL: "mesec",
        HR: "mjesec",
        DE: "Monat",
        IT: "mese",
      },
      YEAR: {
        EN: "year",
        SL: "leto",
        HR: "godina",
        DE: "Jahr",
        IT: "anno",
      },
    },
  },
  careers: {
    title: {
      EN: "Careers",
      SL: "Zaposlitev",
      HR: "Karijere",
      DE: "Karriere",
      IT: "Carriere",
    },
    requirementsTitle: {
      EN: "Requirements",
      SL: "Zahteve",
      HR: "Zahtjevi",
      DE: "Anforderungen",
      IT: "Requisiti",
    },
    applicationSubject: {
      EN: "Job Application",
      SL: "Prijava na Delovno Mesto",
      HR: "Prijava za Posao",
      DE: "Bewerbung um eine Stelle",
      IT: "Candidatura di Lavoro",
    },
    backToCareers: {
      EN: "Back to Careers",
      SL: "Nazaj na Zaposlitve",
      HR: "Natrag na Karijere",
      DE: "Zur\xFCck zu den Stellenangeboten",
      IT: "Torna alle Posizioni Aperte",
    },
  },
  catalogues: {
    title: {
      EN: "Our Catalogues",
      SL: "Na\u0161i Katalogi",
      HR: "Na\u0161i Katalozi",
      DE: "Unsere Kataloge",
      IT: "I Nostri Cataloghi",
    },
  },
  videos: {
    title: {
      EN: "Our Videos",
      SL: "Na\u0161i Videoposnetki",
      HR: "Na\u0161i Videozapisi",
      DE: "Unsere Videos",
      IT: "I Nostri Video",
    },
  },
  whyUs: {
    title: {
      EN: "Why Us",
      SL: "Zakaj Nas",
      HR: "Za\u0161to Nas",
      DE: "Warum Uns",
      IT: "Perch\xE9 Noi",
    },
    clientCount: {
      EN: "Satisfied Customers",
      SL: "Zadovoljnih Strank",
      HR: "Zadovoljnih Kupaca",
      DE: "Zufriedene Kunden",
      IT: "Clienti Soddisfatti",
    },
    projectCount: {
      EN: "Completed Projects",
      SL: "Zaklju\u010Denih Projektov",
      HR: "Zavr\u0161enih Projekata",
      DE: "Abgeschlossene Projekte",
      IT: "Progetti Completati",
    },
  },
  faq: {
    title: {
      EN: "Frequently Asked Questions",
      SL: "Pogosto Zastavljena Vpra\u0161anja",
      HR: "\u010Cesto Postavljana Pitanja",
      DE: "H\xE4ufig Gestellte Fragen",
      IT: "Domande Frequenti",
    },
  },
  schedule: {
    title: {
      EN: "Schedule",
      SL: "Urnik",
      HR: "Raspored",
      DE: "Zeitplan",
      IT: "Orari",
    },
  },
  events: {
    title: {
      EN: "Events",
      SL: "Dogodki",
      HR: "Doga\u0111aji",
      DE: "Veranstaltungen",
      IT: "Eventi",
    },
    cancelled: {
      EN: "Cancelled",
      SL: "Odpovedano",
      HR: "Otkazano",
      DE: "Abgesagt",
      IT: "Annullato",
    },
    today: {
      EN: "Today",
      SL: "Danes",
      HR: "Danas",
      DE: "Heute",
      IT: "Oggi",
    },
    tomorrow: {
      EN: "Tomorrow",
      SL: "Jutri",
      HR: "Sutra",
      DE: "Morgen",
      IT: "Domani",
    },
    search: {
      EN: "Search events",
      SL: "I\u0161\u010Di dogodke",
      HR: "Pretra\u017Ei doga\u0111aje",
      DE: "Veranstaltungen suchen",
      IT: "Cerca eventi",
    },
    sortByDate: {
      EN: "Sort by date",
      SL: "Razvrsti po datumu",
      HR: "Poredaj po datumu",
      DE: "Nach Datum sortieren",
      IT: "Ordina per data",
    },
    dateAscending: {
      EN: "Earliest first",
      SL: "Najzgodnej\u0161i najprej",
      HR: "Najraniji prvo",
      DE: "Fr\xFCheste zuerst",
      IT: "Prima le date meno recenti",
    },
    dateDescending: {
      EN: "Latest first",
      SL: "Najpoznej\u0161i najprej",
      HR: "Najkasniji prvo",
      DE: "Sp\xE4teste zuerst",
      IT: "Prima le date pi\xF9 recenti",
    },
    noResults: {
      EN: "No events found.",
      SL: "Ni najdenih dogodkov.",
      HR: "Nema prona\u0111enih doga\u0111aja.",
      DE: "Keine Veranstaltungen gefunden.",
      IT: "Nessun evento trovato.",
    },
    date: {
      EN: "Date",
      SL: "Datum",
      HR: "Datum",
      DE: "Datum",
      IT: "Data",
    },
    location: {
      EN: "Location",
      SL: "Lokacija",
      HR: "Lokacija",
      DE: "Ort",
      IT: "Luogo",
    },
    category: {
      EN: "Category",
      SL: "Kategorija",
      HR: "Kategorija",
      DE: "Kategorie",
      IT: "Categoria",
    },
    backToEvents: {
      EN: "Back to Events",
      SL: "Nazaj na dogodke",
      HR: "Natrag na doga\u0111aje",
      DE: "Zur\xFCck zu den Veranstaltungen",
      IT: "Torna agli eventi",
    },
    applicationSubject: {
      EN: "Event Application",
      SL: "Prijava na Dogodek",
      HR: "Prijava za Doga\u0111aj",
      DE: "Anmeldung zur Veranstaltung",
      IT: "Iscrizione all'Evento",
    },
  },
  navigation: {
    home: {
      EN: "Home",
      SL: "Domov",
      HR: "Po\u010Detna",
      DE: "Startseite",
      IT: "Home",
    },
    about: {
      EN: "About",
      SL: "O nas",
      HR: "O nama",
      DE: "\xDCber uns",
      IT: "Chi siamo",
    },
    more: {
      EN: "More",
      SL: "Ve\u010D",
      HR: "Vi\u0161e",
      DE: "Mehr",
      IT: "Altro",
    },
    contact: {
      EN: "Contact",
      SL: "Kontakt",
      HR: "Kontakt",
      DE: "Kontakt",
      IT: "Contatto",
    },
    services: {
      EN: "Services",
      SL: "Storitve",
      HR: "Usluge",
      DE: "Dienstleistungen",
      IT: "Servizi",
    },
    portfolio: {
      EN: "Portfolio",
      SL: "Projekti",
      HR: "Portfolio",
      DE: "Portfolio",
      IT: "Portfolio",
    },
    gallery: {
      EN: "Gallery",
      SL: "Galerija",
      HR: "Galerija",
      DE: "Galerie",
      IT: "Galleria",
    },
    blog: {
      EN: "Blog",
      SL: "Blog",
      HR: "Blog",
      DE: "Blog",
      IT: "Blog",
    },
    videos: {
      EN: "Videos",
      SL: "Videi",
      HR: "Videi",
      DE: "Videos",
      IT: "Video",
    },
    catalogues: {
      EN: "Catalogues",
      SL: "Katalogi",
      HR: "Katalozi",
      DE: "Kataloge",
      IT: "Cataloghi",
    },
    pricing: {
      EN: "Pricing",
      SL: "Cenik",
      HR: "Cjenik",
      DE: "Preise",
      IT: "Prezzi",
    },
    careers: {
      EN: "Careers",
      SL: "Zaposlitev",
      HR: "Karijere",
      DE: "Karriere",
      IT: "Carriere",
    },
    faq: {
      EN: "FAQ",
      SL: "FAQ",
      HR: "FAQ",
      DE: "FAQ",
      IT: "FAQ",
    },
    schedule: {
      EN: "Schedule",
      SL: "Urnik",
      HR: "Raspored",
      DE: "Zeitplan",
      IT: "Orari",
    },
    events: {
      EN: "Events",
      SL: "Dogodki",
      HR: "Doga\u0111aji",
      DE: "Veranstaltungen",
      IT: "Eventi",
    },
  },
  subscriptions: {
    title: {
      EN: "Subscribe to our Newsletter",
      SL: "Naro\u010Dite se na na\u0161 Newsletter",
      HR: "Pretplatite se na na\u0161 Newsletter",
      DE: "Abonnieren Sie unseren Newsletter",
      IT: "Iscriviti alla nostra Newsletter",
    },
    callToAction: {
      EN: "Subscribe",
      SL: "Naro\u010Di se",
      HR: "Pretplati se",
      DE: "Abonnieren",
      IT: "Iscriviti",
    },
    successMessage: {
      EN: "You have successfully subscribed to our newsletter.",
      SL: "Uspe\u0161no ste se prijavili na na\u0161e novice.",
      HR: "Uspje\u0161no ste se pretplatili na na\u0161 newsletter.",
      DE: "Sie haben unseren Newsletter erfolgreich abonniert.",
      IT: "Ti sei iscritto con successo alla nostra newsletter.",
    },
    errorMessage: {
      EN: "The subscription could not be completed. Please try again.",
      SL: "Prijave ni bilo mogo\u010De dokon\u010Dati. Poskusite znova.",
      HR: "Pretplatu nije bilo mogu\u0107e dovr\u0161iti. Poku\u0161ajte ponovno.",
      DE: "Die Anmeldung konnte nicht abgeschlossen werden. Bitte versuchen Sie es erneut.",
      IT: "Non \xE8 stato possibile completare l'iscrizione. Riprova.",
    },
  },
  legal: {
    title: {
      EN: "Terms and Privacy",
      SL: "Pogoji in Zasebnost",
      HR: "Uvjeti i Privatnost",
      DE: "AGB und Datenschutz",
      IT: "Termini e Privacy",
    },
    termsOfService: {
      EN: "Terms of Service",
      SL: "Pogoji uporabe",
      HR: "Uvjeti kori\u0161tenja",
      DE: "Nutzungsbedingungen",
      IT: "Termini di servizio",
    },
    privacyPolicy: {
      EN: "Privacy Policy",
      SL: "Politika zasebnosti",
      HR: "Pravila privatnosti",
      DE: "Datenschutzerkl\xE4rung",
      IT: "Informativa sulla privacy",
    },
    openDocument: {
      EN: "Open document",
      SL: "Odpri dokument",
      HR: "Otvori dokument",
      DE: "Dokument \xF6ffnen",
      IT: "Apri documento",
    },
  },
  forms: {
    email: {
      label: {
        EN: "Email",
        SL: "E-po\u0161ta",
        HR: "E-po\u0161ta",
        DE: "E-Mail",
        IT: "Email",
      },
      placeholder: {
        EN: "Enter your email",
        SL: "Vnesite va\u0161 email",
        HR: "Unesite va\u0161 email",
        DE: "Geben Sie Ihre E-Mail ein",
        IT: "Inserisci la tua email",
      },
      errorMessage: {
        EN: "Please enter a valid email address.",
        SL: "Prosimo, vnesite veljaven e-po\u0161tni naslov.",
        HR: "Molimo unesite va\u017Ee\u0107u e-mail adresu.",
        DE: "Bitte geben Sie eine g\xFCltige E-Mail-Adresse ein.",
        IT: "Per favore inserisci un indirizzo email valido.",
      },
    },
    name: {
      label: {
        EN: "Name",
        SL: "Ime",
        HR: "Ime",
        DE: "Name",
        IT: "Nome",
      },
      placeholder: {
        EN: "Enter your name",
        SL: "Vnesite va\u0161e ime",
        HR: "Unesite va\u0161e ime",
        DE: "Geben Sie Ihren Namen ein",
        IT: "Inserisci il tuo nome",
      },
      errorMessage: {
        EN: "Please enter your name.",
        SL: "Prosimo, vnesite va\u0161e ime.",
        HR: "Molimo unesite va\u0161e ime.",
        DE: "Bitte geben Sie Ihren Namen ein.",
        IT: "Per favore inserisci il tuo nome.",
      },
    },
    subject: {
      label: {
        EN: "Subject",
        SL: "Zadeva",
        HR: "Predmet",
        DE: "Betreff",
        IT: "Oggetto",
      },
      placeholder: {
        EN: "Enter the subject",
        SL: "Vnesite zadevo",
        HR: "Unesite predmet",
        DE: "Geben Sie den Betreff ein",
        IT: "Inserisci l'oggetto",
      },
      errorMessage: {
        EN: "Please enter the subject.",
        SL: "Prosimo, vnesite zadevo.",
        HR: "Molimo unesite predmet.",
        DE: "Bitte geben Sie den Betreff ein.",
        IT: "Per favore inserisci l'oggetto.",
      },
    },
    message: {
      label: {
        EN: "Message",
        SL: "Sporo\u010Dilo",
        HR: "Poruka",
        DE: "Nachricht",
        IT: "Messaggio",
      },
      placeholder: {
        EN: "Enter your message",
        SL: "Vnesite va\u0161e sporo\u010Dilo",
        HR: "Unesite va\u0161u poruku",
        DE: "Geben Sie Ihre Nachricht ein",
        IT: "Inserisci il tuo messaggio",
      },
      errorMessage: {
        EN: "Please enter your message.",
        SL: "Prosimo, vnesite va\u0161e sporo\u010Dilo.",
        HR: "Molimo unesite va\u0161u poruku.",
        DE: "Bitte geben Sie Ihre Nachricht ein.",
        IT: "Per favore inserisci il tuo messaggio.",
      },
    },
    disclaimerPrefix: {
      EN: "By sending the form you agree to the",
      SL: "S po\u0161iljanjem obrazca se strinjate s",
      HR: "Slanjem obrasca sla\u017Eete se s",
      DE: "Mit dem Absenden des Formulars stimmen Sie den",
      IT: "Inviando il modulo accetti i",
    },
    termsAndConditions: {
      EN: "terms & conditions",
      SL: "pogoji in dolo\u010Dili",
      HR: "uvjeti i odredbe",
      DE: "Gesch\xE4ftsbedingungen",
      IT: "termini e condizioni",
    },
    disclaimerJoiner: {
      EN: "and",
      SL: "ter",
      HR: "i",
      DE: "und",
      IT: "e",
    },
    privacyPolicy: {
      EN: "privacy policy",
      SL: "politiko zasebnosti",
      HR: "pravila o privatnosti",
      DE: "Datenschutzbestimmungen",
      IT: "politica sulla privacy",
    },
    success: {
      title: {
        EN: "Message sent",
        SL: "Sporo\u010Dilo poslano",
        HR: "Poruka poslana",
        DE: "Nachricht gesendet",
        IT: "Messaggio inviato",
      },
      message: {
        EN: "Thank you for contacting us. Your message has been sent successfully.",
        SL: "Hvala, ker ste nas kontaktirali. Va\u0161e sporo\u010Dilo je bilo uspe\u0161no poslano.",
        HR: "Hvala \u0161to ste nas kontaktirali. Va\u0161a poruka je uspje\u0161no poslana.",
        DE: "Vielen Dank f\xFCr Ihre Nachricht. Ihre Nachricht wurde erfolgreich gesendet.",
        IT: "Grazie per averci contattato. Il tuo messaggio \xE8 stato inviato con successo.",
      },
      close: {
        EN: "Close",
        SL: "Zapri",
        HR: "Zatvori",
        DE: "Schlie\xDFen",
        IT: "Chiudi",
      },
    },
    sendError: {
      EN: "Your message could not be sent. Please try again later.",
      SL: "Va\u0161ega sporo\u010Dila ni bilo mogo\u010De poslati. Poskusite znova pozneje.",
      HR: "Va\u0161u poruku nije bilo mogu\u0107e poslati. Poku\u0161ajte ponovno kasnije.",
      DE: "Ihre Nachricht konnte nicht gesendet werden. Bitte versuchen Sie es sp\xE4ter erneut.",
      IT: "Non \xE8 stato possibile inviare il tuo messaggio. Riprova pi\xF9 tardi.",
    },
    contactMessage: {
      EN: "Our contact us directly at:",
      SL: "Lahko pa nas kontaktirate na:",
      HR: "Mo\u017Eete nas kontaktirati izravno na:",
      DE: "Sie k\xF6nnen uns auch direkt kontaktieren unter:",
      IT: "Puoi contattarci direttamente a:",
    },
  },
  callToAction: {
    title: {
      EN: "See something you like?",
      SL: "Vidite kaj, kar vam je v\u0161e\u010D?",
      HR: "Vidite li ne\u0161to \u0161to vam se svi\u0111a?",
      DE: "Sehen Sie etwas, das Ihnen gef\xE4llt?",
      IT: "Vedi qualcosa che ti piace?",
    },
    contactButton: {
      EN: "Get In Touch",
      SL: "Kontaktiraj Nas",
      HR: "Stupite u Kontakt",
      DE: "Kontaktieren Sie Uns",
      IT: "Mettiti in Contatto",
    },
  },
  buttons: {
    discoverNow: {
      EN: "Discover Now",
      SL: "Odkrij zdaj",
      HR: "Otkrij Sada",
      DE: "Jetzt Entdecken",
      IT: "Scopri Ora",
    },
    learnMore: {
      EN: "Learn More",
      SL: "Izvedi Ve\u010D",
      HR: "Saznajte Vi\u0161e",
      DE: "Erfahren Sie Mehr",
      IT: "Scopri di Pi\xF9",
    },
    sendMessage: {
      EN: "Send Message",
      SL: "Po\u0161lji Sporo\u010Dilo",
      HR: "Po\u0161alji Poruku",
      DE: "Nachricht Senden",
      IT: "Invia Messaggio",
    },
    scrollDown: {
      EN: "Scroll Down",
      SL: "Pomakni Dol",
      HR: "Skrolaj Dolje",
      DE: "Runterscrollen",
      IT: "Scorri Gi\xF9",
    },
    backToTop: {
      EN: "Back to Top",
      SL: "Nazaj na Vrh",
      HR: "Natrag na Vrh",
      DE: "Zur\xFCck nach Oben",
      IT: "Torna Su",
    },
    all: {
      EN: "All",
      SL: "Vse",
      HR: "Sve",
      DE: "Alle",
      IT: "Tutti",
    },
    applyNow: {
      EN: "Apply Now",
      SL: "Prijavi Se Zdaj",
      HR: "Prijavi Se Sada",
      DE: "Jetzt Bewerben",
      IT: "Candidati Ora",
    },
    shareOn: {
      EN: "Share on",
      SL: "Deli na",
      HR: "Podijeli na",
      DE: "Teilen auf",
      IT: "Condividi su",
    },
    copyLink: {
      EN: "Copy Link",
      SL: "Kopiraj Povezavo",
      HR: "Kopiraj Link",
      DE: "Link Kopieren",
      IT: "Copia Link",
    },
  },
  footer: {
    production: {
      EN: "ONAS production",
      SL: "ONAS produkcija",
      HR: "ONAS produkcija",
      DE: "ONAS Produktion",
      IT: "Produzione ONAS",
    },
    slogan: {
      EN: "Making websites easy.",
      SL: "Spletne strani narejene enostavno.",
      HR: "Izrada web stranica jednostavno.",
      DE: "Websites einfach gemacht.",
      IT: "Creare siti web \xE8 facile.",
    },
  },
  notFound: {
    title: {
      EN: "404 - Page Not Found",
      SL: "404 - Stran Ni Najdena",
      HR: "404 - Stranica Nije Prona\u0111ena",
      DE: "404 - Seite Nicht Gefunden",
      IT: "404 - Pagina Non Trovata",
    },
    text: {
      EN: "Sorry, the page you are looking for does not exist.",
      SL: "Oprostite, stran, ki jo i\u0161\u010Dete, ne obstaja.",
      HR: "Oprostite, stran koju tra\u017Eite ne postoji.",
      DE: "Entschuldigung, die von Ihnen gesuchte Seite existiert nicht.",
      IT: "Spiacenti, la pagina che stai cercando non esiste.",
    },
    homeButton: {
      EN: "Home",
      SL: "Domov",
      HR: "Po\u010Detna",
      DE: "Startseite",
      IT: "Home",
    },
  },
  demoPopup: {
    title: {
      EN: "Demo Version",
      SL: "Demo Razli\u010Dica",
      HR: "Demo Verzija",
      DE: "Demo-Version",
      IT: "Versione Demo",
    },
    text: {
      EN: "This is a demo version of the website. Some features may be limited.",
      SL: "To je demo razli\u010Dica spletne strani. Nekatere funkcionalnosti so lahko omejene.",
      HR: "Ovo je demo verzija web stranice. Neke funkcionalnosti mogu biti ograni\u010Dene.",
      DE: "Dies ist eine Demo-Version der Website. Einige Funktionen sind m\xF6glicherweise eingeschr\xE4nkt.",
      IT: "Questa \xE8 una versione demo del sito web. Alcune funzionalit\xE0 potrebbero essere limitate.",
    },
    closeButton: {
      EN: "Close",
      SL: "Zapri",
      HR: "Zatvori",
      DE: "Schlie\xDFen",
      IT: "Chiudi",
    },
    purchaseButton: {
      EN: "Buy",
      SL: "Kupi",
      HR: "Kupi",
      DE: "Kaufen",
      IT: "Acquista",
    },
  },
};

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/translations/translations.js
init_localization();
var getAboutTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.about.title, lang),
    subtitle: getLocalized(translations_default.about.subtitle, lang),
    establishedIn: getLocalized(translations_default.about.establishedIn, lang),
    satisfiedClients: getLocalized(
      translations_default.about.satisfiedClients,
      lang,
    ),
  };
};
var getGalleryTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.gallery.title, lang),
    callToAction: getLocalized(translations_default.gallery.callToAction, lang),
  };
};
var getContactTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.contact.title, lang),
    contactInfo: {
      phone: {
        title: getLocalized(
          translations_default.contact.contactInfo.phone.title,
          lang,
        ),
        callToAction: getLocalized(
          translations_default.contact.contactInfo.phone.callToAction,
          lang,
        ),
      },
      email: {
        title: getLocalized(
          translations_default.contact.contactInfo.email.title,
          lang,
        ),
        callToAction: getLocalized(
          translations_default.contact.contactInfo.email.callToAction,
          lang,
        ),
      },
      address: {
        title: getLocalized(
          translations_default.contact.contactInfo.address.title,
          lang,
        ),
        callToAction: getLocalized(
          translations_default.contact.contactInfo.address.callToAction,
          lang,
        ),
      },
    },
    workingHours: {
      title: getLocalized(
        translations_default.contact.workingHours.title,
        lang,
      ),
      days: Object.fromEntries(
        Object.entries(translations_default.contact.workingHours.days).map(
          ([day, value]) => [day, getLocalized(value, lang)],
        ),
      ),
      closedLabel: getLocalized(
        translations_default.contact.workingHours.closedLabel,
        lang,
      ),
      noteLabel: getLocalized(
        translations_default.contact.workingHours.noteLabel,
        lang,
      ),
    },
  };
};
var getCareersTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.careers.title, lang),
    requirementsTitle: getLocalized(
      translations_default.careers.requirementsTitle,
      lang,
    ),
    applicationSubject: getLocalized(
      translations_default.careers.applicationSubject,
      lang,
    ),
    backToCareers: getLocalized(
      translations_default.careers.backToCareers,
      lang,
    ),
  };
};
var getCataloguesTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.catalogues.title, lang),
  };
};
var getServicesTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.services.title, lang),
    callToAction: getLocalized(
      translations_default.services.callToAction,
      lang,
    ),
  };
};
var getBlogTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.blog.title, lang),
    callToAction: getLocalized(translations_default.blog.callToAction, lang),
    posts: {
      latestPosts: getLocalized(
        translations_default.blog.posts.latestPosts,
        lang,
      ),
      backToBlogs: getLocalized(
        translations_default.blog.posts.backToBlogs,
        lang,
      ),
    },
  };
};
var getPortfolioTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.portfolio.title, lang),
    callToAction: getLocalized(
      translations_default.portfolio.callToAction,
      lang,
    ),
    project: {
      details: {
        label: getLocalized(
          translations_default.portfolio.project.details.label,
          lang,
        ),
        date: getLocalized(
          translations_default.portfolio.project.details.date,
          lang,
        ),
        client: getLocalized(
          translations_default.portfolio.project.details.client,
          lang,
        ),
        category: getLocalized(
          translations_default.portfolio.project.details.category,
          lang,
        ),
      },
      descriptionLabel: getLocalized(
        translations_default.portfolio.project.descriptionLabel,
        lang,
      ),
      backToPortfolio: getLocalized(
        translations_default.portfolio.project.backToPortfolio,
        lang,
      ),
      relatedProjects: getLocalized(
        translations_default.portfolio.project.relatedProjects,
        lang,
      ),
    },
  };
};
var getPriceTranslation = (lang) => {
  return {
    onAgreement: getLocalized(translations_default.pricing.onAgreement, lang),
    comingSoon: getLocalized(translations_default.pricing.comingSoon, lang),
    unavailable: getLocalized(translations_default.pricing.unavailable, lang),
  };
};
var getPricingStoreTranslation = (lang) => {
  return {
    search: getLocalized(translations_default.pricing.store.search, lang),
    category: getLocalized(translations_default.pricing.store.category, lang),
    sortByPrice: getLocalized(
      translations_default.pricing.store.sortByPrice,
      lang,
    ),
    priceAscending: getLocalized(
      translations_default.pricing.store.priceAscending,
      lang,
    ),
    priceDescending: getLocalized(
      translations_default.pricing.store.priceDescending,
      lang,
    ),
    noResults: getLocalized(translations_default.pricing.store.noResults, lang),
  };
};
var getPricingTranslation_priceListWithImages = (lang) => {
  return {
    title: getLocalized(
      translations_default.pricing.priceListWithImages.title,
      lang,
    ),
    items: {
      description: getLocalized(
        translations_default.pricing.priceListWithImages.items.description,
        lang,
      ),
      price: getLocalized(
        translations_default.pricing.priceListWithImages.items.price,
        lang,
      ),
      goBackButton: getLocalized(
        translations_default.pricing.priceListWithImages.items.goBackButton,
        lang,
      ),
    },
    relatedItemsTitle: getLocalized(
      translations_default.pricing.priceListWithImages.relatedItemsTitle,
      lang,
    ),
  };
};
var getPricingTranslation_priceListNoImages = (lang) => {
  return {
    title: getLocalized(
      translations_default.pricing.priceListNoImages.title,
      lang,
    ),
  };
};
var getPricingTranslation_packagesNoImages = (lang) => {
  return {
    title: getLocalized(
      translations_default.pricing.packagesNoImages.title,
      lang,
    ),
    items: {
      recommended: getLocalized(
        translations_default.pricing.packagesNoImages.items.recommended,
        lang,
      ),
      callToAction: getLocalized(
        translations_default.pricing.packagesNoImages.items.callToAction,
        lang,
      ),
    },
    acquirementSubject: getLocalized(
      translations_default.pricing.packagesNoImages.acquirementSubject,
      lang,
    ),
  };
};
var getPricingUnitTranslation = (unit, lang) => {
  return unit
    ? getLocalized(translations_default.pricing.PriceUnit[unit], lang)
    : void 0;
};
var getVideosTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.videos.title, lang),
  };
};
var getReviewTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.reviews.title, lang),
  };
};
var getWhyUsTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.whyUs.title, lang),
    clientCount: getLocalized(translations_default.whyUs.clientCount, lang),
    projectCount: getLocalized(translations_default.whyUs.projectCount, lang),
  };
};
var getNavigationTranslation = (lang) => {
  return {
    home: getLocalized(translations_default.navigation.home, lang),
    about: getLocalized(translations_default.navigation.about, lang),
    more: getLocalized(translations_default.navigation.more, lang),
    contact: getLocalized(translations_default.navigation.contact, lang),
    services: getLocalized(translations_default.navigation.services, lang),
    portfolio: getLocalized(translations_default.navigation.portfolio, lang),
    gallery: getLocalized(translations_default.navigation.gallery, lang),
    blog: getLocalized(translations_default.navigation.blog, lang),
    videos: getLocalized(translations_default.navigation.videos, lang),
    catalogues: getLocalized(translations_default.navigation.catalogues, lang),
    pricing: getLocalized(translations_default.navigation.pricing, lang),
    careers: getLocalized(translations_default.navigation.careers, lang),
    faq: getLocalized(translations_default.navigation.faq, lang),
    schedule: getLocalized(translations_default.navigation.schedule, lang),
    events: getLocalized(translations_default.navigation.events, lang),
  };
};
var getTeamTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.team.title, lang),
  };
};
var getSubscriptionsTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.subscriptions.title, lang),
    callToAction: getLocalized(
      translations_default.subscriptions.callToAction,
      lang,
    ),
    successMessage: getLocalized(
      translations_default.subscriptions.successMessage,
      lang,
    ),
    errorMessage: getLocalized(
      translations_default.subscriptions.errorMessage,
      lang,
    ),
  };
};
var getFaqTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.faq.title, lang),
  };
};
var getScheduleTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.schedule.title, lang),
  };
};
var getEventsTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.events.title, lang),
    cancelled: getLocalized(translations_default.events.cancelled, lang),
    today: getLocalized(translations_default.events.today, lang),
    tomorrow: getLocalized(translations_default.events.tomorrow, lang),
    search: getLocalized(translations_default.events.search, lang),
    sortByDate: getLocalized(translations_default.events.sortByDate, lang),
    dateAscending: getLocalized(
      translations_default.events.dateAscending,
      lang,
    ),
    dateDescending: getLocalized(
      translations_default.events.dateDescending,
      lang,
    ),
    noResults: getLocalized(translations_default.events.noResults, lang),
    date: getLocalized(translations_default.events.date, lang),
    location: getLocalized(translations_default.events.location, lang),
    category: getLocalized(translations_default.events.category, lang),
    backToEvents: getLocalized(translations_default.events.backToEvents, lang),
    applicationSubject: getLocalized(
      translations_default.events.applicationSubject,
      lang,
    ),
  };
};
var getLegalTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.legal.title, lang),
    termsOfService: getLocalized(
      translations_default.legal.termsOfService,
      lang,
    ),
    privacyPolicy: getLocalized(translations_default.legal.privacyPolicy, lang),
    openDocument: getLocalized(translations_default.legal.openDocument, lang),
  };
};
var getFormTranslation = (lang) => {
  return {
    email: {
      label: getLocalized(translations_default.forms.email.label, lang),
      placeholder: getLocalized(
        translations_default.forms.email.placeholder,
        lang,
      ),
      errorMessage: getLocalized(
        translations_default.forms.email.errorMessage,
        lang,
      ),
    },
    name: {
      label: getLocalized(translations_default.forms.name.label, lang),
      placeholder: getLocalized(
        translations_default.forms.name.placeholder,
        lang,
      ),
      errorMessage: getLocalized(
        translations_default.forms.name.errorMessage,
        lang,
      ),
    },
    subject: {
      label: getLocalized(translations_default.forms.subject.label, lang),
      placeholder: getLocalized(
        translations_default.forms.subject.placeholder,
        lang,
      ),
      errorMessage: getLocalized(
        translations_default.forms.subject.errorMessage,
        lang,
      ),
    },
    message: {
      label: getLocalized(translations_default.forms.message.label, lang),
      placeholder: getLocalized(
        translations_default.forms.message.placeholder,
        lang,
      ),
      errorMessage: getLocalized(
        translations_default.forms.message.errorMessage,
        lang,
      ),
    },
    disclaimerPrefix: getLocalized(
      translations_default.forms.disclaimerPrefix,
      lang,
    ),
    termsAndConditions: getLocalized(
      translations_default.forms.termsAndConditions,
      lang,
    ),
    disclaimerJoiner: getLocalized(
      translations_default.forms.disclaimerJoiner,
      lang,
    ),
    privacyPolicy: getLocalized(translations_default.forms.privacyPolicy, lang),
    success: {
      title: getLocalized(translations_default.forms.success.title, lang),
      message: getLocalized(translations_default.forms.success.message, lang),
      close: getLocalized(translations_default.forms.success.close, lang),
    },
    sendError: getLocalized(translations_default.forms.sendError, lang),
    contactMessage: getLocalized(
      translations_default.forms.contactMessage,
      lang,
    ),
  };
};
var getCallToActionTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.callToAction.title, lang),
    contactButton: getLocalized(
      translations_default.callToAction.contactButton,
      lang,
    ),
  };
};
var getButtonTranslation = (lang) => {
  return {
    discoverNow: getLocalized(translations_default.buttons.discoverNow, lang),
    learnMore: getLocalized(translations_default.buttons.learnMore, lang),
    sendMessage: getLocalized(translations_default.buttons.sendMessage, lang),
    scrollDown: getLocalized(translations_default.buttons.scrollDown, lang),
    backToTop: getLocalized(translations_default.buttons.backToTop, lang),
    all: getLocalized(translations_default.buttons.all, lang),
    applyNow: getLocalized(translations_default.buttons.applyNow, lang),
    shareOn: getLocalized(translations_default.buttons.shareOn, lang),
    copyLink: getLocalized(translations_default.buttons.copyLink, lang),
  };
};
var getFooterTranslation = (lang) => {
  return {
    production: getLocalized(translations_default.footer.production, lang),
    slogan: getLocalized(translations_default.footer.slogan, lang),
  };
};
var getNotFoundTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.notFound.title, lang),
    text: getLocalized(translations_default.notFound.text, lang),
    backToHome: getLocalized(translations_default.notFound.homeButton, lang),
  };
};
var getDemoPopupTranslation = (lang) => {
  return {
    title: getLocalized(translations_default.demoPopup.title, lang),
    description: getLocalized(translations_default.demoPopup.text, lang),
    closeButton: getLocalized(translations_default.demoPopup.closeButton, lang),
    purchaseButton: getLocalized(
      translations_default.demoPopup.purchaseButton,
      lang,
    ),
  };
};

// src/core/font-startup.ts
var normalizeFont = (value) => (typeof value === "string" ? value.trim() : "");
function themeFontDescriptors(fonts) {
  const families = [fonts?.heading, fonts?.body, fonts?.banner]
    .map(normalizeFont)
    .filter(Boolean);
  return [...new Set(families)].map((family, index) => ({
    id: `sitegen-theme-font-${index}`,
    family,
    // Keep this identical to the existing runtime loader. The bootstrap runs
    // it before first paint instead of waiting for a React effect.
    href: `https://fonts.googleapis.com/css2?family=${family.replace(
      / /g,
      "+",
    )}:wght@300;400;500;600;700&display=swap`,
  }));
}
function createThemeFontBootstrapScript(fonts) {
  const descriptors = JSON.stringify(themeFontDescriptors(fonts)).replace(
    /</g,
    "\\u003c",
  );
  return `(${fontBootstrap.toString()})(${descriptors});`;
}
function fontBootstrap(fonts) {
  const root = document.documentElement;
  if (!fonts.length) {
    root.dataset.sitegenFontsReady = "true";
    return;
  }
  root.setAttribute("data-sitegen-fonts-pending", "true");
  const guard = document.createElement("style");
  guard.id = "sitegen-font-guard";
  guard.textContent =
    "html[data-sitegen-fonts-pending] body{visibility:hidden!important}";
  document.head.appendChild(guard);
  let finished = false;
  const finish = () => {
    if (finished) return;
    finished = true;
    root.removeAttribute("data-sitegen-fonts-pending");
    root.dataset.sitegenFontsReady = "true";
    window.dispatchEvent(new Event("sitegen:fonts-ready"));
  };
  const fallback = window.setTimeout(finish, 8e3);
  const ensureStylesheet = (font2) =>
    new Promise((resolve) => {
      let link = document.getElementById(font2.id);
      if (!link) {
        link = document.createElement("link");
        link.id = font2.id;
        link.rel = "stylesheet";
        link.href = font2.href;
        link.dataset.sitegenFontFamily = font2.family;
        document.head.appendChild(link);
      }
      if (link.sheet) {
        resolve();
        return;
      }
      const done = () => resolve();
      link.addEventListener("load", done, { once: true });
      link.addEventListener("error", done, { once: true });
    });
  const loadFaces = async () => {
    if (!("fonts" in document) || !document.fonts?.load) return;
    const weights = [300, 400, 500, 600, 700];
    await Promise.all(
      fonts.flatMap((font2) =>
        weights.map((weight) =>
          document.fonts.load(
            `${weight} 1em "${font2.family.replace(/"/g, '\\"')}"`,
          ),
        ),
      ),
    );
    await document.fonts.ready;
  };
  void Promise.all(fonts.map(ensureStylesheet))
    .then(loadFaces)
    .catch(() => void 0)
    .finally(() => {
      window.clearTimeout(fallback);
      finish();
    });
}

// src/core/snapshot-model.ts
var detailSections = ["portfolio", "blog", "pricing", "careers", "events"];
var sectionOf = (site, key) => site[key];
var localText = (value, primary) =>
  typeof value === "string" ? value : (value?.[primary] ?? "");
var safeJson = (value) => JSON.stringify(value).replace(/</g, "\\u003c");
var escapeHtml = (value) =>
  String(value ?? "")
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;")
    .replace(/'/g, "&#39;");
function normalizeRoute(pathname2, basePath) {
  let path3 = pathname2.split(/[?#]/, 1)[0] || "/";
  if (basePath && (path3 === basePath || path3.startsWith(`${basePath}/`))) {
    path3 = path3.slice(basePath.length) || "/";
  }
  return `/${path3.replace(/^\/+|\/+$/g, "")}/`.replace(/^\/\/$/, "/");
}
function validateWebsite(site) {
  if (!site?.general || !site.home || !site.optionalSections)
    throw new Error("Invalid website.json");
  const {
    primaryLanguage: primaryLanguage2,
    supportedLanguages: supportedLanguages2,
  } = site.general;
  if (
    !Object.hasOwn(landingPageSlugs, primaryLanguage2) ||
    !Array.isArray(supportedLanguages2) ||
    supportedLanguages2.some((code) => !Object.hasOwn(landingPageSlugs, code))
  ) {
    throw new Error("Unsupported or missing language configuration");
  }
}
function structureOf(site) {
  validateWebsite(site);
  return JSON.stringify({
    primaryLanguage: site.general.primaryLanguage,
    languages: [
      .../* @__PURE__ */ new Set([
        site.general.primaryLanguage,
        ...site.general.supportedLanguages,
      ]),
    ].sort(),
    optionalSections: Object.fromEntries(
      Object.entries(site.optionalSections).sort(([a], [b]) =>
        a.localeCompare(b),
      ),
    ),
    counts: Object.fromEntries(
      detailSections.map((key) => [
        key,
        sectionOf(site, key)?.items?.length ?? 0,
      ]),
    ),
  });
}
function assertMetaCompatible(site, manifest) {
  if (structureOf(site) !== manifest.structure) {
    throw new Error(
      "FULL build required: languages, enabled sections, or detail-page counts changed. No files were changed.",
    );
  }
}
function makeSeoSeed(site, manifest) {
  assertMetaCompatible(site, manifest);
  const primary = manifest.primaryLanguage;
  const text = (value) => localText(value, primary);
  const clean = (value) => stripRichText(value).trim();
  const siteName = clean(text(site.home.websiteName));
  const nav = getNavigationTranslation(primary);
  const absolute = (resource) => {
    if (!resource) return "";
    if (/^https?:\/\//i.test(resource)) return resource;
    if (/^[a-z][a-z\d+.-]*:/i.test(resource) || resource.startsWith("//"))
      return "";
    const path3 = resource.startsWith("/") ? resource : `/${resource}`;
    const prefixed =
      manifest.basePath &&
      !(
        path3 === manifest.basePath || path3.startsWith(`${manifest.basePath}/`)
      )
        ? `${manifest.basePath}${path3}`
        : path3;
    return `${manifest.siteOrigin}${prefixed}`;
  };
  const pages2 = {};
  for (const route of manifest.routes) {
    const section = sectionOf(site, route.page);
    const item = route.id ? section?.items?.[route.id - 1] : void 0;
    let title = siteName;
    let description = clean(text(site.home.slogan));
    if (route.id) {
      title = `${clean(text(item?.title))} | ${siteName}`;
      description = clean(text(item?.description ?? item?.text));
    } else if (route.page !== "home") {
      const label =
        clean(text(section?.sectionName ?? section?.title ?? section?.name)) ||
        nav[route.page] ||
        siteName;
      title = `${label} | ${siteName}`;
      description = clean(text(section?.text)) || description;
    }
    const noindex = route.page === "not-found" || item?.status === "DISABLED";
    pages2[route.path] = {
      title,
      description: truncateWordSafe(description, 160),
      canonical: manifest.siteOrigin
        ? `${manifest.siteOrigin}${manifest.basePath}${route.path}`
        : "",
      image: absolute(
        item?.image?.file ??
          item?.images?.[0]?.file ??
          site.home.companyLogo.image?.file,
      ),
      noindex,
    };
  }
  const contacts = site.contactInfo?.contact ?? [];
  const company = site.contactInfo?.company;
  const email = contacts.find((c) => c.type === "EMAIL")?.value;
  const phone = contacts.find((c) => c.type === "PHONE")?.value;
  const sameAs = contacts
    .filter((c) =>
      [
        "LINKEDIN",
        "INSTAGRAM",
        "FACEBOOK",
        "TWITTER",
        "TIKTOK",
        "WEBSITE",
      ].includes(c.type),
    )
    .map((c) => c.value)
    .filter((url) => /^https?:\/\//i.test(url));
  const logo = absolute(site.home.companyLogo.image?.file);
  const organization = {
    "@context": "https://schema.org",
    "@type": "Organization",
    name: siteName,
    ...(manifest.siteOrigin
      ? { url: `${manifest.siteOrigin}${manifest.basePath}/` }
      : {}),
    ...(logo ? { logo } : {}),
    description: clean(text(site.home.slogan)),
    ...(email ? { email } : {}),
    ...(phone ? { telephone: phone } : {}),
    ...(sameAs.length ? { sameAs } : {}),
    ...(company?.address
      ? {
          address: {
            "@type": "PostalAddress",
            streetAddress: company.address,
            postalCode: company.postalCode,
            addressLocality: company.postalOffice,
            addressCountry: company.country,
          },
        }
      : {}),
  };
  return {
    schema: 1,
    basePath: manifest.basePath,
    primaryLanguage: primary,
    siteName,
    organization,
    fonts: {
      heading: site.theme?.fonts?.heading ?? null,
      body: site.theme?.fonts?.body ?? null,
      banner: site.theme?.fonts?.banner ?? null,
    },
    pages: pages2,
  };
}
function resolvePageMeta(seed, pathname2) {
  return (
    seed.pages[normalizeRoute(pathname2, seed.basePath)] ??
    seed.pages["/404/"] ??
    seed.pages["/"]
  );
}
function renderSeoHead(seed, pathname2) {
  const meta = resolvePageMeta(seed, pathname2);
  const attr = escapeHtml;
  const fontBootstrap2 = createThemeFontBootstrapScript(seed.fonts).replace(
    /<\/script/gi,
    "<\\/script",
  );
  return (
    `<meta id="sitegen-head-start" name="sitegen:head" content="start"/><script id="sitegen-font-bootstrap">${fontBootstrap2}</script><script id="sitegen-seo-data" type="application/json">${safeJson(seed)}</script><title id="sitegen-title">${attr(meta.title)}</title><meta id="sitegen-description" name="description" content="${attr(meta.description)}"/><meta id="sitegen-robots" name="robots" content="${meta.noindex ? "noindex,nofollow" : "index,follow"}"/>` +
    (meta.canonical
      ? `<link id="sitegen-canonical" rel="canonical" href="${attr(meta.canonical)}"/>`
      : "") +
    `<meta id="sitegen-og-title" property="og:title" content="${attr(meta.title)}"/><meta id="sitegen-og-description" property="og:description" content="${attr(meta.description)}"/><meta id="sitegen-og-site" property="og:site_name" content="${attr(seed.siteName)}"/><meta id="sitegen-og-type" property="og:type" content="website"/>` +
    (meta.canonical
      ? `<meta id="sitegen-og-url" property="og:url" content="${attr(meta.canonical)}"/>`
      : "") +
    (meta.image
      ? `<meta id="sitegen-og-image" property="og:image" content="${attr(meta.image)}"/>`
      : "") +
    `<script id="sitegen-organization" type="application/ld+json">${safeJson(seed.organization)}</script><meta id="sitegen-head-end" name="sitegen:head" content="end"/>`
  );
}
function patchExportedHtml(html2, snapshot, seed, route) {
  const start2 = "<!--sitegen-primary-html:start-->";
  const end2 = "<!--sitegen-primary-html:end-->";
  const left2 = html2.indexOf(start2);
  const right2 = html2.indexOf(end2, left2 + start2.length);
  if (
    left2 < 0 ||
    right2 < 0 ||
    html2.indexOf(start2, left2 + start2.length) !== -1
  ) {
    throw new Error(
      `Missing or duplicate snapshot island in ${route.file}. Run the updated FULL build first.`,
    );
  }
  let output =
    html2.slice(0, left2 + start2.length) + snapshot + html2.slice(right2);
  if (!/<meta\b[^>]*\bid="sitegen-head-start"/.test(output)) {
    throw new Error(`Missing managed SEO head in ${route.file}`);
  }
  output = output
    .replace(
      /<script\b[^>]*\bid="sitegen-(?:font-bootstrap|seo-data|organization)"[^>]*>[\s\S]*?<\/script>/g,
      "",
    )
    .replace(/<title\b[^>]*\bid="sitegen-title"[^>]*>[\s\S]*?<\/title>/g, "")
    .replace(
      /<(?:meta|link)\b[^>]*\bid="sitegen-(?:head-start|head-end|description|robots|canonical|og-title|og-description|og-site|og-type|og-url|og-image)"[^>]*\/?>/g,
      "",
    );
  if (!output.includes("</head>"))
    throw new Error(`Missing head in ${route.file}`);
  return output.replace(
    "</head>",
    () => `${renderSeoHead(seed, route.path)}</head>`,
  );
}

// scripts/snapshot/render.tsx
var import_server = __toESM(require_server_node(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/colors/common.js
var common = {
  black: "#000",
  white: "#fff",
};
var common_default = common;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/colors/red.js
var red = {
  50: "#ffebee",
  100: "#ffcdd2",
  200: "#ef9a9a",
  300: "#e57373",
  400: "#ef5350",
  500: "#f44336",
  600: "#e53935",
  700: "#d32f2f",
  800: "#c62828",
  900: "#b71c1c",
  A100: "#ff8a80",
  A200: "#ff5252",
  A400: "#ff1744",
  A700: "#d50000",
};
var red_default = red;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/colors/purple.js
var purple = {
  50: "#f3e5f5",
  100: "#e1bee7",
  200: "#ce93d8",
  300: "#ba68c8",
  400: "#ab47bc",
  500: "#9c27b0",
  600: "#8e24aa",
  700: "#7b1fa2",
  800: "#6a1b9a",
  900: "#4a148c",
  A100: "#ea80fc",
  A200: "#e040fb",
  A400: "#d500f9",
  A700: "#aa00ff",
};
var purple_default = purple;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/colors/blue.js
var blue = {
  50: "#e3f2fd",
  100: "#bbdefb",
  200: "#90caf9",
  300: "#64b5f6",
  400: "#42a5f5",
  500: "#2196f3",
  600: "#1e88e5",
  700: "#1976d2",
  800: "#1565c0",
  900: "#0d47a1",
  A100: "#82b1ff",
  A200: "#448aff",
  A400: "#2979ff",
  A700: "#2962ff",
};
var blue_default = blue;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/colors/lightBlue.js
var lightBlue = {
  50: "#e1f5fe",
  100: "#b3e5fc",
  200: "#81d4fa",
  300: "#4fc3f7",
  400: "#29b6f6",
  500: "#03a9f4",
  600: "#039be5",
  700: "#0288d1",
  800: "#0277bd",
  900: "#01579b",
  A100: "#80d8ff",
  A200: "#40c4ff",
  A400: "#00b0ff",
  A700: "#0091ea",
};
var lightBlue_default = lightBlue;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/colors/green.js
var green = {
  50: "#e8f5e9",
  100: "#c8e6c9",
  200: "#a5d6a7",
  300: "#81c784",
  400: "#66bb6a",
  500: "#4caf50",
  600: "#43a047",
  700: "#388e3c",
  800: "#2e7d32",
  900: "#1b5e20",
  A100: "#b9f6ca",
  A200: "#69f0ae",
  A400: "#00e676",
  A700: "#00c853",
};
var green_default = green;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/colors/orange.js
var orange = {
  50: "#fff3e0",
  100: "#ffe0b2",
  200: "#ffcc80",
  300: "#ffb74d",
  400: "#ffa726",
  500: "#ff9800",
  600: "#fb8c00",
  700: "#f57c00",
  800: "#ef6c00",
  900: "#e65100",
  A100: "#ffd180",
  A200: "#ffab40",
  A400: "#ff9100",
  A700: "#ff6d00",
};
var orange_default = orange;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/colors/grey.js
var grey = {
  50: "#fafafa",
  100: "#f5f5f5",
  200: "#eeeeee",
  300: "#e0e0e0",
  400: "#bdbdbd",
  500: "#9e9e9e",
  600: "#757575",
  700: "#616161",
  800: "#424242",
  900: "#212121",
  A100: "#f5f5f5",
  A200: "#eeeeee",
  A400: "#bdbdbd",
  A700: "#616161",
};
var grey_default = grey;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/formatMuiErrorMessage/formatMuiErrorMessage.js
function formatMuiErrorMessage(code, ...args) {
  const url = new URL(`https://mui.com/production-error/?code=${code}`);
  args.forEach((arg2) => url.searchParams.append("args[]", arg2));
  return `Minified MUI error #${code}; visit ${url} for the full message.`;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/identifier.js
var identifier_default = "$$material";

// node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/extends.js
function _extends() {
  return (
    (_extends = Object.assign
      ? Object.assign.bind()
      : function (n) {
          for (var e = 1; e < arguments.length; e++) {
            var t = arguments[e];
            for (var r2 in t)
              ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
          }
          return n;
        }),
    _extends.apply(null, arguments)
  );
}

// node_modules/.pnpm/@emotion+react@11.14.0_@types+react@19.2.18_react@19.2.8/node_modules/@emotion/react/dist/emotion-element-d59e098f.esm.js
var React2 = __toESM(require_react());
var import_react = __toESM(require_react());

// node_modules/.pnpm/@emotion+sheet@1.4.0/node_modules/@emotion/sheet/dist/emotion-sheet.esm.js
var isDevelopment = false;
function sheetForTag(tag) {
  if (tag.sheet) {
    return tag.sheet;
  }
  for (var i = 0; i < document.styleSheets.length; i++) {
    if (document.styleSheets[i].ownerNode === tag) {
      return document.styleSheets[i];
    }
  }
  return void 0;
}
function createStyleElement(options) {
  var tag = document.createElement("style");
  tag.setAttribute("data-emotion", options.key);
  if (options.nonce !== void 0) {
    tag.setAttribute("nonce", options.nonce);
  }
  tag.appendChild(document.createTextNode(""));
  tag.setAttribute("data-s", "");
  return tag;
}
var StyleSheet = /* @__PURE__ */ (function () {
  function StyleSheet2(options) {
    var _this = this;
    this._insertTag = function (tag) {
      var before;
      if (_this.tags.length === 0) {
        if (_this.insertionPoint) {
          before = _this.insertionPoint.nextSibling;
        } else if (_this.prepend) {
          before = _this.container.firstChild;
        } else {
          before = _this.before;
        }
      } else {
        before = _this.tags[_this.tags.length - 1].nextSibling;
      }
      _this.container.insertBefore(tag, before);
      _this.tags.push(tag);
    };
    this.isSpeedy = options.speedy === void 0 ? !isDevelopment : options.speedy;
    this.tags = [];
    this.ctr = 0;
    this.nonce = options.nonce;
    this.key = options.key;
    this.container = options.container;
    this.prepend = options.prepend;
    this.insertionPoint = options.insertionPoint;
    this.before = null;
  }
  var _proto = StyleSheet2.prototype;
  _proto.hydrate = function hydrate(nodes) {
    nodes.forEach(this._insertTag);
  };
  _proto.insert = function insert(rule) {
    if (this.ctr % (this.isSpeedy ? 65e3 : 1) === 0) {
      this._insertTag(createStyleElement(this));
    }
    var tag = this.tags[this.tags.length - 1];
    if (this.isSpeedy) {
      var sheet = sheetForTag(tag);
      try {
        sheet.insertRule(rule, sheet.cssRules.length);
      } catch (e) {}
    } else {
      tag.appendChild(document.createTextNode(rule));
    }
    this.ctr++;
  };
  _proto.flush = function flush() {
    this.tags.forEach(function (tag) {
      var _tag$parentNode;
      return (_tag$parentNode = tag.parentNode) == null
        ? void 0
        : _tag$parentNode.removeChild(tag);
    });
    this.tags = [];
    this.ctr = 0;
  };
  return StyleSheet2;
})();

// node_modules/.pnpm/stylis@4.2.0/node_modules/stylis/src/Enum.js
var MS = "-ms-";
var MOZ = "-moz-";
var WEBKIT = "-webkit-";
var COMMENT = "comm";
var RULESET = "rule";
var DECLARATION = "decl";
var IMPORT = "@import";
var KEYFRAMES = "@keyframes";
var LAYER = "@layer";

// node_modules/.pnpm/stylis@4.2.0/node_modules/stylis/src/Utility.js
var abs = Math.abs;
var from = String.fromCharCode;
var assign = Object.assign;
function hash(value, length2) {
  return charat(value, 0) ^ 45
    ? (((((((length2 << 2) ^ charat(value, 0)) << 2) ^ charat(value, 1)) << 2) ^
        charat(value, 2)) <<
        2) ^
        charat(value, 3)
    : 0;
}
function trim(value) {
  return value.trim();
}
function match(value, pattern) {
  return (value = pattern.exec(value)) ? value[0] : value;
}
function replace(value, pattern, replacement) {
  return value.replace(pattern, replacement);
}
function indexof(value, search) {
  return value.indexOf(search);
}
function charat(value, index) {
  return value.charCodeAt(index) | 0;
}
function substr(value, begin, end2) {
  return value.slice(begin, end2);
}
function strlen(value) {
  return value.length;
}
function sizeof(value) {
  return value.length;
}
function append(value, array) {
  return (array.push(value), value);
}
function combine(array, callback) {
  return array.map(callback).join("");
}

// node_modules/.pnpm/stylis@4.2.0/node_modules/stylis/src/Tokenizer.js
var line = 1;
var column = 1;
var length = 0;
var position = 0;
var character = 0;
var characters = "";
function node(value, root, parent, type, props, children, length2) {
  return {
    value,
    root,
    parent,
    type,
    props,
    children,
    line,
    column,
    length: length2,
    return: "",
  };
}
function copy(root, props) {
  return assign(
    node("", null, null, "", null, null, 0),
    root,
    { length: -root.length },
    props,
  );
}
function char() {
  return character;
}
function prev() {
  character = position > 0 ? charat(characters, --position) : 0;
  if ((column--, character === 10)) ((column = 1), line--);
  return character;
}
function next() {
  character = position < length ? charat(characters, position++) : 0;
  if ((column++, character === 10)) ((column = 1), line++);
  return character;
}
function peek() {
  return charat(characters, position);
}
function caret() {
  return position;
}
function slice(begin, end2) {
  return substr(characters, begin, end2);
}
function token(type) {
  switch (type) {
    // \0 \t \n \r \s whitespace token
    case 0:
    case 9:
    case 10:
    case 13:
    case 32:
      return 5;
    // ! + , / > @ ~ isolate token
    case 33:
    case 43:
    case 44:
    case 47:
    case 62:
    case 64:
    case 126:
    // ; { } breakpoint token
    case 59:
    case 123:
    case 125:
      return 4;
    // : accompanied token
    case 58:
      return 3;
    // " ' ( [ opening delimit token
    case 34:
    case 39:
    case 40:
    case 91:
      return 2;
    // ) ] closing delimit token
    case 41:
    case 93:
      return 1;
  }
  return 0;
}
function alloc(value) {
  return (
    (line = column = 1),
    (length = strlen((characters = value))),
    (position = 0),
    []
  );
}
function dealloc(value) {
  return ((characters = ""), value);
}
function delimit(type) {
  return trim(
    slice(
      position - 1,
      delimiter(type === 91 ? type + 2 : type === 40 ? type + 1 : type),
    ),
  );
}
function whitespace(type) {
  while ((character = peek()))
    if (character < 33) next();
    else break;
  return token(type) > 2 || token(character) > 3 ? "" : " ";
}
function escaping(index, count) {
  while (--count && next())
    if (
      character < 48 ||
      character > 102 ||
      (character > 57 && character < 65) ||
      (character > 70 && character < 97)
    )
      break;
  return slice(index, caret() + (count < 6 && peek() == 32 && next() == 32));
}
function delimiter(type) {
  while (next())
    switch (character) {
      // ] ) " '
      case type:
        return position;
      // " '
      case 34:
      case 39:
        if (type !== 34 && type !== 39) delimiter(character);
        break;
      // (
      case 40:
        if (type === 41) delimiter(type);
        break;
      // \
      case 92:
        next();
        break;
    }
  return position;
}
function commenter(type, index) {
  while (next())
    if (type + character === 47 + 10) break;
    else if (type + character === 42 + 42 && peek() === 47) break;
  return (
    "/*" + slice(index, position - 1) + "*" + from(type === 47 ? type : next())
  );
}
function identifier(index) {
  while (!token(peek())) next();
  return slice(index, position);
}

// node_modules/.pnpm/stylis@4.2.0/node_modules/stylis/src/Parser.js
function compile(value) {
  return dealloc(
    parse("", null, null, null, [""], (value = alloc(value)), 0, [0], value),
  );
}
function parse(
  value,
  root,
  parent,
  rule,
  rules,
  rulesets,
  pseudo,
  points,
  declarations,
) {
  var index = 0;
  var offset2 = 0;
  var length2 = pseudo;
  var atrule = 0;
  var property = 0;
  var previous = 0;
  var variable = 1;
  var scanning = 1;
  var ampersand = 1;
  var character2 = 0;
  var type = "";
  var props = rules;
  var children = rulesets;
  var reference2 = rule;
  var characters2 = type;
  while (scanning)
    switch (((previous = character2), (character2 = next()))) {
      // (
      case 40:
        if (previous != 108 && charat(characters2, length2 - 1) == 58) {
          if (
            indexof(
              (characters2 += replace(delimit(character2), "&", "&\f")),
              "&\f",
            ) != -1
          )
            ampersand = -1;
          break;
        }
      // " ' [
      case 34:
      case 39:
      case 91:
        characters2 += delimit(character2);
        break;
      // \t \n \r \s
      case 9:
      case 10:
      case 13:
      case 32:
        characters2 += whitespace(previous);
        break;
      // \
      case 92:
        characters2 += escaping(caret() - 1, 7);
        continue;
      // /
      case 47:
        switch (peek()) {
          case 42:
          case 47:
            append(
              comment(commenter(next(), caret()), root, parent),
              declarations,
            );
            break;
          default:
            characters2 += "/";
        }
        break;
      // {
      case 123 * variable:
        points[index++] = strlen(characters2) * ampersand;
      // } ; \0
      case 125 * variable:
      case 59:
      case 0:
        switch (character2) {
          // \0 }
          case 0:
          case 125:
            scanning = 0;
          // ;
          case 59 + offset2:
            if (ampersand == -1) characters2 = replace(characters2, /\f/g, "");
            if (property > 0 && strlen(characters2) - length2)
              append(
                property > 32
                  ? declaration(characters2 + ";", rule, parent, length2 - 1)
                  : declaration(
                      replace(characters2, " ", "") + ";",
                      rule,
                      parent,
                      length2 - 2,
                    ),
                declarations,
              );
            break;
          // @ ;
          case 59:
            characters2 += ";";
          // { rule/at-rule
          default:
            append(
              (reference2 = ruleset(
                characters2,
                root,
                parent,
                index,
                offset2,
                rules,
                points,
                type,
                (props = []),
                (children = []),
                length2,
              )),
              rulesets,
            );
            if (character2 === 123)
              if (offset2 === 0)
                parse(
                  characters2,
                  root,
                  reference2,
                  reference2,
                  props,
                  rulesets,
                  length2,
                  points,
                  children,
                );
              else
                switch (
                  atrule === 99 && charat(characters2, 3) === 110 ? 100 : atrule
                ) {
                  // d l m s
                  case 100:
                  case 108:
                  case 109:
                  case 115:
                    parse(
                      value,
                      reference2,
                      reference2,
                      rule &&
                        append(
                          ruleset(
                            value,
                            reference2,
                            reference2,
                            0,
                            0,
                            rules,
                            points,
                            type,
                            rules,
                            (props = []),
                            length2,
                          ),
                          children,
                        ),
                      rules,
                      children,
                      length2,
                      points,
                      rule ? props : children,
                    );
                    break;
                  default:
                    parse(
                      characters2,
                      reference2,
                      reference2,
                      reference2,
                      [""],
                      children,
                      0,
                      points,
                      children,
                    );
                }
        }
        ((index = offset2 = property = 0),
          (variable = ampersand = 1),
          (type = characters2 = ""),
          (length2 = pseudo));
        break;
      // :
      case 58:
        ((length2 = 1 + strlen(characters2)), (property = previous));
      default:
        if (variable < 1) {
          if (character2 == 123) --variable;
          else if (character2 == 125 && variable++ == 0 && prev() == 125)
            continue;
        }
        switch (((characters2 += from(character2)), character2 * variable)) {
          // &
          case 38:
            ampersand = offset2 > 0 ? 1 : ((characters2 += "\f"), -1);
            break;
          // ,
          case 44:
            ((points[index++] = (strlen(characters2) - 1) * ampersand),
              (ampersand = 1));
            break;
          // @
          case 64:
            if (peek() === 45) characters2 += delimit(next());
            ((atrule = peek()),
              (offset2 = length2 =
                strlen((type = characters2 += identifier(caret())))),
              character2++);
            break;
          // -
          case 45:
            if (previous === 45 && strlen(characters2) == 2) variable = 0;
        }
    }
  return rulesets;
}
function ruleset(
  value,
  root,
  parent,
  index,
  offset2,
  rules,
  points,
  type,
  props,
  children,
  length2,
) {
  var post = offset2 - 1;
  var rule = offset2 === 0 ? rules : [""];
  var size = sizeof(rule);
  for (var i = 0, j = 0, k = 0; i < index; ++i)
    for (
      var x = 0,
        y = substr(value, post + 1, (post = abs((j = points[i])))),
        z = value;
      x < size;
      ++x
    )
      if ((z = trim(j > 0 ? rule[x] + " " + y : replace(y, /&\f/g, rule[x]))))
        props[k++] = z;
  return node(
    value,
    root,
    parent,
    offset2 === 0 ? RULESET : type,
    props,
    children,
    length2,
  );
}
function comment(value, root, parent) {
  return node(
    value,
    root,
    parent,
    COMMENT,
    from(char()),
    substr(value, 2, -2),
    0,
  );
}
function declaration(value, root, parent, length2) {
  return node(
    value,
    root,
    parent,
    DECLARATION,
    substr(value, 0, length2),
    substr(value, length2 + 1, -1),
    length2,
  );
}

// node_modules/.pnpm/stylis@4.2.0/node_modules/stylis/src/Serializer.js
function serialize(children, callback) {
  var output = "";
  var length2 = sizeof(children);
  for (var i = 0; i < length2; i++)
    output += callback(children[i], i, children, callback) || "";
  return output;
}
function stringify(element, index, children, callback) {
  switch (element.type) {
    case LAYER:
      if (element.children.length) break;
    case IMPORT:
    case DECLARATION:
      return (element.return = element.return || element.value);
    case COMMENT:
      return "";
    case KEYFRAMES:
      return (element.return =
        element.value + "{" + serialize(element.children, callback) + "}");
    case RULESET:
      element.value = element.props.join(",");
  }
  return strlen((children = serialize(element.children, callback)))
    ? (element.return = element.value + "{" + children + "}")
    : "";
}

// node_modules/.pnpm/stylis@4.2.0/node_modules/stylis/src/Middleware.js
function middleware(collection) {
  var length2 = sizeof(collection);
  return function (element, index, children, callback) {
    var output = "";
    for (var i = 0; i < length2; i++)
      output += collection[i](element, index, children, callback) || "";
    return output;
  };
}
function rulesheet(callback) {
  return function (element) {
    if (!element.root) {
      if ((element = element.return)) callback(element);
    }
  };
}

// node_modules/.pnpm/@emotion+weak-memoize@0.4.0/node_modules/@emotion/weak-memoize/dist/emotion-weak-memoize.esm.js
var weakMemoize = function weakMemoize2(func) {
  var cache = /* @__PURE__ */ new WeakMap();
  return function (arg2) {
    if (cache.has(arg2)) {
      return cache.get(arg2);
    }
    var ret = func(arg2);
    cache.set(arg2, ret);
    return ret;
  };
};

// node_modules/.pnpm/@emotion+memoize@0.9.0/node_modules/@emotion/memoize/dist/emotion-memoize.esm.js
function memoize(fn2) {
  var cache = /* @__PURE__ */ Object.create(null);
  return function (arg2) {
    if (cache[arg2] === void 0) cache[arg2] = fn2(arg2);
    return cache[arg2];
  };
}

// node_modules/.pnpm/@emotion+cache@11.14.0/node_modules/@emotion/cache/dist/emotion-cache.esm.js
var isBrowser = typeof document !== "undefined";
var identifierWithPointTracking = function identifierWithPointTracking2(
  begin,
  points,
  index,
) {
  var previous = 0;
  var character2 = 0;
  while (true) {
    previous = character2;
    character2 = peek();
    if (previous === 38 && character2 === 12) {
      points[index] = 1;
    }
    if (token(character2)) {
      break;
    }
    next();
  }
  return slice(begin, position);
};
var toRules = function toRules2(parsed, points) {
  var index = -1;
  var character2 = 44;
  do {
    switch (token(character2)) {
      case 0:
        if (character2 === 38 && peek() === 12) {
          points[index] = 1;
        }
        parsed[index] += identifierWithPointTracking(
          position - 1,
          points,
          index,
        );
        break;
      case 2:
        parsed[index] += delimit(character2);
        break;
      case 4:
        if (character2 === 44) {
          parsed[++index] = peek() === 58 ? "&\f" : "";
          points[index] = parsed[index].length;
          break;
        }
      // fallthrough
      default:
        parsed[index] += from(character2);
    }
  } while ((character2 = next()));
  return parsed;
};
var getRules = function getRules2(value, points) {
  return dealloc(toRules(alloc(value), points));
};
var fixedElements = /* @__PURE__ */ new WeakMap();
var compat = function compat2(element) {
  if (
    element.type !== "rule" ||
    !element.parent || // positive .length indicates that this rule contains pseudo
    // negative .length indicates that this rule has been already prefixed
    element.length < 1
  ) {
    return;
  }
  var value = element.value;
  var parent = element.parent;
  var isImplicitRule =
    element.column === parent.column && element.line === parent.line;
  while (parent.type !== "rule") {
    parent = parent.parent;
    if (!parent) return;
  }
  if (
    element.props.length === 1 &&
    value.charCodeAt(0) !== 58 &&
    !fixedElements.get(parent)
  ) {
    return;
  }
  if (isImplicitRule) {
    return;
  }
  fixedElements.set(element, true);
  var points = [];
  var rules = getRules(value, points);
  var parentRules = parent.props;
  for (var i = 0, k = 0; i < rules.length; i++) {
    for (var j = 0; j < parentRules.length; j++, k++) {
      element.props[k] = points[i]
        ? rules[i].replace(/&\f/g, parentRules[j])
        : parentRules[j] + " " + rules[i];
    }
  }
};
var removeLabel = function removeLabel2(element) {
  if (element.type === "decl") {
    var value = element.value;
    if (
      // charcode for l
      value.charCodeAt(0) === 108 && // charcode for b
      value.charCodeAt(2) === 98
    ) {
      element["return"] = "";
      element.value = "";
    }
  }
};
function prefix(value, length2) {
  switch (hash(value, length2)) {
    // color-adjust
    case 5103:
      return WEBKIT + "print-" + value + value;
    // animation, animation-(delay|direction|duration|fill-mode|iteration-count|name|play-state|timing-function)
    case 5737:
    case 4201:
    case 3177:
    case 3433:
    case 1641:
    case 4457:
    case 2921:
    // text-decoration, filter, clip-path, backface-visibility, column, box-decoration-break
    case 5572:
    case 6356:
    case 5844:
    case 3191:
    case 6645:
    case 3005:
    // mask, mask-image, mask-(mode|clip|size), mask-(repeat|origin), mask-position, mask-composite,
    case 6391:
    case 5879:
    case 5623:
    case 6135:
    case 4599:
    case 4855:
    // background-clip, columns, column-(count|fill|gap|rule|rule-color|rule-style|rule-width|span|width)
    case 4215:
    case 6389:
    case 5109:
    case 5365:
    case 5621:
    case 3829:
      return WEBKIT + value + value;
    // appearance, user-select, transform, hyphens, text-size-adjust
    case 5349:
    case 4246:
    case 4810:
    case 6968:
    case 2756:
      return WEBKIT + value + MOZ + value + MS + value + value;
    // flex, flex-direction
    case 6828:
    case 4268:
      return WEBKIT + value + MS + value + value;
    // order
    case 6165:
      return WEBKIT + value + MS + "flex-" + value + value;
    // align-items
    case 5187:
      return (
        WEBKIT +
        value +
        replace(
          value,
          /(\w+).+(:[^]+)/,
          WEBKIT + "box-$1$2" + MS + "flex-$1$2",
        ) +
        value
      );
    // align-self
    case 5443:
      return (
        WEBKIT +
        value +
        MS +
        "flex-item-" +
        replace(value, /flex-|-self/, "") +
        value
      );
    // align-content
    case 4675:
      return (
        WEBKIT +
        value +
        MS +
        "flex-line-pack" +
        replace(value, /align-content|flex-|-self/, "") +
        value
      );
    // flex-shrink
    case 5548:
      return WEBKIT + value + MS + replace(value, "shrink", "negative") + value;
    // flex-basis
    case 5292:
      return (
        WEBKIT + value + MS + replace(value, "basis", "preferred-size") + value
      );
    // flex-grow
    case 6060:
      return (
        WEBKIT +
        "box-" +
        replace(value, "-grow", "") +
        WEBKIT +
        value +
        MS +
        replace(value, "grow", "positive") +
        value
      );
    // transition
    case 4554:
      return (
        WEBKIT +
        replace(value, /([^-])(transform)/g, "$1" + WEBKIT + "$2") +
        value
      );
    // cursor
    case 6187:
      return (
        replace(
          replace(
            replace(value, /(zoom-|grab)/, WEBKIT + "$1"),
            /(image-set)/,
            WEBKIT + "$1",
          ),
          value,
          "",
        ) + value
      );
    // background, background-image
    case 5495:
    case 3959:
      return replace(value, /(image-set\([^]*)/, WEBKIT + "$1$`$1");
    // justify-content
    case 4968:
      return (
        replace(
          replace(
            value,
            /(.+:)(flex-)?(.*)/,
            WEBKIT + "box-pack:$3" + MS + "flex-pack:$3",
          ),
          /s.+-b[^;]+/,
          "justify",
        ) +
        WEBKIT +
        value +
        value
      );
    // (margin|padding)-inline-(start|end)
    case 4095:
    case 3583:
    case 4068:
    case 2532:
      return replace(value, /(.+)-inline(.+)/, WEBKIT + "$1$2") + value;
    // (min|max)?(width|height|inline-size|block-size)
    case 8116:
    case 7059:
    case 5753:
    case 5535:
    case 5445:
    case 5701:
    case 4933:
    case 4677:
    case 5533:
    case 5789:
    case 5021:
    case 4765:
      if (strlen(value) - 1 - length2 > 6)
        switch (charat(value, length2 + 1)) {
          // (m)ax-content, (m)in-content
          case 109:
            if (charat(value, length2 + 4) !== 45) break;
          // (f)ill-available, (f)it-content
          case 102:
            return (
              replace(
                value,
                /(.+:)(.+)-([^]+)/,
                "$1" +
                  WEBKIT +
                  "$2-$3$1" +
                  MOZ +
                  (charat(value, length2 + 3) == 108 ? "$3" : "$2-$3"),
              ) + value
            );
          // (s)tretch
          case 115:
            return ~indexof(value, "stretch")
              ? prefix(replace(value, "stretch", "fill-available"), length2) +
                  value
              : value;
        }
      break;
    // position: sticky
    case 4949:
      if (charat(value, length2 + 1) !== 115) break;
    // display: (flex|inline-flex)
    case 6444:
      switch (
        charat(value, strlen(value) - 3 - (~indexof(value, "!important") && 10))
      ) {
        // stic(k)y
        case 107:
          return replace(value, ":", ":" + WEBKIT) + value;
        // (inline-)?fl(e)x
        case 101:
          return (
            replace(
              value,
              /(.+:)([^;!]+)(;|!.+)?/,
              "$1" +
                WEBKIT +
                (charat(value, 14) === 45 ? "inline-" : "") +
                "box$3$1" +
                WEBKIT +
                "$2$3$1" +
                MS +
                "$2box$3",
            ) + value
          );
      }
      break;
    // writing-mode
    case 5936:
      switch (charat(value, length2 + 11)) {
        // vertical-l(r)
        case 114:
          return (
            WEBKIT +
            value +
            MS +
            replace(value, /[svh]\w+-[tblr]{2}/, "tb") +
            value
          );
        // vertical-r(l)
        case 108:
          return (
            WEBKIT +
            value +
            MS +
            replace(value, /[svh]\w+-[tblr]{2}/, "tb-rl") +
            value
          );
        // horizontal(-)tb
        case 45:
          return (
            WEBKIT +
            value +
            MS +
            replace(value, /[svh]\w+-[tblr]{2}/, "lr") +
            value
          );
      }
      return WEBKIT + value + MS + value + value;
  }
  return value;
}
var prefixer = function prefixer2(element, index, children, callback) {
  if (element.length > -1) {
    if (!element["return"])
      switch (element.type) {
        case DECLARATION:
          element["return"] = prefix(element.value, element.length);
          break;
        case KEYFRAMES:
          return serialize(
            [
              copy(element, {
                value: replace(element.value, "@", "@" + WEBKIT),
              }),
            ],
            callback,
          );
        case RULESET:
          if (element.length)
            return combine(element.props, function (value) {
              switch (match(value, /(::plac\w+|:read-\w+)/)) {
                // :read-(only|write)
                case ":read-only":
                case ":read-write":
                  return serialize(
                    [
                      copy(element, {
                        props: [
                          replace(value, /:(read-\w+)/, ":" + MOZ + "$1"),
                        ],
                      }),
                    ],
                    callback,
                  );
                // :placeholder
                case "::placeholder":
                  return serialize(
                    [
                      copy(element, {
                        props: [
                          replace(
                            value,
                            /:(plac\w+)/,
                            ":" + WEBKIT + "input-$1",
                          ),
                        ],
                      }),
                      copy(element, {
                        props: [replace(value, /:(plac\w+)/, ":" + MOZ + "$1")],
                      }),
                      copy(element, {
                        props: [replace(value, /:(plac\w+)/, MS + "input-$1")],
                      }),
                    ],
                    callback,
                  );
              }
              return "";
            });
      }
  }
};
var getServerStylisCache = isBrowser
  ? void 0
  : weakMemoize(function () {
      return memoize(function () {
        return {};
      });
    });
var defaultStylisPlugins = [prefixer];
var createCache = function createCache2(options) {
  var key = options.key;
  if (isBrowser && key === "css") {
    var ssrStyles = document.querySelectorAll(
      "style[data-emotion]:not([data-s])",
    );
    Array.prototype.forEach.call(ssrStyles, function (node2) {
      var dataEmotionAttribute = node2.getAttribute("data-emotion");
      if (dataEmotionAttribute.indexOf(" ") === -1) {
        return;
      }
      document.head.appendChild(node2);
      node2.setAttribute("data-s", "");
    });
  }
  var stylisPlugins = options.stylisPlugins || defaultStylisPlugins;
  var inserted = {};
  var container;
  var nodesToHydrate = [];
  if (isBrowser) {
    container = options.container || document.head;
    Array.prototype.forEach.call(
      // this means we will ignore elements which don't have a space in them which
      // means that the style elements we're looking at are only Emotion 11 server-rendered style elements
      document.querySelectorAll('style[data-emotion^="' + key + ' "]'),
      function (node2) {
        var attrib = node2.getAttribute("data-emotion").split(" ");
        for (var i = 1; i < attrib.length; i++) {
          inserted[attrib[i]] = true;
        }
        nodesToHydrate.push(node2);
      },
    );
  }
  var _insert;
  var omnipresentPlugins = [compat, removeLabel];
  if (!getServerStylisCache) {
    var currentSheet;
    var finalizingPlugins = [
      stringify,
      rulesheet(function (rule) {
        currentSheet.insert(rule);
      }),
    ];
    var serializer = middleware(
      omnipresentPlugins.concat(stylisPlugins, finalizingPlugins),
    );
    var stylis = function stylis2(styles5) {
      return serialize(compile(styles5), serializer);
    };
    _insert = function insert(selector, serialized, sheet, shouldCache) {
      currentSheet = sheet;
      stylis(
        selector ? selector + "{" + serialized.styles + "}" : serialized.styles,
      );
      if (shouldCache) {
        cache.inserted[serialized.name] = true;
      }
    };
  } else {
    var _finalizingPlugins = [stringify];
    var _serializer = middleware(
      omnipresentPlugins.concat(stylisPlugins, _finalizingPlugins),
    );
    var _stylis = function _stylis2(styles5) {
      return serialize(compile(styles5), _serializer);
    };
    var serverStylisCache = getServerStylisCache(stylisPlugins)(key);
    var getRules3 = function getRules4(selector, serialized) {
      var name = serialized.name;
      if (serverStylisCache[name] === void 0) {
        serverStylisCache[name] = _stylis(
          selector
            ? selector + "{" + serialized.styles + "}"
            : serialized.styles,
        );
      }
      return serverStylisCache[name];
    };
    _insert = function _insert2(selector, serialized, sheet, shouldCache) {
      var name = serialized.name;
      var rules = getRules3(selector, serialized);
      if (cache.compat === void 0) {
        if (shouldCache) {
          cache.inserted[name] = true;
        }
        return rules;
      } else {
        if (shouldCache) {
          cache.inserted[name] = rules;
        } else {
          return rules;
        }
      }
    };
  }
  var cache = {
    key,
    sheet: new StyleSheet({
      key,
      container,
      nonce: options.nonce,
      speedy: options.speedy,
      prepend: options.prepend,
      insertionPoint: options.insertionPoint,
    }),
    nonce: options.nonce,
    inserted,
    registered: {},
    insert: _insert,
  };
  cache.sheet.hydrate(nodesToHydrate);
  return cache;
};

// node_modules/.pnpm/@emotion+utils@1.4.2/node_modules/@emotion/utils/dist/emotion-utils.esm.js
var isBrowser2 = typeof document !== "undefined";
function getRegisteredStyles(registered, registeredStyles, classNames) {
  var rawClassName = "";
  classNames.split(" ").forEach(function (className) {
    if (registered[className] !== void 0) {
      registeredStyles.push(registered[className] + ";");
    } else if (className) {
      rawClassName += className + " ";
    }
  });
  return rawClassName;
}
var registerStyles = function registerStyles2(cache, serialized, isStringTag2) {
  var className = cache.key + "-" + serialized.name;
  if (
    // we only need to add the styles to the registered cache if the
    // class name could be used further down
    // the tree but if it's a string tag, we know it won't
    // so we don't have to add it to registered cache.
    // this improves memory usage since we can avoid storing the whole style string
    (isStringTag2 === false || // we need to always store it if we're in compat mode and
      // in node since emotion-server relies on whether a style is in
      // the registered cache to know whether a style is global or not
      // also, note that this check will be dead code eliminated in the browser
      (isBrowser2 === false && cache.compat !== void 0)) &&
    cache.registered[className] === void 0
  ) {
    cache.registered[className] = serialized.styles;
  }
};
var insertStyles = function insertStyles2(cache, serialized, isStringTag2) {
  registerStyles(cache, serialized, isStringTag2);
  var className = cache.key + "-" + serialized.name;
  if (cache.inserted[serialized.name] === void 0) {
    var stylesForSSR = "";
    var current2 = serialized;
    do {
      var maybeStyles = cache.insert(
        serialized === current2 ? "." + className : "",
        current2,
        cache.sheet,
        true,
      );
      if (!isBrowser2 && maybeStyles !== void 0) {
        stylesForSSR += maybeStyles;
      }
      current2 = current2.next;
    } while (current2 !== void 0);
    if (!isBrowser2 && stylesForSSR.length !== 0) {
      return stylesForSSR;
    }
  }
};

// node_modules/.pnpm/@emotion+hash@0.9.2/node_modules/@emotion/hash/dist/emotion-hash.esm.js
function murmur2(str) {
  var h = 0;
  var k,
    i = 0,
    len = str.length;
  for (; len >= 4; ++i, len -= 4) {
    k =
      (str.charCodeAt(i) & 255) |
      ((str.charCodeAt(++i) & 255) << 8) |
      ((str.charCodeAt(++i) & 255) << 16) |
      ((str.charCodeAt(++i) & 255) << 24);
    k =
      /* Math.imul(k, m): */
      (k & 65535) * 1540483477 + (((k >>> 16) * 59797) << 16);
    k ^= /* k >>> r: */ k >>> 24;
    h =
      /* Math.imul(k, m): */
      ((k & 65535) * 1540483477 +
        (((k >>> 16) * 59797) << 16)) /* Math.imul(h, m): */ ^
      ((h & 65535) * 1540483477 + (((h >>> 16) * 59797) << 16));
  }
  switch (len) {
    case 3:
      h ^= (str.charCodeAt(i + 2) & 255) << 16;
    case 2:
      h ^= (str.charCodeAt(i + 1) & 255) << 8;
    case 1:
      h ^= str.charCodeAt(i) & 255;
      h =
        /* Math.imul(h, m): */
        (h & 65535) * 1540483477 + (((h >>> 16) * 59797) << 16);
  }
  h ^= h >>> 13;
  h =
    /* Math.imul(h, m): */
    (h & 65535) * 1540483477 + (((h >>> 16) * 59797) << 16);
  return ((h ^ (h >>> 15)) >>> 0).toString(36);
}

// node_modules/.pnpm/@emotion+unitless@0.10.0/node_modules/@emotion/unitless/dist/emotion-unitless.esm.js
var unitlessKeys = {
  animationIterationCount: 1,
  aspectRatio: 1,
  borderImageOutset: 1,
  borderImageSlice: 1,
  borderImageWidth: 1,
  boxFlex: 1,
  boxFlexGroup: 1,
  boxOrdinalGroup: 1,
  columnCount: 1,
  columns: 1,
  flex: 1,
  flexGrow: 1,
  flexPositive: 1,
  flexShrink: 1,
  flexNegative: 1,
  flexOrder: 1,
  gridRow: 1,
  gridRowEnd: 1,
  gridRowSpan: 1,
  gridRowStart: 1,
  gridColumn: 1,
  gridColumnEnd: 1,
  gridColumnSpan: 1,
  gridColumnStart: 1,
  msGridRow: 1,
  msGridRowSpan: 1,
  msGridColumn: 1,
  msGridColumnSpan: 1,
  fontWeight: 1,
  lineHeight: 1,
  opacity: 1,
  order: 1,
  orphans: 1,
  scale: 1,
  tabSize: 1,
  widows: 1,
  zIndex: 1,
  zoom: 1,
  WebkitLineClamp: 1,
  // SVG-related properties
  fillOpacity: 1,
  floodOpacity: 1,
  stopOpacity: 1,
  strokeDasharray: 1,
  strokeDashoffset: 1,
  strokeMiterlimit: 1,
  strokeOpacity: 1,
  strokeWidth: 1,
};

// node_modules/.pnpm/@emotion+serialize@1.3.3/node_modules/@emotion/serialize/dist/emotion-serialize.esm.js
var isDevelopment2 = false;
var hyphenateRegex = /[A-Z]|^ms/g;
var animationRegex = /_EMO_([^_]+?)_([^]*?)_EMO_/g;
var isCustomProperty = function isCustomProperty2(property) {
  return property.charCodeAt(1) === 45;
};
var isProcessableValue = function isProcessableValue2(value) {
  return value != null && typeof value !== "boolean";
};
var processStyleName = /* @__PURE__ */ memoize(function (styleName) {
  return isCustomProperty(styleName)
    ? styleName
    : styleName.replace(hyphenateRegex, "-$&").toLowerCase();
});
var processStyleValue = function processStyleValue2(key, value) {
  switch (key) {
    case "animation":
    case "animationName": {
      if (typeof value === "string") {
        return value.replace(animationRegex, function (match2, p1, p2) {
          cursor = {
            name: p1,
            styles: p2,
            next: cursor,
          };
          return p1;
        });
      }
    }
  }
  if (
    unitlessKeys[key] !== 1 &&
    !isCustomProperty(key) &&
    typeof value === "number" &&
    value !== 0
  ) {
    return value + "px";
  }
  return value;
};
var noComponentSelectorMessage =
  "Component selectors can only be used in conjunction with @emotion/babel-plugin, the swc Emotion plugin, or another Emotion-aware compiler transform.";
function handleInterpolation(mergedProps, registered, interpolation) {
  if (interpolation == null) {
    return "";
  }
  var componentSelector = interpolation;
  if (componentSelector.__emotion_styles !== void 0) {
    return componentSelector;
  }
  switch (typeof interpolation) {
    case "boolean": {
      return "";
    }
    case "object": {
      var keyframes2 = interpolation;
      if (keyframes2.anim === 1) {
        cursor = {
          name: keyframes2.name,
          styles: keyframes2.styles,
          next: cursor,
        };
        return keyframes2.name;
      }
      var serializedStyles = interpolation;
      if (serializedStyles.styles !== void 0) {
        var next2 = serializedStyles.next;
        if (next2 !== void 0) {
          while (next2 !== void 0) {
            cursor = {
              name: next2.name,
              styles: next2.styles,
              next: cursor,
            };
            next2 = next2.next;
          }
        }
        var styles5 = serializedStyles.styles + ";";
        return styles5;
      }
      return createStringFromObject(mergedProps, registered, interpolation);
    }
    case "function": {
      if (mergedProps !== void 0) {
        var previousCursor = cursor;
        var result = interpolation(mergedProps);
        cursor = previousCursor;
        return handleInterpolation(mergedProps, registered, result);
      }
      break;
    }
  }
  var asString = interpolation;
  if (registered == null) {
    return asString;
  }
  var cached = registered[asString];
  return cached !== void 0 ? cached : asString;
}
function createStringFromObject(mergedProps, registered, obj) {
  var string = "";
  if (Array.isArray(obj)) {
    for (var i = 0; i < obj.length; i++) {
      string += handleInterpolation(mergedProps, registered, obj[i]) + ";";
    }
  } else {
    for (var key in obj) {
      var value = obj[key];
      if (typeof value !== "object") {
        var asString = value;
        if (registered != null && registered[asString] !== void 0) {
          string += key + "{" + registered[asString] + "}";
        } else if (isProcessableValue(asString)) {
          string +=
            processStyleName(key) +
            ":" +
            processStyleValue(key, asString) +
            ";";
        }
      } else {
        if (key === "NO_COMPONENT_SELECTOR" && isDevelopment2) {
          throw new Error(noComponentSelectorMessage);
        }
        if (
          Array.isArray(value) &&
          typeof value[0] === "string" &&
          (registered == null || registered[value[0]] === void 0)
        ) {
          for (var _i = 0; _i < value.length; _i++) {
            if (isProcessableValue(value[_i])) {
              string +=
                processStyleName(key) +
                ":" +
                processStyleValue(key, value[_i]) +
                ";";
            }
          }
        } else {
          var interpolated = handleInterpolation(
            mergedProps,
            registered,
            value,
          );
          switch (key) {
            case "animation":
            case "animationName": {
              string += processStyleName(key) + ":" + interpolated + ";";
              break;
            }
            default: {
              string += key + "{" + interpolated + "}";
            }
          }
        }
      }
    }
  }
  return string;
}
var labelPattern = /label:\s*([^\s;{]+)\s*(;|$)/g;
var cursor;
function serializeStyles(args, registered, mergedProps) {
  if (
    args.length === 1 &&
    typeof args[0] === "object" &&
    args[0] !== null &&
    args[0].styles !== void 0
  ) {
    return args[0];
  }
  var stringMode = true;
  var styles5 = "";
  cursor = void 0;
  var strings = args[0];
  if (strings == null || strings.raw === void 0) {
    stringMode = false;
    styles5 += handleInterpolation(mergedProps, registered, strings);
  } else {
    var asTemplateStringsArr = strings;
    styles5 += asTemplateStringsArr[0];
  }
  for (var i = 1; i < args.length; i++) {
    styles5 += handleInterpolation(mergedProps, registered, args[i]);
    if (stringMode) {
      var templateStringsArr = strings;
      styles5 += templateStringsArr[i];
    }
  }
  labelPattern.lastIndex = 0;
  var identifierName = "";
  var match2;
  while ((match2 = labelPattern.exec(styles5)) !== null) {
    identifierName += "-" + match2[1];
  }
  var name = murmur2(styles5) + identifierName;
  return {
    name,
    styles: styles5,
    next: cursor,
  };
}

// node_modules/.pnpm/@emotion+use-insertion-effe_923be80fabfbf3c18f77bc95250214db/node_modules/@emotion/use-insertion-effect-with-fallbacks/dist/emotion-use-insertion-effect-with-fallbacks.esm.js
var React = __toESM(require_react());
var isBrowser3 = typeof document !== "undefined";
var syncFallback = function syncFallback2(create) {
  return create();
};
var useInsertionEffect2 = React["useInsertionEffect"]
  ? React["useInsertionEffect"]
  : false;
var useInsertionEffectAlwaysWithSyncFallback = !isBrowser3
  ? syncFallback
  : useInsertionEffect2 || syncFallback;
var useInsertionEffectWithLayoutFallback =
  useInsertionEffect2 || React.useLayoutEffect;

// node_modules/.pnpm/@emotion+react@11.14.0_@types+react@19.2.18_react@19.2.8/node_modules/@emotion/react/dist/emotion-element-d59e098f.esm.js
var isDevelopment3 = false;
var isBrowser4 = typeof document !== "undefined";
var EmotionCacheContext = /* @__PURE__ */ React2.createContext(
  // we're doing this to avoid preconstruct's dead code elimination in this one case
  // because this module is primarily intended for the browser and node
  // but it's also required in react native and similar environments sometimes
  // and we could have a special build just for that
  // but this is much easier and the native packages
  // might use a different theme context in the future anyway
  typeof HTMLElement !== "undefined"
    ? /* @__PURE__ */ createCache({
        key: "css",
      })
    : null,
);
var CacheProvider = EmotionCacheContext.Provider;
var withEmotionCache = function withEmotionCache2(func) {
  return /* @__PURE__ */ (0, import_react.forwardRef)(function (props, ref) {
    var cache = (0, import_react.useContext)(EmotionCacheContext);
    return func(props, cache, ref);
  });
};
if (!isBrowser4) {
  withEmotionCache = function withEmotionCache3(func) {
    return function (props) {
      var cache = (0, import_react.useContext)(EmotionCacheContext);
      if (cache === null) {
        cache = createCache({
          key: "css",
        });
        return /* @__PURE__ */ React2.createElement(
          EmotionCacheContext.Provider,
          {
            value: cache,
          },
          func(props, cache),
        );
      } else {
        return func(props, cache);
      }
    };
  };
}
var ThemeContext = /* @__PURE__ */ React2.createContext({});
var hasOwn = {}.hasOwnProperty;
var typePropName = "__EMOTION_TYPE_PLEASE_DO_NOT_USE__";
var createEmotionProps = function createEmotionProps2(type, props) {
  var newProps = {};
  for (var _key in props) {
    if (hasOwn.call(props, _key)) {
      newProps[_key] = props[_key];
    }
  }
  newProps[typePropName] = type;
  return newProps;
};
var Insertion = function Insertion2(_ref) {
  var cache = _ref.cache,
    serialized = _ref.serialized,
    isStringTag2 = _ref.isStringTag;
  registerStyles(cache, serialized, isStringTag2);
  var rules = useInsertionEffectAlwaysWithSyncFallback(function () {
    return insertStyles(cache, serialized, isStringTag2);
  });
  if (!isBrowser4 && rules !== void 0) {
    var _ref2;
    var serializedNames = serialized.name;
    var next2 = serialized.next;
    while (next2 !== void 0) {
      serializedNames += " " + next2.name;
      next2 = next2.next;
    }
    return /* @__PURE__ */ React2.createElement(
      "style",
      ((_ref2 = {}),
      (_ref2["data-emotion"] = cache.key + " " + serializedNames),
      (_ref2.dangerouslySetInnerHTML = {
        __html: rules,
      }),
      (_ref2.nonce = cache.sheet.nonce),
      _ref2),
    );
  }
  return null;
};
var Emotion = /* @__PURE__ */ withEmotionCache(function (props, cache, ref) {
  var cssProp = props.css;
  if (typeof cssProp === "string" && cache.registered[cssProp] !== void 0) {
    cssProp = cache.registered[cssProp];
  }
  var WrappedComponent = props[typePropName];
  var registeredStyles = [cssProp];
  var className = "";
  if (typeof props.className === "string") {
    className = getRegisteredStyles(
      cache.registered,
      registeredStyles,
      props.className,
    );
  } else if (props.className != null) {
    className = props.className + " ";
  }
  var serialized = serializeStyles(
    registeredStyles,
    void 0,
    React2.useContext(ThemeContext),
  );
  className += cache.key + "-" + serialized.name;
  var newProps = {};
  for (var _key2 in props) {
    if (
      hasOwn.call(props, _key2) &&
      _key2 !== "css" &&
      _key2 !== typePropName &&
      !isDevelopment3
    ) {
      newProps[_key2] = props[_key2];
    }
  }
  newProps.className = className;
  if (ref) {
    newProps.ref = ref;
  }
  return /* @__PURE__ */ React2.createElement(
    React2.Fragment,
    null,
    /* @__PURE__ */ React2.createElement(Insertion, {
      cache,
      serialized,
      isStringTag: typeof WrappedComponent === "string",
    }),
    /* @__PURE__ */ React2.createElement(WrappedComponent, newProps),
  );
});
var Emotion$1 = Emotion;

// node_modules/.pnpm/@emotion+react@11.14.0_@types+react@19.2.18_react@19.2.8/node_modules/@emotion/react/dist/emotion-react.esm.js
var React3 = __toESM(require_react());
var import_extends2 = __toESM(require_extends());
var import_hoist_non_react_statics = __toESM(
  require_hoist_non_react_statics_cjs(),
);
var jsx = function jsx2(type, props) {
  var args = arguments;
  if (props == null || !hasOwn.call(props, "css")) {
    return React3.createElement.apply(void 0, args);
  }
  var argsLength = args.length;
  var createElementArgArray = new Array(argsLength);
  createElementArgArray[0] = Emotion$1;
  createElementArgArray[1] = createEmotionProps(type, props);
  for (var i = 2; i < argsLength; i++) {
    createElementArgArray[i] = args[i];
  }
  return React3.createElement.apply(null, createElementArgArray);
};
(function (_jsx80) {
  var JSX;
  /* @__PURE__ */ (function (_JSX) {})(
    JSX || (JSX = _jsx80.JSX || (_jsx80.JSX = {})),
  );
})(jsx || (jsx = {}));
var Global = /* @__PURE__ */ withEmotionCache(function (props, cache) {
  var styles5 = props.styles;
  var serialized = serializeStyles(
    [styles5],
    void 0,
    React3.useContext(ThemeContext),
  );
  if (!isBrowser4) {
    var _ref;
    var serializedNames = serialized.name;
    var serializedStyles = serialized.styles;
    var next2 = serialized.next;
    while (next2 !== void 0) {
      serializedNames += " " + next2.name;
      serializedStyles += next2.styles;
      next2 = next2.next;
    }
    var shouldCache = cache.compat === true;
    var rules = cache.insert(
      "",
      {
        name: serializedNames,
        styles: serializedStyles,
      },
      cache.sheet,
      shouldCache,
    );
    if (shouldCache) {
      return null;
    }
    return /* @__PURE__ */ React3.createElement(
      "style",
      ((_ref = {}),
      (_ref["data-emotion"] = cache.key + "-global " + serializedNames),
      (_ref.dangerouslySetInnerHTML = {
        __html: rules,
      }),
      (_ref.nonce = cache.sheet.nonce),
      _ref),
    );
  }
  var sheetRef = React3.useRef();
  useInsertionEffectWithLayoutFallback(
    function () {
      var key = cache.key + "-global";
      var sheet = new cache.sheet.constructor({
        key,
        nonce: cache.sheet.nonce,
        container: cache.sheet.container,
        speedy: cache.sheet.isSpeedy,
      });
      var rehydrating = false;
      var node2 = document.querySelector(
        'style[data-emotion="' + key + " " + serialized.name + '"]',
      );
      if (cache.sheet.tags.length) {
        sheet.before = cache.sheet.tags[0];
      }
      if (node2 !== null) {
        rehydrating = true;
        node2.setAttribute("data-emotion", key);
        sheet.hydrate([node2]);
      }
      sheetRef.current = [sheet, rehydrating];
      return function () {
        sheet.flush();
      };
    },
    [cache],
  );
  useInsertionEffectWithLayoutFallback(
    function () {
      var sheetRefCurrent = sheetRef.current;
      var sheet = sheetRefCurrent[0],
        rehydrating = sheetRefCurrent[1];
      if (rehydrating) {
        sheetRefCurrent[1] = false;
        return;
      }
      if (serialized.next !== void 0) {
        insertStyles(cache, serialized.next, true);
      }
      if (sheet.tags.length) {
        var element = sheet.tags[sheet.tags.length - 1].nextElementSibling;
        sheet.before = element;
        sheet.flush();
      }
      cache.insert("", serialized, sheet, false);
    },
    [cache, serialized.name],
  );
  return null;
});
function css() {
  for (
    var _len = arguments.length, args = new Array(_len), _key = 0;
    _key < _len;
    _key++
  ) {
    args[_key] = arguments[_key];
  }
  return serializeStyles(args);
}
function keyframes() {
  var insertable = css.apply(void 0, arguments);
  var name = "animation-" + insertable.name;
  return {
    name,
    styles: "@keyframes " + name + "{" + insertable.styles + "}",
    anim: 1,
    toString: function toString() {
      return "_EMO_" + this.name + "_" + this.styles + "_EMO_";
    },
  };
}

// node_modules/.pnpm/@emotion+styled@11.14.1_@em_6cb5238edf72fb592a14bdb47b493a9a/node_modules/@emotion/styled/base/dist/emotion-styled-base.esm.js
var React4 = __toESM(require_react());

// node_modules/.pnpm/@emotion+is-prop-valid@1.4.0/node_modules/@emotion/is-prop-valid/dist/emotion-is-prop-valid.esm.js
var reactPropsRegex =
  /^((children|dangerouslySetInnerHTML|key|ref|autoFocus|defaultValue|defaultChecked|innerHTML|suppressContentEditableWarning|suppressHydrationWarning|valueLink|abbr|accept|acceptCharset|accessKey|action|allow|allowUserMedia|allowPaymentRequest|allowFullScreen|allowTransparency|alt|async|autoComplete|autoPlay|capture|cellPadding|cellSpacing|challenge|charSet|checked|cite|classID|className|cols|colSpan|content|contentEditable|contextMenu|controls|controlsList|coords|crossOrigin|data|dateTime|decoding|default|defer|dir|disabled|disablePictureInPicture|disableRemotePlayback|download|draggable|encType|enterKeyHint|fetchpriority|fetchPriority|form|formAction|formEncType|formMethod|formNoValidate|formTarget|frameBorder|headers|height|hidden|high|href|hrefLang|htmlFor|httpEquiv|id|inputMode|integrity|is|keyParams|keyType|kind|label|lang|list|loading|loop|low|marginHeight|marginWidth|max|maxLength|media|mediaGroup|method|min|minLength|multiple|muted|name|nonce|noValidate|open|optimum|pattern|placeholder|playsInline|popover|popoverTarget|popoverTargetAction|poster|preload|profile|radioGroup|readOnly|referrerPolicy|rel|required|reversed|role|rows|rowSpan|sandbox|scope|scoped|scrolling|seamless|selected|shape|size|sizes|slot|span|spellCheck|src|srcDoc|srcLang|srcSet|start|step|style|summary|tabIndex|target|title|translate|type|useMap|value|width|wmode|wrap|about|datatype|inlist|prefix|property|resource|typeof|vocab|autoCapitalize|autoCorrect|autoSave|color|incremental|fallback|inert|itemProp|itemScope|itemType|itemID|itemRef|on|option|results|security|unselectable|accentHeight|accumulate|additive|alignmentBaseline|allowReorder|alphabetic|amplitude|arabicForm|ascent|attributeName|attributeType|autoReverse|azimuth|baseFrequency|baselineShift|baseProfile|bbox|begin|bias|by|calcMode|capHeight|clip|clipPathUnits|clipPath|clipRule|colorInterpolation|colorInterpolationFilters|colorProfile|colorRendering|contentScriptType|contentStyleType|cursor|cx|cy|d|decelerate|descent|diffuseConstant|direction|display|divisor|dominantBaseline|dur|dx|dy|edgeMode|elevation|enableBackground|end|exponent|externalResourcesRequired|fill|fillOpacity|fillRule|filter|filterRes|filterUnits|floodColor|floodOpacity|focusable|fontFamily|fontSize|fontSizeAdjust|fontStretch|fontStyle|fontVariant|fontWeight|format|from|fr|fx|fy|g1|g2|glyphName|glyphOrientationHorizontal|glyphOrientationVertical|glyphRef|gradientTransform|gradientUnits|hanging|horizAdvX|horizOriginX|ideographic|imageRendering|in|in2|intercept|k|k1|k2|k3|k4|kernelMatrix|kernelUnitLength|kerning|keyPoints|keySplines|keyTimes|lengthAdjust|letterSpacing|lightingColor|limitingConeAngle|local|markerEnd|markerMid|markerStart|markerHeight|markerUnits|markerWidth|mask|maskContentUnits|maskUnits|mathematical|mode|numOctaves|offset|opacity|operator|order|orient|orientation|origin|overflow|overlinePosition|overlineThickness|panose1|paintOrder|pathLength|patternContentUnits|patternTransform|patternUnits|pointerEvents|points|pointsAtX|pointsAtY|pointsAtZ|preserveAlpha|preserveAspectRatio|primitiveUnits|r|radius|refX|refY|renderingIntent|repeatCount|repeatDur|requiredExtensions|requiredFeatures|restart|result|rotate|rx|ry|scale|seed|shapeRendering|slope|spacing|specularConstant|specularExponent|speed|spreadMethod|startOffset|stdDeviation|stemh|stemv|stitchTiles|stopColor|stopOpacity|strikethroughPosition|strikethroughThickness|string|stroke|strokeDasharray|strokeDashoffset|strokeLinecap|strokeLinejoin|strokeMiterlimit|strokeOpacity|strokeWidth|surfaceScale|systemLanguage|tableValues|targetX|targetY|textAnchor|textDecoration|textRendering|textLength|to|transform|u1|u2|underlinePosition|underlineThickness|unicode|unicodeBidi|unicodeRange|unitsPerEm|vAlphabetic|vHanging|vIdeographic|vMathematical|values|vectorEffect|version|vertAdvY|vertOriginX|vertOriginY|viewBox|viewTarget|visibility|widths|wordSpacing|writingMode|x|xHeight|x1|x2|xChannelSelector|xlinkActuate|xlinkArcrole|xlinkHref|xlinkRole|xlinkShow|xlinkTitle|xlinkType|xmlBase|xmlns|xmlnsXlink|xmlLang|xmlSpace|y|y1|y2|yChannelSelector|z|zoomAndPan|for|class|autofocus)|(([Dd][Aa][Tt][Aa]|[Aa][Rr][Ii][Aa]|x)-.*))$/;
var isPropValid = /* @__PURE__ */ memoize(
  function (prop) {
    return (
      reactPropsRegex.test(prop) ||
      (prop.charCodeAt(0) === 111 &&
        prop.charCodeAt(1) === 110 &&
        prop.charCodeAt(2) < 91)
    );
  },
  /* Z+1 */
);

// node_modules/.pnpm/@emotion+styled@11.14.1_@em_6cb5238edf72fb592a14bdb47b493a9a/node_modules/@emotion/styled/base/dist/emotion-styled-base.esm.js
var isBrowser5 = typeof document !== "undefined";
var isDevelopment4 = false;
var testOmitPropsOnStringTag = isPropValid;
var testOmitPropsOnComponent = function testOmitPropsOnComponent2(key) {
  return key !== "theme";
};
var getDefaultShouldForwardProp = function getDefaultShouldForwardProp2(tag) {
  return typeof tag === "string" && // 96 is one less than the char code
    // for "a" so this is checking that
    // it's a lowercase character
    tag.charCodeAt(0) > 96
    ? testOmitPropsOnStringTag
    : testOmitPropsOnComponent;
};
var composeShouldForwardProps = function composeShouldForwardProps2(
  tag,
  options,
  isReal,
) {
  var shouldForwardProp2;
  if (options) {
    var optionsShouldForwardProp = options.shouldForwardProp;
    shouldForwardProp2 =
      tag.__emotion_forwardProp && optionsShouldForwardProp
        ? function (propName) {
            return (
              tag.__emotion_forwardProp(propName) &&
              optionsShouldForwardProp(propName)
            );
          }
        : optionsShouldForwardProp;
  }
  if (typeof shouldForwardProp2 !== "function" && isReal) {
    shouldForwardProp2 = tag.__emotion_forwardProp;
  }
  return shouldForwardProp2;
};
var Insertion3 = function Insertion4(_ref) {
  var cache = _ref.cache,
    serialized = _ref.serialized,
    isStringTag2 = _ref.isStringTag;
  registerStyles(cache, serialized, isStringTag2);
  var rules = useInsertionEffectAlwaysWithSyncFallback(function () {
    return insertStyles(cache, serialized, isStringTag2);
  });
  if (!isBrowser5 && rules !== void 0) {
    var _ref2;
    var serializedNames = serialized.name;
    var next2 = serialized.next;
    while (next2 !== void 0) {
      serializedNames += " " + next2.name;
      next2 = next2.next;
    }
    return /* @__PURE__ */ React4.createElement(
      "style",
      ((_ref2 = {}),
      (_ref2["data-emotion"] = cache.key + " " + serializedNames),
      (_ref2.dangerouslySetInnerHTML = {
        __html: rules,
      }),
      (_ref2.nonce = cache.sheet.nonce),
      _ref2),
    );
  }
  return null;
};
var createStyled = function createStyled2(tag, options) {
  var isReal = tag.__emotion_real === tag;
  var baseTag = (isReal && tag.__emotion_base) || tag;
  var identifierName;
  var targetClassName;
  if (options !== void 0) {
    identifierName = options.label;
    targetClassName = options.target;
  }
  var shouldForwardProp2 = composeShouldForwardProps(tag, options, isReal);
  var defaultShouldForwardProp =
    shouldForwardProp2 || getDefaultShouldForwardProp(baseTag);
  var shouldUseAs = !defaultShouldForwardProp("as");
  return function () {
    var args = arguments;
    var styles5 =
      isReal && tag.__emotion_styles !== void 0
        ? tag.__emotion_styles.slice(0)
        : [];
    if (identifierName !== void 0) {
      styles5.push("label:" + identifierName + ";");
    }
    if (args[0] == null || args[0].raw === void 0) {
      styles5.push.apply(styles5, args);
    } else {
      var templateStringsArr = args[0];
      styles5.push(templateStringsArr[0]);
      var len = args.length;
      var i = 1;
      for (; i < len; i++) {
        styles5.push(args[i], templateStringsArr[i]);
      }
    }
    var Styled = withEmotionCache(function (props, cache, ref) {
      var FinalTag = (shouldUseAs && props.as) || baseTag;
      var className = "";
      var classInterpolations = [];
      var mergedProps = props;
      if (props.theme == null) {
        mergedProps = {};
        for (var key in props) {
          mergedProps[key] = props[key];
        }
        mergedProps.theme = React4.useContext(ThemeContext);
      }
      if (typeof props.className === "string") {
        className = getRegisteredStyles(
          cache.registered,
          classInterpolations,
          props.className,
        );
      } else if (props.className != null) {
        className = props.className + " ";
      }
      var serialized = serializeStyles(
        styles5.concat(classInterpolations),
        cache.registered,
        mergedProps,
      );
      className += cache.key + "-" + serialized.name;
      if (targetClassName !== void 0) {
        className += " " + targetClassName;
      }
      var finalShouldForwardProp =
        shouldUseAs && shouldForwardProp2 === void 0
          ? getDefaultShouldForwardProp(FinalTag)
          : defaultShouldForwardProp;
      var newProps = {};
      for (var _key in props) {
        if (shouldUseAs && _key === "as") continue;
        if (finalShouldForwardProp(_key)) {
          newProps[_key] = props[_key];
        }
      }
      newProps.className = className;
      if (ref) {
        newProps.ref = ref;
      }
      return /* @__PURE__ */ React4.createElement(
        React4.Fragment,
        null,
        /* @__PURE__ */ React4.createElement(Insertion3, {
          cache,
          serialized,
          isStringTag: typeof FinalTag === "string",
        }),
        /* @__PURE__ */ React4.createElement(FinalTag, newProps),
      );
    });
    Styled.displayName =
      identifierName !== void 0
        ? identifierName
        : "Styled(" +
          (typeof baseTag === "string"
            ? baseTag
            : baseTag.displayName || baseTag.name || "Component") +
          ")";
    Styled.defaultProps = tag.defaultProps;
    Styled.__emotion_real = Styled;
    Styled.__emotion_base = baseTag;
    Styled.__emotion_styles = styles5;
    Styled.__emotion_forwardProp = shouldForwardProp2;
    Object.defineProperty(Styled, "toString", {
      value: function value() {
        if (targetClassName === void 0 && isDevelopment4) {
          return "NO_COMPONENT_SELECTOR";
        }
        return "." + targetClassName;
      },
    });
    Styled.withComponent = function (nextTag, nextOptions) {
      var newStyled = createStyled2(
        nextTag,
        _extends({}, options, nextOptions, {
          shouldForwardProp: composeShouldForwardProps(
            Styled,
            nextOptions,
            true,
          ),
        }),
      );
      return newStyled.apply(void 0, styles5);
    };
    return Styled;
  };
};

// node_modules/.pnpm/@emotion+styled@11.14.1_@em_6cb5238edf72fb592a14bdb47b493a9a/node_modules/@emotion/styled/dist/emotion-styled.esm.js
var import_extends4 = __toESM(require_extends());
var import_react3 = __toESM(require_react());
var tags = [
  "a",
  "abbr",
  "address",
  "area",
  "article",
  "aside",
  "audio",
  "b",
  "base",
  "bdi",
  "bdo",
  "big",
  "blockquote",
  "body",
  "br",
  "button",
  "canvas",
  "caption",
  "cite",
  "code",
  "col",
  "colgroup",
  "data",
  "datalist",
  "dd",
  "del",
  "details",
  "dfn",
  "dialog",
  "div",
  "dl",
  "dt",
  "em",
  "embed",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "head",
  "header",
  "hgroup",
  "hr",
  "html",
  "i",
  "iframe",
  "img",
  "input",
  "ins",
  "kbd",
  "keygen",
  "label",
  "legend",
  "li",
  "link",
  "main",
  "map",
  "mark",
  "marquee",
  "menu",
  "menuitem",
  "meta",
  "meter",
  "nav",
  "noscript",
  "object",
  "ol",
  "optgroup",
  "option",
  "output",
  "p",
  "param",
  "picture",
  "pre",
  "progress",
  "q",
  "rp",
  "rt",
  "ruby",
  "s",
  "samp",
  "script",
  "section",
  "select",
  "small",
  "source",
  "span",
  "strong",
  "style",
  "sub",
  "summary",
  "sup",
  "table",
  "tbody",
  "td",
  "textarea",
  "tfoot",
  "th",
  "thead",
  "time",
  "title",
  "tr",
  "track",
  "u",
  "ul",
  "var",
  "video",
  "wbr",
  // SVG
  "circle",
  "clipPath",
  "defs",
  "ellipse",
  "foreignObject",
  "g",
  "image",
  "line",
  "linearGradient",
  "mask",
  "path",
  "pattern",
  "polygon",
  "polyline",
  "radialGradient",
  "rect",
  "stop",
  "svg",
  "text",
  "tspan",
];
var styled = createStyled.bind(null);
tags.forEach(function (tagName) {
  styled[tagName] = styled(tagName);
});

// node_modules/.pnpm/@mui+styled-engine@7.3.10_@_e38817b749ae7a6aa579d6a9c06d0c3e/node_modules/@mui/styled-engine/esm/GlobalStyles/GlobalStyles.js
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
function isEmpty(obj) {
  return obj === void 0 || obj === null || Object.keys(obj).length === 0;
}
function GlobalStyles(props) {
  const { styles: styles5, defaultTheme: defaultTheme5 = {} } = props;
  const globalStyles =
    typeof styles5 === "function"
      ? (themeInput) =>
          styles5(isEmpty(themeInput) ? defaultTheme5 : themeInput)
      : styles5;
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Global, {
    styles: globalStyles,
  });
}
false
  ? (GlobalStyles.propTypes = {
      defaultTheme: import_prop_types.default.object,
      styles: import_prop_types.default.oneOfType([
        import_prop_types.default.array,
        import_prop_types.default.string,
        import_prop_types.default.object,
        import_prop_types.default.func,
      ]),
    })
  : void 0;

// node_modules/.pnpm/@mui+styled-engine@7.3.10_@_e38817b749ae7a6aa579d6a9c06d0c3e/node_modules/@mui/styled-engine/esm/index.js
function styled2(tag, options) {
  const stylesFactory = styled(tag, options);
  if (false) {
    return (...styles5) => {
      const component = typeof tag === "string" ? `"${tag}"` : "component";
      if (styles5.length === 0) {
        console.error(
          [
            `MUI: Seems like you called \`styled(${component})()\` without a \`style\` argument.`,
            'You must provide a `styles` argument: `styled("div")(styleYouForgotToPass)`.',
          ].join("\n"),
        );
      } else if (styles5.some((style4) => style4 === void 0)) {
        console.error(
          `MUI: the styled(${component})(...args) API requires all its args to be defined.`,
        );
      }
      return stylesFactory(...styles5);
    };
  }
  return stylesFactory;
}
function internal_mutateStyles(tag, processor) {
  if (Array.isArray(tag.__emotion_styles)) {
    tag.__emotion_styles = processor(tag.__emotion_styles);
  }
}
var wrapper = [];
function internal_serializeStyles(styles5) {
  wrapper[0] = styles5;
  return serializeStyles(wrapper);
}

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/deepmerge/deepmerge.js
var React5 = __toESM(require_react(), 1);
var import_react_is = __toESM(require_react_is2(), 1);
function isPlainObject(item) {
  if (typeof item !== "object" || item === null) {
    return false;
  }
  const prototype = Object.getPrototypeOf(item);
  return (
    (prototype === null ||
      prototype === Object.prototype ||
      Object.getPrototypeOf(prototype) === null) &&
    !(Symbol.toStringTag in item) &&
    !(Symbol.iterator in item)
  );
}
function deepClone(source) {
  if (
    /* @__PURE__ */ React5.isValidElement(source) ||
    (0, import_react_is.isValidElementType)(source) ||
    !isPlainObject(source)
  ) {
    return source;
  }
  const output = {};
  Object.keys(source).forEach((key) => {
    output[key] = deepClone(source[key]);
  });
  return output;
}
function deepmerge(
  target,
  source,
  options = {
    clone: true,
  },
) {
  const output = options.clone
    ? {
        ...target,
      }
    : target;
  if (isPlainObject(target) && isPlainObject(source)) {
    Object.keys(source).forEach((key) => {
      if (
        /* @__PURE__ */ React5.isValidElement(source[key]) ||
        (0, import_react_is.isValidElementType)(source[key])
      ) {
        output[key] = source[key];
      } else if (
        isPlainObject(source[key]) && // Avoid prototype pollution
        Object.prototype.hasOwnProperty.call(target, key) &&
        isPlainObject(target[key])
      ) {
        output[key] = deepmerge(target[key], source[key], options);
      } else if (options.clone) {
        output[key] = isPlainObject(source[key])
          ? deepClone(source[key])
          : source[key];
      } else {
        output[key] = source[key];
      }
    });
  }
  return output;
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/createBreakpoints/createBreakpoints.js
var sortBreakpointsValues = (values3) => {
  const breakpointsAsArray =
    Object.keys(values3).map((key) => ({
      key,
      val: values3[key],
    })) || [];
  breakpointsAsArray.sort(
    (breakpoint1, breakpoint2) => breakpoint1.val - breakpoint2.val,
  );
  return breakpointsAsArray.reduce((acc, obj) => {
    return {
      ...acc,
      [obj.key]: obj.val,
    };
  }, {});
};
function createBreakpoints(breakpoints) {
  const {
    // The breakpoint **start** at this value.
    // For instance with the first breakpoint xs: [xs, sm).
    values: values3 = {
      xs: 0,
      // phone
      sm: 600,
      // tablet
      md: 900,
      // small laptop
      lg: 1200,
      // desktop
      xl: 1536,
      // large screen
    },
    unit = "px",
    step = 5,
    ...other
  } = breakpoints;
  const sortedValues = sortBreakpointsValues(values3);
  const keys = Object.keys(sortedValues);
  function up(key) {
    const value = typeof values3[key] === "number" ? values3[key] : key;
    return `@media (min-width:${value}${unit})`;
  }
  function down(key) {
    const value = typeof values3[key] === "number" ? values3[key] : key;
    return `@media (max-width:${value - step / 100}${unit})`;
  }
  function between(start2, end2) {
    const endIndex = keys.indexOf(end2);
    return `@media (min-width:${typeof values3[start2] === "number" ? values3[start2] : start2}${unit}) and (max-width:${(endIndex !== -1 && typeof values3[keys[endIndex]] === "number" ? values3[keys[endIndex]] : end2) - step / 100}${unit})`;
  }
  function only(key) {
    if (keys.indexOf(key) + 1 < keys.length) {
      return between(key, keys[keys.indexOf(key) + 1]);
    }
    return up(key);
  }
  function not(key) {
    const keyIndex = keys.indexOf(key);
    if (keyIndex === 0) {
      return up(keys[1]);
    }
    if (keyIndex === keys.length - 1) {
      return down(keys[keyIndex]);
    }
    return between(key, keys[keys.indexOf(key) + 1]).replace(
      "@media",
      "@media not all and",
    );
  }
  return {
    keys,
    values: sortedValues,
    up,
    down,
    between,
    only,
    not,
    unit,
    ...other,
  };
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/cssContainerQueries/cssContainerQueries.js
function sortContainerQueries(theme2, css2) {
  if (!theme2.containerQueries) {
    return css2;
  }
  const sorted = Object.keys(css2)
    .filter((key) => key.startsWith("@container"))
    .sort((a, b) => {
      const regex = /min-width:\s*([0-9.]+)/;
      return +(a.match(regex)?.[1] || 0) - +(b.match(regex)?.[1] || 0);
    });
  if (!sorted.length) {
    return css2;
  }
  return sorted.reduce(
    (acc, key) => {
      const value = css2[key];
      delete acc[key];
      acc[key] = value;
      return acc;
    },
    {
      ...css2,
    },
  );
}
function isCqShorthand(breakpointKeys, value) {
  return (
    value === "@" ||
    (value.startsWith("@") &&
      (breakpointKeys.some((key) => value.startsWith(`@${key}`)) ||
        !!value.match(/^@\d/)))
  );
}
function getContainerQuery(theme2, shorthand) {
  const matches = shorthand.match(/^@([^/]+)?\/?(.+)?$/);
  if (!matches) {
    if (false) {
      throw (
        /* minify-error */
        new Error(`MUI: The provided shorthand ${`(${shorthand})`} is invalid. The format should be \`@<breakpoint | number>\` or \`@<breakpoint | number>/<container>\`.
For example, \`@sm\` or \`@600\` or \`@40rem/sidebar\`.`)
      );
    }
    return null;
  }
  const [, containerQuery, containerName] = matches;
  const value = Number.isNaN(+containerQuery)
    ? containerQuery || 0
    : +containerQuery;
  return theme2.containerQueries(containerName).up(value);
}
function cssContainerQueries(themeInput) {
  const toContainerQuery = (mediaQuery, name) =>
    mediaQuery.replace("@media", name ? `@container ${name}` : "@container");
  function attachCq(node3, name) {
    node3.up = (...args) =>
      toContainerQuery(themeInput.breakpoints.up(...args), name);
    node3.down = (...args) =>
      toContainerQuery(themeInput.breakpoints.down(...args), name);
    node3.between = (...args) =>
      toContainerQuery(themeInput.breakpoints.between(...args), name);
    node3.only = (...args) =>
      toContainerQuery(themeInput.breakpoints.only(...args), name);
    node3.not = (...args) => {
      const result = toContainerQuery(
        themeInput.breakpoints.not(...args),
        name,
      );
      if (result.includes("not all and")) {
        return result
          .replace("not all and ", "")
          .replace("min-width:", "width<")
          .replace("max-width:", "width>")
          .replace("and", "or");
      }
      return result;
    };
  }
  const node2 = {};
  const containerQueries = (name) => {
    attachCq(node2, name);
    return node2;
  };
  attachCq(containerQueries);
  return {
    ...themeInput,
    containerQueries,
  };
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/createTheme/shape.js
var shape = {
  borderRadius: 4,
};
var shape_default = shape;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/merge/merge.js
function merge(acc, item) {
  if (!item) {
    return acc;
  }
  return deepmerge(acc, item, {
    clone: false,
    // No need to clone deep, it's way faster.
  });
}
var merge_default = merge;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/breakpoints/breakpoints.js
var values = {
  xs: 0,
  // phone
  sm: 600,
  // tablet
  md: 900,
  // small laptop
  lg: 1200,
  // desktop
  xl: 1536,
  // large screen
};
var defaultBreakpoints = {
  // Sorted ASC by size. That's important.
  // It can't be configured as it's used statically for propTypes.
  keys: ["xs", "sm", "md", "lg", "xl"],
  up: (key) => `@media (min-width:${values[key]}px)`,
};
var defaultContainerQueries = {
  containerQueries: (containerName) => ({
    up: (key) => {
      let result = typeof key === "number" ? key : values[key] || key;
      if (typeof result === "number") {
        result = `${result}px`;
      }
      return containerName
        ? `@container ${containerName} (min-width:${result})`
        : `@container (min-width:${result})`;
    },
  }),
};
function handleBreakpoints(props, propValue, styleFromPropValue) {
  const theme2 = props.theme || {};
  if (Array.isArray(propValue)) {
    const themeBreakpoints = theme2.breakpoints || defaultBreakpoints;
    return propValue.reduce((acc, item, index) => {
      acc[themeBreakpoints.up(themeBreakpoints.keys[index])] =
        styleFromPropValue(propValue[index]);
      return acc;
    }, {});
  }
  if (typeof propValue === "object") {
    const themeBreakpoints = theme2.breakpoints || defaultBreakpoints;
    return Object.keys(propValue).reduce((acc, breakpoint) => {
      if (isCqShorthand(themeBreakpoints.keys, breakpoint)) {
        const containerKey = getContainerQuery(
          theme2.containerQueries ? theme2 : defaultContainerQueries,
          breakpoint,
        );
        if (containerKey) {
          acc[containerKey] = styleFromPropValue(
            propValue[breakpoint],
            breakpoint,
          );
        }
      } else if (
        Object.keys(themeBreakpoints.values || values).includes(breakpoint)
      ) {
        const mediaKey = themeBreakpoints.up(breakpoint);
        acc[mediaKey] = styleFromPropValue(propValue[breakpoint], breakpoint);
      } else {
        const cssKey = breakpoint;
        acc[cssKey] = propValue[cssKey];
      }
      return acc;
    }, {});
  }
  const output = styleFromPropValue(propValue);
  return output;
}
function createEmptyBreakpointObject(breakpointsInput = {}) {
  const breakpointsInOrder = breakpointsInput.keys?.reduce((acc, key) => {
    const breakpointStyleKey = breakpointsInput.up(key);
    acc[breakpointStyleKey] = {};
    return acc;
  }, {});
  return breakpointsInOrder || {};
}
function removeUnusedBreakpoints(breakpointKeys, style4) {
  return breakpointKeys.reduce((acc, key) => {
    const breakpointOutput = acc[key];
    const isBreakpointUnused =
      !breakpointOutput || Object.keys(breakpointOutput).length === 0;
    if (isBreakpointUnused) {
      delete acc[key];
    }
    return acc;
  }, style4);
}
function mergeBreakpointsInOrder(breakpointsInput, ...styles5) {
  const emptyBreakpoints = createEmptyBreakpointObject(breakpointsInput);
  const mergedOutput = [emptyBreakpoints, ...styles5].reduce(
    (prev2, next2) => deepmerge(prev2, next2),
    {},
  );
  return removeUnusedBreakpoints(Object.keys(emptyBreakpoints), mergedOutput);
}
function computeBreakpointsBase(breakpointValues, themeBreakpoints) {
  if (typeof breakpointValues !== "object") {
    return {};
  }
  const base = {};
  const breakpointsKeys = Object.keys(themeBreakpoints);
  if (Array.isArray(breakpointValues)) {
    breakpointsKeys.forEach((breakpoint, i) => {
      if (i < breakpointValues.length) {
        base[breakpoint] = true;
      }
    });
  } else {
    breakpointsKeys.forEach((breakpoint) => {
      if (breakpointValues[breakpoint] != null) {
        base[breakpoint] = true;
      }
    });
  }
  return base;
}
function resolveBreakpointValues({
  values: breakpointValues,
  breakpoints: themeBreakpoints,
  base: customBase,
}) {
  const base =
    customBase || computeBreakpointsBase(breakpointValues, themeBreakpoints);
  const keys = Object.keys(base);
  if (keys.length === 0) {
    return breakpointValues;
  }
  let previous;
  return keys.reduce((acc, breakpoint, i) => {
    if (Array.isArray(breakpointValues)) {
      acc[breakpoint] =
        breakpointValues[i] != null
          ? breakpointValues[i]
          : breakpointValues[previous];
      previous = i;
    } else if (typeof breakpointValues === "object") {
      acc[breakpoint] =
        breakpointValues[breakpoint] != null
          ? breakpointValues[breakpoint]
          : breakpointValues[previous];
      previous = breakpoint;
    } else {
      acc[breakpoint] = breakpointValues;
    }
    return acc;
  }, {});
}

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/capitalize/capitalize.js
function capitalize(string) {
  if (typeof string !== "string") {
    throw new Error(
      false
        ? "MUI: `capitalize(string)` expects a string argument."
        : formatMuiErrorMessage(7),
    );
  }
  return string.charAt(0).toUpperCase() + string.slice(1);
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/style/style.js
function getPath(obj, path3, checkVars = true) {
  if (!path3 || typeof path3 !== "string") {
    return null;
  }
  if (obj && obj.vars && checkVars) {
    const val = `vars.${path3}`
      .split(".")
      .reduce((acc, item) => (acc && acc[item] ? acc[item] : null), obj);
    if (val != null) {
      return val;
    }
  }
  return path3.split(".").reduce((acc, item) => {
    if (acc && acc[item] != null) {
      return acc[item];
    }
    return null;
  }, obj);
}
function getStyleValue(
  themeMapping,
  transform,
  propValueFinal,
  userValue = propValueFinal,
) {
  let value;
  if (typeof themeMapping === "function") {
    value = themeMapping(propValueFinal);
  } else if (Array.isArray(themeMapping)) {
    value = themeMapping[propValueFinal] || userValue;
  } else {
    value = getPath(themeMapping, propValueFinal) || userValue;
  }
  if (transform) {
    value = transform(value, userValue, themeMapping);
  }
  return value;
}
function style(options) {
  const { prop, cssProperty = options.prop, themeKey, transform } = options;
  const fn2 = (props) => {
    if (props[prop] == null) {
      return null;
    }
    const propValue = props[prop];
    const theme2 = props.theme;
    const themeMapping = getPath(theme2, themeKey) || {};
    const styleFromPropValue = (propValueFinal) => {
      let value = getStyleValue(themeMapping, transform, propValueFinal);
      if (propValueFinal === value && typeof propValueFinal === "string") {
        value = getStyleValue(
          themeMapping,
          transform,
          `${prop}${propValueFinal === "default" ? "" : capitalize(propValueFinal)}`,
          propValueFinal,
        );
      }
      if (cssProperty === false) {
        return value;
      }
      return {
        [cssProperty]: value,
      };
    };
    return handleBreakpoints(props, propValue, styleFromPropValue);
  };
  fn2.propTypes = false
    ? {
        [prop]: responsivePropType_default,
      }
    : {};
  fn2.filterProps = [prop];
  return fn2;
}
var style_default = style;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/memoize/memoize.js
function memoize2(fn2) {
  const cache = {};
  return (arg2) => {
    if (cache[arg2] === void 0) {
      cache[arg2] = fn2(arg2);
    }
    return cache[arg2];
  };
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/spacing/spacing.js
var properties = {
  m: "margin",
  p: "padding",
};
var directions = {
  t: "Top",
  r: "Right",
  b: "Bottom",
  l: "Left",
  x: ["Left", "Right"],
  y: ["Top", "Bottom"],
};
var aliases = {
  marginX: "mx",
  marginY: "my",
  paddingX: "px",
  paddingY: "py",
};
var getCssProperties = memoize2((prop) => {
  if (prop.length > 2) {
    if (aliases[prop]) {
      prop = aliases[prop];
    } else {
      return [prop];
    }
  }
  const [a, b] = prop.split("");
  const property = properties[a];
  const direction = directions[b] || "";
  return Array.isArray(direction)
    ? direction.map((dir) => property + dir)
    : [property + direction];
});
var marginKeys = [
  "m",
  "mt",
  "mr",
  "mb",
  "ml",
  "mx",
  "my",
  "margin",
  "marginTop",
  "marginRight",
  "marginBottom",
  "marginLeft",
  "marginX",
  "marginY",
  "marginInline",
  "marginInlineStart",
  "marginInlineEnd",
  "marginBlock",
  "marginBlockStart",
  "marginBlockEnd",
];
var paddingKeys = [
  "p",
  "pt",
  "pr",
  "pb",
  "pl",
  "px",
  "py",
  "padding",
  "paddingTop",
  "paddingRight",
  "paddingBottom",
  "paddingLeft",
  "paddingX",
  "paddingY",
  "paddingInline",
  "paddingInlineStart",
  "paddingInlineEnd",
  "paddingBlock",
  "paddingBlockStart",
  "paddingBlockEnd",
];
var spacingKeys = [...marginKeys, ...paddingKeys];
function createUnaryUnit(theme2, themeKey, defaultValue, propName) {
  const themeSpacing = getPath(theme2, themeKey, true) ?? defaultValue;
  if (typeof themeSpacing === "number" || typeof themeSpacing === "string") {
    return (val) => {
      if (typeof val === "string") {
        return val;
      }
      if (false) {
        if (typeof val !== "number") {
          console.error(
            `MUI: Expected ${propName} argument to be a number or a string, got ${val}.`,
          );
        }
      }
      if (typeof themeSpacing === "string") {
        if (themeSpacing.startsWith("var(") && val === 0) {
          return 0;
        }
        if (themeSpacing.startsWith("var(") && val === 1) {
          return themeSpacing;
        }
        return `calc(${val} * ${themeSpacing})`;
      }
      return themeSpacing * val;
    };
  }
  if (Array.isArray(themeSpacing)) {
    return (val) => {
      if (typeof val === "string") {
        return val;
      }
      const abs2 = Math.abs(val);
      if (false) {
        if (!Number.isInteger(abs2)) {
          console.error(
            [
              `MUI: The \`theme.${themeKey}\` array type cannot be combined with non integer values.You should either use an integer value that can be used as index, or define the \`theme.${themeKey}\` as a number.`,
            ].join("\n"),
          );
        } else if (abs2 > themeSpacing.length - 1) {
          console.error(
            [
              `MUI: The value provided (${abs2}) overflows.`,
              `The supported values are: ${JSON.stringify(themeSpacing)}.`,
              `${abs2} > ${themeSpacing.length - 1}, you need to add the missing values.`,
            ].join("\n"),
          );
        }
      }
      const transformed = themeSpacing[abs2];
      if (val >= 0) {
        return transformed;
      }
      if (typeof transformed === "number") {
        return -transformed;
      }
      if (typeof transformed === "string" && transformed.startsWith("var(")) {
        return `calc(-1 * ${transformed})`;
      }
      return `-${transformed}`;
    };
  }
  if (typeof themeSpacing === "function") {
    return themeSpacing;
  }
  if (false) {
    console.error(
      [
        `MUI: The \`theme.${themeKey}\` value (${themeSpacing}) is invalid.`,
        "It should be a number, an array or a function.",
      ].join("\n"),
    );
  }
  return () => void 0;
}
function createUnarySpacing(theme2) {
  return createUnaryUnit(theme2, "spacing", 8, "spacing");
}
function getValue(transformer, propValue) {
  if (typeof propValue === "string" || propValue == null) {
    return propValue;
  }
  return transformer(propValue);
}
function getStyleFromPropValue(cssProperties, transformer) {
  return (propValue) =>
    cssProperties.reduce((acc, cssProperty) => {
      acc[cssProperty] = getValue(transformer, propValue);
      return acc;
    }, {});
}
function resolveCssProperty(props, keys, prop, transformer) {
  if (!keys.includes(prop)) {
    return null;
  }
  const cssProperties = getCssProperties(prop);
  const styleFromPropValue = getStyleFromPropValue(cssProperties, transformer);
  const propValue = props[prop];
  return handleBreakpoints(props, propValue, styleFromPropValue);
}
function style2(props, keys) {
  const transformer = createUnarySpacing(props.theme);
  return Object.keys(props)
    .map((prop) => resolveCssProperty(props, keys, prop, transformer))
    .reduce(merge_default, {});
}
function margin(props) {
  return style2(props, marginKeys);
}
margin.propTypes = false
  ? marginKeys.reduce((obj, key) => {
      obj[key] = responsivePropType_default;
      return obj;
    }, {})
  : {};
margin.filterProps = marginKeys;
function padding(props) {
  return style2(props, paddingKeys);
}
padding.propTypes = false
  ? paddingKeys.reduce((obj, key) => {
      obj[key] = responsivePropType_default;
      return obj;
    }, {})
  : {};
padding.filterProps = paddingKeys;
function spacing(props) {
  return style2(props, spacingKeys);
}
spacing.propTypes = false
  ? spacingKeys.reduce((obj, key) => {
      obj[key] = responsivePropType_default;
      return obj;
    }, {})
  : {};
spacing.filterProps = spacingKeys;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/createTheme/createSpacing.js
function createSpacing(
  spacingInput = 8,
  transform = createUnarySpacing({
    spacing: spacingInput,
  }),
) {
  if (spacingInput.mui) {
    return spacingInput;
  }
  const spacing2 = (...argsInput) => {
    if (false) {
      if (!(argsInput.length <= 4)) {
        console.error(
          `MUI: Too many arguments provided, expected between 0 and 4, got ${argsInput.length}`,
        );
      }
    }
    const args = argsInput.length === 0 ? [1] : argsInput;
    return args
      .map((argument) => {
        const output = transform(argument);
        return typeof output === "number" ? `${output}px` : output;
      })
      .join(" ");
  };
  spacing2.mui = true;
  return spacing2;
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/compose/compose.js
function compose(...styles5) {
  const handlers = styles5.reduce((acc, style4) => {
    style4.filterProps.forEach((prop) => {
      acc[prop] = style4;
    });
    return acc;
  }, {});
  const fn2 = (props) => {
    return Object.keys(props).reduce((acc, prop) => {
      if (handlers[prop]) {
        return merge_default(acc, handlers[prop](props));
      }
      return acc;
    }, {});
  };
  fn2.propTypes = false
    ? styles5.reduce((acc, style4) => Object.assign(acc, style4.propTypes), {})
    : {};
  fn2.filterProps = styles5.reduce(
    (acc, style4) => acc.concat(style4.filterProps),
    [],
  );
  return fn2;
}
var compose_default = compose;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/borders/borders.js
function borderTransform(value) {
  if (typeof value !== "number") {
    return value;
  }
  return `${value}px solid`;
}
function createBorderStyle(prop, transform) {
  return style_default({
    prop,
    themeKey: "borders",
    transform,
  });
}
var border = createBorderStyle("border", borderTransform);
var borderTop = createBorderStyle("borderTop", borderTransform);
var borderRight = createBorderStyle("borderRight", borderTransform);
var borderBottom = createBorderStyle("borderBottom", borderTransform);
var borderLeft = createBorderStyle("borderLeft", borderTransform);
var borderColor = createBorderStyle("borderColor");
var borderTopColor = createBorderStyle("borderTopColor");
var borderRightColor = createBorderStyle("borderRightColor");
var borderBottomColor = createBorderStyle("borderBottomColor");
var borderLeftColor = createBorderStyle("borderLeftColor");
var outline = createBorderStyle("outline", borderTransform);
var outlineColor = createBorderStyle("outlineColor");
var borderRadius = (props) => {
  if (props.borderRadius !== void 0 && props.borderRadius !== null) {
    const transformer = createUnaryUnit(
      props.theme,
      "shape.borderRadius",
      4,
      "borderRadius",
    );
    const styleFromPropValue = (propValue) => ({
      borderRadius: getValue(transformer, propValue),
    });
    return handleBreakpoints(props, props.borderRadius, styleFromPropValue);
  }
  return null;
};
borderRadius.propTypes = false
  ? {
      borderRadius: responsivePropType_default,
    }
  : {};
borderRadius.filterProps = ["borderRadius"];
var borders = compose_default(
  border,
  borderTop,
  borderRight,
  borderBottom,
  borderLeft,
  borderColor,
  borderTopColor,
  borderRightColor,
  borderBottomColor,
  borderLeftColor,
  borderRadius,
  outline,
  outlineColor,
);

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/cssGrid/cssGrid.js
var gap = (props) => {
  if (props.gap !== void 0 && props.gap !== null) {
    const transformer = createUnaryUnit(props.theme, "spacing", 8, "gap");
    const styleFromPropValue = (propValue) => ({
      gap: getValue(transformer, propValue),
    });
    return handleBreakpoints(props, props.gap, styleFromPropValue);
  }
  return null;
};
gap.propTypes = false
  ? {
      gap: responsivePropType_default,
    }
  : {};
gap.filterProps = ["gap"];
var columnGap = (props) => {
  if (props.columnGap !== void 0 && props.columnGap !== null) {
    const transformer = createUnaryUnit(props.theme, "spacing", 8, "columnGap");
    const styleFromPropValue = (propValue) => ({
      columnGap: getValue(transformer, propValue),
    });
    return handleBreakpoints(props, props.columnGap, styleFromPropValue);
  }
  return null;
};
columnGap.propTypes = false
  ? {
      columnGap: responsivePropType_default,
    }
  : {};
columnGap.filterProps = ["columnGap"];
var rowGap = (props) => {
  if (props.rowGap !== void 0 && props.rowGap !== null) {
    const transformer = createUnaryUnit(props.theme, "spacing", 8, "rowGap");
    const styleFromPropValue = (propValue) => ({
      rowGap: getValue(transformer, propValue),
    });
    return handleBreakpoints(props, props.rowGap, styleFromPropValue);
  }
  return null;
};
rowGap.propTypes = false
  ? {
      rowGap: responsivePropType_default,
    }
  : {};
rowGap.filterProps = ["rowGap"];
var gridColumn = style_default({
  prop: "gridColumn",
});
var gridRow = style_default({
  prop: "gridRow",
});
var gridAutoFlow = style_default({
  prop: "gridAutoFlow",
});
var gridAutoColumns = style_default({
  prop: "gridAutoColumns",
});
var gridAutoRows = style_default({
  prop: "gridAutoRows",
});
var gridTemplateColumns = style_default({
  prop: "gridTemplateColumns",
});
var gridTemplateRows = style_default({
  prop: "gridTemplateRows",
});
var gridTemplateAreas = style_default({
  prop: "gridTemplateAreas",
});
var gridArea = style_default({
  prop: "gridArea",
});
var grid = compose_default(
  gap,
  columnGap,
  rowGap,
  gridColumn,
  gridRow,
  gridAutoFlow,
  gridAutoColumns,
  gridAutoRows,
  gridTemplateColumns,
  gridTemplateRows,
  gridTemplateAreas,
  gridArea,
);

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/palette/palette.js
function paletteTransform(value, userValue) {
  if (userValue === "grey") {
    return userValue;
  }
  return value;
}
var color = style_default({
  prop: "color",
  themeKey: "palette",
  transform: paletteTransform,
});
var bgcolor = style_default({
  prop: "bgcolor",
  cssProperty: "backgroundColor",
  themeKey: "palette",
  transform: paletteTransform,
});
var backgroundColor = style_default({
  prop: "backgroundColor",
  themeKey: "palette",
  transform: paletteTransform,
});
var palette = compose_default(color, bgcolor, backgroundColor);

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/sizing/sizing.js
function sizingTransform(value) {
  return value <= 1 && value !== 0 ? `${value * 100}%` : value;
}
var width = style_default({
  prop: "width",
  transform: sizingTransform,
});
var maxWidth = (props) => {
  if (props.maxWidth !== void 0 && props.maxWidth !== null) {
    const styleFromPropValue = (propValue) => {
      const breakpoint =
        props.theme?.breakpoints?.values?.[propValue] || values[propValue];
      if (!breakpoint) {
        return {
          maxWidth: sizingTransform(propValue),
        };
      }
      if (props.theme?.breakpoints?.unit !== "px") {
        return {
          maxWidth: `${breakpoint}${props.theme.breakpoints.unit}`,
        };
      }
      return {
        maxWidth: breakpoint,
      };
    };
    return handleBreakpoints(props, props.maxWidth, styleFromPropValue);
  }
  return null;
};
maxWidth.filterProps = ["maxWidth"];
var minWidth = style_default({
  prop: "minWidth",
  transform: sizingTransform,
});
var height = style_default({
  prop: "height",
  transform: sizingTransform,
});
var maxHeight = style_default({
  prop: "maxHeight",
  transform: sizingTransform,
});
var minHeight = style_default({
  prop: "minHeight",
  transform: sizingTransform,
});
var sizeWidth = style_default({
  prop: "size",
  cssProperty: "width",
  transform: sizingTransform,
});
var sizeHeight = style_default({
  prop: "size",
  cssProperty: "height",
  transform: sizingTransform,
});
var boxSizing = style_default({
  prop: "boxSizing",
});
var sizing = compose_default(
  width,
  maxWidth,
  minWidth,
  height,
  maxHeight,
  minHeight,
  boxSizing,
);

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/styleFunctionSx/defaultSxConfig.js
var defaultSxConfig = {
  // borders
  border: {
    themeKey: "borders",
    transform: borderTransform,
  },
  borderTop: {
    themeKey: "borders",
    transform: borderTransform,
  },
  borderRight: {
    themeKey: "borders",
    transform: borderTransform,
  },
  borderBottom: {
    themeKey: "borders",
    transform: borderTransform,
  },
  borderLeft: {
    themeKey: "borders",
    transform: borderTransform,
  },
  borderColor: {
    themeKey: "palette",
  },
  borderTopColor: {
    themeKey: "palette",
  },
  borderRightColor: {
    themeKey: "palette",
  },
  borderBottomColor: {
    themeKey: "palette",
  },
  borderLeftColor: {
    themeKey: "palette",
  },
  outline: {
    themeKey: "borders",
    transform: borderTransform,
  },
  outlineColor: {
    themeKey: "palette",
  },
  borderRadius: {
    themeKey: "shape.borderRadius",
    style: borderRadius,
  },
  // palette
  color: {
    themeKey: "palette",
    transform: paletteTransform,
  },
  bgcolor: {
    themeKey: "palette",
    cssProperty: "backgroundColor",
    transform: paletteTransform,
  },
  backgroundColor: {
    themeKey: "palette",
    transform: paletteTransform,
  },
  // spacing
  p: {
    style: padding,
  },
  pt: {
    style: padding,
  },
  pr: {
    style: padding,
  },
  pb: {
    style: padding,
  },
  pl: {
    style: padding,
  },
  px: {
    style: padding,
  },
  py: {
    style: padding,
  },
  padding: {
    style: padding,
  },
  paddingTop: {
    style: padding,
  },
  paddingRight: {
    style: padding,
  },
  paddingBottom: {
    style: padding,
  },
  paddingLeft: {
    style: padding,
  },
  paddingX: {
    style: padding,
  },
  paddingY: {
    style: padding,
  },
  paddingInline: {
    style: padding,
  },
  paddingInlineStart: {
    style: padding,
  },
  paddingInlineEnd: {
    style: padding,
  },
  paddingBlock: {
    style: padding,
  },
  paddingBlockStart: {
    style: padding,
  },
  paddingBlockEnd: {
    style: padding,
  },
  m: {
    style: margin,
  },
  mt: {
    style: margin,
  },
  mr: {
    style: margin,
  },
  mb: {
    style: margin,
  },
  ml: {
    style: margin,
  },
  mx: {
    style: margin,
  },
  my: {
    style: margin,
  },
  margin: {
    style: margin,
  },
  marginTop: {
    style: margin,
  },
  marginRight: {
    style: margin,
  },
  marginBottom: {
    style: margin,
  },
  marginLeft: {
    style: margin,
  },
  marginX: {
    style: margin,
  },
  marginY: {
    style: margin,
  },
  marginInline: {
    style: margin,
  },
  marginInlineStart: {
    style: margin,
  },
  marginInlineEnd: {
    style: margin,
  },
  marginBlock: {
    style: margin,
  },
  marginBlockStart: {
    style: margin,
  },
  marginBlockEnd: {
    style: margin,
  },
  // display
  displayPrint: {
    cssProperty: false,
    transform: (value) => ({
      "@media print": {
        display: value,
      },
    }),
  },
  display: {},
  overflow: {},
  textOverflow: {},
  visibility: {},
  whiteSpace: {},
  // flexbox
  flexBasis: {},
  flexDirection: {},
  flexWrap: {},
  justifyContent: {},
  alignItems: {},
  alignContent: {},
  order: {},
  flex: {},
  flexGrow: {},
  flexShrink: {},
  alignSelf: {},
  justifyItems: {},
  justifySelf: {},
  // grid
  gap: {
    style: gap,
  },
  rowGap: {
    style: rowGap,
  },
  columnGap: {
    style: columnGap,
  },
  gridColumn: {},
  gridRow: {},
  gridAutoFlow: {},
  gridAutoColumns: {},
  gridAutoRows: {},
  gridTemplateColumns: {},
  gridTemplateRows: {},
  gridTemplateAreas: {},
  gridArea: {},
  // positions
  position: {},
  zIndex: {
    themeKey: "zIndex",
  },
  top: {},
  right: {},
  bottom: {},
  left: {},
  // shadows
  boxShadow: {
    themeKey: "shadows",
  },
  // sizing
  width: {
    transform: sizingTransform,
  },
  maxWidth: {
    style: maxWidth,
  },
  minWidth: {
    transform: sizingTransform,
  },
  height: {
    transform: sizingTransform,
  },
  maxHeight: {
    transform: sizingTransform,
  },
  minHeight: {
    transform: sizingTransform,
  },
  boxSizing: {},
  // typography
  font: {
    themeKey: "font",
  },
  fontFamily: {
    themeKey: "typography",
  },
  fontSize: {
    themeKey: "typography",
  },
  fontStyle: {
    themeKey: "typography",
  },
  fontWeight: {
    themeKey: "typography",
  },
  letterSpacing: {},
  textTransform: {},
  lineHeight: {},
  textAlign: {},
  typography: {
    cssProperty: false,
    themeKey: "typography",
  },
};
var defaultSxConfig_default = defaultSxConfig;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/styleFunctionSx/styleFunctionSx.js
function objectsHaveSameKeys(...objects) {
  const allKeys = objects.reduce(
    (keys, object) => keys.concat(Object.keys(object)),
    [],
  );
  const union = new Set(allKeys);
  return objects.every((object) => union.size === Object.keys(object).length);
}
function callIfFn(maybeFn, arg2) {
  return typeof maybeFn === "function" ? maybeFn(arg2) : maybeFn;
}
function unstable_createStyleFunctionSx() {
  function getThemeValue(prop, val, theme2, config) {
    const props = {
      [prop]: val,
      theme: theme2,
    };
    const options = config[prop];
    if (!options) {
      return {
        [prop]: val,
      };
    }
    const { cssProperty = prop, themeKey, transform, style: style4 } = options;
    if (val == null) {
      return null;
    }
    if (themeKey === "typography" && val === "inherit") {
      return {
        [prop]: val,
      };
    }
    const themeMapping = getPath(theme2, themeKey) || {};
    if (style4) {
      return style4(props);
    }
    const styleFromPropValue = (propValueFinal) => {
      let value = getStyleValue(themeMapping, transform, propValueFinal);
      if (propValueFinal === value && typeof propValueFinal === "string") {
        value = getStyleValue(
          themeMapping,
          transform,
          `${prop}${propValueFinal === "default" ? "" : capitalize(propValueFinal)}`,
          propValueFinal,
        );
      }
      if (cssProperty === false) {
        return value;
      }
      return {
        [cssProperty]: value,
      };
    };
    return handleBreakpoints(props, val, styleFromPropValue);
  }
  function styleFunctionSx2(props) {
    const { sx, theme: theme2 = {}, nested } = props || {};
    if (!sx) {
      return null;
    }
    const config = theme2.unstable_sxConfig ?? defaultSxConfig_default;
    function traverse(sxInput) {
      let sxObject = sxInput;
      if (typeof sxInput === "function") {
        sxObject = sxInput(theme2);
      } else if (typeof sxInput !== "object") {
        return sxInput;
      }
      if (!sxObject) {
        return null;
      }
      const emptyBreakpoints = createEmptyBreakpointObject(theme2.breakpoints);
      const breakpointsKeys = Object.keys(emptyBreakpoints);
      let css2 = emptyBreakpoints;
      Object.keys(sxObject).forEach((styleKey) => {
        const value = callIfFn(sxObject[styleKey], theme2);
        if (value !== null && value !== void 0) {
          if (typeof value === "object") {
            if (config[styleKey]) {
              css2 = merge_default(
                css2,
                getThemeValue(styleKey, value, theme2, config),
              );
            } else {
              const breakpointsValues = handleBreakpoints(
                {
                  theme: theme2,
                },
                value,
                (x) => ({
                  [styleKey]: x,
                }),
              );
              if (objectsHaveSameKeys(breakpointsValues, value)) {
                css2[styleKey] = styleFunctionSx2({
                  sx: value,
                  theme: theme2,
                  nested: true,
                });
              } else {
                css2 = merge_default(css2, breakpointsValues);
              }
            }
          } else {
            css2 = merge_default(
              css2,
              getThemeValue(styleKey, value, theme2, config),
            );
          }
        }
      });
      if (!nested && theme2.modularCssLayers) {
        return {
          "@layer sx": sortContainerQueries(
            theme2,
            removeUnusedBreakpoints(breakpointsKeys, css2),
          ),
        };
      }
      return sortContainerQueries(
        theme2,
        removeUnusedBreakpoints(breakpointsKeys, css2),
      );
    }
    return Array.isArray(sx) ? sx.map(traverse) : traverse(sx);
  }
  return styleFunctionSx2;
}
var styleFunctionSx = unstable_createStyleFunctionSx();
styleFunctionSx.filterProps = ["sx"];
var styleFunctionSx_default = styleFunctionSx;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/createTheme/applyStyles.js
function applyStyles(key, styles5) {
  const theme2 = this;
  if (theme2.vars) {
    if (
      !theme2.colorSchemes?.[key] ||
      typeof theme2.getColorSchemeSelector !== "function"
    ) {
      return {};
    }
    let selector = theme2.getColorSchemeSelector(key);
    if (selector === "&") {
      return styles5;
    }
    if (selector.includes("data-") || selector.includes(".")) {
      selector = `*:where(${selector.replace(/\s*&$/, "")}) &`;
    }
    return {
      [selector]: styles5,
    };
  }
  if (theme2.palette.mode === key) {
    return styles5;
  }
  return {};
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/createTheme/createTheme.js
function createTheme(options = {}, ...args) {
  const {
    breakpoints: breakpointsInput = {},
    palette: paletteInput = {},
    spacing: spacingInput,
    shape: shapeInput = {},
    ...other
  } = options;
  const breakpoints = createBreakpoints(breakpointsInput);
  const spacing2 = createSpacing(spacingInput);
  let muiTheme = deepmerge(
    {
      breakpoints,
      direction: "ltr",
      components: {},
      // Inject component definitions.
      palette: {
        mode: "light",
        ...paletteInput,
      },
      spacing: spacing2,
      shape: {
        ...shape_default,
        ...shapeInput,
      },
    },
    other,
  );
  muiTheme = cssContainerQueries(muiTheme);
  muiTheme.applyStyles = applyStyles;
  muiTheme = args.reduce((acc, argument) => deepmerge(acc, argument), muiTheme);
  muiTheme.unstable_sxConfig = {
    ...defaultSxConfig_default,
    ...other?.unstable_sxConfig,
  };
  muiTheme.unstable_sx = function sx(props) {
    return styleFunctionSx_default({
      sx: props,
      theme: this,
    });
  };
  return muiTheme;
}
var createTheme_default = createTheme;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/useThemeWithoutDefault/useThemeWithoutDefault.js
var React6 = __toESM(require_react(), 1);
function isObjectEmpty(obj) {
  return Object.keys(obj).length === 0;
}
function useTheme2(defaultTheme5 = null) {
  const contextTheme = React6.useContext(ThemeContext);
  return !contextTheme || isObjectEmpty(contextTheme)
    ? defaultTheme5
    : contextTheme;
}
var useThemeWithoutDefault_default = useTheme2;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/useTheme/useTheme.js
var systemDefaultTheme = createTheme_default();
function useTheme3(defaultTheme5 = systemDefaultTheme) {
  return useThemeWithoutDefault_default(defaultTheme5);
}
var useTheme_default = useTheme3;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/GlobalStyles/GlobalStyles.js
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
function wrapGlobalLayer(styles5) {
  const serialized = internal_serializeStyles(styles5);
  if (styles5 !== serialized && serialized.styles) {
    if (!serialized.styles.match(/^@layer\s+[^{]*$/)) {
      serialized.styles = `@layer global{${serialized.styles}}`;
    }
    return serialized;
  }
  return styles5;
}
function GlobalStyles2({
  styles: styles5,
  themeId,
  defaultTheme: defaultTheme5 = {},
}) {
  const upperTheme = useTheme_default(defaultTheme5);
  const resolvedTheme = themeId
    ? upperTheme[themeId] || upperTheme
    : upperTheme;
  let globalStyles =
    typeof styles5 === "function" ? styles5(resolvedTheme) : styles5;
  if (resolvedTheme.modularCssLayers) {
    if (Array.isArray(globalStyles)) {
      globalStyles = globalStyles.map((styleArg) => {
        if (typeof styleArg === "function") {
          return wrapGlobalLayer(styleArg(resolvedTheme));
        }
        return wrapGlobalLayer(styleArg);
      });
    } else {
      globalStyles = wrapGlobalLayer(globalStyles);
    }
  }
  return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(GlobalStyles, {
    styles: globalStyles,
  });
}
false
  ? (GlobalStyles2.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * @ignore
       */
      defaultTheme: import_prop_types.default.object,
      /**
       * @ignore
       */
      styles: import_prop_types.default.oneOfType([
        import_prop_types.default.array,
        import_prop_types.default.func,
        import_prop_types.default.number,
        import_prop_types.default.object,
        import_prop_types.default.string,
        import_prop_types.default.bool,
      ]),
      /**
       * @ignore
       */
      themeId: import_prop_types.default.string,
    })
  : void 0;
var GlobalStyles_default = GlobalStyles2;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/styleFunctionSx/extendSxProp.js
var splitProps = (props) => {
  const result = {
    systemProps: {},
    otherProps: {},
  };
  const config = props?.theme?.unstable_sxConfig ?? defaultSxConfig_default;
  Object.keys(props).forEach((prop) => {
    if (config[prop]) {
      result.systemProps[prop] = props[prop];
    } else {
      result.otherProps[prop] = props[prop];
    }
  });
  return result;
};
function extendSxProp(props) {
  const { sx: inSx, ...other } = props;
  const { systemProps, otherProps } = splitProps(other);
  let finalSx;
  if (Array.isArray(inSx)) {
    finalSx = [systemProps, ...inSx];
  } else if (typeof inSx === "function") {
    finalSx = (...args) => {
      const result = inSx(...args);
      if (!isPlainObject(result)) {
        return systemProps;
      }
      return {
        ...systemProps,
        ...result,
      };
    };
  } else {
    finalSx = {
      ...systemProps,
      ...inSx,
    };
  }
  return {
    ...otherProps,
    sx: finalSx,
  };
}

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/ClassNameGenerator/ClassNameGenerator.js
var defaultGenerator = (componentName) => componentName;
var createClassNameGenerator = () => {
  let generate = defaultGenerator;
  return {
    configure(generator) {
      generate = generator;
    },
    generate(componentName) {
      return generate(componentName);
    },
    reset() {
      generate = defaultGenerator;
    },
  };
};
var ClassNameGenerator = createClassNameGenerator();
var ClassNameGenerator_default = ClassNameGenerator;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/createBox/createBox.js
var React7 = __toESM(require_react(), 1);

// node_modules/.pnpm/clsx@2.1.1/node_modules/clsx/dist/clsx.mjs
function r(e) {
  var t,
    f,
    n = "";
  if ("string" == typeof e || "number" == typeof e) n += e;
  else if ("object" == typeof e)
    if (Array.isArray(e)) {
      var o = e.length;
      for (t = 0; t < o; t++)
        e[t] && (f = r(e[t])) && (n && (n += " "), (n += f));
    } else for (f in e) e[f] && (n && (n += " "), (n += f));
  return n;
}
function clsx() {
  for (var e, t, f = 0, n = "", o = arguments.length; f < o; f++)
    (e = arguments[f]) && (t = r(e)) && (n && (n += " "), (n += t));
  return n;
}
var clsx_default = clsx;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/createBox/createBox.js
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
function createBox(options = {}) {
  const {
    themeId,
    defaultTheme: defaultTheme5,
    defaultClassName = "MuiBox-root",
    generateClassName,
  } = options;
  const BoxRoot = styled2("div", {
    shouldForwardProp: (prop) =>
      prop !== "theme" && prop !== "sx" && prop !== "as",
  })(styleFunctionSx_default);
  const Box2 = /* @__PURE__ */ React7.forwardRef(function Box3(inProps, ref) {
    const theme2 = useTheme_default(defaultTheme5);
    const { className, component = "div", ...other } = extendSxProp(inProps);
    return /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(BoxRoot, {
      as: component,
      ref,
      className: clsx_default(
        className,
        generateClassName
          ? generateClassName(defaultClassName)
          : defaultClassName,
      ),
      theme: themeId ? theme2[themeId] || theme2 : theme2,
      ...other,
    });
  });
  return Box2;
}

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/generateUtilityClass/generateUtilityClass.js
var globalStateClasses = {
  active: "active",
  checked: "checked",
  completed: "completed",
  disabled: "disabled",
  error: "error",
  expanded: "expanded",
  focused: "focused",
  focusVisible: "focusVisible",
  open: "open",
  readOnly: "readOnly",
  required: "required",
  selected: "selected",
};
function generateUtilityClass(componentName, slot, globalStatePrefix = "Mui") {
  const globalStateClass = globalStateClasses[slot];
  return globalStateClass
    ? `${globalStatePrefix}-${globalStateClass}`
    : `${ClassNameGenerator_default.generate(componentName)}-${slot}`;
}

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/generateUtilityClasses/generateUtilityClasses.js
function generateUtilityClasses(
  componentName,
  slots,
  globalStatePrefix = "Mui",
) {
  const result = {};
  slots.forEach((slot) => {
    result[slot] = generateUtilityClass(componentName, slot, globalStatePrefix);
  });
  return result;
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/preprocessStyles.js
function preprocessStyles(input) {
  const { variants, ...style4 } = input;
  const result = {
    variants,
    style: internal_serializeStyles(style4),
    isProcessed: true,
  };
  if (result.style === style4) {
    return result;
  }
  if (variants) {
    variants.forEach((variant) => {
      if (typeof variant.style !== "function") {
        variant.style = internal_serializeStyles(variant.style);
      }
    });
  }
  return result;
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/createStyled/createStyled.js
var systemDefaultTheme2 = createTheme_default();
function shouldForwardProp(prop) {
  return (
    prop !== "ownerState" && prop !== "theme" && prop !== "sx" && prop !== "as"
  );
}
function shallowLayer(serialized, layerName) {
  if (
    layerName &&
    serialized &&
    typeof serialized === "object" &&
    serialized.styles &&
    !serialized.styles.startsWith("@layer")
  ) {
    serialized.styles = `@layer ${layerName}{${String(serialized.styles)}}`;
  }
  return serialized;
}
function defaultOverridesResolver(slot) {
  if (!slot) {
    return null;
  }
  return (_props, styles5) => styles5[slot];
}
function attachTheme(props, themeId, defaultTheme5) {
  props.theme = isObjectEmpty2(props.theme)
    ? defaultTheme5
    : props.theme[themeId] || props.theme;
}
function processStyle(props, style4, layerName) {
  const resolvedStyle = typeof style4 === "function" ? style4(props) : style4;
  if (Array.isArray(resolvedStyle)) {
    return resolvedStyle.flatMap((subStyle) =>
      processStyle(props, subStyle, layerName),
    );
  }
  if (Array.isArray(resolvedStyle?.variants)) {
    let rootStyle;
    if (resolvedStyle.isProcessed) {
      rootStyle = layerName
        ? shallowLayer(resolvedStyle.style, layerName)
        : resolvedStyle.style;
    } else {
      const { variants, ...otherStyles } = resolvedStyle;
      rootStyle = layerName
        ? shallowLayer(internal_serializeStyles(otherStyles), layerName)
        : otherStyles;
    }
    return processStyleVariants(
      props,
      resolvedStyle.variants,
      [rootStyle],
      layerName,
    );
  }
  if (resolvedStyle?.isProcessed) {
    return layerName
      ? shallowLayer(internal_serializeStyles(resolvedStyle.style), layerName)
      : resolvedStyle.style;
  }
  return layerName
    ? shallowLayer(internal_serializeStyles(resolvedStyle), layerName)
    : resolvedStyle;
}
function processStyleVariants(
  props,
  variants,
  results = [],
  layerName = void 0,
) {
  let mergedState;
  variantLoop: for (let i = 0; i < variants.length; i += 1) {
    const variant = variants[i];
    if (typeof variant.props === "function") {
      mergedState ??= {
        ...props,
        ...props.ownerState,
        ownerState: props.ownerState,
      };
      if (!variant.props(mergedState)) {
        continue;
      }
    } else {
      for (const key in variant.props) {
        if (
          props[key] !== variant.props[key] &&
          props.ownerState?.[key] !== variant.props[key]
        ) {
          continue variantLoop;
        }
      }
    }
    if (typeof variant.style === "function") {
      mergedState ??= {
        ...props,
        ...props.ownerState,
        ownerState: props.ownerState,
      };
      results.push(
        layerName
          ? shallowLayer(
              internal_serializeStyles(variant.style(mergedState)),
              layerName,
            )
          : variant.style(mergedState),
      );
    } else {
      results.push(
        layerName
          ? shallowLayer(internal_serializeStyles(variant.style), layerName)
          : variant.style,
      );
    }
  }
  return results;
}
function createStyled3(input = {}) {
  const {
    themeId,
    defaultTheme: defaultTheme5 = systemDefaultTheme2,
    rootShouldForwardProp: rootShouldForwardProp2 = shouldForwardProp,
    slotShouldForwardProp: slotShouldForwardProp2 = shouldForwardProp,
  } = input;
  function styleAttachTheme(props) {
    attachTheme(props, themeId, defaultTheme5);
  }
  const styled5 = (tag, inputOptions = {}) => {
    internal_mutateStyles(tag, (styles5) =>
      styles5.filter((style4) => style4 !== styleFunctionSx_default),
    );
    const {
      name: componentName,
      slot: componentSlot,
      skipVariantsResolver: inputSkipVariantsResolver,
      skipSx: inputSkipSx,
      // TODO v6: remove `lowercaseFirstLetter()` in the next major release
      // For more details: https://github.com/mui/material-ui/pull/37908
      overridesResolver: overridesResolver3 = defaultOverridesResolver(
        lowercaseFirstLetter(componentSlot),
      ),
      ...options
    } = inputOptions;
    const layerName =
      (componentName && componentName.startsWith("Mui")) || !!componentSlot
        ? "components"
        : "custom";
    const skipVariantsResolver =
      inputSkipVariantsResolver !== void 0
        ? inputSkipVariantsResolver
        : // TODO v6: remove `Root` in the next major release
          // For more details: https://github.com/mui/material-ui/pull/37908
          (componentSlot &&
            componentSlot !== "Root" &&
            componentSlot !== "root") ||
          false;
    const skipSx = inputSkipSx || false;
    let shouldForwardPropOption = shouldForwardProp;
    if (componentSlot === "Root" || componentSlot === "root") {
      shouldForwardPropOption = rootShouldForwardProp2;
    } else if (componentSlot) {
      shouldForwardPropOption = slotShouldForwardProp2;
    } else if (isStringTag(tag)) {
      shouldForwardPropOption = void 0;
    }
    const defaultStyledResolver = styled2(tag, {
      shouldForwardProp: shouldForwardPropOption,
      label: generateStyledLabel(componentName, componentSlot),
      ...options,
    });
    const transformStyle = (style4) => {
      if (style4.__emotion_real === style4) {
        return style4;
      }
      if (typeof style4 === "function") {
        return function styleFunctionProcessor(props) {
          return processStyle(
            props,
            style4,
            props.theme.modularCssLayers ? layerName : void 0,
          );
        };
      }
      if (isPlainObject(style4)) {
        const serialized = preprocessStyles(style4);
        return function styleObjectProcessor(props) {
          if (!serialized.variants) {
            return props.theme.modularCssLayers
              ? shallowLayer(serialized.style, layerName)
              : serialized.style;
          }
          return processStyle(
            props,
            serialized,
            props.theme.modularCssLayers ? layerName : void 0,
          );
        };
      }
      return style4;
    };
    const muiStyledResolver = (...expressionsInput) => {
      const expressionsHead = [];
      const expressionsBody = expressionsInput.map(transformStyle);
      const expressionsTail = [];
      expressionsHead.push(styleAttachTheme);
      if (componentName && overridesResolver3) {
        expressionsTail.push(function styleThemeOverrides(props) {
          const theme2 = props.theme;
          const styleOverrides =
            theme2.components?.[componentName]?.styleOverrides;
          if (!styleOverrides) {
            return null;
          }
          const resolvedStyleOverrides = {};
          for (const slotKey in styleOverrides) {
            resolvedStyleOverrides[slotKey] = processStyle(
              props,
              styleOverrides[slotKey],
              props.theme.modularCssLayers ? "theme" : void 0,
            );
          }
          return overridesResolver3(props, resolvedStyleOverrides);
        });
      }
      if (componentName && !skipVariantsResolver) {
        expressionsTail.push(function styleThemeVariants(props) {
          const theme2 = props.theme;
          const themeVariants = theme2?.components?.[componentName]?.variants;
          if (!themeVariants) {
            return null;
          }
          return processStyleVariants(
            props,
            themeVariants,
            [],
            props.theme.modularCssLayers ? "theme" : void 0,
          );
        });
      }
      if (!skipSx) {
        expressionsTail.push(styleFunctionSx_default);
      }
      if (Array.isArray(expressionsBody[0])) {
        const inputStrings = expressionsBody.shift();
        const placeholdersHead = new Array(expressionsHead.length).fill("");
        const placeholdersTail = new Array(expressionsTail.length).fill("");
        let outputStrings;
        {
          outputStrings = [
            ...placeholdersHead,
            ...inputStrings,
            ...placeholdersTail,
          ];
          outputStrings.raw = [
            ...placeholdersHead,
            ...inputStrings.raw,
            ...placeholdersTail,
          ];
        }
        expressionsHead.unshift(outputStrings);
      }
      const expressions = [
        ...expressionsHead,
        ...expressionsBody,
        ...expressionsTail,
      ];
      const Component = defaultStyledResolver(...expressions);
      if (tag.muiName) {
        Component.muiName = tag.muiName;
      }
      if (false) {
        Component.displayName = generateDisplayName(
          componentName,
          componentSlot,
          tag,
        );
      }
      return Component;
    };
    if (defaultStyledResolver.withConfig) {
      muiStyledResolver.withConfig = defaultStyledResolver.withConfig;
    }
    return muiStyledResolver;
  };
  return styled5;
}
function generateStyledLabel(componentName, componentSlot) {
  let label;
  if (false) {
    if (componentName) {
      label = `${componentName}-${lowercaseFirstLetter(componentSlot || "Root")}`;
    }
  }
  return label;
}
function isObjectEmpty2(object) {
  for (const _ in object) {
    return false;
  }
  return true;
}
function isStringTag(tag) {
  return (
    typeof tag === "string" && // 96 is one less than the char code
    // for "a" so this is checking that
    // it's a lowercase character
    tag.charCodeAt(0) > 96
  );
}
function lowercaseFirstLetter(string) {
  if (!string) {
    return string;
  }
  return string.charAt(0).toLowerCase() + string.slice(1);
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/styled/styled.js
var styled3 = createStyled3();
var styled_default = styled3;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/resolveProps/resolveProps.js
function resolveProps(defaultProps2, props, mergeClassNameAndStyle = false) {
  const output = {
    ...props,
  };
  for (const key in defaultProps2) {
    if (Object.prototype.hasOwnProperty.call(defaultProps2, key)) {
      const propName = key;
      if (propName === "components" || propName === "slots") {
        output[propName] = {
          ...defaultProps2[propName],
          ...output[propName],
        };
      } else if (propName === "componentsProps" || propName === "slotProps") {
        const defaultSlotProps = defaultProps2[propName];
        const slotProps = props[propName];
        if (!slotProps) {
          output[propName] = defaultSlotProps || {};
        } else if (!defaultSlotProps) {
          output[propName] = slotProps;
        } else {
          output[propName] = {
            ...slotProps,
          };
          for (const slotKey in defaultSlotProps) {
            if (
              Object.prototype.hasOwnProperty.call(defaultSlotProps, slotKey)
            ) {
              const slotPropName = slotKey;
              output[propName][slotPropName] = resolveProps(
                defaultSlotProps[slotPropName],
                slotProps[slotPropName],
                mergeClassNameAndStyle,
              );
            }
          }
        }
      } else if (
        propName === "className" &&
        mergeClassNameAndStyle &&
        props.className
      ) {
        output.className = clsx_default(
          defaultProps2?.className,
          props?.className,
        );
      } else if (
        propName === "style" &&
        mergeClassNameAndStyle &&
        props.style
      ) {
        output.style = {
          ...defaultProps2?.style,
          ...props?.style,
        };
      } else if (output[propName] === void 0) {
        output[propName] = defaultProps2[propName];
      }
    }
  }
  return output;
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/useThemeProps/getThemeProps.js
function getThemeProps(params) {
  const { theme: theme2, name, props } = params;
  if (
    !theme2 ||
    !theme2.components ||
    !theme2.components[name] ||
    !theme2.components[name].defaultProps
  ) {
    return props;
  }
  return resolveProps(theme2.components[name].defaultProps, props);
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/useThemeProps/useThemeProps.js
function useThemeProps({ props, name, defaultTheme: defaultTheme5, themeId }) {
  let theme2 = useTheme_default(defaultTheme5);
  if (themeId) {
    theme2 = theme2[themeId] || theme2;
  }
  return getThemeProps({
    theme: theme2,
    name,
    props,
  });
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/useMediaQuery/useMediaQuery.js
var React9 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/useEnhancedEffect/useEnhancedEffect.js
var React8 = __toESM(require_react(), 1);
var useEnhancedEffect =
  typeof window !== "undefined" ? React8.useLayoutEffect : React8.useEffect;
var useEnhancedEffect_default = useEnhancedEffect;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/useMediaQuery/useMediaQuery.js
function useMediaQueryOld(
  query,
  defaultMatches,
  matchMedia,
  ssrMatchMedia,
  noSsr,
) {
  const [match2, setMatch] = React9.useState(() => {
    if (noSsr && matchMedia) {
      return matchMedia(query).matches;
    }
    if (ssrMatchMedia) {
      return ssrMatchMedia(query).matches;
    }
    return defaultMatches;
  });
  useEnhancedEffect_default(() => {
    if (!matchMedia) {
      return void 0;
    }
    const queryList = matchMedia(query);
    const updateMatch = () => {
      setMatch(queryList.matches);
    };
    updateMatch();
    queryList.addEventListener("change", updateMatch);
    return () => {
      queryList.removeEventListener("change", updateMatch);
    };
  }, [query, matchMedia]);
  return match2;
}
var safeReact = {
  ...React9,
};
var maybeReactUseSyncExternalStore = safeReact.useSyncExternalStore;
function useMediaQueryNew(
  query,
  defaultMatches,
  matchMedia,
  ssrMatchMedia,
  noSsr,
) {
  const getDefaultSnapshot = React9.useCallback(
    () => defaultMatches,
    [defaultMatches],
  );
  const getServerSnapshot = React9.useMemo(() => {
    if (noSsr && matchMedia) {
      return () => matchMedia(query).matches;
    }
    if (ssrMatchMedia !== null) {
      const { matches } = ssrMatchMedia(query);
      return () => matches;
    }
    return getDefaultSnapshot;
  }, [getDefaultSnapshot, query, ssrMatchMedia, noSsr, matchMedia]);
  const [getSnapshot, subscribe] = React9.useMemo(() => {
    if (matchMedia === null) {
      return [getDefaultSnapshot, () => () => {}];
    }
    const mediaQueryList = matchMedia(query);
    return [
      () => mediaQueryList.matches,
      (notify) => {
        mediaQueryList.addEventListener("change", notify);
        return () => {
          mediaQueryList.removeEventListener("change", notify);
        };
      },
    ];
  }, [getDefaultSnapshot, matchMedia, query]);
  const match2 = maybeReactUseSyncExternalStore(
    subscribe,
    getSnapshot,
    getServerSnapshot,
  );
  return match2;
}
function unstable_createUseMediaQuery(params = {}) {
  const { themeId } = params;
  return function useMediaQuery3(queryInput, options = {}) {
    let theme2 = useThemeWithoutDefault_default();
    if (theme2 && themeId) {
      theme2 = theme2[themeId] || theme2;
    }
    const supportMatchMedia =
      typeof window !== "undefined" && typeof window.matchMedia !== "undefined";
    const {
      defaultMatches = false,
      matchMedia = supportMatchMedia ? window.matchMedia : null,
      ssrMatchMedia = null,
      noSsr = false,
    } = getThemeProps({
      name: "MuiUseMediaQuery",
      props: options,
      theme: theme2,
    });
    if (false) {
      if (typeof queryInput === "function" && theme2 === null) {
        console.error(
          [
            "MUI: The `query` argument provided is invalid.",
            "You are providing a function without a theme in the context.",
            "One of the parent elements needs to use a ThemeProvider.",
          ].join("\n"),
        );
      }
    }
    let query =
      typeof queryInput === "function" ? queryInput(theme2) : queryInput;
    query = query.replace(/^@media( ?)/m, "");
    if (query.includes("print")) {
      console.warn(
        [
          `MUI: You have provided a \`print\` query to the \`useMediaQuery\` hook.`,
          "Using the print media query to modify print styles can lead to unexpected results.",
          "Consider using the `displayPrint` field in the `sx` prop instead.",
          "More information about `displayPrint` on our docs: https://mui.com/system/display/#display-in-print.",
        ].join("\n"),
      );
    }
    const useMediaQueryImplementation =
      maybeReactUseSyncExternalStore !== void 0
        ? useMediaQueryNew
        : useMediaQueryOld;
    const match2 = useMediaQueryImplementation(
      query,
      defaultMatches,
      matchMedia,
      ssrMatchMedia,
      noSsr,
    );
    if (false) {
      React9.useDebugValue({
        query,
        match: match2,
      });
    }
    return match2;
  };
}
var useMediaQuery = unstable_createUseMediaQuery();

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/clamp/clamp.js
function clamp(
  val,
  min2 = Number.MIN_SAFE_INTEGER,
  max2 = Number.MAX_SAFE_INTEGER,
) {
  return Math.max(min2, Math.min(val, max2));
}
var clamp_default = clamp;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/colorManipulator/colorManipulator.js
function clampWrapper(value, min2 = 0, max2 = 1) {
  if (false) {
    if (value < min2 || value > max2) {
      console.error(
        `MUI: The value provided ${value} is out of range [${min2}, ${max2}].`,
      );
    }
  }
  return clamp_default(value, min2, max2);
}
function hexToRgb(color2) {
  color2 = color2.slice(1);
  const re = new RegExp(`.{1,${color2.length >= 6 ? 2 : 1}}`, "g");
  let colors = color2.match(re);
  if (colors && colors[0].length === 1) {
    colors = colors.map((n) => n + n);
  }
  if (false) {
    if (color2.length !== color2.trim().length) {
      console.error(
        `MUI: The color: "${color2}" is invalid. Make sure the color input doesn't contain leading/trailing space.`,
      );
    }
  }
  return colors
    ? `rgb${colors.length === 4 ? "a" : ""}(${colors
        .map((n, index) => {
          return index < 3
            ? parseInt(n, 16)
            : Math.round((parseInt(n, 16) / 255) * 1e3) / 1e3;
        })
        .join(", ")})`
    : "";
}
function decomposeColor(color2) {
  if (color2.type) {
    return color2;
  }
  if (color2.charAt(0) === "#") {
    return decomposeColor(hexToRgb(color2));
  }
  const marker = color2.indexOf("(");
  const type = color2.substring(0, marker);
  if (!["rgb", "rgba", "hsl", "hsla", "color"].includes(type)) {
    throw new Error(
      false
        ? `MUI: Unsupported \`${color2}\` color.
The following formats are supported: #nnn, #nnnnnn, rgb(), rgba(), hsl(), hsla(), color().`
        : formatMuiErrorMessage(9, color2),
    );
  }
  let values3 = color2.substring(marker + 1, color2.length - 1);
  let colorSpace;
  if (type === "color") {
    values3 = values3.split(" ");
    colorSpace = values3.shift();
    if (values3.length === 4 && values3[3].charAt(0) === "/") {
      values3[3] = values3[3].slice(1);
    }
    if (
      !["srgb", "display-p3", "a98-rgb", "prophoto-rgb", "rec-2020"].includes(
        colorSpace,
      )
    ) {
      throw new Error(
        false
          ? `MUI: unsupported \`${colorSpace}\` color space.
The following color spaces are supported: srgb, display-p3, a98-rgb, prophoto-rgb, rec-2020.`
          : formatMuiErrorMessage(10, colorSpace),
      );
    }
  } else {
    values3 = values3.split(",");
  }
  values3 = values3.map((value) => parseFloat(value));
  return {
    type,
    values: values3,
    colorSpace,
  };
}
var colorChannel = (color2) => {
  const decomposedColor = decomposeColor(color2);
  return decomposedColor.values
    .slice(0, 3)
    .map((val, idx) =>
      decomposedColor.type.includes("hsl") && idx !== 0 ? `${val}%` : val,
    )
    .join(" ");
};
var private_safeColorChannel = (color2, warning) => {
  try {
    return colorChannel(color2);
  } catch (error) {
    if (warning && false) {
      console.warn(warning);
    }
    return color2;
  }
};
function recomposeColor(color2) {
  const { type, colorSpace } = color2;
  let { values: values3 } = color2;
  if (type.includes("rgb")) {
    values3 = values3.map((n, i) => (i < 3 ? parseInt(n, 10) : n));
  } else if (type.includes("hsl")) {
    values3[1] = `${values3[1]}%`;
    values3[2] = `${values3[2]}%`;
  }
  if (type.includes("color")) {
    values3 = `${colorSpace} ${values3.join(" ")}`;
  } else {
    values3 = `${values3.join(", ")}`;
  }
  return `${type}(${values3})`;
}
function hslToRgb(color2) {
  color2 = decomposeColor(color2);
  const { values: values3 } = color2;
  const h = values3[0];
  const s = values3[1] / 100;
  const l = values3[2] / 100;
  const a = s * Math.min(l, 1 - l);
  const f = (n, k = (n + h / 30) % 12) =>
    l - a * Math.max(Math.min(k - 3, 9 - k, 1), -1);
  let type = "rgb";
  const rgb = [
    Math.round(f(0) * 255),
    Math.round(f(8) * 255),
    Math.round(f(4) * 255),
  ];
  if (color2.type === "hsla") {
    type += "a";
    rgb.push(values3[3]);
  }
  return recomposeColor({
    type,
    values: rgb,
  });
}
function getLuminance(color2) {
  color2 = decomposeColor(color2);
  let rgb =
    color2.type === "hsl" || color2.type === "hsla"
      ? decomposeColor(hslToRgb(color2)).values
      : color2.values;
  rgb = rgb.map((val) => {
    if (color2.type !== "color") {
      val /= 255;
    }
    return val <= 0.03928 ? val / 12.92 : ((val + 0.055) / 1.055) ** 2.4;
  });
  return Number(
    (0.2126 * rgb[0] + 0.7152 * rgb[1] + 0.0722 * rgb[2]).toFixed(3),
  );
}
function getContrastRatio(foreground, background) {
  const lumA = getLuminance(foreground);
  const lumB = getLuminance(background);
  return (Math.max(lumA, lumB) + 0.05) / (Math.min(lumA, lumB) + 0.05);
}
function alpha(color2, value) {
  color2 = decomposeColor(color2);
  value = clampWrapper(value);
  if (color2.type === "rgb" || color2.type === "hsl") {
    color2.type += "a";
  }
  if (color2.type === "color") {
    color2.values[3] = `/${value}`;
  } else {
    color2.values[3] = value;
  }
  return recomposeColor(color2);
}
function private_safeAlpha(color2, value, warning) {
  try {
    return alpha(color2, value);
  } catch (error) {
    if (warning && false) {
      console.warn(warning);
    }
    return color2;
  }
}
function darken(color2, coefficient) {
  color2 = decomposeColor(color2);
  coefficient = clampWrapper(coefficient);
  if (color2.type.includes("hsl")) {
    color2.values[2] *= 1 - coefficient;
  } else if (color2.type.includes("rgb") || color2.type.includes("color")) {
    for (let i = 0; i < 3; i += 1) {
      color2.values[i] *= 1 - coefficient;
    }
  }
  return recomposeColor(color2);
}
function private_safeDarken(color2, coefficient, warning) {
  try {
    return darken(color2, coefficient);
  } catch (error) {
    if (warning && false) {
      console.warn(warning);
    }
    return color2;
  }
}
function lighten(color2, coefficient) {
  color2 = decomposeColor(color2);
  coefficient = clampWrapper(coefficient);
  if (color2.type.includes("hsl")) {
    color2.values[2] += (100 - color2.values[2]) * coefficient;
  } else if (color2.type.includes("rgb")) {
    for (let i = 0; i < 3; i += 1) {
      color2.values[i] += (255 - color2.values[i]) * coefficient;
    }
  } else if (color2.type.includes("color")) {
    for (let i = 0; i < 3; i += 1) {
      color2.values[i] += (1 - color2.values[i]) * coefficient;
    }
  }
  return recomposeColor(color2);
}
function private_safeLighten(color2, coefficient, warning) {
  try {
    return lighten(color2, coefficient);
  } catch (error) {
    if (warning && false) {
      console.warn(warning);
    }
    return color2;
  }
}
function emphasize(color2, coefficient = 0.15) {
  return getLuminance(color2) > 0.5
    ? darken(color2, coefficient)
    : lighten(color2, coefficient);
}
function private_safeEmphasize(color2, coefficient, warning) {
  try {
    return emphasize(color2, coefficient);
  } catch (error) {
    if (warning && false) {
      console.warn(warning);
    }
    return color2;
  }
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/ThemeProvider/ThemeProvider.js
var React16 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+private-theming@7.3.11_0ae3922614f9644d3edd6e44bb89aaa2/node_modules/@mui/private-theming/esm/ThemeProvider/ThemeProvider.js
var React12 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+private-theming@7.3.11_0ae3922614f9644d3edd6e44bb89aaa2/node_modules/@mui/private-theming/esm/useTheme/ThemeContext.js
var React10 = __toESM(require_react(), 1);
var ThemeContext2 = /* @__PURE__ */ React10.createContext(null);
if (false) {
  ThemeContext2.displayName = "ThemeContext";
}
var ThemeContext_default = ThemeContext2;

// node_modules/.pnpm/@mui+private-theming@7.3.11_0ae3922614f9644d3edd6e44bb89aaa2/node_modules/@mui/private-theming/esm/useTheme/useTheme.js
var React11 = __toESM(require_react(), 1);
function useTheme4() {
  const theme2 = React11.useContext(ThemeContext_default);
  if (false) {
    React11.useDebugValue(theme2);
  }
  return theme2;
}

// node_modules/.pnpm/@mui+private-theming@7.3.11_0ae3922614f9644d3edd6e44bb89aaa2/node_modules/@mui/private-theming/esm/ThemeProvider/nested.js
var hasSymbol = typeof Symbol === "function" && Symbol.for;
var nested_default = hasSymbol ? Symbol.for("mui.nested") : "__THEME_NESTED__";

// node_modules/.pnpm/@mui+private-theming@7.3.11_0ae3922614f9644d3edd6e44bb89aaa2/node_modules/@mui/private-theming/esm/ThemeProvider/ThemeProvider.js
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
function mergeOuterLocalTheme(outerTheme, localTheme) {
  if (typeof localTheme === "function") {
    const mergedTheme = localTheme(outerTheme);
    if (false) {
      if (!mergedTheme) {
        console.error(
          [
            "MUI: You should return an object from your theme function, i.e.",
            "<ThemeProvider theme={() => ({})} />",
          ].join("\n"),
        );
      }
    }
    return mergedTheme;
  }
  return {
    ...outerTheme,
    ...localTheme,
  };
}
function ThemeProvider2(props) {
  const { children, theme: localTheme } = props;
  const outerTheme = useTheme4();
  if (false) {
    if (outerTheme === null && typeof localTheme === "function") {
      console.error(
        [
          "MUI: You are providing a theme function prop to the ThemeProvider component:",
          "<ThemeProvider theme={outerTheme => outerTheme} />",
          "",
          "However, no outer theme is present.",
          "Make sure a theme is already injected higher in the React tree or provide a theme object.",
        ].join("\n"),
      );
    }
  }
  const theme2 = React12.useMemo(() => {
    const output =
      outerTheme === null
        ? {
            ...localTheme,
          }
        : mergeOuterLocalTheme(outerTheme, localTheme);
    if (output != null) {
      output[nested_default] = outerTheme !== null;
    }
    return output;
  }, [localTheme, outerTheme]);
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(
    ThemeContext_default.Provider,
    {
      value: theme2,
      children,
    },
  );
}
false
  ? (ThemeProvider2.propTypes = {
      /**
       * Your component tree.
       */
      children: import_prop_types.default.node,
      /**
       * A theme object. You can provide a function to extend the outer theme.
       */
      theme: import_prop_types.default.oneOfType([
        import_prop_types.default.object,
        import_prop_types.default.func,
      ]).isRequired,
    })
  : void 0;
if (false) {
  false
    ? (ThemeProvider2.propTypes = exactProp(ThemeProvider2.propTypes))
    : void 0;
}
var ThemeProvider_default = ThemeProvider2;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/RtlProvider/index.js
var React13 = __toESM(require_react(), 1);
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var RtlContext = /* @__PURE__ */ React13.createContext();
function RtlProvider({ value, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(RtlContext.Provider, {
    value: value ?? true,
    ...props,
  });
}
false
  ? (RtlProvider.propTypes = {
      children: import_prop_types.default.node,
      value: import_prop_types.default.bool,
    })
  : void 0;
var useRtl = () => {
  const value = React13.useContext(RtlContext);
  return value ?? false;
};
var RtlProvider_default = RtlProvider;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/DefaultPropsProvider/DefaultPropsProvider.js
var React14 = __toESM(require_react(), 1);
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
var PropsContext = /* @__PURE__ */ React14.createContext(void 0);
function DefaultPropsProvider({ value, children }) {
  return /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(PropsContext.Provider, {
    value,
    children,
  });
}
false
  ? (DefaultPropsProvider.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * @ignore
       */
      children: import_prop_types.default.node,
      /**
       * @ignore
       */
      value: import_prop_types.default.object,
    })
  : void 0;
function getThemeProps2(params) {
  const { theme: theme2, name, props } = params;
  if (!theme2 || !theme2.components || !theme2.components[name]) {
    return props;
  }
  const config = theme2.components[name];
  if (config.defaultProps) {
    return resolveProps(
      config.defaultProps,
      props,
      theme2.components.mergeClassNameAndStyle,
    );
  }
  if (!config.styleOverrides && !config.variants) {
    return resolveProps(
      config,
      props,
      theme2.components.mergeClassNameAndStyle,
    );
  }
  return props;
}
function useDefaultProps({ props, name }) {
  const ctx = React14.useContext(PropsContext);
  return getThemeProps2({
    props,
    name,
    theme: {
      components: ctx,
    },
  });
}
var DefaultPropsProvider_default = DefaultPropsProvider;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/useId/useId.js
var React15 = __toESM(require_react(), 1);
var globalId = 0;
function useGlobalId(idOverride) {
  const [defaultId, setDefaultId] = React15.useState(idOverride);
  const id = idOverride || defaultId;
  React15.useEffect(() => {
    if (defaultId == null) {
      globalId += 1;
      setDefaultId(`mui-${globalId}`);
    }
  }, [defaultId]);
  return id;
}
var safeReact2 = {
  ...React15,
};
var maybeReactUseId = safeReact2.useId;
function useId(idOverride) {
  if (maybeReactUseId !== void 0) {
    const reactId = maybeReactUseId();
    return idOverride ?? reactId;
  }
  return useGlobalId(idOverride);
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/ThemeProvider/useLayerOrder.js
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
function useLayerOrder(theme2) {
  const upperTheme = useThemeWithoutDefault_default();
  const id = useId() || "";
  const { modularCssLayers } = theme2;
  let layerOrder = "mui.global, mui.components, mui.theme, mui.custom, mui.sx";
  if (!modularCssLayers || upperTheme !== null) {
    layerOrder = "";
  } else if (typeof modularCssLayers === "string") {
    layerOrder = modularCssLayers.replace(/mui(?!\.)/g, layerOrder);
  } else {
    layerOrder = `@layer ${layerOrder};`;
  }
  useEnhancedEffect_default(() => {
    const head = document.querySelector("head");
    if (!head) {
      return;
    }
    const firstChild = head.firstChild;
    if (layerOrder) {
      if (
        firstChild &&
        firstChild.hasAttribute?.("data-mui-layer-order") &&
        firstChild.getAttribute("data-mui-layer-order") === id
      ) {
        return;
      }
      const styleElement = document.createElement("style");
      styleElement.setAttribute("data-mui-layer-order", id);
      styleElement.textContent = layerOrder;
      head.prepend(styleElement);
    } else {
      head.querySelector(`style[data-mui-layer-order="${id}"]`)?.remove();
    }
  }, [layerOrder, id]);
  if (!layerOrder) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(GlobalStyles_default, {
    styles: layerOrder,
  });
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/ThemeProvider/ThemeProvider.js
var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
var EMPTY_THEME = {};
function useThemeScoping(themeId, upperTheme, localTheme, isPrivate = false) {
  return React16.useMemo(() => {
    const resolvedTheme = themeId
      ? upperTheme[themeId] || upperTheme
      : upperTheme;
    if (typeof localTheme === "function") {
      const mergedTheme = localTheme(resolvedTheme);
      const result = themeId
        ? {
            ...upperTheme,
            [themeId]: mergedTheme,
          }
        : mergedTheme;
      if (isPrivate) {
        return () => result;
      }
      return result;
    }
    return themeId
      ? {
          ...upperTheme,
          [themeId]: localTheme,
        }
      : {
          ...upperTheme,
          ...localTheme,
        };
  }, [themeId, upperTheme, localTheme, isPrivate]);
}
function ThemeProvider3(props) {
  const { children, theme: localTheme, themeId } = props;
  const upperTheme = useThemeWithoutDefault_default(EMPTY_THEME);
  const upperPrivateTheme = useTheme4() || EMPTY_THEME;
  if (false) {
    if (
      (upperTheme === null && typeof localTheme === "function") ||
      (themeId &&
        upperTheme &&
        !upperTheme[themeId] &&
        typeof localTheme === "function")
    ) {
      console.error(
        [
          "MUI: You are providing a theme function prop to the ThemeProvider component:",
          "<ThemeProvider theme={outerTheme => outerTheme} />",
          "",
          "However, no outer theme is present.",
          "Make sure a theme is already injected higher in the React tree or provide a theme object.",
        ].join("\n"),
      );
    }
  }
  const engineTheme = useThemeScoping(themeId, upperTheme, localTheme);
  const privateTheme = useThemeScoping(
    themeId,
    upperPrivateTheme,
    localTheme,
    true,
  );
  const rtlValue =
    (themeId ? engineTheme[themeId] : engineTheme).direction === "rtl";
  const layerOrder = useLayerOrder(engineTheme);
  return /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(ThemeProvider_default, {
    theme: privateTheme,
    children: /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
      ThemeContext.Provider,
      {
        value: engineTheme,
        children: /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
          RtlProvider_default,
          {
            value: rtlValue,
            children: /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)(
              DefaultPropsProvider_default,
              {
                value: themeId
                  ? engineTheme[themeId].components
                  : engineTheme.components,
                children: [layerOrder, children],
              },
            ),
          },
        ),
      },
    ),
  });
}
false
  ? (ThemeProvider3.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * Your component tree.
       */
      children: import_prop_types.default.node,
      /**
       * A theme object. You can provide a function to extend the outer theme.
       */
      theme: import_prop_types.default.oneOfType([
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]).isRequired,
      /**
       * The design system's unique id for getting the corresponded theme when there are multiple design systems.
       */
      themeId: import_prop_types.default.string,
    })
  : void 0;
if (false) {
  false
    ? (ThemeProvider3.propTypes = exactProp(ThemeProvider3.propTypes))
    : void 0;
}
var ThemeProvider_default2 = ThemeProvider3;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/memoTheme.js
var arg = {
  theme: void 0,
};
function unstable_memoTheme(styleFn) {
  let lastValue;
  let lastTheme;
  return function styleMemoized(props) {
    let value = lastValue;
    if (value === void 0 || props.theme !== lastTheme) {
      arg.theme = props.theme;
      value = preprocessStyles(styleFn(arg));
      lastValue = value;
      lastTheme = props.theme;
    }
    return value;
  };
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/cssVars/createCssVarsProvider.js
var React18 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/InitColorSchemeScript/InitColorSchemeScript.js
var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
var DEFAULT_MODE_STORAGE_KEY = "mode";
var DEFAULT_COLOR_SCHEME_STORAGE_KEY = "color-scheme";
var DEFAULT_ATTRIBUTE = "data-color-scheme";
function InitColorSchemeScript(options) {
  const {
    defaultMode = "system",
    defaultLightColorScheme = "light",
    defaultDarkColorScheme = "dark",
    modeStorageKey = DEFAULT_MODE_STORAGE_KEY,
    colorSchemeStorageKey = DEFAULT_COLOR_SCHEME_STORAGE_KEY,
    attribute: initialAttribute = DEFAULT_ATTRIBUTE,
    colorSchemeNode = "document.documentElement",
    nonce,
  } = options || {};
  let setter = "";
  let attribute = initialAttribute;
  if (initialAttribute === "class") {
    attribute = ".%s";
  }
  if (initialAttribute === "data") {
    attribute = "[data-%s]";
  }
  if (attribute.startsWith(".")) {
    const selector = attribute.substring(1);
    setter += `${colorSchemeNode}.classList.remove('${selector}'.replace('%s', light), '${selector}'.replace('%s', dark));
      ${colorSchemeNode}.classList.add('${selector}'.replace('%s', colorScheme));`;
  }
  const matches = attribute.match(/\[([^[\]]+)\]/);
  if (matches) {
    const [attr, value] = matches[1].split("=");
    if (!value) {
      setter += `${colorSchemeNode}.removeAttribute('${attr}'.replace('%s', light));
      ${colorSchemeNode}.removeAttribute('${attr}'.replace('%s', dark));`;
    }
    setter += `
      ${colorSchemeNode}.setAttribute('${attr}'.replace('%s', colorScheme), ${value ? `${value}.replace('%s', colorScheme)` : '""'});`;
  } else if (attribute !== ".%s") {
    setter += `${colorSchemeNode}.setAttribute('${attribute}', colorScheme);`;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
    "script",
    {
      suppressHydrationWarning: true,
      nonce: typeof window === "undefined" ? nonce : "",
      dangerouslySetInnerHTML: {
        __html: `(function() {
try {
  let colorScheme = '';
  const mode = localStorage.getItem('${modeStorageKey}') || '${defaultMode}';
  const dark = localStorage.getItem('${colorSchemeStorageKey}-dark') || '${defaultDarkColorScheme}';
  const light = localStorage.getItem('${colorSchemeStorageKey}-light') || '${defaultLightColorScheme}';
  if (mode === 'system') {
    // handle system mode
    const mql = window.matchMedia('(prefers-color-scheme: dark)');
    if (mql.matches) {
      colorScheme = dark
    } else {
      colorScheme = light
    }
  }
  if (mode === 'light') {
    colorScheme = light;
  }
  if (mode === 'dark') {
    colorScheme = dark;
  }
  if (colorScheme) {
    ${setter}
  }
} catch(e){}})();`,
      },
    },
    "mui-color-scheme-init",
  );
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/cssVars/useCurrentColorScheme.js
var React17 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/cssVars/localStorageManager.js
function noop() {}
var localStorageManager = ({ key, storageWindow }) => {
  if (!storageWindow && typeof window !== "undefined") {
    storageWindow = window;
  }
  return {
    get(defaultValue) {
      if (typeof window === "undefined") {
        return void 0;
      }
      if (!storageWindow) {
        return defaultValue;
      }
      let value;
      try {
        value = storageWindow.localStorage.getItem(key);
      } catch {}
      return value || defaultValue;
    },
    set: (value) => {
      if (storageWindow) {
        try {
          storageWindow.localStorage.setItem(key, value);
        } catch {}
      }
    },
    subscribe: (handler) => {
      if (!storageWindow) {
        return noop;
      }
      const listener = (event) => {
        const value = event.newValue;
        if (event.key === key) {
          handler(value);
        }
      };
      storageWindow.addEventListener("storage", listener);
      return () => {
        storageWindow.removeEventListener("storage", listener);
      };
    },
  };
};
var localStorageManager_default = localStorageManager;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/cssVars/useCurrentColorScheme.js
function noop2() {}
function getSystemMode(mode) {
  if (
    typeof window !== "undefined" &&
    typeof window.matchMedia === "function" &&
    mode === "system"
  ) {
    const mql = window.matchMedia("(prefers-color-scheme: dark)");
    if (mql.matches) {
      return "dark";
    }
    return "light";
  }
  return void 0;
}
function processState(state, callback) {
  if (
    state.mode === "light" ||
    (state.mode === "system" && state.systemMode === "light")
  ) {
    return callback("light");
  }
  if (
    state.mode === "dark" ||
    (state.mode === "system" && state.systemMode === "dark")
  ) {
    return callback("dark");
  }
  return void 0;
}
function getColorScheme(state) {
  return processState(state, (mode) => {
    if (mode === "light") {
      return state.lightColorScheme;
    }
    if (mode === "dark") {
      return state.darkColorScheme;
    }
    return void 0;
  });
}
function useCurrentColorScheme(options) {
  const {
    defaultMode = "light",
    defaultLightColorScheme,
    defaultDarkColorScheme,
    supportedColorSchemes = [],
    modeStorageKey = DEFAULT_MODE_STORAGE_KEY,
    colorSchemeStorageKey = DEFAULT_COLOR_SCHEME_STORAGE_KEY,
    storageWindow = typeof window === "undefined" ? void 0 : window,
    storageManager = localStorageManager_default,
    noSsr = false,
  } = options;
  const joinedColorSchemes = supportedColorSchemes.join(",");
  const isMultiSchemes = supportedColorSchemes.length > 1;
  const modeStorage = React17.useMemo(
    () =>
      storageManager?.({
        key: modeStorageKey,
        storageWindow,
      }),
    [storageManager, modeStorageKey, storageWindow],
  );
  const lightStorage = React17.useMemo(
    () =>
      storageManager?.({
        key: `${colorSchemeStorageKey}-light`,
        storageWindow,
      }),
    [storageManager, colorSchemeStorageKey, storageWindow],
  );
  const darkStorage = React17.useMemo(
    () =>
      storageManager?.({
        key: `${colorSchemeStorageKey}-dark`,
        storageWindow,
      }),
    [storageManager, colorSchemeStorageKey, storageWindow],
  );
  const [state, setState] = React17.useState(() => {
    const initialMode = modeStorage?.get(defaultMode) || defaultMode;
    const lightColorScheme =
      lightStorage?.get(defaultLightColorScheme) || defaultLightColorScheme;
    const darkColorScheme =
      darkStorage?.get(defaultDarkColorScheme) || defaultDarkColorScheme;
    return {
      mode: initialMode,
      systemMode: getSystemMode(initialMode),
      lightColorScheme,
      darkColorScheme,
    };
  });
  const [isClient, setIsClient] = React17.useState(noSsr || !isMultiSchemes);
  React17.useEffect(() => {
    setIsClient(true);
  }, []);
  const colorScheme = getColorScheme(state);
  const setMode = React17.useCallback(
    (mode) => {
      setState((currentState) => {
        if (mode === currentState.mode) {
          return currentState;
        }
        const newMode = mode ?? defaultMode;
        modeStorage?.set(newMode);
        return {
          ...currentState,
          mode: newMode,
          systemMode: getSystemMode(newMode),
        };
      });
    },
    [modeStorage, defaultMode],
  );
  const setColorScheme = React17.useCallback(
    (value) => {
      if (!value) {
        setState((currentState) => {
          lightStorage?.set(defaultLightColorScheme);
          darkStorage?.set(defaultDarkColorScheme);
          return {
            ...currentState,
            lightColorScheme: defaultLightColorScheme,
            darkColorScheme: defaultDarkColorScheme,
          };
        });
      } else if (typeof value === "string") {
        if (value && !joinedColorSchemes.includes(value)) {
          console.error(
            `\`${value}\` does not exist in \`theme.colorSchemes\`.`,
          );
        } else {
          setState((currentState) => {
            const newState = {
              ...currentState,
            };
            processState(currentState, (mode) => {
              if (mode === "light") {
                lightStorage?.set(value);
                newState.lightColorScheme = value;
              }
              if (mode === "dark") {
                darkStorage?.set(value);
                newState.darkColorScheme = value;
              }
            });
            return newState;
          });
        }
      } else {
        setState((currentState) => {
          const newState = {
            ...currentState,
          };
          const newLightColorScheme =
            value.light === null ? defaultLightColorScheme : value.light;
          const newDarkColorScheme =
            value.dark === null ? defaultDarkColorScheme : value.dark;
          if (newLightColorScheme) {
            if (!joinedColorSchemes.includes(newLightColorScheme)) {
              console.error(
                `\`${newLightColorScheme}\` does not exist in \`theme.colorSchemes\`.`,
              );
            } else {
              newState.lightColorScheme = newLightColorScheme;
              lightStorage?.set(newLightColorScheme);
            }
          }
          if (newDarkColorScheme) {
            if (!joinedColorSchemes.includes(newDarkColorScheme)) {
              console.error(
                `\`${newDarkColorScheme}\` does not exist in \`theme.colorSchemes\`.`,
              );
            } else {
              newState.darkColorScheme = newDarkColorScheme;
              darkStorage?.set(newDarkColorScheme);
            }
          }
          return newState;
        });
      }
    },
    [
      joinedColorSchemes,
      lightStorage,
      darkStorage,
      defaultLightColorScheme,
      defaultDarkColorScheme,
    ],
  );
  const handleMediaQuery = React17.useCallback(
    (event) => {
      if (state.mode === "system") {
        setState((currentState) => {
          const systemMode = event?.matches ? "dark" : "light";
          if (currentState.systemMode === systemMode) {
            return currentState;
          }
          return {
            ...currentState,
            systemMode,
          };
        });
      }
    },
    [state.mode],
  );
  const mediaListener = React17.useRef(handleMediaQuery);
  mediaListener.current = handleMediaQuery;
  React17.useEffect(() => {
    if (typeof window.matchMedia !== "function" || !isMultiSchemes) {
      return void 0;
    }
    const handler = (...args) => mediaListener.current(...args);
    const media = window.matchMedia("(prefers-color-scheme: dark)");
    media.addListener(handler);
    handler(media);
    return () => {
      media.removeListener(handler);
    };
  }, [isMultiSchemes]);
  React17.useEffect(() => {
    if (isMultiSchemes) {
      const unsubscribeMode =
        modeStorage?.subscribe((value) => {
          if (!value || ["light", "dark", "system"].includes(value)) {
            setMode(value || defaultMode);
          }
        }) || noop2;
      const unsubscribeLight =
        lightStorage?.subscribe((value) => {
          if (!value || joinedColorSchemes.match(value)) {
            setColorScheme({
              light: value,
            });
          }
        }) || noop2;
      const unsubscribeDark =
        darkStorage?.subscribe((value) => {
          if (!value || joinedColorSchemes.match(value)) {
            setColorScheme({
              dark: value,
            });
          }
        }) || noop2;
      return () => {
        unsubscribeMode();
        unsubscribeLight();
        unsubscribeDark();
      };
    }
    return void 0;
  }, [
    setColorScheme,
    setMode,
    joinedColorSchemes,
    defaultMode,
    storageWindow,
    isMultiSchemes,
    modeStorage,
    lightStorage,
    darkStorage,
  ]);
  return {
    ...state,
    mode: isClient ? state.mode : void 0,
    systemMode: isClient ? state.systemMode : void 0,
    colorScheme: isClient ? colorScheme : void 0,
    setMode,
    setColorScheme,
  };
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/cssVars/createCssVarsProvider.js
var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);
var DISABLE_CSS_TRANSITION =
  "*{-webkit-transition:none!important;-moz-transition:none!important;-o-transition:none!important;-ms-transition:none!important;transition:none!important}";
function createCssVarsProvider(options) {
  const {
    themeId,
    /**
     * This `theme` object needs to follow a certain structure to
     * be used correctly by the finel `CssVarsProvider`. It should have a
     * `colorSchemes` key with the light and dark (and any other) palette.
     * It should also ideally have a vars object created using `prepareCssVars`.
     */
    theme: defaultTheme5 = {},
    modeStorageKey: defaultModeStorageKey = DEFAULT_MODE_STORAGE_KEY,
    colorSchemeStorageKey:
      defaultColorSchemeStorageKey = DEFAULT_COLOR_SCHEME_STORAGE_KEY,
    disableTransitionOnChange: designSystemTransitionOnChange = false,
    defaultColorScheme,
    resolveTheme,
  } = options;
  const defaultContext = {
    allColorSchemes: [],
    colorScheme: void 0,
    darkColorScheme: void 0,
    lightColorScheme: void 0,
    mode: void 0,
    setColorScheme: () => {},
    setMode: () => {},
    systemMode: void 0,
  };
  const ColorSchemeContext = /* @__PURE__ */ React18.createContext(void 0);
  if (false) {
    ColorSchemeContext.displayName = "ColorSchemeContext";
  }
  const useColorScheme2 = () =>
    React18.useContext(ColorSchemeContext) || defaultContext;
  const defaultColorSchemes = {};
  const defaultComponents = {};
  function CssVarsProvider2(props) {
    const {
      children,
      theme: themeProp,
      modeStorageKey = defaultModeStorageKey,
      colorSchemeStorageKey = defaultColorSchemeStorageKey,
      disableTransitionOnChange = designSystemTransitionOnChange,
      storageManager,
      storageWindow = typeof window === "undefined" ? void 0 : window,
      documentNode = typeof document === "undefined" ? void 0 : document,
      colorSchemeNode = typeof document === "undefined"
        ? void 0
        : document.documentElement,
      disableNestedContext = false,
      disableStyleSheetGeneration = false,
      defaultMode: initialMode = "system",
      forceThemeRerender = false,
      noSsr,
    } = props;
    const hasMounted = React18.useRef(false);
    const upperTheme = useTheme4();
    const ctx = React18.useContext(ColorSchemeContext);
    const nested = !!ctx && !disableNestedContext;
    const initialTheme = React18.useMemo(() => {
      if (themeProp) {
        return themeProp;
      }
      return typeof defaultTheme5 === "function"
        ? defaultTheme5()
        : defaultTheme5;
    }, [themeProp]);
    const scopedTheme = initialTheme[themeId];
    const restThemeProp = scopedTheme || initialTheme;
    const {
      colorSchemes = defaultColorSchemes,
      components = defaultComponents,
      cssVarPrefix,
    } = restThemeProp;
    const joinedColorSchemes = Object.keys(colorSchemes)
      .filter((k) => !!colorSchemes[k])
      .join(",");
    const allColorSchemes = React18.useMemo(
      () => joinedColorSchemes.split(","),
      [joinedColorSchemes],
    );
    const defaultLightColorScheme2 =
      typeof defaultColorScheme === "string"
        ? defaultColorScheme
        : defaultColorScheme.light;
    const defaultDarkColorScheme2 =
      typeof defaultColorScheme === "string"
        ? defaultColorScheme
        : defaultColorScheme.dark;
    const defaultMode =
      colorSchemes[defaultLightColorScheme2] &&
      colorSchemes[defaultDarkColorScheme2]
        ? initialMode
        : colorSchemes[restThemeProp.defaultColorScheme]?.palette?.mode ||
          restThemeProp.palette?.mode;
    const {
      mode: stateMode,
      setMode,
      systemMode,
      lightColorScheme,
      darkColorScheme,
      colorScheme: stateColorScheme,
      setColorScheme,
    } = useCurrentColorScheme({
      supportedColorSchemes: allColorSchemes,
      defaultLightColorScheme: defaultLightColorScheme2,
      defaultDarkColorScheme: defaultDarkColorScheme2,
      modeStorageKey,
      colorSchemeStorageKey,
      defaultMode,
      storageManager,
      storageWindow,
      noSsr,
    });
    let mode = stateMode;
    let colorScheme = stateColorScheme;
    if (nested) {
      mode = ctx.mode;
      colorScheme = ctx.colorScheme;
    }
    if (false) {
      if (forceThemeRerender && !restThemeProp.vars) {
        console.warn(
          [
            "MUI: The `forceThemeRerender` prop should only be used with CSS theme variables.",
            "Note that it will slow down the app when changing between modes, so only do this when you cannot find a better solution.",
          ].join("\n"),
        );
      }
    }
    let calculatedColorScheme = colorScheme || restThemeProp.defaultColorScheme;
    if (restThemeProp.vars && !forceThemeRerender) {
      calculatedColorScheme = restThemeProp.defaultColorScheme;
    }
    const memoTheme2 = React18.useMemo(() => {
      const themeVars =
        restThemeProp.generateThemeVars?.() || restThemeProp.vars;
      const theme2 = {
        ...restThemeProp,
        components,
        colorSchemes,
        cssVarPrefix,
        vars: themeVars,
      };
      if (typeof theme2.generateSpacing === "function") {
        theme2.spacing = theme2.generateSpacing();
      }
      if (calculatedColorScheme) {
        const scheme = colorSchemes[calculatedColorScheme];
        if (scheme && typeof scheme === "object") {
          Object.keys(scheme).forEach((schemeKey) => {
            if (scheme[schemeKey] && typeof scheme[schemeKey] === "object") {
              theme2[schemeKey] = {
                ...theme2[schemeKey],
                ...scheme[schemeKey],
              };
            } else {
              theme2[schemeKey] = scheme[schemeKey];
            }
          });
        }
      }
      return resolveTheme ? resolveTheme(theme2) : theme2;
    }, [
      restThemeProp,
      calculatedColorScheme,
      components,
      colorSchemes,
      cssVarPrefix,
    ]);
    const colorSchemeSelector = restThemeProp.colorSchemeSelector;
    useEnhancedEffect_default(() => {
      if (
        colorScheme &&
        colorSchemeNode &&
        colorSchemeSelector &&
        colorSchemeSelector !== "media"
      ) {
        const selector = colorSchemeSelector;
        let rule = colorSchemeSelector;
        if (selector === "class") {
          rule = `.%s`;
        }
        if (selector === "data") {
          rule = `[data-%s]`;
        }
        if (selector?.startsWith("data-") && !selector.includes("%s")) {
          rule = `[${selector}="%s"]`;
        }
        if (rule.startsWith(".")) {
          colorSchemeNode.classList.remove(
            ...allColorSchemes.map((scheme) =>
              rule.substring(1).replace("%s", scheme),
            ),
          );
          colorSchemeNode.classList.add(
            rule.substring(1).replace("%s", colorScheme),
          );
        } else {
          const matches = rule.replace("%s", colorScheme).match(/\[([^\]]+)\]/);
          if (matches) {
            const [attr, value] = matches[1].split("=");
            if (!value) {
              allColorSchemes.forEach((scheme) => {
                colorSchemeNode.removeAttribute(
                  attr.replace(colorScheme, scheme),
                );
              });
            }
            colorSchemeNode.setAttribute(
              attr,
              value ? value.replace(/"|'/g, "") : "",
            );
          } else {
            colorSchemeNode.setAttribute(rule, colorScheme);
          }
        }
      }
    }, [colorScheme, colorSchemeSelector, colorSchemeNode, allColorSchemes]);
    React18.useEffect(() => {
      let timer;
      if (disableTransitionOnChange && hasMounted.current && documentNode) {
        const css2 = documentNode.createElement("style");
        css2.appendChild(documentNode.createTextNode(DISABLE_CSS_TRANSITION));
        documentNode.head.appendChild(css2);
        (() => window.getComputedStyle(documentNode.body))();
        timer = setTimeout(() => {
          documentNode.head.removeChild(css2);
        }, 1);
      }
      return () => {
        clearTimeout(timer);
      };
    }, [colorScheme, disableTransitionOnChange, documentNode]);
    React18.useEffect(() => {
      hasMounted.current = true;
      return () => {
        hasMounted.current = false;
      };
    }, []);
    const contextValue = React18.useMemo(
      () => ({
        allColorSchemes,
        colorScheme,
        darkColorScheme,
        lightColorScheme,
        mode,
        setColorScheme,
        setMode: true
          ? setMode
          : (newMode) => {
              if (memoTheme2.colorSchemeSelector === "media") {
                console.error(
                  [
                    "MUI: The `setMode` function has no effect if `colorSchemeSelector` is `media` (`media` is the default value).",
                    "To toggle the mode manually, please configure `colorSchemeSelector` to use a class or data attribute.",
                    "To learn more, visit https://v7.mui.com/material-ui/customization/css-theme-variables/configuration/#toggling-dark-mode-manually",
                  ].join("\n"),
                );
              }
              setMode(newMode);
            },
        systemMode,
      }),
      [
        allColorSchemes,
        colorScheme,
        darkColorScheme,
        lightColorScheme,
        mode,
        setColorScheme,
        setMode,
        systemMode,
        memoTheme2.colorSchemeSelector,
      ],
    );
    let shouldGenerateStyleSheet = true;
    if (
      disableStyleSheetGeneration ||
      restThemeProp.cssVariables === false ||
      (nested && upperTheme?.cssVarPrefix === cssVarPrefix)
    ) {
      shouldGenerateStyleSheet = false;
    }
    const element = /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)(
      React18.Fragment,
      {
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(
            ThemeProvider_default2,
            {
              themeId: scopedTheme ? themeId : void 0,
              theme: memoTheme2,
              children,
            },
          ),
          shouldGenerateStyleSheet &&
            /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(GlobalStyles, {
              styles: memoTheme2.generateStyleSheets?.() || [],
            }),
        ],
      },
    );
    if (nested) {
      return element;
    }
    return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(
      ColorSchemeContext.Provider,
      {
        value: contextValue,
        children: element,
      },
    );
  }
  false
    ? (CssVarsProvider2.propTypes = {
        /**
         * The component tree.
         */
        children: import_prop_types.default.node,
        /**
         * The node used to attach the color-scheme attribute
         */
        colorSchemeNode: import_prop_types.default.any,
        /**
         * localStorage key used to store `colorScheme`
         */
        colorSchemeStorageKey: import_prop_types.default.string,
        /**
         * The default mode when the storage is empty,
         * require the theme to have `colorSchemes` with light and dark.
         */
        defaultMode: import_prop_types.default.string,
        /**
         * If `true`, the provider creates its own context and generate stylesheet as if it is a root `CssVarsProvider`.
         */
        disableNestedContext: import_prop_types.default.bool,
        /**
         * If `true`, the style sheet won't be generated.
         *
         * This is useful for controlling nested CssVarsProvider behavior.
         */
        disableStyleSheetGeneration: import_prop_types.default.bool,
        /**
         * Disable CSS transitions when switching between modes or color schemes.
         */
        disableTransitionOnChange: import_prop_types.default.bool,
        /**
         * The document to attach the attribute to.
         */
        documentNode: import_prop_types.default.any,
        /**
         * If `true`, theme values are recalculated when the mode changes.
         */
        forceThemeRerender: import_prop_types.default.bool,
        /**
         * The key in the local storage used to store current color scheme.
         */
        modeStorageKey: import_prop_types.default.string,
        /**
         * If `true`, the mode will be the same value as the storage without an extra rerendering after the hydration.
         * You should use this option in conjunction with `InitColorSchemeScript` component.
         */
        noSsr: import_prop_types.default.bool,
        /**
         * The storage manager to be used for storing the mode and color scheme
         * @default using `window.localStorage`
         */
        storageManager: import_prop_types.default.func,
        /**
         * The window that attaches the 'storage' event listener.
         * @default window
         */
        storageWindow: import_prop_types.default.any,
        /**
         * The calculated theme object that will be passed through context.
         */
        theme: import_prop_types.default.object,
      })
    : void 0;
  const defaultLightColorScheme =
    typeof defaultColorScheme === "string"
      ? defaultColorScheme
      : defaultColorScheme.light;
  const defaultDarkColorScheme =
    typeof defaultColorScheme === "string"
      ? defaultColorScheme
      : defaultColorScheme.dark;
  const getInitColorSchemeScript = (params) =>
    InitColorSchemeScript({
      colorSchemeStorageKey: defaultColorSchemeStorageKey,
      defaultLightColorScheme,
      defaultDarkColorScheme,
      modeStorageKey: defaultModeStorageKey,
      ...params,
    });
  return {
    CssVarsProvider: CssVarsProvider2,
    useColorScheme: useColorScheme2,
    getInitColorSchemeScript,
  };
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/cssVars/createGetCssVar.js
function createGetCssVar(prefix2 = "") {
  function appendVar(...vars) {
    if (!vars.length) {
      return "";
    }
    const value = vars[0];
    if (
      typeof value === "string" &&
      !value.match(
        /(#|\(|\)|(-?(\d*\.)?\d+)(px|em|%|ex|ch|rem|vw|vh|vmin|vmax|cm|mm|in|pt|pc))|^(-?(\d*\.)?\d+)$|(\d+ \d+ \d+)/,
      )
    ) {
      return `, var(--${prefix2 ? `${prefix2}-` : ""}${value}${appendVar(...vars.slice(1))})`;
    }
    return `, ${value}`;
  }
  const getCssVar = (field, ...fallbacks) => {
    return `var(--${prefix2 ? `${prefix2}-` : ""}${field}${appendVar(...fallbacks)})`;
  };
  return getCssVar;
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/cssVars/cssVarsParser.js
var assignNestedKeys = (obj, keys, value, arrayKeys = []) => {
  let temp = obj;
  keys.forEach((k, index) => {
    if (index === keys.length - 1) {
      if (Array.isArray(temp)) {
        temp[Number(k)] = value;
      } else if (temp && typeof temp === "object") {
        temp[k] = value;
      }
    } else if (temp && typeof temp === "object") {
      if (!temp[k]) {
        temp[k] = arrayKeys.includes(k) ? [] : {};
      }
      temp = temp[k];
    }
  });
};
var walkObjectDeep = (obj, callback, shouldSkipPaths) => {
  function recurse(object, parentKeys = [], arrayKeys = []) {
    Object.entries(object).forEach(([key, value]) => {
      if (
        !shouldSkipPaths ||
        (shouldSkipPaths && !shouldSkipPaths([...parentKeys, key]))
      ) {
        if (value !== void 0 && value !== null) {
          if (typeof value === "object" && Object.keys(value).length > 0) {
            recurse(
              value,
              [...parentKeys, key],
              Array.isArray(value) ? [...arrayKeys, key] : arrayKeys,
            );
          } else {
            callback([...parentKeys, key], value, arrayKeys);
          }
        }
      }
    });
  }
  recurse(obj);
};
var getCssValue = (keys, value) => {
  if (typeof value === "number") {
    if (
      ["lineHeight", "fontWeight", "opacity", "zIndex"].some((prop) =>
        keys.includes(prop),
      )
    ) {
      return value;
    }
    const lastKey = keys[keys.length - 1];
    if (lastKey.toLowerCase().includes("opacity")) {
      return value;
    }
    return `${value}px`;
  }
  return value;
};
function cssVarsParser(theme2, options) {
  const { prefix: prefix2, shouldSkipGeneratingVar: shouldSkipGeneratingVar2 } =
    options || {};
  const css2 = {};
  const vars = {};
  const varsWithDefaults = {};
  walkObjectDeep(
    theme2,
    (keys, value, arrayKeys) => {
      if (typeof value === "string" || typeof value === "number") {
        if (
          !shouldSkipGeneratingVar2 ||
          !shouldSkipGeneratingVar2(keys, value)
        ) {
          const cssVar = `--${prefix2 ? `${prefix2}-` : ""}${keys.join("-")}`;
          const resolvedValue = getCssValue(keys, value);
          Object.assign(css2, {
            [cssVar]: resolvedValue,
          });
          assignNestedKeys(vars, keys, `var(${cssVar})`, arrayKeys);
          assignNestedKeys(
            varsWithDefaults,
            keys,
            `var(${cssVar}, ${resolvedValue})`,
            arrayKeys,
          );
        }
      }
    },
    (keys) => keys[0] === "vars",
    // skip 'vars/*' paths
  );
  return {
    css: css2,
    vars,
    varsWithDefaults,
  };
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/cssVars/prepareCssVars.js
function prepareCssVars(theme2, parserConfig = {}) {
  const {
    getSelector = defaultGetSelector,
    disableCssColorScheme,
    colorSchemeSelector: selector,
    enableContrastVars,
  } = parserConfig;
  const {
    colorSchemes = {},
    components,
    defaultColorScheme = "light",
    ...otherTheme
  } = theme2;
  const {
    vars: rootVars,
    css: rootCss,
    varsWithDefaults: rootVarsWithDefaults,
  } = cssVarsParser(otherTheme, parserConfig);
  let themeVars = rootVarsWithDefaults;
  const colorSchemesMap = {};
  const { [defaultColorScheme]: defaultScheme, ...otherColorSchemes } =
    colorSchemes;
  Object.entries(otherColorSchemes || {}).forEach(([key, scheme]) => {
    const {
      vars,
      css: css2,
      varsWithDefaults,
    } = cssVarsParser(scheme, parserConfig);
    themeVars = deepmerge(themeVars, varsWithDefaults);
    colorSchemesMap[key] = {
      css: css2,
      vars,
    };
  });
  if (defaultScheme) {
    const {
      css: css2,
      vars,
      varsWithDefaults,
    } = cssVarsParser(defaultScheme, parserConfig);
    themeVars = deepmerge(themeVars, varsWithDefaults);
    colorSchemesMap[defaultColorScheme] = {
      css: css2,
      vars,
    };
  }
  function defaultGetSelector(colorScheme, cssObject) {
    let rule = selector;
    if (selector === "class") {
      rule = ".%s";
    }
    if (selector === "data") {
      rule = "[data-%s]";
    }
    if (selector?.startsWith("data-") && !selector.includes("%s")) {
      rule = `[${selector}="%s"]`;
    }
    if (colorScheme) {
      if (rule === "media") {
        if (theme2.defaultColorScheme === colorScheme) {
          return ":root";
        }
        const mode = colorSchemes[colorScheme]?.palette?.mode || colorScheme;
        return {
          [`@media (prefers-color-scheme: ${mode})`]: {
            ":root": cssObject,
          },
        };
      }
      if (rule) {
        if (theme2.defaultColorScheme === colorScheme) {
          return `:root, ${rule.replace("%s", String(colorScheme))}`;
        }
        return rule.replace("%s", String(colorScheme));
      }
    }
    return ":root";
  }
  const generateThemeVars = () => {
    let vars = {
      ...rootVars,
    };
    Object.entries(colorSchemesMap).forEach(([, { vars: schemeVars }]) => {
      vars = deepmerge(vars, schemeVars);
    });
    return vars;
  };
  const generateStyleSheets = () => {
    const stylesheets = [];
    const colorScheme = theme2.defaultColorScheme || "light";
    function insertStyleSheet(key, css2) {
      if (Object.keys(css2).length) {
        stylesheets.push(
          typeof key === "string"
            ? {
                [key]: {
                  ...css2,
                },
              }
            : key,
        );
      }
    }
    insertStyleSheet(
      getSelector(void 0, {
        ...rootCss,
      }),
      rootCss,
    );
    const { [colorScheme]: defaultSchemeVal, ...other } = colorSchemesMap;
    if (defaultSchemeVal) {
      const { css: css2 } = defaultSchemeVal;
      const cssColorSheme = colorSchemes[colorScheme]?.palette?.mode;
      const finalCss =
        !disableCssColorScheme && cssColorSheme
          ? {
              colorScheme: cssColorSheme,
              ...css2,
            }
          : {
              ...css2,
            };
      insertStyleSheet(
        getSelector(colorScheme, {
          ...finalCss,
        }),
        finalCss,
      );
    }
    Object.entries(other).forEach(([key, { css: css2 }]) => {
      const cssColorSheme = colorSchemes[key]?.palette?.mode;
      const finalCss =
        !disableCssColorScheme && cssColorSheme
          ? {
              colorScheme: cssColorSheme,
              ...css2,
            }
          : {
              ...css2,
            };
      insertStyleSheet(
        getSelector(key, {
          ...finalCss,
        }),
        finalCss,
      );
    });
    if (enableContrastVars) {
      stylesheets.push({
        ":root": {
          // use double underscore to indicate that these are private variables
          "--__l-threshold": "0.7",
          "--__l": "clamp(0, (l / var(--__l-threshold) - 1) * -infinity, 1)",
          "--__a": "clamp(0.87, (l / var(--__l-threshold) - 1) * -infinity, 1)",
          // 0.87 is the default alpha value for black text.
        },
      });
    }
    return stylesheets;
  };
  return {
    vars: themeVars,
    generateThemeVars,
    generateStyleSheets,
  };
}
var prepareCssVars_default = prepareCssVars;

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/cssVars/getColorSchemeSelector.js
function createGetColorSchemeSelector(selector) {
  return function getColorSchemeSelector(colorScheme) {
    if (selector === "media") {
      if (false) {
        if (colorScheme !== "light" && colorScheme !== "dark") {
          console.error(
            `MUI: @media (prefers-color-scheme) supports only 'light' or 'dark', but receive '${colorScheme}'.`,
          );
        }
      }
      return `@media (prefers-color-scheme: ${colorScheme})`;
    }
    if (selector) {
      if (selector.startsWith("data-") && !selector.includes("%s")) {
        return `[${selector}="${colorScheme}"] &`;
      }
      if (selector === "class") {
        return `.${colorScheme} &`;
      }
      if (selector === "data") {
        return `[data-${colorScheme}] &`;
      }
      return `${selector.replace("%s", colorScheme)} &`;
    }
    return "&";
  };
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/Container/createContainer.js
var React19 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/composeClasses/composeClasses.js
function composeClasses(slots, getUtilityClass, classes = void 0) {
  const output = {};
  for (const slotName in slots) {
    const slot = slots[slotName];
    let buffer = "";
    let start2 = true;
    for (let i = 0; i < slot.length; i += 1) {
      const value = slot[i];
      if (value) {
        buffer += (start2 === true ? "" : " ") + getUtilityClass(value);
        start2 = false;
        if (classes && classes[value]) {
          buffer += " " + classes[value];
        }
      }
    }
    output[slotName] = buffer;
  }
  return output;
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/Container/createContainer.js
var import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
var defaultTheme = createTheme_default();
var defaultCreateStyledComponent = styled_default("div", {
  name: "MuiContainer",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      styles5[`maxWidth${capitalize(String(ownerState.maxWidth))}`],
      ownerState.fixed && styles5.fixed,
      ownerState.disableGutters && styles5.disableGutters,
    ];
  },
});
var useThemePropsDefault = (inProps) =>
  useThemeProps({
    props: inProps,
    name: "MuiContainer",
    defaultTheme,
  });
var useUtilityClasses = (ownerState, componentName) => {
  const getContainerUtilityClass = (slot) => {
    return generateUtilityClass(componentName, slot);
  };
  const { classes, fixed, disableGutters, maxWidth: maxWidth2 } = ownerState;
  const slots = {
    root: [
      "root",
      maxWidth2 && `maxWidth${capitalize(String(maxWidth2))}`,
      fixed && "fixed",
      disableGutters && "disableGutters",
    ],
  };
  return composeClasses(slots, getContainerUtilityClass, classes);
};
function createContainer(options = {}) {
  const {
    // This will allow adding custom styled fn (for example for custom sx style function)
    createStyledComponent = defaultCreateStyledComponent,
    useThemeProps: useThemeProps2 = useThemePropsDefault,
    componentName = "MuiContainer",
  } = options;
  const ContainerRoot = createStyledComponent(
    ({ theme: theme2, ownerState }) => ({
      width: "100%",
      marginLeft: "auto",
      boxSizing: "border-box",
      marginRight: "auto",
      ...(!ownerState.disableGutters && {
        paddingLeft: theme2.spacing(2),
        paddingRight: theme2.spacing(2),
        // @ts-ignore module augmentation fails if custom breakpoints are used
        [theme2.breakpoints.up("sm")]: {
          paddingLeft: theme2.spacing(3),
          paddingRight: theme2.spacing(3),
        },
      }),
    }),
    ({ theme: theme2, ownerState }) =>
      ownerState.fixed &&
      Object.keys(theme2.breakpoints.values).reduce(
        (acc, breakpointValueKey) => {
          const breakpoint = breakpointValueKey;
          const value = theme2.breakpoints.values[breakpoint];
          if (value !== 0) {
            acc[theme2.breakpoints.up(breakpoint)] = {
              maxWidth: `${value}${theme2.breakpoints.unit}`,
            };
          }
          return acc;
        },
        {},
      ),
    ({ theme: theme2, ownerState }) => ({
      // @ts-ignore module augmentation fails if custom breakpoints are used
      ...(ownerState.maxWidth === "xs" && {
        // @ts-ignore module augmentation fails if custom breakpoints are used
        [theme2.breakpoints.up("xs")]: {
          // @ts-ignore module augmentation fails if custom breakpoints are used
          maxWidth: Math.max(theme2.breakpoints.values.xs, 444),
        },
      }),
      ...(ownerState.maxWidth && // @ts-ignore module augmentation fails if custom breakpoints are used
        ownerState.maxWidth !== "xs" && {
          // @ts-ignore module augmentation fails if custom breakpoints are used
          [theme2.breakpoints.up(ownerState.maxWidth)]: {
            // @ts-ignore module augmentation fails if custom breakpoints are used
            maxWidth: `${theme2.breakpoints.values[ownerState.maxWidth]}${theme2.breakpoints.unit}`,
          },
        }),
    }),
  );
  const Container2 = /* @__PURE__ */ React19.forwardRef(
    function Container3(inProps, ref) {
      const props = useThemeProps2(inProps);
      const {
        className,
        component = "div",
        disableGutters = false,
        fixed = false,
        maxWidth: maxWidth2 = "lg",
        classes: classesProp,
        ...other
      } = props;
      const ownerState = {
        ...props,
        component,
        disableGutters,
        fixed,
        maxWidth: maxWidth2,
      };
      const classes = useUtilityClasses(ownerState, componentName);
      return (
        // @ts-ignore theme is injected by the styled util
        /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(ContainerRoot, {
          as: component,
          ownerState,
          className: clsx_default(classes.root, className),
          ref,
          ...other,
        })
      );
    },
  );
  false
    ? (Container2.propTypes = {
        children: import_prop_types.default.node,
        classes: import_prop_types.default.object,
        className: import_prop_types.default.string,
        component: import_prop_types.default.elementType,
        disableGutters: import_prop_types.default.bool,
        fixed: import_prop_types.default.bool,
        maxWidth: import_prop_types.default.oneOfType([
          import_prop_types.default.oneOf([
            "xs",
            "sm",
            "md",
            "lg",
            "xl",
            false,
          ]),
          import_prop_types.default.string,
        ]),
        sx: import_prop_types.default.oneOfType([
          import_prop_types.default.arrayOf(
            import_prop_types.default.oneOfType([
              import_prop_types.default.func,
              import_prop_types.default.object,
              import_prop_types.default.bool,
            ]),
          ),
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      })
    : void 0;
  return Container2;
}

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/isMuiElement/isMuiElement.js
var React20 = __toESM(require_react(), 1);
function isMuiElement(element, muiNames) {
  return (
    /* @__PURE__ */ React20.isValidElement(element) &&
    muiNames.indexOf(
      // For server components `muiName` is available in element.type._payload.value.muiName
      // relevant info - https://github.com/facebook/react/blob/2807d781a08db8e9873687fccc25c0f12b4fb3d4/packages/react/src/ReactLazy.js#L45
      // eslint-disable-next-line no-underscore-dangle
      element.type.muiName ?? element.type?._payload?.value?.muiName,
    ) !== -1
  );
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/Stack/createStack.js
var React21 = __toESM(require_react(), 1);
var import_jsx_runtime12 = __toESM(require_jsx_runtime(), 1);
var defaultTheme2 = createTheme_default();
var defaultCreateStyledComponent2 = styled_default("div", {
  name: "MuiStack",
  slot: "Root",
});
function useThemePropsDefault2(props) {
  return useThemeProps({
    props,
    name: "MuiStack",
    defaultTheme: defaultTheme2,
  });
}
function joinChildren(children, separator) {
  const childrenArray = React21.Children.toArray(children).filter(Boolean);
  return childrenArray.reduce((output, child, index) => {
    output.push(child);
    if (index < childrenArray.length - 1) {
      output.push(
        /* @__PURE__ */ React21.cloneElement(separator, {
          key: `separator-${index}`,
        }),
      );
    }
    return output;
  }, []);
}
var getSideFromDirection = (direction) => {
  return {
    row: "Left",
    "row-reverse": "Right",
    column: "Top",
    "column-reverse": "Bottom",
  }[direction];
};
var style3 = ({ ownerState, theme: theme2 }) => {
  let styles5 = {
    display: "flex",
    flexDirection: "column",
    ...handleBreakpoints(
      {
        theme: theme2,
      },
      resolveBreakpointValues({
        values: ownerState.direction,
        breakpoints: theme2.breakpoints.values,
      }),
      (propValue) => ({
        flexDirection: propValue,
      }),
    ),
  };
  if (ownerState.spacing) {
    const transformer = createUnarySpacing(theme2);
    const base = Object.keys(theme2.breakpoints.values).reduce(
      (acc, breakpoint) => {
        if (
          (typeof ownerState.spacing === "object" &&
            ownerState.spacing[breakpoint] != null) ||
          (typeof ownerState.direction === "object" &&
            ownerState.direction[breakpoint] != null)
        ) {
          acc[breakpoint] = true;
        }
        return acc;
      },
      {},
    );
    const directionValues = resolveBreakpointValues({
      values: ownerState.direction,
      base,
    });
    const spacingValues = resolveBreakpointValues({
      values: ownerState.spacing,
      base,
    });
    if (typeof directionValues === "object") {
      Object.keys(directionValues).forEach((breakpoint, index, breakpoints) => {
        const directionValue = directionValues[breakpoint];
        if (!directionValue) {
          const previousDirectionValue =
            index > 0 ? directionValues[breakpoints[index - 1]] : "column";
          directionValues[breakpoint] = previousDirectionValue;
        }
      });
    }
    const styleFromPropValue = (propValue, breakpoint) => {
      if (ownerState.useFlexGap) {
        return {
          gap: getValue(transformer, propValue),
        };
      }
      return {
        // The useFlexGap={false} implement relies on each child to give up control of the margin.
        // We need to reset the margin to avoid double spacing.
        "& > :not(style):not(style)": {
          margin: 0,
        },
        "& > :not(style) ~ :not(style)": {
          [`margin${getSideFromDirection(breakpoint ? directionValues[breakpoint] : ownerState.direction)}`]:
            getValue(transformer, propValue),
        },
      };
    };
    styles5 = deepmerge(
      styles5,
      handleBreakpoints(
        {
          theme: theme2,
        },
        spacingValues,
        styleFromPropValue,
      ),
    );
  }
  styles5 = mergeBreakpointsInOrder(theme2.breakpoints, styles5);
  return styles5;
};
function createStack(options = {}) {
  const {
    // This will allow adding custom styled fn (for example for custom sx style function)
    createStyledComponent = defaultCreateStyledComponent2,
    useThemeProps: useThemeProps2 = useThemePropsDefault2,
    componentName = "MuiStack",
  } = options;
  const useUtilityClasses38 = () => {
    const slots = {
      root: ["root"],
    };
    return composeClasses(
      slots,
      (slot) => generateUtilityClass(componentName, slot),
      {},
    );
  };
  const StackRoot = createStyledComponent(style3);
  const Stack2 = /* @__PURE__ */ React21.forwardRef(
    function Grid(inProps, ref) {
      const themeProps = useThemeProps2(inProps);
      const props = extendSxProp(themeProps);
      const {
        component = "div",
        direction = "column",
        spacing: spacing2 = 0,
        divider,
        children,
        className,
        useFlexGap = false,
        ...other
      } = props;
      const ownerState = {
        direction,
        spacing: spacing2,
        useFlexGap,
      };
      const classes = useUtilityClasses38();
      return /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(StackRoot, {
        as: component,
        ownerState,
        ref,
        className: clsx_default(classes.root, className),
        ...other,
        children: divider ? joinChildren(children, divider) : children,
      });
    },
  );
  false
    ? (Stack2.propTypes = {
        children: import_prop_types.default.node,
        direction: import_prop_types.default.oneOfType([
          import_prop_types.default.oneOf([
            "column-reverse",
            "column",
            "row-reverse",
            "row",
          ]),
          import_prop_types.default.arrayOf(
            import_prop_types.default.oneOf([
              "column-reverse",
              "column",
              "row-reverse",
              "row",
            ]),
          ),
          import_prop_types.default.object,
        ]),
        divider: import_prop_types.default.node,
        spacing: import_prop_types.default.oneOfType([
          import_prop_types.default.arrayOf(
            import_prop_types.default.oneOfType([
              import_prop_types.default.number,
              import_prop_types.default.string,
            ]),
          ),
          import_prop_types.default.number,
          import_prop_types.default.object,
          import_prop_types.default.string,
        ]),
        sx: import_prop_types.default.oneOfType([
          import_prop_types.default.arrayOf(
            import_prop_types.default.oneOfType([
              import_prop_types.default.func,
              import_prop_types.default.object,
              import_prop_types.default.bool,
            ]),
          ),
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      })
    : void 0;
  return Stack2;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/createPalette.js
function getLight() {
  return {
    // The colors used to style the text.
    text: {
      // The most important text.
      primary: "rgba(0, 0, 0, 0.87)",
      // Secondary text.
      secondary: "rgba(0, 0, 0, 0.6)",
      // Disabled text have even lower visual prominence.
      disabled: "rgba(0, 0, 0, 0.38)",
    },
    // The color used to divide different elements.
    divider: "rgba(0, 0, 0, 0.12)",
    // The background colors used to style the surfaces.
    // Consistency between these values is important.
    background: {
      paper: common_default.white,
      default: common_default.white,
    },
    // The colors used to style the action elements.
    action: {
      // The color of an active action like an icon button.
      active: "rgba(0, 0, 0, 0.54)",
      // The color of an hovered action.
      hover: "rgba(0, 0, 0, 0.04)",
      hoverOpacity: 0.04,
      // The color of a selected action.
      selected: "rgba(0, 0, 0, 0.08)",
      selectedOpacity: 0.08,
      // The color of a disabled action.
      disabled: "rgba(0, 0, 0, 0.26)",
      // The background color of a disabled action.
      disabledBackground: "rgba(0, 0, 0, 0.12)",
      disabledOpacity: 0.38,
      focus: "rgba(0, 0, 0, 0.12)",
      focusOpacity: 0.12,
      activatedOpacity: 0.12,
    },
  };
}
var light = getLight();
function getDark() {
  return {
    text: {
      primary: common_default.white,
      secondary: "rgba(255, 255, 255, 0.7)",
      disabled: "rgba(255, 255, 255, 0.5)",
      icon: "rgba(255, 255, 255, 0.5)",
    },
    divider: "rgba(255, 255, 255, 0.12)",
    background: {
      paper: "#121212",
      default: "#121212",
    },
    action: {
      active: common_default.white,
      hover: "rgba(255, 255, 255, 0.08)",
      hoverOpacity: 0.08,
      selected: "rgba(255, 255, 255, 0.16)",
      selectedOpacity: 0.16,
      disabled: "rgba(255, 255, 255, 0.3)",
      disabledBackground: "rgba(255, 255, 255, 0.12)",
      disabledOpacity: 0.38,
      focus: "rgba(255, 255, 255, 0.12)",
      focusOpacity: 0.12,
      activatedOpacity: 0.24,
    },
  };
}
var dark = getDark();
function addLightOrDark(intent, direction, shade, tonalOffset) {
  const tonalOffsetLight = tonalOffset.light || tonalOffset;
  const tonalOffsetDark = tonalOffset.dark || tonalOffset * 1.5;
  if (!intent[direction]) {
    if (intent.hasOwnProperty(shade)) {
      intent[direction] = intent[shade];
    } else if (direction === "light") {
      intent.light = lighten(intent.main, tonalOffsetLight);
    } else if (direction === "dark") {
      intent.dark = darken(intent.main, tonalOffsetDark);
    }
  }
}
function mixLightOrDark(colorSpace, intent, direction, shade, tonalOffset) {
  const tonalOffsetLight = tonalOffset.light || tonalOffset;
  const tonalOffsetDark = tonalOffset.dark || tonalOffset * 1.5;
  if (!intent[direction]) {
    if (intent.hasOwnProperty(shade)) {
      intent[direction] = intent[shade];
    } else if (direction === "light") {
      intent.light = `color-mix(in ${colorSpace}, ${intent.main}, #fff ${(tonalOffsetLight * 100).toFixed(0)}%)`;
    } else if (direction === "dark") {
      intent.dark = `color-mix(in ${colorSpace}, ${intent.main}, #000 ${(tonalOffsetDark * 100).toFixed(0)}%)`;
    }
  }
}
function getDefaultPrimary(mode = "light") {
  if (mode === "dark") {
    return {
      main: blue_default[200],
      light: blue_default[50],
      dark: blue_default[400],
    };
  }
  return {
    main: blue_default[700],
    light: blue_default[400],
    dark: blue_default[800],
  };
}
function getDefaultSecondary(mode = "light") {
  if (mode === "dark") {
    return {
      main: purple_default[200],
      light: purple_default[50],
      dark: purple_default[400],
    };
  }
  return {
    main: purple_default[500],
    light: purple_default[300],
    dark: purple_default[700],
  };
}
function getDefaultError(mode = "light") {
  if (mode === "dark") {
    return {
      main: red_default[500],
      light: red_default[300],
      dark: red_default[700],
    };
  }
  return {
    main: red_default[700],
    light: red_default[400],
    dark: red_default[800],
  };
}
function getDefaultInfo(mode = "light") {
  if (mode === "dark") {
    return {
      main: lightBlue_default[400],
      light: lightBlue_default[300],
      dark: lightBlue_default[700],
    };
  }
  return {
    main: lightBlue_default[700],
    light: lightBlue_default[500],
    dark: lightBlue_default[900],
  };
}
function getDefaultSuccess(mode = "light") {
  if (mode === "dark") {
    return {
      main: green_default[400],
      light: green_default[300],
      dark: green_default[700],
    };
  }
  return {
    main: green_default[800],
    light: green_default[500],
    dark: green_default[900],
  };
}
function getDefaultWarning(mode = "light") {
  if (mode === "dark") {
    return {
      main: orange_default[400],
      light: orange_default[300],
      dark: orange_default[700],
    };
  }
  return {
    main: "#ed6c02",
    // closest to orange[800] that pass 3:1.
    light: orange_default[500],
    dark: orange_default[900],
  };
}
function contrastColor(background) {
  return `oklch(from ${background} var(--__l) 0 h / var(--__a))`;
}
function createPalette(palette2) {
  const {
    mode = "light",
    contrastThreshold = 3,
    tonalOffset = 0.2,
    colorSpace,
    ...other
  } = palette2;
  const primary = palette2.primary || getDefaultPrimary(mode);
  const secondary = palette2.secondary || getDefaultSecondary(mode);
  const error = palette2.error || getDefaultError(mode);
  const info = palette2.info || getDefaultInfo(mode);
  const success = palette2.success || getDefaultSuccess(mode);
  const warning = palette2.warning || getDefaultWarning(mode);
  function getContrastText(background) {
    if (colorSpace) {
      return contrastColor(background);
    }
    const contrastText =
      getContrastRatio(background, dark.text.primary) >= contrastThreshold
        ? dark.text.primary
        : light.text.primary;
    if (false) {
      const contrast = getContrastRatio(background, contrastText);
      if (contrast < 3) {
        console.error(
          [
            `MUI: The contrast ratio of ${contrast}:1 for ${contrastText} on ${background}`,
            "falls below the WCAG recommended absolute minimum contrast ratio of 3:1.",
            "https://www.w3.org/TR/2008/REC-WCAG20-20081211/#visual-audio-contrast-contrast",
          ].join("\n"),
        );
      }
    }
    return contrastText;
  }
  const augmentColor = ({
    color: color2,
    name,
    mainShade = 500,
    lightShade = 300,
    darkShade = 700,
  }) => {
    color2 = {
      ...color2,
    };
    if (!color2.main && color2[mainShade]) {
      color2.main = color2[mainShade];
    }
    if (!color2.hasOwnProperty("main")) {
      throw new Error(
        false
          ? `MUI: The color${name ? ` (${name})` : ""} provided to augmentColor(color) is invalid.
The color object needs to have a \`main\` property or a \`${mainShade}\` property.`
          : formatMuiErrorMessage(11, name ? ` (${name})` : "", mainShade),
      );
    }
    if (typeof color2.main !== "string") {
      throw new Error(
        false
          ? `MUI: The color${name ? ` (${name})` : ""} provided to augmentColor(color) is invalid.
\`color.main\` should be a string, but \`${JSON.stringify(color2.main)}\` was provided instead.

Did you intend to use one of the following approaches?

import { green } from "@mui/material/colors";

const theme1 = createTheme({ palette: {
  primary: green,
} });

const theme2 = createTheme({ palette: {
  primary: { main: green[500] },
} });`
          : formatMuiErrorMessage(
              12,
              name ? ` (${name})` : "",
              JSON.stringify(color2.main),
            ),
      );
    }
    if (colorSpace) {
      mixLightOrDark(colorSpace, color2, "light", lightShade, tonalOffset);
      mixLightOrDark(colorSpace, color2, "dark", darkShade, tonalOffset);
    } else {
      addLightOrDark(color2, "light", lightShade, tonalOffset);
      addLightOrDark(color2, "dark", darkShade, tonalOffset);
    }
    if (!color2.contrastText) {
      color2.contrastText = getContrastText(color2.main);
    }
    return color2;
  };
  let modeHydrated;
  if (mode === "light") {
    modeHydrated = getLight();
  } else if (mode === "dark") {
    modeHydrated = getDark();
  }
  if (false) {
    if (!modeHydrated) {
      console.error(`MUI: The palette mode \`${mode}\` is not supported.`);
    }
  }
  const paletteOutput = deepmerge(
    {
      // A collection of common colors.
      common: {
        ...common_default,
      },
      // prevent mutable object.
      // The palette mode, can be light or dark.
      mode,
      // The colors used to represent primary interface elements for a user.
      primary: augmentColor({
        color: primary,
        name: "primary",
      }),
      // The colors used to represent secondary interface elements for a user.
      secondary: augmentColor({
        color: secondary,
        name: "secondary",
        mainShade: "A400",
        lightShade: "A200",
        darkShade: "A700",
      }),
      // The colors used to represent interface elements that the user should be made aware of.
      error: augmentColor({
        color: error,
        name: "error",
      }),
      // The colors used to represent potentially dangerous actions or important messages.
      warning: augmentColor({
        color: warning,
        name: "warning",
      }),
      // The colors used to present information to the user that is neutral and not necessarily important.
      info: augmentColor({
        color: info,
        name: "info",
      }),
      // The colors used to indicate the successful completion of an action that user triggered.
      success: augmentColor({
        color: success,
        name: "success",
      }),
      // The grey colors.
      grey: grey_default,
      // Used by `getContrastText()` to maximize the contrast between
      // the background and the text.
      contrastThreshold,
      // Takes a background color and returns the text color that maximizes the contrast.
      getContrastText,
      // Generate a rich color object.
      augmentColor,
      // Used by the functions below to shift a color's luminance by approximately
      // two indexes within its tonal palette.
      // E.g., shift from Red 500 to Red 300 or Red 700.
      tonalOffset,
      // The light and dark mode object.
      ...modeHydrated,
    },
    other,
  );
  return paletteOutput;
}

// node_modules/.pnpm/@mui+system@7.3.11_@emotion_23ec257031448fd18ce096112900c8b8/node_modules/@mui/system/esm/cssVars/prepareTypographyVars.js
function prepareTypographyVars(typography) {
  const vars = {};
  const entries = Object.entries(typography);
  entries.forEach((entry) => {
    const [key, value] = entry;
    if (typeof value === "object") {
      vars[key] =
        `${value.fontStyle ? `${value.fontStyle} ` : ""}${value.fontVariant ? `${value.fontVariant} ` : ""}${value.fontWeight ? `${value.fontWeight} ` : ""}${value.fontStretch ? `${value.fontStretch} ` : ""}${value.fontSize || ""}${value.lineHeight ? `/${value.lineHeight} ` : ""}${value.fontFamily || ""}`;
    }
  });
  return vars;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/createMixins.js
function createMixins(breakpoints, mixins) {
  return {
    toolbar: {
      minHeight: 56,
      [breakpoints.up("xs")]: {
        "@media (orientation: landscape)": {
          minHeight: 48,
        },
      },
      [breakpoints.up("sm")]: {
        minHeight: 64,
      },
    },
    ...mixins,
  };
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/createTypography.js
function round(value) {
  return Math.round(value * 1e5) / 1e5;
}
var caseAllCaps = {
  textTransform: "uppercase",
};
var defaultFontFamily = '"Roboto", "Helvetica", "Arial", sans-serif';
function createTypography(palette2, typography) {
  const {
    fontFamily: fontFamily2 = defaultFontFamily,
    // The default font size of the Material Specification.
    fontSize = 14,
    // px
    fontWeightLight = 300,
    fontWeightRegular = 400,
    fontWeightMedium = 500,
    fontWeightBold = 700,
    // Tell MUI what's the font-size on the html element.
    // 16px is the default font-size used by browsers.
    htmlFontSize = 16,
    // Apply the CSS properties to all the variants.
    allVariants,
    pxToRem: pxToRem2,
    ...other
  } = typeof typography === "function" ? typography(palette2) : typography;
  if (false) {
    if (typeof fontSize !== "number") {
      console.error("MUI: `fontSize` is required to be a number.");
    }
    if (typeof htmlFontSize !== "number") {
      console.error("MUI: `htmlFontSize` is required to be a number.");
    }
  }
  const coef = fontSize / 14;
  const pxToRem = pxToRem2 || ((size) => `${(size / htmlFontSize) * coef}rem`);
  const buildVariant = (
    fontWeight,
    size,
    lineHeight,
    letterSpacing,
    casing,
  ) => ({
    fontFamily: fontFamily2,
    fontWeight,
    fontSize: pxToRem(size),
    // Unitless following https://meyerweb.com/eric/thoughts/2006/02/08/unitless-line-heights/
    lineHeight,
    // The letter spacing was designed for the Roboto font-family. Using the same letter-spacing
    // across font-families can cause issues with the kerning.
    ...(fontFamily2 === defaultFontFamily
      ? {
          letterSpacing: `${round(letterSpacing / size)}em`,
        }
      : {}),
    ...casing,
    ...allVariants,
  });
  const variants = {
    h1: buildVariant(fontWeightLight, 96, 1.167, -1.5),
    h2: buildVariant(fontWeightLight, 60, 1.2, -0.5),
    h3: buildVariant(fontWeightRegular, 48, 1.167, 0),
    h4: buildVariant(fontWeightRegular, 34, 1.235, 0.25),
    h5: buildVariant(fontWeightRegular, 24, 1.334, 0),
    h6: buildVariant(fontWeightMedium, 20, 1.6, 0.15),
    subtitle1: buildVariant(fontWeightRegular, 16, 1.75, 0.15),
    subtitle2: buildVariant(fontWeightMedium, 14, 1.57, 0.1),
    body1: buildVariant(fontWeightRegular, 16, 1.5, 0.15),
    body2: buildVariant(fontWeightRegular, 14, 1.43, 0.15),
    button: buildVariant(fontWeightMedium, 14, 1.75, 0.4, caseAllCaps),
    caption: buildVariant(fontWeightRegular, 12, 1.66, 0.4),
    overline: buildVariant(fontWeightRegular, 12, 2.66, 1, caseAllCaps),
    // TODO v6: Remove handling of 'inherit' variant from the theme as it is already handled in Material UI's Typography component. Also, remember to remove the associated types.
    inherit: {
      fontFamily: "inherit",
      fontWeight: "inherit",
      fontSize: "inherit",
      lineHeight: "inherit",
      letterSpacing: "inherit",
    },
  };
  return deepmerge(
    {
      htmlFontSize,
      pxToRem,
      fontFamily: fontFamily2,
      fontSize,
      fontWeightLight,
      fontWeightRegular,
      fontWeightMedium,
      fontWeightBold,
      ...variants,
    },
    other,
    {
      clone: false,
      // No need to clone deep
    },
  );
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/shadows.js
var shadowKeyUmbraOpacity = 0.2;
var shadowKeyPenumbraOpacity = 0.14;
var shadowAmbientShadowOpacity = 0.12;
function createShadow(...px3) {
  return [
    `${px3[0]}px ${px3[1]}px ${px3[2]}px ${px3[3]}px rgba(0,0,0,${shadowKeyUmbraOpacity})`,
    `${px3[4]}px ${px3[5]}px ${px3[6]}px ${px3[7]}px rgba(0,0,0,${shadowKeyPenumbraOpacity})`,
    `${px3[8]}px ${px3[9]}px ${px3[10]}px ${px3[11]}px rgba(0,0,0,${shadowAmbientShadowOpacity})`,
  ].join(",");
}
var shadows = [
  "none",
  createShadow(0, 2, 1, -1, 0, 1, 1, 0, 0, 1, 3, 0),
  createShadow(0, 3, 1, -2, 0, 2, 2, 0, 0, 1, 5, 0),
  createShadow(0, 3, 3, -2, 0, 3, 4, 0, 0, 1, 8, 0),
  createShadow(0, 2, 4, -1, 0, 4, 5, 0, 0, 1, 10, 0),
  createShadow(0, 3, 5, -1, 0, 5, 8, 0, 0, 1, 14, 0),
  createShadow(0, 3, 5, -1, 0, 6, 10, 0, 0, 1, 18, 0),
  createShadow(0, 4, 5, -2, 0, 7, 10, 1, 0, 2, 16, 1),
  createShadow(0, 5, 5, -3, 0, 8, 10, 1, 0, 3, 14, 2),
  createShadow(0, 5, 6, -3, 0, 9, 12, 1, 0, 3, 16, 2),
  createShadow(0, 6, 6, -3, 0, 10, 14, 1, 0, 4, 18, 3),
  createShadow(0, 6, 7, -4, 0, 11, 15, 1, 0, 4, 20, 3),
  createShadow(0, 7, 8, -4, 0, 12, 17, 2, 0, 5, 22, 4),
  createShadow(0, 7, 8, -4, 0, 13, 19, 2, 0, 5, 24, 4),
  createShadow(0, 7, 9, -4, 0, 14, 21, 2, 0, 5, 26, 4),
  createShadow(0, 8, 9, -5, 0, 15, 22, 2, 0, 6, 28, 5),
  createShadow(0, 8, 10, -5, 0, 16, 24, 2, 0, 6, 30, 5),
  createShadow(0, 8, 11, -5, 0, 17, 26, 2, 0, 6, 32, 5),
  createShadow(0, 9, 11, -5, 0, 18, 28, 2, 0, 7, 34, 6),
  createShadow(0, 9, 12, -6, 0, 19, 29, 2, 0, 7, 36, 6),
  createShadow(0, 10, 13, -6, 0, 20, 31, 3, 0, 8, 38, 7),
  createShadow(0, 10, 13, -6, 0, 21, 33, 3, 0, 8, 40, 7),
  createShadow(0, 10, 14, -6, 0, 22, 35, 3, 0, 8, 42, 7),
  createShadow(0, 11, 14, -7, 0, 23, 36, 3, 0, 9, 44, 8),
  createShadow(0, 11, 15, -7, 0, 24, 38, 3, 0, 9, 46, 8),
];
var shadows_default = shadows;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/createTransitions.js
var easing = {
  // This is the most common easing curve.
  easeInOut: "cubic-bezier(0.4, 0, 0.2, 1)",
  // Objects enter the screen at full velocity from off-screen and
  // slowly decelerate to a resting point.
  easeOut: "cubic-bezier(0.0, 0, 0.2, 1)",
  // Objects leave the screen at full velocity. They do not decelerate when off-screen.
  easeIn: "cubic-bezier(0.4, 0, 1, 1)",
  // The sharp curve is used by objects that may return to the screen at any time.
  sharp: "cubic-bezier(0.4, 0, 0.6, 1)",
};
var duration = {
  shortest: 150,
  shorter: 200,
  short: 250,
  // most basic recommended timing
  standard: 300,
  // this is to be used in complex animations
  complex: 375,
  // recommended when something is entering screen
  enteringScreen: 225,
  // recommended when something is leaving screen
  leavingScreen: 195,
};
function formatMs(milliseconds) {
  return `${Math.round(milliseconds)}ms`;
}
function getAutoHeightDuration(height2) {
  if (!height2) {
    return 0;
  }
  const constant = height2 / 36;
  return Math.min(
    Math.round((4 + 15 * constant ** 0.25 + constant / 5) * 10),
    3e3,
  );
}
function createTransitions(inputTransitions) {
  const mergedEasing = {
    ...easing,
    ...inputTransitions.easing,
  };
  const mergedDuration = {
    ...duration,
    ...inputTransitions.duration,
  };
  const create = (props = ["all"], options = {}) => {
    const {
      duration: durationOption = mergedDuration.standard,
      easing: easingOption = mergedEasing.easeInOut,
      delay = 0,
      ...other
    } = options;
    if (false) {
      const isString = (value) => typeof value === "string";
      const isNumber = (value) => !Number.isNaN(parseFloat(value));
      if (!isString(props) && !Array.isArray(props)) {
        console.error('MUI: Argument "props" must be a string or Array.');
      }
      if (!isNumber(durationOption) && !isString(durationOption)) {
        console.error(
          `MUI: Argument "duration" must be a number or a string but found ${durationOption}.`,
        );
      }
      if (!isString(easingOption)) {
        console.error('MUI: Argument "easing" must be a string.');
      }
      if (!isNumber(delay) && !isString(delay)) {
        console.error('MUI: Argument "delay" must be a number or a string.');
      }
      if (typeof options !== "object") {
        console.error(
          [
            "MUI: Secong argument of transition.create must be an object.",
            "Arguments should be either `create('prop1', options)` or `create(['prop1', 'prop2'], options)`",
          ].join("\n"),
        );
      }
      if (Object.keys(other).length !== 0) {
        console.error(
          `MUI: Unrecognized argument(s) [${Object.keys(other).join(",")}].`,
        );
      }
    }
    return (Array.isArray(props) ? props : [props])
      .map(
        (animatedProp) =>
          `${animatedProp} ${typeof durationOption === "string" ? durationOption : formatMs(durationOption)} ${easingOption} ${typeof delay === "string" ? delay : formatMs(delay)}`,
      )
      .join(",");
  };
  return {
    getAutoHeightDuration,
    create,
    ...inputTransitions,
    easing: mergedEasing,
    duration: mergedDuration,
  };
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/zIndex.js
var zIndex = {
  mobileStepper: 1e3,
  fab: 1050,
  speedDial: 1050,
  appBar: 1100,
  drawer: 1200,
  modal: 1300,
  snackbar: 1400,
  tooltip: 1500,
};
var zIndex_default = zIndex;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/stringifyTheme.js
function isSerializable(val) {
  return (
    isPlainObject(val) ||
    typeof val === "undefined" ||
    typeof val === "string" ||
    typeof val === "boolean" ||
    typeof val === "number" ||
    Array.isArray(val)
  );
}
function stringifyTheme(baseTheme = {}) {
  const serializableTheme = {
    ...baseTheme,
  };
  function serializeTheme(object) {
    const array = Object.entries(object);
    for (let index = 0; index < array.length; index++) {
      const [key, value] = array[index];
      if (!isSerializable(value) || key.startsWith("unstable_")) {
        delete object[key];
      } else if (isPlainObject(value)) {
        object[key] = {
          ...value,
        };
        serializeTheme(object[key]);
      }
    }
  }
  serializeTheme(serializableTheme);
  return `import { unstable_createBreakpoints as createBreakpoints, createTransitions } from '@mui/material/styles';

const theme = ${JSON.stringify(serializableTheme, null, 2)};

theme.breakpoints = createBreakpoints(theme.breakpoints || {});
theme.transitions = createTransitions(theme.transitions || {});

export default theme;`;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/createThemeNoVars.js
function coefficientToPercentage(coefficient) {
  if (typeof coefficient === "number") {
    return `${(coefficient * 100).toFixed(0)}%`;
  }
  return `calc((${coefficient}) * 100%)`;
}
var parseAddition = (str) => {
  if (!Number.isNaN(+str)) {
    return +str;
  }
  const numbers = str.match(/\d*\.?\d+/g);
  if (!numbers) {
    return 0;
  }
  let sum = 0;
  for (let i = 0; i < numbers.length; i += 1) {
    sum += +numbers[i];
  }
  return sum;
};
function attachColorManipulators(theme2) {
  Object.assign(theme2, {
    alpha(color2, coefficient) {
      const obj = this || theme2;
      if (obj.colorSpace) {
        return `oklch(from ${color2} l c h / ${typeof coefficient === "string" ? `calc(${coefficient})` : coefficient})`;
      }
      if (obj.vars) {
        return `rgba(${color2.replace(/var\(--([^,\s)]+)(?:,[^)]+)?\)+/g, "var(--$1Channel)")} / ${typeof coefficient === "string" ? `calc(${coefficient})` : coefficient})`;
      }
      return alpha(color2, parseAddition(coefficient));
    },
    lighten(color2, coefficient) {
      const obj = this || theme2;
      if (obj.colorSpace) {
        return `color-mix(in ${obj.colorSpace}, ${color2}, #fff ${coefficientToPercentage(coefficient)})`;
      }
      return lighten(color2, coefficient);
    },
    darken(color2, coefficient) {
      const obj = this || theme2;
      if (obj.colorSpace) {
        return `color-mix(in ${obj.colorSpace}, ${color2}, #000 ${coefficientToPercentage(coefficient)})`;
      }
      return darken(color2, coefficient);
    },
  });
}
function createThemeNoVars(options = {}, ...args) {
  const {
    breakpoints: breakpointsInput,
    mixins: mixinsInput = {},
    spacing: spacingInput,
    palette: paletteInput = {},
    transitions: transitionsInput = {},
    typography: typographyInput = {},
    shape: shapeInput,
    colorSpace,
    ...other
  } = options;
  if (
    options.vars && // The error should throw only for the root theme creation because user is not allowed to use a custom node `vars`.
    // `generateThemeVars` is the closest identifier for checking that the `options` is a result of `createTheme` with CSS variables so that user can create new theme for nested ThemeProvider.
    options.generateThemeVars === void 0
  ) {
    throw new Error(
      false
        ? "MUI: `vars` is a private field used for CSS variables support.\nPlease use another name or follow the [docs](https://v7.mui.com/material-ui/customization/css-theme-variables/usage/) to enable the feature."
        : formatMuiErrorMessage(20),
    );
  }
  const palette2 = createPalette({
    ...paletteInput,
    colorSpace,
  });
  const systemTheme = createTheme_default(options);
  let muiTheme = deepmerge(systemTheme, {
    mixins: createMixins(systemTheme.breakpoints, mixinsInput),
    palette: palette2,
    // Don't use [...shadows] until you've verified its transpiled code is not invoking the iterator protocol.
    shadows: shadows_default.slice(),
    typography: createTypography(palette2, typographyInput),
    transitions: createTransitions(transitionsInput),
    zIndex: {
      ...zIndex_default,
    },
  });
  muiTheme = deepmerge(muiTheme, other);
  muiTheme = args.reduce((acc, argument) => deepmerge(acc, argument), muiTheme);
  if (false) {
    const stateClasses = [
      "active",
      "checked",
      "completed",
      "disabled",
      "error",
      "expanded",
      "focused",
      "focusVisible",
      "required",
      "selected",
    ];
    const traverse = (node2, component) => {
      let key;
      for (key in node2) {
        const child = node2[key];
        if (stateClasses.includes(key) && Object.keys(child).length > 0) {
          if (false) {
            const stateClass = generateUtilityClass("", key);
            console.error(
              [
                `MUI: The \`${component}\` component increases the CSS specificity of the \`${key}\` internal state.`,
                "You can not override it like this: ",
                JSON.stringify(node2, null, 2),
                "",
                `Instead, you need to use the '&.${stateClass}' syntax:`,
                JSON.stringify(
                  {
                    root: {
                      [`&.${stateClass}`]: child,
                    },
                  },
                  null,
                  2,
                ),
                "",
                "https://mui.com/r/state-classes-guide",
              ].join("\n"),
            );
          }
          node2[key] = {};
        }
      }
    };
    Object.keys(muiTheme.components).forEach((component) => {
      const styleOverrides = muiTheme.components[component].styleOverrides;
      if (styleOverrides && component.startsWith("Mui")) {
        traverse(styleOverrides, component);
      }
    });
  }
  muiTheme.unstable_sxConfig = {
    ...defaultSxConfig_default,
    ...other?.unstable_sxConfig,
  };
  muiTheme.unstable_sx = function sx(props) {
    return styleFunctionSx_default({
      sx: props,
      theme: this,
    });
  };
  muiTheme.toRuntimeSource = stringifyTheme;
  attachColorManipulators(muiTheme);
  return muiTheme;
}
var createThemeNoVars_default = createThemeNoVars;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/getOverlayAlpha.js
function getOverlayAlpha(elevation) {
  let alphaValue;
  if (elevation < 1) {
    alphaValue = 5.11916 * elevation ** 2;
  } else {
    alphaValue = 4.5 * Math.log(elevation + 1) + 2;
  }
  return Math.round(alphaValue * 10) / 1e3;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/createColorScheme.js
var defaultDarkOverlays = [...Array(25)].map((_, index) => {
  if (index === 0) {
    return "none";
  }
  const overlay = getOverlayAlpha(index);
  return `linear-gradient(rgba(255 255 255 / ${overlay}), rgba(255 255 255 / ${overlay}))`;
});
function getOpacity(mode) {
  return {
    inputPlaceholder: mode === "dark" ? 0.5 : 0.42,
    inputUnderline: mode === "dark" ? 0.7 : 0.42,
    switchTrackDisabled: mode === "dark" ? 0.2 : 0.12,
    switchTrack: mode === "dark" ? 0.3 : 0.38,
  };
}
function getOverlays(mode) {
  return mode === "dark" ? defaultDarkOverlays : [];
}
function createColorScheme(options) {
  const {
    palette: paletteInput = {
      mode: "light",
    },
    // need to cast to avoid module augmentation test
    opacity,
    overlays,
    colorSpace,
    ...other
  } = options;
  const palette2 = createPalette({
    ...paletteInput,
    colorSpace,
  });
  return {
    palette: palette2,
    opacity: {
      ...getOpacity(palette2.mode),
      ...opacity,
    },
    overlays: overlays || getOverlays(palette2.mode),
    ...other,
  };
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/shouldSkipGeneratingVar.js
function shouldSkipGeneratingVar(keys) {
  return (
    !!keys[0].match(
      /(cssVarPrefix|colorSchemeSelector|modularCssLayers|rootSelector|typography|mixins|breakpoints|direction|transitions)/,
    ) ||
    !!keys[0].match(/sxConfig$/) || // ends with sxConfig
    (keys[0] === "palette" &&
      !!keys[1]?.match(/(mode|contrastThreshold|tonalOffset)/))
  );
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/excludeVariablesFromRoot.js
var excludeVariablesFromRoot = (cssVarPrefix) => [
  ...[...Array(25)].map(
    (_, index) =>
      `--${cssVarPrefix ? `${cssVarPrefix}-` : ""}overlays-${index}`,
  ),
  `--${cssVarPrefix ? `${cssVarPrefix}-` : ""}palette-AppBar-darkBg`,
  `--${cssVarPrefix ? `${cssVarPrefix}-` : ""}palette-AppBar-darkColor`,
];
var excludeVariablesFromRoot_default = excludeVariablesFromRoot;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/createGetSelector.js
var createGetSelector_default = (theme2) => (colorScheme, css2) => {
  const root = theme2.rootSelector || ":root";
  const selector = theme2.colorSchemeSelector;
  let rule = selector;
  if (selector === "class") {
    rule = ".%s";
  }
  if (selector === "data") {
    rule = "[data-%s]";
  }
  if (selector?.startsWith("data-") && !selector.includes("%s")) {
    rule = `[${selector}="%s"]`;
  }
  if (theme2.defaultColorScheme === colorScheme) {
    if (colorScheme === "dark") {
      const excludedVariables = {};
      excludeVariablesFromRoot_default(theme2.cssVarPrefix).forEach(
        (cssVar) => {
          excludedVariables[cssVar] = css2[cssVar];
          delete css2[cssVar];
        },
      );
      if (rule === "media") {
        return {
          [root]: css2,
          [`@media (prefers-color-scheme: dark)`]: {
            [root]: excludedVariables,
          },
        };
      }
      if (rule) {
        return {
          [rule.replace("%s", colorScheme)]: excludedVariables,
          [`${root}, ${rule.replace("%s", colorScheme)}`]: css2,
        };
      }
      return {
        [root]: {
          ...css2,
          ...excludedVariables,
        },
      };
    }
    if (rule && rule !== "media") {
      return `${root}, ${rule.replace("%s", String(colorScheme))}`;
    }
  } else if (colorScheme) {
    if (rule === "media") {
      return {
        [`@media (prefers-color-scheme: ${String(colorScheme)})`]: {
          [root]: css2,
        },
      };
    }
    if (rule) {
      return rule.replace("%s", String(colorScheme));
    }
  }
  return root;
};

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/createThemeWithVars.js
function assignNode(obj, keys) {
  keys.forEach((k) => {
    if (!obj[k]) {
      obj[k] = {};
    }
  });
}
function setColor(obj, key, defaultValue) {
  if (!obj[key] && defaultValue) {
    obj[key] = defaultValue;
  }
}
function toRgb(color2) {
  if (typeof color2 !== "string" || !color2.startsWith("hsl")) {
    return color2;
  }
  return hslToRgb(color2);
}
function setColorChannel(obj, key) {
  if (!(`${key}Channel` in obj)) {
    obj[`${key}Channel`] = private_safeColorChannel(
      toRgb(obj[key]),
      `MUI: Can't create \`palette.${key}Channel\` because \`palette.${key}\` is not one of these formats: #nnn, #nnnnnn, rgb(), rgba(), hsl(), hsla(), color().
To suppress this warning, you need to explicitly provide the \`palette.${key}Channel\` as a string (in rgb format, for example "12 12 12") or undefined if you want to remove the channel token.`,
    );
  }
}
function getSpacingVal(spacingInput) {
  if (typeof spacingInput === "number") {
    return `${spacingInput}px`;
  }
  if (
    typeof spacingInput === "string" ||
    typeof spacingInput === "function" ||
    Array.isArray(spacingInput)
  ) {
    return spacingInput;
  }
  return "8px";
}
var silent = (fn2) => {
  try {
    return fn2();
  } catch (error) {}
  return void 0;
};
var createGetCssVar2 = (cssVarPrefix = "mui") => createGetCssVar(cssVarPrefix);
function attachColorScheme(
  colorSpace,
  colorSchemes,
  scheme,
  restTheme,
  colorScheme,
) {
  if (!scheme) {
    return void 0;
  }
  scheme = scheme === true ? {} : scheme;
  const mode = colorScheme === "dark" ? "dark" : "light";
  if (!restTheme) {
    colorSchemes[colorScheme] = createColorScheme({
      ...scheme,
      palette: {
        mode,
        ...scheme?.palette,
      },
      colorSpace,
    });
    return void 0;
  }
  const { palette: palette2, ...muiTheme } = createThemeNoVars_default({
    ...restTheme,
    palette: {
      mode,
      ...scheme?.palette,
    },
    colorSpace,
  });
  colorSchemes[colorScheme] = {
    ...scheme,
    palette: palette2,
    opacity: {
      ...getOpacity(mode),
      ...scheme?.opacity,
    },
    overlays: scheme?.overlays || getOverlays(mode),
  };
  return muiTheme;
}
function createThemeWithVars(options = {}, ...args) {
  const {
    colorSchemes: colorSchemesInput = {
      light: true,
    },
    defaultColorScheme: defaultColorSchemeInput,
    disableCssColorScheme = false,
    cssVarPrefix = "mui",
    nativeColor = false,
    shouldSkipGeneratingVar: shouldSkipGeneratingVar2 = shouldSkipGeneratingVar,
    colorSchemeSelector: selector = colorSchemesInput.light &&
    colorSchemesInput.dark
      ? "media"
      : void 0,
    rootSelector = ":root",
    ...input
  } = options;
  const firstColorScheme = Object.keys(colorSchemesInput)[0];
  const defaultColorScheme =
    defaultColorSchemeInput ||
    (colorSchemesInput.light && firstColorScheme !== "light"
      ? "light"
      : firstColorScheme);
  const getCssVar = createGetCssVar2(cssVarPrefix);
  const {
    [defaultColorScheme]: defaultSchemeInput,
    light: builtInLight,
    dark: builtInDark,
    ...customColorSchemes
  } = colorSchemesInput;
  const colorSchemes = {
    ...customColorSchemes,
  };
  let defaultScheme = defaultSchemeInput;
  if (
    (defaultColorScheme === "dark" && !("dark" in colorSchemesInput)) ||
    (defaultColorScheme === "light" && !("light" in colorSchemesInput))
  ) {
    defaultScheme = true;
  }
  if (!defaultScheme) {
    throw new Error(
      false
        ? `MUI: The \`colorSchemes.${defaultColorScheme}\` option is either missing or invalid.`
        : formatMuiErrorMessage(21, defaultColorScheme),
    );
  }
  let colorSpace;
  if (nativeColor) {
    colorSpace = "oklch";
  }
  const muiTheme = attachColorScheme(
    colorSpace,
    colorSchemes,
    defaultScheme,
    input,
    defaultColorScheme,
  );
  if (builtInLight && !colorSchemes.light) {
    attachColorScheme(colorSpace, colorSchemes, builtInLight, void 0, "light");
  }
  if (builtInDark && !colorSchemes.dark) {
    attachColorScheme(colorSpace, colorSchemes, builtInDark, void 0, "dark");
  }
  let theme2 = {
    defaultColorScheme,
    ...muiTheme,
    cssVarPrefix,
    colorSchemeSelector: selector,
    rootSelector,
    getCssVar,
    colorSchemes,
    font: {
      ...prepareTypographyVars(muiTheme.typography),
      ...muiTheme.font,
    },
    spacing: getSpacingVal(input.spacing),
  };
  Object.keys(theme2.colorSchemes).forEach((key) => {
    const palette2 = theme2.colorSchemes[key].palette;
    const setCssVarColor = (cssVar) => {
      const tokens = cssVar.split("-");
      const color2 = tokens[1];
      const colorToken = tokens[2];
      return getCssVar(cssVar, palette2[color2][colorToken]);
    };
    if (palette2.mode === "light") {
      setColor(palette2.common, "background", "#fff");
      setColor(palette2.common, "onBackground", "#000");
    }
    if (palette2.mode === "dark") {
      setColor(palette2.common, "background", "#000");
      setColor(palette2.common, "onBackground", "#fff");
    }
    function colorMix(method, color2, coefficient) {
      if (colorSpace) {
        let mixer;
        if (method === private_safeAlpha) {
          mixer = `transparent ${((1 - coefficient) * 100).toFixed(0)}%`;
        }
        if (method === private_safeDarken) {
          mixer = `#000 ${(coefficient * 100).toFixed(0)}%`;
        }
        if (method === private_safeLighten) {
          mixer = `#fff ${(coefficient * 100).toFixed(0)}%`;
        }
        return `color-mix(in ${colorSpace}, ${color2}, ${mixer})`;
      }
      return method(color2, coefficient);
    }
    assignNode(palette2, [
      "Alert",
      "AppBar",
      "Avatar",
      "Button",
      "Chip",
      "FilledInput",
      "LinearProgress",
      "Skeleton",
      "Slider",
      "SnackbarContent",
      "SpeedDialAction",
      "StepConnector",
      "StepContent",
      "Switch",
      "TableCell",
      "Tooltip",
    ]);
    if (palette2.mode === "light") {
      setColor(
        palette2.Alert,
        "errorColor",
        colorMix(
          private_safeDarken,
          nativeColor ? getCssVar("palette-error-light") : palette2.error.light,
          0.6,
        ),
      );
      setColor(
        palette2.Alert,
        "infoColor",
        colorMix(
          private_safeDarken,
          nativeColor ? getCssVar("palette-info-light") : palette2.info.light,
          0.6,
        ),
      );
      setColor(
        palette2.Alert,
        "successColor",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-success-light")
            : palette2.success.light,
          0.6,
        ),
      );
      setColor(
        palette2.Alert,
        "warningColor",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-warning-light")
            : palette2.warning.light,
          0.6,
        ),
      );
      setColor(
        palette2.Alert,
        "errorFilledBg",
        setCssVarColor("palette-error-main"),
      );
      setColor(
        palette2.Alert,
        "infoFilledBg",
        setCssVarColor("palette-info-main"),
      );
      setColor(
        palette2.Alert,
        "successFilledBg",
        setCssVarColor("palette-success-main"),
      );
      setColor(
        palette2.Alert,
        "warningFilledBg",
        setCssVarColor("palette-warning-main"),
      );
      setColor(
        palette2.Alert,
        "errorFilledColor",
        silent(() => palette2.getContrastText(palette2.error.main)),
      );
      setColor(
        palette2.Alert,
        "infoFilledColor",
        silent(() => palette2.getContrastText(palette2.info.main)),
      );
      setColor(
        palette2.Alert,
        "successFilledColor",
        silent(() => palette2.getContrastText(palette2.success.main)),
      );
      setColor(
        palette2.Alert,
        "warningFilledColor",
        silent(() => palette2.getContrastText(palette2.warning.main)),
      );
      setColor(
        palette2.Alert,
        "errorStandardBg",
        colorMix(
          private_safeLighten,
          nativeColor ? getCssVar("palette-error-light") : palette2.error.light,
          0.9,
        ),
      );
      setColor(
        palette2.Alert,
        "infoStandardBg",
        colorMix(
          private_safeLighten,
          nativeColor ? getCssVar("palette-info-light") : palette2.info.light,
          0.9,
        ),
      );
      setColor(
        palette2.Alert,
        "successStandardBg",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-success-light")
            : palette2.success.light,
          0.9,
        ),
      );
      setColor(
        palette2.Alert,
        "warningStandardBg",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-warning-light")
            : palette2.warning.light,
          0.9,
        ),
      );
      setColor(
        palette2.Alert,
        "errorIconColor",
        setCssVarColor("palette-error-main"),
      );
      setColor(
        palette2.Alert,
        "infoIconColor",
        setCssVarColor("palette-info-main"),
      );
      setColor(
        palette2.Alert,
        "successIconColor",
        setCssVarColor("palette-success-main"),
      );
      setColor(
        palette2.Alert,
        "warningIconColor",
        setCssVarColor("palette-warning-main"),
      );
      setColor(
        palette2.AppBar,
        "defaultBg",
        setCssVarColor("palette-grey-100"),
      );
      setColor(
        palette2.Avatar,
        "defaultBg",
        setCssVarColor("palette-grey-400"),
      );
      setColor(
        palette2.Button,
        "inheritContainedBg",
        setCssVarColor("palette-grey-300"),
      );
      setColor(
        palette2.Button,
        "inheritContainedHoverBg",
        setCssVarColor("palette-grey-A100"),
      );
      setColor(
        palette2.Chip,
        "defaultBorder",
        setCssVarColor("palette-grey-400"),
      );
      setColor(
        palette2.Chip,
        "defaultAvatarColor",
        setCssVarColor("palette-grey-700"),
      );
      setColor(
        palette2.Chip,
        "defaultIconColor",
        setCssVarColor("palette-grey-700"),
      );
      setColor(palette2.FilledInput, "bg", "rgba(0, 0, 0, 0.06)");
      setColor(palette2.FilledInput, "hoverBg", "rgba(0, 0, 0, 0.09)");
      setColor(palette2.FilledInput, "disabledBg", "rgba(0, 0, 0, 0.12)");
      setColor(
        palette2.LinearProgress,
        "primaryBg",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-primary-main")
            : palette2.primary.main,
          0.62,
        ),
      );
      setColor(
        palette2.LinearProgress,
        "secondaryBg",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-secondary-main")
            : palette2.secondary.main,
          0.62,
        ),
      );
      setColor(
        palette2.LinearProgress,
        "errorBg",
        colorMix(
          private_safeLighten,
          nativeColor ? getCssVar("palette-error-main") : palette2.error.main,
          0.62,
        ),
      );
      setColor(
        palette2.LinearProgress,
        "infoBg",
        colorMix(
          private_safeLighten,
          nativeColor ? getCssVar("palette-info-main") : palette2.info.main,
          0.62,
        ),
      );
      setColor(
        palette2.LinearProgress,
        "successBg",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-success-main")
            : palette2.success.main,
          0.62,
        ),
      );
      setColor(
        palette2.LinearProgress,
        "warningBg",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-warning-light")
            : palette2.warning.main,
          0.62,
        ),
      );
      setColor(
        palette2.Skeleton,
        "bg",
        colorSpace
          ? colorMix(
              private_safeAlpha,
              nativeColor
                ? getCssVar("palette-text-primary")
                : palette2.text.primary,
              0.11,
            )
          : `rgba(${setCssVarColor("palette-text-primaryChannel")} / 0.11)`,
      );
      setColor(
        palette2.Slider,
        "primaryTrack",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-primary-main")
            : palette2.primary.main,
          0.62,
        ),
      );
      setColor(
        palette2.Slider,
        "secondaryTrack",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-secondary-main")
            : palette2.secondary.main,
          0.62,
        ),
      );
      setColor(
        palette2.Slider,
        "errorTrack",
        colorMix(
          private_safeLighten,
          nativeColor ? getCssVar("palette-error-main") : palette2.error.main,
          0.62,
        ),
      );
      setColor(
        palette2.Slider,
        "infoTrack",
        colorMix(
          private_safeLighten,
          nativeColor ? getCssVar("palette-info-main") : palette2.info.main,
          0.62,
        ),
      );
      setColor(
        palette2.Slider,
        "successTrack",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-success-main")
            : palette2.success.main,
          0.62,
        ),
      );
      setColor(
        palette2.Slider,
        "warningTrack",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-warning-main")
            : palette2.warning.main,
          0.62,
        ),
      );
      const snackbarContentBackground = colorSpace
        ? colorMix(
            private_safeDarken,
            nativeColor
              ? getCssVar("palette-background-default")
              : palette2.background.default,
            0.6825,
          )
        : private_safeEmphasize(palette2.background.default, 0.8);
      setColor(palette2.SnackbarContent, "bg", snackbarContentBackground);
      setColor(
        palette2.SnackbarContent,
        "color",
        silent(() =>
          colorSpace
            ? dark.text.primary
            : palette2.getContrastText(snackbarContentBackground),
        ),
      );
      setColor(
        palette2.SpeedDialAction,
        "fabHoverBg",
        private_safeEmphasize(palette2.background.paper, 0.15),
      );
      setColor(
        palette2.StepConnector,
        "border",
        setCssVarColor("palette-grey-400"),
      );
      setColor(
        palette2.StepContent,
        "border",
        setCssVarColor("palette-grey-400"),
      );
      setColor(
        palette2.Switch,
        "defaultColor",
        setCssVarColor("palette-common-white"),
      );
      setColor(
        palette2.Switch,
        "defaultDisabledColor",
        setCssVarColor("palette-grey-100"),
      );
      setColor(
        palette2.Switch,
        "primaryDisabledColor",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-primary-main")
            : palette2.primary.main,
          0.62,
        ),
      );
      setColor(
        palette2.Switch,
        "secondaryDisabledColor",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-secondary-main")
            : palette2.secondary.main,
          0.62,
        ),
      );
      setColor(
        palette2.Switch,
        "errorDisabledColor",
        colorMix(
          private_safeLighten,
          nativeColor ? getCssVar("palette-error-main") : palette2.error.main,
          0.62,
        ),
      );
      setColor(
        palette2.Switch,
        "infoDisabledColor",
        colorMix(
          private_safeLighten,
          nativeColor ? getCssVar("palette-info-main") : palette2.info.main,
          0.62,
        ),
      );
      setColor(
        palette2.Switch,
        "successDisabledColor",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-success-main")
            : palette2.success.main,
          0.62,
        ),
      );
      setColor(
        palette2.Switch,
        "warningDisabledColor",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-warning-main")
            : palette2.warning.main,
          0.62,
        ),
      );
      setColor(
        palette2.TableCell,
        "border",
        colorMix(
          private_safeLighten,
          private_safeAlpha(
            nativeColor ? getCssVar("palette-divider") : palette2.divider,
            1,
          ),
          0.88,
        ),
      );
      setColor(
        palette2.Tooltip,
        "bg",
        colorMix(
          private_safeAlpha,
          nativeColor ? getCssVar("palette-grey-700") : palette2.grey[700],
          0.92,
        ),
      );
    }
    if (palette2.mode === "dark") {
      setColor(
        palette2.Alert,
        "errorColor",
        colorMix(
          private_safeLighten,
          nativeColor ? getCssVar("palette-error-light") : palette2.error.light,
          0.6,
        ),
      );
      setColor(
        palette2.Alert,
        "infoColor",
        colorMix(
          private_safeLighten,
          nativeColor ? getCssVar("palette-info-light") : palette2.info.light,
          0.6,
        ),
      );
      setColor(
        palette2.Alert,
        "successColor",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-success-light")
            : palette2.success.light,
          0.6,
        ),
      );
      setColor(
        palette2.Alert,
        "warningColor",
        colorMix(
          private_safeLighten,
          nativeColor
            ? getCssVar("palette-warning-light")
            : palette2.warning.light,
          0.6,
        ),
      );
      setColor(
        palette2.Alert,
        "errorFilledBg",
        setCssVarColor("palette-error-dark"),
      );
      setColor(
        palette2.Alert,
        "infoFilledBg",
        setCssVarColor("palette-info-dark"),
      );
      setColor(
        palette2.Alert,
        "successFilledBg",
        setCssVarColor("palette-success-dark"),
      );
      setColor(
        palette2.Alert,
        "warningFilledBg",
        setCssVarColor("palette-warning-dark"),
      );
      setColor(
        palette2.Alert,
        "errorFilledColor",
        silent(() => palette2.getContrastText(palette2.error.dark)),
      );
      setColor(
        palette2.Alert,
        "infoFilledColor",
        silent(() => palette2.getContrastText(palette2.info.dark)),
      );
      setColor(
        palette2.Alert,
        "successFilledColor",
        silent(() => palette2.getContrastText(palette2.success.dark)),
      );
      setColor(
        palette2.Alert,
        "warningFilledColor",
        silent(() => palette2.getContrastText(palette2.warning.dark)),
      );
      setColor(
        palette2.Alert,
        "errorStandardBg",
        colorMix(
          private_safeDarken,
          nativeColor ? getCssVar("palette-error-light") : palette2.error.light,
          0.9,
        ),
      );
      setColor(
        palette2.Alert,
        "infoStandardBg",
        colorMix(
          private_safeDarken,
          nativeColor ? getCssVar("palette-info-light") : palette2.info.light,
          0.9,
        ),
      );
      setColor(
        palette2.Alert,
        "successStandardBg",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-success-light")
            : palette2.success.light,
          0.9,
        ),
      );
      setColor(
        palette2.Alert,
        "warningStandardBg",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-warning-light")
            : palette2.warning.light,
          0.9,
        ),
      );
      setColor(
        palette2.Alert,
        "errorIconColor",
        setCssVarColor("palette-error-main"),
      );
      setColor(
        palette2.Alert,
        "infoIconColor",
        setCssVarColor("palette-info-main"),
      );
      setColor(
        palette2.Alert,
        "successIconColor",
        setCssVarColor("palette-success-main"),
      );
      setColor(
        palette2.Alert,
        "warningIconColor",
        setCssVarColor("palette-warning-main"),
      );
      setColor(
        palette2.AppBar,
        "defaultBg",
        setCssVarColor("palette-grey-900"),
      );
      setColor(
        palette2.AppBar,
        "darkBg",
        setCssVarColor("palette-background-paper"),
      );
      setColor(
        palette2.AppBar,
        "darkColor",
        setCssVarColor("palette-text-primary"),
      );
      setColor(
        palette2.Avatar,
        "defaultBg",
        setCssVarColor("palette-grey-600"),
      );
      setColor(
        palette2.Button,
        "inheritContainedBg",
        setCssVarColor("palette-grey-800"),
      );
      setColor(
        palette2.Button,
        "inheritContainedHoverBg",
        setCssVarColor("palette-grey-700"),
      );
      setColor(
        palette2.Chip,
        "defaultBorder",
        setCssVarColor("palette-grey-700"),
      );
      setColor(
        palette2.Chip,
        "defaultAvatarColor",
        setCssVarColor("palette-grey-300"),
      );
      setColor(
        palette2.Chip,
        "defaultIconColor",
        setCssVarColor("palette-grey-300"),
      );
      setColor(palette2.FilledInput, "bg", "rgba(255, 255, 255, 0.09)");
      setColor(palette2.FilledInput, "hoverBg", "rgba(255, 255, 255, 0.13)");
      setColor(palette2.FilledInput, "disabledBg", "rgba(255, 255, 255, 0.12)");
      setColor(
        palette2.LinearProgress,
        "primaryBg",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-primary-main")
            : palette2.primary.main,
          0.5,
        ),
      );
      setColor(
        palette2.LinearProgress,
        "secondaryBg",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-secondary-main")
            : palette2.secondary.main,
          0.5,
        ),
      );
      setColor(
        palette2.LinearProgress,
        "errorBg",
        colorMix(
          private_safeDarken,
          nativeColor ? getCssVar("palette-error-main") : palette2.error.main,
          0.5,
        ),
      );
      setColor(
        palette2.LinearProgress,
        "infoBg",
        colorMix(
          private_safeDarken,
          nativeColor ? getCssVar("palette-info-main") : palette2.info.main,
          0.5,
        ),
      );
      setColor(
        palette2.LinearProgress,
        "successBg",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-success-main")
            : palette2.success.main,
          0.5,
        ),
      );
      setColor(
        palette2.LinearProgress,
        "warningBg",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-warning-main")
            : palette2.warning.main,
          0.5,
        ),
      );
      setColor(
        palette2.Skeleton,
        "bg",
        colorSpace
          ? colorMix(
              private_safeAlpha,
              nativeColor
                ? getCssVar("palette-text-primary")
                : palette2.text.primary,
              0.13,
            )
          : `rgba(${setCssVarColor("palette-text-primaryChannel")} / 0.13)`,
      );
      setColor(
        palette2.Slider,
        "primaryTrack",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-primary-main")
            : palette2.primary.main,
          0.5,
        ),
      );
      setColor(
        palette2.Slider,
        "secondaryTrack",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-secondary-main")
            : palette2.secondary.main,
          0.5,
        ),
      );
      setColor(
        palette2.Slider,
        "errorTrack",
        colorMix(
          private_safeDarken,
          nativeColor ? getCssVar("palette-error-main") : palette2.error.main,
          0.5,
        ),
      );
      setColor(
        palette2.Slider,
        "infoTrack",
        colorMix(
          private_safeDarken,
          nativeColor ? getCssVar("palette-info-main") : palette2.info.main,
          0.5,
        ),
      );
      setColor(
        palette2.Slider,
        "successTrack",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-success-main")
            : palette2.success.main,
          0.5,
        ),
      );
      setColor(
        palette2.Slider,
        "warningTrack",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-warning-light")
            : palette2.warning.main,
          0.5,
        ),
      );
      const snackbarContentBackground = colorSpace
        ? colorMix(
            private_safeLighten,
            nativeColor
              ? getCssVar("palette-background-default")
              : palette2.background.default,
            0.985,
          )
        : private_safeEmphasize(palette2.background.default, 0.98);
      setColor(palette2.SnackbarContent, "bg", snackbarContentBackground);
      setColor(
        palette2.SnackbarContent,
        "color",
        silent(() =>
          colorSpace
            ? light.text.primary
            : palette2.getContrastText(snackbarContentBackground),
        ),
      );
      setColor(
        palette2.SpeedDialAction,
        "fabHoverBg",
        private_safeEmphasize(palette2.background.paper, 0.15),
      );
      setColor(
        palette2.StepConnector,
        "border",
        setCssVarColor("palette-grey-600"),
      );
      setColor(
        palette2.StepContent,
        "border",
        setCssVarColor("palette-grey-600"),
      );
      setColor(
        palette2.Switch,
        "defaultColor",
        setCssVarColor("palette-grey-300"),
      );
      setColor(
        palette2.Switch,
        "defaultDisabledColor",
        setCssVarColor("palette-grey-600"),
      );
      setColor(
        palette2.Switch,
        "primaryDisabledColor",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-primary-main")
            : palette2.primary.main,
          0.55,
        ),
      );
      setColor(
        palette2.Switch,
        "secondaryDisabledColor",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-secondary-main")
            : palette2.secondary.main,
          0.55,
        ),
      );
      setColor(
        palette2.Switch,
        "errorDisabledColor",
        colorMix(
          private_safeDarken,
          nativeColor ? getCssVar("palette-error-main") : palette2.error.main,
          0.55,
        ),
      );
      setColor(
        palette2.Switch,
        "infoDisabledColor",
        colorMix(
          private_safeDarken,
          nativeColor ? getCssVar("palette-info-main") : palette2.info.main,
          0.55,
        ),
      );
      setColor(
        palette2.Switch,
        "successDisabledColor",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-success-main")
            : palette2.success.main,
          0.55,
        ),
      );
      setColor(
        palette2.Switch,
        "warningDisabledColor",
        colorMix(
          private_safeDarken,
          nativeColor
            ? getCssVar("palette-warning-light")
            : palette2.warning.main,
          0.55,
        ),
      );
      setColor(
        palette2.TableCell,
        "border",
        colorMix(
          private_safeDarken,
          private_safeAlpha(
            nativeColor ? getCssVar("palette-divider") : palette2.divider,
            1,
          ),
          0.68,
        ),
      );
      setColor(
        palette2.Tooltip,
        "bg",
        colorMix(
          private_safeAlpha,
          nativeColor ? getCssVar("palette-grey-700") : palette2.grey[700],
          0.92,
        ),
      );
    }
    if (!nativeColor) {
      setColorChannel(palette2.background, "default");
      setColorChannel(palette2.background, "paper");
      setColorChannel(palette2.common, "background");
      setColorChannel(palette2.common, "onBackground");
      setColorChannel(palette2, "divider");
    }
    Object.keys(palette2).forEach((color2) => {
      const colors = palette2[color2];
      if (
        color2 !== "tonalOffset" &&
        !nativeColor &&
        colors &&
        typeof colors === "object"
      ) {
        if (colors.main) {
          setColor(
            palette2[color2],
            "mainChannel",
            private_safeColorChannel(toRgb(colors.main)),
          );
        }
        if (colors.light) {
          setColor(
            palette2[color2],
            "lightChannel",
            private_safeColorChannel(toRgb(colors.light)),
          );
        }
        if (colors.dark) {
          setColor(
            palette2[color2],
            "darkChannel",
            private_safeColorChannel(toRgb(colors.dark)),
          );
        }
        if (colors.contrastText) {
          setColor(
            palette2[color2],
            "contrastTextChannel",
            private_safeColorChannel(toRgb(colors.contrastText)),
          );
        }
        if (color2 === "text") {
          setColorChannel(palette2[color2], "primary");
          setColorChannel(palette2[color2], "secondary");
        }
        if (color2 === "action") {
          if (colors.active) {
            setColorChannel(palette2[color2], "active");
          }
          if (colors.selected) {
            setColorChannel(palette2[color2], "selected");
          }
        }
      }
    });
  });
  theme2 = args.reduce((acc, argument) => deepmerge(acc, argument), theme2);
  const parserConfig = {
    prefix: cssVarPrefix,
    disableCssColorScheme,
    shouldSkipGeneratingVar: shouldSkipGeneratingVar2,
    getSelector: createGetSelector_default(theme2),
    enableContrastVars: nativeColor,
  };
  const { vars, generateThemeVars, generateStyleSheets } =
    prepareCssVars_default(theme2, parserConfig);
  theme2.vars = vars;
  Object.entries(theme2.colorSchemes[theme2.defaultColorScheme]).forEach(
    ([key, value]) => {
      theme2[key] = value;
    },
  );
  theme2.generateThemeVars = generateThemeVars;
  theme2.generateStyleSheets = generateStyleSheets;
  theme2.generateSpacing = function generateSpacing() {
    return createSpacing(input.spacing, createUnarySpacing(this));
  };
  theme2.getColorSchemeSelector = createGetColorSchemeSelector(selector);
  theme2.spacing = theme2.generateSpacing();
  theme2.shouldSkipGeneratingVar = shouldSkipGeneratingVar2;
  theme2.unstable_sxConfig = {
    ...defaultSxConfig_default,
    ...input?.unstable_sxConfig,
  };
  theme2.unstable_sx = function sx(props) {
    return styleFunctionSx_default({
      sx: props,
      theme: this,
    });
  };
  theme2.toRuntimeSource = stringifyTheme;
  return theme2;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/createTheme.js
function attachColorScheme2(theme2, scheme, colorScheme) {
  if (!theme2.colorSchemes) {
    return void 0;
  }
  if (colorScheme) {
    theme2.colorSchemes[scheme] = {
      ...(colorScheme !== true && colorScheme),
      palette: createPalette({
        ...(colorScheme === true ? {} : colorScheme.palette),
        mode: scheme,
      }),
      // cast type to skip module augmentation test
    };
  }
}
function createTheme2(options = {}, ...args) {
  const {
    palette: palette2,
    cssVariables = false,
    colorSchemes: initialColorSchemes = !palette2
      ? {
          light: true,
        }
      : void 0,
    defaultColorScheme: initialDefaultColorScheme = palette2?.mode,
    ...other
  } = options;
  const defaultColorSchemeInput = initialDefaultColorScheme || "light";
  const defaultScheme = initialColorSchemes?.[defaultColorSchemeInput];
  const colorSchemesInput = {
    ...initialColorSchemes,
    ...(palette2
      ? {
          [defaultColorSchemeInput]: {
            ...(typeof defaultScheme !== "boolean" && defaultScheme),
            palette: palette2,
          },
        }
      : void 0),
  };
  if (cssVariables === false) {
    if (!("colorSchemes" in options)) {
      return createThemeNoVars_default(options, ...args);
    }
    let paletteOptions = palette2;
    if (!("palette" in options)) {
      if (colorSchemesInput[defaultColorSchemeInput]) {
        if (colorSchemesInput[defaultColorSchemeInput] !== true) {
          paletteOptions = colorSchemesInput[defaultColorSchemeInput].palette;
        } else if (defaultColorSchemeInput === "dark") {
          paletteOptions = {
            mode: "dark",
          };
        }
      }
    }
    const theme2 = createThemeNoVars_default(
      {
        ...options,
        palette: paletteOptions,
      },
      ...args,
    );
    theme2.defaultColorScheme = defaultColorSchemeInput;
    theme2.colorSchemes = colorSchemesInput;
    if (theme2.palette.mode === "light") {
      theme2.colorSchemes.light = {
        ...(colorSchemesInput.light !== true && colorSchemesInput.light),
        palette: theme2.palette,
      };
      attachColorScheme2(theme2, "dark", colorSchemesInput.dark);
    }
    if (theme2.palette.mode === "dark") {
      theme2.colorSchemes.dark = {
        ...(colorSchemesInput.dark !== true && colorSchemesInput.dark),
        palette: theme2.palette,
      };
      attachColorScheme2(theme2, "light", colorSchemesInput.light);
    }
    return theme2;
  }
  if (
    !palette2 &&
    !("light" in colorSchemesInput) &&
    defaultColorSchemeInput === "light"
  ) {
    colorSchemesInput.light = true;
  }
  return createThemeWithVars(
    {
      ...other,
      colorSchemes: colorSchemesInput,
      defaultColorScheme: defaultColorSchemeInput,
      ...(typeof cssVariables !== "boolean" && cssVariables),
    },
    ...args,
  );
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/cssUtils.js
function getUnit(input) {
  return String(input).match(/[\d.\-+]*\s*(.*)/)[1] || "";
}
function toUnitless(length2) {
  return parseFloat(length2);
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/useTheme.js
var React22 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/defaultTheme.js
var defaultTheme3 = createTheme2();
var defaultTheme_default = defaultTheme3;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/useTheme.js
function useTheme5() {
  const theme2 = useTheme_default(defaultTheme_default);
  if (false) {
    React22.useDebugValue(theme2);
  }
  return theme2[identifier_default] || theme2;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/slotShouldForwardProp.js
function slotShouldForwardProp(prop) {
  return (
    prop !== "ownerState" && prop !== "theme" && prop !== "sx" && prop !== "as"
  );
}
var slotShouldForwardProp_default = slotShouldForwardProp;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/rootShouldForwardProp.js
var rootShouldForwardProp = (prop) =>
  slotShouldForwardProp_default(prop) && prop !== "classes";
var rootShouldForwardProp_default = rootShouldForwardProp;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/styled.js
var styled4 = createStyled3({
  themeId: identifier_default,
  defaultTheme: defaultTheme_default,
  rootShouldForwardProp: rootShouldForwardProp_default,
});
var styled_default2 = styled4;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/ThemeProvider.js
var React24 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/ThemeProviderNoVars.js
var React23 = __toESM(require_react(), 1);
var import_jsx_runtime13 = __toESM(require_jsx_runtime(), 1);
function ThemeProviderNoVars({ theme: themeInput, ...props }) {
  const scopedTheme =
    identifier_default in themeInput ? themeInput[identifier_default] : void 0;
  return /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(ThemeProvider_default2, {
    ...props,
    themeId: scopedTheme ? identifier_default : void 0,
    theme: scopedTheme || themeInput,
  });
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/InitColorSchemeScript/InitColorSchemeScript.js
var import_jsx_runtime14 = __toESM(require_jsx_runtime(), 1);
var defaultConfig = {
  attribute: "data-mui-color-scheme",
  colorSchemeStorageKey: "mui-color-scheme",
  defaultLightColorScheme: "light",
  defaultDarkColorScheme: "dark",
  modeStorageKey: "mui-mode",
};
false
  ? (InitColorSchemeScript.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * DOM attribute for applying a color scheme.
       * @default 'data-mui-color-scheme'
       * @example '.mode-%s' // for class based color scheme
       * @example '[data-mode-%s]' // for data-attribute without '='
       */
      attribute: import_prop_types.default.string,
      /**
       * The node (provided as string) used to attach the color-scheme attribute.
       * @default 'document.documentElement'
       */
      colorSchemeNode: import_prop_types.default.string,
      /**
       * localStorage key used to store `colorScheme`.
       * @default 'mui-color-scheme'
       */
      colorSchemeStorageKey: import_prop_types.default.string,
      /**
       * The default color scheme to be used in dark mode.
       * @default 'dark'
       */
      defaultDarkColorScheme: import_prop_types.default.string,
      /**
       * The default color scheme to be used in light mode.
       * @default 'light'
       */
      defaultLightColorScheme: import_prop_types.default.string,
      /**
       * The default mode when the storage is empty (user's first visit).
       * @default 'system'
       */
      defaultMode: import_prop_types.default.oneOf(["dark", "light", "system"]),
      /**
       * localStorage key used to store `mode`.
       * @default 'mui-mode'
       */
      modeStorageKey: import_prop_types.default.string,
      /**
       * Nonce string to pass to the inline script for CSP headers.
       */
      nonce: import_prop_types.default.string,
    })
  : void 0;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/ThemeProviderWithVars.js
var import_jsx_runtime15 = __toESM(require_jsx_runtime(), 1);
var {
  CssVarsProvider: InternalCssVarsProvider,
  useColorScheme,
  getInitColorSchemeScript: deprecatedGetInitColorSchemeScript,
} = createCssVarsProvider({
  themeId: identifier_default,
  // @ts-ignore ignore module augmentation tests
  theme: () =>
    createTheme2({
      cssVariables: true,
    }),
  colorSchemeStorageKey: defaultConfig.colorSchemeStorageKey,
  modeStorageKey: defaultConfig.modeStorageKey,
  defaultColorScheme: {
    light: defaultConfig.defaultLightColorScheme,
    dark: defaultConfig.defaultDarkColorScheme,
  },
  resolveTheme: (theme2) => {
    const newTheme = {
      ...theme2,
      typography: createTypography(theme2.palette, theme2.typography),
    };
    newTheme.unstable_sx = function sx(props) {
      return styleFunctionSx_default({
        sx: props,
        theme: this,
      });
    };
    return newTheme;
  },
});
var CssVarsProvider = InternalCssVarsProvider;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/styles/ThemeProvider.js
var import_jsx_runtime16 = __toESM(require_jsx_runtime(), 1);
function ThemeProvider4({ theme: theme2, ...props }) {
  const noVarsTheme = React24.useMemo(() => {
    if (typeof theme2 === "function") {
      return theme2;
    }
    const muiTheme =
      identifier_default in theme2 ? theme2[identifier_default] : theme2;
    if (!("colorSchemes" in muiTheme)) {
      if (!("vars" in muiTheme)) {
        return {
          ...theme2,
          vars: null,
        };
      }
      return theme2;
    }
    return null;
  }, [theme2]);
  if (noVarsTheme) {
    return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(ThemeProviderNoVars, {
      theme: noVarsTheme,
      ...props,
    });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(CssVarsProvider, {
    theme: theme2,
    ...props,
  });
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/capitalize.js
var capitalize_default = capitalize;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/createChainedFunction/createChainedFunction.js
function createChainedFunction(...funcs) {
  return funcs.reduce(
    (acc, func) => {
      if (func == null) {
        return acc;
      }
      return function chainedFunction(...args) {
        acc.apply(this, args);
        func.apply(this, args);
      };
    },
    () => {},
  );
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/createSvgIcon.js
var React27 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/SvgIcon/SvgIcon.js
var React26 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/GlobalStyles/GlobalStyles.js
var import_jsx_runtime17 = __toESM(require_jsx_runtime(), 1);
function GlobalStyles3(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(GlobalStyles_default, {
    ...props,
    defaultTheme: defaultTheme_default,
    themeId: identifier_default,
  });
}
false
  ? (GlobalStyles3.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The styles you want to apply globally.
       */
      styles: import_prop_types.default.oneOfType([
        import_prop_types.default.array,
        import_prop_types.default.func,
        import_prop_types.default.number,
        import_prop_types.default.object,
        import_prop_types.default.string,
        import_prop_types.default.bool,
      ]),
    })
  : void 0;
var GlobalStyles_default2 = GlobalStyles3;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/zero-styled/index.js
var import_jsx_runtime18 = __toESM(require_jsx_runtime(), 1);
function globalCss(styles5) {
  return function GlobalStylesWrapper(props) {
    return (
      // Pigment CSS `globalCss` support callback with theme inside an object but `GlobalStyles` support theme as a callback value.
      /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(GlobalStyles_default2, {
        styles:
          typeof styles5 === "function"
            ? (theme2) =>
                styles5({
                  theme: theme2,
                  ...props,
                })
            : styles5,
      })
    );
  };
}
function internal_createExtendSxProp() {
  return extendSxProp;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/memoTheme.js
var memoTheme = unstable_memoTheme;
var memoTheme_default = memoTheme;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/DefaultPropsProvider/DefaultPropsProvider.js
var React25 = __toESM(require_react(), 1);
var import_jsx_runtime19 = __toESM(require_jsx_runtime(), 1);
false
  ? (DefaultPropsProvider.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * @ignore
       */
      children: import_prop_types.default.node,
      /**
       * @ignore
       */
      value: import_prop_types.default.object.isRequired,
    })
  : void 0;
function useDefaultProps2(params) {
  return useDefaultProps(params);
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/SvgIcon/svgIconClasses.js
function getSvgIconUtilityClass(slot) {
  return generateUtilityClass("MuiSvgIcon", slot);
}
var svgIconClasses = generateUtilityClasses("MuiSvgIcon", [
  "root",
  "colorPrimary",
  "colorSecondary",
  "colorAction",
  "colorError",
  "colorDisabled",
  "fontSizeInherit",
  "fontSizeSmall",
  "fontSizeMedium",
  "fontSizeLarge",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/SvgIcon/SvgIcon.js
var import_jsx_runtime20 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses2 = (ownerState) => {
  const { color: color2, fontSize, classes } = ownerState;
  const slots = {
    root: [
      "root",
      color2 !== "inherit" && `color${capitalize_default(color2)}`,
      `fontSize${capitalize_default(fontSize)}`,
    ],
  };
  return composeClasses(slots, getSvgIconUtilityClass, classes);
};
var SvgIconRoot = styled_default2("svg", {
  name: "MuiSvgIcon",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      ownerState.color !== "inherit" &&
        styles5[`color${capitalize_default(ownerState.color)}`],
      styles5[`fontSize${capitalize_default(ownerState.fontSize)}`],
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    userSelect: "none",
    width: "1em",
    height: "1em",
    display: "inline-block",
    flexShrink: 0,
    transition: theme2.transitions?.create?.("fill", {
      duration: (theme2.vars ?? theme2).transitions?.duration?.shorter,
    }),
    variants: [
      {
        props: (props) => !props.hasSvgAsChild,
        style: {
          // the <svg> will define the property that has `currentColor`
          // for example heroicons uses fill="none" and stroke="currentColor"
          fill: "currentColor",
        },
      },
      {
        props: {
          fontSize: "inherit",
        },
        style: {
          fontSize: "inherit",
        },
      },
      {
        props: {
          fontSize: "small",
        },
        style: {
          fontSize: theme2.typography?.pxToRem?.(20) || "1.25rem",
        },
      },
      {
        props: {
          fontSize: "medium",
        },
        style: {
          fontSize: theme2.typography?.pxToRem?.(24) || "1.5rem",
        },
      },
      {
        props: {
          fontSize: "large",
        },
        style: {
          fontSize: theme2.typography?.pxToRem?.(35) || "2.1875rem",
        },
      },
      // TODO v5 deprecate color prop, v6 remove for sx
      ...Object.entries((theme2.vars ?? theme2).palette)
        .filter(([, value]) => value && value.main)
        .map(([color2]) => ({
          props: {
            color: color2,
          },
          style: {
            color: (theme2.vars ?? theme2).palette?.[color2]?.main,
          },
        })),
      {
        props: {
          color: "action",
        },
        style: {
          color: (theme2.vars ?? theme2).palette?.action?.active,
        },
      },
      {
        props: {
          color: "disabled",
        },
        style: {
          color: (theme2.vars ?? theme2).palette?.action?.disabled,
        },
      },
      {
        props: {
          color: "inherit",
        },
        style: {
          color: void 0,
        },
      },
    ],
  })),
);
var SvgIcon = /* @__PURE__ */ React26.forwardRef(
  function SvgIcon2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiSvgIcon",
    });
    const {
      children,
      className,
      color: color2 = "inherit",
      component = "svg",
      fontSize = "medium",
      htmlColor,
      inheritViewBox = false,
      titleAccess,
      viewBox = "0 0 24 24",
      ...other
    } = props;
    const hasSvgAsChild =
      /* @__PURE__ */ React26.isValidElement(children) &&
      children.type === "svg";
    const ownerState = {
      ...props,
      color: color2,
      component,
      fontSize,
      instanceFontSize: inProps.fontSize,
      inheritViewBox,
      viewBox,
      hasSvgAsChild,
    };
    const more = {};
    if (!inheritViewBox) {
      more.viewBox = viewBox;
    }
    const classes = useUtilityClasses2(ownerState);
    return /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)(SvgIconRoot, {
      as: component,
      className: clsx_default(classes.root, className),
      focusable: "false",
      color: htmlColor,
      "aria-hidden": titleAccess ? void 0 : true,
      role: titleAccess ? "img" : void 0,
      ref,
      ...more,
      ...other,
      ...(hasSvgAsChild && children.props),
      ownerState,
      children: [
        hasSvgAsChild ? children.props.children : children,
        titleAccess
          ? /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("title", {
              children: titleAccess,
            })
          : null,
      ],
    });
  },
);
false
  ? (SvgIcon.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * Node passed into the SVG element.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The color of the component.
       * It supports both default and custom theme colors, which can be added as shown in the
       * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
       * You can use the `htmlColor` prop to apply a color attribute to the SVG element.
       * @default 'inherit'
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "inherit",
          "action",
          "disabled",
          "primary",
          "secondary",
          "error",
          "info",
          "success",
          "warning",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * The fontSize applied to the icon. Defaults to 24px, but can be configure to inherit font size.
       * @default 'medium'
       */
      fontSize: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "inherit",
          "large",
          "medium",
          "small",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * Applies a color attribute to the SVG element.
       */
      htmlColor: import_prop_types.default.string,
      /**
       * If `true`, the root node will inherit the custom `component`'s viewBox and the `viewBox`
       * prop will be ignored.
       * Useful when you want to reference a custom `component` and have `SvgIcon` pass that
       * `component`'s viewBox to the root node.
       * @default false
       */
      inheritViewBox: import_prop_types.default.bool,
      /**
       * The shape-rendering attribute. The behavior of the different options is described on the
       * [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/SVG/Reference/Attribute/shape-rendering).
       * If you are having issues with blurry icons you should investigate this prop.
       */
      shapeRendering: import_prop_types.default.string,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * Provides a human-readable title for the element that contains it.
       * https://www.w3.org/TR/SVG-access/#Equivalent
       */
      titleAccess: import_prop_types.default.string,
      /**
       * Allows you to redefine what the coordinates without units mean inside an SVG element.
       * For example, if the SVG element is 500 (width) by 200 (height),
       * and you pass viewBox="0 0 50 20",
       * this means that the coordinates inside the SVG will go from the top left corner (0,0)
       * to bottom right (50,20) and each unit will be worth 10px.
       * @default '0 0 24 24'
       */
      viewBox: import_prop_types.default.string,
    })
  : void 0;
SvgIcon.muiName = "SvgIcon";
var SvgIcon_default = SvgIcon;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/createSvgIcon.js
var import_jsx_runtime21 = __toESM(require_jsx_runtime(), 1);
function createSvgIcon(path3, displayName) {
  function Component(props, ref) {
    return /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(SvgIcon_default, {
      "data-testid": false ? `${displayName}Icon` : void 0,
      ref,
      ...props,
      children: path3,
    });
  }
  if (false) {
    Component.displayName = `${displayName}Icon`;
  }
  Component.muiName = SvgIcon_default.muiName;
  return /* @__PURE__ */ React27.memo(
    /* @__PURE__ */ React27.forwardRef(Component),
  );
}

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/debounce/debounce.js
function debounce(func, wait = 166) {
  let timeout2;
  function debounced(...args) {
    const later = () => {
      func.apply(this, args);
    };
    clearTimeout(timeout2);
    timeout2 = setTimeout(later, wait);
  }
  debounced.clear = () => {
    clearTimeout(timeout2);
  };
  return debounced;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/debounce.js
var debounce_default = debounce;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/isMuiElement.js
var isMuiElement_default = isMuiElement;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/ownerDocument/ownerDocument.js
function ownerDocument(node2) {
  return (node2 && node2.ownerDocument) || document;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/ownerDocument.js
var ownerDocument_default = ownerDocument;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/ownerWindow/ownerWindow.js
function ownerWindow(node2) {
  const doc = ownerDocument(node2);
  return doc.defaultView || window;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/ownerWindow.js
var ownerWindow_default = ownerWindow;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/setRef/setRef.js
function setRef(ref, value) {
  if (typeof ref === "function") {
    ref(value);
  } else if (ref) {
    ref.current = value;
  }
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/useEnhancedEffect.js
var useEnhancedEffect_default2 = useEnhancedEffect_default;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/useId.js
var useId_default = useId;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/useControlled/useControlled.js
var React28 = __toESM(require_react(), 1);
function useControlled(props) {
  const { controlled, default: defaultProp, name, state = "value" } = props;
  const { current: isControlled } = React28.useRef(controlled !== void 0);
  const [valueState, setValue] = React28.useState(defaultProp);
  const value = isControlled ? controlled : valueState;
  if (false) {
    React28.useEffect(() => {
      if (isControlled !== (controlled !== void 0)) {
        console.error(
          [
            `MUI: A component is changing the ${isControlled ? "" : "un"}controlled ${state} state of ${name} to be ${isControlled ? "un" : ""}controlled.`,
            "Elements should not switch from uncontrolled to controlled (or vice versa).",
            `Decide between using a controlled or uncontrolled ${name} element for the lifetime of the component.`,
            "The nature of the state is determined during the first render. It's considered controlled if the value is not `undefined`.",
            "More info: https://fb.me/react-controlled-components",
          ].join("\n"),
        );
      }
    }, [state, name, controlled]);
    const { current: defaultValue } = React28.useRef(defaultProp);
    React28.useEffect(() => {
      if (
        !isControlled &&
        JSON.stringify(defaultProp) !== JSON.stringify(defaultValue)
      ) {
        console.error(
          [
            `MUI: A component is changing the default ${state} state of an uncontrolled ${name} after being initialized. To suppress this warning opt to use a controlled ${name}.`,
          ].join("\n"),
        );
      }
    }, [JSON.stringify(defaultProp)]);
  }
  const setValueIfUncontrolled = React28.useCallback((newValue) => {
    if (!isControlled) {
      setValue(newValue);
    }
  }, []);
  return [value, setValueIfUncontrolled];
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/useControlled.js
var useControlled_default = useControlled;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/useEventCallback/useEventCallback.js
var React29 = __toESM(require_react(), 1);
function useEventCallback(fn2) {
  const ref = React29.useRef(fn2);
  useEnhancedEffect_default(() => {
    ref.current = fn2;
  });
  return React29.useRef((...args) =>
    // @ts-expect-error hide `this`
    (0, ref.current)(...args),
  ).current;
}
var useEventCallback_default = useEventCallback;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/useEventCallback.js
var useEventCallback_default2 = useEventCallback_default;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/useForkRef/useForkRef.js
var React30 = __toESM(require_react(), 1);
function useForkRef(...refs) {
  const cleanupRef = React30.useRef(void 0);
  const refEffect = React30.useCallback((instance) => {
    const cleanups = refs.map((ref) => {
      if (ref == null) {
        return null;
      }
      if (typeof ref === "function") {
        const refCallback = ref;
        const refCleanup = refCallback(instance);
        return typeof refCleanup === "function"
          ? refCleanup
          : () => {
              refCallback(null);
            };
      }
      ref.current = instance;
      return () => {
        ref.current = null;
      };
    });
    return () => {
      cleanups.forEach((refCleanup) => refCleanup?.());
    };
  }, refs);
  return React30.useMemo(() => {
    if (refs.every((ref) => ref == null)) {
      return null;
    }
    return (value) => {
      if (cleanupRef.current) {
        cleanupRef.current();
        cleanupRef.current = void 0;
      }
      if (value != null) {
        cleanupRef.current = refEffect(value);
      }
    };
  }, refs);
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/useForkRef.js
var useForkRef_default = useForkRef;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/mergeSlotProps.js
function isEventHandler(key, value) {
  const thirdCharCode = key.charCodeAt(2);
  return (
    key[0] === "o" &&
    key[1] === "n" &&
    thirdCharCode >= 65 &&
    thirdCharCode <= 90 &&
    typeof value === "function"
  );
}
function mergeSlotProps(externalSlotProps, defaultSlotProps) {
  if (!externalSlotProps) {
    return defaultSlotProps;
  }
  function extractHandlers(externalSlotPropsValue, defaultSlotPropsValue) {
    const handlers2 = {};
    Object.keys(defaultSlotPropsValue).forEach((key) => {
      if (
        isEventHandler(key, defaultSlotPropsValue[key]) &&
        typeof externalSlotPropsValue[key] === "function"
      ) {
        handlers2[key] = (...args) => {
          externalSlotPropsValue[key](...args);
          defaultSlotPropsValue[key](...args);
        };
      }
    });
    return handlers2;
  }
  if (
    typeof externalSlotProps === "function" ||
    typeof defaultSlotProps === "function"
  ) {
    return (ownerState) => {
      const defaultSlotPropsValue =
        typeof defaultSlotProps === "function"
          ? defaultSlotProps(ownerState)
          : defaultSlotProps;
      const externalSlotPropsValue =
        typeof externalSlotProps === "function"
          ? externalSlotProps({
              ...ownerState,
              ...defaultSlotPropsValue,
            })
          : externalSlotProps;
      const className2 = clsx_default(
        ownerState?.className,
        defaultSlotPropsValue?.className,
        externalSlotPropsValue?.className,
      );
      const handlers2 = extractHandlers(
        externalSlotPropsValue,
        defaultSlotPropsValue,
      );
      return {
        ...defaultSlotPropsValue,
        ...externalSlotPropsValue,
        ...handlers2,
        ...(!!className2 && {
          className: className2,
        }),
        ...(defaultSlotPropsValue?.style &&
          externalSlotPropsValue?.style && {
            style: {
              ...defaultSlotPropsValue.style,
              ...externalSlotPropsValue.style,
            },
          }),
        ...(defaultSlotPropsValue?.sx &&
          externalSlotPropsValue?.sx && {
            sx: [
              ...(Array.isArray(defaultSlotPropsValue.sx)
                ? defaultSlotPropsValue.sx
                : [defaultSlotPropsValue.sx]),
              ...(Array.isArray(externalSlotPropsValue.sx)
                ? externalSlotPropsValue.sx
                : [externalSlotPropsValue.sx]),
            ],
          }),
      };
    };
  }
  const typedDefaultSlotProps = defaultSlotProps;
  const handlers = extractHandlers(externalSlotProps, typedDefaultSlotProps);
  const className = clsx_default(
    typedDefaultSlotProps?.className,
    externalSlotProps?.className,
  );
  return {
    ...defaultSlotProps,
    ...externalSlotProps,
    ...handlers,
    ...(!!className && {
      className,
    }),
    ...(typedDefaultSlotProps?.style &&
      externalSlotProps?.style && {
        style: {
          ...typedDefaultSlotProps.style,
          ...externalSlotProps.style,
        },
      }),
    ...(typedDefaultSlotProps?.sx &&
      externalSlotProps?.sx && {
        sx: [
          ...(Array.isArray(typedDefaultSlotProps.sx)
            ? typedDefaultSlotProps.sx
            : [typedDefaultSlotProps.sx]),
          ...(Array.isArray(externalSlotProps.sx)
            ? externalSlotProps.sx
            : [externalSlotProps.sx]),
        ],
      }),
  };
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Collapse/Collapse.js
var React36 = __toESM(require_react(), 1);

// node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js
function _objectWithoutPropertiesLoose(r2, e) {
  if (null == r2) return {};
  var t = {};
  for (var n in r2)
    if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
  return t;
}

// node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/setPrototypeOf.js
function _setPrototypeOf(t, e) {
  return (
    (_setPrototypeOf = Object.setPrototypeOf
      ? Object.setPrototypeOf.bind()
      : function (t2, e2) {
          return ((t2.__proto__ = e2), t2);
        }),
    _setPrototypeOf(t, e)
  );
}

// node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/inheritsLoose.js
function _inheritsLoose(t, o) {
  ((t.prototype = Object.create(o.prototype)),
    (t.prototype.constructor = t),
    _setPrototypeOf(t, o));
}

// node_modules/.pnpm/react-transition-group@4.4._c8c71776d32028e3f61da5b0688040c1/node_modules/react-transition-group/esm/Transition.js
var import_react7 = __toESM(require_react());
var import_react_dom = __toESM(require_react_dom());

// node_modules/.pnpm/react-transition-group@4.4._c8c71776d32028e3f61da5b0688040c1/node_modules/react-transition-group/esm/config.js
var config_default = {
  disabled: false,
};

// node_modules/.pnpm/react-transition-group@4.4._c8c71776d32028e3f61da5b0688040c1/node_modules/react-transition-group/esm/TransitionGroupContext.js
var import_react6 = __toESM(require_react());
var TransitionGroupContext_default = import_react6.default.createContext(null);

// node_modules/.pnpm/react-transition-group@4.4._c8c71776d32028e3f61da5b0688040c1/node_modules/react-transition-group/esm/utils/reflow.js
var forceReflow = function forceReflow2(node2) {
  return node2.scrollTop;
};

// node_modules/.pnpm/react-transition-group@4.4._c8c71776d32028e3f61da5b0688040c1/node_modules/react-transition-group/esm/Transition.js
var UNMOUNTED = "unmounted";
var EXITED = "exited";
var ENTERING = "entering";
var ENTERED = "entered";
var EXITING = "exiting";
var Transition = /* @__PURE__ */ (function (_React$Component) {
  _inheritsLoose(Transition2, _React$Component);
  function Transition2(props, context) {
    var _this;
    _this = _React$Component.call(this, props, context) || this;
    var parentGroup = context;
    var appear =
      parentGroup && !parentGroup.isMounting ? props.enter : props.appear;
    var initialStatus;
    _this.appearStatus = null;
    if (props.in) {
      if (appear) {
        initialStatus = EXITED;
        _this.appearStatus = ENTERING;
      } else {
        initialStatus = ENTERED;
      }
    } else {
      if (props.unmountOnExit || props.mountOnEnter) {
        initialStatus = UNMOUNTED;
      } else {
        initialStatus = EXITED;
      }
    }
    _this.state = {
      status: initialStatus,
    };
    _this.nextCallback = null;
    return _this;
  }
  Transition2.getDerivedStateFromProps = function getDerivedStateFromProps(
    _ref,
    prevState,
  ) {
    var nextIn = _ref.in;
    if (nextIn && prevState.status === UNMOUNTED) {
      return {
        status: EXITED,
      };
    }
    return null;
  };
  var _proto = Transition2.prototype;
  _proto.componentDidMount = function componentDidMount() {
    this.updateStatus(true, this.appearStatus);
  };
  _proto.componentDidUpdate = function componentDidUpdate(prevProps) {
    var nextStatus = null;
    if (prevProps !== this.props) {
      var status = this.state.status;
      if (this.props.in) {
        if (status !== ENTERING && status !== ENTERED) {
          nextStatus = ENTERING;
        }
      } else {
        if (status === ENTERING || status === ENTERED) {
          nextStatus = EXITING;
        }
      }
    }
    this.updateStatus(false, nextStatus);
  };
  _proto.componentWillUnmount = function componentWillUnmount() {
    this.cancelNextCallback();
  };
  _proto.getTimeouts = function getTimeouts() {
    var timeout2 = this.props.timeout;
    var exit, enter, appear;
    exit = enter = appear = timeout2;
    if (timeout2 != null && typeof timeout2 !== "number") {
      exit = timeout2.exit;
      enter = timeout2.enter;
      appear = timeout2.appear !== void 0 ? timeout2.appear : enter;
    }
    return {
      exit,
      enter,
      appear,
    };
  };
  _proto.updateStatus = function updateStatus(mounting, nextStatus) {
    if (mounting === void 0) {
      mounting = false;
    }
    if (nextStatus !== null) {
      this.cancelNextCallback();
      if (nextStatus === ENTERING) {
        if (this.props.unmountOnExit || this.props.mountOnEnter) {
          var node2 = this.props.nodeRef
            ? this.props.nodeRef.current
            : import_react_dom.default.findDOMNode(this);
          if (node2) forceReflow(node2);
        }
        this.performEnter(mounting);
      } else {
        this.performExit();
      }
    } else if (this.props.unmountOnExit && this.state.status === EXITED) {
      this.setState({
        status: UNMOUNTED,
      });
    }
  };
  _proto.performEnter = function performEnter(mounting) {
    var _this2 = this;
    var enter = this.props.enter;
    var appearing = this.context ? this.context.isMounting : mounting;
    var _ref2 = this.props.nodeRef
        ? [appearing]
        : [import_react_dom.default.findDOMNode(this), appearing],
      maybeNode = _ref2[0],
      maybeAppearing = _ref2[1];
    var timeouts = this.getTimeouts();
    var enterTimeout = appearing ? timeouts.appear : timeouts.enter;
    if ((!mounting && !enter) || config_default.disabled) {
      this.safeSetState(
        {
          status: ENTERED,
        },
        function () {
          _this2.props.onEntered(maybeNode);
        },
      );
      return;
    }
    this.props.onEnter(maybeNode, maybeAppearing);
    this.safeSetState(
      {
        status: ENTERING,
      },
      function () {
        _this2.props.onEntering(maybeNode, maybeAppearing);
        _this2.onTransitionEnd(enterTimeout, function () {
          _this2.safeSetState(
            {
              status: ENTERED,
            },
            function () {
              _this2.props.onEntered(maybeNode, maybeAppearing);
            },
          );
        });
      },
    );
  };
  _proto.performExit = function performExit() {
    var _this3 = this;
    var exit = this.props.exit;
    var timeouts = this.getTimeouts();
    var maybeNode = this.props.nodeRef
      ? void 0
      : import_react_dom.default.findDOMNode(this);
    if (!exit || config_default.disabled) {
      this.safeSetState(
        {
          status: EXITED,
        },
        function () {
          _this3.props.onExited(maybeNode);
        },
      );
      return;
    }
    this.props.onExit(maybeNode);
    this.safeSetState(
      {
        status: EXITING,
      },
      function () {
        _this3.props.onExiting(maybeNode);
        _this3.onTransitionEnd(timeouts.exit, function () {
          _this3.safeSetState(
            {
              status: EXITED,
            },
            function () {
              _this3.props.onExited(maybeNode);
            },
          );
        });
      },
    );
  };
  _proto.cancelNextCallback = function cancelNextCallback() {
    if (this.nextCallback !== null) {
      this.nextCallback.cancel();
      this.nextCallback = null;
    }
  };
  _proto.safeSetState = function safeSetState(nextState, callback) {
    callback = this.setNextCallback(callback);
    this.setState(nextState, callback);
  };
  _proto.setNextCallback = function setNextCallback(callback) {
    var _this4 = this;
    var active = true;
    this.nextCallback = function (event) {
      if (active) {
        active = false;
        _this4.nextCallback = null;
        callback(event);
      }
    };
    this.nextCallback.cancel = function () {
      active = false;
    };
    return this.nextCallback;
  };
  _proto.onTransitionEnd = function onTransitionEnd(timeout2, handler) {
    this.setNextCallback(handler);
    var node2 = this.props.nodeRef
      ? this.props.nodeRef.current
      : import_react_dom.default.findDOMNode(this);
    var doesNotHaveTimeoutOrListener =
      timeout2 == null && !this.props.addEndListener;
    if (!node2 || doesNotHaveTimeoutOrListener) {
      setTimeout(this.nextCallback, 0);
      return;
    }
    if (this.props.addEndListener) {
      var _ref3 = this.props.nodeRef
          ? [this.nextCallback]
          : [node2, this.nextCallback],
        maybeNode = _ref3[0],
        maybeNextCallback = _ref3[1];
      this.props.addEndListener(maybeNode, maybeNextCallback);
    }
    if (timeout2 != null) {
      setTimeout(this.nextCallback, timeout2);
    }
  };
  _proto.render = function render() {
    var status = this.state.status;
    if (status === UNMOUNTED) {
      return null;
    }
    var _this$props = this.props,
      children = _this$props.children,
      _in = _this$props.in,
      _mountOnEnter = _this$props.mountOnEnter,
      _unmountOnExit = _this$props.unmountOnExit,
      _appear = _this$props.appear,
      _enter = _this$props.enter,
      _exit = _this$props.exit,
      _timeout = _this$props.timeout,
      _addEndListener = _this$props.addEndListener,
      _onEnter = _this$props.onEnter,
      _onEntering = _this$props.onEntering,
      _onEntered = _this$props.onEntered,
      _onExit = _this$props.onExit,
      _onExiting = _this$props.onExiting,
      _onExited = _this$props.onExited,
      _nodeRef = _this$props.nodeRef,
      childProps = _objectWithoutPropertiesLoose(_this$props, [
        "children",
        "in",
        "mountOnEnter",
        "unmountOnExit",
        "appear",
        "enter",
        "exit",
        "timeout",
        "addEndListener",
        "onEnter",
        "onEntering",
        "onEntered",
        "onExit",
        "onExiting",
        "onExited",
        "nodeRef",
      ]);
    return (
      // allows for nested Transitions
      /* @__PURE__ */ import_react7.default.createElement(
        TransitionGroupContext_default.Provider,
        {
          value: null,
        },
        typeof children === "function"
          ? children(status, childProps)
          : import_react7.default.cloneElement(
              import_react7.default.Children.only(children),
              childProps,
            ),
      )
    );
  };
  return Transition2;
})(import_react7.default.Component);
Transition.contextType = TransitionGroupContext_default;
Transition.propTypes = false
  ? {
      /**
       * A React reference to DOM element that need to transition:
       * https://stackoverflow.com/a/51127130/4671932
       *
       *   - When `nodeRef` prop is used, `node` is not passed to callback functions
       *      (e.g. `onEnter`) because user already has direct access to the node.
       *   - When changing `key` prop of `Transition` in a `TransitionGroup` a new
       *     `nodeRef` need to be provided to `Transition` with changed `key` prop
       *     (see
       *     [test/CSSTransition-test.js](https://github.com/reactjs/react-transition-group/blob/13435f897b3ab71f6e19d724f145596f5910581c/test/CSSTransition-test.js#L362-L437)).
       */
      nodeRef: import_prop_types.default.shape({
        current:
          typeof Element === "undefined"
            ? import_prop_types.default.any
            : function (
                propValue,
                key,
                componentName,
                location2,
                propFullName,
                secret,
              ) {
                var value = propValue[key];
                return import_prop_types.default.instanceOf(
                  value && "ownerDocument" in value
                    ? value.ownerDocument.defaultView.Element
                    : Element,
                )(
                  propValue,
                  key,
                  componentName,
                  location2,
                  propFullName,
                  secret,
                );
              },
      }),
      /**
       * A `function` child can be used instead of a React element. This function is
       * called with the current transition status (`'entering'`, `'entered'`,
       * `'exiting'`, `'exited'`), which can be used to apply context
       * specific props to a component.
       *
       * ```jsx
       * <Transition in={this.state.in} timeout={150}>
       *   {state => (
       *     <MyComponent className={`fade fade-${state}`} />
       *   )}
       * </Transition>
       * ```
       */
      children: import_prop_types.default.oneOfType([
        import_prop_types.default.func.isRequired,
        import_prop_types.default.element.isRequired,
      ]).isRequired,
      /**
       * Show the component; triggers the enter or exit states
       */
      in: import_prop_types.default.bool,
      /**
       * By default the child component is mounted immediately along with
       * the parent `Transition` component. If you want to "lazy mount" the component on the
       * first `in={true}` you can set `mountOnEnter`. After the first enter transition the component will stay
       * mounted, even on "exited", unless you also specify `unmountOnExit`.
       */
      mountOnEnter: import_prop_types.default.bool,
      /**
       * By default the child component stays mounted after it reaches the `'exited'` state.
       * Set `unmountOnExit` if you'd prefer to unmount the component after it finishes exiting.
       */
      unmountOnExit: import_prop_types.default.bool,
      /**
       * By default the child component does not perform the enter transition when
       * it first mounts, regardless of the value of `in`. If you want this
       * behavior, set both `appear` and `in` to `true`.
       *
       * > **Note**: there are no special appear states like `appearing`/`appeared`, this prop
       * > only adds an additional enter transition. However, in the
       * > `<CSSTransition>` component that first enter transition does result in
       * > additional `.appear-*` classes, that way you can choose to style it
       * > differently.
       */
      appear: import_prop_types.default.bool,
      /**
       * Enable or disable enter transitions.
       */
      enter: import_prop_types.default.bool,
      /**
       * Enable or disable exit transitions.
       */
      exit: import_prop_types.default.bool,
      /**
       * The duration of the transition, in milliseconds.
       * Required unless `addEndListener` is provided.
       *
       * You may specify a single timeout for all transitions:
       *
       * ```jsx
       * timeout={500}
       * ```
       *
       * or individually:
       *
       * ```jsx
       * timeout={{
       *  appear: 500,
       *  enter: 300,
       *  exit: 500,
       * }}
       * ```
       *
       * - `appear` defaults to the value of `enter`
       * - `enter` defaults to `0`
       * - `exit` defaults to `0`
       *
       * @type {number | { enter?: number, exit?: number, appear?: number }}
       */
      timeout: function timeout(props) {
        var pt = timeoutsShape;
        if (!props.addEndListener) pt = pt.isRequired;
        for (
          var _len = arguments.length,
            args = new Array(_len > 1 ? _len - 1 : 0),
            _key = 1;
          _key < _len;
          _key++
        ) {
          args[_key - 1] = arguments[_key];
        }
        return pt.apply(void 0, [props].concat(args));
      },
      /**
       * Add a custom transition end trigger. Called with the transitioning
       * DOM node and a `done` callback. Allows for more fine grained transition end
       * logic. Timeouts are still used as a fallback if provided.
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * ```jsx
       * addEndListener={(node, done) => {
       *   // use the css transitionend event to mark the finish of a transition
       *   node.addEventListener('transitionend', done, false);
       * }}
       * ```
       */
      addEndListener: import_prop_types.default.func,
      /**
       * Callback fired before the "entering" status is applied. An extra parameter
       * `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * @type Function(node: HtmlElement, isAppearing: bool) -> void
       */
      onEnter: import_prop_types.default.func,
      /**
       * Callback fired after the "entering" status is applied. An extra parameter
       * `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * @type Function(node: HtmlElement, isAppearing: bool)
       */
      onEntering: import_prop_types.default.func,
      /**
       * Callback fired after the "entered" status is applied. An extra parameter
       * `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * @type Function(node: HtmlElement, isAppearing: bool) -> void
       */
      onEntered: import_prop_types.default.func,
      /**
       * Callback fired before the "exiting" status is applied.
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * @type Function(node: HtmlElement) -> void
       */
      onExit: import_prop_types.default.func,
      /**
       * Callback fired after the "exiting" status is applied.
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * @type Function(node: HtmlElement) -> void
       */
      onExiting: import_prop_types.default.func,
      /**
       * Callback fired after the "exited" status is applied.
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed
       *
       * @type Function(node: HtmlElement) -> void
       */
      onExited: import_prop_types.default.func,
    }
  : {};
function noop3() {}
Transition.defaultProps = {
  in: false,
  mountOnEnter: false,
  unmountOnExit: false,
  appear: false,
  enter: true,
  exit: true,
  onEnter: noop3,
  onEntering: noop3,
  onEntered: noop3,
  onExit: noop3,
  onExiting: noop3,
  onExited: noop3,
};
Transition.UNMOUNTED = UNMOUNTED;
Transition.EXITED = EXITED;
Transition.ENTERING = ENTERING;
Transition.ENTERED = ENTERED;
Transition.EXITING = EXITING;
var Transition_default = Transition;

// node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/assertThisInitialized.js
function _assertThisInitialized(e) {
  if (void 0 === e)
    throw new ReferenceError(
      "this hasn't been initialised - super() hasn't been called",
    );
  return e;
}

// node_modules/.pnpm/react-transition-group@4.4._c8c71776d32028e3f61da5b0688040c1/node_modules/react-transition-group/esm/TransitionGroup.js
var import_react9 = __toESM(require_react());

// node_modules/.pnpm/react-transition-group@4.4._c8c71776d32028e3f61da5b0688040c1/node_modules/react-transition-group/esm/utils/ChildMapping.js
var import_react8 = __toESM(require_react());
function getChildMapping(children, mapFn) {
  var mapper = function mapper2(child) {
    return mapFn && (0, import_react8.isValidElement)(child)
      ? mapFn(child)
      : child;
  };
  var result = /* @__PURE__ */ Object.create(null);
  if (children)
    import_react8.Children.map(children, function (c) {
      return c;
    }).forEach(function (child) {
      result[child.key] = mapper(child);
    });
  return result;
}
function mergeChildMappings(prev2, next2) {
  prev2 = prev2 || {};
  next2 = next2 || {};
  function getValueForKey(key) {
    return key in next2 ? next2[key] : prev2[key];
  }
  var nextKeysPending = /* @__PURE__ */ Object.create(null);
  var pendingKeys = [];
  for (var prevKey in prev2) {
    if (prevKey in next2) {
      if (pendingKeys.length) {
        nextKeysPending[prevKey] = pendingKeys;
        pendingKeys = [];
      }
    } else {
      pendingKeys.push(prevKey);
    }
  }
  var i;
  var childMapping = {};
  for (var nextKey in next2) {
    if (nextKeysPending[nextKey]) {
      for (i = 0; i < nextKeysPending[nextKey].length; i++) {
        var pendingNextKey = nextKeysPending[nextKey][i];
        childMapping[nextKeysPending[nextKey][i]] =
          getValueForKey(pendingNextKey);
      }
    }
    childMapping[nextKey] = getValueForKey(nextKey);
  }
  for (i = 0; i < pendingKeys.length; i++) {
    childMapping[pendingKeys[i]] = getValueForKey(pendingKeys[i]);
  }
  return childMapping;
}
function getProp(child, prop, props) {
  return props[prop] != null ? props[prop] : child.props[prop];
}
function getInitialChildMapping(props, onExited) {
  return getChildMapping(props.children, function (child) {
    return (0, import_react8.cloneElement)(child, {
      onExited: onExited.bind(null, child),
      in: true,
      appear: getProp(child, "appear", props),
      enter: getProp(child, "enter", props),
      exit: getProp(child, "exit", props),
    });
  });
}
function getNextChildMapping(nextProps, prevChildMapping, onExited) {
  var nextChildMapping = getChildMapping(nextProps.children);
  var children = mergeChildMappings(prevChildMapping, nextChildMapping);
  Object.keys(children).forEach(function (key) {
    var child = children[key];
    if (!(0, import_react8.isValidElement)(child)) return;
    var hasPrev = key in prevChildMapping;
    var hasNext = key in nextChildMapping;
    var prevChild = prevChildMapping[key];
    var isLeaving =
      (0, import_react8.isValidElement)(prevChild) && !prevChild.props.in;
    if (hasNext && (!hasPrev || isLeaving)) {
      children[key] = (0, import_react8.cloneElement)(child, {
        onExited: onExited.bind(null, child),
        in: true,
        exit: getProp(child, "exit", nextProps),
        enter: getProp(child, "enter", nextProps),
      });
    } else if (!hasNext && hasPrev && !isLeaving) {
      children[key] = (0, import_react8.cloneElement)(child, {
        in: false,
      });
    } else if (
      hasNext &&
      hasPrev &&
      (0, import_react8.isValidElement)(prevChild)
    ) {
      children[key] = (0, import_react8.cloneElement)(child, {
        onExited: onExited.bind(null, child),
        in: prevChild.props.in,
        exit: getProp(child, "exit", nextProps),
        enter: getProp(child, "enter", nextProps),
      });
    }
  });
  return children;
}

// node_modules/.pnpm/react-transition-group@4.4._c8c71776d32028e3f61da5b0688040c1/node_modules/react-transition-group/esm/TransitionGroup.js
var values2 =
  Object.values ||
  function (obj) {
    return Object.keys(obj).map(function (k) {
      return obj[k];
    });
  };
var defaultProps = {
  component: "div",
  childFactory: function childFactory(child) {
    return child;
  },
};
var TransitionGroup = /* @__PURE__ */ (function (_React$Component) {
  _inheritsLoose(TransitionGroup2, _React$Component);
  function TransitionGroup2(props, context) {
    var _this;
    _this = _React$Component.call(this, props, context) || this;
    var handleExited = _this.handleExited.bind(_assertThisInitialized(_this));
    _this.state = {
      contextValue: {
        isMounting: true,
      },
      handleExited,
      firstRender: true,
    };
    return _this;
  }
  var _proto = TransitionGroup2.prototype;
  _proto.componentDidMount = function componentDidMount() {
    this.mounted = true;
    this.setState({
      contextValue: {
        isMounting: false,
      },
    });
  };
  _proto.componentWillUnmount = function componentWillUnmount() {
    this.mounted = false;
  };
  TransitionGroup2.getDerivedStateFromProps = function getDerivedStateFromProps(
    nextProps,
    _ref,
  ) {
    var prevChildMapping = _ref.children,
      handleExited = _ref.handleExited,
      firstRender = _ref.firstRender;
    return {
      children: firstRender
        ? getInitialChildMapping(nextProps, handleExited)
        : getNextChildMapping(nextProps, prevChildMapping, handleExited),
      firstRender: false,
    };
  };
  _proto.handleExited = function handleExited(child, node2) {
    var currentChildMapping = getChildMapping(this.props.children);
    if (child.key in currentChildMapping) return;
    if (child.props.onExited) {
      child.props.onExited(node2);
    }
    if (this.mounted) {
      this.setState(function (state) {
        var children = _extends({}, state.children);
        delete children[child.key];
        return {
          children,
        };
      });
    }
  };
  _proto.render = function render() {
    var _this$props = this.props,
      Component = _this$props.component,
      childFactory2 = _this$props.childFactory,
      props = _objectWithoutPropertiesLoose(_this$props, [
        "component",
        "childFactory",
      ]);
    var contextValue = this.state.contextValue;
    var children = values2(this.state.children).map(childFactory2);
    delete props.appear;
    delete props.enter;
    delete props.exit;
    if (Component === null) {
      return /* @__PURE__ */ import_react9.default.createElement(
        TransitionGroupContext_default.Provider,
        {
          value: contextValue,
        },
        children,
      );
    }
    return /* @__PURE__ */ import_react9.default.createElement(
      TransitionGroupContext_default.Provider,
      {
        value: contextValue,
      },
      /* @__PURE__ */ import_react9.default.createElement(
        Component,
        props,
        children,
      ),
    );
  };
  return TransitionGroup2;
})(import_react9.default.Component);
TransitionGroup.propTypes = false
  ? {
      /**
       * `<TransitionGroup>` renders a `<div>` by default. You can change this
       * behavior by providing a `component` prop.
       * If you use React v16+ and would like to avoid a wrapping `<div>` element
       * you can pass in `component={null}`. This is useful if the wrapping div
       * borks your css styles.
       */
      component: import_prop_types.default.any,
      /**
       * A set of `<Transition>` components, that are toggled `in` and out as they
       * leave. the `<TransitionGroup>` will inject specific transition props, so
       * remember to spread them through if you are wrapping the `<Transition>` as
       * with our `<Fade>` example.
       *
       * While this component is meant for multiple `Transition` or `CSSTransition`
       * children, sometimes you may want to have a single transition child with
       * content that you want to be transitioned out and in when you change it
       * (e.g. routes, images etc.) In that case you can change the `key` prop of
       * the transition child as you change its content, this will cause
       * `TransitionGroup` to transition the child out and back in.
       */
      children: import_prop_types.default.node,
      /**
       * A convenience prop that enables or disables appear animations
       * for all children. Note that specifying this will override any defaults set
       * on individual children Transitions.
       */
      appear: import_prop_types.default.bool,
      /**
       * A convenience prop that enables or disables enter animations
       * for all children. Note that specifying this will override any defaults set
       * on individual children Transitions.
       */
      enter: import_prop_types.default.bool,
      /**
       * A convenience prop that enables or disables exit animations
       * for all children. Note that specifying this will override any defaults set
       * on individual children Transitions.
       */
      exit: import_prop_types.default.bool,
      /**
       * You may need to apply reactive updates to a child as it is exiting.
       * This is generally done by using `cloneElement` however in the case of an exiting
       * child the element has already been removed and not accessible to the consumer.
       *
       * If you do need to update a child as it leaves you can provide a `childFactory`
       * to wrap every child, even the ones that are leaving.
       *
       * @type Function(child: ReactElement) -> ReactElement
       */
      childFactory: import_prop_types.default.func,
    }
  : {};
TransitionGroup.defaultProps = defaultProps;
var TransitionGroup_default = TransitionGroup;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/useLazyRef/useLazyRef.js
var React34 = __toESM(require_react(), 1);
var UNINITIALIZED = {};
function useLazyRef(init, initArg) {
  const ref = React34.useRef(UNINITIALIZED);
  if (ref.current === UNINITIALIZED) {
    ref.current = init(initArg);
  }
  return ref;
}

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/useOnMount/useOnMount.js
var React35 = __toESM(require_react(), 1);
var EMPTY = [];
function useOnMount(fn2) {
  React35.useEffect(fn2, EMPTY);
}

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/useTimeout/useTimeout.js
var Timeout = class _Timeout {
  static create() {
    return new _Timeout();
  }
  currentId = null;
  /**
   * Executes `fn` after `delay`, clearing any previously scheduled call.
   */
  start(delay, fn2) {
    this.clear();
    this.currentId = setTimeout(() => {
      this.currentId = null;
      fn2();
    }, delay);
  }
  clear = () => {
    if (this.currentId !== null) {
      clearTimeout(this.currentId);
      this.currentId = null;
    }
  };
  disposeEffect = () => {
    return this.clear;
  };
};
function useTimeout() {
  const timeout2 = useLazyRef(Timeout.create).current;
  useOnMount(timeout2.disposeEffect);
  return timeout2;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/transitions/utils.js
var reflow = (node2) => node2.scrollTop;
function normalizedTransitionCallback(nodeRef, callback) {
  return (maybeIsAppearing) => {
    if (callback) {
      const node2 = nodeRef.current;
      if (maybeIsAppearing === void 0) {
        callback(node2);
      } else {
        callback(node2, maybeIsAppearing);
      }
    }
  };
}
function getTransitionChildStyle(
  state,
  inProp,
  baseStyles,
  hiddenStyles4,
  styleProp,
  childStyle,
) {
  const base =
    state === "exited" && !inProp
      ? hiddenStyles4
      : baseStyles[state] || baseStyles.exited;
  return styleProp || childStyle
    ? {
        ...base,
        ...styleProp,
        ...childStyle,
      }
    : base;
}
function getTransitionProps(props, options) {
  const { timeout: timeout2, easing: easing2, style: style4 = {} } = props;
  return {
    duration:
      style4.transitionDuration ??
      (typeof timeout2 === "number" ? timeout2 : timeout2[options.mode] || 0),
    easing:
      style4.transitionTimingFunction ??
      (typeof easing2 === "object" ? easing2[options.mode] : easing2),
    delay: style4.transitionDelay,
  };
}

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/isHostComponent/isHostComponent.js
function isHostComponent(element) {
  return typeof element === "string";
}
var isHostComponent_default = isHostComponent;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/appendOwnerState/appendOwnerState.js
function appendOwnerState(elementType, otherProps, ownerState) {
  if (elementType === void 0 || isHostComponent_default(elementType)) {
    return otherProps;
  }
  return {
    ...otherProps,
    ownerState: {
      ...otherProps.ownerState,
      ...ownerState,
    },
  };
}
var appendOwnerState_default = appendOwnerState;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/resolveComponentProps/resolveComponentProps.js
function resolveComponentProps(componentProps, ownerState, slotState) {
  if (typeof componentProps === "function") {
    return componentProps(ownerState, slotState);
  }
  return componentProps;
}
var resolveComponentProps_default = resolveComponentProps;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/extractEventHandlers/extractEventHandlers.js
function extractEventHandlers(object, excludeKeys = []) {
  if (object === void 0) {
    return {};
  }
  const result = {};
  Object.keys(object)
    .filter(
      (prop) =>
        prop.match(/^on[A-Z]/) &&
        typeof object[prop] === "function" &&
        !excludeKeys.includes(prop),
    )
    .forEach((prop) => {
      result[prop] = object[prop];
    });
  return result;
}
var extractEventHandlers_default = extractEventHandlers;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/omitEventHandlers/omitEventHandlers.js
function omitEventHandlers(object) {
  if (object === void 0) {
    return {};
  }
  const result = {};
  Object.keys(object)
    .filter(
      (prop) => !(prop.match(/^on[A-Z]/) && typeof object[prop] === "function"),
    )
    .forEach((prop) => {
      result[prop] = object[prop];
    });
  return result;
}
var omitEventHandlers_default = omitEventHandlers;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/mergeSlotProps/mergeSlotProps.js
function mergeSlotProps2(parameters) {
  const {
    getSlotProps,
    additionalProps,
    externalSlotProps,
    externalForwardedProps,
    className,
  } = parameters;
  if (!getSlotProps) {
    const joinedClasses2 = clsx_default(
      additionalProps?.className,
      className,
      externalForwardedProps?.className,
      externalSlotProps?.className,
    );
    const mergedStyle2 = {
      ...additionalProps?.style,
      ...externalForwardedProps?.style,
      ...externalSlotProps?.style,
    };
    const props2 = {
      ...additionalProps,
      ...externalForwardedProps,
      ...externalSlotProps,
    };
    if (joinedClasses2.length > 0) {
      props2.className = joinedClasses2;
    }
    if (Object.keys(mergedStyle2).length > 0) {
      props2.style = mergedStyle2;
    }
    return {
      props: props2,
      internalRef: void 0,
    };
  }
  const eventHandlers = extractEventHandlers_default({
    ...externalForwardedProps,
    ...externalSlotProps,
  });
  const componentsPropsWithoutEventHandlers =
    omitEventHandlers_default(externalSlotProps);
  const otherPropsWithoutEventHandlers = omitEventHandlers_default(
    externalForwardedProps,
  );
  const internalSlotProps = getSlotProps(eventHandlers);
  const joinedClasses = clsx_default(
    internalSlotProps?.className,
    additionalProps?.className,
    className,
    externalForwardedProps?.className,
    externalSlotProps?.className,
  );
  const mergedStyle = {
    ...internalSlotProps?.style,
    ...additionalProps?.style,
    ...externalForwardedProps?.style,
    ...externalSlotProps?.style,
  };
  const props = {
    ...internalSlotProps,
    ...additionalProps,
    ...otherPropsWithoutEventHandlers,
    ...componentsPropsWithoutEventHandlers,
  };
  if (joinedClasses.length > 0) {
    props.className = joinedClasses;
  }
  if (Object.keys(mergedStyle).length > 0) {
    props.style = mergedStyle;
  }
  return {
    props,
    internalRef: internalSlotProps.ref,
  };
}
var mergeSlotProps_default = mergeSlotProps2;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/useSlot.js
function useSlot(name, parameters) {
  const {
    className,
    elementType: initialElementType,
    ownerState,
    externalForwardedProps,
    internalForwardedProps,
    shouldForwardComponentProp = false,
    ...useSlotPropsParams
  } = parameters;
  const {
    component: rootComponent,
    slots = {
      [name]: void 0,
    },
    slotProps = {
      [name]: void 0,
    },
    ...other
  } = externalForwardedProps;
  const elementType = slots[name] || initialElementType;
  const resolvedComponentsProps = resolveComponentProps_default(
    slotProps[name],
    ownerState,
  );
  const {
    props: { component: slotComponent, ...mergedProps },
    internalRef,
  } = mergeSlotProps_default({
    className,
    ...useSlotPropsParams,
    externalForwardedProps: name === "root" ? other : void 0,
    externalSlotProps: resolvedComponentsProps,
  });
  const ref = useForkRef(
    internalRef,
    resolvedComponentsProps?.ref,
    parameters.ref,
  );
  const LeafComponent =
    name === "root" ? slotComponent || rootComponent : slotComponent;
  const props = appendOwnerState_default(
    elementType,
    {
      ...(name === "root" &&
        !rootComponent &&
        !slots[name] &&
        internalForwardedProps),
      ...(name !== "root" && !slots[name] && internalForwardedProps),
      ...mergedProps,
      ...(LeafComponent &&
        !shouldForwardComponentProp && {
          as: LeafComponent,
        }),
      ...(LeafComponent &&
        shouldForwardComponentProp && {
          component: LeafComponent,
        }),
      ref,
    },
    ownerState,
  );
  return [elementType, props];
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Collapse/collapseClasses.js
function getCollapseUtilityClass(slot) {
  return generateUtilityClass("MuiCollapse", slot);
}
var collapseClasses = generateUtilityClasses("MuiCollapse", [
  "root",
  "horizontal",
  "vertical",
  "entered",
  "hidden",
  "wrapper",
  "wrapperInner",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Collapse/Collapse.js
var import_jsx_runtime22 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses3 = (ownerState) => {
  const { orientation, classes } = ownerState;
  const slots = {
    root: ["root", orientation],
    entered: ["entered"],
    hidden: ["hidden"],
    wrapper: ["wrapper", orientation],
    wrapperInner: ["wrapperInner", orientation],
  };
  return composeClasses(slots, getCollapseUtilityClass, classes);
};
var CollapseRoot = styled_default2("div", {
  name: "MuiCollapse",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      styles5[ownerState.orientation],
      ownerState.state === "entered" && styles5.entered,
      ownerState.state === "exited" &&
        !ownerState.in &&
        ownerState.collapsedSize === "0px" &&
        styles5.hidden,
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    height: 0,
    overflow: "hidden",
    transition: theme2.transitions.create("height"),
    variants: [
      {
        props: {
          orientation: "horizontal",
        },
        style: {
          height: "auto",
          width: 0,
          transition: theme2.transitions.create("width"),
        },
      },
      {
        props: {
          state: "entered",
        },
        style: {
          height: "auto",
          overflow: "visible",
        },
      },
      {
        props: {
          state: "entered",
          orientation: "horizontal",
        },
        style: {
          width: "auto",
        },
      },
      {
        props: ({ ownerState }) =>
          ownerState.state === "exited" &&
          !ownerState.in &&
          ownerState.collapsedSize === "0px",
        style: {
          visibility: "hidden",
        },
      },
    ],
  })),
);
var CollapseWrapper = styled_default2("div", {
  name: "MuiCollapse",
  slot: "Wrapper",
})({
  // Hack to get children with a negative margin to not falsify the height computation.
  display: "flex",
  width: "100%",
  variants: [
    {
      props: {
        orientation: "horizontal",
      },
      style: {
        width: "auto",
        height: "100%",
      },
    },
  ],
});
var CollapseWrapperInner = styled_default2("div", {
  name: "MuiCollapse",
  slot: "WrapperInner",
})({
  width: "100%",
  variants: [
    {
      props: {
        orientation: "horizontal",
      },
      style: {
        width: "auto",
        height: "100%",
      },
    },
  ],
});
var Collapse = /* @__PURE__ */ React36.forwardRef(
  function Collapse2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiCollapse",
    });
    const {
      addEndListener,
      children,
      className,
      collapsedSize: collapsedSizeProp = "0px",
      component,
      easing: easing2,
      in: inProp,
      onEnter,
      onEntered,
      onEntering,
      onExit,
      onExited,
      onExiting,
      orientation = "vertical",
      slots = {},
      slotProps = {},
      style: style4,
      timeout: timeout2 = duration.standard,
      // eslint-disable-next-line react/prop-types
      TransitionComponent = Transition_default,
      ...other
    } = props;
    const ownerState = {
      ...props,
      orientation,
      collapsedSize: collapsedSizeProp,
    };
    const classes = useUtilityClasses3(ownerState);
    const theme2 = useTheme5();
    const timer = useTimeout();
    const wrapperRef = React36.useRef(null);
    const autoTransitionDuration = React36.useRef();
    const collapsedSize =
      typeof collapsedSizeProp === "number"
        ? `${collapsedSizeProp}px`
        : collapsedSizeProp;
    const isHorizontal2 = orientation === "horizontal";
    const size = isHorizontal2 ? "width" : "height";
    const nodeRef = React36.useRef(null);
    const handleRef = useForkRef_default(ref, nodeRef);
    const getWrapperSize = () =>
      wrapperRef.current
        ? wrapperRef.current[isHorizontal2 ? "clientWidth" : "clientHeight"]
        : 0;
    const handleEnter = normalizedTransitionCallback(
      nodeRef,
      (node2, isAppearing) => {
        if (wrapperRef.current && isHorizontal2) {
          wrapperRef.current.style.position = "absolute";
        }
        node2.style[size] = collapsedSize;
        if (onEnter) {
          onEnter(node2, isAppearing);
        }
      },
    );
    const handleEntering = normalizedTransitionCallback(
      nodeRef,
      (node2, isAppearing) => {
        const wrapperSize = getWrapperSize();
        if (wrapperRef.current && isHorizontal2) {
          wrapperRef.current.style.position = "";
        }
        const {
          duration: transitionDuration,
          easing: transitionTimingFunction,
        } = getTransitionProps(
          {
            style: style4,
            timeout: timeout2,
            easing: easing2,
          },
          {
            mode: "enter",
          },
        );
        if (timeout2 === "auto") {
          const duration2 =
            theme2.transitions.getAutoHeightDuration(wrapperSize);
          node2.style.transitionDuration = `${duration2}ms`;
          autoTransitionDuration.current = duration2;
        } else {
          node2.style.transitionDuration =
            typeof transitionDuration === "string"
              ? transitionDuration
              : `${transitionDuration}ms`;
        }
        node2.style[size] = `${wrapperSize}px`;
        node2.style.transitionTimingFunction = transitionTimingFunction;
        if (onEntering) {
          onEntering(node2, isAppearing);
        }
      },
    );
    const handleEntered = normalizedTransitionCallback(
      nodeRef,
      (node2, isAppearing) => {
        node2.style[size] = "auto";
        if (onEntered) {
          onEntered(node2, isAppearing);
        }
      },
    );
    const handleExit = normalizedTransitionCallback(nodeRef, (node2) => {
      node2.style[size] = `${getWrapperSize()}px`;
      if (onExit) {
        onExit(node2);
      }
    });
    const handleExited = normalizedTransitionCallback(nodeRef, onExited);
    const handleExiting = normalizedTransitionCallback(nodeRef, (node2) => {
      const wrapperSize = getWrapperSize();
      const { duration: transitionDuration, easing: transitionTimingFunction } =
        getTransitionProps(
          {
            style: style4,
            timeout: timeout2,
            easing: easing2,
          },
          {
            mode: "exit",
          },
        );
      if (timeout2 === "auto") {
        const duration2 = theme2.transitions.getAutoHeightDuration(wrapperSize);
        node2.style.transitionDuration = `${duration2}ms`;
        autoTransitionDuration.current = duration2;
      } else {
        node2.style.transitionDuration =
          typeof transitionDuration === "string"
            ? transitionDuration
            : `${transitionDuration}ms`;
      }
      node2.style[size] = collapsedSize;
      node2.style.transitionTimingFunction = transitionTimingFunction;
      if (onExiting) {
        onExiting(node2);
      }
    });
    const handleAddEndListener = (next2) => {
      if (timeout2 === "auto") {
        timer.start(autoTransitionDuration.current || 0, next2);
      }
      if (addEndListener) {
        addEndListener(nodeRef.current, next2);
      }
    };
    const externalForwardedProps = {
      slots,
      slotProps,
      component,
    };
    const [RootSlot, rootSlotProps] = useSlot("root", {
      ref: handleRef,
      className: clsx_default(classes.root, className),
      elementType: CollapseRoot,
      externalForwardedProps,
      ownerState,
      additionalProps: {
        style: {
          [isHorizontal2 ? "minWidth" : "minHeight"]: collapsedSize,
          ...style4,
        },
      },
    });
    const [WrapperSlot, wrapperSlotProps] = useSlot("wrapper", {
      ref: wrapperRef,
      className: classes.wrapper,
      elementType: CollapseWrapper,
      externalForwardedProps,
      ownerState,
    });
    const [WrapperInnerSlot, wrapperInnerSlotProps] = useSlot("wrapperInner", {
      className: classes.wrapperInner,
      elementType: CollapseWrapperInner,
      externalForwardedProps,
      ownerState,
    });
    return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(TransitionComponent, {
      in: inProp,
      onEnter: handleEnter,
      onEntered: handleEntered,
      onEntering: handleEntering,
      onExit: handleExit,
      onExited: handleExited,
      onExiting: handleExiting,
      addEndListener: handleAddEndListener,
      nodeRef,
      timeout: timeout2 === "auto" ? null : timeout2,
      ...other,
      children: (
        state,
        { ownerState: incomingOwnerState, ...restChildProps },
      ) => {
        const stateOwnerState = {
          ...ownerState,
          state,
        };
        return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(RootSlot, {
          ...rootSlotProps,
          className: clsx_default(
            rootSlotProps.className,
            {
              entered: classes.entered,
              exited: !inProp && collapsedSize === "0px" && classes.hidden,
            }[state],
          ),
          ownerState: stateOwnerState,
          ...restChildProps,
          children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(WrapperSlot, {
            ...wrapperSlotProps,
            ownerState: stateOwnerState,
            children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
              WrapperInnerSlot,
              {
                ...wrapperInnerSlotProps,
                ownerState: stateOwnerState,
                children,
              },
            ),
          }),
        });
      },
    });
  },
);
false
  ? (Collapse.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * Add a custom transition end trigger. Called with the transitioning DOM
       * node and a done callback. Allows for more fine grained transition end
       * logic. Note: Timeouts are still used as a fallback if provided.
       */
      addEndListener: import_prop_types.default.func,
      /**
       * The content node to be collapsed.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The width (horizontal) or height (vertical) of the container when collapsed.
       * @default '0px'
       */
      collapsedSize: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: elementTypeAcceptingRef_default,
      /**
       * The transition timing function.
       * You may specify a single easing or a object containing enter and exit values.
       */
      easing: import_prop_types.default.oneOfType([
        import_prop_types.default.shape({
          enter: import_prop_types.default.string,
          exit: import_prop_types.default.string,
        }),
        import_prop_types.default.string,
      ]),
      /**
       * If `true`, the component will transition in.
       */
      in: import_prop_types.default.bool,
      /**
       * @ignore
       */
      onEnter: import_prop_types.default.func,
      /**
       * @ignore
       */
      onEntered: import_prop_types.default.func,
      /**
       * @ignore
       */
      onEntering: import_prop_types.default.func,
      /**
       * @ignore
       */
      onExit: import_prop_types.default.func,
      /**
       * @ignore
       */
      onExited: import_prop_types.default.func,
      /**
       * @ignore
       */
      onExiting: import_prop_types.default.func,
      /**
       * The transition orientation.
       * @default 'vertical'
       */
      orientation: import_prop_types.default.oneOf(["horizontal", "vertical"]),
      /**
       * The props used for each slot inside.
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        root: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        wrapper: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        wrapperInner: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      }),
      /**
       * The components used for each slot inside.
       * @default {}
       */
      slots: import_prop_types.default.shape({
        root: import_prop_types.default.elementType,
        wrapper: import_prop_types.default.elementType,
        wrapperInner: import_prop_types.default.elementType,
      }),
      /**
       * @ignore
       */
      style: import_prop_types.default.object,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * The duration for the transition, in milliseconds.
       * You may specify a single timeout for all transitions, or individually with an object.
       *
       * Set to 'auto' to automatically calculate transition time based on height.
       * @default duration.standard
       */
      timeout: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["auto"]),
        import_prop_types.default.number,
        import_prop_types.default.shape({
          appear: import_prop_types.default.number,
          enter: import_prop_types.default.number,
          exit: import_prop_types.default.number,
        }),
      ]),
    })
  : void 0;
if (Collapse) {
  Collapse.muiSupportAuto = true;
}
var Collapse_default = Collapse;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Paper/Paper.js
var React37 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Paper/paperClasses.js
function getPaperUtilityClass(slot) {
  return generateUtilityClass("MuiPaper", slot);
}
var paperClasses = generateUtilityClasses("MuiPaper", [
  "root",
  "rounded",
  "outlined",
  "elevation",
  "elevation0",
  "elevation1",
  "elevation2",
  "elevation3",
  "elevation4",
  "elevation5",
  "elevation6",
  "elevation7",
  "elevation8",
  "elevation9",
  "elevation10",
  "elevation11",
  "elevation12",
  "elevation13",
  "elevation14",
  "elevation15",
  "elevation16",
  "elevation17",
  "elevation18",
  "elevation19",
  "elevation20",
  "elevation21",
  "elevation22",
  "elevation23",
  "elevation24",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Paper/Paper.js
var import_jsx_runtime23 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses4 = (ownerState) => {
  const { square, elevation, variant, classes } = ownerState;
  const slots = {
    root: [
      "root",
      variant,
      !square && "rounded",
      variant === "elevation" && `elevation${elevation}`,
    ],
  };
  return composeClasses(slots, getPaperUtilityClass, classes);
};
var PaperRoot = styled_default2("div", {
  name: "MuiPaper",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      styles5[ownerState.variant],
      !ownerState.square && styles5.rounded,
      ownerState.variant === "elevation" &&
        styles5[`elevation${ownerState.elevation}`],
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    backgroundColor: (theme2.vars || theme2).palette.background.paper,
    color: (theme2.vars || theme2).palette.text.primary,
    transition: theme2.transitions.create("box-shadow"),
    variants: [
      {
        props: ({ ownerState }) => !ownerState.square,
        style: {
          borderRadius: theme2.shape.borderRadius,
        },
      },
      {
        props: {
          variant: "outlined",
        },
        style: {
          border: `1px solid ${(theme2.vars || theme2).palette.divider}`,
        },
      },
      {
        props: {
          variant: "elevation",
        },
        style: {
          boxShadow: "var(--Paper-shadow)",
          backgroundImage: "var(--Paper-overlay)",
        },
      },
    ],
  })),
);
var Paper = /* @__PURE__ */ React37.forwardRef(function Paper2(inProps, ref) {
  const props = useDefaultProps2({
    props: inProps,
    name: "MuiPaper",
  });
  const theme2 = useTheme5();
  const {
    className,
    component = "div",
    elevation = 1,
    square = false,
    variant = "elevation",
    ...other
  } = props;
  const ownerState = {
    ...props,
    component,
    elevation,
    square,
    variant,
  };
  const classes = useUtilityClasses4(ownerState);
  if (false) {
    if (theme2.shadows[elevation] === void 0) {
      console.error(
        [
          `MUI: The elevation provided <Paper elevation={${elevation}}> is not available in the theme.`,
          `Please make sure that \`theme.shadows[${elevation}]\` is defined.`,
        ].join("\n"),
      );
    }
  }
  return /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(PaperRoot, {
    as: component,
    ownerState,
    className: clsx_default(classes.root, className),
    ref,
    ...other,
    style: {
      ...(variant === "elevation" && {
        "--Paper-shadow": (theme2.vars || theme2).shadows[elevation],
        ...(theme2.vars && {
          "--Paper-overlay": theme2.vars.overlays?.[elevation],
        }),
        ...(!theme2.vars &&
          theme2.palette.mode === "dark" && {
            "--Paper-overlay": `linear-gradient(${alpha("#fff", getOverlayAlpha(elevation))}, ${alpha("#fff", getOverlayAlpha(elevation))})`,
          }),
      }),
      ...other.style,
    },
  });
});
false
  ? (Paper.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * Shadow depth, corresponds to `dp` in the spec.
       * It accepts values between 0 and 24 inclusive.
       * @default 1
       */
      elevation: chainPropTypes(integerPropType_default, (props) => {
        const { elevation, variant } = props;
        if (elevation > 0 && variant === "outlined") {
          return new Error(
            `MUI: Combining \`elevation={${elevation}}\` with \`variant="${variant}"\` has no effect. Either use \`elevation={0}\` or use a different \`variant\`.`,
          );
        }
        return null;
      }),
      /**
       * If `true`, rounded corners are disabled.
       * @default false
       */
      square: import_prop_types.default.bool,
      /**
       * @ignore
       */
      style: import_prop_types.default.object,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * The variant to use.
       * @default 'elevation'
       */
      variant: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["elevation", "outlined"]),
        import_prop_types.default.string,
      ]),
    })
  : void 0;
var Paper_default = Paper;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/ButtonBase/ButtonBase.js
var React41 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/isFocusVisible/isFocusVisible.js
function isFocusVisible(element) {
  try {
    return element.matches(":focus-visible");
  } catch (error) {
    if (false) {
      console.warn(
        [
          "MUI: The `:focus-visible` pseudo class is not supported in this browser.",
          "Some components rely on this feature to work properly.",
        ].join("\n"),
      );
    }
  }
  return false;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/useLazyRipple/useLazyRipple.js
var React38 = __toESM(require_react(), 1);
var LazyRipple = class _LazyRipple {
  /** React ref to the ripple instance */
  /** If the ripple component should be mounted */
  /** Promise that resolves when the ripple component is mounted */
  /** If the ripple component has been mounted */
  /** React state hook setter */
  static create() {
    return new _LazyRipple();
  }
  static use() {
    const ripple = useLazyRef(_LazyRipple.create).current;
    const [shouldMount, setShouldMount] = React38.useState(false);
    ripple.shouldMount = shouldMount;
    ripple.setShouldMount = setShouldMount;
    React38.useEffect(ripple.mountEffect, [shouldMount]);
    return ripple;
  }
  constructor() {
    this.ref = {
      current: null,
    };
    this.mounted = null;
    this.didMount = false;
    this.shouldMount = false;
    this.setShouldMount = null;
  }
  mount() {
    if (!this.mounted) {
      this.mounted = createControlledPromise();
      this.shouldMount = true;
      this.setShouldMount(this.shouldMount);
    }
    return this.mounted;
  }
  mountEffect = () => {
    if (this.shouldMount && !this.didMount) {
      if (this.ref.current !== null) {
        this.didMount = true;
        this.mounted.resolve();
      }
    }
  };
  /* Ripple API */
  start(...args) {
    this.mount().then(() => this.ref.current?.start(...args));
  }
  stop(...args) {
    this.mount().then(() => this.ref.current?.stop(...args));
  }
  pulsate(...args) {
    this.mount().then(() => this.ref.current?.pulsate(...args));
  }
};
function useLazyRipple() {
  return LazyRipple.use();
}
function createControlledPromise() {
  let resolve;
  let reject;
  const p = new Promise((resolveFn, rejectFn) => {
    resolve = resolveFn;
    reject = rejectFn;
  });
  p.resolve = resolve;
  p.reject = reject;
  return p;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/ButtonBase/TouchRipple.js
var React40 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/ButtonBase/Ripple.js
var React39 = __toESM(require_react(), 1);
var import_jsx_runtime24 = __toESM(require_jsx_runtime(), 1);
function Ripple(props) {
  const {
    className,
    classes,
    pulsate = false,
    rippleX,
    rippleY,
    rippleSize,
    in: inProp,
    onExited,
    timeout: timeout2,
  } = props;
  const [leaving, setLeaving] = React39.useState(false);
  const rippleClassName = clsx_default(
    className,
    classes.ripple,
    classes.rippleVisible,
    pulsate && classes.ripplePulsate,
  );
  const rippleStyles = {
    width: rippleSize,
    height: rippleSize,
    top: -(rippleSize / 2) + rippleY,
    left: -(rippleSize / 2) + rippleX,
  };
  const childClassName = clsx_default(
    classes.child,
    leaving && classes.childLeaving,
    pulsate && classes.childPulsate,
  );
  if (!inProp && !leaving) {
    setLeaving(true);
  }
  React39.useEffect(() => {
    if (!inProp && onExited != null) {
      const timeoutId = setTimeout(onExited, timeout2);
      return () => {
        clearTimeout(timeoutId);
      };
    }
    return void 0;
  }, [onExited, inProp, timeout2]);
  return /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("span", {
    className: rippleClassName,
    style: rippleStyles,
    children: /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("span", {
      className: childClassName,
    }),
  });
}
false
  ? (Ripple.propTypes = {
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object.isRequired,
      className: import_prop_types.default.string,
      /**
       * @ignore - injected from TransitionGroup
       */
      in: import_prop_types.default.bool,
      /**
       * @ignore - injected from TransitionGroup
       */
      onExited: import_prop_types.default.func,
      /**
       * If `true`, the ripple pulsates, typically indicating the keyboard focus state of an element.
       */
      pulsate: import_prop_types.default.bool,
      /**
       * Diameter of the ripple.
       */
      rippleSize: import_prop_types.default.number,
      /**
       * Horizontal position of the ripple center.
       */
      rippleX: import_prop_types.default.number,
      /**
       * Vertical position of the ripple center.
       */
      rippleY: import_prop_types.default.number,
      /**
       * exit delay
       */
      timeout: import_prop_types.default.number.isRequired,
    })
  : void 0;
var Ripple_default = Ripple;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/ButtonBase/touchRippleClasses.js
var touchRippleClasses = generateUtilityClasses("MuiTouchRipple", [
  "root",
  "ripple",
  "rippleVisible",
  "ripplePulsate",
  "child",
  "childLeaving",
  "childPulsate",
]);
var touchRippleClasses_default = touchRippleClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/ButtonBase/TouchRipple.js
var import_jsx_runtime25 = __toESM(require_jsx_runtime(), 1);
var DURATION = 550;
var DELAY_RIPPLE = 80;
var enterKeyframe = keyframes`
  0% {
    transform: scale(0);
    opacity: 0.1;
  }

  100% {
    transform: scale(1);
    opacity: 0.3;
  }
`;
var exitKeyframe = keyframes`
  0% {
    opacity: 1;
  }

  100% {
    opacity: 0;
  }
`;
var pulsateKeyframe = keyframes`
  0% {
    transform: scale(1);
  }

  50% {
    transform: scale(0.92);
  }

  100% {
    transform: scale(1);
  }
`;
var TouchRippleRoot = styled_default2("span", {
  name: "MuiTouchRipple",
  slot: "Root",
})({
  overflow: "hidden",
  pointerEvents: "none",
  position: "absolute",
  zIndex: 0,
  top: 0,
  right: 0,
  bottom: 0,
  left: 0,
  borderRadius: "inherit",
});
var TouchRippleRipple = styled_default2(Ripple_default, {
  name: "MuiTouchRipple",
  slot: "Ripple",
})`
  opacity: 0;
  position: absolute;

  &.${touchRippleClasses_default.rippleVisible} {
    opacity: 0.3;
    transform: scale(1);
    animation-name: ${enterKeyframe};
    animation-duration: ${DURATION}ms;
    animation-timing-function: ${({ theme: theme2 }) =>
      theme2.transitions.easing.easeInOut};
  }

  &.${touchRippleClasses_default.ripplePulsate} {
    animation-duration: ${({ theme: theme2 }) =>
      theme2.transitions.duration.shorter}ms;
  }

  & .${touchRippleClasses_default.child} {
    opacity: 1;
    display: block;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    background-color: currentColor;
  }

  & .${touchRippleClasses_default.childLeaving} {
    opacity: 0;
    animation-name: ${exitKeyframe};
    animation-duration: ${DURATION}ms;
    animation-timing-function: ${({ theme: theme2 }) =>
      theme2.transitions.easing.easeInOut};
  }

  & .${touchRippleClasses_default.childPulsate} {
    position: absolute;
    /* @noflip */
    left: 0px;
    top: 0;
    animation-name: ${pulsateKeyframe};
    animation-duration: 2500ms;
    animation-timing-function: ${({ theme: theme2 }) =>
      theme2.transitions.easing.easeInOut};
    animation-iteration-count: infinite;
    animation-delay: 200ms;
  }
`;
var TouchRipple = /* @__PURE__ */ React40.forwardRef(
  function TouchRipple2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiTouchRipple",
    });
    const {
      center: centerProp = false,
      classes = {},
      className,
      ...other
    } = props;
    const [ripples, setRipples] = React40.useState([]);
    const nextKey = React40.useRef(0);
    const rippleCallback = React40.useRef(null);
    React40.useEffect(() => {
      if (rippleCallback.current) {
        rippleCallback.current();
        rippleCallback.current = null;
      }
    }, [ripples]);
    const ignoringMouseDown = React40.useRef(false);
    const startTimer = useTimeout();
    const startTimerCommit = React40.useRef(null);
    const container = React40.useRef(null);
    const startCommit = React40.useCallback(
      (params) => {
        const { pulsate: pulsate2, rippleX, rippleY, rippleSize, cb } = params;
        setRipples((oldRipples) => [
          ...oldRipples,
          /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
            TouchRippleRipple,
            {
              classes: {
                ripple: clsx_default(
                  classes.ripple,
                  touchRippleClasses_default.ripple,
                ),
                rippleVisible: clsx_default(
                  classes.rippleVisible,
                  touchRippleClasses_default.rippleVisible,
                ),
                ripplePulsate: clsx_default(
                  classes.ripplePulsate,
                  touchRippleClasses_default.ripplePulsate,
                ),
                child: clsx_default(
                  classes.child,
                  touchRippleClasses_default.child,
                ),
                childLeaving: clsx_default(
                  classes.childLeaving,
                  touchRippleClasses_default.childLeaving,
                ),
                childPulsate: clsx_default(
                  classes.childPulsate,
                  touchRippleClasses_default.childPulsate,
                ),
              },
              timeout: DURATION,
              pulsate: pulsate2,
              rippleX,
              rippleY,
              rippleSize,
            },
            nextKey.current,
          ),
        ]);
        nextKey.current += 1;
        rippleCallback.current = cb;
      },
      [classes],
    );
    const start2 = React40.useCallback(
      (event = {}, options = {}, cb = () => {}) => {
        const {
          pulsate: pulsate2 = false,
          center = centerProp || options.pulsate,
          fakeElement = false,
          // For test purposes
        } = options;
        if (event?.type === "mousedown" && ignoringMouseDown.current) {
          ignoringMouseDown.current = false;
          return;
        }
        if (event?.type === "touchstart") {
          ignoringMouseDown.current = true;
        }
        const element = fakeElement ? null : container.current;
        const rect = element
          ? element.getBoundingClientRect()
          : {
              width: 0,
              height: 0,
              left: 0,
              top: 0,
            };
        let rippleX;
        let rippleY;
        let rippleSize;
        if (
          center ||
          event === void 0 ||
          (event.clientX === 0 && event.clientY === 0) ||
          (!event.clientX && !event.touches)
        ) {
          rippleX = Math.round(rect.width / 2);
          rippleY = Math.round(rect.height / 2);
        } else {
          const { clientX, clientY } =
            event.touches && event.touches.length > 0
              ? event.touches[0]
              : event;
          rippleX = Math.round(clientX - rect.left);
          rippleY = Math.round(clientY - rect.top);
        }
        if (center) {
          rippleSize = Math.sqrt((2 * rect.width ** 2 + rect.height ** 2) / 3);
          if (rippleSize % 2 === 0) {
            rippleSize += 1;
          }
        } else {
          const sizeX =
            Math.max(
              Math.abs((element ? element.clientWidth : 0) - rippleX),
              rippleX,
            ) *
              2 +
            2;
          const sizeY =
            Math.max(
              Math.abs((element ? element.clientHeight : 0) - rippleY),
              rippleY,
            ) *
              2 +
            2;
          rippleSize = Math.sqrt(sizeX ** 2 + sizeY ** 2);
        }
        if (event?.touches) {
          if (startTimerCommit.current === null) {
            startTimerCommit.current = () => {
              startCommit({
                pulsate: pulsate2,
                rippleX,
                rippleY,
                rippleSize,
                cb,
              });
            };
            startTimer.start(DELAY_RIPPLE, () => {
              if (startTimerCommit.current) {
                startTimerCommit.current();
                startTimerCommit.current = null;
              }
            });
          }
        } else {
          startCommit({
            pulsate: pulsate2,
            rippleX,
            rippleY,
            rippleSize,
            cb,
          });
        }
      },
      [centerProp, startCommit, startTimer],
    );
    const pulsate = React40.useCallback(() => {
      start2(
        {},
        {
          pulsate: true,
        },
      );
    }, [start2]);
    const stop = React40.useCallback(
      (event, cb) => {
        startTimer.clear();
        if (event?.type === "touchend" && startTimerCommit.current) {
          startTimerCommit.current();
          startTimerCommit.current = null;
          startTimer.start(0, () => {
            stop(event, cb);
          });
          return;
        }
        startTimerCommit.current = null;
        setRipples((oldRipples) => {
          if (oldRipples.length > 0) {
            return oldRipples.slice(1);
          }
          return oldRipples;
        });
        rippleCallback.current = cb;
      },
      [startTimer],
    );
    React40.useImperativeHandle(
      ref,
      () => ({
        pulsate,
        start: start2,
        stop,
      }),
      [pulsate, start2, stop],
    );
    return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(TouchRippleRoot, {
      className: clsx_default(
        touchRippleClasses_default.root,
        classes.root,
        className,
      ),
      ref: container,
      ...other,
      children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        TransitionGroup_default,
        {
          component: null,
          exit: true,
          children: ripples,
        },
      ),
    });
  },
);
false
  ? (TouchRipple.propTypes = {
      /**
       * If `true`, the ripple starts at the center of the component
       * rather than at the point of interaction.
       */
      center: import_prop_types.default.bool,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
    })
  : void 0;
var TouchRipple_default = TouchRipple;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/ButtonBase/buttonBaseClasses.js
function getButtonBaseUtilityClass(slot) {
  return generateUtilityClass("MuiButtonBase", slot);
}
var buttonBaseClasses = generateUtilityClasses("MuiButtonBase", [
  "root",
  "disabled",
  "focusVisible",
]);
var buttonBaseClasses_default = buttonBaseClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/ButtonBase/ButtonBase.js
var import_jsx_runtime26 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses5 = (ownerState) => {
  const { disabled, focusVisible, focusVisibleClassName, classes } = ownerState;
  const slots = {
    root: ["root", disabled && "disabled", focusVisible && "focusVisible"],
  };
  const composedClasses = composeClasses(
    slots,
    getButtonBaseUtilityClass,
    classes,
  );
  if (focusVisible && focusVisibleClassName) {
    composedClasses.root += ` ${focusVisibleClassName}`;
  }
  return composedClasses;
};
var ButtonBaseRoot = styled_default2("button", {
  name: "MuiButtonBase",
  slot: "Root",
})({
  display: "inline-flex",
  alignItems: "center",
  justifyContent: "center",
  position: "relative",
  boxSizing: "border-box",
  WebkitTapHighlightColor: "transparent",
  backgroundColor: "transparent",
  // Reset default value
  // We disable the focus ring for mouse, touch and keyboard users.
  outline: 0,
  border: 0,
  margin: 0,
  // Remove the margin in Safari
  borderRadius: 0,
  padding: 0,
  // Remove the padding in Firefox
  cursor: "pointer",
  userSelect: "none",
  verticalAlign: "middle",
  MozAppearance: "none",
  // Reset
  WebkitAppearance: "none",
  // Reset
  textDecoration: "none",
  // So we take precedent over the style of a native <a /> element.
  color: "inherit",
  "&::-moz-focus-inner": {
    borderStyle: "none",
    // Remove Firefox dotted outline.
  },
  [`&.${buttonBaseClasses_default.disabled}`]: {
    pointerEvents: "none",
    // Disable link interactions
    cursor: "default",
  },
  "@media print": {
    colorAdjust: "exact",
  },
});
var ButtonBase = /* @__PURE__ */ React41.forwardRef(
  function ButtonBase2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiButtonBase",
    });
    const {
      action,
      centerRipple = false,
      children,
      className,
      component = "button",
      disabled = false,
      disableRipple = false,
      disableTouchRipple = false,
      focusRipple = false,
      focusVisibleClassName,
      LinkComponent = "a",
      onBlur,
      onClick,
      onContextMenu,
      onDragLeave,
      onFocus,
      onFocusVisible,
      onKeyDown,
      onKeyUp,
      onMouseDown,
      onMouseLeave,
      onMouseUp,
      onTouchEnd,
      onTouchMove,
      onTouchStart,
      tabIndex = 0,
      TouchRippleProps,
      touchRippleRef,
      type,
      ...other
    } = props;
    const buttonRef = React41.useRef(null);
    const ripple = useLazyRipple();
    const handleRippleRef = useForkRef_default(ripple.ref, touchRippleRef);
    const [focusVisible, setFocusVisible] = React41.useState(false);
    if (disabled && focusVisible) {
      setFocusVisible(false);
    }
    React41.useImperativeHandle(
      action,
      () => ({
        focusVisible: () => {
          setFocusVisible(true);
          buttonRef.current.focus();
        },
      }),
      [],
    );
    const enableTouchRipple = ripple.shouldMount && !disableRipple && !disabled;
    React41.useEffect(() => {
      if (focusVisible && focusRipple && !disableRipple) {
        ripple.pulsate();
      }
    }, [disableRipple, focusRipple, focusVisible, ripple]);
    const handleMouseDown = useRippleHandler(
      ripple,
      "start",
      onMouseDown,
      disableTouchRipple,
    );
    const handleContextMenu = useRippleHandler(
      ripple,
      "stop",
      onContextMenu,
      disableTouchRipple,
    );
    const handleDragLeave = useRippleHandler(
      ripple,
      "stop",
      onDragLeave,
      disableTouchRipple,
    );
    const handleMouseUp = useRippleHandler(
      ripple,
      "stop",
      onMouseUp,
      disableTouchRipple,
    );
    const handleMouseLeave = useRippleHandler(
      ripple,
      "stop",
      (event) => {
        if (focusVisible) {
          event.preventDefault();
        }
        if (onMouseLeave) {
          onMouseLeave(event);
        }
      },
      disableTouchRipple,
    );
    const handleTouchStart = useRippleHandler(
      ripple,
      "start",
      onTouchStart,
      disableTouchRipple,
    );
    const handleTouchEnd = useRippleHandler(
      ripple,
      "stop",
      onTouchEnd,
      disableTouchRipple,
    );
    const handleTouchMove = useRippleHandler(
      ripple,
      "stop",
      onTouchMove,
      disableTouchRipple,
    );
    const handleBlur = useRippleHandler(
      ripple,
      "stop",
      (event) => {
        if (!isFocusVisible(event.target)) {
          setFocusVisible(false);
        }
        if (onBlur) {
          onBlur(event);
        }
      },
      false,
    );
    const handleFocus = useEventCallback_default2((event) => {
      if (!buttonRef.current) {
        buttonRef.current = event.currentTarget;
      }
      if (isFocusVisible(event.target)) {
        setFocusVisible(true);
        if (onFocusVisible) {
          onFocusVisible(event);
        }
      }
      if (onFocus) {
        onFocus(event);
      }
    });
    const isNonNativeButton = () => {
      const button = buttonRef.current;
      if (!button) {
        return component && component !== "button";
      }
      if (button.tagName === "BUTTON") {
        return false;
      }
      return !(button.tagName === "A" && button.href);
    };
    const handleKeyDown = useEventCallback_default2((event) => {
      if (focusRipple && !event.repeat && focusVisible && event.key === " ") {
        ripple.stop(event, () => {
          ripple.start(event);
        });
      }
      if (
        event.target === event.currentTarget &&
        isNonNativeButton() &&
        event.key === " "
      ) {
        event.preventDefault();
      }
      if (onKeyDown) {
        onKeyDown(event);
      }
      if (
        event.target === event.currentTarget &&
        isNonNativeButton() &&
        event.key === "Enter" &&
        !disabled
      ) {
        event.preventDefault();
        if (onClick) {
          onClick(event);
        }
      }
    });
    const handleKeyUp = useEventCallback_default2((event) => {
      if (
        focusRipple &&
        event.key === " " &&
        focusVisible &&
        !event.defaultPrevented
      ) {
        ripple.stop(event, () => {
          ripple.pulsate(event);
        });
      }
      if (onKeyUp) {
        onKeyUp(event);
      }
      if (
        onClick &&
        event.target === event.currentTarget &&
        isNonNativeButton() &&
        event.key === " " &&
        !event.defaultPrevented &&
        !disabled
      ) {
        onClick(event);
      }
    });
    let ComponentProp = component;
    if (ComponentProp === "button" && (other.href || other.to)) {
      ComponentProp = LinkComponent;
    }
    const buttonProps = {};
    if (ComponentProp === "button") {
      const hasFormAttributes = !!other.formAction;
      buttonProps.type =
        type === void 0 && !hasFormAttributes ? "button" : type;
      buttonProps.disabled = disabled;
    } else {
      if (!other.href && !other.to) {
        buttonProps.role = "button";
      }
      if (disabled) {
        buttonProps["aria-disabled"] = disabled;
      }
    }
    const handleRef = useForkRef_default(ref, buttonRef);
    const ownerState = {
      ...props,
      centerRipple,
      component,
      disabled,
      disableRipple,
      disableTouchRipple,
      focusRipple,
      tabIndex,
      focusVisible,
    };
    const classes = useUtilityClasses5(ownerState);
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)(ButtonBaseRoot, {
      as: ComponentProp,
      className: clsx_default(classes.root, className),
      ownerState,
      onBlur: handleBlur,
      onClick,
      onContextMenu: handleContextMenu,
      onFocus: handleFocus,
      onKeyDown: handleKeyDown,
      onKeyUp: handleKeyUp,
      onMouseDown: handleMouseDown,
      onMouseLeave: handleMouseLeave,
      onMouseUp: handleMouseUp,
      onDragLeave: handleDragLeave,
      onTouchEnd: handleTouchEnd,
      onTouchMove: handleTouchMove,
      onTouchStart: handleTouchStart,
      ref: handleRef,
      tabIndex: disabled ? -1 : tabIndex,
      type,
      ...buttonProps,
      ...other,
      children: [
        children,
        enableTouchRipple
          ? /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(TouchRipple_default, {
              ref: handleRippleRef,
              center: centerRipple,
              ...TouchRippleProps,
            })
          : null,
      ],
    });
  },
);
function useRippleHandler(
  ripple,
  rippleAction,
  eventCallback,
  skipRippleAction = false,
) {
  return useEventCallback_default2((event) => {
    if (eventCallback) {
      eventCallback(event);
    }
    if (!skipRippleAction) {
      ripple[rippleAction](event);
    }
    return true;
  });
}
false
  ? (ButtonBase.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * A ref for imperative actions.
       * It currently only supports `focusVisible()` action.
       */
      action: refType_default,
      /**
       * If `true`, the ripples are centered.
       * They won't start at the cursor interaction position.
       * @default false
       */
      centerRipple: import_prop_types.default.bool,
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: elementTypeAcceptingRef_default,
      /**
       * If `true`, the component is disabled.
       * @default false
       */
      disabled: import_prop_types.default.bool,
      /**
       * If `true`, the ripple effect is disabled.
       *
       * ⚠️ Without a ripple there is no styling for :focus-visible by default. Be sure
       * to highlight the element by applying separate styles with the `.Mui-focusVisible` class.
       * @default false
       */
      disableRipple: import_prop_types.default.bool,
      /**
       * If `true`, the touch ripple effect is disabled.
       * @default false
       */
      disableTouchRipple: import_prop_types.default.bool,
      /**
       * If `true`, the base button will have a keyboard focus ripple.
       * @default false
       */
      focusRipple: import_prop_types.default.bool,
      /**
       * This prop can help identify which element has keyboard focus.
       * The class name will be applied when the element gains the focus through keyboard interaction.
       * It's a polyfill for the [CSS :focus-visible selector](https://drafts.csswg.org/selectors-4/#the-focus-visible-pseudo).
       * The rationale for using this feature [is explained here](https://github.com/WICG/focus-visible/blob/HEAD/explainer.md).
       * A [polyfill can be used](https://github.com/WICG/focus-visible) to apply a `focus-visible` class to other components
       * if needed.
       */
      focusVisibleClassName: import_prop_types.default.string,
      /**
       * @ignore
       */
      formAction: import_prop_types.default.oneOfType([
        import_prop_types.default.func,
        import_prop_types.default.string,
      ]),
      /**
       * @ignore
       */
      href: import_prop_types.default.any,
      /**
       * The component used to render a link when the `href` prop is provided.
       * @default 'a'
       */
      LinkComponent: import_prop_types.default.elementType,
      /**
       * @ignore
       */
      onBlur: import_prop_types.default.func,
      /**
       * @ignore
       */
      onClick: import_prop_types.default.func,
      /**
       * @ignore
       */
      onContextMenu: import_prop_types.default.func,
      /**
       * @ignore
       */
      onDragLeave: import_prop_types.default.func,
      /**
       * @ignore
       */
      onFocus: import_prop_types.default.func,
      /**
       * Callback fired when the component is focused with a keyboard.
       * We trigger a `onFocus` callback too.
       */
      onFocusVisible: import_prop_types.default.func,
      /**
       * @ignore
       */
      onKeyDown: import_prop_types.default.func,
      /**
       * @ignore
       */
      onKeyUp: import_prop_types.default.func,
      /**
       * @ignore
       */
      onMouseDown: import_prop_types.default.func,
      /**
       * @ignore
       */
      onMouseLeave: import_prop_types.default.func,
      /**
       * @ignore
       */
      onMouseUp: import_prop_types.default.func,
      /**
       * @ignore
       */
      onTouchEnd: import_prop_types.default.func,
      /**
       * @ignore
       */
      onTouchMove: import_prop_types.default.func,
      /**
       * @ignore
       */
      onTouchStart: import_prop_types.default.func,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * @default 0
       */
      tabIndex: import_prop_types.default.number,
      /**
       * Props applied to the `TouchRipple` element.
       */
      TouchRippleProps: import_prop_types.default.object,
      /**
       * A ref that points to the `TouchRipple` element.
       */
      touchRippleRef: import_prop_types.default.oneOfType([
        import_prop_types.default.func,
        import_prop_types.default.shape({
          current: import_prop_types.default.shape({
            pulsate: import_prop_types.default.func.isRequired,
            start: import_prop_types.default.func.isRequired,
            stop: import_prop_types.default.func.isRequired,
          }),
        }),
      ]),
      /**
       * @ignore
       */
      type: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["button", "reset", "submit"]),
        import_prop_types.default.string,
      ]),
    })
  : void 0;
var ButtonBase_default = ButtonBase;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/createSimplePaletteValueFilter.js
function hasCorrectMainProperty(obj) {
  return typeof obj.main === "string";
}
function checkSimplePaletteColorValues(obj, additionalPropertiesToCheck = []) {
  if (!hasCorrectMainProperty(obj)) {
    return false;
  }
  for (const value of additionalPropertiesToCheck) {
    if (!obj.hasOwnProperty(value) || typeof obj[value] !== "string") {
      return false;
    }
  }
  return true;
}
function createSimplePaletteValueFilter(additionalPropertiesToCheck = []) {
  return ([, value]) =>
    value && checkSimplePaletteColorValues(value, additionalPropertiesToCheck);
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/IconButton/IconButton.js
var React43 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/CircularProgress/CircularProgress.js
var React42 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/CircularProgress/circularProgressClasses.js
function getCircularProgressUtilityClass(slot) {
  return generateUtilityClass("MuiCircularProgress", slot);
}
var circularProgressClasses = generateUtilityClasses("MuiCircularProgress", [
  "root",
  "determinate",
  "indeterminate",
  "colorPrimary",
  "colorSecondary",
  "svg",
  "track",
  "circle",
  "circleDeterminate",
  "circleIndeterminate",
  "circleDisableShrink",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/CircularProgress/CircularProgress.js
var import_jsx_runtime27 = __toESM(require_jsx_runtime(), 1);
var SIZE = 44;
var circularRotateKeyframe = keyframes`
  0% {
    transform: rotate(0deg);
  }

  100% {
    transform: rotate(360deg);
  }
`;
var circularDashKeyframe = keyframes`
  0% {
    stroke-dasharray: 1px, 200px;
    stroke-dashoffset: 0;
  }

  50% {
    stroke-dasharray: 100px, 200px;
    stroke-dashoffset: -15px;
  }

  100% {
    stroke-dasharray: 1px, 200px;
    stroke-dashoffset: -126px;
  }
`;
var rotateAnimation =
  typeof circularRotateKeyframe !== "string"
    ? css`
        animation: ${circularRotateKeyframe} 1.4s linear infinite;
      `
    : null;
var dashAnimation =
  typeof circularDashKeyframe !== "string"
    ? css`
        animation: ${circularDashKeyframe} 1.4s ease-in-out infinite;
      `
    : null;
var useUtilityClasses6 = (ownerState) => {
  const { classes, variant, color: color2, disableShrink } = ownerState;
  const slots = {
    root: ["root", variant, `color${capitalize_default(color2)}`],
    svg: ["svg"],
    track: ["track"],
    circle: [
      "circle",
      `circle${capitalize_default(variant)}`,
      disableShrink && "circleDisableShrink",
    ],
  };
  return composeClasses(slots, getCircularProgressUtilityClass, classes);
};
var CircularProgressRoot = styled_default2("span", {
  name: "MuiCircularProgress",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      styles5[ownerState.variant],
      styles5[`color${capitalize_default(ownerState.color)}`],
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    display: "inline-block",
    variants: [
      {
        props: {
          variant: "determinate",
        },
        style: {
          transition: theme2.transitions.create("transform"),
        },
      },
      {
        props: {
          variant: "indeterminate",
        },
        style: rotateAnimation || {
          animation: `${circularRotateKeyframe} 1.4s linear infinite`,
        },
      },
      ...Object.entries(theme2.palette)
        .filter(createSimplePaletteValueFilter())
        .map(([color2]) => ({
          props: {
            color: color2,
          },
          style: {
            color: (theme2.vars || theme2).palette[color2].main,
          },
        })),
    ],
  })),
);
var CircularProgressSVG = styled_default2("svg", {
  name: "MuiCircularProgress",
  slot: "Svg",
})({
  display: "block",
  // Keeps the progress centered
});
var CircularProgressCircle = styled_default2("circle", {
  name: "MuiCircularProgress",
  slot: "Circle",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.circle,
      styles5[`circle${capitalize_default(ownerState.variant)}`],
      ownerState.disableShrink && styles5.circleDisableShrink,
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    stroke: "currentColor",
    variants: [
      {
        props: {
          variant: "determinate",
        },
        style: {
          transition: theme2.transitions.create("stroke-dashoffset"),
        },
      },
      {
        props: {
          variant: "indeterminate",
        },
        style: {
          // Some default value that looks fine waiting for the animation to kicks in.
          strokeDasharray: "80px, 200px",
          strokeDashoffset: 0,
          // Add the unit to fix a Edge 16 and below bug.
        },
      },
      {
        props: ({ ownerState }) =>
          ownerState.variant === "indeterminate" && !ownerState.disableShrink,
        style: dashAnimation || {
          // At runtime for Pigment CSS, `bufferAnimation` will be null and the generated keyframe will be used.
          animation: `${circularDashKeyframe} 1.4s ease-in-out infinite`,
        },
      },
    ],
  })),
);
var CircularProgressTrack = styled_default2("circle", {
  name: "MuiCircularProgress",
  slot: "Track",
})(
  memoTheme_default(({ theme: theme2 }) => ({
    stroke: "currentColor",
    opacity: (theme2.vars || theme2).palette.action.activatedOpacity,
  })),
);
var CircularProgress = /* @__PURE__ */ React42.forwardRef(
  function CircularProgress2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiCircularProgress",
    });
    const {
      className,
      color: color2 = "primary",
      disableShrink = false,
      enableTrackSlot = false,
      size = 40,
      style: style4,
      thickness = 3.6,
      value = 0,
      variant = "indeterminate",
      ...other
    } = props;
    const ownerState = {
      ...props,
      color: color2,
      disableShrink,
      size,
      thickness,
      value,
      variant,
      enableTrackSlot,
    };
    const classes = useUtilityClasses6(ownerState);
    const circleStyle = {};
    const rootStyle = {};
    const rootProps = {};
    if (variant === "determinate") {
      const circumference = 2 * Math.PI * ((SIZE - thickness) / 2);
      circleStyle.strokeDasharray = circumference.toFixed(3);
      rootProps["aria-valuenow"] = Math.round(value);
      circleStyle.strokeDashoffset = `${(((100 - value) / 100) * circumference).toFixed(3)}px`;
      rootStyle.transform = "rotate(-90deg)";
    }
    return /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(CircularProgressRoot, {
      className: clsx_default(classes.root, className),
      style: {
        width: size,
        height: size,
        ...rootStyle,
        ...style4,
      },
      ownerState,
      ref,
      role: "progressbar",
      ...rootProps,
      ...other,
      children: /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)(
        CircularProgressSVG,
        {
          className: classes.svg,
          ownerState,
          viewBox: `${SIZE / 2} ${SIZE / 2} ${SIZE} ${SIZE}`,
          children: [
            enableTrackSlot
              ? /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
                  CircularProgressTrack,
                  {
                    className: classes.track,
                    ownerState,
                    cx: SIZE,
                    cy: SIZE,
                    r: (SIZE - thickness) / 2,
                    fill: "none",
                    strokeWidth: thickness,
                    "aria-hidden": "true",
                  },
                )
              : null,
            /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
              CircularProgressCircle,
              {
                className: classes.circle,
                style: circleStyle,
                ownerState,
                cx: SIZE,
                cy: SIZE,
                r: (SIZE - thickness) / 2,
                fill: "none",
                strokeWidth: thickness,
              },
            ),
          ],
        },
      ),
    });
  },
);
false
  ? (CircularProgress.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The color of the component.
       * It supports both default and custom theme colors, which can be added as shown in the
       * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
       * @default 'primary'
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "inherit",
          "primary",
          "secondary",
          "error",
          "info",
          "success",
          "warning",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * If `true`, the shrink animation is disabled.
       * This only works if variant is `indeterminate`.
       * @default false
       */
      disableShrink: chainPropTypes(import_prop_types.default.bool, (props) => {
        if (
          props.disableShrink &&
          props.variant &&
          props.variant !== "indeterminate"
        ) {
          return new Error(
            "MUI: You have provided the `disableShrink` prop with a variant other than `indeterminate`. This will have no effect.",
          );
        }
        return null;
      }),
      /**
       * If `true`, a track circle slot is mounted to show a subtle background for the progress.
       * The `size` and `thickness` apply to the track slot to be consistent with the progress circle.
       * @default false
       */
      enableTrackSlot: import_prop_types.default.bool,
      /**
       * The size of the component.
       * If using a number, the pixel unit is assumed.
       * If using a string, you need to provide the CSS unit, for example '3rem'.
       * @default 40
       */
      size: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * @ignore
       */
      style: import_prop_types.default.object,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * The thickness of the circle.
       * @default 3.6
       */
      thickness: import_prop_types.default.number,
      /**
       * The value of the progress indicator for the determinate variant.
       * Value between 0 and 100.
       * @default 0
       */
      value: import_prop_types.default.number,
      /**
       * The variant to use.
       * Use indeterminate when there is no progress value.
       * @default 'indeterminate'
       */
      variant: import_prop_types.default.oneOf([
        "determinate",
        "indeterminate",
      ]),
    })
  : void 0;
var CircularProgress_default = CircularProgress;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/IconButton/iconButtonClasses.js
function getIconButtonUtilityClass(slot) {
  return generateUtilityClass("MuiIconButton", slot);
}
var iconButtonClasses = generateUtilityClasses("MuiIconButton", [
  "root",
  "disabled",
  "colorInherit",
  "colorPrimary",
  "colorSecondary",
  "colorError",
  "colorInfo",
  "colorSuccess",
  "colorWarning",
  "edgeStart",
  "edgeEnd",
  "sizeSmall",
  "sizeMedium",
  "sizeLarge",
  "loading",
  "loadingIndicator",
  "loadingWrapper",
]);
var iconButtonClasses_default = iconButtonClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/IconButton/IconButton.js
var import_jsx_runtime28 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses7 = (ownerState) => {
  const { classes, disabled, color: color2, edge, size, loading } = ownerState;
  const slots = {
    root: [
      "root",
      loading && "loading",
      disabled && "disabled",
      color2 !== "default" && `color${capitalize_default(color2)}`,
      edge && `edge${capitalize_default(edge)}`,
      `size${capitalize_default(size)}`,
    ],
    loadingIndicator: ["loadingIndicator"],
    loadingWrapper: ["loadingWrapper"],
  };
  return composeClasses(slots, getIconButtonUtilityClass, classes);
};
var IconButtonRoot = styled_default2(ButtonBase_default, {
  name: "MuiIconButton",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      ownerState.loading && styles5.loading,
      ownerState.color !== "default" &&
        styles5[`color${capitalize_default(ownerState.color)}`],
      ownerState.edge && styles5[`edge${capitalize_default(ownerState.edge)}`],
      styles5[`size${capitalize_default(ownerState.size)}`],
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    textAlign: "center",
    flex: "0 0 auto",
    fontSize: theme2.typography.pxToRem(24),
    padding: 8,
    borderRadius: "50%",
    color: (theme2.vars || theme2).palette.action.active,
    transition: theme2.transitions.create("background-color", {
      duration: theme2.transitions.duration.shortest,
    }),
    variants: [
      {
        props: (props) => !props.disableRipple,
        style: {
          "--IconButton-hoverBg": theme2.alpha(
            (theme2.vars || theme2).palette.action.active,
            (theme2.vars || theme2).palette.action.hoverOpacity,
          ),
          "&:hover": {
            backgroundColor: "var(--IconButton-hoverBg)",
            // Reset on touch devices, it doesn't add specificity
            "@media (hover: none)": {
              backgroundColor: "transparent",
            },
          },
        },
      },
      {
        props: {
          edge: "start",
        },
        style: {
          marginLeft: -12,
        },
      },
      {
        props: {
          edge: "start",
          size: "small",
        },
        style: {
          marginLeft: -3,
        },
      },
      {
        props: {
          edge: "end",
        },
        style: {
          marginRight: -12,
        },
      },
      {
        props: {
          edge: "end",
          size: "small",
        },
        style: {
          marginRight: -3,
        },
      },
    ],
  })),
  memoTheme_default(({ theme: theme2 }) => ({
    variants: [
      {
        props: {
          color: "inherit",
        },
        style: {
          color: "inherit",
        },
      },
      ...Object.entries(theme2.palette)
        .filter(createSimplePaletteValueFilter())
        .map(([color2]) => ({
          props: {
            color: color2,
          },
          style: {
            color: (theme2.vars || theme2).palette[color2].main,
          },
        })),
      ...Object.entries(theme2.palette)
        .filter(createSimplePaletteValueFilter())
        .map(([color2]) => ({
          props: {
            color: color2,
          },
          style: {
            "--IconButton-hoverBg": theme2.alpha(
              (theme2.vars || theme2).palette[color2].main,
              (theme2.vars || theme2).palette.action.hoverOpacity,
            ),
          },
        })),
      {
        props: {
          size: "small",
        },
        style: {
          padding: 5,
          fontSize: theme2.typography.pxToRem(18),
        },
      },
      {
        props: {
          size: "large",
        },
        style: {
          padding: 12,
          fontSize: theme2.typography.pxToRem(28),
        },
      },
    ],
    [`&.${iconButtonClasses_default.disabled}`]: {
      backgroundColor: "transparent",
      color: (theme2.vars || theme2).palette.action.disabled,
    },
    [`&.${iconButtonClasses_default.loading}`]: {
      color: "transparent",
    },
  })),
);
var IconButtonLoadingIndicator = styled_default2("span", {
  name: "MuiIconButton",
  slot: "LoadingIndicator",
})(({ theme: theme2 }) => ({
  display: "none",
  position: "absolute",
  visibility: "visible",
  top: "50%",
  left: "50%",
  transform: "translate(-50%, -50%)",
  color: (theme2.vars || theme2).palette.action.disabled,
  variants: [
    {
      props: {
        loading: true,
      },
      style: {
        display: "flex",
      },
    },
  ],
}));
var IconButton = /* @__PURE__ */ React43.forwardRef(
  function IconButton2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiIconButton",
    });
    const {
      edge = false,
      children,
      className,
      color: color2 = "default",
      disabled = false,
      disableFocusRipple = false,
      size = "medium",
      id: idProp,
      loading = null,
      loadingIndicator: loadingIndicatorProp,
      ...other
    } = props;
    const loadingId = useId_default(idProp);
    const loadingIndicator2 =
      loadingIndicatorProp ??
      /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(CircularProgress_default, {
        "aria-labelledby": loadingId,
        color: "inherit",
        size: 16,
      });
    const ownerState = {
      ...props,
      edge,
      color: color2,
      disabled,
      disableFocusRipple,
      loading,
      loadingIndicator: loadingIndicator2,
      size,
    };
    const classes = useUtilityClasses7(ownerState);
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(IconButtonRoot, {
      id: loading ? loadingId : idProp,
      className: clsx_default(classes.root, className),
      centerRipple: true,
      focusRipple: !disableFocusRipple,
      disabled: disabled || loading,
      ref,
      ...other,
      ownerState,
      children: [
        typeof loading === "boolean" && // use plain HTML span to minimize the runtime overhead
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("span", {
            className: classes.loadingWrapper,
            style: {
              display: "contents",
            },
            children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
              IconButtonLoadingIndicator,
              {
                className: classes.loadingIndicator,
                ownerState,
                children: loading && loadingIndicator2,
              },
            ),
          }),
        children,
      ],
    });
  },
);
false
  ? (IconButton.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The icon to display.
       */
      children: chainPropTypes(import_prop_types.default.node, (props) => {
        const found = React43.Children.toArray(props.children).some(
          (child) =>
            /* @__PURE__ */ React43.isValidElement(child) &&
            child.props.onClick,
        );
        if (found) {
          return new Error(
            [
              "MUI: You are providing an onClick event listener to a child of a button element.",
              "Prefer applying it to the IconButton directly.",
              "This guarantees that the whole <button> will be responsive to click events.",
            ].join("\n"),
          );
        }
        return null;
      }),
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The color of the component.
       * It supports both default and custom theme colors, which can be added as shown in the
       * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
       * @default 'default'
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "inherit",
          "default",
          "primary",
          "secondary",
          "error",
          "info",
          "success",
          "warning",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * If `true`, the component is disabled.
       * @default false
       */
      disabled: import_prop_types.default.bool,
      /**
       * If `true`, the  keyboard focus ripple is disabled.
       * @default false
       */
      disableFocusRipple: import_prop_types.default.bool,
      /**
       * If `true`, the ripple effect is disabled.
       *
       * ⚠️ Without a ripple there is no styling for :focus-visible by default. Be sure
       * to highlight the element by applying separate styles with the `.Mui-focusVisible` class.
       * @default false
       */
      disableRipple: import_prop_types.default.bool,
      /**
       * If given, uses a negative margin to counteract the padding on one
       * side (this is often helpful for aligning the left or right
       * side of the icon with content above or below, without ruining the border
       * size and shape).
       * @default false
       */
      edge: import_prop_types.default.oneOf(["end", "start", false]),
      /**
       * @ignore
       */
      id: import_prop_types.default.string,
      /**
       * If `true`, the loading indicator is visible and the button is disabled.
       * If `true | false`, the loading wrapper is always rendered before the children to prevent [Google Translation Crash](https://github.com/mui/material-ui/issues/27853).
       * @default null
       */
      loading: import_prop_types.default.bool,
      /**
       * Element placed before the children if the button is in loading state.
       * The node should contain an element with `role="progressbar"` with an accessible name.
       * By default, it renders a `CircularProgress` that is labeled by the button itself.
       * @default <CircularProgress color="inherit" size={16} />
       */
      loadingIndicator: import_prop_types.default.node,
      /**
       * The size of the component.
       * `small` is equivalent to the dense button styling.
       * @default 'medium'
       */
      size: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["small", "medium", "large"]),
        import_prop_types.default.string,
      ]),
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
    })
  : void 0;
var IconButton_default = IconButton;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Typography/Typography.js
var React44 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Typography/typographyClasses.js
function getTypographyUtilityClass(slot) {
  return generateUtilityClass("MuiTypography", slot);
}
var typographyClasses = generateUtilityClasses("MuiTypography", [
  "root",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "subtitle1",
  "subtitle2",
  "body1",
  "body2",
  "inherit",
  "button",
  "caption",
  "overline",
  "alignLeft",
  "alignRight",
  "alignCenter",
  "alignJustify",
  "noWrap",
  "gutterBottom",
  "paragraph",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Typography/Typography.js
var import_jsx_runtime29 = __toESM(require_jsx_runtime(), 1);
var v6Colors = {
  primary: true,
  secondary: true,
  error: true,
  info: true,
  success: true,
  warning: true,
  textPrimary: true,
  textSecondary: true,
  textDisabled: true,
};
var extendSxProp2 = internal_createExtendSxProp();
var useUtilityClasses8 = (ownerState) => {
  const { align, gutterBottom, noWrap, paragraph, variant, classes } =
    ownerState;
  const slots = {
    root: [
      "root",
      variant,
      ownerState.align !== "inherit" && `align${capitalize_default(align)}`,
      gutterBottom && "gutterBottom",
      noWrap && "noWrap",
      paragraph && "paragraph",
    ],
  };
  return composeClasses(slots, getTypographyUtilityClass, classes);
};
var TypographyRoot = styled_default2("span", {
  name: "MuiTypography",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      ownerState.variant && styles5[ownerState.variant],
      ownerState.align !== "inherit" &&
        styles5[`align${capitalize_default(ownerState.align)}`],
      ownerState.noWrap && styles5.noWrap,
      ownerState.gutterBottom && styles5.gutterBottom,
      ownerState.paragraph && styles5.paragraph,
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    margin: 0,
    variants: [
      {
        props: {
          variant: "inherit",
        },
        style: {
          // Some elements, like <button> on Chrome have default font that doesn't inherit, reset this.
          font: "inherit",
          lineHeight: "inherit",
          letterSpacing: "inherit",
        },
      },
      ...Object.entries(theme2.typography)
        .filter(
          ([variant, value]) =>
            variant !== "inherit" && value && typeof value === "object",
        )
        .map(([variant, value]) => ({
          props: {
            variant,
          },
          style: value,
        })),
      ...Object.entries(theme2.palette)
        .filter(createSimplePaletteValueFilter())
        .map(([color2]) => ({
          props: {
            color: color2,
          },
          style: {
            color: (theme2.vars || theme2).palette[color2].main,
          },
        })),
      ...Object.entries(theme2.palette?.text || {})
        .filter(([, value]) => typeof value === "string")
        .map(([color2]) => ({
          props: {
            color: `text${capitalize_default(color2)}`,
          },
          style: {
            color: (theme2.vars || theme2).palette.text[color2],
          },
        })),
      {
        props: ({ ownerState }) => ownerState.align !== "inherit",
        style: {
          textAlign: "var(--Typography-textAlign)",
        },
      },
      {
        props: ({ ownerState }) => ownerState.noWrap,
        style: {
          overflow: "hidden",
          textOverflow: "ellipsis",
          whiteSpace: "nowrap",
        },
      },
      {
        props: ({ ownerState }) => ownerState.gutterBottom,
        style: {
          marginBottom: "0.35em",
        },
      },
      {
        props: ({ ownerState }) => ownerState.paragraph,
        style: {
          marginBottom: 16,
        },
      },
    ],
  })),
);
var defaultVariantMapping = {
  h1: "h1",
  h2: "h2",
  h3: "h3",
  h4: "h4",
  h5: "h5",
  h6: "h6",
  subtitle1: "h6",
  subtitle2: "h6",
  body1: "p",
  body2: "p",
  inherit: "p",
};
var Typography = /* @__PURE__ */ React44.forwardRef(
  function Typography2(inProps, ref) {
    const { color: color2, ...themeProps } = useDefaultProps2({
      props: inProps,
      name: "MuiTypography",
    });
    const isSxColor = !v6Colors[color2];
    const props = extendSxProp2({
      ...themeProps,
      ...(isSxColor && {
        color: color2,
      }),
    });
    const {
      align = "inherit",
      className,
      component,
      gutterBottom = false,
      noWrap = false,
      paragraph = false,
      variant = "body1",
      variantMapping = defaultVariantMapping,
      ...other
    } = props;
    const ownerState = {
      ...props,
      align,
      color: color2,
      className,
      component,
      gutterBottom,
      noWrap,
      paragraph,
      variant,
      variantMapping,
    };
    const Component =
      component ||
      (paragraph
        ? "p"
        : variantMapping[variant] || defaultVariantMapping[variant]) ||
      "span";
    const classes = useUtilityClasses8(ownerState);
    return /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(TypographyRoot, {
      as: Component,
      ref,
      className: clsx_default(classes.root, className),
      ...other,
      ownerState,
      style: {
        ...(align !== "inherit" && {
          "--Typography-textAlign": align,
        }),
        ...other.style,
      },
    });
  },
);
false
  ? (Typography.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * Set the text-align on the component.
       * @default 'inherit'
       */
      align: import_prop_types.default.oneOf([
        "center",
        "inherit",
        "justify",
        "left",
        "right",
      ]),
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The color of the component.
       * It supports both default and custom theme colors, which can be added as shown in the
       * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "primary",
          "secondary",
          "success",
          "error",
          "info",
          "warning",
          "textPrimary",
          "textSecondary",
          "textDisabled",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * If `true`, the text will have a bottom margin.
       * @default false
       */
      gutterBottom: import_prop_types.default.bool,
      /**
       * If `true`, the text will not wrap, but instead will truncate with a text overflow ellipsis.
       *
       * Note that text overflow can only happen with block or inline-block level elements
       * (the element needs to have a width in order to overflow).
       * @default false
       */
      noWrap: import_prop_types.default.bool,
      /**
       * If `true`, the element will be a paragraph element.
       * @default false
       * @deprecated Use the `component` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       */
      paragraph: import_prop_types.default.bool,
      /**
       * @ignore
       */
      style: import_prop_types.default.object,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * Applies the theme typography styles.
       * @default 'body1'
       */
      variant: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "body1",
          "body2",
          "button",
          "caption",
          "h1",
          "h2",
          "h3",
          "h4",
          "h5",
          "h6",
          "inherit",
          "overline",
          "subtitle1",
          "subtitle2",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * The component maps the variant prop to a range of different HTML element types.
       * For instance, subtitle1 to `<h6>`.
       * If you wish to change that mapping, you can provide your own.
       * Alternatively, you can use the `component` prop.
       * @default {
       *   h1: 'h1',
       *   h2: 'h2',
       *   h3: 'h3',
       *   h4: 'h4',
       *   h5: 'h5',
       *   h6: 'h6',
       *   subtitle1: 'h6',
       *   subtitle2: 'h6',
       *   body1: 'p',
       *   body2: 'p',
       *   inherit: 'p',
       * }
       */
      variantMapping: import_prop_types.default.object,
    })
  : void 0;
var Typography_default = Typography;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/AppBar/AppBar.js
var React45 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/AppBar/appBarClasses.js
function getAppBarUtilityClass(slot) {
  return generateUtilityClass("MuiAppBar", slot);
}
var appBarClasses = generateUtilityClasses("MuiAppBar", [
  "root",
  "positionFixed",
  "positionAbsolute",
  "positionSticky",
  "positionStatic",
  "positionRelative",
  "colorDefault",
  "colorPrimary",
  "colorSecondary",
  "colorInherit",
  "colorTransparent",
  "colorError",
  "colorInfo",
  "colorSuccess",
  "colorWarning",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/AppBar/AppBar.js
var import_jsx_runtime30 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses9 = (ownerState) => {
  const { color: color2, position: position2, classes } = ownerState;
  const slots = {
    root: [
      "root",
      `color${capitalize_default(color2)}`,
      `position${capitalize_default(position2)}`,
    ],
  };
  return composeClasses(slots, getAppBarUtilityClass, classes);
};
var joinVars = (var1, var2) =>
  var1 ? `${var1.replace(")", "")}, ${var2})` : var2;
var AppBarRoot = styled_default2(Paper_default, {
  name: "MuiAppBar",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      styles5[`position${capitalize_default(ownerState.position)}`],
      styles5[`color${capitalize_default(ownerState.color)}`],
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    display: "flex",
    flexDirection: "column",
    width: "100%",
    boxSizing: "border-box",
    // Prevent padding issue with the Modal and fixed positioned AppBar.
    flexShrink: 0,
    variants: [
      {
        props: {
          position: "fixed",
        },
        style: {
          position: "fixed",
          zIndex: (theme2.vars || theme2).zIndex.appBar,
          top: 0,
          left: "auto",
          right: 0,
          "@media print": {
            // Prevent the app bar to be visible on each printed page.
            position: "absolute",
          },
        },
      },
      {
        props: {
          position: "absolute",
        },
        style: {
          position: "absolute",
          zIndex: (theme2.vars || theme2).zIndex.appBar,
          top: 0,
          left: "auto",
          right: 0,
        },
      },
      {
        props: {
          position: "sticky",
        },
        style: {
          position: "sticky",
          zIndex: (theme2.vars || theme2).zIndex.appBar,
          top: 0,
          left: "auto",
          right: 0,
        },
      },
      {
        props: {
          position: "static",
        },
        style: {
          position: "static",
        },
      },
      {
        props: {
          position: "relative",
        },
        style: {
          position: "relative",
        },
      },
      {
        props: {
          color: "inherit",
        },
        style: {
          "--AppBar-color": "inherit",
          color: "var(--AppBar-color)",
        },
      },
      {
        props: {
          color: "default",
        },
        style: {
          "--AppBar-background": theme2.vars
            ? theme2.vars.palette.AppBar.defaultBg
            : theme2.palette.grey[100],
          "--AppBar-color": theme2.vars
            ? theme2.vars.palette.text.primary
            : theme2.palette.getContrastText(theme2.palette.grey[100]),
          ...theme2.applyStyles("dark", {
            "--AppBar-background": theme2.vars
              ? theme2.vars.palette.AppBar.defaultBg
              : theme2.palette.grey[900],
            "--AppBar-color": theme2.vars
              ? theme2.vars.palette.text.primary
              : theme2.palette.getContrastText(theme2.palette.grey[900]),
          }),
        },
      },
      ...Object.entries(theme2.palette)
        .filter(createSimplePaletteValueFilter(["contrastText"]))
        .map(([color2]) => ({
          props: {
            color: color2,
          },
          style: {
            "--AppBar-background": (theme2.vars ?? theme2).palette[color2].main,
            "--AppBar-color": (theme2.vars ?? theme2).palette[color2]
              .contrastText,
          },
        })),
      {
        props: (props) =>
          props.enableColorOnDark === true &&
          !["inherit", "transparent"].includes(props.color),
        style: {
          backgroundColor: "var(--AppBar-background)",
          color: "var(--AppBar-color)",
        },
      },
      {
        props: (props) =>
          props.enableColorOnDark === false &&
          !["inherit", "transparent"].includes(props.color),
        style: {
          backgroundColor: "var(--AppBar-background)",
          color: "var(--AppBar-color)",
          ...theme2.applyStyles("dark", {
            backgroundColor: theme2.vars
              ? joinVars(
                  theme2.vars.palette.AppBar.darkBg,
                  "var(--AppBar-background)",
                )
              : null,
            color: theme2.vars
              ? joinVars(
                  theme2.vars.palette.AppBar.darkColor,
                  "var(--AppBar-color)",
                )
              : null,
          }),
        },
      },
      {
        props: {
          color: "transparent",
        },
        style: {
          "--AppBar-background": "transparent",
          "--AppBar-color": "inherit",
          backgroundColor: "var(--AppBar-background)",
          color: "var(--AppBar-color)",
          ...theme2.applyStyles("dark", {
            backgroundImage: "none",
          }),
        },
      },
    ],
  })),
);
var AppBar = /* @__PURE__ */ React45.forwardRef(function AppBar2(inProps, ref) {
  const props = useDefaultProps2({
    props: inProps,
    name: "MuiAppBar",
  });
  const {
    className,
    color: color2 = "primary",
    enableColorOnDark = false,
    position: position2 = "fixed",
    ...other
  } = props;
  const ownerState = {
    ...props,
    color: color2,
    position: position2,
    enableColorOnDark,
  };
  const classes = useUtilityClasses9(ownerState);
  return /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(AppBarRoot, {
    square: true,
    component: "header",
    ownerState,
    elevation: 4,
    className: clsx_default(
      classes.root,
      className,
      position2 === "fixed" && "mui-fixed",
    ),
    ref,
    ...other,
  });
});
false
  ? (AppBar.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The color of the component.
       * It supports both default and custom theme colors, which can be added as shown in the
       * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
       * @default 'primary'
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "default",
          "inherit",
          "primary",
          "secondary",
          "transparent",
          "error",
          "info",
          "success",
          "warning",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * Shadow depth, corresponds to `dp` in the spec.
       * It accepts values between 0 and 24 inclusive.
       * @default 4
       */
      elevation: import_prop_types.default.number,
      /**
       * If true, the `color` prop is applied in dark mode.
       * @default false
       */
      enableColorOnDark: import_prop_types.default.bool,
      /**
       * The positioning type. The behavior of the different options is described
       * [in the MDN web docs](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/position).
       * Note: `sticky` is not universally supported and will fall back to `static` when unavailable.
       * @default 'fixed'
       */
      position: import_prop_types.default.oneOf([
        "absolute",
        "fixed",
        "relative",
        "static",
        "sticky",
      ]),
      /**
       * If `false`, rounded corners are enabled.
       * @default true
       */
      square: import_prop_types.default.bool,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
    })
  : void 0;
var AppBar_default = AppBar;

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/contains/contains.js
function contains(parent, child) {
  if (!parent || !child) {
    return false;
  }
  if (parent.contains(child)) {
    return true;
  }
  const rootNode = child.getRootNode?.();
  if (rootNode && rootNode instanceof ShadowRoot) {
    let next2 = child;
    while (next2) {
      if (parent === next2) {
        return true;
      }
      next2 = next2.parentNode ?? next2.host ?? null;
    }
  }
  return false;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Popper/Popper.js
var React49 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Popper/BasePopper.js
var React48 = __toESM(require_react(), 1);

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/enums.js
var top = "top";
var bottom = "bottom";
var right = "right";
var left = "left";
var auto = "auto";
var basePlacements = [top, bottom, right, left];
var start = "start";
var end = "end";
var clippingParents = "clippingParents";
var viewport = "viewport";
var popper = "popper";
var reference = "reference";
var variationPlacements = /* @__PURE__ */ basePlacements.reduce(function (
  acc,
  placement,
) {
  return acc.concat([placement + "-" + start, placement + "-" + end]);
}, []);
var placements = /* @__PURE__ */ []
  .concat(basePlacements, [auto])
  .reduce(function (acc, placement) {
    return acc.concat([
      placement,
      placement + "-" + start,
      placement + "-" + end,
    ]);
  }, []);
var beforeRead = "beforeRead";
var read = "read";
var afterRead = "afterRead";
var beforeMain = "beforeMain";
var main = "main";
var afterMain = "afterMain";
var beforeWrite = "beforeWrite";
var write = "write";
var afterWrite = "afterWrite";
var modifierPhases = [
  beforeRead,
  read,
  afterRead,
  beforeMain,
  main,
  afterMain,
  beforeWrite,
  write,
  afterWrite,
];

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getNodeName.js
function getNodeName(element) {
  return element ? (element.nodeName || "").toLowerCase() : null;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getWindow.js
function getWindow(node2) {
  if (node2 == null) {
    return window;
  }
  if (node2.toString() !== "[object Window]") {
    var ownerDocument2 = node2.ownerDocument;
    return ownerDocument2 ? ownerDocument2.defaultView || window : window;
  }
  return node2;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/instanceOf.js
function isElement(node2) {
  var OwnElement = getWindow(node2).Element;
  return node2 instanceof OwnElement || node2 instanceof Element;
}
function isHTMLElement(node2) {
  var OwnElement = getWindow(node2).HTMLElement;
  return node2 instanceof OwnElement || node2 instanceof HTMLElement;
}
function isShadowRoot(node2) {
  if (typeof ShadowRoot === "undefined") {
    return false;
  }
  var OwnElement = getWindow(node2).ShadowRoot;
  return node2 instanceof OwnElement || node2 instanceof ShadowRoot;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/applyStyles.js
function applyStyles2(_ref) {
  var state = _ref.state;
  Object.keys(state.elements).forEach(function (name) {
    var style4 = state.styles[name] || {};
    var attributes = state.attributes[name] || {};
    var element = state.elements[name];
    if (!isHTMLElement(element) || !getNodeName(element)) {
      return;
    }
    Object.assign(element.style, style4);
    Object.keys(attributes).forEach(function (name2) {
      var value = attributes[name2];
      if (value === false) {
        element.removeAttribute(name2);
      } else {
        element.setAttribute(name2, value === true ? "" : value);
      }
    });
  });
}
function effect(_ref2) {
  var state = _ref2.state;
  var initialStyles = {
    popper: {
      position: state.options.strategy,
      left: "0",
      top: "0",
      margin: "0",
    },
    arrow: {
      position: "absolute",
    },
    reference: {},
  };
  Object.assign(state.elements.popper.style, initialStyles.popper);
  state.styles = initialStyles;
  if (state.elements.arrow) {
    Object.assign(state.elements.arrow.style, initialStyles.arrow);
  }
  return function () {
    Object.keys(state.elements).forEach(function (name) {
      var element = state.elements[name];
      var attributes = state.attributes[name] || {};
      var styleProperties = Object.keys(
        state.styles.hasOwnProperty(name)
          ? state.styles[name]
          : initialStyles[name],
      );
      var style4 = styleProperties.reduce(function (style5, property) {
        style5[property] = "";
        return style5;
      }, {});
      if (!isHTMLElement(element) || !getNodeName(element)) {
        return;
      }
      Object.assign(element.style, style4);
      Object.keys(attributes).forEach(function (attribute) {
        element.removeAttribute(attribute);
      });
    });
  };
}
var applyStyles_default = {
  name: "applyStyles",
  enabled: true,
  phase: "write",
  fn: applyStyles2,
  effect,
  requires: ["computeStyles"],
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/getBasePlacement.js
function getBasePlacement(placement) {
  return placement.split("-")[0];
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/math.js
var max = Math.max;
var min = Math.min;
var round2 = Math.round;

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/userAgent.js
function getUAString() {
  var uaData = navigator.userAgentData;
  if (uaData != null && uaData.brands && Array.isArray(uaData.brands)) {
    return uaData.brands
      .map(function (item) {
        return item.brand + "/" + item.version;
      })
      .join(" ");
  }
  return navigator.userAgent;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/isLayoutViewport.js
function isLayoutViewport() {
  return !/^((?!chrome|android).)*safari/i.test(getUAString());
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getBoundingClientRect.js
function getBoundingClientRect(element, includeScale, isFixedStrategy) {
  if (includeScale === void 0) {
    includeScale = false;
  }
  if (isFixedStrategy === void 0) {
    isFixedStrategy = false;
  }
  var clientRect = element.getBoundingClientRect();
  var scaleX = 1;
  var scaleY = 1;
  if (includeScale && isHTMLElement(element)) {
    scaleX =
      element.offsetWidth > 0
        ? round2(clientRect.width) / element.offsetWidth || 1
        : 1;
    scaleY =
      element.offsetHeight > 0
        ? round2(clientRect.height) / element.offsetHeight || 1
        : 1;
  }
  var _ref = isElement(element) ? getWindow(element) : window,
    visualViewport = _ref.visualViewport;
  var addVisualOffsets = !isLayoutViewport() && isFixedStrategy;
  var x =
    (clientRect.left +
      (addVisualOffsets && visualViewport ? visualViewport.offsetLeft : 0)) /
    scaleX;
  var y =
    (clientRect.top +
      (addVisualOffsets && visualViewport ? visualViewport.offsetTop : 0)) /
    scaleY;
  var width2 = clientRect.width / scaleX;
  var height2 = clientRect.height / scaleY;
  return {
    width: width2,
    height: height2,
    top: y,
    right: x + width2,
    bottom: y + height2,
    left: x,
    x,
    y,
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getLayoutRect.js
function getLayoutRect(element) {
  var clientRect = getBoundingClientRect(element);
  var width2 = element.offsetWidth;
  var height2 = element.offsetHeight;
  if (Math.abs(clientRect.width - width2) <= 1) {
    width2 = clientRect.width;
  }
  if (Math.abs(clientRect.height - height2) <= 1) {
    height2 = clientRect.height;
  }
  return {
    x: element.offsetLeft,
    y: element.offsetTop,
    width: width2,
    height: height2,
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/contains.js
function contains2(parent, child) {
  var rootNode = child.getRootNode && child.getRootNode();
  if (parent.contains(child)) {
    return true;
  } else if (rootNode && isShadowRoot(rootNode)) {
    var next2 = child;
    do {
      if (next2 && parent.isSameNode(next2)) {
        return true;
      }
      next2 = next2.parentNode || next2.host;
    } while (next2);
  }
  return false;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getComputedStyle.js
function getComputedStyle2(element) {
  return getWindow(element).getComputedStyle(element);
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/isTableElement.js
function isTableElement(element) {
  return ["table", "td", "th"].indexOf(getNodeName(element)) >= 0;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getDocumentElement.js
function getDocumentElement(element) {
  return (
    (isElement(element)
      ? element.ownerDocument
      : // $FlowFixMe[prop-missing]
        element.document) || window.document
  ).documentElement;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getParentNode.js
function getParentNode(element) {
  if (getNodeName(element) === "html") {
    return element;
  }
  return (
    // this is a quicker (but less type safe) way to save quite some bytes from the bundle
    // $FlowFixMe[incompatible-return]
    // $FlowFixMe[prop-missing]
    element.assignedSlot || // step into the shadow DOM of the parent of a slotted node
    element.parentNode || // DOM Element detected
    (isShadowRoot(element) ? element.host : null) || // ShadowRoot detected
    // $FlowFixMe[incompatible-call]: HTMLElement is a Node
    getDocumentElement(element)
  );
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getOffsetParent.js
function getTrueOffsetParent(element) {
  if (
    !isHTMLElement(element) || // https://github.com/popperjs/popper-core/issues/837
    getComputedStyle2(element).position === "fixed"
  ) {
    return null;
  }
  return element.offsetParent;
}
function getContainingBlock(element) {
  var isFirefox = /firefox/i.test(getUAString());
  var isIE = /Trident/i.test(getUAString());
  if (isIE && isHTMLElement(element)) {
    var elementCss = getComputedStyle2(element);
    if (elementCss.position === "fixed") {
      return null;
    }
  }
  var currentNode = getParentNode(element);
  if (isShadowRoot(currentNode)) {
    currentNode = currentNode.host;
  }
  while (
    isHTMLElement(currentNode) &&
    ["html", "body"].indexOf(getNodeName(currentNode)) < 0
  ) {
    var css2 = getComputedStyle2(currentNode);
    if (
      css2.transform !== "none" ||
      css2.perspective !== "none" ||
      css2.contain === "paint" ||
      ["transform", "perspective"].indexOf(css2.willChange) !== -1 ||
      (isFirefox && css2.willChange === "filter") ||
      (isFirefox && css2.filter && css2.filter !== "none")
    ) {
      return currentNode;
    } else {
      currentNode = currentNode.parentNode;
    }
  }
  return null;
}
function getOffsetParent(element) {
  var window2 = getWindow(element);
  var offsetParent = getTrueOffsetParent(element);
  while (
    offsetParent &&
    isTableElement(offsetParent) &&
    getComputedStyle2(offsetParent).position === "static"
  ) {
    offsetParent = getTrueOffsetParent(offsetParent);
  }
  if (
    offsetParent &&
    (getNodeName(offsetParent) === "html" ||
      (getNodeName(offsetParent) === "body" &&
        getComputedStyle2(offsetParent).position === "static"))
  ) {
    return window2;
  }
  return offsetParent || getContainingBlock(element) || window2;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/getMainAxisFromPlacement.js
function getMainAxisFromPlacement(placement) {
  return ["top", "bottom"].indexOf(placement) >= 0 ? "x" : "y";
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/within.js
function within(min2, value, max2) {
  return max(min2, min(value, max2));
}
function withinMaxClamp(min2, value, max2) {
  var v = within(min2, value, max2);
  return v > max2 ? max2 : v;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/getFreshSideObject.js
function getFreshSideObject() {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/mergePaddingObject.js
function mergePaddingObject(paddingObject) {
  return Object.assign({}, getFreshSideObject(), paddingObject);
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/expandToHashMap.js
function expandToHashMap(value, keys) {
  return keys.reduce(function (hashMap, key) {
    hashMap[key] = value;
    return hashMap;
  }, {});
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/arrow.js
var toPaddingObject = function toPaddingObject2(padding2, state) {
  padding2 =
    typeof padding2 === "function"
      ? padding2(
          Object.assign({}, state.rects, {
            placement: state.placement,
          }),
        )
      : padding2;
  return mergePaddingObject(
    typeof padding2 !== "number"
      ? padding2
      : expandToHashMap(padding2, basePlacements),
  );
};
function arrow(_ref) {
  var _state$modifiersData$;
  var state = _ref.state,
    name = _ref.name,
    options = _ref.options;
  var arrowElement = state.elements.arrow;
  var popperOffsets2 = state.modifiersData.popperOffsets;
  var basePlacement = getBasePlacement(state.placement);
  var axis = getMainAxisFromPlacement(basePlacement);
  var isVertical = [left, right].indexOf(basePlacement) >= 0;
  var len = isVertical ? "height" : "width";
  if (!arrowElement || !popperOffsets2) {
    return;
  }
  var paddingObject = toPaddingObject(options.padding, state);
  var arrowRect = getLayoutRect(arrowElement);
  var minProp = axis === "y" ? top : left;
  var maxProp = axis === "y" ? bottom : right;
  var endDiff =
    state.rects.reference[len] +
    state.rects.reference[axis] -
    popperOffsets2[axis] -
    state.rects.popper[len];
  var startDiff = popperOffsets2[axis] - state.rects.reference[axis];
  var arrowOffsetParent = getOffsetParent(arrowElement);
  var clientSize = arrowOffsetParent
    ? axis === "y"
      ? arrowOffsetParent.clientHeight || 0
      : arrowOffsetParent.clientWidth || 0
    : 0;
  var centerToReference = endDiff / 2 - startDiff / 2;
  var min2 = paddingObject[minProp];
  var max2 = clientSize - arrowRect[len] - paddingObject[maxProp];
  var center = clientSize / 2 - arrowRect[len] / 2 + centerToReference;
  var offset2 = within(min2, center, max2);
  var axisProp = axis;
  state.modifiersData[name] =
    ((_state$modifiersData$ = {}),
    (_state$modifiersData$[axisProp] = offset2),
    (_state$modifiersData$.centerOffset = offset2 - center),
    _state$modifiersData$);
}
function effect2(_ref2) {
  var state = _ref2.state,
    options = _ref2.options;
  var _options$element = options.element,
    arrowElement =
      _options$element === void 0 ? "[data-popper-arrow]" : _options$element;
  if (arrowElement == null) {
    return;
  }
  if (typeof arrowElement === "string") {
    arrowElement = state.elements.popper.querySelector(arrowElement);
    if (!arrowElement) {
      return;
    }
  }
  if (!contains2(state.elements.popper, arrowElement)) {
    return;
  }
  state.elements.arrow = arrowElement;
}
var arrow_default = {
  name: "arrow",
  enabled: true,
  phase: "main",
  fn: arrow,
  effect: effect2,
  requires: ["popperOffsets"],
  requiresIfExists: ["preventOverflow"],
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/getVariation.js
function getVariation(placement) {
  return placement.split("-")[1];
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/computeStyles.js
var unsetSides = {
  top: "auto",
  right: "auto",
  bottom: "auto",
  left: "auto",
};
function roundOffsetsByDPR(_ref, win) {
  var x = _ref.x,
    y = _ref.y;
  var dpr = win.devicePixelRatio || 1;
  return {
    x: round2(x * dpr) / dpr || 0,
    y: round2(y * dpr) / dpr || 0,
  };
}
function mapToStyles(_ref2) {
  var _Object$assign2;
  var popper2 = _ref2.popper,
    popperRect = _ref2.popperRect,
    placement = _ref2.placement,
    variation = _ref2.variation,
    offsets = _ref2.offsets,
    position2 = _ref2.position,
    gpuAcceleration = _ref2.gpuAcceleration,
    adaptive = _ref2.adaptive,
    roundOffsets = _ref2.roundOffsets,
    isFixed = _ref2.isFixed;
  var _offsets$x = offsets.x,
    x = _offsets$x === void 0 ? 0 : _offsets$x,
    _offsets$y = offsets.y,
    y = _offsets$y === void 0 ? 0 : _offsets$y;
  var _ref3 =
    typeof roundOffsets === "function"
      ? roundOffsets({
          x,
          y,
        })
      : {
          x,
          y,
        };
  x = _ref3.x;
  y = _ref3.y;
  var hasX = offsets.hasOwnProperty("x");
  var hasY = offsets.hasOwnProperty("y");
  var sideX = left;
  var sideY = top;
  var win = window;
  if (adaptive) {
    var offsetParent = getOffsetParent(popper2);
    var heightProp = "clientHeight";
    var widthProp = "clientWidth";
    if (offsetParent === getWindow(popper2)) {
      offsetParent = getDocumentElement(popper2);
      if (
        getComputedStyle2(offsetParent).position !== "static" &&
        position2 === "absolute"
      ) {
        heightProp = "scrollHeight";
        widthProp = "scrollWidth";
      }
    }
    offsetParent = offsetParent;
    if (
      placement === top ||
      ((placement === left || placement === right) && variation === end)
    ) {
      sideY = bottom;
      var offsetY =
        isFixed && offsetParent === win && win.visualViewport
          ? win.visualViewport.height
          : // $FlowFixMe[prop-missing]
            offsetParent[heightProp];
      y -= offsetY - popperRect.height;
      y *= gpuAcceleration ? 1 : -1;
    }
    if (
      placement === left ||
      ((placement === top || placement === bottom) && variation === end)
    ) {
      sideX = right;
      var offsetX =
        isFixed && offsetParent === win && win.visualViewport
          ? win.visualViewport.width
          : // $FlowFixMe[prop-missing]
            offsetParent[widthProp];
      x -= offsetX - popperRect.width;
      x *= gpuAcceleration ? 1 : -1;
    }
  }
  var commonStyles = Object.assign(
    {
      position: position2,
    },
    adaptive && unsetSides,
  );
  var _ref4 =
    roundOffsets === true
      ? roundOffsetsByDPR(
          {
            x,
            y,
          },
          getWindow(popper2),
        )
      : {
          x,
          y,
        };
  x = _ref4.x;
  y = _ref4.y;
  if (gpuAcceleration) {
    var _Object$assign;
    return Object.assign(
      {},
      commonStyles,
      ((_Object$assign = {}),
      (_Object$assign[sideY] = hasY ? "0" : ""),
      (_Object$assign[sideX] = hasX ? "0" : ""),
      (_Object$assign.transform =
        (win.devicePixelRatio || 1) <= 1
          ? "translate(" + x + "px, " + y + "px)"
          : "translate3d(" + x + "px, " + y + "px, 0)"),
      _Object$assign),
    );
  }
  return Object.assign(
    {},
    commonStyles,
    ((_Object$assign2 = {}),
    (_Object$assign2[sideY] = hasY ? y + "px" : ""),
    (_Object$assign2[sideX] = hasX ? x + "px" : ""),
    (_Object$assign2.transform = ""),
    _Object$assign2),
  );
}
function computeStyles(_ref5) {
  var state = _ref5.state,
    options = _ref5.options;
  var _options$gpuAccelerat = options.gpuAcceleration,
    gpuAcceleration =
      _options$gpuAccelerat === void 0 ? true : _options$gpuAccelerat,
    _options$adaptive = options.adaptive,
    adaptive = _options$adaptive === void 0 ? true : _options$adaptive,
    _options$roundOffsets = options.roundOffsets,
    roundOffsets =
      _options$roundOffsets === void 0 ? true : _options$roundOffsets;
  var commonStyles = {
    placement: getBasePlacement(state.placement),
    variation: getVariation(state.placement),
    popper: state.elements.popper,
    popperRect: state.rects.popper,
    gpuAcceleration,
    isFixed: state.options.strategy === "fixed",
  };
  if (state.modifiersData.popperOffsets != null) {
    state.styles.popper = Object.assign(
      {},
      state.styles.popper,
      mapToStyles(
        Object.assign({}, commonStyles, {
          offsets: state.modifiersData.popperOffsets,
          position: state.options.strategy,
          adaptive,
          roundOffsets,
        }),
      ),
    );
  }
  if (state.modifiersData.arrow != null) {
    state.styles.arrow = Object.assign(
      {},
      state.styles.arrow,
      mapToStyles(
        Object.assign({}, commonStyles, {
          offsets: state.modifiersData.arrow,
          position: "absolute",
          adaptive: false,
          roundOffsets,
        }),
      ),
    );
  }
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-placement": state.placement,
  });
}
var computeStyles_default = {
  name: "computeStyles",
  enabled: true,
  phase: "beforeWrite",
  fn: computeStyles,
  data: {},
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/eventListeners.js
var passive = {
  passive: true,
};
function effect3(_ref) {
  var state = _ref.state,
    instance = _ref.instance,
    options = _ref.options;
  var _options$scroll = options.scroll,
    scroll = _options$scroll === void 0 ? true : _options$scroll,
    _options$resize = options.resize,
    resize = _options$resize === void 0 ? true : _options$resize;
  var window2 = getWindow(state.elements.popper);
  var scrollParents = [].concat(
    state.scrollParents.reference,
    state.scrollParents.popper,
  );
  if (scroll) {
    scrollParents.forEach(function (scrollParent) {
      scrollParent.addEventListener("scroll", instance.update, passive);
    });
  }
  if (resize) {
    window2.addEventListener("resize", instance.update, passive);
  }
  return function () {
    if (scroll) {
      scrollParents.forEach(function (scrollParent) {
        scrollParent.removeEventListener("scroll", instance.update, passive);
      });
    }
    if (resize) {
      window2.removeEventListener("resize", instance.update, passive);
    }
  };
}
var eventListeners_default = {
  name: "eventListeners",
  enabled: true,
  phase: "write",
  fn: function fn() {},
  effect: effect3,
  data: {},
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/getOppositePlacement.js
var hash2 = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom",
};
function getOppositePlacement(placement) {
  return placement.replace(/left|right|bottom|top/g, function (matched) {
    return hash2[matched];
  });
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/getOppositeVariationPlacement.js
var hash3 = {
  start: "end",
  end: "start",
};
function getOppositeVariationPlacement(placement) {
  return placement.replace(/start|end/g, function (matched) {
    return hash3[matched];
  });
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getWindowScroll.js
function getWindowScroll(node2) {
  var win = getWindow(node2);
  var scrollLeft = win.pageXOffset;
  var scrollTop = win.pageYOffset;
  return {
    scrollLeft,
    scrollTop,
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getWindowScrollBarX.js
function getWindowScrollBarX(element) {
  return (
    getBoundingClientRect(getDocumentElement(element)).left +
    getWindowScroll(element).scrollLeft
  );
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getViewportRect.js
function getViewportRect(element, strategy) {
  var win = getWindow(element);
  var html2 = getDocumentElement(element);
  var visualViewport = win.visualViewport;
  var width2 = html2.clientWidth;
  var height2 = html2.clientHeight;
  var x = 0;
  var y = 0;
  if (visualViewport) {
    width2 = visualViewport.width;
    height2 = visualViewport.height;
    var layoutViewport = isLayoutViewport();
    if (layoutViewport || (!layoutViewport && strategy === "fixed")) {
      x = visualViewport.offsetLeft;
      y = visualViewport.offsetTop;
    }
  }
  return {
    width: width2,
    height: height2,
    x: x + getWindowScrollBarX(element),
    y,
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getDocumentRect.js
function getDocumentRect(element) {
  var _element$ownerDocumen;
  var html2 = getDocumentElement(element);
  var winScroll = getWindowScroll(element);
  var body2 =
    (_element$ownerDocumen = element.ownerDocument) == null
      ? void 0
      : _element$ownerDocumen.body;
  var width2 = max(
    html2.scrollWidth,
    html2.clientWidth,
    body2 ? body2.scrollWidth : 0,
    body2 ? body2.clientWidth : 0,
  );
  var height2 = max(
    html2.scrollHeight,
    html2.clientHeight,
    body2 ? body2.scrollHeight : 0,
    body2 ? body2.clientHeight : 0,
  );
  var x = -winScroll.scrollLeft + getWindowScrollBarX(element);
  var y = -winScroll.scrollTop;
  if (getComputedStyle2(body2 || html2).direction === "rtl") {
    x += max(html2.clientWidth, body2 ? body2.clientWidth : 0) - width2;
  }
  return {
    width: width2,
    height: height2,
    x,
    y,
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/isScrollParent.js
function isScrollParent(element) {
  var _getComputedStyle = getComputedStyle2(element),
    overflow = _getComputedStyle.overflow,
    overflowX = _getComputedStyle.overflowX,
    overflowY = _getComputedStyle.overflowY;
  return /auto|scroll|overlay|hidden/.test(overflow + overflowY + overflowX);
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getScrollParent.js
function getScrollParent(node2) {
  if (["html", "body", "#document"].indexOf(getNodeName(node2)) >= 0) {
    return node2.ownerDocument.body;
  }
  if (isHTMLElement(node2) && isScrollParent(node2)) {
    return node2;
  }
  return getScrollParent(getParentNode(node2));
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/listScrollParents.js
function listScrollParents(element, list) {
  var _element$ownerDocumen;
  if (list === void 0) {
    list = [];
  }
  var scrollParent = getScrollParent(element);
  var isBody =
    scrollParent ===
    ((_element$ownerDocumen = element.ownerDocument) == null
      ? void 0
      : _element$ownerDocumen.body);
  var win = getWindow(scrollParent);
  var target = isBody
    ? [win].concat(
        win.visualViewport || [],
        isScrollParent(scrollParent) ? scrollParent : [],
      )
    : scrollParent;
  var updatedList = list.concat(target);
  return isBody
    ? updatedList
    : // $FlowFixMe[incompatible-call]: isBody tells us target will be an HTMLElement here
      updatedList.concat(listScrollParents(getParentNode(target)));
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/rectToClientRect.js
function rectToClientRect(rect) {
  return Object.assign({}, rect, {
    left: rect.x,
    top: rect.y,
    right: rect.x + rect.width,
    bottom: rect.y + rect.height,
  });
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getClippingRect.js
function getInnerBoundingClientRect(element, strategy) {
  var rect = getBoundingClientRect(element, false, strategy === "fixed");
  rect.top = rect.top + element.clientTop;
  rect.left = rect.left + element.clientLeft;
  rect.bottom = rect.top + element.clientHeight;
  rect.right = rect.left + element.clientWidth;
  rect.width = element.clientWidth;
  rect.height = element.clientHeight;
  rect.x = rect.left;
  rect.y = rect.top;
  return rect;
}
function getClientRectFromMixedType(element, clippingParent, strategy) {
  return clippingParent === viewport
    ? rectToClientRect(getViewportRect(element, strategy))
    : isElement(clippingParent)
      ? getInnerBoundingClientRect(clippingParent, strategy)
      : rectToClientRect(getDocumentRect(getDocumentElement(element)));
}
function getClippingParents(element) {
  var clippingParents2 = listScrollParents(getParentNode(element));
  var canEscapeClipping =
    ["absolute", "fixed"].indexOf(getComputedStyle2(element).position) >= 0;
  var clipperElement =
    canEscapeClipping && isHTMLElement(element)
      ? getOffsetParent(element)
      : element;
  if (!isElement(clipperElement)) {
    return [];
  }
  return clippingParents2.filter(function (clippingParent) {
    return (
      isElement(clippingParent) &&
      contains2(clippingParent, clipperElement) &&
      getNodeName(clippingParent) !== "body"
    );
  });
}
function getClippingRect(element, boundary, rootBoundary, strategy) {
  var mainClippingParents =
    boundary === "clippingParents"
      ? getClippingParents(element)
      : [].concat(boundary);
  var clippingParents2 = [].concat(mainClippingParents, [rootBoundary]);
  var firstClippingParent = clippingParents2[0];
  var clippingRect = clippingParents2.reduce(
    function (accRect, clippingParent) {
      var rect = getClientRectFromMixedType(element, clippingParent, strategy);
      accRect.top = max(rect.top, accRect.top);
      accRect.right = min(rect.right, accRect.right);
      accRect.bottom = min(rect.bottom, accRect.bottom);
      accRect.left = max(rect.left, accRect.left);
      return accRect;
    },
    getClientRectFromMixedType(element, firstClippingParent, strategy),
  );
  clippingRect.width = clippingRect.right - clippingRect.left;
  clippingRect.height = clippingRect.bottom - clippingRect.top;
  clippingRect.x = clippingRect.left;
  clippingRect.y = clippingRect.top;
  return clippingRect;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/computeOffsets.js
function computeOffsets(_ref) {
  var reference2 = _ref.reference,
    element = _ref.element,
    placement = _ref.placement;
  var basePlacement = placement ? getBasePlacement(placement) : null;
  var variation = placement ? getVariation(placement) : null;
  var commonX = reference2.x + reference2.width / 2 - element.width / 2;
  var commonY = reference2.y + reference2.height / 2 - element.height / 2;
  var offsets;
  switch (basePlacement) {
    case top:
      offsets = {
        x: commonX,
        y: reference2.y - element.height,
      };
      break;
    case bottom:
      offsets = {
        x: commonX,
        y: reference2.y + reference2.height,
      };
      break;
    case right:
      offsets = {
        x: reference2.x + reference2.width,
        y: commonY,
      };
      break;
    case left:
      offsets = {
        x: reference2.x - element.width,
        y: commonY,
      };
      break;
    default:
      offsets = {
        x: reference2.x,
        y: reference2.y,
      };
  }
  var mainAxis = basePlacement ? getMainAxisFromPlacement(basePlacement) : null;
  if (mainAxis != null) {
    var len = mainAxis === "y" ? "height" : "width";
    switch (variation) {
      case start:
        offsets[mainAxis] =
          offsets[mainAxis] - (reference2[len] / 2 - element[len] / 2);
        break;
      case end:
        offsets[mainAxis] =
          offsets[mainAxis] + (reference2[len] / 2 - element[len] / 2);
        break;
      default:
    }
  }
  return offsets;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/detectOverflow.js
function detectOverflow(state, options) {
  if (options === void 0) {
    options = {};
  }
  var _options = options,
    _options$placement = _options.placement,
    placement =
      _options$placement === void 0 ? state.placement : _options$placement,
    _options$strategy = _options.strategy,
    strategy =
      _options$strategy === void 0 ? state.strategy : _options$strategy,
    _options$boundary = _options.boundary,
    boundary =
      _options$boundary === void 0 ? clippingParents : _options$boundary,
    _options$rootBoundary = _options.rootBoundary,
    rootBoundary =
      _options$rootBoundary === void 0 ? viewport : _options$rootBoundary,
    _options$elementConte = _options.elementContext,
    elementContext =
      _options$elementConte === void 0 ? popper : _options$elementConte,
    _options$altBoundary = _options.altBoundary,
    altBoundary =
      _options$altBoundary === void 0 ? false : _options$altBoundary,
    _options$padding = _options.padding,
    padding2 = _options$padding === void 0 ? 0 : _options$padding;
  var paddingObject = mergePaddingObject(
    typeof padding2 !== "number"
      ? padding2
      : expandToHashMap(padding2, basePlacements),
  );
  var altContext = elementContext === popper ? reference : popper;
  var popperRect = state.rects.popper;
  var element = state.elements[altBoundary ? altContext : elementContext];
  var clippingClientRect = getClippingRect(
    isElement(element)
      ? element
      : element.contextElement || getDocumentElement(state.elements.popper),
    boundary,
    rootBoundary,
    strategy,
  );
  var referenceClientRect = getBoundingClientRect(state.elements.reference);
  var popperOffsets2 = computeOffsets({
    reference: referenceClientRect,
    element: popperRect,
    strategy: "absolute",
    placement,
  });
  var popperClientRect = rectToClientRect(
    Object.assign({}, popperRect, popperOffsets2),
  );
  var elementClientRect =
    elementContext === popper ? popperClientRect : referenceClientRect;
  var overflowOffsets = {
    top: clippingClientRect.top - elementClientRect.top + paddingObject.top,
    bottom:
      elementClientRect.bottom -
      clippingClientRect.bottom +
      paddingObject.bottom,
    left: clippingClientRect.left - elementClientRect.left + paddingObject.left,
    right:
      elementClientRect.right - clippingClientRect.right + paddingObject.right,
  };
  var offsetData = state.modifiersData.offset;
  if (elementContext === popper && offsetData) {
    var offset2 = offsetData[placement];
    Object.keys(overflowOffsets).forEach(function (key) {
      var multiply = [right, bottom].indexOf(key) >= 0 ? 1 : -1;
      var axis = [top, bottom].indexOf(key) >= 0 ? "y" : "x";
      overflowOffsets[key] += offset2[axis] * multiply;
    });
  }
  return overflowOffsets;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/computeAutoPlacement.js
function computeAutoPlacement(state, options) {
  if (options === void 0) {
    options = {};
  }
  var _options = options,
    placement = _options.placement,
    boundary = _options.boundary,
    rootBoundary = _options.rootBoundary,
    padding2 = _options.padding,
    flipVariations = _options.flipVariations,
    _options$allowedAutoP = _options.allowedAutoPlacements,
    allowedAutoPlacements =
      _options$allowedAutoP === void 0 ? placements : _options$allowedAutoP;
  var variation = getVariation(placement);
  var placements2 = variation
    ? flipVariations
      ? variationPlacements
      : variationPlacements.filter(function (placement2) {
          return getVariation(placement2) === variation;
        })
    : basePlacements;
  var allowedPlacements = placements2.filter(function (placement2) {
    return allowedAutoPlacements.indexOf(placement2) >= 0;
  });
  if (allowedPlacements.length === 0) {
    allowedPlacements = placements2;
  }
  var overflows = allowedPlacements.reduce(function (acc, placement2) {
    acc[placement2] = detectOverflow(state, {
      placement: placement2,
      boundary,
      rootBoundary,
      padding: padding2,
    })[getBasePlacement(placement2)];
    return acc;
  }, {});
  return Object.keys(overflows).sort(function (a, b) {
    return overflows[a] - overflows[b];
  });
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/flip.js
function getExpandedFallbackPlacements(placement) {
  if (getBasePlacement(placement) === auto) {
    return [];
  }
  var oppositePlacement = getOppositePlacement(placement);
  return [
    getOppositeVariationPlacement(placement),
    oppositePlacement,
    getOppositeVariationPlacement(oppositePlacement),
  ];
}
function flip(_ref) {
  var state = _ref.state,
    options = _ref.options,
    name = _ref.name;
  if (state.modifiersData[name]._skip) {
    return;
  }
  var _options$mainAxis = options.mainAxis,
    checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis,
    _options$altAxis = options.altAxis,
    checkAltAxis = _options$altAxis === void 0 ? true : _options$altAxis,
    specifiedFallbackPlacements = options.fallbackPlacements,
    padding2 = options.padding,
    boundary = options.boundary,
    rootBoundary = options.rootBoundary,
    altBoundary = options.altBoundary,
    _options$flipVariatio = options.flipVariations,
    flipVariations =
      _options$flipVariatio === void 0 ? true : _options$flipVariatio,
    allowedAutoPlacements = options.allowedAutoPlacements;
  var preferredPlacement = state.options.placement;
  var basePlacement = getBasePlacement(preferredPlacement);
  var isBasePlacement = basePlacement === preferredPlacement;
  var fallbackPlacements =
    specifiedFallbackPlacements ||
    (isBasePlacement || !flipVariations
      ? [getOppositePlacement(preferredPlacement)]
      : getExpandedFallbackPlacements(preferredPlacement));
  var placements2 = [preferredPlacement]
    .concat(fallbackPlacements)
    .reduce(function (acc, placement2) {
      return acc.concat(
        getBasePlacement(placement2) === auto
          ? computeAutoPlacement(state, {
              placement: placement2,
              boundary,
              rootBoundary,
              padding: padding2,
              flipVariations,
              allowedAutoPlacements,
            })
          : placement2,
      );
    }, []);
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var checksMap = /* @__PURE__ */ new Map();
  var makeFallbackChecks = true;
  var firstFittingPlacement = placements2[0];
  for (var i = 0; i < placements2.length; i++) {
    var placement = placements2[i];
    var _basePlacement = getBasePlacement(placement);
    var isStartVariation = getVariation(placement) === start;
    var isVertical = [top, bottom].indexOf(_basePlacement) >= 0;
    var len = isVertical ? "width" : "height";
    var overflow = detectOverflow(state, {
      placement,
      boundary,
      rootBoundary,
      altBoundary,
      padding: padding2,
    });
    var mainVariationSide = isVertical
      ? isStartVariation
        ? right
        : left
      : isStartVariation
        ? bottom
        : top;
    if (referenceRect[len] > popperRect[len]) {
      mainVariationSide = getOppositePlacement(mainVariationSide);
    }
    var altVariationSide = getOppositePlacement(mainVariationSide);
    var checks = [];
    if (checkMainAxis) {
      checks.push(overflow[_basePlacement] <= 0);
    }
    if (checkAltAxis) {
      checks.push(
        overflow[mainVariationSide] <= 0,
        overflow[altVariationSide] <= 0,
      );
    }
    if (
      checks.every(function (check) {
        return check;
      })
    ) {
      firstFittingPlacement = placement;
      makeFallbackChecks = false;
      break;
    }
    checksMap.set(placement, checks);
  }
  if (makeFallbackChecks) {
    var numberOfChecks = flipVariations ? 3 : 1;
    var _loop = function _loop2(_i2) {
      var fittingPlacement = placements2.find(function (placement2) {
        var checks2 = checksMap.get(placement2);
        if (checks2) {
          return checks2.slice(0, _i2).every(function (check) {
            return check;
          });
        }
      });
      if (fittingPlacement) {
        firstFittingPlacement = fittingPlacement;
        return "break";
      }
    };
    for (var _i = numberOfChecks; _i > 0; _i--) {
      var _ret = _loop(_i);
      if (_ret === "break") break;
    }
  }
  if (state.placement !== firstFittingPlacement) {
    state.modifiersData[name]._skip = true;
    state.placement = firstFittingPlacement;
    state.reset = true;
  }
}
var flip_default = {
  name: "flip",
  enabled: true,
  phase: "main",
  fn: flip,
  requiresIfExists: ["offset"],
  data: {
    _skip: false,
  },
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/hide.js
function getSideOffsets(overflow, rect, preventedOffsets) {
  if (preventedOffsets === void 0) {
    preventedOffsets = {
      x: 0,
      y: 0,
    };
  }
  return {
    top: overflow.top - rect.height - preventedOffsets.y,
    right: overflow.right - rect.width + preventedOffsets.x,
    bottom: overflow.bottom - rect.height + preventedOffsets.y,
    left: overflow.left - rect.width - preventedOffsets.x,
  };
}
function isAnySideFullyClipped(overflow) {
  return [top, right, bottom, left].some(function (side) {
    return overflow[side] >= 0;
  });
}
function hide(_ref) {
  var state = _ref.state,
    name = _ref.name;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var preventedOffsets = state.modifiersData.preventOverflow;
  var referenceOverflow = detectOverflow(state, {
    elementContext: "reference",
  });
  var popperAltOverflow = detectOverflow(state, {
    altBoundary: true,
  });
  var referenceClippingOffsets = getSideOffsets(
    referenceOverflow,
    referenceRect,
  );
  var popperEscapeOffsets = getSideOffsets(
    popperAltOverflow,
    popperRect,
    preventedOffsets,
  );
  var isReferenceHidden = isAnySideFullyClipped(referenceClippingOffsets);
  var hasPopperEscaped = isAnySideFullyClipped(popperEscapeOffsets);
  state.modifiersData[name] = {
    referenceClippingOffsets,
    popperEscapeOffsets,
    isReferenceHidden,
    hasPopperEscaped,
  };
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-reference-hidden": isReferenceHidden,
    "data-popper-escaped": hasPopperEscaped,
  });
}
var hide_default = {
  name: "hide",
  enabled: true,
  phase: "main",
  requiresIfExists: ["preventOverflow"],
  fn: hide,
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/offset.js
function distanceAndSkiddingToXY(placement, rects, offset2) {
  var basePlacement = getBasePlacement(placement);
  var invertDistance = [left, top].indexOf(basePlacement) >= 0 ? -1 : 1;
  var _ref =
      typeof offset2 === "function"
        ? offset2(
            Object.assign({}, rects, {
              placement,
            }),
          )
        : offset2,
    skidding = _ref[0],
    distance = _ref[1];
  skidding = skidding || 0;
  distance = (distance || 0) * invertDistance;
  return [left, right].indexOf(basePlacement) >= 0
    ? {
        x: distance,
        y: skidding,
      }
    : {
        x: skidding,
        y: distance,
      };
}
function offset(_ref2) {
  var state = _ref2.state,
    options = _ref2.options,
    name = _ref2.name;
  var _options$offset = options.offset,
    offset2 = _options$offset === void 0 ? [0, 0] : _options$offset;
  var data = placements.reduce(function (acc, placement) {
    acc[placement] = distanceAndSkiddingToXY(placement, state.rects, offset2);
    return acc;
  }, {});
  var _data$state$placement = data[state.placement],
    x = _data$state$placement.x,
    y = _data$state$placement.y;
  if (state.modifiersData.popperOffsets != null) {
    state.modifiersData.popperOffsets.x += x;
    state.modifiersData.popperOffsets.y += y;
  }
  state.modifiersData[name] = data;
}
var offset_default = {
  name: "offset",
  enabled: true,
  phase: "main",
  requires: ["popperOffsets"],
  fn: offset,
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/popperOffsets.js
function popperOffsets(_ref) {
  var state = _ref.state,
    name = _ref.name;
  state.modifiersData[name] = computeOffsets({
    reference: state.rects.reference,
    element: state.rects.popper,
    strategy: "absolute",
    placement: state.placement,
  });
}
var popperOffsets_default = {
  name: "popperOffsets",
  enabled: true,
  phase: "read",
  fn: popperOffsets,
  data: {},
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/getAltAxis.js
function getAltAxis(axis) {
  return axis === "x" ? "y" : "x";
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/preventOverflow.js
function preventOverflow(_ref) {
  var state = _ref.state,
    options = _ref.options,
    name = _ref.name;
  var _options$mainAxis = options.mainAxis,
    checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis,
    _options$altAxis = options.altAxis,
    checkAltAxis = _options$altAxis === void 0 ? false : _options$altAxis,
    boundary = options.boundary,
    rootBoundary = options.rootBoundary,
    altBoundary = options.altBoundary,
    padding2 = options.padding,
    _options$tether = options.tether,
    tether = _options$tether === void 0 ? true : _options$tether,
    _options$tetherOffset = options.tetherOffset,
    tetherOffset = _options$tetherOffset === void 0 ? 0 : _options$tetherOffset;
  var overflow = detectOverflow(state, {
    boundary,
    rootBoundary,
    padding: padding2,
    altBoundary,
  });
  var basePlacement = getBasePlacement(state.placement);
  var variation = getVariation(state.placement);
  var isBasePlacement = !variation;
  var mainAxis = getMainAxisFromPlacement(basePlacement);
  var altAxis = getAltAxis(mainAxis);
  var popperOffsets2 = state.modifiersData.popperOffsets;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var tetherOffsetValue =
    typeof tetherOffset === "function"
      ? tetherOffset(
          Object.assign({}, state.rects, {
            placement: state.placement,
          }),
        )
      : tetherOffset;
  var normalizedTetherOffsetValue =
    typeof tetherOffsetValue === "number"
      ? {
          mainAxis: tetherOffsetValue,
          altAxis: tetherOffsetValue,
        }
      : Object.assign(
          {
            mainAxis: 0,
            altAxis: 0,
          },
          tetherOffsetValue,
        );
  var offsetModifierState = state.modifiersData.offset
    ? state.modifiersData.offset[state.placement]
    : null;
  var data = {
    x: 0,
    y: 0,
  };
  if (!popperOffsets2) {
    return;
  }
  if (checkMainAxis) {
    var _offsetModifierState$;
    var mainSide = mainAxis === "y" ? top : left;
    var altSide = mainAxis === "y" ? bottom : right;
    var len = mainAxis === "y" ? "height" : "width";
    var offset2 = popperOffsets2[mainAxis];
    var min2 = offset2 + overflow[mainSide];
    var max2 = offset2 - overflow[altSide];
    var additive = tether ? -popperRect[len] / 2 : 0;
    var minLen = variation === start ? referenceRect[len] : popperRect[len];
    var maxLen = variation === start ? -popperRect[len] : -referenceRect[len];
    var arrowElement = state.elements.arrow;
    var arrowRect =
      tether && arrowElement
        ? getLayoutRect(arrowElement)
        : {
            width: 0,
            height: 0,
          };
    var arrowPaddingObject = state.modifiersData["arrow#persistent"]
      ? state.modifiersData["arrow#persistent"].padding
      : getFreshSideObject();
    var arrowPaddingMin = arrowPaddingObject[mainSide];
    var arrowPaddingMax = arrowPaddingObject[altSide];
    var arrowLen = within(0, referenceRect[len], arrowRect[len]);
    var minOffset = isBasePlacement
      ? referenceRect[len] / 2 -
        additive -
        arrowLen -
        arrowPaddingMin -
        normalizedTetherOffsetValue.mainAxis
      : minLen -
        arrowLen -
        arrowPaddingMin -
        normalizedTetherOffsetValue.mainAxis;
    var maxOffset = isBasePlacement
      ? -referenceRect[len] / 2 +
        additive +
        arrowLen +
        arrowPaddingMax +
        normalizedTetherOffsetValue.mainAxis
      : maxLen +
        arrowLen +
        arrowPaddingMax +
        normalizedTetherOffsetValue.mainAxis;
    var arrowOffsetParent =
      state.elements.arrow && getOffsetParent(state.elements.arrow);
    var clientOffset = arrowOffsetParent
      ? mainAxis === "y"
        ? arrowOffsetParent.clientTop || 0
        : arrowOffsetParent.clientLeft || 0
      : 0;
    var offsetModifierValue =
      (_offsetModifierState$ =
        offsetModifierState == null ? void 0 : offsetModifierState[mainAxis]) !=
      null
        ? _offsetModifierState$
        : 0;
    var tetherMin = offset2 + minOffset - offsetModifierValue - clientOffset;
    var tetherMax = offset2 + maxOffset - offsetModifierValue;
    var preventedOffset = within(
      tether ? min(min2, tetherMin) : min2,
      offset2,
      tether ? max(max2, tetherMax) : max2,
    );
    popperOffsets2[mainAxis] = preventedOffset;
    data[mainAxis] = preventedOffset - offset2;
  }
  if (checkAltAxis) {
    var _offsetModifierState$2;
    var _mainSide = mainAxis === "x" ? top : left;
    var _altSide = mainAxis === "x" ? bottom : right;
    var _offset = popperOffsets2[altAxis];
    var _len = altAxis === "y" ? "height" : "width";
    var _min = _offset + overflow[_mainSide];
    var _max = _offset - overflow[_altSide];
    var isOriginSide = [top, left].indexOf(basePlacement) !== -1;
    var _offsetModifierValue =
      (_offsetModifierState$2 =
        offsetModifierState == null ? void 0 : offsetModifierState[altAxis]) !=
      null
        ? _offsetModifierState$2
        : 0;
    var _tetherMin = isOriginSide
      ? _min
      : _offset -
        referenceRect[_len] -
        popperRect[_len] -
        _offsetModifierValue +
        normalizedTetherOffsetValue.altAxis;
    var _tetherMax = isOriginSide
      ? _offset +
        referenceRect[_len] +
        popperRect[_len] -
        _offsetModifierValue -
        normalizedTetherOffsetValue.altAxis
      : _max;
    var _preventedOffset =
      tether && isOriginSide
        ? withinMaxClamp(_tetherMin, _offset, _tetherMax)
        : within(
            tether ? _tetherMin : _min,
            _offset,
            tether ? _tetherMax : _max,
          );
    popperOffsets2[altAxis] = _preventedOffset;
    data[altAxis] = _preventedOffset - _offset;
  }
  state.modifiersData[name] = data;
}
var preventOverflow_default = {
  name: "preventOverflow",
  enabled: true,
  phase: "main",
  fn: preventOverflow,
  requiresIfExists: ["offset"],
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getHTMLElementScroll.js
function getHTMLElementScroll(element) {
  return {
    scrollLeft: element.scrollLeft,
    scrollTop: element.scrollTop,
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getNodeScroll.js
function getNodeScroll(node2) {
  if (node2 === getWindow(node2) || !isHTMLElement(node2)) {
    return getWindowScroll(node2);
  } else {
    return getHTMLElementScroll(node2);
  }
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getCompositeRect.js
function isElementScaled(element) {
  var rect = element.getBoundingClientRect();
  var scaleX = round2(rect.width) / element.offsetWidth || 1;
  var scaleY = round2(rect.height) / element.offsetHeight || 1;
  return scaleX !== 1 || scaleY !== 1;
}
function getCompositeRect(elementOrVirtualElement, offsetParent, isFixed) {
  if (isFixed === void 0) {
    isFixed = false;
  }
  var isOffsetParentAnElement = isHTMLElement(offsetParent);
  var offsetParentIsScaled =
    isHTMLElement(offsetParent) && isElementScaled(offsetParent);
  var documentElement = getDocumentElement(offsetParent);
  var rect = getBoundingClientRect(
    elementOrVirtualElement,
    offsetParentIsScaled,
    isFixed,
  );
  var scroll = {
    scrollLeft: 0,
    scrollTop: 0,
  };
  var offsets = {
    x: 0,
    y: 0,
  };
  if (isOffsetParentAnElement || (!isOffsetParentAnElement && !isFixed)) {
    if (
      getNodeName(offsetParent) !== "body" || // https://github.com/popperjs/popper-core/issues/1078
      isScrollParent(documentElement)
    ) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isHTMLElement(offsetParent)) {
      offsets = getBoundingClientRect(offsetParent, true);
      offsets.x += offsetParent.clientLeft;
      offsets.y += offsetParent.clientTop;
    } else if (documentElement) {
      offsets.x = getWindowScrollBarX(documentElement);
    }
  }
  return {
    x: rect.left + scroll.scrollLeft - offsets.x,
    y: rect.top + scroll.scrollTop - offsets.y,
    width: rect.width,
    height: rect.height,
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/orderModifiers.js
function order(modifiers) {
  var map = /* @__PURE__ */ new Map();
  var visited = /* @__PURE__ */ new Set();
  var result = [];
  modifiers.forEach(function (modifier) {
    map.set(modifier.name, modifier);
  });
  function sort(modifier) {
    visited.add(modifier.name);
    var requires = [].concat(
      modifier.requires || [],
      modifier.requiresIfExists || [],
    );
    requires.forEach(function (dep) {
      if (!visited.has(dep)) {
        var depModifier = map.get(dep);
        if (depModifier) {
          sort(depModifier);
        }
      }
    });
    result.push(modifier);
  }
  modifiers.forEach(function (modifier) {
    if (!visited.has(modifier.name)) {
      sort(modifier);
    }
  });
  return result;
}
function orderModifiers(modifiers) {
  var orderedModifiers = order(modifiers);
  return modifierPhases.reduce(function (acc, phase) {
    return acc.concat(
      orderedModifiers.filter(function (modifier) {
        return modifier.phase === phase;
      }),
    );
  }, []);
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/debounce.js
function debounce2(fn2) {
  var pending;
  return function () {
    if (!pending) {
      pending = new Promise(function (resolve) {
        Promise.resolve().then(function () {
          pending = void 0;
          resolve(fn2());
        });
      });
    }
    return pending;
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/mergeByName.js
function mergeByName(modifiers) {
  var merged = modifiers.reduce(function (merged2, current2) {
    var existing = merged2[current2.name];
    merged2[current2.name] = existing
      ? Object.assign({}, existing, current2, {
          options: Object.assign({}, existing.options, current2.options),
          data: Object.assign({}, existing.data, current2.data),
        })
      : current2;
    return merged2;
  }, {});
  return Object.keys(merged).map(function (key) {
    return merged[key];
  });
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/createPopper.js
var DEFAULT_OPTIONS = {
  placement: "bottom",
  modifiers: [],
  strategy: "absolute",
};
function areValidElements() {
  for (
    var _len = arguments.length, args = new Array(_len), _key = 0;
    _key < _len;
    _key++
  ) {
    args[_key] = arguments[_key];
  }
  return !args.some(function (element) {
    return !(element && typeof element.getBoundingClientRect === "function");
  });
}
function popperGenerator(generatorOptions) {
  if (generatorOptions === void 0) {
    generatorOptions = {};
  }
  var _generatorOptions = generatorOptions,
    _generatorOptions$def = _generatorOptions.defaultModifiers,
    defaultModifiers2 =
      _generatorOptions$def === void 0 ? [] : _generatorOptions$def,
    _generatorOptions$def2 = _generatorOptions.defaultOptions,
    defaultOptions2 =
      _generatorOptions$def2 === void 0
        ? DEFAULT_OPTIONS
        : _generatorOptions$def2;
  return function createPopper2(reference2, popper2, options) {
    if (options === void 0) {
      options = defaultOptions2;
    }
    var state = {
      placement: "bottom",
      orderedModifiers: [],
      options: Object.assign({}, DEFAULT_OPTIONS, defaultOptions2),
      modifiersData: {},
      elements: {
        reference: reference2,
        popper: popper2,
      },
      attributes: {},
      styles: {},
    };
    var effectCleanupFns = [];
    var isDestroyed = false;
    var instance = {
      state,
      setOptions: function setOptions(setOptionsAction) {
        var options2 =
          typeof setOptionsAction === "function"
            ? setOptionsAction(state.options)
            : setOptionsAction;
        cleanupModifierEffects();
        state.options = Object.assign(
          {},
          defaultOptions2,
          state.options,
          options2,
        );
        state.scrollParents = {
          reference: isElement(reference2)
            ? listScrollParents(reference2)
            : reference2.contextElement
              ? listScrollParents(reference2.contextElement)
              : [],
          popper: listScrollParents(popper2),
        };
        var orderedModifiers = orderModifiers(
          mergeByName([].concat(defaultModifiers2, state.options.modifiers)),
        );
        state.orderedModifiers = orderedModifiers.filter(function (m) {
          return m.enabled;
        });
        runModifierEffects();
        return instance.update();
      },
      // Sync update – it will always be executed, even if not necessary. This
      // is useful for low frequency updates where sync behavior simplifies the
      // logic.
      // For high frequency updates (e.g. `resize` and `scroll` events), always
      // prefer the async Popper#update method
      forceUpdate: function forceUpdate() {
        if (isDestroyed) {
          return;
        }
        var _state$elements = state.elements,
          reference3 = _state$elements.reference,
          popper3 = _state$elements.popper;
        if (!areValidElements(reference3, popper3)) {
          return;
        }
        state.rects = {
          reference: getCompositeRect(
            reference3,
            getOffsetParent(popper3),
            state.options.strategy === "fixed",
          ),
          popper: getLayoutRect(popper3),
        };
        state.reset = false;
        state.placement = state.options.placement;
        state.orderedModifiers.forEach(function (modifier) {
          return (state.modifiersData[modifier.name] = Object.assign(
            {},
            modifier.data,
          ));
        });
        for (var index = 0; index < state.orderedModifiers.length; index++) {
          if (state.reset === true) {
            state.reset = false;
            index = -1;
            continue;
          }
          var _state$orderedModifie = state.orderedModifiers[index],
            fn2 = _state$orderedModifie.fn,
            _state$orderedModifie2 = _state$orderedModifie.options,
            _options =
              _state$orderedModifie2 === void 0 ? {} : _state$orderedModifie2,
            name = _state$orderedModifie.name;
          if (typeof fn2 === "function") {
            state =
              fn2({
                state,
                options: _options,
                name,
                instance,
              }) || state;
          }
        }
      },
      // Async and optimistically optimized update – it will not be executed if
      // not necessary (debounced to run at most once-per-tick)
      update: debounce2(function () {
        return new Promise(function (resolve) {
          instance.forceUpdate();
          resolve(state);
        });
      }),
      destroy: function destroy() {
        cleanupModifierEffects();
        isDestroyed = true;
      },
    };
    if (!areValidElements(reference2, popper2)) {
      return instance;
    }
    instance.setOptions(options).then(function (state2) {
      if (!isDestroyed && options.onFirstUpdate) {
        options.onFirstUpdate(state2);
      }
    });
    function runModifierEffects() {
      state.orderedModifiers.forEach(function (_ref) {
        var name = _ref.name,
          _ref$options = _ref.options,
          options2 = _ref$options === void 0 ? {} : _ref$options,
          effect4 = _ref.effect;
        if (typeof effect4 === "function") {
          var cleanupFn = effect4({
            state,
            name,
            instance,
            options: options2,
          });
          var noopFn = function noopFn2() {};
          effectCleanupFns.push(cleanupFn || noopFn);
        }
      });
    }
    function cleanupModifierEffects() {
      effectCleanupFns.forEach(function (fn2) {
        return fn2();
      });
      effectCleanupFns = [];
    }
    return instance;
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/popper.js
var defaultModifiers = [
  eventListeners_default,
  popperOffsets_default,
  computeStyles_default,
  applyStyles_default,
  offset_default,
  flip_default,
  preventOverflow_default,
  arrow_default,
  hide_default,
];
var createPopper = /* @__PURE__ */ popperGenerator({
  defaultModifiers,
});

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/useSlotProps/useSlotProps.js
function useSlotProps(parameters) {
  const {
    elementType,
    externalSlotProps,
    ownerState,
    skipResolvingSlotProps = false,
    ...other
  } = parameters;
  const resolvedComponentsProps = skipResolvingSlotProps
    ? {}
    : resolveComponentProps_default(externalSlotProps, ownerState);
  const { props: mergedProps, internalRef } = mergeSlotProps_default({
    ...other,
    externalSlotProps: resolvedComponentsProps,
  });
  const ref = useForkRef(
    internalRef,
    resolvedComponentsProps?.ref,
    parameters.additionalProps?.ref,
  );
  const props = appendOwnerState_default(
    elementType,
    {
      ...mergedProps,
      ref,
    },
    ownerState,
  );
  return props;
}
var useSlotProps_default = useSlotProps;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Portal/Portal.js
var React47 = __toESM(require_react(), 1);
var ReactDOM2 = __toESM(require_react_dom(), 1);

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/getReactElementRef/getReactElementRef.js
var React46 = __toESM(require_react(), 1);
function getReactElementRef(element) {
  if (parseInt(React46.version, 10) >= 19) {
    return element?.props?.ref || null;
  }
  return element?.ref || null;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Portal/Portal.js
function getContainer(container) {
  return typeof container === "function" ? container() : container;
}
var Portal = /* @__PURE__ */ React47.forwardRef(
  function Portal2(props, forwardedRef) {
    const { children, container, disablePortal = false } = props;
    const [mountNode, setMountNode] = React47.useState(null);
    const handleRef = useForkRef(
      /* @__PURE__ */ React47.isValidElement(children)
        ? getReactElementRef(children)
        : null,
      forwardedRef,
    );
    useEnhancedEffect_default(() => {
      if (!disablePortal) {
        setMountNode(getContainer(container) || document.body);
      }
    }, [container, disablePortal]);
    useEnhancedEffect_default(() => {
      if (mountNode && !disablePortal) {
        setRef(forwardedRef, mountNode);
        return () => {
          setRef(forwardedRef, null);
        };
      }
      return void 0;
    }, [forwardedRef, mountNode, disablePortal]);
    if (disablePortal) {
      if (/* @__PURE__ */ React47.isValidElement(children)) {
        const newProps = {
          ref: handleRef,
        };
        return /* @__PURE__ */ React47.cloneElement(children, newProps);
      }
      return children;
    }
    return mountNode
      ? /* @__PURE__ */ ReactDOM2.createPortal(children, mountNode)
      : mountNode;
  },
);
false
  ? (Portal.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The children to render into the `container`.
       */
      children: import_prop_types.default.node,
      /**
       * An HTML element or function that returns one.
       * The `container` will have the portal children appended to it.
       *
       * You can also provide a callback, which is called in a React layout effect.
       * This lets you set the container from a ref, and also makes server-side rendering possible.
       *
       * By default, it uses the body of the top-level document object,
       * so it's simply `document.body` most of the time.
       */
      container: import_prop_types.default.oneOfType([
        HTMLElementType,
        import_prop_types.default.func,
      ]),
      /**
       * The `children` will be under the DOM hierarchy of the parent component.
       * @default false
       */
      disablePortal: import_prop_types.default.bool,
    })
  : void 0;
if (false) {
  Portal["propTypes"] = exactProp(Portal.propTypes);
}
var Portal_default = Portal;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Popper/popperClasses.js
function getPopperUtilityClass(slot) {
  return generateUtilityClass("MuiPopper", slot);
}
var popperClasses = generateUtilityClasses("MuiPopper", ["root"]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Popper/BasePopper.js
var import_jsx_runtime31 = __toESM(require_jsx_runtime(), 1);
function flipPlacement(placement, direction) {
  if (direction === "ltr") {
    return placement;
  }
  switch (placement) {
    case "bottom-end":
      return "bottom-start";
    case "bottom-start":
      return "bottom-end";
    case "top-end":
      return "top-start";
    case "top-start":
      return "top-end";
    default:
      return placement;
  }
}
function resolveAnchorEl(anchorEl) {
  return typeof anchorEl === "function" ? anchorEl() : anchorEl;
}
function isHTMLElement2(element) {
  return element.nodeType !== void 0;
}
var useUtilityClasses10 = (ownerState) => {
  const { classes } = ownerState;
  const slots = {
    root: ["root"],
  };
  return composeClasses(slots, getPopperUtilityClass, classes);
};
var defaultPopperOptions = {};
var PopperTooltip = /* @__PURE__ */ React48.forwardRef(
  function PopperTooltip2(props, forwardedRef) {
    const {
      anchorEl,
      children,
      direction,
      disablePortal,
      modifiers,
      open,
      placement: initialPlacement,
      popperOptions,
      popperRef: popperRefProp,
      slotProps = {},
      slots = {},
      TransitionProps,
      // @ts-ignore internal logic
      ownerState: ownerStateProp,
      // prevent from spreading to DOM, it can come from the parent component e.g. Select.
      ...other
    } = props;
    const tooltipRef = React48.useRef(null);
    const ownRef = useForkRef(tooltipRef, forwardedRef);
    const popperRef = React48.useRef(null);
    const handlePopperRef = useForkRef(popperRef, popperRefProp);
    const handlePopperRefRef = React48.useRef(handlePopperRef);
    useEnhancedEffect_default(() => {
      handlePopperRefRef.current = handlePopperRef;
    }, [handlePopperRef]);
    React48.useImperativeHandle(popperRefProp, () => popperRef.current, []);
    const rtlPlacement = flipPlacement(initialPlacement, direction);
    const [placement, setPlacement] = React48.useState(rtlPlacement);
    const [resolvedAnchorElement, setResolvedAnchorElement] = React48.useState(
      resolveAnchorEl(anchorEl),
    );
    React48.useEffect(() => {
      if (popperRef.current) {
        popperRef.current.forceUpdate();
      }
    });
    React48.useEffect(() => {
      if (anchorEl) {
        setResolvedAnchorElement(resolveAnchorEl(anchorEl));
      }
    }, [anchorEl]);
    useEnhancedEffect_default(() => {
      if (!resolvedAnchorElement || !open) {
        return void 0;
      }
      const handlePopperUpdate = (data) => {
        setPlacement(data.placement);
      };
      if (false) {
        if (
          resolvedAnchorElement &&
          isHTMLElement2(resolvedAnchorElement) &&
          resolvedAnchorElement.nodeType === 1
        ) {
          const box = resolvedAnchorElement.getBoundingClientRect();
          if (
            isLayoutSupported() &&
            box.top === 0 &&
            box.left === 0 &&
            box.right === 0 &&
            box.bottom === 0
          ) {
            console.warn(
              [
                "MUI: The `anchorEl` prop provided to the component is invalid.",
                "The anchor element should be part of the document layout.",
                "Make sure the element is present in the document or that it's not display none.",
              ].join("\n"),
            );
          }
        }
      }
      let popperModifiers = [
        {
          name: "preventOverflow",
          options: {
            altBoundary: disablePortal,
          },
        },
        {
          name: "flip",
          options: {
            altBoundary: disablePortal,
          },
        },
        {
          name: "onUpdate",
          enabled: true,
          phase: "afterWrite",
          fn: ({ state }) => {
            handlePopperUpdate(state);
          },
        },
      ];
      if (modifiers != null) {
        popperModifiers = popperModifiers.concat(modifiers);
      }
      if (popperOptions && popperOptions.modifiers != null) {
        popperModifiers = popperModifiers.concat(popperOptions.modifiers);
      }
      const popper2 = createPopper(resolvedAnchorElement, tooltipRef.current, {
        placement: rtlPlacement,
        ...popperOptions,
        modifiers: popperModifiers,
      });
      handlePopperRefRef.current(popper2);
      const popperElement = tooltipRef.current;
      return () => {
        if (popperElement) {
          const { style: style4 } = popperElement;
          const position2 = style4.position;
          const top2 = style4.top;
          const left2 = style4.left;
          const transform = style4.transform;
          popper2.destroy();
          style4.position = position2;
          style4.top = top2;
          style4.left = left2;
          style4.transform = transform;
        } else {
          popper2.destroy();
        }
        handlePopperRefRef.current(null);
      };
    }, [
      resolvedAnchorElement,
      disablePortal,
      modifiers,
      open,
      popperOptions,
      rtlPlacement,
    ]);
    const childProps = {
      placement,
    };
    if (TransitionProps !== null) {
      childProps.TransitionProps = TransitionProps;
    }
    const classes = useUtilityClasses10(props);
    const Root = slots.root ?? "div";
    const rootProps = useSlotProps_default({
      elementType: Root,
      externalSlotProps: slotProps.root,
      externalForwardedProps: other,
      additionalProps: {
        role: "tooltip",
        ref: ownRef,
      },
      ownerState: props,
      className: classes.root,
    });
    return /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(Root, {
      ...rootProps,
      children:
        typeof children === "function" ? children(childProps) : children,
    });
  },
);
var Popper = /* @__PURE__ */ React48.forwardRef(
  function Popper2(props, forwardedRef) {
    const {
      anchorEl,
      children,
      container: containerProp,
      direction = "ltr",
      disablePortal = false,
      keepMounted = false,
      modifiers,
      open,
      placement = "bottom",
      popperOptions = defaultPopperOptions,
      popperRef,
      style: style4,
      transition = false,
      slotProps = {},
      slots = {},
      ...other
    } = props;
    const [exited, setExited] = React48.useState(true);
    const handleEnter = () => {
      setExited(false);
    };
    const handleExited = () => {
      setExited(true);
    };
    if (!keepMounted && !open && (!transition || exited)) {
      return null;
    }
    let container;
    if (containerProp) {
      container = containerProp;
    } else if (anchorEl) {
      const resolvedAnchorEl = resolveAnchorEl(anchorEl);
      container =
        resolvedAnchorEl && isHTMLElement2(resolvedAnchorEl)
          ? ownerDocument(resolvedAnchorEl).body
          : ownerDocument(null).body;
    }
    const display =
      !open && keepMounted && (!transition || exited) ? "none" : void 0;
    const transitionProps = transition
      ? {
          in: open,
          onEnter: handleEnter,
          onExited: handleExited,
        }
      : void 0;
    return /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(Portal_default, {
      disablePortal,
      container,
      children: /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(PopperTooltip, {
        anchorEl,
        direction,
        disablePortal,
        modifiers,
        ref: forwardedRef,
        open: transition ? !exited : open,
        placement,
        popperOptions,
        popperRef,
        slotProps,
        slots,
        ...other,
        style: {
          // Prevents scroll issue, waiting for Popper.js to add this style once initiated.
          position: "fixed",
          // Fix Popper.js display issue
          top: 0,
          left: 0,
          display,
          ...style4,
        },
        TransitionProps: transitionProps,
        children,
      }),
    });
  },
);
false
  ? (Popper.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * An HTML element, [virtualElement](https://popper.js.org/docs/v2/virtual-elements/),
       * or a function that returns either.
       * It's used to set the position of the popper.
       * The return value will passed as the reference object of the Popper instance.
       */
      anchorEl: chainPropTypes(
        import_prop_types.default.oneOfType([
          HTMLElementType,
          import_prop_types.default.object,
          import_prop_types.default.func,
        ]),
        (props) => {
          if (props.open) {
            const resolvedAnchorEl = resolveAnchorEl(props.anchorEl);
            if (
              resolvedAnchorEl &&
              isHTMLElement2(resolvedAnchorEl) &&
              resolvedAnchorEl.nodeType === 1
            ) {
              const box = resolvedAnchorEl.getBoundingClientRect();
              if (false) {
                if (
                  isLayoutSupported() &&
                  box.top === 0 &&
                  box.left === 0 &&
                  box.right === 0 &&
                  box.bottom === 0
                ) {
                  return new Error(
                    [
                      "MUI: The `anchorEl` prop provided to the component is invalid.",
                      "The anchor element should be part of the document layout.",
                      "Make sure the element is present in the document or that it's not display none.",
                    ].join("\n"),
                  );
                }
              }
            } else if (
              !resolvedAnchorEl ||
              typeof resolvedAnchorEl.getBoundingClientRect !== "function" ||
              (isVirtualElement(resolvedAnchorEl) &&
                resolvedAnchorEl.contextElement != null &&
                resolvedAnchorEl.contextElement.nodeType !== 1)
            ) {
              return new Error(
                [
                  "MUI: The `anchorEl` prop provided to the component is invalid.",
                  "It should be an HTML element instance or a virtualElement ",
                  "(https://popper.js.org/docs/v2/virtual-elements/).",
                ].join("\n"),
              );
            }
          }
          return null;
        },
      ),
      /**
       * Popper render function or node.
       */
      children: import_prop_types.default.oneOfType([
        import_prop_types.default.node,
        import_prop_types.default.func,
      ]),
      /**
       * An HTML element or function that returns one.
       * The `container` will have the portal children appended to it.
       *
       * You can also provide a callback, which is called in a React layout effect.
       * This lets you set the container from a ref, and also makes server-side rendering possible.
       *
       * By default, it uses the body of the top-level document object,
       * so it's simply `document.body` most of the time.
       */
      container: import_prop_types.default.oneOfType([
        HTMLElementType,
        import_prop_types.default.func,
      ]),
      /**
       * Direction of the text.
       * @default 'ltr'
       */
      direction: import_prop_types.default.oneOf(["ltr", "rtl"]),
      /**
       * The `children` will be under the DOM hierarchy of the parent component.
       * @default false
       */
      disablePortal: import_prop_types.default.bool,
      /**
       * Always keep the children in the DOM.
       * This prop can be useful in SEO situation or
       * when you want to maximize the responsiveness of the Popper.
       * @default false
       */
      keepMounted: import_prop_types.default.bool,
      /**
       * Popper.js is based on a "plugin-like" architecture,
       * most of its features are fully encapsulated "modifiers".
       *
       * A modifier is a function that is called each time Popper.js needs to
       * compute the position of the popper.
       * For this reason, modifiers should be very performant to avoid bottlenecks.
       * To learn how to create a modifier, [read the modifiers documentation](https://popper.js.org/docs/v2/modifiers/).
       */
      modifiers: import_prop_types.default.arrayOf(
        import_prop_types.default.shape({
          data: import_prop_types.default.object,
          effect: import_prop_types.default.func,
          enabled: import_prop_types.default.bool,
          fn: import_prop_types.default.func,
          name: import_prop_types.default.any,
          options: import_prop_types.default.object,
          phase: import_prop_types.default.oneOf([
            "afterMain",
            "afterRead",
            "afterWrite",
            "beforeMain",
            "beforeRead",
            "beforeWrite",
            "main",
            "read",
            "write",
          ]),
          requires: import_prop_types.default.arrayOf(
            import_prop_types.default.string,
          ),
          requiresIfExists: import_prop_types.default.arrayOf(
            import_prop_types.default.string,
          ),
        }),
      ),
      /**
       * If `true`, the component is shown.
       */
      open: import_prop_types.default.bool.isRequired,
      /**
       * Popper placement.
       * @default 'bottom'
       */
      placement: import_prop_types.default.oneOf([
        "auto-end",
        "auto-start",
        "auto",
        "bottom-end",
        "bottom-start",
        "bottom",
        "left-end",
        "left-start",
        "left",
        "right-end",
        "right-start",
        "right",
        "top-end",
        "top-start",
        "top",
      ]),
      /**
       * Options provided to the [`Popper.js`](https://popper.js.org/docs/v2/constructors/#options) instance.
       * @default {}
       */
      popperOptions: import_prop_types.default.shape({
        modifiers: import_prop_types.default.array,
        onFirstUpdate: import_prop_types.default.func,
        placement: import_prop_types.default.oneOf([
          "auto-end",
          "auto-start",
          "auto",
          "bottom-end",
          "bottom-start",
          "bottom",
          "left-end",
          "left-start",
          "left",
          "right-end",
          "right-start",
          "right",
          "top-end",
          "top-start",
          "top",
        ]),
        strategy: import_prop_types.default.oneOf(["absolute", "fixed"]),
      }),
      /**
       * A ref that points to the used popper instance.
       */
      popperRef: refType_default,
      /**
       * The props used for each slot inside the Popper.
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        root: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      }),
      /**
       * The components used for each slot inside the Popper.
       * Either a string to use a HTML element or a component.
       * @default {}
       */
      slots: import_prop_types.default.shape({
        root: import_prop_types.default.elementType,
      }),
      /**
       * Help supporting a react-transition-group/Transition component.
       * @default false
       */
      transition: import_prop_types.default.bool,
    })
  : void 0;
var BasePopper_default = Popper;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Popper/Popper.js
var import_jsx_runtime32 = __toESM(require_jsx_runtime(), 1);
var PopperRoot = styled_default2(BasePopper_default, {
  name: "MuiPopper",
  slot: "Root",
})({});
var Popper3 = /* @__PURE__ */ React49.forwardRef(
  function Popper4(inProps, ref) {
    const isRtl = useRtl();
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiPopper",
    });
    const {
      anchorEl,
      component,
      components,
      componentsProps,
      container,
      disablePortal,
      keepMounted,
      modifiers,
      open,
      placement,
      popperOptions,
      popperRef,
      transition,
      slots,
      slotProps,
      ...other
    } = props;
    const RootComponent = slots?.root ?? components?.Root;
    const otherProps = {
      anchorEl,
      container,
      disablePortal,
      keepMounted,
      modifiers,
      open,
      placement,
      popperOptions,
      popperRef,
      transition,
      ...other,
    };
    return /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(PopperRoot, {
      as: component,
      direction: isRtl ? "rtl" : "ltr",
      slots: {
        root: RootComponent,
      },
      slotProps: slotProps ?? componentsProps,
      ...otherProps,
      ref,
    });
  },
);
false
  ? (Popper3.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * An HTML element, [virtualElement](https://popper.js.org/docs/v2/virtual-elements/),
       * or a function that returns either.
       * It's used to set the position of the popper.
       * The return value will passed as the reference object of the Popper instance.
       */
      anchorEl: import_prop_types.default.oneOfType([
        HTMLElementType,
        import_prop_types.default.object,
        import_prop_types.default.func,
      ]),
      /**
       * Popper render function or node.
       */
      children: import_prop_types.default.oneOfType([
        import_prop_types.default.node,
        import_prop_types.default.func,
      ]),
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * The components used for each slot inside the Popper.
       * Either a string to use a HTML element or a component.
       *
       * @deprecated use the `slots` prop instead. This prop will be removed in a future major release. [How to migrate](/material-ui/migration/migrating-from-deprecated-apis/).
       * @default {}
       */
      components: import_prop_types.default.shape({
        Root: import_prop_types.default.elementType,
      }),
      /**
       * The props used for each slot inside the Popper.
       *
       * @deprecated use the `slotProps` prop instead. This prop will be removed in a future major release. [How to migrate](/material-ui/migration/migrating-from-deprecated-apis/).
       * @default {}
       */
      componentsProps: import_prop_types.default.shape({
        root: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      }),
      /**
       * An HTML element or function that returns one.
       * The `container` will have the portal children appended to it.
       *
       * You can also provide a callback, which is called in a React layout effect.
       * This lets you set the container from a ref, and also makes server-side rendering possible.
       *
       * By default, it uses the body of the top-level document object,
       * so it's simply `document.body` most of the time.
       */
      container: import_prop_types.default.oneOfType([
        HTMLElementType,
        import_prop_types.default.func,
      ]),
      /**
       * The `children` will be under the DOM hierarchy of the parent component.
       * @default false
       */
      disablePortal: import_prop_types.default.bool,
      /**
       * Always keep the children in the DOM.
       * This prop can be useful in SEO situation or
       * when you want to maximize the responsiveness of the Popper.
       * @default false
       */
      keepMounted: import_prop_types.default.bool,
      /**
       * Popper.js is based on a "plugin-like" architecture,
       * most of its features are fully encapsulated "modifiers".
       *
       * A modifier is a function that is called each time Popper.js needs to
       * compute the position of the popper.
       * For this reason, modifiers should be very performant to avoid bottlenecks.
       * To learn how to create a modifier, [read the modifiers documentation](https://popper.js.org/docs/v2/modifiers/).
       */
      modifiers: import_prop_types.default.arrayOf(
        import_prop_types.default.shape({
          data: import_prop_types.default.object,
          effect: import_prop_types.default.func,
          enabled: import_prop_types.default.bool,
          fn: import_prop_types.default.func,
          name: import_prop_types.default.any,
          options: import_prop_types.default.object,
          phase: import_prop_types.default.oneOf([
            "afterMain",
            "afterRead",
            "afterWrite",
            "beforeMain",
            "beforeRead",
            "beforeWrite",
            "main",
            "read",
            "write",
          ]),
          requires: import_prop_types.default.arrayOf(
            import_prop_types.default.string,
          ),
          requiresIfExists: import_prop_types.default.arrayOf(
            import_prop_types.default.string,
          ),
        }),
      ),
      /**
       * If `true`, the component is shown.
       */
      open: import_prop_types.default.bool.isRequired,
      /**
       * Popper placement.
       * @default 'bottom'
       */
      placement: import_prop_types.default.oneOf([
        "auto-end",
        "auto-start",
        "auto",
        "bottom-end",
        "bottom-start",
        "bottom",
        "left-end",
        "left-start",
        "left",
        "right-end",
        "right-start",
        "right",
        "top-end",
        "top-start",
        "top",
      ]),
      /**
       * Options provided to the [`Popper.js`](https://popper.js.org/docs/v2/constructors/#options) instance.
       * @default {}
       */
      popperOptions: import_prop_types.default.shape({
        modifiers: import_prop_types.default.array,
        onFirstUpdate: import_prop_types.default.func,
        placement: import_prop_types.default.oneOf([
          "auto-end",
          "auto-start",
          "auto",
          "bottom-end",
          "bottom-start",
          "bottom",
          "left-end",
          "left-start",
          "left",
          "right-end",
          "right-start",
          "right",
          "top-end",
          "top-start",
          "top",
        ]),
        strategy: import_prop_types.default.oneOf(["absolute", "fixed"]),
      }),
      /**
       * A ref that points to the used popper instance.
       */
      popperRef: refType_default,
      /**
       * The props used for each slot inside the Popper.
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        root: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      }),
      /**
       * The components used for each slot inside the Popper.
       * Either a string to use a HTML element or a component.
       * @default {}
       */
      slots: import_prop_types.default.shape({
        root: import_prop_types.default.elementType,
      }),
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * Help supporting a react-transition-group/Transition component.
       * @default false
       */
      transition: import_prop_types.default.bool,
    })
  : void 0;
var Popper_default = Popper3;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/InputBase/InputBase.js
var React53 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/TextareaAutosize/TextareaAutosize.js
var React50 = __toESM(require_react(), 1);
var import_jsx_runtime33 = __toESM(require_jsx_runtime(), 1);
function getStyleValue2(value) {
  return parseInt(value, 10) || 0;
}
var styles = {
  shadow: {
    // Visibility needed to hide the extra text area on iPads
    visibility: "hidden",
    // Remove from the content flow
    position: "absolute",
    // Ignore the scrollbar width
    overflow: "hidden",
    height: 0,
    top: 0,
    left: 0,
    // Create a new layer, increase the isolation of the computed values
    transform: "translateZ(0)",
  },
};
function isObjectEmpty3(object) {
  for (const _ in object) {
    return false;
  }
  return true;
}
function isEmpty2(obj) {
  return (
    isObjectEmpty3(obj) || (obj.outerHeightStyle === 0 && !obj.overflowing)
  );
}
var TextareaAutosize = /* @__PURE__ */ React50.forwardRef(
  function TextareaAutosize2(props, forwardedRef) {
    const {
      onChange,
      maxRows,
      minRows = 1,
      style: style4,
      value,
      ...other
    } = props;
    const { current: isControlled } = React50.useRef(value != null);
    const textareaRef = React50.useRef(null);
    const handleRef = useForkRef(forwardedRef, textareaRef);
    const heightRef = React50.useRef(null);
    const hiddenTextareaRef = React50.useRef(null);
    const calculateTextareaStyles = React50.useCallback(() => {
      const textarea = textareaRef.current;
      const hiddenTextarea = hiddenTextareaRef.current;
      if (!textarea || !hiddenTextarea) {
        return void 0;
      }
      const containerWindow = ownerWindow(textarea);
      const computedStyle = containerWindow.getComputedStyle(textarea);
      if (computedStyle.width === "0px") {
        return {
          outerHeightStyle: 0,
          overflowing: false,
        };
      }
      hiddenTextarea.style.width = computedStyle.width;
      hiddenTextarea.value = textarea.value || props.placeholder || "x";
      if (hiddenTextarea.value.slice(-1) === "\n") {
        hiddenTextarea.value += " ";
      }
      const boxSizing2 = computedStyle.boxSizing;
      const padding2 =
        getStyleValue2(computedStyle.paddingBottom) +
        getStyleValue2(computedStyle.paddingTop);
      const border2 =
        getStyleValue2(computedStyle.borderBottomWidth) +
        getStyleValue2(computedStyle.borderTopWidth);
      const innerHeight = hiddenTextarea.scrollHeight;
      hiddenTextarea.value = "x";
      const singleRowHeight = hiddenTextarea.scrollHeight;
      let outerHeight = innerHeight;
      if (minRows) {
        outerHeight = Math.max(Number(minRows) * singleRowHeight, outerHeight);
      }
      if (maxRows) {
        outerHeight = Math.min(Number(maxRows) * singleRowHeight, outerHeight);
      }
      outerHeight = Math.max(outerHeight, singleRowHeight);
      const outerHeightStyle =
        outerHeight + (boxSizing2 === "border-box" ? padding2 + border2 : 0);
      const overflowing = Math.abs(outerHeight - innerHeight) <= 1;
      return {
        outerHeightStyle,
        overflowing,
      };
    }, [maxRows, minRows, props.placeholder]);
    const didHeightChange = useEventCallback_default(() => {
      const textarea = textareaRef.current;
      const textareaStyles = calculateTextareaStyles();
      if (!textarea || !textareaStyles || isEmpty2(textareaStyles)) {
        return false;
      }
      const outerHeightStyle = textareaStyles.outerHeightStyle;
      return (
        heightRef.current != null && heightRef.current !== outerHeightStyle
      );
    });
    const syncHeight = React50.useCallback(() => {
      const textarea = textareaRef.current;
      const textareaStyles = calculateTextareaStyles();
      if (!textarea || !textareaStyles || isEmpty2(textareaStyles)) {
        return;
      }
      const outerHeightStyle = textareaStyles.outerHeightStyle;
      if (heightRef.current !== outerHeightStyle) {
        heightRef.current = outerHeightStyle;
        textarea.style.height = `${outerHeightStyle}px`;
      }
      textarea.style.overflow = textareaStyles.overflowing ? "hidden" : "";
    }, [calculateTextareaStyles]);
    const frameRef = React50.useRef(-1);
    useEnhancedEffect_default(() => {
      const debouncedHandleResize = debounce(syncHeight);
      const textarea = textareaRef?.current;
      if (!textarea) {
        return void 0;
      }
      const containerWindow = ownerWindow(textarea);
      containerWindow.addEventListener("resize", debouncedHandleResize);
      let resizeObserver;
      if (typeof ResizeObserver !== "undefined") {
        resizeObserver = new ResizeObserver(() => {
          if (didHeightChange()) {
            resizeObserver.unobserve(textarea);
            cancelAnimationFrame(frameRef.current);
            syncHeight();
            frameRef.current = requestAnimationFrame(() => {
              resizeObserver.observe(textarea);
            });
          }
        });
        resizeObserver.observe(textarea);
      }
      return () => {
        debouncedHandleResize.clear();
        cancelAnimationFrame(frameRef.current);
        containerWindow.removeEventListener("resize", debouncedHandleResize);
        if (resizeObserver) {
          resizeObserver.disconnect();
        }
      };
    }, [calculateTextareaStyles, syncHeight, didHeightChange]);
    useEnhancedEffect_default(() => {
      syncHeight();
    });
    const handleChange = (event) => {
      if (!isControlled) {
        syncHeight();
      }
      const textarea = event.target;
      const countOfCharacters = textarea.value.length;
      const isLastCharacterNewLine = textarea.value.endsWith("\n");
      const isEndOfTheLine = textarea.selectionStart === countOfCharacters;
      if (isLastCharacterNewLine && isEndOfTheLine) {
        textarea.setSelectionRange(countOfCharacters, countOfCharacters);
      }
      if (onChange) {
        onChange(event);
      }
    };
    return /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)(React50.Fragment, {
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("textarea", {
          value,
          onChange: handleChange,
          ref: handleRef,
          rows: minRows,
          style: style4,
          ...other,
        }),
        /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("textarea", {
          "aria-hidden": true,
          className: props.className,
          readOnly: true,
          ref: hiddenTextareaRef,
          tabIndex: -1,
          style: {
            ...styles.shadow,
            ...style4,
            paddingTop: 0,
            paddingBottom: 0,
          },
        }),
      ],
    });
  },
);
false
  ? (TextareaAutosize.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * Maximum number of rows to display.
       */
      maxRows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * Minimum number of rows to display.
       * @default 1
       */
      minRows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * @ignore
       */
      onChange: import_prop_types.default.func,
      /**
       * @ignore
       */
      placeholder: import_prop_types.default.string,
      /**
       * @ignore
       */
      style: import_prop_types.default.object,
      /**
       * @ignore
       */
      value: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(import_prop_types.default.string),
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
    })
  : void 0;
var TextareaAutosize_default = TextareaAutosize;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/FormControl/formControlState.js
function formControlState({ props, states, muiFormControl }) {
  return states.reduce((acc, state) => {
    acc[state] = props[state];
    if (muiFormControl) {
      if (typeof props[state] === "undefined") {
        acc[state] = muiFormControl[state];
      }
    }
    return acc;
  }, {});
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/FormControl/FormControlContext.js
var React51 = __toESM(require_react(), 1);
var FormControlContext = /* @__PURE__ */ React51.createContext(void 0);
if (false) {
  FormControlContext.displayName = "FormControlContext";
}
var FormControlContext_default = FormControlContext;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/FormControl/useFormControl.js
var React52 = __toESM(require_react(), 1);
function useFormControl() {
  return React52.useContext(FormControlContext_default);
}

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/getActiveElement/getActiveElement.js
function activeElement(doc) {
  let element = doc.activeElement;
  while (element?.shadowRoot?.activeElement != null) {
    element = element.shadowRoot.activeElement;
  }
  return element;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/getActiveElement.js
var getActiveElement_default = activeElement;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/InputBase/utils.js
function hasValue(value) {
  return value != null && !(Array.isArray(value) && value.length === 0);
}
function isFilled(obj, SSR = false) {
  return (
    obj &&
    ((hasValue(obj.value) && obj.value !== "") ||
      (SSR && hasValue(obj.defaultValue) && obj.defaultValue !== ""))
  );
}
function isAdornedStart(obj) {
  return obj.startAdornment;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/InputBase/inputBaseClasses.js
function getInputBaseUtilityClass(slot) {
  return generateUtilityClass("MuiInputBase", slot);
}
var inputBaseClasses = generateUtilityClasses("MuiInputBase", [
  "root",
  "formControl",
  "focused",
  "disabled",
  "adornedStart",
  "adornedEnd",
  "error",
  "sizeSmall",
  "multiline",
  "colorSecondary",
  "fullWidth",
  "hiddenLabel",
  "readOnly",
  "input",
  "inputSizeSmall",
  "inputMultiline",
  "inputTypeSearch",
  "inputAdornedStart",
  "inputAdornedEnd",
  "inputHiddenLabel",
]);
var inputBaseClasses_default = inputBaseClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/InputBase/InputBase.js
var import_jsx_runtime34 = __toESM(require_jsx_runtime(), 1);
var _InputGlobalStyles;
var MUI_AUTO_FILL = "mui-auto-fill";
var MUI_AUTO_FILL_CANCEL = "mui-auto-fill-cancel";
var rootOverridesResolver = (props, styles5) => {
  const { ownerState } = props;
  return [
    styles5.root,
    ownerState.formControl && styles5.formControl,
    ownerState.startAdornment && styles5.adornedStart,
    ownerState.endAdornment && styles5.adornedEnd,
    ownerState.error && styles5.error,
    ownerState.size === "small" && styles5.sizeSmall,
    ownerState.multiline && styles5.multiline,
    ownerState.color && styles5[`color${capitalize_default(ownerState.color)}`],
    ownerState.fullWidth && styles5.fullWidth,
    ownerState.hiddenLabel && styles5.hiddenLabel,
  ];
};
var inputOverridesResolver = (props, styles5) => {
  const { ownerState } = props;
  return [
    styles5.input,
    ownerState.size === "small" && styles5.inputSizeSmall,
    ownerState.multiline && styles5.inputMultiline,
    ownerState.type === "search" && styles5.inputTypeSearch,
    ownerState.startAdornment && styles5.inputAdornedStart,
    ownerState.endAdornment && styles5.inputAdornedEnd,
    ownerState.hiddenLabel && styles5.inputHiddenLabel,
  ];
};
var useUtilityClasses11 = (ownerState) => {
  const {
    classes,
    color: color2,
    disabled,
    error,
    endAdornment,
    focused,
    formControl,
    fullWidth,
    hiddenLabel,
    multiline,
    readOnly,
    size,
    startAdornment,
    type,
  } = ownerState;
  const slots = {
    root: [
      "root",
      `color${capitalize_default(color2)}`,
      disabled && "disabled",
      error && "error",
      fullWidth && "fullWidth",
      focused && "focused",
      formControl && "formControl",
      size && size !== "medium" && `size${capitalize_default(size)}`,
      multiline && "multiline",
      startAdornment && "adornedStart",
      endAdornment && "adornedEnd",
      hiddenLabel && "hiddenLabel",
      readOnly && "readOnly",
    ],
    input: [
      "input",
      disabled && "disabled",
      type === "search" && "inputTypeSearch",
      multiline && "inputMultiline",
      size === "small" && "inputSizeSmall",
      hiddenLabel && "inputHiddenLabel",
      startAdornment && "inputAdornedStart",
      endAdornment && "inputAdornedEnd",
      readOnly && "readOnly",
    ],
  };
  return composeClasses(slots, getInputBaseUtilityClass, classes);
};
var InputBaseRoot = styled_default2("div", {
  name: "MuiInputBase",
  slot: "Root",
  overridesResolver: rootOverridesResolver,
})(
  memoTheme_default(({ theme: theme2 }) => ({
    ...theme2.typography.body1,
    color: (theme2.vars || theme2).palette.text.primary,
    lineHeight: "1.4375em",
    // 23px
    boxSizing: "border-box",
    // Prevent padding issue with fullWidth.
    position: "relative",
    cursor: "text",
    display: "inline-flex",
    alignItems: "center",
    [`&.${inputBaseClasses_default.disabled}`]: {
      color: (theme2.vars || theme2).palette.text.disabled,
      cursor: "default",
    },
    variants: [
      {
        props: ({ ownerState }) => ownerState.multiline,
        style: {
          padding: "4px 0 5px",
        },
      },
      {
        props: ({ ownerState, size }) =>
          ownerState.multiline && size === "small",
        style: {
          paddingTop: 1,
        },
      },
      {
        props: ({ ownerState }) => ownerState.fullWidth,
        style: {
          width: "100%",
        },
      },
    ],
  })),
);
var InputBaseInput = styled_default2("input", {
  name: "MuiInputBase",
  slot: "Input",
  overridesResolver: inputOverridesResolver,
})(
  memoTheme_default(({ theme: theme2 }) => {
    const light2 = theme2.palette.mode === "light";
    const placeholder = {
      color: "currentColor",
      ...(theme2.vars
        ? {
            opacity: theme2.vars.opacity.inputPlaceholder,
          }
        : {
            opacity: light2 ? 0.42 : 0.5,
          }),
      transition: theme2.transitions.create("opacity", {
        duration: theme2.transitions.duration.shorter,
      }),
    };
    const placeholderHidden = {
      opacity: "0 !important",
    };
    const placeholderVisible = theme2.vars
      ? {
          opacity: theme2.vars.opacity.inputPlaceholder,
        }
      : {
          opacity: light2 ? 0.42 : 0.5,
        };
    return {
      font: "inherit",
      letterSpacing: "inherit",
      color: "currentColor",
      padding: "4px 0 5px",
      border: 0,
      boxSizing: "content-box",
      background: "none",
      height: "1.4375em",
      // Reset 23pxthe native input line-height
      margin: 0,
      // Reset for Safari
      WebkitTapHighlightColor: "transparent",
      display: "block",
      // Make the flex item shrink with Firefox
      minWidth: 0,
      width: "100%",
      "&::-webkit-input-placeholder": placeholder,
      "&::-moz-placeholder": placeholder,
      // Firefox 19+
      "&::-ms-input-placeholder": placeholder,
      // Edge
      "&:focus": {
        outline: 0,
      },
      // Reset Firefox invalid required input style
      "&:invalid": {
        boxShadow: "none",
      },
      "&::-webkit-search-decoration": {
        // Remove the padding when type=search.
        WebkitAppearance: "none",
      },
      // Show and hide the placeholder logic
      [`label[data-shrink=false] + .${inputBaseClasses_default.formControl} &`]:
        {
          "&::-webkit-input-placeholder": placeholderHidden,
          "&::-moz-placeholder": placeholderHidden,
          // Firefox 19+
          "&::-ms-input-placeholder": placeholderHidden,
          // Edge
          "&:focus::-webkit-input-placeholder": placeholderVisible,
          "&:focus::-moz-placeholder": placeholderVisible,
          // Firefox 19+
          "&:focus::-ms-input-placeholder": placeholderVisible,
          // Edge
        },
      [`&.${inputBaseClasses_default.disabled}`]: {
        opacity: 1,
        // Reset iOS opacity
        WebkitTextFillColor: (theme2.vars || theme2).palette.text.disabled,
        // Fix opacity Safari bug
      },
      variants: [
        {
          props: ({ ownerState }) => !ownerState.disableInjectingGlobalStyles,
          style: {
            animationName: MUI_AUTO_FILL_CANCEL,
            animationDuration: "10ms",
            "&:-webkit-autofill": {
              animationDuration: "5000s",
              animationName: MUI_AUTO_FILL,
            },
          },
        },
        {
          props: {
            size: "small",
          },
          style: {
            paddingTop: 1,
          },
        },
        {
          props: ({ ownerState }) => ownerState.multiline,
          style: {
            height: "auto",
            resize: "none",
            padding: 0,
            paddingTop: 0,
          },
        },
        {
          props: {
            type: "search",
          },
          style: {
            MozAppearance: "textfield",
            // Improve type search style.
          },
        },
      ],
    };
  }),
);
var InputGlobalStyles = globalCss({
  // Keep keyframes non-empty for Emotion production builds. Animation properties are ignored
  // inside keyframes, avoiding the visible display animation triggered by Chrome 117+.
  [`@keyframes ${MUI_AUTO_FILL}`]: {
    from: {
      animationName: MUI_AUTO_FILL,
    },
  },
  [`@keyframes ${MUI_AUTO_FILL_CANCEL}`]: {
    from: {
      animationName: MUI_AUTO_FILL_CANCEL,
    },
  },
});
var InputBase = /* @__PURE__ */ React53.forwardRef(
  function InputBase2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiInputBase",
    });
    const {
      "aria-describedby": ariaDescribedby,
      autoComplete,
      autoFocus,
      className,
      color: color2,
      components = {},
      componentsProps = {},
      defaultValue,
      disabled,
      disableInjectingGlobalStyles,
      endAdornment,
      error,
      fullWidth = false,
      id,
      inputComponent = "input",
      inputProps: inputPropsProp = {},
      inputRef: inputRefProp,
      margin: margin2,
      maxRows,
      minRows,
      multiline = false,
      name,
      onBlur,
      onChange,
      onClick,
      onFocus,
      onKeyDown,
      onKeyUp,
      placeholder,
      readOnly,
      renderSuffix,
      rows,
      size,
      slotProps = {},
      slots = {},
      startAdornment,
      type = "text",
      value: valueProp,
      ...other
    } = props;
    const value =
      inputPropsProp.value != null ? inputPropsProp.value : valueProp;
    const { current: isControlled } = React53.useRef(value != null);
    const inputRef = React53.useRef();
    const handleInputRefWarning = React53.useCallback((instance) => {
      if (false) {
        if (instance && instance.nodeName !== "INPUT" && !instance.focus) {
          console.error(
            [
              "MUI: You have provided a `inputComponent` to the input component",
              "that does not correctly handle the `ref` prop.",
              "Make sure the `ref` prop is called with a HTMLInputElement.",
            ].join("\n"),
          );
        }
      }
    }, []);
    const handleInputRef = useForkRef_default(
      inputRef,
      inputRefProp,
      inputPropsProp.ref,
      handleInputRefWarning,
    );
    const [focused, setFocused] = React53.useState(false);
    const muiFormControl = useFormControl();
    if (false) {
      React53.useEffect(() => {
        if (muiFormControl) {
          return muiFormControl.registerEffect();
        }
        return void 0;
      }, [muiFormControl]);
    }
    const fcs = formControlState({
      props,
      muiFormControl,
      states: [
        "color",
        "disabled",
        "error",
        "hiddenLabel",
        "size",
        "required",
        "filled",
      ],
    });
    fcs.focused = muiFormControl ? muiFormControl.focused : focused;
    React53.useEffect(() => {
      if (!muiFormControl && disabled && focused) {
        setFocused(false);
        if (onBlur) {
          onBlur();
        }
      }
    }, [muiFormControl, disabled, focused, onBlur]);
    const onFilled = muiFormControl && muiFormControl.onFilled;
    const onEmpty = muiFormControl && muiFormControl.onEmpty;
    const checkDirty = React53.useCallback(
      (obj) => {
        if (isFilled(obj)) {
          if (onFilled) {
            onFilled();
          }
        } else if (onEmpty) {
          onEmpty();
        }
      },
      [onFilled, onEmpty],
    );
    useEnhancedEffect_default2(() => {
      if (isControlled) {
        checkDirty({
          value,
        });
      }
    }, [value, checkDirty, isControlled]);
    useEnhancedEffect_default2(() => {
      if (!autoFocus) {
        return;
      }
      const input = inputRef.current;
      if (!input) {
        return;
      }
      const doc = ownerDocument_default(input);
      const activeElement2 = getActiveElement_default(doc);
      const noElementFocused =
        activeElement2 == null ||
        activeElement2 === doc.body ||
        activeElement2 === doc.documentElement;
      if (input === activeElement2) {
        if (muiFormControl && muiFormControl.onFocus) {
          muiFormControl.onFocus();
        } else {
          setFocused(true);
        }
      } else if (noElementFocused) {
        input.focus();
      }
    }, [autoFocus]);
    const handleFocus = (event) => {
      if (onFocus) {
        onFocus(event);
      }
      if (inputPropsProp.onFocus) {
        inputPropsProp.onFocus(event);
      }
      if (muiFormControl && muiFormControl.onFocus) {
        muiFormControl.onFocus(event);
      } else {
        setFocused(true);
      }
    };
    const handleBlur = (event) => {
      if (onBlur) {
        onBlur(event);
      }
      if (inputPropsProp.onBlur) {
        inputPropsProp.onBlur(event);
      }
      if (muiFormControl && muiFormControl.onBlur) {
        muiFormControl.onBlur(event);
      } else {
        setFocused(false);
      }
    };
    const handleChange = (event, ...args) => {
      if (!isControlled) {
        const element = event.target || inputRef.current;
        if (element == null) {
          throw new Error(
            false
              ? "MUI: Expected valid input target. Did you use a custom `inputComponent` and forget to forward refs? See https://mui.com/r/input-component-ref-interface for more info."
              : formatMuiErrorMessage(1),
          );
        }
        checkDirty({
          value: element.value,
        });
      }
      if (inputPropsProp.onChange) {
        inputPropsProp.onChange(event, ...args);
      }
      if (onChange) {
        onChange(event, ...args);
      }
    };
    React53.useEffect(() => {
      checkDirty(inputRef.current);
    }, []);
    const handleClick = (event) => {
      if (inputRef.current && event.currentTarget === event.target) {
        inputRef.current.focus();
      }
      if (onClick) {
        onClick(event);
      }
    };
    let InputComponent = inputComponent;
    let inputProps = inputPropsProp;
    if (multiline && InputComponent === "input") {
      if (rows) {
        if (false) {
          if (minRows || maxRows) {
            console.warn(
              "MUI: You can not use the `minRows` or `maxRows` props when the input `rows` prop is set.",
            );
          }
        }
        inputProps = {
          type: void 0,
          minRows: rows,
          maxRows: rows,
          ...inputProps,
        };
      } else {
        inputProps = {
          type: void 0,
          maxRows,
          minRows,
          ...inputProps,
        };
      }
      InputComponent = TextareaAutosize_default;
    }
    const handleAutoFill = (event) => {
      checkDirty(
        event.animationName === MUI_AUTO_FILL_CANCEL
          ? inputRef.current
          : {
              value: "x",
            },
      );
    };
    React53.useEffect(() => {
      if (muiFormControl) {
        muiFormControl.setAdornedStart(Boolean(startAdornment));
      }
    }, [muiFormControl, startAdornment]);
    const ownerState = {
      ...props,
      color: fcs.color || "primary",
      disabled: fcs.disabled,
      endAdornment,
      error: fcs.error,
      focused: fcs.focused,
      formControl: muiFormControl,
      fullWidth,
      hiddenLabel: fcs.hiddenLabel,
      multiline,
      size: fcs.size,
      startAdornment,
      type,
    };
    const classes = useUtilityClasses11(ownerState);
    const Root = slots.root || components.Root || InputBaseRoot;
    const rootProps = slotProps.root || componentsProps.root || {};
    const Input3 = slots.input || components.Input || InputBaseInput;
    inputProps = {
      ...inputProps,
      ...(slotProps.input ?? componentsProps.input),
    };
    return /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)(React53.Fragment, {
      children: [
        !disableInjectingGlobalStyles &&
          typeof InputGlobalStyles === "function" && // For Emotion/Styled-components, InputGlobalStyles will be a function
          // For Pigment CSS, this has no effect because the InputGlobalStyles will be null.
          (_InputGlobalStyles ||
            (_InputGlobalStyles = /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
              InputGlobalStyles,
              {},
            ))),
        /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)(Root, {
          ...rootProps,
          ref,
          onClick: handleClick,
          ...other,
          ...(!isHostComponent_default(Root) && {
            ownerState: {
              ...ownerState,
              ...rootProps.ownerState,
            },
          }),
          className: clsx_default(
            classes.root,
            rootProps.className,
            className,
            readOnly && "MuiInputBase-readOnly",
          ),
          children: [
            startAdornment,
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
              FormControlContext_default.Provider,
              {
                value: null,
                children: /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
                  Input3,
                  {
                    "aria-invalid": fcs.error,
                    "aria-describedby": ariaDescribedby,
                    autoComplete,
                    autoFocus,
                    defaultValue,
                    disabled: fcs.disabled,
                    id,
                    onAnimationStart: handleAutoFill,
                    name,
                    placeholder,
                    readOnly,
                    required: fcs.required,
                    rows,
                    value,
                    onKeyDown,
                    onKeyUp,
                    type,
                    ...inputProps,
                    ...(!isHostComponent_default(Input3) && {
                      as: InputComponent,
                      ownerState: {
                        ...ownerState,
                        ...inputProps.ownerState,
                      },
                    }),
                    ref: handleInputRef,
                    className: clsx_default(
                      classes.input,
                      inputProps.className,
                      readOnly && "MuiInputBase-readOnly",
                    ),
                    onBlur: handleBlur,
                    onChange: handleChange,
                    onFocus: handleFocus,
                  },
                ),
              },
            ),
            endAdornment,
            renderSuffix
              ? renderSuffix({
                  ...fcs,
                  startAdornment,
                })
              : null,
          ],
        }),
      ],
    });
  },
);
false
  ? (InputBase.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * @ignore
       */
      "aria-describedby": import_prop_types.default.string,
      /**
       * This prop helps users to fill forms faster, especially on mobile devices.
       * The name can be confusing, as it's more like an autofill.
       * You can learn more about it [following the specification](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#autofill).
       */
      autoComplete: import_prop_types.default.string,
      /**
       * If `true`, the `input` element is focused during the first mount.
       */
      autoFocus: import_prop_types.default.bool,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The color of the component.
       * It supports both default and custom theme colors, which can be added as shown in the
       * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
       * The prop defaults to the value (`'primary'`) inherited from the parent FormControl component.
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "primary",
          "secondary",
          "error",
          "info",
          "success",
          "warning",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * The components used for each slot inside.
       *
       * @deprecated use the `slots` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       *
       * @default {}
       */
      components: import_prop_types.default.shape({
        Input: import_prop_types.default.elementType,
        Root: import_prop_types.default.elementType,
      }),
      /**
       * The extra props for the slot components.
       * You can override the existing props or add new ones.
       *
       * @deprecated use the `slotProps` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       *
       * @default {}
       */
      componentsProps: import_prop_types.default.shape({
        input: import_prop_types.default.object,
        root: import_prop_types.default.object,
      }),
      /**
       * The default value. Use when the component is not controlled.
       */
      defaultValue: import_prop_types.default.any,
      /**
       * If `true`, the component is disabled.
       * The prop defaults to the value (`false`) inherited from the parent FormControl component.
       */
      disabled: import_prop_types.default.bool,
      /**
       * If `true`, GlobalStyles for the auto-fill keyframes will not be injected/removed on mount/unmount. Make sure to inject them at the top of your application.
       * This option is intended to help with boosting the initial rendering performance if you are loading a big amount of Input components at once.
       * @default false
       */
      disableInjectingGlobalStyles: import_prop_types.default.bool,
      /**
       * End `InputAdornment` for this component.
       */
      endAdornment: import_prop_types.default.node,
      /**
       * If `true`, the `input` will indicate an error.
       * The prop defaults to the value (`false`) inherited from the parent FormControl component.
       */
      error: import_prop_types.default.bool,
      /**
       * If `true`, the `input` will take up the full width of its container.
       * @default false
       */
      fullWidth: import_prop_types.default.bool,
      /**
       * The id of the `input` element.
       */
      id: import_prop_types.default.string,
      /**
       * The component used for the `input` element.
       * Either a string to use a HTML element or a component.
       * @default 'input'
       */
      inputComponent: elementTypeAcceptingRef_default,
      /**
       * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
       * @default {}
       */
      inputProps: import_prop_types.default.object,
      /**
       * Pass a ref to the `input` element.
       */
      inputRef: refType_default,
      /**
       * If `dense`, will adjust vertical spacing. This is normally obtained via context from
       * FormControl.
       * The prop defaults to the value (`'none'`) inherited from the parent FormControl component.
       */
      margin: import_prop_types.default.oneOf(["dense", "none"]),
      /**
       * Maximum number of rows to display when multiline option is set to true.
       */
      maxRows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * Minimum number of rows to display when multiline option is set to true.
       */
      minRows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * If `true`, a [TextareaAutosize](https://mui.com/material-ui/react-textarea-autosize/) element is rendered.
       * @default false
       */
      multiline: import_prop_types.default.bool,
      /**
       * Name attribute of the `input` element.
       */
      name: import_prop_types.default.string,
      /**
       * Callback fired when the `input` is blurred.
       *
       * Notice that the first argument (event) might be undefined.
       */
      onBlur: import_prop_types.default.func,
      /**
       * Callback fired when the value is changed.
       *
       * @param {React.ChangeEvent<HTMLTextAreaElement | HTMLInputElement>} event The event source of the callback.
       * You can pull out the new value by accessing `event.target.value` (string).
       */
      onChange: import_prop_types.default.func,
      /**
       * @ignore
       */
      onClick: import_prop_types.default.func,
      /**
       * @ignore
       */
      onFocus: import_prop_types.default.func,
      /**
       * Callback fired when the `input` doesn't satisfy its constraints.
       */
      onInvalid: import_prop_types.default.func,
      /**
       * @ignore
       */
      onKeyDown: import_prop_types.default.func,
      /**
       * @ignore
       */
      onKeyUp: import_prop_types.default.func,
      /**
       * The short hint displayed in the `input` before the user enters a value.
       */
      placeholder: import_prop_types.default.string,
      /**
       * It prevents the user from changing the value of the field
       * (not from interacting with the field).
       */
      readOnly: import_prop_types.default.bool,
      /**
       * @ignore
       */
      renderSuffix: import_prop_types.default.func,
      /**
       * If `true`, the `input` element is required.
       * The prop defaults to the value (`false`) inherited from the parent FormControl component.
       */
      required: import_prop_types.default.bool,
      /**
       * Number of rows to display when multiline option is set to true.
       */
      rows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * The size of the component.
       */
      size: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["medium", "small"]),
        import_prop_types.default.string,
      ]),
      /**
       * The extra props for the slot components.
       * You can override the existing props or add new ones.
       *
       * This prop is an alias for the `componentsProps` prop, which will be deprecated in the future.
       *
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        input: import_prop_types.default.object,
        root: import_prop_types.default.object,
      }),
      /**
       * The components used for each slot inside.
       *
       * This prop is an alias for the `components` prop, which will be deprecated in the future.
       *
       * @default {}
       */
      slots: import_prop_types.default.shape({
        input: import_prop_types.default.elementType,
        root: import_prop_types.default.elementType,
      }),
      /**
       * Start `InputAdornment` for this component.
       */
      startAdornment: import_prop_types.default.node,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * Type of the `input` element. It should be [a valid HTML5 input type](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#input_types).
       * @default 'text'
       */
      type: import_prop_types.default.string,
      /**
       * The value of the `input` element, required for a controlled component.
       */
      value: import_prop_types.default.any,
    })
  : void 0;
var InputBase_default = InputBase;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Input/inputClasses.js
function getInputUtilityClass(slot) {
  return generateUtilityClass("MuiInput", slot);
}
var inputClasses = {
  ...inputBaseClasses_default,
  ...generateUtilityClasses("MuiInput", ["root", "underline", "input"]),
};
var inputClasses_default = inputClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/OutlinedInput/outlinedInputClasses.js
function getOutlinedInputUtilityClass(slot) {
  return generateUtilityClass("MuiOutlinedInput", slot);
}
var outlinedInputClasses = {
  ...inputBaseClasses_default,
  ...generateUtilityClasses("MuiOutlinedInput", [
    "root",
    "notchedOutline",
    "input",
  ]),
};
var outlinedInputClasses_default = outlinedInputClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/FilledInput/filledInputClasses.js
function getFilledInputUtilityClass(slot) {
  return generateUtilityClass("MuiFilledInput", slot);
}
var filledInputClasses = {
  ...inputBaseClasses_default,
  ...generateUtilityClasses("MuiFilledInput", [
    "root",
    "underline",
    "input",
    "adornedStart",
    "adornedEnd",
    "sizeSmall",
    "multiline",
    "hiddenLabel",
  ]),
};
var filledInputClasses_default = filledInputClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/internal/svg-icons/ArrowDropDown.js
var import_jsx_runtime35 = __toESM(require_jsx_runtime(), 1);
var ArrowDropDown_default = createSvgIcon(
  /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("path", {
    d: "M7 10l5 5 5-5z",
  }),
  "ArrowDropDown",
);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Avatar/Avatar.js
var React54 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/internal/svg-icons/Person.js
var import_jsx_runtime36 = __toESM(require_jsx_runtime(), 1);
var Person_default = createSvgIcon(
  /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("path", {
    d: "M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z",
  }),
  "Person",
);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Avatar/avatarClasses.js
function getAvatarUtilityClass(slot) {
  return generateUtilityClass("MuiAvatar", slot);
}
var avatarClasses = generateUtilityClasses("MuiAvatar", [
  "root",
  "colorDefault",
  "circular",
  "rounded",
  "square",
  "img",
  "fallback",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Avatar/Avatar.js
var import_jsx_runtime37 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses12 = (ownerState) => {
  const { classes, variant, colorDefault } = ownerState;
  const slots = {
    root: ["root", variant, colorDefault && "colorDefault"],
    img: ["img"],
    fallback: ["fallback"],
  };
  return composeClasses(slots, getAvatarUtilityClass, classes);
};
var AvatarRoot = styled_default2("div", {
  name: "MuiAvatar",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      styles5[ownerState.variant],
      ownerState.colorDefault && styles5.colorDefault,
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    position: "relative",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
    width: 40,
    height: 40,
    fontFamily: theme2.typography.fontFamily,
    fontSize: theme2.typography.pxToRem(20),
    lineHeight: 1,
    borderRadius: "50%",
    overflow: "hidden",
    userSelect: "none",
    variants: [
      {
        props: {
          variant: "rounded",
        },
        style: {
          borderRadius: (theme2.vars || theme2).shape.borderRadius,
        },
      },
      {
        props: {
          variant: "square",
        },
        style: {
          borderRadius: 0,
        },
      },
      {
        props: {
          colorDefault: true,
        },
        style: {
          color: (theme2.vars || theme2).palette.background.default,
          ...(theme2.vars
            ? {
                backgroundColor: theme2.vars.palette.Avatar.defaultBg,
              }
            : {
                backgroundColor: theme2.palette.grey[400],
                ...theme2.applyStyles("dark", {
                  backgroundColor: theme2.palette.grey[600],
                }),
              }),
        },
      },
    ],
  })),
);
var AvatarImg = styled_default2("img", {
  name: "MuiAvatar",
  slot: "Img",
})({
  width: "100%",
  height: "100%",
  textAlign: "center",
  // Handle non-square image.
  objectFit: "cover",
  // Hide alt text.
  color: "transparent",
  // Hide the image broken icon, only works on Chrome.
  textIndent: 1e4,
});
var AvatarFallback = styled_default2(Person_default, {
  name: "MuiAvatar",
  slot: "Fallback",
})({
  width: "75%",
  height: "75%",
});
function useLoaded({ crossOrigin, referrerPolicy, src, srcSet }) {
  const [loaded, setLoaded] = React54.useState(false);
  React54.useEffect(() => {
    if (!src && !srcSet) {
      return void 0;
    }
    setLoaded(false);
    let active = true;
    const image = new Image();
    image.onload = () => {
      if (!active) {
        return;
      }
      setLoaded("loaded");
    };
    image.onerror = () => {
      if (!active) {
        return;
      }
      setLoaded("error");
    };
    image.crossOrigin = crossOrigin;
    image.referrerPolicy = referrerPolicy;
    image.src = src;
    if (srcSet) {
      image.srcset = srcSet;
    }
    return () => {
      active = false;
    };
  }, [crossOrigin, referrerPolicy, src, srcSet]);
  return loaded;
}
var Avatar = /* @__PURE__ */ React54.forwardRef(function Avatar2(inProps, ref) {
  const props = useDefaultProps2({
    props: inProps,
    name: "MuiAvatar",
  });
  const {
    alt,
    children: childrenProp,
    className,
    component = "div",
    slots = {},
    slotProps = {},
    imgProps,
    sizes,
    src,
    srcSet,
    variant = "circular",
    ...other
  } = props;
  let children = null;
  const ownerState = {
    ...props,
    component,
    variant,
  };
  const loaded = useLoaded({
    ...imgProps,
    ...(typeof slotProps.img === "function"
      ? slotProps.img(ownerState)
      : slotProps.img),
    src,
    srcSet,
  });
  const hasImg = src || srcSet;
  const hasImgNotFailing = hasImg && loaded !== "error";
  ownerState.colorDefault = !hasImgNotFailing;
  delete ownerState.ownerState;
  const classes = useUtilityClasses12(ownerState);
  const [RootSlot, rootSlotProps] = useSlot("root", {
    ref,
    className: clsx_default(classes.root, className),
    elementType: AvatarRoot,
    externalForwardedProps: {
      slots,
      slotProps,
      component,
      ...other,
    },
    ownerState,
  });
  const [ImgSlot, imgSlotProps] = useSlot("img", {
    className: classes.img,
    elementType: AvatarImg,
    externalForwardedProps: {
      slots,
      slotProps: {
        img: {
          ...imgProps,
          ...slotProps.img,
        },
      },
    },
    additionalProps: {
      alt,
      src,
      srcSet,
      sizes,
    },
    ownerState,
  });
  const [FallbackSlot, fallbackSlotProps] = useSlot("fallback", {
    className: classes.fallback,
    elementType: AvatarFallback,
    externalForwardedProps: {
      slots,
      slotProps,
    },
    shouldForwardComponentProp: true,
    ownerState,
  });
  if (hasImgNotFailing) {
    children = /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(ImgSlot, {
      ...imgSlotProps,
    });
  } else if (!!childrenProp || childrenProp === 0) {
    children = childrenProp;
  } else if (hasImg && alt) {
    children = alt[0];
  } else {
    children = /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(FallbackSlot, {
      ...fallbackSlotProps,
    });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(RootSlot, {
    ...rootSlotProps,
    children,
  });
});
false
  ? (Avatar.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * Used in combination with `src` or `srcSet` to
       * provide an alt attribute for the rendered `img` element.
       */
      alt: import_prop_types.default.string,
      /**
       * Used to render icon or text elements inside the Avatar if `src` is not set.
       * This can be an element, or just a string.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/img#attributes) applied to the `img` element if the component is used to display an image.
       * It can be used to listen for the loading error event.
       * @deprecated Use `slotProps.img` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       */
      imgProps: import_prop_types.default.object,
      /**
       * The `sizes` attribute for the `img` element.
       */
      sizes: import_prop_types.default.string,
      /**
       * The props used for each slot inside.
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        fallback: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        img: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        root: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      }),
      /**
       * The components used for each slot inside.
       * @default {}
       */
      slots: import_prop_types.default.shape({
        fallback: import_prop_types.default.elementType,
        img: import_prop_types.default.elementType,
        root: import_prop_types.default.elementType,
      }),
      /**
       * The `src` attribute for the `img` element.
       */
      src: import_prop_types.default.string,
      /**
       * The `srcSet` attribute for the `img` element.
       * Use this attribute for responsive image display.
       */
      srcSet: import_prop_types.default.string,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * The shape of the avatar.
       * @default 'circular'
       */
      variant: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["circular", "rounded", "square"]),
        import_prop_types.default.string,
      ]),
    })
  : void 0;
var Avatar_default = Avatar;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Backdrop/Backdrop.js
var React56 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Fade/Fade.js
var React55 = __toESM(require_react(), 1);
var import_jsx_runtime38 = __toESM(require_jsx_runtime(), 1);
var styles2 = {
  entering: {
    opacity: 1,
  },
  entered: {
    opacity: 1,
  },
  exiting: {
    opacity: 0,
  },
  exited: {
    opacity: 0,
  },
};
var hiddenStyles = {
  opacity: 0,
  visibility: "hidden",
};
var Fade = /* @__PURE__ */ React55.forwardRef(function Fade2(props, ref) {
  const theme2 = useTheme5();
  const defaultTimeout = {
    enter: theme2.transitions.duration.enteringScreen,
    exit: theme2.transitions.duration.leavingScreen,
  };
  const {
    addEndListener,
    appear = true,
    children,
    easing: easing2,
    in: inProp,
    onEnter,
    onEntered,
    onEntering,
    onExit,
    onExited,
    onExiting,
    style: style4,
    timeout: timeout2 = defaultTimeout,
    ...other
  } = props;
  const nodeRef = React55.useRef(null);
  const handleRef = useForkRef_default(
    nodeRef,
    getReactElementRef(children),
    ref,
  );
  const handleEntering = normalizedTransitionCallback(nodeRef, onEntering);
  const handleEnter = normalizedTransitionCallback(
    nodeRef,
    (node2, isAppearing) => {
      reflow(node2);
      const transitionProps = getTransitionProps(
        {
          style: style4,
          timeout: timeout2,
          easing: easing2,
        },
        {
          mode: "enter",
        },
      );
      node2.style.transition = theme2.transitions.create(
        "opacity",
        transitionProps,
      );
      if (onEnter) {
        onEnter(node2, isAppearing);
      }
    },
  );
  const handleEntered = normalizedTransitionCallback(nodeRef, onEntered);
  const handleExiting = normalizedTransitionCallback(nodeRef, onExiting);
  const handleExit = normalizedTransitionCallback(nodeRef, (node2) => {
    const transitionProps = getTransitionProps(
      {
        style: style4,
        timeout: timeout2,
        easing: easing2,
      },
      {
        mode: "exit",
      },
    );
    node2.style.transition = theme2.transitions.create(
      "opacity",
      transitionProps,
    );
    if (onExit) {
      onExit(node2);
    }
  });
  const handleExited = normalizedTransitionCallback(nodeRef, (node2) => {
    node2.style.transition = "";
    if (onExited) {
      onExited(node2);
    }
  });
  const handleAddEndListener = (next2) => {
    if (addEndListener) {
      addEndListener(nodeRef.current, next2);
    }
  };
  return /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Transition_default, {
    appear,
    in: inProp,
    nodeRef,
    onEnter: handleEnter,
    onEntered: handleEntered,
    onEntering: handleEntering,
    onExit: handleExit,
    onExited: handleExited,
    onExiting: handleExiting,
    addEndListener: handleAddEndListener,
    timeout: timeout2,
    ...other,
    children: (state, { ownerState, ...restChildProps }) => {
      const childStyle = getTransitionChildStyle(
        state,
        inProp,
        styles2,
        hiddenStyles,
        style4,
        children.props.style,
      );
      return /* @__PURE__ */ React55.cloneElement(children, {
        style: childStyle,
        ref: handleRef,
        ...restChildProps,
      });
    },
  });
});
false
  ? (Fade.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * Add a custom transition end trigger. Called with the transitioning DOM
       * node and a done callback. Allows for more fine grained transition end
       * logic. Note: Timeouts are still used as a fallback if provided.
       */
      addEndListener: import_prop_types.default.func,
      /**
       * Perform the enter transition when it first mounts if `in` is also `true`.
       * Set this to `false` to disable this behavior.
       * @default true
       */
      appear: import_prop_types.default.bool,
      /**
       * A single child content element.
       */
      children: elementAcceptingRef_default.isRequired,
      /**
       * The transition timing function.
       * You may specify a single easing or a object containing enter and exit values.
       */
      easing: import_prop_types.default.oneOfType([
        import_prop_types.default.shape({
          enter: import_prop_types.default.string,
          exit: import_prop_types.default.string,
        }),
        import_prop_types.default.string,
      ]),
      /**
       * If `true`, the component will transition in.
       */
      in: import_prop_types.default.bool,
      /**
       * @ignore
       */
      onEnter: import_prop_types.default.func,
      /**
       * @ignore
       */
      onEntered: import_prop_types.default.func,
      /**
       * @ignore
       */
      onEntering: import_prop_types.default.func,
      /**
       * @ignore
       */
      onExit: import_prop_types.default.func,
      /**
       * @ignore
       */
      onExited: import_prop_types.default.func,
      /**
       * @ignore
       */
      onExiting: import_prop_types.default.func,
      /**
       * @ignore
       */
      style: import_prop_types.default.object,
      /**
       * The duration for the transition, in milliseconds.
       * You may specify a single timeout for all transitions, or individually with an object.
       * @default {
       *   enter: theme.transitions.duration.enteringScreen,
       *   exit: theme.transitions.duration.leavingScreen,
       * }
       */
      timeout: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.shape({
          appear: import_prop_types.default.number,
          enter: import_prop_types.default.number,
          exit: import_prop_types.default.number,
        }),
      ]),
    })
  : void 0;
var Fade_default = Fade;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Backdrop/backdropClasses.js
function getBackdropUtilityClass(slot) {
  return generateUtilityClass("MuiBackdrop", slot);
}
var backdropClasses = generateUtilityClasses("MuiBackdrop", [
  "root",
  "invisible",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Backdrop/Backdrop.js
var import_jsx_runtime39 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses13 = (ownerState) => {
  const { classes, invisible } = ownerState;
  const slots = {
    root: ["root", invisible && "invisible"],
  };
  return composeClasses(slots, getBackdropUtilityClass, classes);
};
var BackdropRoot = styled_default2("div", {
  name: "MuiBackdrop",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [styles5.root, ownerState.invisible && styles5.invisible];
  },
})({
  position: "fixed",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  right: 0,
  bottom: 0,
  top: 0,
  left: 0,
  backgroundColor: "rgba(0, 0, 0, 0.5)",
  WebkitTapHighlightColor: "transparent",
  variants: [
    {
      props: {
        invisible: true,
      },
      style: {
        backgroundColor: "transparent",
      },
    },
  ],
});
var Backdrop = /* @__PURE__ */ React56.forwardRef(
  function Backdrop2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiBackdrop",
    });
    const {
      children,
      className,
      component = "div",
      invisible = false,
      open,
      components = {},
      componentsProps = {},
      slotProps = {},
      slots = {},
      TransitionComponent: TransitionComponentProp,
      transitionDuration,
      ...other
    } = props;
    const ownerState = {
      ...props,
      component,
      invisible,
    };
    const classes = useUtilityClasses13(ownerState);
    const backwardCompatibleSlots = {
      transition: TransitionComponentProp,
      root: components.Root,
      ...slots,
    };
    const backwardCompatibleSlotProps = {
      ...componentsProps,
      ...slotProps,
    };
    const externalForwardedProps = {
      component,
      slots: backwardCompatibleSlots,
      slotProps: backwardCompatibleSlotProps,
    };
    const [RootSlot, rootProps] = useSlot("root", {
      elementType: BackdropRoot,
      externalForwardedProps,
      className: clsx_default(classes.root, className),
      ownerState,
    });
    const [TransitionSlot, transitionProps] = useSlot("transition", {
      elementType: Fade_default,
      externalForwardedProps,
      ownerState,
    });
    return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(TransitionSlot, {
      in: open,
      timeout: transitionDuration,
      ...other,
      ...transitionProps,
      children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(RootSlot, {
        "aria-hidden": true,
        ...rootProps,
        ref,
        children,
      }),
    });
  },
);
false
  ? (Backdrop.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * The components used for each slot inside.
       *
       * @deprecated Use the `slots` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       *
       * @default {}
       */
      components: import_prop_types.default.shape({
        Root: import_prop_types.default.elementType,
      }),
      /**
       * The extra props for the slot components.
       * You can override the existing props or add new ones.
       *
       * @deprecated Use the `slotProps` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       *
       * @default {}
       */
      componentsProps: import_prop_types.default.shape({
        root: import_prop_types.default.object,
      }),
      /**
       * If `true`, the backdrop is invisible.
       * It can be used when rendering a popover or a custom select component.
       * @default false
       */
      invisible: import_prop_types.default.bool,
      /**
       * If `true`, the component is shown.
       */
      open: import_prop_types.default.bool.isRequired,
      /**
       * The props used for each slot inside.
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        root: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        transition: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      }),
      /**
       * The components used for each slot inside.
       * @default {}
       */
      slots: import_prop_types.default.shape({
        root: import_prop_types.default.elementType,
        transition: import_prop_types.default.elementType,
      }),
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * The component used for the transition.
       * [Follow this guide](https://mui.com/material-ui/transitions/#transitioncomponent-prop) to learn more about the requirements for this component.
       * @default Fade
       * @deprecated Use `slots.transition` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       */
      TransitionComponent: import_prop_types.default.elementType,
      /**
       * The duration for the transition, in milliseconds.
       * You may specify a single timeout for all transitions, or individually with an object.
       */
      transitionDuration: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.shape({
          appear: import_prop_types.default.number,
          enter: import_prop_types.default.number,
          exit: import_prop_types.default.number,
        }),
      ]),
    })
  : void 0;
var Backdrop_default = Backdrop;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Box/boxClasses.js
var boxClasses = generateUtilityClasses("MuiBox", ["root"]);
var boxClasses_default = boxClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Box/Box.js
var defaultTheme4 = createTheme2();
var Box = createBox({
  themeId: identifier_default,
  defaultTheme: defaultTheme4,
  defaultClassName: boxClasses_default.root,
  generateClassName: ClassNameGenerator_default.generate,
});
false
  ? (Box.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * @ignore
       */
      children: import_prop_types.default.node,
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
    })
  : void 0;
var Box_default = Box;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Button/Button.js
var React59 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Button/buttonClasses.js
function getButtonUtilityClass(slot) {
  return generateUtilityClass("MuiButton", slot);
}
var buttonClasses = generateUtilityClasses("MuiButton", [
  "root",
  "text",
  "textInherit",
  "textPrimary",
  "textSecondary",
  "textSuccess",
  "textError",
  "textInfo",
  "textWarning",
  "outlined",
  "outlinedInherit",
  "outlinedPrimary",
  "outlinedSecondary",
  "outlinedSuccess",
  "outlinedError",
  "outlinedInfo",
  "outlinedWarning",
  "contained",
  "containedInherit",
  "containedPrimary",
  "containedSecondary",
  "containedSuccess",
  "containedError",
  "containedInfo",
  "containedWarning",
  "disableElevation",
  "focusVisible",
  "disabled",
  "colorInherit",
  "colorPrimary",
  "colorSecondary",
  "colorSuccess",
  "colorError",
  "colorInfo",
  "colorWarning",
  "textSizeSmall",
  "textSizeMedium",
  "textSizeLarge",
  "outlinedSizeSmall",
  "outlinedSizeMedium",
  "outlinedSizeLarge",
  "containedSizeSmall",
  "containedSizeMedium",
  "containedSizeLarge",
  "sizeMedium",
  "sizeSmall",
  "sizeLarge",
  "fullWidth",
  "startIcon",
  "endIcon",
  "icon",
  "iconSizeSmall",
  "iconSizeMedium",
  "iconSizeLarge",
  "loading",
  "loadingWrapper",
  "loadingIconPlaceholder",
  "loadingIndicator",
  "loadingPositionCenter",
  "loadingPositionStart",
  "loadingPositionEnd",
]);
var buttonClasses_default = buttonClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/ButtonGroup/ButtonGroupContext.js
var React57 = __toESM(require_react(), 1);
var ButtonGroupContext = /* @__PURE__ */ React57.createContext({});
if (false) {
  ButtonGroupContext.displayName = "ButtonGroupContext";
}
var ButtonGroupContext_default = ButtonGroupContext;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/ButtonGroup/ButtonGroupButtonContext.js
var React58 = __toESM(require_react(), 1);
var ButtonGroupButtonContext = /* @__PURE__ */ React58.createContext(void 0);
if (false) {
  ButtonGroupButtonContext.displayName = "ButtonGroupButtonContext";
}
var ButtonGroupButtonContext_default = ButtonGroupButtonContext;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Button/Button.js
var import_jsx_runtime40 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses14 = (ownerState) => {
  const {
    color: color2,
    disableElevation,
    fullWidth,
    size,
    variant,
    loading,
    loadingPosition,
    classes,
  } = ownerState;
  const slots = {
    root: [
      "root",
      loading && "loading",
      variant,
      `${variant}${capitalize_default(color2)}`,
      `size${capitalize_default(size)}`,
      `${variant}Size${capitalize_default(size)}`,
      `color${capitalize_default(color2)}`,
      disableElevation && "disableElevation",
      fullWidth && "fullWidth",
      loading && `loadingPosition${capitalize_default(loadingPosition)}`,
    ],
    startIcon: ["icon", "startIcon", `iconSize${capitalize_default(size)}`],
    endIcon: ["icon", "endIcon", `iconSize${capitalize_default(size)}`],
    loadingIndicator: ["loadingIndicator"],
    loadingWrapper: ["loadingWrapper"],
  };
  const composedClasses = composeClasses(slots, getButtonUtilityClass, classes);
  return {
    ...classes,
    // forward the focused, disabled, etc. classes to the ButtonBase
    ...composedClasses,
  };
};
var commonIconStyles = [
  {
    props: {
      size: "small",
    },
    style: {
      "& > *:nth-of-type(1)": {
        fontSize: 18,
      },
    },
  },
  {
    props: {
      size: "medium",
    },
    style: {
      "& > *:nth-of-type(1)": {
        fontSize: 20,
      },
    },
  },
  {
    props: {
      size: "large",
    },
    style: {
      "& > *:nth-of-type(1)": {
        fontSize: 22,
      },
    },
  },
];
var ButtonRoot = styled_default2(ButtonBase_default, {
  shouldForwardProp: (prop) =>
    rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiButton",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      styles5[ownerState.variant],
      styles5[`${ownerState.variant}${capitalize_default(ownerState.color)}`],
      styles5[`size${capitalize_default(ownerState.size)}`],
      styles5[
        `${ownerState.variant}Size${capitalize_default(ownerState.size)}`
      ],
      ownerState.color === "inherit" && styles5.colorInherit,
      ownerState.disableElevation && styles5.disableElevation,
      ownerState.fullWidth && styles5.fullWidth,
      ownerState.loading && styles5.loading,
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => {
    const inheritContainedBackgroundColor =
      theme2.palette.mode === "light"
        ? theme2.palette.grey[300]
        : theme2.palette.grey[800];
    const inheritContainedHoverBackgroundColor =
      theme2.palette.mode === "light"
        ? theme2.palette.grey.A100
        : theme2.palette.grey[700];
    return {
      ...theme2.typography.button,
      minWidth: 64,
      padding: "6px 16px",
      border: 0,
      borderRadius: (theme2.vars || theme2).shape.borderRadius,
      transition: theme2.transitions.create(
        ["background-color", "box-shadow", "border-color", "color"],
        {
          duration: theme2.transitions.duration.short,
        },
      ),
      "&:hover": {
        textDecoration: "none",
      },
      [`&.${buttonClasses_default.disabled}`]: {
        color: (theme2.vars || theme2).palette.action.disabled,
      },
      variants: [
        {
          props: ({ ownerState }) =>
            ownerState.startIcon ||
            (ownerState.loading && ownerState.loadingPosition === "start"),
          style: {
            "&::before": {
              content: '"\\200b"',
              width: 0,
              overflow: "hidden",
            },
          },
        },
        {
          props: {
            variant: "contained",
          },
          style: {
            color: `var(--variant-containedColor)`,
            backgroundColor: `var(--variant-containedBg)`,
            boxShadow: (theme2.vars || theme2).shadows[2],
            "&:hover": {
              boxShadow: (theme2.vars || theme2).shadows[4],
              // Reset on touch devices, it doesn't add specificity
              "@media (hover: none)": {
                boxShadow: (theme2.vars || theme2).shadows[2],
              },
            },
            "&:active": {
              boxShadow: (theme2.vars || theme2).shadows[8],
            },
            [`&.${buttonClasses_default.focusVisible}`]: {
              boxShadow: (theme2.vars || theme2).shadows[6],
            },
            [`&.${buttonClasses_default.disabled}`]: {
              color: (theme2.vars || theme2).palette.action.disabled,
              boxShadow: (theme2.vars || theme2).shadows[0],
              backgroundColor: (theme2.vars || theme2).palette.action
                .disabledBackground,
            },
          },
        },
        {
          props: {
            variant: "outlined",
          },
          style: {
            padding: "5px 15px",
            border: "1px solid currentColor",
            borderColor: `var(--variant-outlinedBorder, currentColor)`,
            backgroundColor: `var(--variant-outlinedBg)`,
            color: `var(--variant-outlinedColor)`,
            [`&.${buttonClasses_default.disabled}`]: {
              border: `1px solid ${(theme2.vars || theme2).palette.action.disabledBackground}`,
            },
          },
        },
        {
          props: {
            variant: "text",
          },
          style: {
            padding: "6px 8px",
            color: `var(--variant-textColor)`,
            backgroundColor: `var(--variant-textBg)`,
          },
        },
        ...Object.entries(theme2.palette)
          .filter(createSimplePaletteValueFilter())
          .map(([color2]) => ({
            props: {
              color: color2,
            },
            style: {
              "--variant-textColor": (theme2.vars || theme2).palette[color2]
                .main,
              "--variant-outlinedColor": (theme2.vars || theme2).palette[color2]
                .main,
              "--variant-outlinedBorder": theme2.alpha(
                (theme2.vars || theme2).palette[color2].main,
                0.5,
              ),
              "--variant-containedColor": (theme2.vars || theme2).palette[
                color2
              ].contrastText,
              "--variant-containedBg": (theme2.vars || theme2).palette[color2]
                .main,
              "@media (hover: hover)": {
                "&:hover": {
                  "--variant-containedBg": (theme2.vars || theme2).palette[
                    color2
                  ].dark,
                  "--variant-textBg": theme2.alpha(
                    (theme2.vars || theme2).palette[color2].main,
                    (theme2.vars || theme2).palette.action.hoverOpacity,
                  ),
                  "--variant-outlinedBorder": (theme2.vars || theme2).palette[
                    color2
                  ].main,
                  "--variant-outlinedBg": theme2.alpha(
                    (theme2.vars || theme2).palette[color2].main,
                    (theme2.vars || theme2).palette.action.hoverOpacity,
                  ),
                },
              },
            },
          })),
        {
          props: {
            color: "inherit",
          },
          style: {
            color: "inherit",
            borderColor: "currentColor",
            "--variant-containedBg": theme2.vars
              ? theme2.vars.palette.Button.inheritContainedBg
              : inheritContainedBackgroundColor,
            "@media (hover: hover)": {
              "&:hover": {
                "--variant-containedBg": theme2.vars
                  ? theme2.vars.palette.Button.inheritContainedHoverBg
                  : inheritContainedHoverBackgroundColor,
                "--variant-textBg": theme2.alpha(
                  (theme2.vars || theme2).palette.text.primary,
                  (theme2.vars || theme2).palette.action.hoverOpacity,
                ),
                "--variant-outlinedBg": theme2.alpha(
                  (theme2.vars || theme2).palette.text.primary,
                  (theme2.vars || theme2).palette.action.hoverOpacity,
                ),
              },
            },
          },
        },
        {
          props: {
            size: "small",
            variant: "text",
          },
          style: {
            padding: "4px 5px",
            fontSize: theme2.typography.pxToRem(13),
          },
        },
        {
          props: {
            size: "large",
            variant: "text",
          },
          style: {
            padding: "8px 11px",
            fontSize: theme2.typography.pxToRem(15),
          },
        },
        {
          props: {
            size: "small",
            variant: "outlined",
          },
          style: {
            padding: "3px 9px",
            fontSize: theme2.typography.pxToRem(13),
          },
        },
        {
          props: {
            size: "large",
            variant: "outlined",
          },
          style: {
            padding: "7px 21px",
            fontSize: theme2.typography.pxToRem(15),
          },
        },
        {
          props: {
            size: "small",
            variant: "contained",
          },
          style: {
            padding: "4px 10px",
            fontSize: theme2.typography.pxToRem(13),
          },
        },
        {
          props: {
            size: "large",
            variant: "contained",
          },
          style: {
            padding: "8px 22px",
            fontSize: theme2.typography.pxToRem(15),
          },
        },
        {
          props: {
            disableElevation: true,
          },
          style: {
            boxShadow: "none",
            "&:hover": {
              boxShadow: "none",
            },
            [`&.${buttonClasses_default.focusVisible}`]: {
              boxShadow: "none",
            },
            "&:active": {
              boxShadow: "none",
            },
            [`&.${buttonClasses_default.disabled}`]: {
              boxShadow: "none",
            },
          },
        },
        {
          props: {
            fullWidth: true,
          },
          style: {
            width: "100%",
          },
        },
        {
          props: {
            loadingPosition: "center",
          },
          style: {
            transition: theme2.transitions.create(
              ["background-color", "box-shadow", "border-color"],
              {
                duration: theme2.transitions.duration.short,
              },
            ),
            [`&.${buttonClasses_default.loading}`]: {
              color: "transparent",
            },
          },
        },
      ],
    };
  }),
);
var ButtonStartIcon = styled_default2("span", {
  name: "MuiButton",
  slot: "StartIcon",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.startIcon,
      ownerState.loading && styles5.startIconLoadingStart,
      styles5[`iconSize${capitalize_default(ownerState.size)}`],
    ];
  },
})(({ theme: theme2 }) => ({
  display: "inherit",
  marginRight: 8,
  marginLeft: -4,
  variants: [
    {
      props: {
        size: "small",
      },
      style: {
        marginLeft: -2,
      },
    },
    {
      props: {
        loadingPosition: "start",
        loading: true,
      },
      style: {
        transition: theme2.transitions.create(["opacity"], {
          duration: theme2.transitions.duration.short,
        }),
        opacity: 0,
      },
    },
    {
      props: {
        loadingPosition: "start",
        loading: true,
        fullWidth: true,
      },
      style: {
        marginRight: -8,
      },
    },
    ...commonIconStyles,
  ],
}));
var ButtonEndIcon = styled_default2("span", {
  name: "MuiButton",
  slot: "EndIcon",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.endIcon,
      ownerState.loading && styles5.endIconLoadingEnd,
      styles5[`iconSize${capitalize_default(ownerState.size)}`],
    ];
  },
})(({ theme: theme2 }) => ({
  display: "inherit",
  marginRight: -4,
  marginLeft: 8,
  variants: [
    {
      props: {
        size: "small",
      },
      style: {
        marginRight: -2,
      },
    },
    {
      props: {
        loadingPosition: "end",
        loading: true,
      },
      style: {
        transition: theme2.transitions.create(["opacity"], {
          duration: theme2.transitions.duration.short,
        }),
        opacity: 0,
      },
    },
    {
      props: {
        loadingPosition: "end",
        loading: true,
        fullWidth: true,
      },
      style: {
        marginLeft: -8,
      },
    },
    ...commonIconStyles,
  ],
}));
var ButtonLoadingIndicator = styled_default2("span", {
  name: "MuiButton",
  slot: "LoadingIndicator",
})(({ theme: theme2 }) => ({
  display: "none",
  position: "absolute",
  visibility: "visible",
  variants: [
    {
      props: {
        loading: true,
      },
      style: {
        display: "flex",
      },
    },
    {
      props: {
        loadingPosition: "start",
      },
      style: {
        left: 14,
      },
    },
    {
      props: {
        loadingPosition: "start",
        size: "small",
      },
      style: {
        left: 10,
      },
    },
    {
      props: {
        variant: "text",
        loadingPosition: "start",
      },
      style: {
        left: 6,
      },
    },
    {
      props: {
        loadingPosition: "center",
      },
      style: {
        left: "50%",
        transform: "translate(-50%)",
        color: (theme2.vars || theme2).palette.action.disabled,
      },
    },
    {
      props: {
        loadingPosition: "end",
      },
      style: {
        right: 14,
      },
    },
    {
      props: {
        loadingPosition: "end",
        size: "small",
      },
      style: {
        right: 10,
      },
    },
    {
      props: {
        variant: "text",
        loadingPosition: "end",
      },
      style: {
        right: 6,
      },
    },
    {
      props: {
        loadingPosition: "start",
        fullWidth: true,
      },
      style: {
        position: "relative",
        left: -10,
      },
    },
    {
      props: {
        loadingPosition: "end",
        fullWidth: true,
      },
      style: {
        position: "relative",
        right: -10,
      },
    },
  ],
}));
var ButtonLoadingIconPlaceholder = styled_default2("span", {
  name: "MuiButton",
  slot: "LoadingIconPlaceholder",
})({
  display: "inline-block",
  width: "1em",
  height: "1em",
});
var Button = /* @__PURE__ */ React59.forwardRef(function Button2(inProps, ref) {
  const contextProps = React59.useContext(ButtonGroupContext_default);
  const buttonGroupButtonContextPositionClassName = React59.useContext(
    ButtonGroupButtonContext_default,
  );
  const resolvedProps = resolveProps(contextProps, inProps);
  const props = useDefaultProps2({
    props: resolvedProps,
    name: "MuiButton",
  });
  const {
    children,
    color: color2 = "primary",
    component = "button",
    className,
    disabled = false,
    disableElevation = false,
    disableFocusRipple = false,
    endIcon: endIconProp,
    focusVisibleClassName,
    fullWidth = false,
    id: idProp,
    loading = null,
    loadingIndicator: loadingIndicatorProp,
    loadingPosition = "center",
    size = "medium",
    startIcon: startIconProp,
    type,
    variant = "text",
    ...other
  } = props;
  const loadingId = useId_default(idProp);
  const loadingIndicator2 =
    loadingIndicatorProp ??
    /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(CircularProgress_default, {
      "aria-labelledby": loadingId,
      color: "inherit",
      size: 16,
    });
  const ownerState = {
    ...props,
    color: color2,
    component,
    disabled,
    disableElevation,
    disableFocusRipple,
    fullWidth,
    loading,
    loadingIndicator: loadingIndicator2,
    loadingPosition,
    size,
    type,
    variant,
  };
  const classes = useUtilityClasses14(ownerState);
  const startIcon =
    (startIconProp || (loading && loadingPosition === "start")) &&
    /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(ButtonStartIcon, {
      className: classes.startIcon,
      ownerState,
      children:
        startIconProp ||
        /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(
          ButtonLoadingIconPlaceholder,
          {
            className: classes.loadingIconPlaceholder,
            ownerState,
          },
        ),
    });
  const endIcon =
    (endIconProp || (loading && loadingPosition === "end")) &&
    /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(ButtonEndIcon, {
      className: classes.endIcon,
      ownerState,
      children:
        endIconProp ||
        /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(
          ButtonLoadingIconPlaceholder,
          {
            className: classes.loadingIconPlaceholder,
            ownerState,
          },
        ),
    });
  const positionClassName = buttonGroupButtonContextPositionClassName || "";
  const loader =
    typeof loading === "boolean"
      ? // use plain HTML span to minimize the runtime overhead
        /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("span", {
          className: classes.loadingWrapper,
          style: {
            display: "contents",
          },
          children:
            loading &&
            /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(
              ButtonLoadingIndicator,
              {
                className: classes.loadingIndicator,
                ownerState,
                children: loadingIndicator2,
              },
            ),
        })
      : null;
  const { root, ...forwardedClasses } = classes;
  return /* @__PURE__ */ (0, import_jsx_runtime40.jsxs)(ButtonRoot, {
    ownerState,
    className: clsx_default(
      contextProps.className,
      classes.root,
      className,
      positionClassName,
    ),
    component,
    disabled: disabled || loading,
    focusRipple: !disableFocusRipple,
    focusVisibleClassName: clsx_default(
      classes.focusVisible,
      focusVisibleClassName,
    ),
    ref,
    type,
    id: loading ? loadingId : idProp,
    ...other,
    classes: forwardedClasses,
    children: [
      startIcon,
      loadingPosition !== "end" && loader,
      children,
      loadingPosition === "end" && loader,
      endIcon,
    ],
  });
});
false
  ? (Button.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The color of the component.
       * It supports both default and custom theme colors, which can be added as shown in the
       * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
       * @default 'primary'
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "inherit",
          "primary",
          "secondary",
          "success",
          "error",
          "info",
          "warning",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * If `true`, the component is disabled.
       * @default false
       */
      disabled: import_prop_types.default.bool,
      /**
       * If `true`, no elevation is used.
       * @default false
       */
      disableElevation: import_prop_types.default.bool,
      /**
       * If `true`, the  keyboard focus ripple is disabled.
       * @default false
       */
      disableFocusRipple: import_prop_types.default.bool,
      /**
       * If `true`, the ripple effect is disabled.
       *
       * ⚠️ Without a ripple there is no styling for :focus-visible by default. Be sure
       * to highlight the element by applying separate styles with the `.Mui-focusVisible` class.
       * @default false
       */
      disableRipple: import_prop_types.default.bool,
      /**
       * Element placed after the children.
       */
      endIcon: import_prop_types.default.node,
      /**
       * @ignore
       */
      focusVisibleClassName: import_prop_types.default.string,
      /**
       * If `true`, the button will take up the full width of its container.
       * @default false
       */
      fullWidth: import_prop_types.default.bool,
      /**
       * The URL to link to when the button is clicked.
       * If defined, an `a` element will be used as the root node.
       */
      href: import_prop_types.default.string,
      /**
       * @ignore
       */
      id: import_prop_types.default.string,
      /**
       * If `true`, the loading indicator is visible and the button is disabled.
       * If `true | false`, the loading wrapper is always rendered before the children to prevent [Google Translation Crash](https://github.com/mui/material-ui/issues/27853).
       * @default null
       */
      loading: import_prop_types.default.bool,
      /**
       * Element placed before the children if the button is in loading state.
       * The node should contain an element with `role="progressbar"` with an accessible name.
       * By default, it renders a `CircularProgress` that is labeled by the button itself.
       * @default <CircularProgress color="inherit" size={16} />
       */
      loadingIndicator: import_prop_types.default.node,
      /**
       * The loading indicator can be positioned on the start, end, or the center of the button.
       * @default 'center'
       */
      loadingPosition: import_prop_types.default.oneOf([
        "center",
        "end",
        "start",
      ]),
      /**
       * The size of the component.
       * `small` is equivalent to the dense button styling.
       * @default 'medium'
       */
      size: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["small", "medium", "large"]),
        import_prop_types.default.string,
      ]),
      /**
       * Element placed before the children.
       */
      startIcon: import_prop_types.default.node,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * @ignore
       */
      type: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["button", "reset", "submit"]),
        import_prop_types.default.string,
      ]),
      /**
       * The variant to use.
       * @default 'text'
       */
      variant: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["contained", "outlined", "text"]),
        import_prop_types.default.string,
      ]),
    })
  : void 0;
var Button_default = Button;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Container/Container.js
var Container = createContainer({
  createStyledComponent: styled_default2("div", {
    name: "MuiContainer",
    slot: "Root",
    overridesResolver: (props, styles5) => {
      const { ownerState } = props;
      return [
        styles5.root,
        styles5[`maxWidth${capitalize_default(String(ownerState.maxWidth))}`],
        ownerState.fixed && styles5.fixed,
        ownerState.disableGutters && styles5.disableGutters,
      ];
    },
  }),
  useThemeProps: (inProps) =>
    useDefaultProps2({
      props: inProps,
      name: "MuiContainer",
    }),
});
false
  ? (Container.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * @ignore
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * If `true`, the left and right padding is removed.
       * @default false
       */
      disableGutters: import_prop_types.default.bool,
      /**
       * Set the max-width to match the min-width of the current breakpoint.
       * This is useful if you'd prefer to design for a fixed set of sizes
       * instead of trying to accommodate a fully fluid viewport.
       * It's fluid by default.
       * @default false
       */
      fixed: import_prop_types.default.bool,
      /**
       * Determine the max-width of the container.
       * The container width grows with the size of the screen.
       * Set to `false` to disable `maxWidth`.
       * @default 'lg'
       */
      maxWidth: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["xs", "sm", "md", "lg", "xl", false]),
        import_prop_types.default.string,
      ]),
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
    })
  : void 0;
var Container_default = Container;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/CssBaseline/CssBaseline.js
var React60 = __toESM(require_react(), 1);
var import_jsx_runtime41 = __toESM(require_jsx_runtime(), 1);
var isDynamicSupport = typeof globalCss({}) === "function";
var html = (theme2, enableColorScheme) => ({
  WebkitFontSmoothing: "antialiased",
  // Antialiasing.
  MozOsxFontSmoothing: "grayscale",
  // Antialiasing.
  // Change from `box-sizing: content-box` so that `width`
  // is not affected by `padding` or `border`.
  boxSizing: "border-box",
  // Fix font resize problem in iOS
  WebkitTextSizeAdjust: "100%",
  // When used under CssVarsProvider, colorScheme should not be applied dynamically because it will generate the stylesheet twice for server-rendered applications.
  ...(enableColorScheme &&
    !theme2.vars && {
      colorScheme: theme2.palette.mode,
    }),
});
var body = (theme2) => ({
  color: (theme2.vars || theme2).palette.text.primary,
  ...theme2.typography.body1,
  backgroundColor: (theme2.vars || theme2).palette.background.default,
  "@media print": {
    // Save printer ink.
    backgroundColor: (theme2.vars || theme2).palette.common.white,
  },
});
var styles3 = (theme2, enableColorScheme = false) => {
  const colorSchemeStyles = {};
  if (
    enableColorScheme &&
    theme2.colorSchemes &&
    typeof theme2.getColorSchemeSelector === "function"
  ) {
    Object.entries(theme2.colorSchemes).forEach(([key, scheme]) => {
      const selector = theme2.getColorSchemeSelector(key);
      if (selector.startsWith("@")) {
        colorSchemeStyles[selector] = {
          ":root": {
            colorScheme: scheme.palette?.mode,
          },
        };
      } else {
        colorSchemeStyles[selector.replace(/\s*&/, "")] = {
          colorScheme: scheme.palette?.mode,
        };
      }
    });
  }
  let defaultStyles = {
    html: html(theme2, enableColorScheme),
    "*, *::before, *::after": {
      boxSizing: "inherit",
    },
    "strong, b": {
      fontWeight: theme2.typography.fontWeightBold,
    },
    body: {
      margin: 0,
      // Remove the margin in all browsers.
      ...body(theme2),
      // Add support for document.body.requestFullScreen().
      // Other elements, if background transparent, are not supported.
      "&::backdrop": {
        backgroundColor: (theme2.vars || theme2).palette.background.default,
      },
    },
    ...colorSchemeStyles,
  };
  const themeOverrides = theme2.components?.MuiCssBaseline?.styleOverrides;
  if (themeOverrides) {
    defaultStyles = [defaultStyles, themeOverrides];
  }
  return defaultStyles;
};
var SELECTOR = "mui-ecs";
var staticStyles = (theme2) => {
  const result = styles3(theme2, false);
  const baseStyles = Array.isArray(result) ? result[0] : result;
  if (!theme2.vars && baseStyles) {
    baseStyles.html[`:root:has(${SELECTOR})`] = {
      colorScheme: theme2.palette.mode,
    };
  }
  if (theme2.colorSchemes) {
    Object.entries(theme2.colorSchemes).forEach(([key, scheme]) => {
      const selector = theme2.getColorSchemeSelector(key);
      if (selector.startsWith("@")) {
        baseStyles[selector] = {
          [`:root:not(:has(.${SELECTOR}))`]: {
            colorScheme: scheme.palette?.mode,
          },
        };
      } else {
        baseStyles[selector.replace(/\s*&/, "")] = {
          [`&:not(:has(.${SELECTOR}))`]: {
            colorScheme: scheme.palette?.mode,
          },
        };
      }
    });
  }
  return result;
};
var GlobalStyles4 = globalCss(
  isDynamicSupport
    ? ({ theme: theme2, enableColorScheme }) =>
        styles3(theme2, enableColorScheme)
    : ({ theme: theme2 }) => staticStyles(theme2),
);
function CssBaseline(inProps) {
  const props = useDefaultProps2({
    props: inProps,
    name: "MuiCssBaseline",
  });
  const { children, enableColorScheme = false } = props;
  return /* @__PURE__ */ (0, import_jsx_runtime41.jsxs)(React60.Fragment, {
    children: [
      isDynamicSupport &&
        /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(GlobalStyles4, {
          enableColorScheme,
        }),
      !isDynamicSupport &&
        !enableColorScheme &&
        /* @__PURE__ */ (0, import_jsx_runtime41.jsx)("span", {
          className: SELECTOR,
          style: {
            display: "none",
          },
        }),
      children,
    ],
  });
}
false
  ? (CssBaseline.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * You can wrap a node.
       */
      children: import_prop_types.default.node,
      /**
       * Enable `color-scheme` CSS property to use `theme.palette.mode`.
       * For more details, check out https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/color-scheme
       * For browser support, check out https://caniuse.com/?search=color-scheme
       * @default false
       */
      enableColorScheme: import_prop_types.default.bool,
    })
  : void 0;
var CssBaseline_default = CssBaseline;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Dialog/Dialog.js
var React65 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+utils@7.3.11_@types+react@19.2.18_react@19.2.8/node_modules/@mui/utils/esm/getScrollbarSize/getScrollbarSize.js
function getScrollbarSize(win = window) {
  const documentWidth = win.document.documentElement.clientWidth;
  return win.innerWidth - documentWidth;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Modal/ModalManager.js
function isOverflowing(container) {
  const doc = ownerDocument(container);
  if (doc.body === container) {
    return ownerWindow(container).innerWidth > doc.documentElement.clientWidth;
  }
  return container.scrollHeight > container.clientHeight;
}
function ariaHidden(element, hide2) {
  if (hide2) {
    element.setAttribute("aria-hidden", "true");
  } else {
    element.removeAttribute("aria-hidden");
  }
}
function getPaddingRight(element) {
  return (
    parseFloat(ownerWindow(element).getComputedStyle(element).paddingRight) || 0
  );
}
function isAriaHiddenForbiddenOnElement(element) {
  const forbiddenTagNames = [
    "TEMPLATE",
    "SCRIPT",
    "STYLE",
    "LINK",
    "MAP",
    "META",
    "NOSCRIPT",
    "PICTURE",
    "COL",
    "COLGROUP",
    "PARAM",
    "SLOT",
    "SOURCE",
    "TRACK",
  ];
  const isForbiddenTagName = forbiddenTagNames.includes(element.tagName);
  const isInputHidden =
    element.tagName === "INPUT" && element.getAttribute("type") === "hidden";
  return isForbiddenTagName || isInputHidden;
}
function ariaHiddenSiblings(
  container,
  mountElement,
  currentElement,
  elementsToExclude,
  hide2,
) {
  const blacklist = [mountElement, currentElement, ...elementsToExclude];
  [].forEach.call(container.children, (element) => {
    const isNotExcludedElement = !blacklist.includes(element);
    const isNotForbiddenElement = !isAriaHiddenForbiddenOnElement(element);
    if (isNotExcludedElement && isNotForbiddenElement) {
      ariaHidden(element, hide2);
    }
  });
}
function findIndexOf(items, callback) {
  let idx = -1;
  items.some((item, index) => {
    if (callback(item)) {
      idx = index;
      return true;
    }
    return false;
  });
  return idx;
}
function handleContainer(containerInfo, props) {
  const restoreStyle = [];
  const container = containerInfo.container;
  if (!props.disableScrollLock) {
    if (isOverflowing(container)) {
      const scrollbarSize = getScrollbarSize(ownerWindow(container));
      restoreStyle.push({
        value: container.style.paddingRight,
        property: "padding-right",
        el: container,
      });
      container.style.paddingRight = `${getPaddingRight(container) + scrollbarSize}px`;
      const fixedElements2 =
        ownerDocument(container).querySelectorAll(".mui-fixed");
      [].forEach.call(fixedElements2, (element) => {
        restoreStyle.push({
          value: element.style.paddingRight,
          property: "padding-right",
          el: element,
        });
        element.style.paddingRight = `${getPaddingRight(element) + scrollbarSize}px`;
      });
    }
    let scrollContainer;
    if (container.parentNode instanceof DocumentFragment) {
      scrollContainer = ownerDocument(container).body;
    } else {
      const parent = container.parentElement;
      const containerWindow = ownerWindow(container);
      scrollContainer =
        parent?.nodeName === "HTML" &&
        containerWindow.getComputedStyle(parent).overflowY === "scroll"
          ? parent
          : container;
    }
    restoreStyle.push(
      {
        value: scrollContainer.style.overflow,
        property: "overflow",
        el: scrollContainer,
      },
      {
        value: scrollContainer.style.overflowX,
        property: "overflow-x",
        el: scrollContainer,
      },
      {
        value: scrollContainer.style.overflowY,
        property: "overflow-y",
        el: scrollContainer,
      },
    );
    scrollContainer.style.overflow = "hidden";
  }
  const restore = () => {
    restoreStyle.forEach(({ value, el, property }) => {
      if (value) {
        el.style.setProperty(property, value);
      } else {
        el.style.removeProperty(property);
      }
    });
  };
  return restore;
}
function getHiddenSiblings(container) {
  const hiddenSiblings = [];
  [].forEach.call(container.children, (element) => {
    if (element.getAttribute("aria-hidden") === "true") {
      hiddenSiblings.push(element);
    }
  });
  return hiddenSiblings;
}
var ModalManager = class {
  constructor() {
    this.modals = [];
    this.containers = [];
  }
  add(modal, container) {
    let modalIndex = this.modals.indexOf(modal);
    if (modalIndex !== -1) {
      return modalIndex;
    }
    modalIndex = this.modals.length;
    this.modals.push(modal);
    if (modal.modalRef) {
      ariaHidden(modal.modalRef, false);
    }
    const hiddenSiblings = getHiddenSiblings(container);
    ariaHiddenSiblings(
      container,
      modal.mount,
      modal.modalRef,
      hiddenSiblings,
      true,
    );
    const containerIndex = findIndexOf(
      this.containers,
      (item) => item.container === container,
    );
    if (containerIndex !== -1) {
      this.containers[containerIndex].modals.push(modal);
      return modalIndex;
    }
    this.containers.push({
      modals: [modal],
      container,
      restore: null,
      hiddenSiblings,
    });
    return modalIndex;
  }
  mount(modal, props) {
    const containerIndex = findIndexOf(this.containers, (item) =>
      item.modals.includes(modal),
    );
    const containerInfo = this.containers[containerIndex];
    if (!containerInfo.restore) {
      containerInfo.restore = handleContainer(containerInfo, props);
    }
  }
  remove(modal, ariaHiddenState = true) {
    const modalIndex = this.modals.indexOf(modal);
    if (modalIndex === -1) {
      return modalIndex;
    }
    const containerIndex = findIndexOf(this.containers, (item) =>
      item.modals.includes(modal),
    );
    const containerInfo = this.containers[containerIndex];
    containerInfo.modals.splice(containerInfo.modals.indexOf(modal), 1);
    this.modals.splice(modalIndex, 1);
    if (containerInfo.modals.length === 0) {
      if (containerInfo.restore) {
        containerInfo.restore();
      }
      if (modal.modalRef) {
        ariaHidden(modal.modalRef, ariaHiddenState);
      }
      ariaHiddenSiblings(
        containerInfo.container,
        modal.mount,
        modal.modalRef,
        containerInfo.hiddenSiblings,
        false,
      );
      this.containers.splice(containerIndex, 1);
    } else {
      const nextTop = containerInfo.modals[containerInfo.modals.length - 1];
      if (nextTop.modalRef) {
        ariaHidden(nextTop.modalRef, false);
      }
    }
    return modalIndex;
  }
  isTopModal(modal) {
    return (
      this.modals.length > 0 && this.modals[this.modals.length - 1] === modal
    );
  }
};

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Modal/Modal.js
var React63 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Unstable_TrapFocus/FocusTrap.js
var React61 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/contains.js
var contains_default = contains;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/focusable.js
var FOCUSABLE_ATTRIBUTE = "data-mui-focusable";
function getFocusTarget(rootElement) {
  if (!rootElement) {
    return null;
  }
  return rootElement.hasAttribute(FOCUSABLE_ATTRIBUTE)
    ? rootElement
    : rootElement.querySelector(`[${FOCUSABLE_ATTRIBUTE}]`);
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Unstable_TrapFocus/FocusTrap.js
var import_jsx_runtime42 = __toESM(require_jsx_runtime(), 1);
var candidatesSelector = [
  "input",
  "select",
  "textarea",
  "a[href]",
  "button",
  "[tabindex]",
  "audio[controls]",
  "video[controls]",
  '[contenteditable]:not([contenteditable="false"])',
].join(",");
function getTabIndex(node2) {
  const tabindexAttr = parseInt(node2.getAttribute("tabindex") || "", 10);
  if (!Number.isNaN(tabindexAttr)) {
    return tabindexAttr;
  }
  if (
    node2.contentEditable === "true" ||
    ((node2.nodeName === "AUDIO" ||
      node2.nodeName === "VIDEO" ||
      node2.nodeName === "DETAILS") &&
      node2.getAttribute("tabindex") === null)
  ) {
    return 0;
  }
  return node2.tabIndex;
}
function isNonTabbableRadio(node2) {
  if (node2.tagName !== "INPUT" || node2.type !== "radio") {
    return false;
  }
  if (!node2.name) {
    return false;
  }
  const getRadio = (selector) =>
    node2.ownerDocument.querySelector(`input[type="radio"]${selector}`);
  let roving = getRadio(`[name="${node2.name}"]:checked`);
  if (!roving) {
    roving = getRadio(`[name="${node2.name}"]`);
  }
  return roving !== node2;
}
function isNodeMatchingSelectorFocusable(node2) {
  if (
    node2.disabled ||
    (node2.tagName === "INPUT" && node2.type === "hidden") ||
    isNonTabbableRadio(node2)
  ) {
    return false;
  }
  return true;
}
function defaultGetTabbable(root) {
  const regularTabNodes = [];
  const orderedTabNodes = [];
  Array.from(root.querySelectorAll(candidatesSelector)).forEach((node2, i) => {
    const nodeTabIndex = getTabIndex(node2);
    if (nodeTabIndex === -1 || !isNodeMatchingSelectorFocusable(node2)) {
      return;
    }
    if (nodeTabIndex === 0) {
      regularTabNodes.push(node2);
    } else {
      orderedTabNodes.push({
        documentOrder: i,
        tabIndex: nodeTabIndex,
        node: node2,
      });
    }
  });
  return orderedTabNodes
    .sort((a, b) =>
      a.tabIndex === b.tabIndex
        ? a.documentOrder - b.documentOrder
        : a.tabIndex - b.tabIndex,
    )
    .map((a) => a.node)
    .concat(regularTabNodes);
}
function defaultIsEnabled() {
  return true;
}
function FocusTrap(props) {
  const {
    children,
    disableAutoFocus = false,
    disableEnforceFocus = false,
    disableRestoreFocus = false,
    getTabbable = defaultGetTabbable,
    isEnabled = defaultIsEnabled,
    open,
  } = props;
  const ignoreNextEnforceFocus = React61.useRef(false);
  const sentinelStart = React61.useRef(null);
  const sentinelEnd = React61.useRef(null);
  const nodeToRestore = React61.useRef(null);
  const reactFocusEventTarget = React61.useRef(null);
  const activated = React61.useRef(false);
  const rootRef = React61.useRef(null);
  const handleRef = useForkRef(getReactElementRef(children), rootRef);
  const lastKeydown = React61.useRef(null);
  React61.useEffect(() => {
    if (!open || !rootRef.current) {
      return;
    }
    activated.current = !disableAutoFocus;
  }, [disableAutoFocus, open]);
  React61.useEffect(() => {
    ignoreNextEnforceFocus.current = false;
    if (!open || !rootRef.current) {
      return;
    }
    const doc = ownerDocument(rootRef.current);
    const activeElement2 = getActiveElement_default(doc);
    const focusTarget = getFocusTarget(rootRef.current) ?? rootRef.current;
    if (!contains_default(rootRef.current, activeElement2)) {
      if (!focusTarget.hasAttribute("tabIndex")) {
        if (false) {
          console.error(
            [
              "MUI: The modal content node does not accept focus.",
              'For the benefit of assistive technologies, the tabIndex of the node is being set to "-1".',
            ].join("\n"),
          );
        }
        focusTarget.setAttribute("tabIndex", "-1");
      }
      if (activated.current) {
        focusTarget.focus();
      }
    }
    return () => {
      if (!disableRestoreFocus) {
        if (nodeToRestore.current && nodeToRestore.current.focus) {
          ignoreNextEnforceFocus.current = true;
          nodeToRestore.current.focus();
        }
        nodeToRestore.current = null;
      }
    };
  }, [open]);
  React61.useEffect(() => {
    if (!open || !rootRef.current) {
      return;
    }
    const doc = ownerDocument(rootRef.current);
    const loopFocus = (nativeEvent) => {
      lastKeydown.current = nativeEvent;
      if (disableEnforceFocus || !isEnabled() || nativeEvent.key !== "Tab") {
        return;
      }
      const activeElement2 = getActiveElement_default(doc);
      if (activeElement2 === rootRef.current && nativeEvent.shiftKey) {
        ignoreNextEnforceFocus.current = true;
        if (sentinelEnd.current) {
          sentinelEnd.current.focus();
        }
      }
    };
    const contain = () => {
      const rootElement = rootRef.current;
      if (rootElement === null) {
        return;
      }
      const activeEl = getActiveElement_default(doc);
      if (!doc.hasFocus() || !isEnabled() || ignoreNextEnforceFocus.current) {
        ignoreNextEnforceFocus.current = false;
        return;
      }
      if (contains_default(rootElement, activeEl)) {
        return;
      }
      if (
        disableEnforceFocus &&
        activeEl !== sentinelStart.current &&
        activeEl !== sentinelEnd.current
      ) {
        return;
      }
      if (activeEl !== reactFocusEventTarget.current) {
        reactFocusEventTarget.current = null;
      } else if (reactFocusEventTarget.current !== null) {
        return;
      }
      if (!activated.current) {
        return;
      }
      let tabbable = [];
      if (
        activeEl === sentinelStart.current ||
        activeEl === sentinelEnd.current
      ) {
        tabbable = getTabbable(rootRef.current);
      }
      if (tabbable.length > 0) {
        const isShiftTab = Boolean(
          lastKeydown.current?.shiftKey && lastKeydown.current?.key === "Tab",
        );
        const focusNext = tabbable[0];
        const focusPrevious = tabbable[tabbable.length - 1];
        if (
          typeof focusNext !== "string" &&
          typeof focusPrevious !== "string"
        ) {
          if (isShiftTab) {
            focusPrevious.focus();
          } else {
            focusNext.focus();
          }
        }
      } else {
        rootElement.focus();
      }
    };
    doc.addEventListener("focusin", contain);
    doc.addEventListener("keydown", loopFocus, true);
    const interval = setInterval(() => {
      const activeEl = getActiveElement_default(doc);
      if (activeEl && activeEl.tagName === "BODY") {
        contain();
      }
    }, 50);
    return () => {
      clearInterval(interval);
      doc.removeEventListener("focusin", contain);
      doc.removeEventListener("keydown", loopFocus, true);
    };
  }, [
    disableAutoFocus,
    disableEnforceFocus,
    disableRestoreFocus,
    isEnabled,
    open,
    getTabbable,
  ]);
  const onFocus = (event) => {
    if (nodeToRestore.current === null) {
      nodeToRestore.current = event.relatedTarget;
    }
    activated.current = true;
    reactFocusEventTarget.current = event.target;
    const childrenPropsHandler = children.props.onFocus;
    if (childrenPropsHandler) {
      childrenPropsHandler(event);
    }
  };
  const handleFocusSentinel = (event) => {
    if (nodeToRestore.current === null) {
      nodeToRestore.current = event.relatedTarget;
    }
    activated.current = true;
  };
  return /* @__PURE__ */ (0, import_jsx_runtime42.jsxs)(React61.Fragment, {
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime42.jsx)("div", {
        tabIndex: open ? 0 : -1,
        onFocus: handleFocusSentinel,
        ref: sentinelStart,
        "data-testid": "sentinelStart",
      }),
      /* @__PURE__ */ React61.cloneElement(children, {
        ref: handleRef,
        onFocus,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime42.jsx)("div", {
        tabIndex: open ? 0 : -1,
        onFocus: handleFocusSentinel,
        ref: sentinelEnd,
        "data-testid": "sentinelEnd",
      }),
    ],
  });
}
false
  ? (FocusTrap.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * A single child content element.
       */
      children: elementAcceptingRef_default,
      /**
       * If `true`, the focus trap will not automatically shift focus to itself when it opens, and
       * replace it to the last focused element when it closes.
       * This also works correctly with any focus trap children that have the `disableAutoFocus` prop.
       *
       * Generally this should never be set to `true` as it makes the focus trap less
       * accessible to assistive technologies, like screen readers.
       * @default false
       */
      disableAutoFocus: import_prop_types.default.bool,
      /**
       * If `true`, the focus trap will not prevent focus from leaving the focus trap while open.
       *
       * Generally this should never be set to `true` as it makes the focus trap less
       * accessible to assistive technologies, like screen readers.
       * @default false
       */
      disableEnforceFocus: import_prop_types.default.bool,
      /**
       * If `true`, the focus trap will not restore focus to previously focused element once
       * focus trap is hidden or unmounted.
       * @default false
       */
      disableRestoreFocus: import_prop_types.default.bool,
      /**
       * Returns an array of ordered tabbable nodes (i.e. in tab order) within the root.
       * For instance, you can provide the "tabbable" npm dependency.
       * @param {HTMLElement} root
       */
      getTabbable: import_prop_types.default.func,
      /**
       * This prop extends the `open` prop.
       * It allows to toggle the open state without having to wait for a rerender when changing the `open` prop.
       * This prop should be memoized.
       * It can be used to support multiple focus trap mounted at the same time.
       * @default function defaultIsEnabled(): boolean {
       *   return true;
       * }
       */
      isEnabled: import_prop_types.default.func,
      /**
       * If `true`, focus is locked.
       */
      open: import_prop_types.default.bool.isRequired,
    })
  : void 0;
if (false) {
  FocusTrap["propTypes"] = exactProp(FocusTrap.propTypes);
}
var FocusTrap_default = FocusTrap;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Modal/useModal.js
var React62 = __toESM(require_react(), 1);
function getContainer2(container) {
  return typeof container === "function" ? container() : container;
}
function getHasTransition(children) {
  return children ? children.props.hasOwnProperty("in") : false;
}
var noop4 = () => {};
var manager = new ModalManager();
function useModal(parameters) {
  const {
    container,
    disableEscapeKeyDown = false,
    disableScrollLock = false,
    closeAfterTransition = false,
    onTransitionEnter,
    onTransitionExited,
    children,
    onClose,
    open,
    rootRef,
  } = parameters;
  const modal = React62.useRef({});
  const mountNodeRef = React62.useRef(null);
  const modalRef = React62.useRef(null);
  const handleRef = useForkRef(modalRef, rootRef);
  const [exited, setExited] = React62.useState(!open);
  const hasTransition = getHasTransition(children);
  let ariaHiddenProp = true;
  if (
    parameters["aria-hidden"] === "false" ||
    parameters["aria-hidden"] === false
  ) {
    ariaHiddenProp = false;
  }
  const getDoc = () => ownerDocument(mountNodeRef.current);
  const getModal = () => {
    modal.current.modalRef = modalRef.current;
    modal.current.mount = mountNodeRef.current;
    return modal.current;
  };
  const handleMounted = () => {
    manager.mount(getModal(), {
      disableScrollLock,
    });
    if (modalRef.current) {
      modalRef.current.scrollTop = 0;
    }
  };
  const handleOpen = useEventCallback_default(() => {
    const resolvedContainer = getContainer2(container) || getDoc().body;
    manager.add(getModal(), resolvedContainer);
    if (modalRef.current) {
      handleMounted();
    }
  });
  const isTopModal = () => manager.isTopModal(getModal());
  const handlePortalRef = useEventCallback_default((node2) => {
    mountNodeRef.current = node2;
    if (!node2) {
      return;
    }
    if (open && isTopModal()) {
      handleMounted();
    } else if (modalRef.current) {
      ariaHidden(modalRef.current, ariaHiddenProp);
    }
  });
  const handleClose = React62.useCallback(() => {
    manager.remove(getModal(), ariaHiddenProp);
  }, [ariaHiddenProp]);
  React62.useEffect(() => {
    return () => {
      handleClose();
    };
  }, [handleClose]);
  React62.useEffect(() => {
    if (open) {
      handleOpen();
    } else if (!hasTransition || !closeAfterTransition) {
      handleClose();
    }
  }, [open, handleClose, hasTransition, closeAfterTransition, handleOpen]);
  const createHandleKeyDown = (otherHandlers) => (event) => {
    otherHandlers.onKeyDown?.(event);
    if (
      event.key !== "Escape" ||
      event.which === 229 || // Wait until IME is settled.
      !isTopModal()
    ) {
      return;
    }
    if (!disableEscapeKeyDown) {
      event.stopPropagation();
      if (onClose) {
        onClose(event, "escapeKeyDown");
      }
    }
  };
  const createHandleBackdropClick = (otherHandlers) => (event) => {
    otherHandlers.onClick?.(event);
    if (event.target !== event.currentTarget) {
      return;
    }
    if (onClose) {
      onClose(event, "backdropClick");
    }
  };
  const getRootProps = (otherHandlers = {}) => {
    const propsEventHandlers = extractEventHandlers_default(parameters);
    delete propsEventHandlers.onTransitionEnter;
    delete propsEventHandlers.onTransitionExited;
    const externalEventHandlers = {
      ...propsEventHandlers,
      ...otherHandlers,
    };
    return {
      /*
       * Marking an element with the role presentation indicates to assistive technology
       * that this element should be ignored; it exists to support the web application and
       * is not meant for humans to interact with directly.
       * https://github.com/evcohen/eslint-plugin-jsx-a11y/blob/master/docs/rules/no-static-element-interactions.md
       */
      role: "presentation",
      ...externalEventHandlers,
      onKeyDown: createHandleKeyDown(externalEventHandlers),
      ref: handleRef,
    };
  };
  const getBackdropProps = (otherHandlers = {}) => {
    const externalEventHandlers = otherHandlers;
    return {
      "aria-hidden": true,
      ...externalEventHandlers,
      onClick: createHandleBackdropClick(externalEventHandlers),
      open,
    };
  };
  const getTransitionProps2 = () => {
    const handleEnter = () => {
      setExited(false);
      if (onTransitionEnter) {
        onTransitionEnter();
      }
    };
    const handleExited = () => {
      setExited(true);
      if (onTransitionExited) {
        onTransitionExited();
      }
      if (closeAfterTransition) {
        handleClose();
      }
    };
    return {
      onEnter: createChainedFunction(
        handleEnter,
        children?.props.onEnter ?? noop4,
      ),
      onExited: createChainedFunction(
        handleExited,
        children?.props.onExited ?? noop4,
      ),
    };
  };
  return {
    getRootProps,
    getBackdropProps,
    getTransitionProps: getTransitionProps2,
    rootRef: handleRef,
    portalRef: handlePortalRef,
    isTopModal,
    exited,
    hasTransition,
  };
}
var useModal_default = useModal;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Modal/modalClasses.js
function getModalUtilityClass(slot) {
  return generateUtilityClass("MuiModal", slot);
}
var modalClasses = generateUtilityClasses("MuiModal", [
  "root",
  "hidden",
  "backdrop",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Modal/Modal.js
var import_jsx_runtime43 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses15 = (ownerState) => {
  const { open, exited, classes } = ownerState;
  const slots = {
    root: ["root", !open && exited && "hidden"],
    backdrop: ["backdrop"],
  };
  return composeClasses(slots, getModalUtilityClass, classes);
};
var ModalRoot = styled_default2("div", {
  name: "MuiModal",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      !ownerState.open && ownerState.exited && styles5.hidden,
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    position: "fixed",
    zIndex: (theme2.vars || theme2).zIndex.modal,
    right: 0,
    bottom: 0,
    top: 0,
    left: 0,
    variants: [
      {
        props: ({ ownerState }) => !ownerState.open && ownerState.exited,
        style: {
          visibility: "hidden",
        },
      },
    ],
  })),
);
var ModalBackdrop = styled_default2(Backdrop_default, {
  name: "MuiModal",
  slot: "Backdrop",
})({
  zIndex: -1,
});
var Modal = /* @__PURE__ */ React63.forwardRef(function Modal2(inProps, ref) {
  const props = useDefaultProps2({
    name: "MuiModal",
    props: inProps,
  });
  const {
    BackdropComponent = ModalBackdrop,
    BackdropProps,
    classes: classesProp,
    className,
    closeAfterTransition = false,
    children,
    container,
    component,
    components = {},
    componentsProps = {},
    disableAutoFocus = false,
    disableEnforceFocus = false,
    disableEscapeKeyDown = false,
    disablePortal = false,
    disableRestoreFocus = false,
    disableScrollLock = false,
    hideBackdrop = false,
    keepMounted = false,
    onClose,
    onTransitionEnter,
    onTransitionExited,
    open,
    slotProps = {},
    slots = {},
    // eslint-disable-next-line react/prop-types
    theme: theme2,
    ...other
  } = props;
  const propsWithDefaults = {
    ...props,
    closeAfterTransition,
    disableAutoFocus,
    disableEnforceFocus,
    disableEscapeKeyDown,
    disablePortal,
    disableRestoreFocus,
    disableScrollLock,
    hideBackdrop,
    keepMounted,
  };
  const {
    getRootProps,
    getBackdropProps,
    getTransitionProps: getTransitionProps2,
    portalRef,
    isTopModal,
    exited,
    hasTransition,
  } = useModal_default({
    ...propsWithDefaults,
    rootRef: ref,
  });
  const ownerState = {
    ...propsWithDefaults,
    exited,
  };
  const classes = useUtilityClasses15(ownerState);
  const childProps = {};
  if (children.props.tabIndex === void 0) {
    childProps.tabIndex = "-1";
  }
  if (hasTransition) {
    const { onEnter, onExited } = getTransitionProps2();
    childProps.onEnter = onEnter;
    childProps.onExited = onExited;
  }
  const externalForwardedProps = {
    slots: {
      root: components.Root,
      backdrop: components.Backdrop,
      ...slots,
    },
    slotProps: {
      ...componentsProps,
      ...slotProps,
    },
  };
  const [RootSlot, rootProps] = useSlot("root", {
    ref,
    elementType: ModalRoot,
    externalForwardedProps: {
      ...externalForwardedProps,
      ...other,
      component,
    },
    getSlotProps: getRootProps,
    ownerState,
    className: clsx_default(
      className,
      classes?.root,
      !ownerState.open && ownerState.exited && classes?.hidden,
    ),
  });
  const [BackdropSlot, backdropProps] = useSlot("backdrop", {
    ref: BackdropProps?.ref,
    elementType: BackdropComponent,
    externalForwardedProps,
    shouldForwardComponentProp: true,
    additionalProps: BackdropProps,
    getSlotProps: (otherHandlers) => {
      return getBackdropProps({
        ...otherHandlers,
        onClick: (event) => {
          if (otherHandlers?.onClick) {
            otherHandlers.onClick(event);
          }
        },
      });
    },
    className: clsx_default(BackdropProps?.className, classes?.backdrop),
    ownerState,
  });
  if (!keepMounted && !open && (!hasTransition || exited)) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(Portal_default, {
    ref: portalRef,
    container,
    disablePortal,
    children: /* @__PURE__ */ (0, import_jsx_runtime43.jsxs)(RootSlot, {
      ...rootProps,
      children: [
        !hideBackdrop && BackdropComponent
          ? /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(BackdropSlot, {
              ...backdropProps,
            })
          : null,
        /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(FocusTrap_default, {
          disableEnforceFocus,
          disableAutoFocus,
          disableRestoreFocus,
          isEnabled: isTopModal,
          open,
          children: /* @__PURE__ */ React63.cloneElement(children, childProps),
        }),
      ],
    }),
  });
});
false
  ? (Modal.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * A backdrop component. This prop enables custom backdrop rendering.
       * @deprecated Use `slots.backdrop` instead. While this prop currently works, it will be removed in the next major version.
       * Use the `slots.backdrop` prop to make your application ready for the next version of Material UI.
       * @default styled(Backdrop, {
       *   name: 'MuiModal',
       *   slot: 'Backdrop',
       * })({
       *   zIndex: -1,
       * })
       */
      BackdropComponent: import_prop_types.default.elementType,
      /**
       * Props applied to the [`Backdrop`](https://mui.com/material-ui/api/backdrop/) element.
       * @deprecated Use `slotProps.backdrop` instead.
       */
      BackdropProps: import_prop_types.default.object,
      /**
       * A single child content element.
       */
      children: elementAcceptingRef_default.isRequired,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * When set to true the Modal waits until a nested Transition is completed before closing.
       * @default false
       */
      closeAfterTransition: import_prop_types.default.bool,
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * The components used for each slot inside.
       *
       * @deprecated Use the `slots` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       *
       * @default {}
       */
      components: import_prop_types.default.shape({
        Backdrop: import_prop_types.default.elementType,
        Root: import_prop_types.default.elementType,
      }),
      /**
       * The extra props for the slot components.
       * You can override the existing props or add new ones.
       *
       * @deprecated Use the `slotProps` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       *
       * @default {}
       */
      componentsProps: import_prop_types.default.shape({
        backdrop: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        root: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      }),
      /**
       * An HTML element or function that returns one.
       * The `container` will have the portal children appended to it.
       *
       * You can also provide a callback, which is called in a React layout effect.
       * This lets you set the container from a ref, and also makes server-side rendering possible.
       *
       * By default, it uses the body of the top-level document object,
       * so it's simply `document.body` most of the time.
       */
      container: import_prop_types.default.oneOfType([
        HTMLElementType,
        import_prop_types.default.func,
      ]),
      /**
       * If `true`, the modal will not automatically shift focus to itself when it opens, and
       * replace it to the last focused element when it closes.
       * This also works correctly with any modal children that have the `disableAutoFocus` prop.
       *
       * Generally this should never be set to `true` as it makes the modal less
       * accessible to assistive technologies, like screen readers.
       * @default false
       */
      disableAutoFocus: import_prop_types.default.bool,
      /**
       * If `true`, the modal will not prevent focus from leaving the modal while open.
       *
       * Generally this should never be set to `true` as it makes the modal less
       * accessible to assistive technologies, like screen readers.
       * @default false
       */
      disableEnforceFocus: import_prop_types.default.bool,
      /**
       * If `true`, hitting escape will not fire the `onClose` callback.
       * @default false
       */
      disableEscapeKeyDown: import_prop_types.default.bool,
      /**
       * The `children` will be under the DOM hierarchy of the parent component.
       * @default false
       */
      disablePortal: import_prop_types.default.bool,
      /**
       * If `true`, the modal will not restore focus to previously focused element once
       * modal is hidden or unmounted.
       * @default false
       */
      disableRestoreFocus: import_prop_types.default.bool,
      /**
       * Disable the scroll lock behavior.
       * @default false
       */
      disableScrollLock: import_prop_types.default.bool,
      /**
       * If `true`, the backdrop is not rendered.
       * @default false
       */
      hideBackdrop: import_prop_types.default.bool,
      /**
       * Always keep the children in the DOM.
       * This prop can be useful in SEO situation or
       * when you want to maximize the responsiveness of the Modal.
       * @default false
       */
      keepMounted: import_prop_types.default.bool,
      /**
       * Callback fired when the component requests to be closed.
       * The `reason` parameter can optionally be used to control the response to `onClose`.
       *
       * @param {object} event The event source of the callback.
       * @param {string} reason Can be: `"escapeKeyDown"`, `"backdropClick"`.
       */
      onClose: import_prop_types.default.func,
      /**
       * A function called when a transition enters.
       */
      onTransitionEnter: import_prop_types.default.func,
      /**
       * A function called when a transition has exited.
       */
      onTransitionExited: import_prop_types.default.func,
      /**
       * If `true`, the component is shown.
       */
      open: import_prop_types.default.bool.isRequired,
      /**
       * The props used for each slot inside the Modal.
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        backdrop: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        root: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      }),
      /**
       * The components used for each slot inside the Modal.
       * Either a string to use a HTML element or a component.
       * @default {}
       */
      slots: import_prop_types.default.shape({
        backdrop: import_prop_types.default.elementType,
        root: import_prop_types.default.elementType,
      }),
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
    })
  : void 0;
var Modal_default = Modal;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Dialog/dialogClasses.js
function getDialogUtilityClass(slot) {
  return generateUtilityClass("MuiDialog", slot);
}
var dialogClasses = generateUtilityClasses("MuiDialog", [
  "root",
  "backdrop",
  "scrollPaper",
  "scrollBody",
  "container",
  "paper",
  "paperScrollPaper",
  "paperScrollBody",
  "paperWidthFalse",
  "paperWidthXs",
  "paperWidthSm",
  "paperWidthMd",
  "paperWidthLg",
  "paperWidthXl",
  "paperFullWidth",
  "paperFullScreen",
]);
var dialogClasses_default = dialogClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Dialog/DialogContext.js
var React64 = __toESM(require_react(), 1);
var DialogContext = /* @__PURE__ */ React64.createContext({});
if (false) {
  DialogContext.displayName = "DialogContext";
}
var DialogContext_default = DialogContext;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Dialog/Dialog.js
var import_jsx_runtime44 = __toESM(require_jsx_runtime(), 1);
var DialogBackdrop = styled_default2(Backdrop_default, {
  name: "MuiDialog",
  slot: "Backdrop",
})({
  // Improve scrollable dialog support.
  zIndex: -1,
});
var useUtilityClasses16 = (ownerState) => {
  const {
    classes,
    scroll,
    maxWidth: maxWidth2,
    fullWidth,
    fullScreen,
  } = ownerState;
  const slots = {
    root: ["root"],
    backdrop: ["backdrop"],
    container: ["container", `scroll${capitalize_default(scroll)}`],
    paper: [
      "paper",
      `paperScroll${capitalize_default(scroll)}`,
      `paperWidth${capitalize_default(String(maxWidth2))}`,
      fullWidth && "paperFullWidth",
      fullScreen && "paperFullScreen",
    ],
  };
  return composeClasses(slots, getDialogUtilityClass, classes);
};
var DialogRoot = styled_default2(Modal_default, {
  name: "MuiDialog",
  slot: "Root",
})({
  "@media print": {
    // Use !important to override the Modal inline-style.
    position: "absolute !important",
  },
});
var DialogContainer = styled_default2("div", {
  name: "MuiDialog",
  slot: "Container",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.container,
      styles5[`scroll${capitalize_default(ownerState.scroll)}`],
    ];
  },
})({
  height: "100%",
  "@media print": {
    height: "auto",
  },
  // We disable the focus ring for mouse, touch and keyboard users.
  outline: 0,
  variants: [
    {
      props: {
        scroll: "paper",
      },
      style: {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      },
    },
    {
      props: {
        scroll: "body",
      },
      style: {
        overflowY: "auto",
        overflowX: "hidden",
        textAlign: "center",
        "&::after": {
          content: '""',
          display: "inline-block",
          verticalAlign: "middle",
          height: "100%",
          width: "0",
        },
      },
    },
  ],
});
var DialogPaper = styled_default2(Paper_default, {
  name: "MuiDialog",
  slot: "Paper",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.paper,
      styles5[`scrollPaper${capitalize_default(ownerState.scroll)}`],
      styles5[`paperWidth${capitalize_default(String(ownerState.maxWidth))}`],
      ownerState.fullWidth && styles5.paperFullWidth,
      ownerState.fullScreen && styles5.paperFullScreen,
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    margin: 32,
    position: "relative",
    overflowY: "auto",
    "@media print": {
      overflowY: "visible",
      boxShadow: "none",
    },
    variants: [
      {
        props: {
          scroll: "paper",
        },
        style: {
          display: "flex",
          flexDirection: "column",
          maxHeight: "calc(100% - 64px)",
        },
      },
      {
        props: {
          scroll: "body",
        },
        style: {
          display: "inline-block",
          verticalAlign: "middle",
          textAlign: "initial",
        },
      },
      {
        props: ({ ownerState }) => !ownerState.maxWidth,
        style: {
          maxWidth: "calc(100% - 64px)",
        },
      },
      {
        props: {
          maxWidth: "xs",
        },
        style: {
          maxWidth:
            theme2.breakpoints.unit === "px"
              ? Math.max(theme2.breakpoints.values.xs, 444)
              : `max(${theme2.breakpoints.values.xs}${theme2.breakpoints.unit}, 444px)`,
          [`&.${dialogClasses_default.paperScrollBody}`]: {
            [theme2.breakpoints.down(
              Math.max(theme2.breakpoints.values.xs, 444) + 32 * 2,
            )]: {
              maxWidth: "calc(100% - 64px)",
            },
          },
        },
      },
      ...Object.keys(theme2.breakpoints.values)
        .filter((maxWidth2) => maxWidth2 !== "xs")
        .map((maxWidth2) => ({
          props: {
            maxWidth: maxWidth2,
          },
          style: {
            maxWidth: `${theme2.breakpoints.values[maxWidth2]}${theme2.breakpoints.unit}`,
            [`&.${dialogClasses_default.paperScrollBody}`]: {
              [theme2.breakpoints.down(
                theme2.breakpoints.values[maxWidth2] + 32 * 2,
              )]: {
                maxWidth: "calc(100% - 64px)",
              },
            },
          },
        })),
      {
        props: ({ ownerState }) => ownerState.fullWidth,
        style: {
          width: "calc(100% - 64px)",
        },
      },
      {
        props: ({ ownerState }) => ownerState.fullScreen,
        style: {
          margin: 0,
          width: "100%",
          maxWidth: "100%",
          height: "100%",
          maxHeight: "none",
          borderRadius: 0,
          [`&.${dialogClasses_default.paperScrollBody}`]: {
            margin: 0,
            maxWidth: "100%",
          },
        },
      },
    ],
  })),
);
var Dialog = /* @__PURE__ */ React65.forwardRef(function Dialog2(inProps, ref) {
  const props = useDefaultProps2({
    props: inProps,
    name: "MuiDialog",
  });
  const theme2 = useTheme5();
  const defaultTransitionDuration = {
    enter: theme2.transitions.duration.enteringScreen,
    exit: theme2.transitions.duration.leavingScreen,
  };
  const {
    "aria-describedby": ariaDescribedby,
    "aria-labelledby": ariaLabelledbyProp,
    "aria-modal": ariaModal = true,
    BackdropComponent,
    BackdropProps,
    children,
    className,
    disableEscapeKeyDown = false,
    fullScreen = false,
    fullWidth = false,
    maxWidth: maxWidth2 = "sm",
    onClick,
    onClose,
    open,
    PaperComponent = Paper_default,
    PaperProps = {},
    role = "dialog",
    scroll = "paper",
    slots = {},
    slotProps = {},
    TransitionComponent = Fade_default,
    transitionDuration = defaultTransitionDuration,
    TransitionProps,
    ...other
  } = props;
  const ownerState = {
    ...props,
    disableEscapeKeyDown,
    fullScreen,
    fullWidth,
    maxWidth: maxWidth2,
    scroll,
  };
  const classes = useUtilityClasses16(ownerState);
  const backdropClick = React65.useRef();
  const handleMouseDown = (event) => {
    backdropClick.current = event.target === event.currentTarget;
  };
  const handleBackdropClick = (event) => {
    if (onClick) {
      onClick(event);
    }
    if (!backdropClick.current) {
      return;
    }
    backdropClick.current = null;
    if (onClose) {
      onClose(event, "backdropClick");
    }
  };
  const ariaLabelledby = useId(ariaLabelledbyProp);
  const dialogContextValue = React65.useMemo(() => {
    return {
      titleId: ariaLabelledby,
    };
  }, [ariaLabelledby]);
  const backwardCompatibleSlots = {
    transition: TransitionComponent,
    ...slots,
  };
  const backwardCompatibleSlotProps = {
    transition: TransitionProps,
    paper: PaperProps,
    backdrop: BackdropProps,
    ...slotProps,
  };
  const externalForwardedProps = {
    slots: backwardCompatibleSlots,
    slotProps: backwardCompatibleSlotProps,
  };
  const [RootSlot, rootSlotProps] = useSlot("root", {
    elementType: DialogRoot,
    shouldForwardComponentProp: true,
    externalForwardedProps,
    ownerState,
    className: clsx_default(classes.root, className),
    ref,
  });
  const [BackdropSlot, backdropSlotProps] = useSlot("backdrop", {
    elementType: DialogBackdrop,
    shouldForwardComponentProp: true,
    externalForwardedProps,
    ownerState,
    className: classes.backdrop,
  });
  const [PaperSlot, paperSlotProps] = useSlot("paper", {
    elementType: DialogPaper,
    shouldForwardComponentProp: true,
    externalForwardedProps,
    ownerState,
    className: classes.paper,
    additionalProps: {
      elevation: 24,
      role,
      "aria-describedby": ariaDescribedby,
      "aria-labelledby": ariaLabelledby,
      "aria-modal": ariaModal,
      tabIndex: -1,
      [FOCUSABLE_ATTRIBUTE]: "",
    },
  });
  const [ContainerSlot, containerSlotProps] = useSlot("container", {
    elementType: DialogContainer,
    externalForwardedProps,
    ownerState,
    className: classes.container,
  });
  const [TransitionSlot, transitionSlotProps] = useSlot("transition", {
    elementType: Fade_default,
    externalForwardedProps,
    ownerState,
    additionalProps: {
      appear: true,
      in: open,
      timeout: transitionDuration,
      role: "presentation",
    },
  });
  return /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(RootSlot, {
    closeAfterTransition: true,
    slots: {
      backdrop: BackdropSlot,
    },
    slotProps: {
      backdrop: {
        transitionDuration,
        as: BackdropComponent,
        ...backdropSlotProps,
      },
    },
    disableEscapeKeyDown,
    onClose,
    open,
    onClick: handleBackdropClick,
    ...rootSlotProps,
    ...other,
    children: /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(TransitionSlot, {
      ...transitionSlotProps,
      children: /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(ContainerSlot, {
        onMouseDown: handleMouseDown,
        ...containerSlotProps,
        children: /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(PaperSlot, {
          as: PaperComponent,
          ...paperSlotProps,
          children: /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(
            DialogContext_default.Provider,
            {
              value: dialogContextValue,
              children,
            },
          ),
        }),
      }),
    }),
  });
});
false
  ? (Dialog.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The id(s) of the element(s) that describe the dialog.
       */
      "aria-describedby": import_prop_types.default.string,
      /**
       * The id(s) of the element(s) that label the dialog.
       */
      "aria-labelledby": import_prop_types.default.string,
      /**
       * Informs assistive technologies that the element is modal.
       * It's added on the element with role="dialog".
       * @default true
       */
      "aria-modal": import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["false", "true"]),
        import_prop_types.default.bool,
      ]),
      /**
       * A backdrop component. This prop enables custom backdrop rendering.
       * @deprecated Use `slots.backdrop` instead. While this prop currently works, it will be removed in the next major version.
       * Use the `slots.backdrop` prop to make your application ready for the next version of Material UI.
       * @default styled(Backdrop, {
       *   name: 'MuiModal',
       *   slot: 'Backdrop',
       * })({
       *   zIndex: -1,
       * })
       */
      BackdropComponent: import_prop_types.default.elementType,
      /**
       * @ignore
       */
      BackdropProps: import_prop_types.default.object,
      /**
       * Dialog children, usually the included sub-components.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * If `true`, hitting escape will not fire the `onClose` callback.
       * @default false
       */
      disableEscapeKeyDown: import_prop_types.default.bool,
      /**
       * If `true`, the dialog is full-screen.
       * @default false
       */
      fullScreen: import_prop_types.default.bool,
      /**
       * If `true`, the dialog stretches to `maxWidth`.
       *
       * Notice that the dialog width grow is limited by the default margin.
       * @default false
       */
      fullWidth: import_prop_types.default.bool,
      /**
       * Determine the max-width of the dialog.
       * The dialog width grows with the size of the screen.
       * Set to `false` to disable `maxWidth`.
       * @default 'sm'
       */
      maxWidth: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["xs", "sm", "md", "lg", "xl", false]),
        import_prop_types.default.string,
      ]),
      /**
       * @ignore
       */
      onClick: import_prop_types.default.func,
      /**
       * Callback fired when the component requests to be closed.
       *
       * @param {object} event The event source of the callback.
       * @param {string} reason Can be: `"escapeKeyDown"`, `"backdropClick"`.
       */
      onClose: import_prop_types.default.func,
      /**
       * If `true`, the component is shown.
       */
      open: import_prop_types.default.bool.isRequired,
      /**
       * The component used to render the body of the dialog.
       * @default Paper
       */
      PaperComponent: import_prop_types.default.elementType,
      /**
       * Props applied to the [`Paper`](https://mui.com/material-ui/api/paper/) element.
       * @default {}
       * @deprecated Use `slotProps.paper` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       */
      PaperProps: import_prop_types.default.object,
      /**
       * The ARIA role for the dialog element.
       * The main dialog role is `dialog`, but `alertdialog` can be used if the content of the dialog requires immediate attention.
       * See https://www.w3.org/TR/wai-aria-1.2/#dialog and https://www.w3.org/TR/wai-aria-1.2/#alertdialog for more details.
       * @default 'dialog'
       */
      role: import_prop_types.default.oneOf(["alertdialog", "dialog"]),
      /**
       * Determine the container for scrolling the dialog.
       * @default 'paper'
       */
      scroll: import_prop_types.default.oneOf(["body", "paper"]),
      /**
       * The props used for each slot inside.
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        backdrop: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        container: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        paper: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        root: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        transition: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      }),
      /**
       * The components used for each slot inside.
       * @default {}
       */
      slots: import_prop_types.default.shape({
        backdrop: import_prop_types.default.elementType,
        container: import_prop_types.default.elementType,
        paper: import_prop_types.default.elementType,
        root: import_prop_types.default.elementType,
        transition: import_prop_types.default.elementType,
      }),
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * The component used for the transition.
       * [Follow this guide](https://mui.com/material-ui/transitions/#transitioncomponent-prop) to learn more about the requirements for this component.
       * @default Fade
       * @deprecated Use `slots.transition` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       */
      TransitionComponent: import_prop_types.default.elementType,
      /**
       * The duration for the transition, in milliseconds.
       * You may specify a single timeout for all transitions, or individually with an object.
       * @default {
       *   enter: theme.transitions.duration.enteringScreen,
       *   exit: theme.transitions.duration.leavingScreen,
       * }
       */
      transitionDuration: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.shape({
          appear: import_prop_types.default.number,
          enter: import_prop_types.default.number,
          exit: import_prop_types.default.number,
        }),
      ]),
      /**
       * Props applied to the transition element.
       * By default, the element is based on this [`Transition`](https://reactcommunity.org/react-transition-group/transition/) component.
       * @deprecated Use `slotProps.transition` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       */
      TransitionProps: import_prop_types.default.object,
    })
  : void 0;
var Dialog_default = Dialog;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/DialogActions/DialogActions.js
var React66 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/DialogActions/dialogActionsClasses.js
function getDialogActionsUtilityClass(slot) {
  return generateUtilityClass("MuiDialogActions", slot);
}
var dialogActionsClasses = generateUtilityClasses("MuiDialogActions", [
  "root",
  "spacing",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/DialogActions/DialogActions.js
var import_jsx_runtime45 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses17 = (ownerState) => {
  const { classes, disableSpacing } = ownerState;
  const slots = {
    root: ["root", !disableSpacing && "spacing"],
  };
  return composeClasses(slots, getDialogActionsUtilityClass, classes);
};
var DialogActionsRoot = styled_default2("div", {
  name: "MuiDialogActions",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [styles5.root, !ownerState.disableSpacing && styles5.spacing];
  },
})({
  display: "flex",
  alignItems: "center",
  padding: 8,
  justifyContent: "flex-end",
  flex: "0 0 auto",
  variants: [
    {
      props: ({ ownerState }) => !ownerState.disableSpacing,
      style: {
        "& > :not(style) ~ :not(style)": {
          marginLeft: 8,
        },
      },
    },
  ],
});
var DialogActions = /* @__PURE__ */ React66.forwardRef(
  function DialogActions2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiDialogActions",
    });
    const { className, disableSpacing = false, ...other } = props;
    const ownerState = {
      ...props,
      disableSpacing,
    };
    const classes = useUtilityClasses17(ownerState);
    return /* @__PURE__ */ (0, import_jsx_runtime45.jsx)(DialogActionsRoot, {
      className: clsx_default(classes.root, className),
      ownerState,
      ref,
      ...other,
    });
  },
);
false
  ? (DialogActions.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * If `true`, the actions do not have additional margin.
       * @default false
       */
      disableSpacing: import_prop_types.default.bool,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
    })
  : void 0;
var DialogActions_default = DialogActions;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/DialogContent/DialogContent.js
var React67 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/DialogContent/dialogContentClasses.js
function getDialogContentUtilityClass(slot) {
  return generateUtilityClass("MuiDialogContent", slot);
}
var dialogContentClasses = generateUtilityClasses("MuiDialogContent", [
  "root",
  "dividers",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/DialogTitle/dialogTitleClasses.js
function getDialogTitleUtilityClass(slot) {
  return generateUtilityClass("MuiDialogTitle", slot);
}
var dialogTitleClasses = generateUtilityClasses("MuiDialogTitle", ["root"]);
var dialogTitleClasses_default = dialogTitleClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/DialogContent/DialogContent.js
var import_jsx_runtime46 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses18 = (ownerState) => {
  const { classes, dividers } = ownerState;
  const slots = {
    root: ["root", dividers && "dividers"],
  };
  return composeClasses(slots, getDialogContentUtilityClass, classes);
};
var DialogContentRoot = styled_default2("div", {
  name: "MuiDialogContent",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [styles5.root, ownerState.dividers && styles5.dividers];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    flex: "1 1 auto",
    // Add iOS momentum scrolling for iOS < 13.0
    WebkitOverflowScrolling: "touch",
    overflowY: "auto",
    padding: "20px 24px",
    variants: [
      {
        props: ({ ownerState }) => ownerState.dividers,
        style: {
          padding: "16px 24px",
          borderTop: `1px solid ${(theme2.vars || theme2).palette.divider}`,
          borderBottom: `1px solid ${(theme2.vars || theme2).palette.divider}`,
        },
      },
      {
        props: ({ ownerState }) => !ownerState.dividers,
        style: {
          [`.${dialogTitleClasses_default.root} + &`]: {
            paddingTop: 0,
          },
        },
      },
    ],
  })),
);
var DialogContent = /* @__PURE__ */ React67.forwardRef(
  function DialogContent2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiDialogContent",
    });
    const { className, dividers = false, ...other } = props;
    const ownerState = {
      ...props,
      dividers,
    };
    const classes = useUtilityClasses18(ownerState);
    return /* @__PURE__ */ (0, import_jsx_runtime46.jsx)(DialogContentRoot, {
      className: clsx_default(classes.root, className),
      ownerState,
      ref,
      ...other,
    });
  },
);
false
  ? (DialogContent.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * Display the top and bottom dividers.
       * @default false
       */
      dividers: import_prop_types.default.bool,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
    })
  : void 0;
var DialogContent_default = DialogContent;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/DialogTitle/DialogTitle.js
var React68 = __toESM(require_react(), 1);
var import_jsx_runtime47 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses19 = (ownerState) => {
  const { classes } = ownerState;
  const slots = {
    root: ["root"],
  };
  return composeClasses(slots, getDialogTitleUtilityClass, classes);
};
var DialogTitleRoot = styled_default2(Typography_default, {
  name: "MuiDialogTitle",
  slot: "Root",
})({
  padding: "16px 24px",
  flex: "0 0 auto",
});
var DialogTitle = /* @__PURE__ */ React68.forwardRef(
  function DialogTitle2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiDialogTitle",
    });
    const { className, id: idProp, ...other } = props;
    const ownerState = props;
    const classes = useUtilityClasses19(ownerState);
    const { titleId = idProp } = React68.useContext(DialogContext_default);
    return /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(DialogTitleRoot, {
      component: "h2",
      className: clsx_default(classes.root, className),
      ownerState,
      ref,
      variant: "h6",
      id: idProp ?? titleId,
      ...other,
    });
  },
);
false
  ? (DialogTitle.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * @ignore
       */
      id: import_prop_types.default.string,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
    })
  : void 0;
var DialogTitle_default = DialogTitle;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Drawer/Drawer.js
var React70 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Slide/Slide.js
var React69 = __toESM(require_react(), 1);
var import_jsx_runtime48 = __toESM(require_jsx_runtime(), 1);
var hiddenStyles2 = {
  visibility: "hidden",
};
function getTranslateValue(direction, node2, resolvedContainer) {
  const containerRect =
    resolvedContainer && resolvedContainer.getBoundingClientRect();
  const containerWindow = ownerWindow_default(node2);
  const previousTransform = node2.style.transform;
  const previousTransition = node2.style.transition;
  node2.style.transition = "";
  node2.style.transform = "";
  const rect = node2.getBoundingClientRect();
  const computedStyle = containerWindow.getComputedStyle(node2);
  const transform = computedStyle.getPropertyValue("transform");
  node2.style.transform = previousTransform;
  node2.style.transition = previousTransition;
  let offsetX = 0;
  let offsetY = 0;
  if (transform && transform !== "none" && typeof transform === "string") {
    const transformValues = transform.split("(")[1].split(")")[0].split(",");
    offsetX = parseInt(transformValues[4], 10);
    offsetY = parseInt(transformValues[5], 10);
  }
  if (direction === "left") {
    if (containerRect) {
      return `translateX(${containerRect.right + offsetX - rect.left}px)`;
    }
    return `translateX(${containerWindow.innerWidth + offsetX - rect.left}px)`;
  }
  if (direction === "right") {
    if (containerRect) {
      return `translateX(-${rect.right - containerRect.left - offsetX}px)`;
    }
    return `translateX(-${rect.left + rect.width - offsetX}px)`;
  }
  if (direction === "up") {
    if (containerRect) {
      return `translateY(${containerRect.bottom + offsetY - rect.top}px)`;
    }
    return `translateY(${containerWindow.innerHeight + offsetY - rect.top}px)`;
  }
  if (containerRect) {
    return `translateY(-${rect.top - containerRect.top + rect.height - offsetY}px)`;
  }
  return `translateY(-${rect.top + rect.height - offsetY}px)`;
}
function resolveContainer(containerPropProp) {
  return typeof containerPropProp === "function"
    ? containerPropProp()
    : containerPropProp;
}
function setTranslateValue(direction, node2, containerProp) {
  const resolvedContainer = resolveContainer(containerProp);
  const transform = getTranslateValue(direction, node2, resolvedContainer);
  if (transform) {
    node2.style.transform = transform;
  }
}
var Slide = /* @__PURE__ */ React69.forwardRef(function Slide2(props, ref) {
  const theme2 = useTheme5();
  const defaultEasing = {
    enter: theme2.transitions.easing.easeOut,
    exit: theme2.transitions.easing.sharp,
  };
  const defaultTimeout = {
    enter: theme2.transitions.duration.enteringScreen,
    exit: theme2.transitions.duration.leavingScreen,
  };
  const {
    addEndListener,
    appear = true,
    children,
    container: containerProp,
    direction = "down",
    easing: easingProp = defaultEasing,
    in: inProp,
    onEnter,
    onEntered,
    onEntering,
    onExit,
    onExited,
    onExiting,
    style: style4,
    timeout: timeout2 = defaultTimeout,
    ...other
  } = props;
  const childrenRef = React69.useRef(null);
  const handleRef = useForkRef_default(
    getReactElementRef(children),
    childrenRef,
    ref,
  );
  const handleEnter = normalizedTransitionCallback(
    childrenRef,
    (node2, isAppearing) => {
      setTranslateValue(direction, node2, containerProp);
      reflow(node2);
      if (onEnter) {
        onEnter(node2, isAppearing);
      }
    },
  );
  const handleEntering = normalizedTransitionCallback(
    childrenRef,
    (node2, isAppearing) => {
      const transitionProps = getTransitionProps(
        {
          timeout: timeout2,
          style: style4,
          easing: easingProp,
        },
        {
          mode: "enter",
        },
      );
      node2.style.transition = theme2.transitions.create(
        "transform",
        transitionProps,
      );
      node2.style.transform = "none";
      if (onEntering) {
        onEntering(node2, isAppearing);
      }
    },
  );
  const handleEntered = normalizedTransitionCallback(childrenRef, onEntered);
  const handleExiting = normalizedTransitionCallback(childrenRef, onExiting);
  const handleExit = normalizedTransitionCallback(childrenRef, (node2) => {
    const transitionProps = getTransitionProps(
      {
        timeout: timeout2,
        style: style4,
        easing: easingProp,
      },
      {
        mode: "exit",
      },
    );
    node2.style.transition = theme2.transitions.create(
      "transform",
      transitionProps,
    );
    setTranslateValue(direction, node2, containerProp);
    if (onExit) {
      onExit(node2);
    }
  });
  const handleExited = normalizedTransitionCallback(childrenRef, (node2) => {
    node2.style.transition = "";
    if (onExited) {
      onExited(node2);
    }
  });
  const handleAddEndListener = (next2) => {
    if (addEndListener) {
      addEndListener(childrenRef.current, next2);
    }
  };
  const updatePosition = React69.useCallback(() => {
    if (childrenRef.current) {
      setTranslateValue(direction, childrenRef.current, containerProp);
    }
  }, [direction, containerProp]);
  React69.useEffect(() => {
    if (inProp || direction === "down" || direction === "right") {
      return void 0;
    }
    const handleResize = debounce_default(() => {
      if (childrenRef.current) {
        setTranslateValue(direction, childrenRef.current, containerProp);
      }
    });
    const containerWindow = ownerWindow_default(childrenRef.current);
    containerWindow.addEventListener("resize", handleResize);
    return () => {
      handleResize.clear();
      containerWindow.removeEventListener("resize", handleResize);
    };
  }, [direction, inProp, containerProp]);
  React69.useEffect(() => {
    if (!inProp) {
      updatePosition();
    }
  }, [inProp, updatePosition]);
  return /* @__PURE__ */ (0, import_jsx_runtime48.jsx)(Transition_default, {
    nodeRef: childrenRef,
    onEnter: handleEnter,
    onEntered: handleEntered,
    onEntering: handleEntering,
    onExit: handleExit,
    onExited: handleExited,
    onExiting: handleExiting,
    addEndListener: handleAddEndListener,
    appear,
    in: inProp,
    timeout: timeout2,
    ...other,
    children: (state, { ownerState, ...restChildProps }) => {
      let childStyle;
      if (state === "exited" && !inProp) {
        childStyle =
          style4 || children.props.style
            ? {
                visibility: "hidden",
                ...style4,
                ...children.props.style,
              }
            : hiddenStyles2;
      } else if (style4 && children.props.style) {
        childStyle = {
          ...style4,
          ...children.props.style,
        };
      } else {
        childStyle = style4 || children.props.style;
      }
      return /* @__PURE__ */ React69.cloneElement(children, {
        ref: handleRef,
        style: childStyle,
        ...restChildProps,
      });
    },
  });
});
false
  ? (Slide.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * Add a custom transition end trigger. Called with the transitioning DOM
       * node and a done callback. Allows for more fine grained transition end
       * logic. Note: Timeouts are still used as a fallback if provided.
       */
      addEndListener: import_prop_types.default.func,
      /**
       * Perform the enter transition when it first mounts if `in` is also `true`.
       * Set this to `false` to disable this behavior.
       * @default true
       */
      appear: import_prop_types.default.bool,
      /**
       * A single child content element.
       */
      children: elementAcceptingRef_default.isRequired,
      /**
       * An HTML element, or a function that returns one.
       * It's used to set the container the Slide is transitioning from.
       */
      container: chainPropTypes(
        import_prop_types.default.oneOfType([
          HTMLElementType,
          import_prop_types.default.func,
        ]),
        (props) => {
          if (props.open) {
            const resolvedContainer = resolveContainer(props.container);
            if (resolvedContainer && resolvedContainer.nodeType === 1) {
              const box = resolvedContainer.getBoundingClientRect();
              if (false) {
                if (
                  isLayoutSupported() &&
                  box.top === 0 &&
                  box.left === 0 &&
                  box.right === 0 &&
                  box.bottom === 0
                ) {
                  return new Error(
                    [
                      "MUI: The `container` prop provided to the component is invalid.",
                      "The anchor element should be part of the document layout.",
                      "Make sure the element is present in the document or that it's not display none.",
                    ].join("\n"),
                  );
                }
              }
            } else if (
              !resolvedContainer ||
              typeof resolvedContainer.getBoundingClientRect !== "function" ||
              (resolvedContainer.contextElement != null &&
                resolvedContainer.contextElement.nodeType !== 1)
            ) {
              return new Error(
                [
                  "MUI: The `container` prop provided to the component is invalid.",
                  "It should be an HTML element instance.",
                ].join("\n"),
              );
            }
          }
          return null;
        },
      ),
      /**
       * Direction the child node will enter from.
       * @default 'down'
       */
      direction: import_prop_types.default.oneOf([
        "down",
        "left",
        "right",
        "up",
      ]),
      /**
       * The transition timing function.
       * You may specify a single easing or a object containing enter and exit values.
       * @default {
       *   enter: theme.transitions.easing.easeOut,
       *   exit: theme.transitions.easing.sharp,
       * }
       */
      easing: import_prop_types.default.oneOfType([
        import_prop_types.default.shape({
          enter: import_prop_types.default.string,
          exit: import_prop_types.default.string,
        }),
        import_prop_types.default.string,
      ]),
      /**
       * If `true`, the component will transition in.
       */
      in: import_prop_types.default.bool,
      /**
       * @ignore
       */
      onEnter: import_prop_types.default.func,
      /**
       * @ignore
       */
      onEntered: import_prop_types.default.func,
      /**
       * @ignore
       */
      onEntering: import_prop_types.default.func,
      /**
       * @ignore
       */
      onExit: import_prop_types.default.func,
      /**
       * @ignore
       */
      onExited: import_prop_types.default.func,
      /**
       * @ignore
       */
      onExiting: import_prop_types.default.func,
      /**
       * @ignore
       */
      style: import_prop_types.default.object,
      /**
       * The duration for the transition, in milliseconds.
       * You may specify a single timeout for all transitions, or individually with an object.
       * @default {
       *   enter: theme.transitions.duration.enteringScreen,
       *   exit: theme.transitions.duration.leavingScreen,
       * }
       */
      timeout: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.shape({
          appear: import_prop_types.default.number,
          enter: import_prop_types.default.number,
          exit: import_prop_types.default.number,
        }),
      ]),
    })
  : void 0;
var Slide_default = Slide;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Drawer/drawerClasses.js
function getDrawerUtilityClass(slot) {
  return generateUtilityClass("MuiDrawer", slot);
}
var drawerClasses = generateUtilityClasses("MuiDrawer", [
  "root",
  "docked",
  "paper",
  "anchorLeft",
  "anchorRight",
  "anchorTop",
  "anchorBottom",
  "paperAnchorLeft",
  "paperAnchorRight",
  "paperAnchorTop",
  "paperAnchorBottom",
  "paperAnchorDockedLeft",
  "paperAnchorDockedRight",
  "paperAnchorDockedTop",
  "paperAnchorDockedBottom",
  "modal",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Drawer/Drawer.js
var import_jsx_runtime49 = __toESM(require_jsx_runtime(), 1);
var overridesResolver = (props, styles5) => {
  const { ownerState } = props;
  return [
    styles5.root,
    (ownerState.variant === "permanent" ||
      ownerState.variant === "persistent") &&
      styles5.docked,
    ownerState.variant === "temporary" && styles5.modal,
  ];
};
var useUtilityClasses20 = (ownerState) => {
  const { classes, anchor, variant } = ownerState;
  const slots = {
    root: ["root", `anchor${capitalize_default(anchor)}`],
    docked: [(variant === "permanent" || variant === "persistent") && "docked"],
    modal: ["modal"],
    paper: [
      "paper",
      `paperAnchor${capitalize_default(anchor)}`,
      variant !== "temporary" &&
        `paperAnchorDocked${capitalize_default(anchor)}`,
    ],
  };
  return composeClasses(slots, getDrawerUtilityClass, classes);
};
var DrawerRoot = styled_default2(Modal_default, {
  name: "MuiDrawer",
  slot: "Root",
  overridesResolver,
})(
  memoTheme_default(({ theme: theme2 }) => ({
    zIndex: (theme2.vars || theme2).zIndex.drawer,
  })),
);
var DrawerDockedRoot = styled_default2("div", {
  shouldForwardProp: rootShouldForwardProp_default,
  name: "MuiDrawer",
  slot: "Docked",
  skipVariantsResolver: false,
  overridesResolver,
})({
  flex: "0 0 auto",
});
var DrawerPaper = styled_default2(Paper_default, {
  name: "MuiDrawer",
  slot: "Paper",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.paper,
      styles5[`paperAnchor${capitalize_default(ownerState.anchor)}`],
      ownerState.variant !== "temporary" &&
        styles5[`paperAnchorDocked${capitalize_default(ownerState.anchor)}`],
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    overflowY: "auto",
    display: "flex",
    flexDirection: "column",
    height: "100%",
    flex: "1 0 auto",
    zIndex: (theme2.vars || theme2).zIndex.drawer,
    // Add iOS momentum scrolling for iOS < 13.0
    WebkitOverflowScrolling: "touch",
    // temporary style
    position: "fixed",
    top: 0,
    // We disable the focus ring for mouse, touch and keyboard users.
    // At some point, it would be better to keep it for keyboard users.
    // :focus-ring CSS pseudo-class will help.
    outline: 0,
    variants: [
      {
        props: {
          anchor: "left",
        },
        style: {
          left: 0,
        },
      },
      {
        props: {
          anchor: "top",
        },
        style: {
          top: 0,
          left: 0,
          right: 0,
          height: "auto",
          maxHeight: "100%",
        },
      },
      {
        props: {
          anchor: "right",
        },
        style: {
          right: 0,
        },
      },
      {
        props: {
          anchor: "bottom",
        },
        style: {
          top: "auto",
          left: 0,
          bottom: 0,
          right: 0,
          height: "auto",
          maxHeight: "100%",
        },
      },
      {
        props: ({ ownerState }) =>
          ownerState.anchor === "left" && ownerState.variant !== "temporary",
        style: {
          borderRight: `1px solid ${(theme2.vars || theme2).palette.divider}`,
        },
      },
      {
        props: ({ ownerState }) =>
          ownerState.anchor === "top" && ownerState.variant !== "temporary",
        style: {
          borderBottom: `1px solid ${(theme2.vars || theme2).palette.divider}`,
        },
      },
      {
        props: ({ ownerState }) =>
          ownerState.anchor === "right" && ownerState.variant !== "temporary",
        style: {
          borderLeft: `1px solid ${(theme2.vars || theme2).palette.divider}`,
        },
      },
      {
        props: ({ ownerState }) =>
          ownerState.anchor === "bottom" && ownerState.variant !== "temporary",
        style: {
          borderTop: `1px solid ${(theme2.vars || theme2).palette.divider}`,
        },
      },
    ],
  })),
);
var oppositeDirection = {
  left: "right",
  right: "left",
  top: "down",
  bottom: "up",
};
function isHorizontal(anchor) {
  return ["left", "right"].includes(anchor);
}
function getAnchor({ direction }, anchor) {
  return direction === "rtl" && isHorizontal(anchor)
    ? oppositeDirection[anchor]
    : anchor;
}
var Drawer = /* @__PURE__ */ React70.forwardRef(function Drawer2(inProps, ref) {
  const props = useDefaultProps2({
    props: inProps,
    name: "MuiDrawer",
  });
  const theme2 = useTheme5();
  const isRtl = useRtl();
  const defaultTransitionDuration = {
    enter: theme2.transitions.duration.enteringScreen,
    exit: theme2.transitions.duration.leavingScreen,
  };
  const {
    anchor: anchorProp = "left",
    BackdropProps,
    children,
    className,
    elevation = 16,
    hideBackdrop = false,
    ModalProps: { BackdropProps: BackdropPropsProp, ...ModalProps } = {},
    onClose,
    open = false,
    PaperProps = {},
    SlideProps,
    // eslint-disable-next-line react/prop-types
    TransitionComponent,
    transitionDuration = defaultTransitionDuration,
    variant = "temporary",
    slots = {},
    slotProps = {},
    ...other
  } = props;
  const mounted = React70.useRef(false);
  const rootRef = React70.useRef(null);
  const handleRef = useForkRef_default(ref, rootRef);
  React70.useEffect(() => {
    mounted.current = true;
  }, []);
  const resolveSlideContainer = React70.useCallback(() => rootRef.current, []);
  const anchorInvariant = getAnchor(
    {
      direction: isRtl ? "rtl" : "ltr",
    },
    anchorProp,
  );
  const anchor = anchorProp;
  const ownerState = {
    ...props,
    anchor,
    elevation,
    open,
    variant,
    ...other,
  };
  const classes = useUtilityClasses20(ownerState);
  const externalForwardedProps = {
    slots: {
      transition: TransitionComponent,
      ...slots,
    },
    slotProps: {
      paper: PaperProps,
      transition: SlideProps,
      ...slotProps,
      backdrop: mergeSlotProps(
        slotProps.backdrop || {
          ...BackdropProps,
          ...BackdropPropsProp,
        },
        {
          transitionDuration,
        },
      ),
    },
  };
  const [RootSlot, rootSlotProps] = useSlot("root", {
    ref: handleRef,
    elementType: DrawerRoot,
    className: clsx_default(classes.root, classes.modal, className),
    shouldForwardComponentProp: true,
    ownerState,
    externalForwardedProps: {
      ...externalForwardedProps,
      ...other,
      ...ModalProps,
    },
    additionalProps: {
      closeAfterTransition: true,
      open,
      onClose,
      hideBackdrop,
      slots: {
        backdrop: externalForwardedProps.slots.backdrop,
      },
      slotProps: {
        backdrop: externalForwardedProps.slotProps.backdrop,
      },
    },
  });
  const [PaperSlot, paperSlotProps] = useSlot("paper", {
    elementType: DrawerPaper,
    shouldForwardComponentProp: true,
    className: clsx_default(classes.paper, PaperProps.className),
    ownerState,
    externalForwardedProps,
    additionalProps: {
      elevation: variant === "temporary" ? elevation : 0,
      square: true,
      ...(variant === "temporary" && {
        role: "dialog",
        "aria-modal": "true",
        [FOCUSABLE_ATTRIBUTE]: "",
        tabIndex: -1,
      }),
    },
  });
  const [DockedSlot, dockedSlotProps] = useSlot("docked", {
    elementType: DrawerDockedRoot,
    ref: handleRef,
    className: clsx_default(classes.root, classes.docked, className),
    ownerState,
    externalForwardedProps,
    additionalProps: other,
    // pass `other` here because `DockedSlot` is also a root slot for some variants
  });
  const [TransitionSlot, transitionSlotProps] = useSlot("transition", {
    elementType: Slide_default,
    ownerState,
    externalForwardedProps,
    additionalProps: {
      in: open,
      direction: oppositeDirection[anchorInvariant],
      timeout: transitionDuration,
      appear: mounted.current,
      ...(variant === "temporary" &&
        (slots.transition == null || slots.transition === Slide_default) && {
          container: resolveSlideContainer,
        }),
    },
  });
  const drawer = /* @__PURE__ */ (0, import_jsx_runtime49.jsx)(PaperSlot, {
    ...paperSlotProps,
    children,
  });
  if (variant === "permanent") {
    return /* @__PURE__ */ (0, import_jsx_runtime49.jsx)(DockedSlot, {
      ...dockedSlotProps,
      children: drawer,
    });
  }
  const slidingDrawer = /* @__PURE__ */ (0, import_jsx_runtime49.jsx)(
    TransitionSlot,
    {
      ...transitionSlotProps,
      children: drawer,
    },
  );
  if (variant === "persistent") {
    return /* @__PURE__ */ (0, import_jsx_runtime49.jsx)(DockedSlot, {
      ...dockedSlotProps,
      children: slidingDrawer,
    });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime49.jsx)(RootSlot, {
    ...rootSlotProps,
    children: slidingDrawer,
  });
});
false
  ? (Drawer.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * Side from which the drawer will appear.
       * @default 'left'
       */
      anchor: import_prop_types.default.oneOf([
        "bottom",
        "left",
        "right",
        "top",
      ]),
      /**
       * @ignore
       */
      BackdropProps: import_prop_types.default.object,
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The elevation of the drawer.
       * @default 16
       */
      elevation: integerPropType_default,
      /**
       * If `true`, the backdrop is not rendered.
       * @default false
       */
      hideBackdrop: import_prop_types.default.bool,
      /**
       * Props applied to the [`Modal`](https://mui.com/material-ui/api/modal/) element.
       * @default {}
       */
      ModalProps: import_prop_types.default.object,
      /**
       * Callback fired when the component requests to be closed.
       * The `reason` parameter can optionally be used to control the response to `onClose`.
       *
       * @param {object} event The event source of the callback.
       * @param {string} reason Can be: `"escapeKeyDown"`, `"backdropClick"`.
       */
      onClose: import_prop_types.default.func,
      /**
       * If `true`, the component is shown.
       * @default false
       */
      open: import_prop_types.default.bool,
      /**
       * Props applied to the [`Paper`](https://mui.com/material-ui/api/paper/) element.
       * @deprecated use the `slotProps.paper` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       * @default {}
       */
      PaperProps: import_prop_types.default.object,
      /**
       * Props applied to the [`Slide`](https://mui.com/material-ui/api/slide/) element.
       * @deprecated use the `slotProps.transition` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       */
      SlideProps: import_prop_types.default.object,
      /**
       * The props used for each slot inside.
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        backdrop: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        docked: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        paper: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        root: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        transition: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      }),
      /**
       * The components used for each slot inside.
       * @default {}
       */
      slots: import_prop_types.default.shape({
        backdrop: import_prop_types.default.elementType,
        docked: import_prop_types.default.elementType,
        paper: import_prop_types.default.elementType,
        root: import_prop_types.default.elementType,
        transition: import_prop_types.default.elementType,
      }),
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * The duration for the transition, in milliseconds.
       * You may specify a single timeout for all transitions, or individually with an object.
       * @default {
       *   enter: theme.transitions.duration.enteringScreen,
       *   exit: theme.transitions.duration.leavingScreen,
       * }
       */
      transitionDuration: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.shape({
          appear: import_prop_types.default.number,
          enter: import_prop_types.default.number,
          exit: import_prop_types.default.number,
        }),
      ]),
      /**
       * The variant to use.
       * @default 'temporary'
       */
      variant: import_prop_types.default.oneOf([
        "permanent",
        "persistent",
        "temporary",
      ]),
    })
  : void 0;
var Drawer_default = Drawer;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/FilledInput/FilledInput.js
var React71 = __toESM(require_react(), 1);
var import_jsx_runtime50 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses21 = (ownerState) => {
  const {
    classes,
    disableUnderline,
    startAdornment,
    endAdornment,
    size,
    hiddenLabel,
    multiline,
  } = ownerState;
  const slots = {
    root: [
      "root",
      !disableUnderline && "underline",
      startAdornment && "adornedStart",
      endAdornment && "adornedEnd",
      size === "small" && `size${capitalize_default(size)}`,
      hiddenLabel && "hiddenLabel",
      multiline && "multiline",
    ],
    input: ["input"],
  };
  const composedClasses = composeClasses(
    slots,
    getFilledInputUtilityClass,
    classes,
  );
  return {
    ...classes,
    // forward classes to the InputBase
    ...composedClasses,
  };
};
var FilledInputRoot = styled_default2(InputBaseRoot, {
  shouldForwardProp: (prop) =>
    rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiFilledInput",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      ...rootOverridesResolver(props, styles5),
      !ownerState.disableUnderline && styles5.underline,
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => {
    const light2 = theme2.palette.mode === "light";
    const bottomLineColor = light2
      ? "rgba(0, 0, 0, 0.42)"
      : "rgba(255, 255, 255, 0.7)";
    const backgroundColor2 = light2
      ? "rgba(0, 0, 0, 0.06)"
      : "rgba(255, 255, 255, 0.09)";
    const hoverBackground = light2
      ? "rgba(0, 0, 0, 0.09)"
      : "rgba(255, 255, 255, 0.13)";
    const disabledBackground = light2
      ? "rgba(0, 0, 0, 0.12)"
      : "rgba(255, 255, 255, 0.12)";
    return {
      position: "relative",
      backgroundColor: theme2.vars
        ? theme2.vars.palette.FilledInput.bg
        : backgroundColor2,
      borderTopLeftRadius: (theme2.vars || theme2).shape.borderRadius,
      borderTopRightRadius: (theme2.vars || theme2).shape.borderRadius,
      transition: theme2.transitions.create("background-color", {
        duration: theme2.transitions.duration.shorter,
        easing: theme2.transitions.easing.easeOut,
      }),
      "&:hover": {
        backgroundColor: theme2.vars
          ? theme2.vars.palette.FilledInput.hoverBg
          : hoverBackground,
        // Reset on touch devices, it doesn't add specificity
        "@media (hover: none)": {
          backgroundColor: theme2.vars
            ? theme2.vars.palette.FilledInput.bg
            : backgroundColor2,
        },
      },
      [`&.${filledInputClasses_default.focused}`]: {
        backgroundColor: theme2.vars
          ? theme2.vars.palette.FilledInput.bg
          : backgroundColor2,
      },
      [`&.${filledInputClasses_default.disabled}`]: {
        backgroundColor: theme2.vars
          ? theme2.vars.palette.FilledInput.disabledBg
          : disabledBackground,
      },
      variants: [
        {
          props: ({ ownerState }) => !ownerState.disableUnderline,
          style: {
            "&::after": {
              left: 0,
              bottom: 0,
              content: '""',
              position: "absolute",
              right: 0,
              transform: "scaleX(0)",
              transition: theme2.transitions.create("transform", {
                duration: theme2.transitions.duration.shorter,
                easing: theme2.transitions.easing.easeOut,
              }),
              pointerEvents: "none",
              // Transparent to the hover style.
            },
            [`&.${filledInputClasses_default.focused}:after`]: {
              // translateX(0) is a workaround for Safari transform scale bug
              // See https://github.com/mui/material-ui/issues/31766
              transform: "scaleX(1) translateX(0)",
            },
            [`&.${filledInputClasses_default.error}`]: {
              "&::before, &::after": {
                borderBottomColor: (theme2.vars || theme2).palette.error.main,
              },
            },
            "&::before": {
              borderBottom: `1px solid ${theme2.vars ? theme2.alpha(theme2.vars.palette.common.onBackground, theme2.vars.opacity.inputUnderline) : bottomLineColor}`,
              left: 0,
              bottom: 0,
              content: '""',
              position: "absolute",
              right: 0,
              transition: theme2.transitions.create("border-bottom-color", {
                duration: theme2.transitions.duration.shorter,
              }),
              pointerEvents: "none",
              // Transparent to the hover style.
            },
            [`&:hover:not(.${filledInputClasses_default.disabled}, .${filledInputClasses_default.error}):before`]:
              {
                borderBottom: `1px solid ${(theme2.vars || theme2).palette.text.primary}`,
              },
            [`&.${filledInputClasses_default.disabled}:before`]: {
              borderBottomStyle: "dotted",
            },
          },
        },
        ...Object.entries(theme2.palette)
          .filter(createSimplePaletteValueFilter())
          .map(([color2]) => ({
            props: {
              disableUnderline: false,
              color: color2,
            },
            style: {
              "&::after": {
                borderBottom: `2px solid ${(theme2.vars || theme2).palette[color2]?.main}`,
              },
            },
          })),
        {
          props: ({ ownerState }) => ownerState.startAdornment,
          style: {
            paddingLeft: 12,
          },
        },
        {
          props: ({ ownerState }) => ownerState.endAdornment,
          style: {
            paddingRight: 12,
          },
        },
        {
          props: ({ ownerState }) => ownerState.multiline,
          style: {
            padding: "25px 12px 8px",
          },
        },
        {
          props: ({ ownerState, size }) =>
            ownerState.multiline && size === "small",
          style: {
            paddingTop: 21,
            paddingBottom: 4,
          },
        },
        {
          props: ({ ownerState }) =>
            ownerState.multiline && ownerState.hiddenLabel,
          style: {
            paddingTop: 16,
            paddingBottom: 17,
          },
        },
        {
          props: ({ ownerState }) =>
            ownerState.multiline &&
            ownerState.hiddenLabel &&
            ownerState.size === "small",
          style: {
            paddingTop: 8,
            paddingBottom: 9,
          },
        },
      ],
    };
  }),
);
var FilledInputInput = styled_default2(InputBaseInput, {
  name: "MuiFilledInput",
  slot: "Input",
  overridesResolver: inputOverridesResolver,
})(
  memoTheme_default(({ theme: theme2 }) => ({
    paddingTop: 25,
    paddingRight: 12,
    paddingBottom: 8,
    paddingLeft: 12,
    ...(!theme2.vars && {
      "&:-webkit-autofill": {
        WebkitBoxShadow:
          theme2.palette.mode === "light" ? null : "0 0 0 100px #266798 inset",
        WebkitTextFillColor: theme2.palette.mode === "light" ? null : "#fff",
        caretColor: theme2.palette.mode === "light" ? null : "#fff",
        borderTopLeftRadius: "inherit",
        borderTopRightRadius: "inherit",
      },
    }),
    ...(theme2.vars && {
      "&:-webkit-autofill": {
        borderTopLeftRadius: "inherit",
        borderTopRightRadius: "inherit",
      },
      [theme2.getColorSchemeSelector("dark")]: {
        "&:-webkit-autofill": {
          WebkitBoxShadow: "0 0 0 100px #266798 inset",
          WebkitTextFillColor: "#fff",
          caretColor: "#fff",
        },
      },
    }),
    variants: [
      {
        props: {
          size: "small",
        },
        style: {
          paddingTop: 21,
          paddingBottom: 4,
        },
      },
      {
        props: ({ ownerState }) => ownerState.hiddenLabel,
        style: {
          paddingTop: 16,
          paddingBottom: 17,
        },
      },
      {
        props: ({ ownerState }) => ownerState.startAdornment,
        style: {
          paddingLeft: 0,
        },
      },
      {
        props: ({ ownerState }) => ownerState.endAdornment,
        style: {
          paddingRight: 0,
        },
      },
      {
        props: ({ ownerState }) =>
          ownerState.hiddenLabel && ownerState.size === "small",
        style: {
          paddingTop: 8,
          paddingBottom: 9,
        },
      },
      {
        props: ({ ownerState }) => ownerState.multiline,
        style: {
          paddingTop: 0,
          paddingBottom: 0,
          paddingLeft: 0,
          paddingRight: 0,
        },
      },
    ],
  })),
);
var FilledInput = /* @__PURE__ */ React71.forwardRef(
  function FilledInput2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiFilledInput",
    });
    const {
      disableUnderline = false,
      components = {},
      componentsProps: componentsPropsProp,
      fullWidth = false,
      hiddenLabel,
      // declare here to prevent spreading to DOM
      inputComponent = "input",
      multiline = false,
      slotProps,
      slots = {},
      type = "text",
      ...other
    } = props;
    const ownerState = {
      ...props,
      disableUnderline,
      fullWidth,
      inputComponent,
      multiline,
      type,
    };
    const classes = useUtilityClasses21(props);
    const filledInputComponentsProps = {
      root: {
        ownerState,
      },
      input: {
        ownerState,
      },
    };
    const componentsProps =
      (slotProps ?? componentsPropsProp)
        ? deepmerge(
            filledInputComponentsProps,
            slotProps ?? componentsPropsProp,
          )
        : filledInputComponentsProps;
    const RootSlot = slots.root ?? components.Root ?? FilledInputRoot;
    const InputSlot = slots.input ?? components.Input ?? FilledInputInput;
    return /* @__PURE__ */ (0, import_jsx_runtime50.jsx)(InputBase_default, {
      slots: {
        root: RootSlot,
        input: InputSlot,
      },
      slotProps: componentsProps,
      fullWidth,
      inputComponent,
      multiline,
      ref,
      type,
      ...other,
      classes,
    });
  },
);
false
  ? (FilledInput.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * This prop helps users to fill forms faster, especially on mobile devices.
       * The name can be confusing, as it's more like an autofill.
       * You can learn more about it [following the specification](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#autofill).
       */
      autoComplete: import_prop_types.default.string,
      /**
       * If `true`, the `input` element is focused during the first mount.
       */
      autoFocus: import_prop_types.default.bool,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * The color of the component.
       * It supports both default and custom theme colors, which can be added as shown in the
       * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
       * The prop defaults to the value (`'primary'`) inherited from the parent FormControl component.
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["primary", "secondary"]),
        import_prop_types.default.string,
      ]),
      /**
       * The components used for each slot inside.
       *
       * @deprecated use the `slots` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       *
       * @default {}
       */
      components: import_prop_types.default.shape({
        Input: import_prop_types.default.elementType,
        Root: import_prop_types.default.elementType,
      }),
      /**
       * The extra props for the slot components.
       * You can override the existing props or add new ones.
       *
       * @deprecated use the `slotProps` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       *
       * @default {}
       */
      componentsProps: import_prop_types.default.shape({
        input: import_prop_types.default.object,
        root: import_prop_types.default.object,
      }),
      /**
       * The default value. Use when the component is not controlled.
       */
      defaultValue: import_prop_types.default.any,
      /**
       * If `true`, the component is disabled.
       * The prop defaults to the value (`false`) inherited from the parent FormControl component.
       */
      disabled: import_prop_types.default.bool,
      /**
       * If `true`, the input will not have an underline.
       * @default false
       */
      disableUnderline: import_prop_types.default.bool,
      /**
       * End `InputAdornment` for this component.
       */
      endAdornment: import_prop_types.default.node,
      /**
       * If `true`, the `input` will indicate an error.
       * The prop defaults to the value (`false`) inherited from the parent FormControl component.
       */
      error: import_prop_types.default.bool,
      /**
       * If `true`, the `input` will take up the full width of its container.
       * @default false
       */
      fullWidth: import_prop_types.default.bool,
      /**
       * If `true`, the label is hidden.
       * This is used to increase density for a `FilledInput`.
       * Be sure to add `aria-label` to the `input` element.
       * @default false
       */
      hiddenLabel: import_prop_types.default.bool,
      /**
       * The id of the `input` element.
       */
      id: import_prop_types.default.string,
      /**
       * The component used for the `input` element.
       * Either a string to use a HTML element or a component.
       * @default 'input'
       */
      inputComponent: import_prop_types.default.elementType,
      /**
       * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
       * @default {}
       */
      inputProps: import_prop_types.default.object,
      /**
       * Pass a ref to the `input` element.
       */
      inputRef: refType_default,
      /**
       * If `dense`, will adjust vertical spacing. This is normally obtained via context from
       * FormControl.
       * The prop defaults to the value (`'none'`) inherited from the parent FormControl component.
       */
      margin: import_prop_types.default.oneOf(["dense", "none"]),
      /**
       * Maximum number of rows to display when multiline option is set to true.
       */
      maxRows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * Minimum number of rows to display when multiline option is set to true.
       */
      minRows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * If `true`, a [TextareaAutosize](https://mui.com/material-ui/react-textarea-autosize/) element is rendered.
       * @default false
       */
      multiline: import_prop_types.default.bool,
      /**
       * Name attribute of the `input` element.
       */
      name: import_prop_types.default.string,
      /**
       * Callback fired when the value is changed.
       *
       * @param {React.ChangeEvent<HTMLTextAreaElement | HTMLInputElement>} event The event source of the callback.
       * You can pull out the new value by accessing `event.target.value` (string).
       */
      onChange: import_prop_types.default.func,
      /**
       * The short hint displayed in the `input` before the user enters a value.
       */
      placeholder: import_prop_types.default.string,
      /**
       * It prevents the user from changing the value of the field
       * (not from interacting with the field).
       */
      readOnly: import_prop_types.default.bool,
      /**
       * If `true`, the `input` element is required.
       * The prop defaults to the value (`false`) inherited from the parent FormControl component.
       */
      required: import_prop_types.default.bool,
      /**
       * Number of rows to display when multiline option is set to true.
       */
      rows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * The extra props for the slot components.
       * You can override the existing props or add new ones.
       *
       * This prop is an alias for the `componentsProps` prop, which will be deprecated in the future.
       *
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        input: import_prop_types.default.object,
        root: import_prop_types.default.object,
      }),
      /**
       * The components used for each slot inside.
       *
       * This prop is an alias for the `components` prop, which will be deprecated in the future.
       *
       * @default {}
       */
      slots: import_prop_types.default.shape({
        input: import_prop_types.default.elementType,
        root: import_prop_types.default.elementType,
      }),
      /**
       * Start `InputAdornment` for this component.
       */
      startAdornment: import_prop_types.default.node,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * Type of the `input` element. It should be [a valid HTML5 input type](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#input_types).
       * @default 'text'
       */
      type: import_prop_types.default.string,
      /**
       * The value of the `input` element, required for a controlled component.
       */
      value: import_prop_types.default.any,
    })
  : void 0;
FilledInput.muiName = "Input";
var FilledInput_default = FilledInput;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/FormControl/FormControl.js
var React72 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/FormControl/formControlClasses.js
function getFormControlUtilityClasses(slot) {
  return generateUtilityClass("MuiFormControl", slot);
}
var formControlClasses = generateUtilityClasses("MuiFormControl", [
  "root",
  "marginNone",
  "marginNormal",
  "marginDense",
  "fullWidth",
  "disabled",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/FormControl/FormControl.js
var import_jsx_runtime51 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses22 = (ownerState) => {
  const { classes, margin: margin2, fullWidth } = ownerState;
  const slots = {
    root: [
      "root",
      margin2 !== "none" && `margin${capitalize_default(margin2)}`,
      fullWidth && "fullWidth",
    ],
  };
  return composeClasses(slots, getFormControlUtilityClasses, classes);
};
var FormControlRoot = styled_default2("div", {
  name: "MuiFormControl",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      styles5[`margin${capitalize_default(ownerState.margin)}`],
      ownerState.fullWidth && styles5.fullWidth,
    ];
  },
})({
  display: "inline-flex",
  flexDirection: "column",
  position: "relative",
  // Reset fieldset default style.
  minWidth: 0,
  padding: 0,
  margin: 0,
  border: 0,
  verticalAlign: "top",
  // Fix alignment issue on Safari.
  variants: [
    {
      props: {
        margin: "normal",
      },
      style: {
        marginTop: 16,
        marginBottom: 8,
      },
    },
    {
      props: {
        margin: "dense",
      },
      style: {
        marginTop: 8,
        marginBottom: 4,
      },
    },
    {
      props: {
        fullWidth: true,
      },
      style: {
        width: "100%",
      },
    },
  ],
});
var FormControl = /* @__PURE__ */ React72.forwardRef(
  function FormControl2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiFormControl",
    });
    const {
      children,
      className,
      color: color2 = "primary",
      component = "div",
      disabled = false,
      error = false,
      focused: visuallyFocused,
      fullWidth = false,
      hiddenLabel = false,
      margin: margin2 = "none",
      required = false,
      size = "medium",
      variant = "outlined",
      ...other
    } = props;
    const ownerState = {
      ...props,
      color: color2,
      component,
      disabled,
      error,
      fullWidth,
      hiddenLabel,
      margin: margin2,
      required,
      size,
      variant,
    };
    const classes = useUtilityClasses22(ownerState);
    const [adornedStart, setAdornedStart] = React72.useState(() => {
      let initialAdornedStart = false;
      if (children) {
        React72.Children.forEach(children, (child) => {
          if (!isMuiElement_default(child, ["Input", "Select"])) {
            return;
          }
          const input = isMuiElement_default(child, ["Select"])
            ? child.props.input
            : child;
          if (input && isAdornedStart(input.props)) {
            initialAdornedStart = true;
          }
        });
      }
      return initialAdornedStart;
    });
    const [filled, setFilled] = React72.useState(() => {
      let initialFilled = false;
      if (children) {
        React72.Children.forEach(children, (child) => {
          if (!isMuiElement_default(child, ["Input", "Select"])) {
            return;
          }
          if (
            isFilled(child.props, true) ||
            isFilled(child.props.inputProps, true)
          ) {
            initialFilled = true;
          }
        });
      }
      return initialFilled;
    });
    const [focusedState, setFocused] = React72.useState(false);
    if (disabled && focusedState) {
      setFocused(false);
    }
    const focused =
      visuallyFocused !== void 0 && !disabled ? visuallyFocused : focusedState;
    let registerEffect;
    const registeredInput = React72.useRef(false);
    if (false) {
      registerEffect = () => {
        if (registeredInput.current) {
          console.error(
            [
              "MUI: There are multiple `InputBase` components inside a FormControl.",
              "This creates visual inconsistencies, only use one `InputBase`.",
            ].join("\n"),
          );
        }
        registeredInput.current = true;
        return () => {
          registeredInput.current = false;
        };
      };
    }
    const onFilled = React72.useCallback(() => {
      setFilled(true);
    }, []);
    const onEmpty = React72.useCallback(() => {
      setFilled(false);
    }, []);
    const childContext = React72.useMemo(() => {
      return {
        adornedStart,
        setAdornedStart,
        color: color2,
        disabled,
        error,
        filled,
        focused,
        fullWidth,
        hiddenLabel,
        size,
        onBlur: () => {
          setFocused(false);
        },
        onFocus: () => {
          setFocused(true);
        },
        onEmpty,
        onFilled,
        registerEffect,
        required,
        variant,
      };
    }, [
      adornedStart,
      color2,
      disabled,
      error,
      filled,
      focused,
      fullWidth,
      hiddenLabel,
      registerEffect,
      onEmpty,
      onFilled,
      required,
      size,
      variant,
    ]);
    return /* @__PURE__ */ (0, import_jsx_runtime51.jsx)(
      FormControlContext_default.Provider,
      {
        value: childContext,
        children: /* @__PURE__ */ (0, import_jsx_runtime51.jsx)(
          FormControlRoot,
          {
            as: component,
            ownerState,
            className: clsx_default(classes.root, className),
            ref,
            ...other,
            children,
          },
        ),
      },
    );
  },
);
false
  ? (FormControl.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The color of the component.
       * It supports both default and custom theme colors, which can be added as shown in the
       * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
       * @default 'primary'
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "primary",
          "secondary",
          "error",
          "info",
          "success",
          "warning",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * If `true`, the label, input and helper text should be displayed in a disabled state.
       * @default false
       */
      disabled: import_prop_types.default.bool,
      /**
       * If `true`, the label is displayed in an error state.
       * @default false
       */
      error: import_prop_types.default.bool,
      /**
       * If `true`, the component is displayed in focused state.
       */
      focused: import_prop_types.default.bool,
      /**
       * If `true`, the component will take up the full width of its container.
       * @default false
       */
      fullWidth: import_prop_types.default.bool,
      /**
       * If `true`, the label is hidden.
       * This is used to increase density for a `FilledInput`.
       * Be sure to add `aria-label` to the `input` element.
       * @default false
       */
      hiddenLabel: import_prop_types.default.bool,
      /**
       * If `dense` or `normal`, will adjust vertical spacing of this and contained components.
       * @default 'none'
       */
      margin: import_prop_types.default.oneOf(["dense", "none", "normal"]),
      /**
       * If `true`, the label will indicate that the `input` is required.
       * @default false
       */
      required: import_prop_types.default.bool,
      /**
       * The size of the component.
       * @default 'medium'
       */
      size: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["medium", "small"]),
        import_prop_types.default.string,
      ]),
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * The variant to use.
       * @default 'outlined'
       */
      variant: import_prop_types.default.oneOf([
        "filled",
        "outlined",
        "standard",
      ]),
    })
  : void 0;
var FormControl_default = FormControl;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/FormHelperText/FormHelperText.js
var React73 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/FormHelperText/formHelperTextClasses.js
function getFormHelperTextUtilityClasses(slot) {
  return generateUtilityClass("MuiFormHelperText", slot);
}
var formHelperTextClasses = generateUtilityClasses("MuiFormHelperText", [
  "root",
  "error",
  "disabled",
  "sizeSmall",
  "sizeMedium",
  "contained",
  "focused",
  "filled",
  "required",
]);
var formHelperTextClasses_default = formHelperTextClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/FormHelperText/FormHelperText.js
var import_jsx_runtime52 = __toESM(require_jsx_runtime(), 1);
var _span;
var useUtilityClasses23 = (ownerState) => {
  const {
    classes,
    contained,
    size,
    disabled,
    error,
    filled,
    focused,
    required,
  } = ownerState;
  const slots = {
    root: [
      "root",
      disabled && "disabled",
      error && "error",
      size && `size${capitalize_default(size)}`,
      contained && "contained",
      focused && "focused",
      filled && "filled",
      required && "required",
    ],
  };
  return composeClasses(slots, getFormHelperTextUtilityClasses, classes);
};
var FormHelperTextRoot = styled_default2("p", {
  name: "MuiFormHelperText",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      ownerState.size && styles5[`size${capitalize_default(ownerState.size)}`],
      ownerState.contained && styles5.contained,
      ownerState.filled && styles5.filled,
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    color: (theme2.vars || theme2).palette.text.secondary,
    ...theme2.typography.caption,
    textAlign: "left",
    marginTop: 3,
    marginRight: 0,
    marginBottom: 0,
    marginLeft: 0,
    [`&.${formHelperTextClasses_default.disabled}`]: {
      color: (theme2.vars || theme2).palette.text.disabled,
    },
    [`&.${formHelperTextClasses_default.error}`]: {
      color: (theme2.vars || theme2).palette.error.main,
    },
    variants: [
      {
        props: {
          size: "small",
        },
        style: {
          marginTop: 4,
        },
      },
      {
        props: ({ ownerState }) => ownerState.contained,
        style: {
          marginLeft: 14,
          marginRight: 14,
        },
      },
    ],
  })),
);
var FormHelperText = /* @__PURE__ */ React73.forwardRef(
  function FormHelperText2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiFormHelperText",
    });
    const {
      children,
      className,
      component = "p",
      disabled,
      error,
      filled,
      focused,
      margin: margin2,
      required,
      variant,
      ...other
    } = props;
    const muiFormControl = useFormControl();
    const fcs = formControlState({
      props,
      muiFormControl,
      states: [
        "variant",
        "size",
        "disabled",
        "error",
        "filled",
        "focused",
        "required",
      ],
    });
    const ownerState = {
      ...props,
      component,
      contained: fcs.variant === "filled" || fcs.variant === "outlined",
      variant: fcs.variant,
      size: fcs.size,
      disabled: fcs.disabled,
      error: fcs.error,
      filled: fcs.filled,
      focused: fcs.focused,
      required: fcs.required,
    };
    delete ownerState.ownerState;
    const classes = useUtilityClasses23(ownerState);
    return /* @__PURE__ */ (0, import_jsx_runtime52.jsx)(FormHelperTextRoot, {
      as: component,
      className: clsx_default(classes.root, className),
      ref,
      ...other,
      ownerState,
      children:
        children === " "
          ? // notranslate needed while Google Translate will not fix zero-width space issue
            _span ||
            (_span = /* @__PURE__ */ (0, import_jsx_runtime52.jsx)("span", {
              className: "notranslate",
              "aria-hidden": true,
              children: "\u200B",
            }))
          : children,
    });
  },
);
false
  ? (FormHelperText.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The content of the component.
       *
       * If `' '` is provided, the component reserves one line height for displaying a future message.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * If `true`, the helper text should be displayed in a disabled state.
       */
      disabled: import_prop_types.default.bool,
      /**
       * If `true`, helper text should be displayed in an error state.
       */
      error: import_prop_types.default.bool,
      /**
       * If `true`, the helper text should use filled classes key.
       */
      filled: import_prop_types.default.bool,
      /**
       * If `true`, the helper text should use focused classes key.
       */
      focused: import_prop_types.default.bool,
      /**
       * If `dense`, will adjust vertical spacing. This is normally obtained via context from
       * FormControl.
       */
      margin: import_prop_types.default.oneOf(["dense"]),
      /**
       * If `true`, the helper text should use required classes key.
       */
      required: import_prop_types.default.bool,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * The variant to use.
       */
      variant: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["filled", "outlined", "standard"]),
        import_prop_types.default.string,
      ]),
    })
  : void 0;
var FormHelperText_default = FormHelperText;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/FormLabel/FormLabel.js
var React74 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/FormLabel/formLabelClasses.js
function getFormLabelUtilityClasses(slot) {
  return generateUtilityClass("MuiFormLabel", slot);
}
var formLabelClasses = generateUtilityClasses("MuiFormLabel", [
  "root",
  "colorSecondary",
  "focused",
  "disabled",
  "error",
  "filled",
  "required",
  "asterisk",
]);
var formLabelClasses_default = formLabelClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/FormLabel/FormLabel.js
var import_jsx_runtime53 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses24 = (ownerState) => {
  const {
    classes,
    color: color2,
    focused,
    disabled,
    error,
    filled,
    required,
  } = ownerState;
  const slots = {
    root: [
      "root",
      `color${capitalize_default(color2)}`,
      disabled && "disabled",
      error && "error",
      filled && "filled",
      focused && "focused",
      required && "required",
    ],
    asterisk: ["asterisk", error && "error"],
  };
  return composeClasses(slots, getFormLabelUtilityClasses, classes);
};
var FormLabelRoot = styled_default2("label", {
  name: "MuiFormLabel",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      ownerState.color === "secondary" && styles5.colorSecondary,
      ownerState.filled && styles5.filled,
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    color: (theme2.vars || theme2).palette.text.secondary,
    ...theme2.typography.body1,
    lineHeight: "1.4375em",
    padding: 0,
    position: "relative",
    variants: [
      ...Object.entries(theme2.palette)
        .filter(createSimplePaletteValueFilter())
        .map(([color2]) => ({
          props: {
            color: color2,
          },
          style: {
            [`&.${formLabelClasses_default.focused}`]: {
              color: (theme2.vars || theme2).palette[color2].main,
            },
          },
        })),
      {
        props: {},
        style: {
          [`&.${formLabelClasses_default.disabled}`]: {
            color: (theme2.vars || theme2).palette.text.disabled,
          },
          [`&.${formLabelClasses_default.error}`]: {
            color: (theme2.vars || theme2).palette.error.main,
          },
        },
      },
    ],
  })),
);
var AsteriskComponent = styled_default2("span", {
  name: "MuiFormLabel",
  slot: "Asterisk",
})(
  memoTheme_default(({ theme: theme2 }) => ({
    [`&.${formLabelClasses_default.error}`]: {
      color: (theme2.vars || theme2).palette.error.main,
    },
  })),
);
var FormLabel = /* @__PURE__ */ React74.forwardRef(
  function FormLabel2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiFormLabel",
    });
    const {
      children,
      className,
      color: color2,
      component = "label",
      disabled,
      error,
      filled,
      focused,
      required,
      ...other
    } = props;
    const muiFormControl = useFormControl();
    const fcs = formControlState({
      props,
      muiFormControl,
      states: ["color", "required", "focused", "disabled", "error", "filled"],
    });
    const ownerState = {
      ...props,
      color: fcs.color || "primary",
      component,
      disabled: fcs.disabled,
      error: fcs.error,
      filled: fcs.filled,
      focused: fcs.focused,
      required: fcs.required,
    };
    const classes = useUtilityClasses24(ownerState);
    return /* @__PURE__ */ (0, import_jsx_runtime53.jsxs)(FormLabelRoot, {
      as: component,
      ownerState,
      className: clsx_default(classes.root, className),
      ref,
      ...other,
      children: [
        children,
        fcs.required &&
          /* @__PURE__ */ (0, import_jsx_runtime53.jsxs)(AsteriskComponent, {
            ownerState,
            "aria-hidden": true,
            className: classes.asterisk,
            children: ["\u2009", "*"],
          }),
      ],
    });
  },
);
false
  ? (FormLabel.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The color of the component.
       * It supports both default and custom theme colors, which can be added as shown in the
       * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "error",
          "info",
          "primary",
          "secondary",
          "success",
          "warning",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * If `true`, the label should be displayed in a disabled state.
       */
      disabled: import_prop_types.default.bool,
      /**
       * If `true`, the label is displayed in an error state.
       */
      error: import_prop_types.default.bool,
      /**
       * If `true`, the label should use filled classes key.
       */
      filled: import_prop_types.default.bool,
      /**
       * If `true`, the input of this label is focused (used by `FormGroup` components).
       */
      focused: import_prop_types.default.bool,
      /**
       * If `true`, the label will indicate that the `input` is required.
       */
      required: import_prop_types.default.bool,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
    })
  : void 0;
var FormLabel_default = FormLabel;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Grow/Grow.js
var React75 = __toESM(require_react(), 1);
var import_jsx_runtime54 = __toESM(require_jsx_runtime(), 1);
function getScale(value) {
  return `scale(${value}, ${value ** 2})`;
}
var styles4 = {
  entering: {
    opacity: 1,
    transform: getScale(1),
  },
  entered: {
    opacity: 1,
    transform: "none",
  },
  exiting: {
    opacity: 0,
    transform: getScale(0.75),
  },
  exited: {
    opacity: 0,
    transform: getScale(0.75),
  },
};
var hiddenStyles3 = {
  opacity: 0,
  transform: getScale(0.75),
  visibility: "hidden",
};
var Grow = /* @__PURE__ */ React75.forwardRef(function Grow2(props, ref) {
  const {
    addEndListener,
    appear = true,
    children,
    easing: easing2,
    in: inProp,
    onEnter,
    onEntered,
    onEntering,
    onExit,
    onExited,
    onExiting,
    style: style4,
    timeout: timeout2 = "auto",
    ...other
  } = props;
  const timer = useTimeout();
  const autoTimeout = React75.useRef();
  const theme2 = useTheme5();
  const nodeRef = React75.useRef(null);
  const handleRef = useForkRef_default(
    nodeRef,
    getReactElementRef(children),
    ref,
  );
  const handleEntering = normalizedTransitionCallback(nodeRef, onEntering);
  const handleEnter = normalizedTransitionCallback(
    nodeRef,
    (node2, isAppearing) => {
      reflow(node2);
      const {
        duration: transitionDuration,
        delay,
        easing: transitionTimingFunction,
      } = getTransitionProps(
        {
          style: style4,
          timeout: timeout2,
          easing: easing2,
        },
        {
          mode: "enter",
        },
      );
      let duration2;
      if (timeout2 === "auto") {
        duration2 = theme2.transitions.getAutoHeightDuration(
          node2.clientHeight,
        );
        autoTimeout.current = duration2;
      } else {
        duration2 = transitionDuration;
      }
      node2.style.transition = [
        theme2.transitions.create("opacity", {
          duration: duration2,
          delay,
        }),
        theme2.transitions.create("transform", {
          duration: duration2 * 0.666,
          delay,
          easing: transitionTimingFunction,
        }),
      ].join(",");
      if (onEnter) {
        onEnter(node2, isAppearing);
      }
    },
  );
  const handleEntered = normalizedTransitionCallback(nodeRef, onEntered);
  const handleExiting = normalizedTransitionCallback(nodeRef, onExiting);
  const handleExit = normalizedTransitionCallback(nodeRef, (node2) => {
    const {
      duration: transitionDuration,
      delay,
      easing: transitionTimingFunction,
    } = getTransitionProps(
      {
        style: style4,
        timeout: timeout2,
        easing: easing2,
      },
      {
        mode: "exit",
      },
    );
    let duration2;
    if (timeout2 === "auto") {
      duration2 = theme2.transitions.getAutoHeightDuration(node2.clientHeight);
      autoTimeout.current = duration2;
    } else {
      duration2 = transitionDuration;
    }
    node2.style.transition = [
      theme2.transitions.create("opacity", {
        duration: duration2,
        delay,
      }),
      theme2.transitions.create("transform", {
        duration: duration2 * 0.666,
        delay: delay || duration2 * 0.333,
        easing: transitionTimingFunction,
      }),
    ].join(",");
    node2.style.opacity = 0;
    node2.style.transform = getScale(0.75);
    if (onExit) {
      onExit(node2);
    }
  });
  const handleExited = normalizedTransitionCallback(nodeRef, (node2) => {
    node2.style.transition = "";
    if (onExited) {
      onExited(node2);
    }
  });
  const handleAddEndListener = (next2) => {
    if (timeout2 === "auto") {
      timer.start(autoTimeout.current || 0, next2);
    }
    if (addEndListener) {
      addEndListener(nodeRef.current, next2);
    }
  };
  return /* @__PURE__ */ (0, import_jsx_runtime54.jsx)(Transition_default, {
    appear,
    in: inProp,
    nodeRef,
    onEnter: handleEnter,
    onEntered: handleEntered,
    onEntering: handleEntering,
    onExit: handleExit,
    onExited: handleExited,
    onExiting: handleExiting,
    addEndListener: handleAddEndListener,
    timeout: timeout2 === "auto" ? null : timeout2,
    ...other,
    children: (state, { ownerState, ...restChildProps }) => {
      const childStyle = getTransitionChildStyle(
        state,
        inProp,
        styles4,
        hiddenStyles3,
        style4,
        children.props.style,
      );
      return /* @__PURE__ */ React75.cloneElement(children, {
        style: childStyle,
        ref: handleRef,
        ...restChildProps,
      });
    },
  });
});
false
  ? (Grow.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * Add a custom transition end trigger. Called with the transitioning DOM
       * node and a done callback. Allows for more fine grained transition end
       * logic. Note: Timeouts are still used as a fallback if provided.
       */
      addEndListener: import_prop_types.default.func,
      /**
       * Perform the enter transition when it first mounts if `in` is also `true`.
       * Set this to `false` to disable this behavior.
       * @default true
       */
      appear: import_prop_types.default.bool,
      /**
       * A single child content element.
       */
      children: elementAcceptingRef_default.isRequired,
      /**
       * The transition timing function.
       * You may specify a single easing or a object containing enter and exit values.
       */
      easing: import_prop_types.default.oneOfType([
        import_prop_types.default.shape({
          enter: import_prop_types.default.string,
          exit: import_prop_types.default.string,
        }),
        import_prop_types.default.string,
      ]),
      /**
       * If `true`, the component will transition in.
       */
      in: import_prop_types.default.bool,
      /**
       * @ignore
       */
      onEnter: import_prop_types.default.func,
      /**
       * @ignore
       */
      onEntered: import_prop_types.default.func,
      /**
       * @ignore
       */
      onEntering: import_prop_types.default.func,
      /**
       * @ignore
       */
      onExit: import_prop_types.default.func,
      /**
       * @ignore
       */
      onExited: import_prop_types.default.func,
      /**
       * @ignore
       */
      onExiting: import_prop_types.default.func,
      /**
       * @ignore
       */
      style: import_prop_types.default.object,
      /**
       * The duration for the transition, in milliseconds.
       * You may specify a single timeout for all transitions, or individually with an object.
       *
       * Set to 'auto' to automatically calculate transition time based on height.
       * @default 'auto'
       */
      timeout: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["auto"]),
        import_prop_types.default.number,
        import_prop_types.default.shape({
          appear: import_prop_types.default.number,
          enter: import_prop_types.default.number,
          exit: import_prop_types.default.number,
        }),
      ]),
    })
  : void 0;
if (Grow) {
  Grow.muiSupportAuto = true;
}
var Grow_default = Grow;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Input/Input.js
var React76 = __toESM(require_react(), 1);
var import_jsx_runtime55 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses25 = (ownerState) => {
  const { classes, disableUnderline } = ownerState;
  const slots = {
    root: ["root", !disableUnderline && "underline"],
    input: ["input"],
  };
  const composedClasses = composeClasses(slots, getInputUtilityClass, classes);
  return {
    ...classes,
    // forward classes to the InputBase
    ...composedClasses,
  };
};
var InputRoot = styled_default2(InputBaseRoot, {
  shouldForwardProp: (prop) =>
    rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiInput",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      ...rootOverridesResolver(props, styles5),
      !ownerState.disableUnderline && styles5.underline,
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => {
    const light2 = theme2.palette.mode === "light";
    let bottomLineColor = light2
      ? "rgba(0, 0, 0, 0.42)"
      : "rgba(255, 255, 255, 0.7)";
    if (theme2.vars) {
      bottomLineColor = theme2.alpha(
        theme2.vars.palette.common.onBackground,
        theme2.vars.opacity.inputUnderline,
      );
    }
    return {
      position: "relative",
      variants: [
        {
          props: ({ ownerState }) => ownerState.formControl,
          style: {
            "label + &": {
              marginTop: 16,
            },
          },
        },
        {
          props: ({ ownerState }) => !ownerState.disableUnderline,
          style: {
            "&::after": {
              left: 0,
              bottom: 0,
              content: '""',
              position: "absolute",
              right: 0,
              transform: "scaleX(0)",
              transition: theme2.transitions.create("transform", {
                duration: theme2.transitions.duration.shorter,
                easing: theme2.transitions.easing.easeOut,
              }),
              pointerEvents: "none",
              // Transparent to the hover style.
            },
            [`&.${inputClasses_default.focused}:after`]: {
              // translateX(0) is a workaround for Safari transform scale bug
              // See https://github.com/mui/material-ui/issues/31766
              transform: "scaleX(1) translateX(0)",
            },
            [`&.${inputClasses_default.error}`]: {
              "&::before, &::after": {
                borderBottomColor: (theme2.vars || theme2).palette.error.main,
              },
            },
            "&::before": {
              borderBottom: `1px solid ${bottomLineColor}`,
              left: 0,
              bottom: 0,
              content: '""',
              position: "absolute",
              right: 0,
              transition: theme2.transitions.create("border-bottom-color", {
                duration: theme2.transitions.duration.shorter,
              }),
              pointerEvents: "none",
              // Transparent to the hover style.
            },
            [`&:hover:not(.${inputClasses_default.disabled}, .${inputClasses_default.error}):before`]:
              {
                borderBottom: `2px solid ${(theme2.vars || theme2).palette.text.primary}`,
                // Reset on touch devices, it doesn't add specificity
                "@media (hover: none)": {
                  borderBottom: `1px solid ${bottomLineColor}`,
                },
              },
            [`&.${inputClasses_default.disabled}:before`]: {
              borderBottomStyle: "dotted",
            },
          },
        },
        ...Object.entries(theme2.palette)
          .filter(createSimplePaletteValueFilter())
          .map(([color2]) => ({
            props: {
              color: color2,
              disableUnderline: false,
            },
            style: {
              "&::after": {
                borderBottom: `2px solid ${(theme2.vars || theme2).palette[color2].main}`,
              },
            },
          })),
      ],
    };
  }),
);
var InputInput = styled_default2(InputBaseInput, {
  name: "MuiInput",
  slot: "Input",
  overridesResolver: inputOverridesResolver,
})({});
var Input = /* @__PURE__ */ React76.forwardRef(function Input2(inProps, ref) {
  const props = useDefaultProps2({
    props: inProps,
    name: "MuiInput",
  });
  const {
    disableUnderline = false,
    components = {},
    componentsProps: componentsPropsProp,
    fullWidth = false,
    inputComponent = "input",
    multiline = false,
    slotProps,
    slots = {},
    type = "text",
    ...other
  } = props;
  const classes = useUtilityClasses25(props);
  const ownerState = {
    disableUnderline,
  };
  const inputComponentsProps = {
    root: {
      ownerState,
    },
  };
  const componentsProps =
    (slotProps ?? componentsPropsProp)
      ? deepmerge(slotProps ?? componentsPropsProp, inputComponentsProps)
      : inputComponentsProps;
  const RootSlot = slots.root ?? components.Root ?? InputRoot;
  const InputSlot = slots.input ?? components.Input ?? InputInput;
  return /* @__PURE__ */ (0, import_jsx_runtime55.jsx)(InputBase_default, {
    slots: {
      root: RootSlot,
      input: InputSlot,
    },
    slotProps: componentsProps,
    fullWidth,
    inputComponent,
    multiline,
    ref,
    type,
    ...other,
    classes,
  });
});
false
  ? (Input.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * This prop helps users to fill forms faster, especially on mobile devices.
       * The name can be confusing, as it's more like an autofill.
       * You can learn more about it [following the specification](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#autofill).
       */
      autoComplete: import_prop_types.default.string,
      /**
       * If `true`, the `input` element is focused during the first mount.
       */
      autoFocus: import_prop_types.default.bool,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * The color of the component.
       * It supports both default and custom theme colors, which can be added as shown in the
       * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
       * The prop defaults to the value (`'primary'`) inherited from the parent FormControl component.
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["primary", "secondary"]),
        import_prop_types.default.string,
      ]),
      /**
       * The components used for each slot inside.
       *
       * @deprecated use the `slots` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       *
       * @default {}
       */
      components: import_prop_types.default.shape({
        Input: import_prop_types.default.elementType,
        Root: import_prop_types.default.elementType,
      }),
      /**
       * The extra props for the slot components.
       * You can override the existing props or add new ones.
       *
       * @deprecated use the `slotProps` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       *
       * @default {}
       */
      componentsProps: import_prop_types.default.shape({
        input: import_prop_types.default.object,
        root: import_prop_types.default.object,
      }),
      /**
       * The default value. Use when the component is not controlled.
       */
      defaultValue: import_prop_types.default.any,
      /**
       * If `true`, the component is disabled.
       * The prop defaults to the value (`false`) inherited from the parent FormControl component.
       */
      disabled: import_prop_types.default.bool,
      /**
       * If `true`, the `input` will not have an underline.
       * @default false
       */
      disableUnderline: import_prop_types.default.bool,
      /**
       * End `InputAdornment` for this component.
       */
      endAdornment: import_prop_types.default.node,
      /**
       * If `true`, the `input` will indicate an error.
       * The prop defaults to the value (`false`) inherited from the parent FormControl component.
       */
      error: import_prop_types.default.bool,
      /**
       * If `true`, the `input` will take up the full width of its container.
       * @default false
       */
      fullWidth: import_prop_types.default.bool,
      /**
       * The id of the `input` element.
       */
      id: import_prop_types.default.string,
      /**
       * The component used for the `input` element.
       * Either a string to use a HTML element or a component.
       * @default 'input'
       */
      inputComponent: import_prop_types.default.elementType,
      /**
       * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
       * @default {}
       */
      inputProps: import_prop_types.default.object,
      /**
       * Pass a ref to the `input` element.
       */
      inputRef: refType_default,
      /**
       * If `dense`, will adjust vertical spacing. This is normally obtained via context from
       * FormControl.
       * The prop defaults to the value (`'none'`) inherited from the parent FormControl component.
       */
      margin: import_prop_types.default.oneOf(["dense", "none"]),
      /**
       * Maximum number of rows to display when multiline option is set to true.
       */
      maxRows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * Minimum number of rows to display when multiline option is set to true.
       */
      minRows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * If `true`, a [TextareaAutosize](https://mui.com/material-ui/react-textarea-autosize/) element is rendered.
       * @default false
       */
      multiline: import_prop_types.default.bool,
      /**
       * Name attribute of the `input` element.
       */
      name: import_prop_types.default.string,
      /**
       * Callback fired when the value is changed.
       *
       * @param {React.ChangeEvent<HTMLTextAreaElement | HTMLInputElement>} event The event source of the callback.
       * You can pull out the new value by accessing `event.target.value` (string).
       */
      onChange: import_prop_types.default.func,
      /**
       * The short hint displayed in the `input` before the user enters a value.
       */
      placeholder: import_prop_types.default.string,
      /**
       * It prevents the user from changing the value of the field
       * (not from interacting with the field).
       */
      readOnly: import_prop_types.default.bool,
      /**
       * If `true`, the `input` element is required.
       * The prop defaults to the value (`false`) inherited from the parent FormControl component.
       */
      required: import_prop_types.default.bool,
      /**
       * Number of rows to display when multiline option is set to true.
       */
      rows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * The extra props for the slot components.
       * You can override the existing props or add new ones.
       *
       * This prop is an alias for the `componentsProps` prop, which will be deprecated in the future.
       *
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        input: import_prop_types.default.object,
        root: import_prop_types.default.object,
      }),
      /**
       * The components used for each slot inside.
       *
       * This prop is an alias for the `components` prop, which will be deprecated in the future.
       *
       * @default {}
       */
      slots: import_prop_types.default.shape({
        input: import_prop_types.default.elementType,
        root: import_prop_types.default.elementType,
      }),
      /**
       * Start `InputAdornment` for this component.
       */
      startAdornment: import_prop_types.default.node,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * Type of the `input` element. It should be [a valid HTML5 input type](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#input_types).
       * @default 'text'
       */
      type: import_prop_types.default.string,
      /**
       * The value of the `input` element, required for a controlled component.
       */
      value: import_prop_types.default.any,
    })
  : void 0;
Input.muiName = "Input";
var Input_default = Input;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/InputLabel/InputLabel.js
var React77 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/InputLabel/inputLabelClasses.js
function getInputLabelUtilityClasses(slot) {
  return generateUtilityClass("MuiInputLabel", slot);
}
var inputLabelClasses = generateUtilityClasses("MuiInputLabel", [
  "root",
  "focused",
  "disabled",
  "error",
  "required",
  "asterisk",
  "formControl",
  "sizeSmall",
  "shrink",
  "animated",
  "standard",
  "filled",
  "outlined",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/InputLabel/InputLabel.js
var import_jsx_runtime56 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses26 = (ownerState) => {
  const {
    classes,
    formControl,
    size,
    shrink,
    disableAnimation,
    variant,
    required,
  } = ownerState;
  const slots = {
    root: [
      "root",
      formControl && "formControl",
      !disableAnimation && "animated",
      shrink && "shrink",
      size && size !== "medium" && `size${capitalize_default(size)}`,
      variant,
    ],
    asterisk: [required && "asterisk"],
  };
  const composedClasses = composeClasses(
    slots,
    getInputLabelUtilityClasses,
    classes,
  );
  return {
    ...classes,
    // forward the focused, disabled, etc. classes to the FormLabel
    ...composedClasses,
  };
};
var InputLabelRoot = styled_default2(FormLabel_default, {
  shouldForwardProp: (prop) =>
    rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiInputLabel",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      {
        [`& .${formLabelClasses_default.asterisk}`]: styles5.asterisk,
      },
      styles5.root,
      ownerState.formControl && styles5.formControl,
      ownerState.size === "small" && styles5.sizeSmall,
      ownerState.shrink && styles5.shrink,
      !ownerState.disableAnimation && styles5.animated,
      ownerState.focused && styles5.focused,
      styles5[ownerState.variant],
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => ({
    display: "block",
    transformOrigin: "top left",
    whiteSpace: "nowrap",
    overflow: "hidden",
    textOverflow: "ellipsis",
    maxWidth: "100%",
    variants: [
      {
        props: ({ ownerState }) => ownerState.formControl,
        style: {
          position: "absolute",
          left: 0,
          top: 0,
          // slight alteration to spec spacing to match visual spec result
          transform: "translate(0, 20px) scale(1)",
        },
      },
      {
        props: {
          size: "small",
        },
        style: {
          // Compensation for the `Input.inputSizeSmall` style.
          transform: "translate(0, 17px) scale(1)",
        },
      },
      {
        props: ({ ownerState }) => ownerState.shrink,
        style: {
          transform: "translate(0, -1.5px) scale(0.75)",
          transformOrigin: "top left",
          maxWidth: "133%",
        },
      },
      {
        props: ({ ownerState }) => !ownerState.disableAnimation,
        style: {
          transition: theme2.transitions.create(
            ["color", "transform", "max-width"],
            {
              duration: theme2.transitions.duration.shorter,
              easing: theme2.transitions.easing.easeOut,
            },
          ),
        },
      },
      {
        props: {
          variant: "filled",
        },
        style: {
          // Chrome's autofill feature gives the input field a yellow background.
          // Since the input field is behind the label in the HTML tree,
          // the input field is drawn last and hides the label with an opaque background color.
          // zIndex: 1 will raise the label above opaque background-colors of input.
          zIndex: 1,
          pointerEvents: "none",
          transform: "translate(12px, 16px) scale(1)",
          maxWidth: "calc(100% - 24px)",
        },
      },
      {
        props: {
          variant: "filled",
          size: "small",
        },
        style: {
          transform: "translate(12px, 13px) scale(1)",
        },
      },
      {
        props: ({ variant, ownerState }) =>
          variant === "filled" && ownerState.shrink,
        style: {
          userSelect: "none",
          pointerEvents: "auto",
          transform: "translate(12px, 7px) scale(0.75)",
          maxWidth: "calc(133% - 24px)",
        },
      },
      {
        props: ({ variant, ownerState, size }) =>
          variant === "filled" && ownerState.shrink && size === "small",
        style: {
          transform: "translate(12px, 4px) scale(0.75)",
        },
      },
      {
        props: {
          variant: "outlined",
        },
        style: {
          // see comment above on filled.zIndex
          zIndex: 1,
          pointerEvents: "none",
          transform: "translate(14px, 16px) scale(1)",
          maxWidth: "calc(100% - 24px)",
        },
      },
      {
        props: {
          variant: "outlined",
          size: "small",
        },
        style: {
          transform: "translate(14px, 9px) scale(1)",
        },
      },
      {
        props: ({ variant, ownerState }) =>
          variant === "outlined" && ownerState.shrink,
        style: {
          userSelect: "none",
          pointerEvents: "auto",
          // Theoretically, we should have (8+5)*2/0.75 = 34px
          // but it feels a better when it bleeds a bit on the left, so 32px.
          maxWidth: "calc(133% - 32px)",
          transform: "translate(14px, -9px) scale(0.75)",
        },
      },
    ],
  })),
);
var InputLabel = /* @__PURE__ */ React77.forwardRef(
  function InputLabel2(inProps, ref) {
    const props = useDefaultProps2({
      name: "MuiInputLabel",
      props: inProps,
    });
    const {
      disableAnimation = false,
      margin: margin2,
      shrink: shrinkProp,
      variant,
      className,
      ...other
    } = props;
    const muiFormControl = useFormControl();
    let shrink = shrinkProp;
    if (typeof shrink === "undefined" && muiFormControl) {
      shrink =
        muiFormControl.filled ||
        muiFormControl.focused ||
        muiFormControl.adornedStart;
    }
    const fcs = formControlState({
      props,
      muiFormControl,
      states: ["size", "variant", "required", "focused"],
    });
    const ownerState = {
      ...props,
      disableAnimation,
      formControl: muiFormControl,
      shrink,
      size: fcs.size,
      variant: fcs.variant,
      required: fcs.required,
      focused: fcs.focused,
    };
    const classes = useUtilityClasses26(ownerState);
    return /* @__PURE__ */ (0, import_jsx_runtime56.jsx)(InputLabelRoot, {
      "data-shrink": shrink,
      ref,
      className: clsx_default(classes.root, className),
      ...other,
      ownerState,
      classes,
    });
  },
);
false
  ? (InputLabel.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The color of the component.
       * It supports both default and custom theme colors, which can be added as shown in the
       * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "error",
          "info",
          "primary",
          "secondary",
          "success",
          "warning",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * If `true`, the transition animation is disabled.
       * @default false
       */
      disableAnimation: import_prop_types.default.bool,
      /**
       * If `true`, the component is disabled.
       */
      disabled: import_prop_types.default.bool,
      /**
       * If `true`, the label is displayed in an error state.
       */
      error: import_prop_types.default.bool,
      /**
       * If `true`, the `input` of this label is focused.
       */
      focused: import_prop_types.default.bool,
      /**
       * If `dense`, will adjust vertical spacing. This is normally obtained via context from
       * FormControl.
       */
      margin: import_prop_types.default.oneOf(["dense"]),
      /**
       * if `true`, the label will indicate that the `input` is required.
       */
      required: import_prop_types.default.bool,
      /**
       * If `true`, the label is shrunk.
       */
      shrink: import_prop_types.default.bool,
      /**
       * The size of the component.
       * @default 'medium'
       */
      size: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["medium", "small"]),
        import_prop_types.default.string,
      ]),
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * The variant to use.
       */
      variant: import_prop_types.default.oneOf([
        "filled",
        "outlined",
        "standard",
      ]),
    })
  : void 0;
var InputLabel_default = InputLabel;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Link/Link.js
var React78 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Link/linkClasses.js
function getLinkUtilityClass(slot) {
  return generateUtilityClass("MuiLink", slot);
}
var linkClasses = generateUtilityClasses("MuiLink", [
  "root",
  "underlineNone",
  "underlineHover",
  "underlineAlways",
  "button",
  "focusVisible",
]);
var linkClasses_default = linkClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Link/getTextDecoration.js
var getTextDecoration = ({ theme: theme2, ownerState }) => {
  const transformedColor = ownerState.color;
  if ("colorSpace" in theme2 && theme2.colorSpace) {
    const color3 =
      getPath(theme2, `palette.${transformedColor}.main`) ||
      getPath(theme2, `palette.${transformedColor}`) ||
      ownerState.color;
    return theme2.alpha(color3, 0.4);
  }
  const color2 =
    getPath(theme2, `palette.${transformedColor}.main`, false) ||
    getPath(theme2, `palette.${transformedColor}`, false) ||
    ownerState.color;
  const channelColor =
    getPath(theme2, `palette.${transformedColor}.mainChannel`) ||
    getPath(theme2, `palette.${transformedColor}Channel`);
  if ("vars" in theme2 && channelColor) {
    return `rgba(${channelColor} / 0.4)`;
  }
  return alpha(color2, 0.4);
};
var getTextDecoration_default = getTextDecoration;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Link/Link.js
var import_jsx_runtime57 = __toESM(require_jsx_runtime(), 1);
var v6Colors2 = {
  primary: true,
  secondary: true,
  error: true,
  info: true,
  success: true,
  warning: true,
  textPrimary: true,
  textSecondary: true,
  textDisabled: true,
};
var useUtilityClasses27 = (ownerState) => {
  const { classes, component, focusVisible, underline } = ownerState;
  const slots = {
    root: [
      "root",
      `underline${capitalize_default(underline)}`,
      component === "button" && "button",
      focusVisible && "focusVisible",
    ],
  };
  return composeClasses(slots, getLinkUtilityClass, classes);
};
var LinkRoot = styled_default2(Typography_default, {
  name: "MuiLink",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      styles5[`underline${capitalize_default(ownerState.underline)}`],
      ownerState.component === "button" && styles5.button,
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => {
    return {
      variants: [
        {
          props: {
            underline: "none",
          },
          style: {
            textDecoration: "none",
          },
        },
        {
          props: {
            underline: "hover",
          },
          style: {
            textDecoration: "none",
            "&:hover": {
              textDecoration: "underline",
            },
          },
        },
        {
          props: {
            underline: "always",
          },
          style: {
            textDecoration: "underline",
            "&:hover": {
              textDecorationColor: "inherit",
            },
          },
        },
        {
          props: ({ underline, ownerState }) =>
            underline === "always" && ownerState.color !== "inherit",
          style: {
            textDecorationColor: "var(--Link-underlineColor)",
          },
        },
        {
          props: ({ underline, ownerState }) =>
            underline === "always" && ownerState.color === "inherit",
          style: theme2.colorSpace
            ? {
                textDecorationColor: theme2.alpha("currentColor", 0.4),
              }
            : null,
        },
        ...Object.entries(theme2.palette)
          .filter(createSimplePaletteValueFilter())
          .map(([color2]) => ({
            props: {
              underline: "always",
              color: color2,
            },
            style: {
              "--Link-underlineColor": theme2.alpha(
                (theme2.vars || theme2).palette[color2].main,
                0.4,
              ),
            },
          })),
        {
          props: {
            underline: "always",
            color: "textPrimary",
          },
          style: {
            "--Link-underlineColor": theme2.alpha(
              (theme2.vars || theme2).palette.text.primary,
              0.4,
            ),
          },
        },
        {
          props: {
            underline: "always",
            color: "textSecondary",
          },
          style: {
            "--Link-underlineColor": theme2.alpha(
              (theme2.vars || theme2).palette.text.secondary,
              0.4,
            ),
          },
        },
        {
          props: {
            underline: "always",
            color: "textDisabled",
          },
          style: {
            "--Link-underlineColor": (theme2.vars || theme2).palette.text
              .disabled,
          },
        },
        {
          props: {
            component: "button",
          },
          style: {
            position: "relative",
            WebkitTapHighlightColor: "transparent",
            backgroundColor: "transparent",
            // Reset default value
            // We disable the focus ring for mouse, touch and keyboard users.
            outline: 0,
            border: 0,
            margin: 0,
            // Remove the margin in Safari
            borderRadius: 0,
            padding: 0,
            // Remove the padding in Firefox
            cursor: "pointer",
            userSelect: "none",
            verticalAlign: "middle",
            MozAppearance: "none",
            // Reset
            WebkitAppearance: "none",
            // Reset
            "&::-moz-focus-inner": {
              borderStyle: "none",
              // Remove Firefox dotted outline.
            },
            [`&.${linkClasses_default.focusVisible}`]: {
              outline: "auto",
            },
          },
        },
      ],
    };
  }),
);
var Link = /* @__PURE__ */ React78.forwardRef(function Link2(inProps, ref) {
  const props = useDefaultProps2({
    props: inProps,
    name: "MuiLink",
  });
  const theme2 = useTheme5();
  const {
    className,
    color: color2 = "primary",
    component = "a",
    onBlur,
    onFocus,
    TypographyClasses,
    underline = "always",
    variant = "inherit",
    sx,
    ...other
  } = props;
  const [focusVisible, setFocusVisible] = React78.useState(false);
  const handleBlur = (event) => {
    if (!isFocusVisible(event.target)) {
      setFocusVisible(false);
    }
    if (onBlur) {
      onBlur(event);
    }
  };
  const handleFocus = (event) => {
    if (isFocusVisible(event.target)) {
      setFocusVisible(true);
    }
    if (onFocus) {
      onFocus(event);
    }
  };
  const ownerState = {
    ...props,
    color: color2,
    component,
    focusVisible,
    underline,
    variant,
  };
  const classes = useUtilityClasses27(ownerState);
  return /* @__PURE__ */ (0, import_jsx_runtime57.jsx)(LinkRoot, {
    color: color2,
    className: clsx_default(classes.root, className),
    classes: TypographyClasses,
    component,
    onBlur: handleBlur,
    onFocus: handleFocus,
    ref,
    ownerState,
    variant,
    ...other,
    sx: [
      ...(v6Colors2[color2] === void 0
        ? [
            {
              color: color2,
            },
          ]
        : []),
      ...(Array.isArray(sx) ? sx : [sx]),
    ],
    style: {
      ...other.style,
      ...(underline === "always" &&
        color2 !== "inherit" &&
        !v6Colors2[color2] && {
          "--Link-underlineColor": getTextDecoration_default({
            theme: theme2,
            ownerState,
          }),
        }),
    },
  });
});
false
  ? (Link.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The color of the link.
       * @default 'primary'
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "primary",
          "secondary",
          "success",
          "error",
          "info",
          "warning",
          "textPrimary",
          "textSecondary",
          "textDisabled",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: elementTypeAcceptingRef_default,
      /**
       * @ignore
       */
      onBlur: import_prop_types.default.func,
      /**
       * @ignore
       */
      onFocus: import_prop_types.default.func,
      /**
       * @ignore
       */
      style: import_prop_types.default.object,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * `classes` prop applied to the [`Typography`](https://mui.com/material-ui/api/typography/) element.
       */
      TypographyClasses: import_prop_types.default.object,
      /**
       * Controls when the link should have an underline.
       * @default 'always'
       */
      underline: import_prop_types.default.oneOf(["always", "hover", "none"]),
      /**
       * Applies the theme typography styles.
       * @default 'inherit'
       */
      variant: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "body1",
          "body2",
          "button",
          "caption",
          "h1",
          "h2",
          "h3",
          "h4",
          "h5",
          "h6",
          "inherit",
          "overline",
          "subtitle1",
          "subtitle2",
        ]),
        import_prop_types.default.string,
      ]),
    })
  : void 0;
var Link_default = Link;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/List/List.js
var React80 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/List/ListContext.js
var React79 = __toESM(require_react(), 1);
var ListContext = /* @__PURE__ */ React79.createContext({});
if (false) {
  ListContext.displayName = "ListContext";
}
var ListContext_default = ListContext;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/List/listClasses.js
function getListUtilityClass(slot) {
  return generateUtilityClass("MuiList", slot);
}
var listClasses = generateUtilityClasses("MuiList", [
  "root",
  "padding",
  "dense",
  "subheader",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/List/List.js
var import_jsx_runtime58 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses28 = (ownerState) => {
  const { classes, disablePadding, dense, subheader } = ownerState;
  const slots = {
    root: [
      "root",
      !disablePadding && "padding",
      dense && "dense",
      subheader && "subheader",
    ],
  };
  return composeClasses(slots, getListUtilityClass, classes);
};
var ListRoot = styled_default2("ul", {
  name: "MuiList",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      !ownerState.disablePadding && styles5.padding,
      ownerState.dense && styles5.dense,
      ownerState.subheader && styles5.subheader,
    ];
  },
})({
  listStyle: "none",
  margin: 0,
  padding: 0,
  position: "relative",
  variants: [
    {
      props: ({ ownerState }) => !ownerState.disablePadding,
      style: {
        paddingTop: 8,
        paddingBottom: 8,
      },
    },
    {
      props: ({ ownerState }) => ownerState.subheader,
      style: {
        paddingTop: 0,
      },
    },
  ],
});
var List = /* @__PURE__ */ React80.forwardRef(function List2(inProps, ref) {
  const props = useDefaultProps2({
    props: inProps,
    name: "MuiList",
  });
  const {
    children,
    className,
    component = "ul",
    dense = false,
    disablePadding = false,
    subheader,
    ...other
  } = props;
  const context = React80.useMemo(
    () => ({
      dense,
    }),
    [dense],
  );
  const ownerState = {
    ...props,
    component,
    dense,
    disablePadding,
  };
  const classes = useUtilityClasses28(ownerState);
  return /* @__PURE__ */ (0, import_jsx_runtime58.jsx)(
    ListContext_default.Provider,
    {
      value: context,
      children: /* @__PURE__ */ (0, import_jsx_runtime58.jsxs)(ListRoot, {
        as: component,
        className: clsx_default(classes.root, className),
        ref,
        ownerState,
        ...other,
        children: [subheader, children],
      }),
    },
  );
});
false
  ? (List.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * If `true`, compact vertical padding designed for keyboard and mouse input is used for
       * the list and list items.
       * The prop is available to descendant components as the `dense` context.
       * @default false
       */
      dense: import_prop_types.default.bool,
      /**
       * If `true`, vertical padding is removed from the list.
       * @default false
       */
      disablePadding: import_prop_types.default.bool,
      /**
       * The content of the subheader, normally `ListSubheader`.
       */
      subheader: import_prop_types.default.node,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
    })
  : void 0;
var List_default = List;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/ListItemButton/ListItemButton.js
var React81 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/ListItemButton/listItemButtonClasses.js
function getListItemButtonUtilityClass(slot) {
  return generateUtilityClass("MuiListItemButton", slot);
}
var listItemButtonClasses = generateUtilityClasses("MuiListItemButton", [
  "root",
  "focusVisible",
  "dense",
  "alignItemsFlexStart",
  "disabled",
  "divider",
  "gutters",
  "selected",
]);
var listItemButtonClasses_default = listItemButtonClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/ListItemButton/ListItemButton.js
var import_jsx_runtime59 = __toESM(require_jsx_runtime(), 1);
var overridesResolver2 = (props, styles5) => {
  const { ownerState } = props;
  return [
    styles5.root,
    ownerState.dense && styles5.dense,
    ownerState.alignItems === "flex-start" && styles5.alignItemsFlexStart,
    ownerState.divider && styles5.divider,
    !ownerState.disableGutters && styles5.gutters,
  ];
};
var useUtilityClasses29 = (ownerState) => {
  const {
    alignItems,
    classes,
    dense,
    disabled,
    disableGutters,
    divider,
    selected,
  } = ownerState;
  const slots = {
    root: [
      "root",
      dense && "dense",
      !disableGutters && "gutters",
      divider && "divider",
      disabled && "disabled",
      alignItems === "flex-start" && "alignItemsFlexStart",
      selected && "selected",
    ],
  };
  const composedClasses = composeClasses(
    slots,
    getListItemButtonUtilityClass,
    classes,
  );
  return {
    ...classes,
    ...composedClasses,
  };
};
var ListItemButtonRoot = styled_default2(ButtonBase_default, {
  shouldForwardProp: (prop) =>
    rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiListItemButton",
  slot: "Root",
  overridesResolver: overridesResolver2,
})(
  memoTheme_default(({ theme: theme2 }) => ({
    display: "flex",
    flexGrow: 1,
    justifyContent: "flex-start",
    alignItems: "center",
    position: "relative",
    textDecoration: "none",
    minWidth: 0,
    boxSizing: "border-box",
    textAlign: "left",
    paddingTop: 8,
    paddingBottom: 8,
    transition: theme2.transitions.create("background-color", {
      duration: theme2.transitions.duration.shortest,
    }),
    "&:hover": {
      textDecoration: "none",
      backgroundColor: (theme2.vars || theme2).palette.action.hover,
      // Reset on touch devices, it doesn't add specificity
      "@media (hover: none)": {
        backgroundColor: "transparent",
      },
    },
    [`&.${listItemButtonClasses_default.selected}`]: {
      backgroundColor: theme2.alpha(
        (theme2.vars || theme2).palette.primary.main,
        (theme2.vars || theme2).palette.action.selectedOpacity,
      ),
      [`&.${listItemButtonClasses_default.focusVisible}`]: {
        backgroundColor: theme2.alpha(
          (theme2.vars || theme2).palette.primary.main,
          `${(theme2.vars || theme2).palette.action.selectedOpacity} + ${(theme2.vars || theme2).palette.action.focusOpacity}`,
        ),
      },
    },
    [`&.${listItemButtonClasses_default.selected}:hover`]: {
      backgroundColor: theme2.alpha(
        (theme2.vars || theme2).palette.primary.main,
        `${(theme2.vars || theme2).palette.action.selectedOpacity} + ${(theme2.vars || theme2).palette.action.hoverOpacity}`,
      ),
      // Reset on touch devices, it doesn't add specificity
      "@media (hover: none)": {
        backgroundColor: theme2.alpha(
          (theme2.vars || theme2).palette.primary.main,
          (theme2.vars || theme2).palette.action.selectedOpacity,
        ),
      },
    },
    [`&.${listItemButtonClasses_default.focusVisible}`]: {
      backgroundColor: (theme2.vars || theme2).palette.action.focus,
    },
    [`&.${listItemButtonClasses_default.disabled}`]: {
      opacity: (theme2.vars || theme2).palette.action.disabledOpacity,
    },
    variants: [
      {
        props: ({ ownerState }) => ownerState.divider,
        style: {
          borderBottom: `1px solid ${(theme2.vars || theme2).palette.divider}`,
          backgroundClip: "padding-box",
        },
      },
      {
        props: {
          alignItems: "flex-start",
        },
        style: {
          alignItems: "flex-start",
        },
      },
      {
        props: ({ ownerState }) => !ownerState.disableGutters,
        style: {
          paddingLeft: 16,
          paddingRight: 16,
        },
      },
      {
        props: ({ ownerState }) => ownerState.dense,
        style: {
          paddingTop: 4,
          paddingBottom: 4,
        },
      },
    ],
  })),
);
var ListItemButton = /* @__PURE__ */ React81.forwardRef(
  function ListItemButton2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiListItemButton",
    });
    const {
      alignItems = "center",
      autoFocus = false,
      component = "div",
      children,
      dense = false,
      disableGutters = false,
      divider = false,
      focusVisibleClassName,
      selected = false,
      className,
      ...other
    } = props;
    const context = React81.useContext(ListContext_default);
    const childContext = React81.useMemo(
      () => ({
        dense: dense || context.dense || false,
        alignItems,
        disableGutters,
      }),
      [alignItems, context.dense, dense, disableGutters],
    );
    const listItemRef = React81.useRef(null);
    useEnhancedEffect_default2(() => {
      if (autoFocus) {
        if (listItemRef.current) {
          listItemRef.current.focus();
        } else if (false) {
          console.error(
            "MUI: Unable to set focus to a ListItemButton whose component has not been rendered.",
          );
        }
      }
    }, [autoFocus]);
    const ownerState = {
      ...props,
      alignItems,
      dense: childContext.dense,
      disableGutters,
      divider,
      selected,
    };
    const classes = useUtilityClasses29(ownerState);
    const { root, ...forwardedClasses } = classes;
    const handleRef = useForkRef_default(listItemRef, ref);
    return /* @__PURE__ */ (0, import_jsx_runtime59.jsx)(
      ListContext_default.Provider,
      {
        value: childContext,
        children: /* @__PURE__ */ (0, import_jsx_runtime59.jsx)(
          ListItemButtonRoot,
          {
            ref: handleRef,
            href: other.href || other.to,
            component:
              (other.href || other.to) && component === "div"
                ? "button"
                : component,
            focusVisibleClassName: clsx_default(
              classes.focusVisible,
              focusVisibleClassName,
            ),
            ownerState,
            className: clsx_default(classes.root, className),
            ...other,
            classes: forwardedClasses,
            children,
          },
        ),
      },
    );
  },
);
false
  ? (ListItemButton.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * Defines the `align-items` style property.
       * @default 'center'
       */
      alignItems: import_prop_types.default.oneOf(["center", "flex-start"]),
      /**
       * If `true`, the list item is focused during the first mount.
       * Focus will also be triggered if the value changes from false to true.
       * @default false
       */
      autoFocus: import_prop_types.default.bool,
      /**
       * The content of the component if a `ListItemSecondaryAction` is used it must
       * be the last child.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * If `true`, compact vertical padding designed for keyboard and mouse input is used.
       * The prop defaults to the value inherited from the parent List component.
       * @default false
       */
      dense: import_prop_types.default.bool,
      /**
       * If `true`, the component is disabled.
       * @default false
       */
      disabled: import_prop_types.default.bool,
      /**
       * If `true`, the left and right padding is removed.
       * @default false
       */
      disableGutters: import_prop_types.default.bool,
      /**
       * If `true`, a 1px light border is added to the bottom of the list item.
       * @default false
       */
      divider: import_prop_types.default.bool,
      /**
       * This prop can help identify which element has keyboard focus.
       * The class name will be applied when the element gains the focus through keyboard interaction.
       * It's a polyfill for the [CSS :focus-visible selector](https://drafts.csswg.org/selectors-4/#the-focus-visible-pseudo).
       * The rationale for using this feature [is explained here](https://github.com/WICG/focus-visible/blob/HEAD/explainer.md).
       * A [polyfill can be used](https://github.com/WICG/focus-visible) to apply a `focus-visible` class to other components
       * if needed.
       */
      focusVisibleClassName: import_prop_types.default.string,
      /**
       * @ignore
       */
      href: import_prop_types.default.string,
      /**
       * Use to apply selected styling.
       * @default false
       */
      selected: import_prop_types.default.bool,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
    })
  : void 0;
var ListItemButton_default = ListItemButton;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Menu/Menu.js
var React84 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/MenuList/MenuList.js
var React82 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/utils/getScrollbarSize.js
var getScrollbarSize_default = getScrollbarSize;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/MenuList/MenuList.js
var import_jsx_runtime60 = __toESM(require_jsx_runtime(), 1);
function nextItem(list, item, disableListWrap) {
  if (list === item) {
    return list.firstChild;
  }
  if (item && item.nextElementSibling) {
    return item.nextElementSibling;
  }
  return disableListWrap ? null : list.firstChild;
}
function previousItem(list, item, disableListWrap) {
  if (list === item) {
    return disableListWrap ? list.firstChild : list.lastChild;
  }
  if (item && item.previousElementSibling) {
    return item.previousElementSibling;
  }
  return disableListWrap ? null : list.lastChild;
}
function textCriteriaMatches(nextFocus, textCriteria) {
  if (textCriteria === void 0) {
    return true;
  }
  let text = nextFocus.innerText;
  if (text === void 0) {
    text = nextFocus.textContent;
  }
  text = text.trim().toLowerCase();
  if (text.length === 0) {
    return false;
  }
  if (textCriteria.repeating) {
    return text[0] === textCriteria.keys[0];
  }
  return text.startsWith(textCriteria.keys.join(""));
}
function moveFocus(
  list,
  currentFocus,
  disableListWrap,
  disabledItemsFocusable,
  traversalFunction,
  textCriteria,
) {
  let wrappedOnce = false;
  let nextFocus = traversalFunction(
    list,
    currentFocus,
    currentFocus ? disableListWrap : false,
  );
  while (nextFocus) {
    if (nextFocus === list.firstChild) {
      if (wrappedOnce) {
        return false;
      }
      wrappedOnce = true;
    }
    const nextFocusDisabled = disabledItemsFocusable
      ? false
      : nextFocus.disabled ||
        nextFocus.getAttribute("aria-disabled") === "true";
    if (
      !nextFocus.hasAttribute("tabindex") ||
      !textCriteriaMatches(nextFocus, textCriteria) ||
      nextFocusDisabled
    ) {
      nextFocus = traversalFunction(list, nextFocus, disableListWrap);
    } else {
      nextFocus.focus();
      return true;
    }
  }
  return false;
}
var MenuList = /* @__PURE__ */ React82.forwardRef(
  function MenuList2(props, ref) {
    const {
      // private
      // eslint-disable-next-line react/prop-types
      actions,
      autoFocus = false,
      autoFocusItem = false,
      children,
      className,
      disabledItemsFocusable = false,
      disableListWrap = false,
      onKeyDown,
      variant = "selectedMenu",
      ...other
    } = props;
    const listRef = React82.useRef(null);
    const textCriteriaRef = React82.useRef({
      keys: [],
      repeating: true,
      previousKeyMatched: true,
      lastTime: null,
    });
    useEnhancedEffect_default2(() => {
      if (autoFocus) {
        listRef.current.focus();
      }
    }, [autoFocus]);
    React82.useImperativeHandle(
      actions,
      () => ({
        adjustStyleForScrollbar: (containerElement, { direction }) => {
          const noExplicitWidth = !listRef.current.style.width;
          if (
            containerElement.clientHeight < listRef.current.clientHeight &&
            noExplicitWidth
          ) {
            const scrollbarSize = `${getScrollbarSize_default(ownerWindow_default(containerElement))}px`;
            listRef.current.style[
              direction === "rtl" ? "paddingLeft" : "paddingRight"
            ] = scrollbarSize;
            listRef.current.style.width = `calc(100% + ${scrollbarSize})`;
          }
          return listRef.current;
        },
      }),
      [],
    );
    const handleKeyDown = (event) => {
      const list = listRef.current;
      const key = event.key;
      const isModifierKeyPressed =
        event.ctrlKey || event.metaKey || event.altKey;
      if (isModifierKeyPressed) {
        if (onKeyDown) {
          onKeyDown(event);
        }
        return;
      }
      const currentFocus = getActiveElement_default(
        ownerDocument_default(list),
      );
      if (key === "ArrowDown") {
        event.preventDefault();
        moveFocus(
          list,
          currentFocus,
          disableListWrap,
          disabledItemsFocusable,
          nextItem,
        );
      } else if (key === "ArrowUp") {
        event.preventDefault();
        moveFocus(
          list,
          currentFocus,
          disableListWrap,
          disabledItemsFocusable,
          previousItem,
        );
      } else if (key === "Home") {
        event.preventDefault();
        moveFocus(
          list,
          null,
          disableListWrap,
          disabledItemsFocusable,
          nextItem,
        );
      } else if (key === "End") {
        event.preventDefault();
        moveFocus(
          list,
          null,
          disableListWrap,
          disabledItemsFocusable,
          previousItem,
        );
      } else if (key.length === 1) {
        const criteria = textCriteriaRef.current;
        const lowerKey = key.toLowerCase();
        const currTime = performance.now();
        if (criteria.keys.length > 0) {
          if (currTime - criteria.lastTime > 500) {
            criteria.keys = [];
            criteria.repeating = true;
            criteria.previousKeyMatched = true;
          } else if (criteria.repeating && lowerKey !== criteria.keys[0]) {
            criteria.repeating = false;
          }
        }
        criteria.lastTime = currTime;
        criteria.keys.push(lowerKey);
        const keepFocusOnCurrent =
          currentFocus &&
          !criteria.repeating &&
          textCriteriaMatches(currentFocus, criteria);
        if (
          criteria.previousKeyMatched &&
          (keepFocusOnCurrent ||
            moveFocus(
              list,
              currentFocus,
              false,
              disabledItemsFocusable,
              nextItem,
              criteria,
            ))
        ) {
          event.preventDefault();
        } else {
          criteria.previousKeyMatched = false;
        }
      }
      if (onKeyDown) {
        onKeyDown(event);
      }
    };
    const handleRef = useForkRef_default(listRef, ref);
    let activeItemIndex = -1;
    React82.Children.forEach(children, (child, index) => {
      if (!(/* @__PURE__ */ React82.isValidElement(child))) {
        if (activeItemIndex === index) {
          activeItemIndex += 1;
          if (activeItemIndex >= children.length) {
            activeItemIndex = -1;
          }
        }
        return;
      }
      if (false) {
        if ((0, import_react_is.isFragment)(child)) {
          console.error(
            [
              "MUI: The Menu component doesn't accept a Fragment as a child.",
              "Consider providing an array instead.",
            ].join("\n"),
          );
        }
      }
      if (!child.props.disabled) {
        if (variant === "selectedMenu" && child.props.selected) {
          activeItemIndex = index;
        } else if (activeItemIndex === -1) {
          activeItemIndex = index;
        }
      }
      if (
        activeItemIndex === index &&
        (child.props.disabled ||
          child.props.muiSkipListHighlight ||
          child.type.muiSkipListHighlight)
      ) {
        activeItemIndex += 1;
        if (activeItemIndex >= children.length) {
          activeItemIndex = -1;
        }
      }
    });
    const items = React82.Children.map(children, (child, index) => {
      if (index === activeItemIndex) {
        const newChildProps = {};
        if (autoFocusItem) {
          newChildProps.autoFocus = true;
        }
        if (child.props.tabIndex === void 0 && variant === "selectedMenu") {
          newChildProps.tabIndex = 0;
        }
        return /* @__PURE__ */ React82.cloneElement(child, newChildProps);
      }
      return child;
    });
    return /* @__PURE__ */ (0, import_jsx_runtime60.jsx)(List_default, {
      role: "menu",
      ref: handleRef,
      className,
      onKeyDown: handleKeyDown,
      tabIndex: autoFocus ? 0 : -1,
      ...other,
      children: items,
    });
  },
);
false
  ? (MenuList.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * If `true`, will focus the `[role="menu"]` container and move into tab order.
       * @default false
       */
      autoFocus: import_prop_types.default.bool,
      /**
       * If `true`, will focus the first menuitem if `variant="menu"` or selected item
       * if `variant="selectedMenu"`.
       * @default false
       */
      autoFocusItem: import_prop_types.default.bool,
      /**
       * MenuList contents, normally `MenuItem`s.
       */
      children: import_prop_types.default.node,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * If `true`, will allow focus on disabled items.
       * @default false
       */
      disabledItemsFocusable: import_prop_types.default.bool,
      /**
       * If `true`, the menu items will not wrap focus.
       * @default false
       */
      disableListWrap: import_prop_types.default.bool,
      /**
       * @ignore
       */
      onKeyDown: import_prop_types.default.func,
      /**
       * The variant to use. Use `menu` to prevent selected items from impacting the initial focus
       * and the vertical alignment relative to the anchor element.
       * @default 'selectedMenu'
       */
      variant: import_prop_types.default.oneOf(["menu", "selectedMenu"]),
    })
  : void 0;
var MenuList_default = MenuList;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Popover/Popover.js
var React83 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Popover/popoverClasses.js
function getPopoverUtilityClass(slot) {
  return generateUtilityClass("MuiPopover", slot);
}
var popoverClasses = generateUtilityClasses("MuiPopover", ["root", "paper"]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Popover/Popover.js
var import_jsx_runtime61 = __toESM(require_jsx_runtime(), 1);
function getOffsetTop(rect, vertical) {
  let offset2 = 0;
  if (typeof vertical === "number") {
    offset2 = vertical;
  } else if (vertical === "center") {
    offset2 = rect.height / 2;
  } else if (vertical === "bottom") {
    offset2 = rect.height;
  }
  return offset2;
}
function getOffsetLeft(rect, horizontal) {
  let offset2 = 0;
  if (typeof horizontal === "number") {
    offset2 = horizontal;
  } else if (horizontal === "center") {
    offset2 = rect.width / 2;
  } else if (horizontal === "right") {
    offset2 = rect.width;
  }
  return offset2;
}
function getTransformOriginValue(transformOrigin) {
  return [transformOrigin.horizontal, transformOrigin.vertical]
    .map((n) => (typeof n === "number" ? `${n}px` : n))
    .join(" ");
}
function resolveAnchorEl2(anchorEl) {
  return typeof anchorEl === "function" ? anchorEl() : anchorEl;
}
var useUtilityClasses30 = (ownerState) => {
  const { classes } = ownerState;
  const slots = {
    root: ["root"],
    paper: ["paper"],
  };
  return composeClasses(slots, getPopoverUtilityClass, classes);
};
var PopoverRoot = styled_default2(Modal_default, {
  name: "MuiPopover",
  slot: "Root",
})({});
var PopoverPaper = styled_default2(Paper_default, {
  name: "MuiPopover",
  slot: "Paper",
})({
  position: "absolute",
  overflowY: "auto",
  overflowX: "hidden",
  // So we see the popover when it's empty.
  // It's most likely on issue on userland.
  minWidth: 16,
  minHeight: 16,
  maxWidth: "calc(100% - 32px)",
  maxHeight: "calc(100% - 32px)",
  // We disable the focus ring for mouse, touch and keyboard users.
  outline: 0,
});
var Popover = /* @__PURE__ */ React83.forwardRef(
  function Popover2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiPopover",
    });
    const {
      action,
      anchorEl,
      anchorOrigin = {
        vertical: "top",
        horizontal: "left",
      },
      anchorPosition,
      anchorReference = "anchorEl",
      children,
      className,
      container: containerProp,
      elevation = 8,
      marginThreshold = 16,
      open,
      PaperProps: PaperPropsProp = {},
      // TODO: remove in v7
      slots = {},
      slotProps = {},
      transformOrigin = {
        vertical: "top",
        horizontal: "left",
      },
      TransitionComponent,
      // TODO: remove in v7
      transitionDuration: transitionDurationProp = "auto",
      TransitionProps = {},
      // TODO: remove in v7
      disableScrollLock = false,
      ...other
    } = props;
    const paperRef = React83.useRef();
    const ownerState = {
      ...props,
      anchorOrigin,
      anchorReference,
      elevation,
      marginThreshold,
      transformOrigin,
      TransitionComponent,
      transitionDuration: transitionDurationProp,
      TransitionProps,
    };
    const classes = useUtilityClasses30(ownerState);
    const getAnchorOffset = React83.useCallback(() => {
      if (anchorReference === "anchorPosition") {
        if (false) {
          if (!anchorPosition) {
            console.error(
              'MUI: You need to provide a `anchorPosition` prop when using <Popover anchorReference="anchorPosition" />.',
            );
          }
        }
        return anchorPosition;
      }
      const resolvedAnchorEl = resolveAnchorEl2(anchorEl);
      const anchorElement =
        resolvedAnchorEl && resolvedAnchorEl.nodeType === 1
          ? resolvedAnchorEl
          : ownerDocument_default(paperRef.current).body;
      const anchorRect = anchorElement.getBoundingClientRect();
      if (false) {
        const box = anchorElement.getBoundingClientRect();
        if (
          isLayoutSupported() &&
          box.top === 0 &&
          box.left === 0 &&
          box.right === 0 &&
          box.bottom === 0
        ) {
          console.warn(
            [
              "MUI: The `anchorEl` prop provided to the component is invalid.",
              "The anchor element should be part of the document layout.",
              "Make sure the element is present in the document or that it's not display none.",
            ].join("\n"),
          );
        }
      }
      return {
        top: anchorRect.top + getOffsetTop(anchorRect, anchorOrigin.vertical),
        left:
          anchorRect.left + getOffsetLeft(anchorRect, anchorOrigin.horizontal),
      };
    }, [
      anchorEl,
      anchorOrigin.horizontal,
      anchorOrigin.vertical,
      anchorPosition,
      anchorReference,
    ]);
    const getTransformOrigin = React83.useCallback(
      (elemRect) => {
        return {
          vertical: getOffsetTop(elemRect, transformOrigin.vertical),
          horizontal: getOffsetLeft(elemRect, transformOrigin.horizontal),
        };
      },
      [transformOrigin.horizontal, transformOrigin.vertical],
    );
    const getPositioningStyle = React83.useCallback(
      (element) => {
        const elemRect = {
          width: element.offsetWidth,
          height: element.offsetHeight,
        };
        const elemTransformOrigin = getTransformOrigin(elemRect);
        if (anchorReference === "none") {
          return {
            top: null,
            left: null,
            transformOrigin: getTransformOriginValue(elemTransformOrigin),
          };
        }
        const anchorOffset = getAnchorOffset();
        let top2 = anchorOffset.top - elemTransformOrigin.vertical;
        let left2 = anchorOffset.left - elemTransformOrigin.horizontal;
        const bottom2 = top2 + elemRect.height;
        const right2 = left2 + elemRect.width;
        const containerWindow = ownerWindow_default(resolveAnchorEl2(anchorEl));
        const heightThreshold = containerWindow.innerHeight - marginThreshold;
        const widthThreshold = containerWindow.innerWidth - marginThreshold;
        if (marginThreshold !== null && top2 < marginThreshold) {
          const diff = top2 - marginThreshold;
          top2 -= diff;
          elemTransformOrigin.vertical += diff;
        } else if (marginThreshold !== null && bottom2 > heightThreshold) {
          const diff = bottom2 - heightThreshold;
          top2 -= diff;
          elemTransformOrigin.vertical += diff;
        }
        if (false) {
          if (
            elemRect.height > heightThreshold &&
            elemRect.height &&
            heightThreshold
          ) {
            console.error(
              [
                "MUI: The popover component is too tall.",
                `Some part of it can not be seen on the screen (${elemRect.height - heightThreshold}px).`,
                "Please consider adding a `max-height` to improve the user-experience.",
              ].join("\n"),
            );
          }
        }
        if (marginThreshold !== null && left2 < marginThreshold) {
          const diff = left2 - marginThreshold;
          left2 -= diff;
          elemTransformOrigin.horizontal += diff;
        } else if (right2 > widthThreshold) {
          const diff = right2 - widthThreshold;
          left2 -= diff;
          elemTransformOrigin.horizontal += diff;
        }
        return {
          top: `${Math.round(top2)}px`,
          left: `${Math.round(left2)}px`,
          transformOrigin: getTransformOriginValue(elemTransformOrigin),
        };
      },
      [
        anchorEl,
        anchorReference,
        getAnchorOffset,
        getTransformOrigin,
        marginThreshold,
      ],
    );
    const [isPositioned, setIsPositioned] = React83.useState(open);
    const setPositioningStyles = React83.useCallback(() => {
      const element = paperRef.current;
      if (!element) {
        return;
      }
      const positioning = getPositioningStyle(element);
      if (positioning.top !== null) {
        element.style.setProperty("top", positioning.top);
      }
      if (positioning.left !== null) {
        element.style.left = positioning.left;
      }
      element.style.transformOrigin = positioning.transformOrigin;
      setIsPositioned(true);
    }, [getPositioningStyle]);
    React83.useEffect(() => {
      if (disableScrollLock) {
        window.addEventListener("scroll", setPositioningStyles);
      }
      return () => window.removeEventListener("scroll", setPositioningStyles);
    }, [anchorEl, disableScrollLock, setPositioningStyles]);
    const handleEntering = () => {
      setPositioningStyles();
    };
    const handleExited = () => {
      setIsPositioned(false);
    };
    React83.useEffect(() => {
      if (open) {
        setPositioningStyles();
      }
    });
    React83.useImperativeHandle(
      action,
      () =>
        open
          ? {
              updatePosition: () => {
                setPositioningStyles();
              },
            }
          : null,
      [open, setPositioningStyles],
    );
    React83.useEffect(() => {
      if (!open) {
        return void 0;
      }
      const handleResize = debounce_default(() => {
        setPositioningStyles();
      });
      const containerWindow = ownerWindow_default(resolveAnchorEl2(anchorEl));
      containerWindow.addEventListener("resize", handleResize);
      return () => {
        handleResize.clear();
        containerWindow.removeEventListener("resize", handleResize);
      };
    }, [anchorEl, open, setPositioningStyles]);
    let transitionDuration = transitionDurationProp;
    const externalForwardedProps = {
      slots: {
        transition: TransitionComponent,
        ...slots,
      },
      slotProps: {
        transition: TransitionProps,
        paper: PaperPropsProp,
        ...slotProps,
      },
    };
    const [TransitionSlot, transitionSlotProps] = useSlot("transition", {
      elementType: Grow_default,
      externalForwardedProps,
      ownerState,
      getSlotProps: (handlers) => ({
        ...handlers,
        onEntering: (element, isAppearing) => {
          handlers.onEntering?.(element, isAppearing);
          handleEntering();
        },
        onExited: (element) => {
          handlers.onExited?.(element);
          handleExited();
        },
      }),
      additionalProps: {
        appear: true,
        in: open,
      },
    });
    if (transitionDurationProp === "auto" && !TransitionSlot.muiSupportAuto) {
      transitionDuration = void 0;
    }
    const container =
      containerProp ||
      (anchorEl
        ? ownerDocument_default(resolveAnchorEl2(anchorEl)).body
        : void 0);
    const [
      RootSlot,
      { slots: rootSlotsProp, slotProps: rootSlotPropsProp, ...rootProps },
    ] = useSlot("root", {
      ref,
      elementType: PopoverRoot,
      externalForwardedProps: {
        ...externalForwardedProps,
        ...other,
      },
      shouldForwardComponentProp: true,
      additionalProps: {
        slots: {
          backdrop: slots.backdrop,
        },
        slotProps: {
          backdrop: mergeSlotProps(
            typeof slotProps.backdrop === "function"
              ? slotProps.backdrop(ownerState)
              : slotProps.backdrop,
            {
              invisible: true,
            },
          ),
        },
        container,
        open,
      },
      ownerState,
      className: clsx_default(classes.root, className),
    });
    const [PaperSlot, paperProps] = useSlot("paper", {
      ref: paperRef,
      className: classes.paper,
      elementType: PopoverPaper,
      externalForwardedProps,
      shouldForwardComponentProp: true,
      additionalProps: {
        elevation,
        style: isPositioned
          ? void 0
          : {
              opacity: 0,
            },
      },
      ownerState,
    });
    return /* @__PURE__ */ (0, import_jsx_runtime61.jsx)(RootSlot, {
      ...rootProps,
      ...(!isHostComponent_default(RootSlot) && {
        slots: rootSlotsProp,
        slotProps: rootSlotPropsProp,
        disableScrollLock,
      }),
      children: /* @__PURE__ */ (0, import_jsx_runtime61.jsx)(TransitionSlot, {
        ...transitionSlotProps,
        timeout: transitionDuration,
        children: /* @__PURE__ */ (0, import_jsx_runtime61.jsx)(PaperSlot, {
          ...paperProps,
          children,
        }),
      }),
    });
  },
);
false
  ? (Popover.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * A ref for imperative actions.
       * It currently only supports updatePosition() action.
       */
      action: refType_default,
      /**
       * An HTML element, [PopoverVirtualElement](https://mui.com/material-ui/react-popover/#virtual-element),
       * or a function that returns either.
       * It's used to set the position of the popover.
       */
      anchorEl: chainPropTypes(
        import_prop_types.default.oneOfType([
          HTMLElementType,
          import_prop_types.default.func,
        ]),
        (props) => {
          if (
            props.open &&
            (!props.anchorReference || props.anchorReference === "anchorEl")
          ) {
            const resolvedAnchorEl = resolveAnchorEl2(props.anchorEl);
            if (resolvedAnchorEl && resolvedAnchorEl.nodeType === 1) {
              const box = resolvedAnchorEl.getBoundingClientRect();
              if (false) {
                if (
                  isLayoutSupported() &&
                  box.top === 0 &&
                  box.left === 0 &&
                  box.right === 0 &&
                  box.bottom === 0
                ) {
                  return new Error(
                    [
                      "MUI: The `anchorEl` prop provided to the component is invalid.",
                      "The anchor element should be part of the document layout.",
                      "Make sure the element is present in the document or that it's not display none.",
                    ].join("\n"),
                  );
                }
              }
            } else {
              return new Error(
                [
                  "MUI: The `anchorEl` prop provided to the component is invalid.",
                  `It should be an Element or PopoverVirtualElement instance but it's \`${resolvedAnchorEl}\` instead.`,
                ].join("\n"),
              );
            }
          }
          return null;
        },
      ),
      /**
       * This is the point on the anchor where the popover's
       * `anchorEl` will attach to. This is not used when the
       * anchorReference is 'anchorPosition'.
       *
       * Options:
       * vertical: [top, center, bottom];
       * horizontal: [left, center, right].
       * @default {
       *   vertical: 'top',
       *   horizontal: 'left',
       * }
       */
      anchorOrigin: import_prop_types.default.shape({
        horizontal: import_prop_types.default.oneOfType([
          import_prop_types.default.oneOf(["center", "left", "right"]),
          import_prop_types.default.number,
        ]).isRequired,
        vertical: import_prop_types.default.oneOfType([
          import_prop_types.default.oneOf(["bottom", "center", "top"]),
          import_prop_types.default.number,
        ]).isRequired,
      }),
      /**
       * This is the position that may be used to set the position of the popover.
       * The coordinates are relative to the application's client area.
       */
      anchorPosition: import_prop_types.default.shape({
        left: import_prop_types.default.number.isRequired,
        top: import_prop_types.default.number.isRequired,
      }),
      /**
       * This determines which anchor prop to refer to when setting
       * the position of the popover.
       * @default 'anchorEl'
       */
      anchorReference: import_prop_types.default.oneOf([
        "anchorEl",
        "anchorPosition",
        "none",
      ]),
      /**
       * A backdrop component. This prop enables custom backdrop rendering.
       * @deprecated Use `slots.backdrop` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       * @default styled(Backdrop, {
       *   name: 'MuiModal',
       *   slot: 'Backdrop',
       *   overridesResolver: (props, styles) => {
       *     return styles.backdrop;
       *   },
       * })({
       *   zIndex: -1,
       * })
       */
      BackdropComponent: import_prop_types.default.elementType,
      /**
       * Props applied to the [`Backdrop`](/material-ui/api/backdrop/) element.
       * @deprecated Use `slotProps.backdrop` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       */
      BackdropProps: import_prop_types.default.object,
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * An HTML element, component instance, or function that returns either.
       * The `container` will passed to the Modal component.
       *
       * By default, it uses the body of the anchorEl's top-level document object,
       * so it's simply `document.body` most of the time.
       */
      container: import_prop_types.default.oneOfType([
        HTMLElementType,
        import_prop_types.default.func,
      ]),
      /**
       * Disable the scroll lock behavior.
       * @default false
       */
      disableScrollLock: import_prop_types.default.bool,
      /**
       * The elevation of the popover.
       * @default 8
       */
      elevation: integerPropType_default,
      /**
       * Specifies how close to the edge of the window the popover can appear.
       * If null, the popover will not be constrained by the window.
       * @default 16
       */
      marginThreshold: import_prop_types.default.number,
      /**
       * Callback fired when the component requests to be closed.
       * The `reason` parameter can optionally be used to control the response to `onClose`.
       */
      onClose: import_prop_types.default.func,
      /**
       * If `true`, the component is shown.
       */
      open: import_prop_types.default.bool.isRequired,
      /**
       * Props applied to the [`Paper`](https://mui.com/material-ui/api/paper/) element.
       *
       * This prop is an alias for `slotProps.paper` and will be overridden by it if both are used.
       * @deprecated Use `slotProps.paper` instead.
       *
       * @default {}
       */
      PaperProps: import_prop_types.default.shape({
        component: elementTypeAcceptingRef_default,
      }),
      /**
       * The props used for each slot inside.
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        backdrop: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        paper: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        root: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        transition: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      }),
      /**
       * The components used for each slot inside.
       * @default {}
       */
      slots: import_prop_types.default.shape({
        backdrop: import_prop_types.default.elementType,
        paper: import_prop_types.default.elementType,
        root: import_prop_types.default.elementType,
        transition: import_prop_types.default.elementType,
      }),
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * This is the point on the popover which
       * will attach to the anchor's origin.
       *
       * Options:
       * vertical: [top, center, bottom, x(px)];
       * horizontal: [left, center, right, x(px)].
       * @default {
       *   vertical: 'top',
       *   horizontal: 'left',
       * }
       */
      transformOrigin: import_prop_types.default.shape({
        horizontal: import_prop_types.default.oneOfType([
          import_prop_types.default.oneOf(["center", "left", "right"]),
          import_prop_types.default.number,
        ]).isRequired,
        vertical: import_prop_types.default.oneOfType([
          import_prop_types.default.oneOf(["bottom", "center", "top"]),
          import_prop_types.default.number,
        ]).isRequired,
      }),
      /**
       * The component used for the transition.
       * [Follow this guide](https://mui.com/material-ui/transitions/#transitioncomponent-prop) to learn more about the requirements for this component.
       * @deprecated use the `slots.transition` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       * @default Grow
       */
      TransitionComponent: import_prop_types.default.elementType,
      /**
       * Set to 'auto' to automatically calculate transition time based on height.
       * @default 'auto'
       */
      transitionDuration: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["auto"]),
        import_prop_types.default.number,
        import_prop_types.default.shape({
          appear: import_prop_types.default.number,
          enter: import_prop_types.default.number,
          exit: import_prop_types.default.number,
        }),
      ]),
      /**
       * Props applied to the transition element.
       * By default, the element is based on this [`Transition`](https://reactcommunity.org/react-transition-group/transition/) component.
       * @deprecated use the `slotProps.transition` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       * @default {}
       */
      TransitionProps: import_prop_types.default.object,
    })
  : void 0;
var Popover_default = Popover;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Menu/menuClasses.js
function getMenuUtilityClass(slot) {
  return generateUtilityClass("MuiMenu", slot);
}
var menuClasses = generateUtilityClasses("MuiMenu", ["root", "paper", "list"]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Menu/Menu.js
var import_jsx_runtime62 = __toESM(require_jsx_runtime(), 1);
var RTL_ORIGIN = {
  vertical: "top",
  horizontal: "right",
};
var LTR_ORIGIN = {
  vertical: "top",
  horizontal: "left",
};
var useUtilityClasses31 = (ownerState) => {
  const { classes } = ownerState;
  const slots = {
    root: ["root"],
    paper: ["paper"],
    list: ["list"],
  };
  return composeClasses(slots, getMenuUtilityClass, classes);
};
var MenuRoot = styled_default2(Popover_default, {
  shouldForwardProp: (prop) =>
    rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiMenu",
  slot: "Root",
})({});
var MenuPaper = styled_default2(PopoverPaper, {
  name: "MuiMenu",
  slot: "Paper",
})({
  // specZ: The maximum height of a simple menu should be one or more rows less than the view
  // height. This ensures a tappable area outside of the simple menu with which to dismiss
  // the menu.
  maxHeight: "calc(100% - 96px)",
  // Add iOS momentum scrolling for iOS < 13.0
  WebkitOverflowScrolling: "touch",
});
var MenuMenuList = styled_default2(MenuList_default, {
  name: "MuiMenu",
  slot: "List",
})({
  // We disable the focus ring for mouse, touch and keyboard users.
  outline: 0,
});
var Menu = /* @__PURE__ */ React84.forwardRef(function Menu2(inProps, ref) {
  const props = useDefaultProps2({
    props: inProps,
    name: "MuiMenu",
  });
  const {
    autoFocus = true,
    children,
    className,
    disableAutoFocusItem = false,
    MenuListProps = {},
    onClose,
    open,
    PaperProps = {},
    PopoverClasses,
    transitionDuration = "auto",
    TransitionProps: { onEntering, ...TransitionProps } = {},
    variant = "selectedMenu",
    slots = {},
    slotProps = {},
    ...other
  } = props;
  const isRtl = useRtl();
  const ownerState = {
    ...props,
    autoFocus,
    disableAutoFocusItem,
    MenuListProps,
    onEntering,
    PaperProps,
    transitionDuration,
    TransitionProps,
    variant,
  };
  const classes = useUtilityClasses31(ownerState);
  const autoFocusItem = autoFocus && !disableAutoFocusItem && open;
  const menuListActionsRef = React84.useRef(null);
  const handleEntering = (element, isAppearing) => {
    if (menuListActionsRef.current) {
      menuListActionsRef.current.adjustStyleForScrollbar(element, {
        direction: isRtl ? "rtl" : "ltr",
      });
    }
    if (onEntering) {
      onEntering(element, isAppearing);
    }
  };
  const handleListKeyDown = (event) => {
    if (event.key === "Tab") {
      event.preventDefault();
      if (onClose) {
        onClose(event, "tabKeyDown");
      }
    }
  };
  let activeItemIndex = -1;
  React84.Children.map(children, (child, index) => {
    if (!(/* @__PURE__ */ React84.isValidElement(child))) {
      return;
    }
    if (false) {
      if ((0, import_react_is.isFragment)(child)) {
        console.error(
          [
            "MUI: The Menu component doesn't accept a Fragment as a child.",
            "Consider providing an array instead.",
          ].join("\n"),
        );
      }
    }
    if (!child.props.disabled) {
      if (variant === "selectedMenu" && child.props.selected) {
        activeItemIndex = index;
      } else if (activeItemIndex === -1) {
        activeItemIndex = index;
      }
    }
  });
  const externalForwardedProps = {
    slots,
    slotProps: {
      list: MenuListProps,
      transition: TransitionProps,
      paper: PaperProps,
      ...slotProps,
    },
  };
  const rootSlotProps = useSlotProps_default({
    elementType: slots.root,
    externalSlotProps: slotProps.root,
    ownerState,
    className: [classes.root, className],
  });
  const [PaperSlot, paperSlotProps] = useSlot("paper", {
    className: classes.paper,
    elementType: MenuPaper,
    externalForwardedProps,
    shouldForwardComponentProp: true,
    ownerState,
  });
  const [ListSlot, listSlotProps] = useSlot("list", {
    className: clsx_default(classes.list, MenuListProps.className),
    elementType: MenuMenuList,
    shouldForwardComponentProp: true,
    externalForwardedProps,
    getSlotProps: (handlers) => ({
      ...handlers,
      onKeyDown: (event) => {
        handleListKeyDown(event);
        handlers.onKeyDown?.(event);
      },
    }),
    ownerState,
  });
  const resolvedTransitionProps =
    typeof externalForwardedProps.slotProps.transition === "function"
      ? externalForwardedProps.slotProps.transition(ownerState)
      : externalForwardedProps.slotProps.transition;
  return /* @__PURE__ */ (0, import_jsx_runtime62.jsx)(MenuRoot, {
    onClose,
    anchorOrigin: {
      vertical: "bottom",
      horizontal: isRtl ? "right" : "left",
    },
    transformOrigin: isRtl ? RTL_ORIGIN : LTR_ORIGIN,
    slots: {
      root: slots.root,
      paper: PaperSlot,
      backdrop: slots.backdrop,
      ...(slots.transition && {
        // TODO: pass `slots.transition` directly once `TransitionComponent` is removed from Popover
        transition: slots.transition,
      }),
    },
    slotProps: {
      root: rootSlotProps,
      paper: paperSlotProps,
      backdrop:
        typeof slotProps.backdrop === "function"
          ? slotProps.backdrop(ownerState)
          : slotProps.backdrop,
      transition: {
        ...resolvedTransitionProps,
        onEntering: (...args) => {
          handleEntering(...args);
          resolvedTransitionProps?.onEntering?.(...args);
        },
      },
    },
    open,
    ref,
    transitionDuration,
    ownerState,
    ...other,
    classes: PopoverClasses,
    children: /* @__PURE__ */ (0, import_jsx_runtime62.jsx)(ListSlot, {
      actions: menuListActionsRef,
      autoFocus: autoFocus && (activeItemIndex === -1 || disableAutoFocusItem),
      autoFocusItem,
      variant,
      ...listSlotProps,
      children,
    }),
  });
});
false
  ? (Menu.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * An HTML element, or a function that returns one.
       * It's used to set the position of the menu.
       */
      anchorEl: import_prop_types.default.oneOfType([
        HTMLElementType,
        import_prop_types.default.func,
      ]),
      /**
       * If `true` (Default) will focus the `[role="menu"]` if no focusable child is found. Disabled
       * children are not focusable. If you set this prop to `false` focus will be placed
       * on the parent modal container. This has severe accessibility implications
       * and should only be considered if you manage focus otherwise.
       * @default true
       */
      autoFocus: import_prop_types.default.bool,
      /**
       * Menu contents, normally `MenuItem`s.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * When opening the menu will not focus the active item but the `[role="menu"]`
       * unless `autoFocus` is also set to `false`. Not using the default means not
       * following WAI-ARIA authoring practices. Please be considerate about possible
       * accessibility implications.
       * @default false
       */
      disableAutoFocusItem: import_prop_types.default.bool,
      /**
       * Props applied to the [`MenuList`](https://mui.com/material-ui/api/menu-list/) element.
       * @deprecated use the `slotProps.list` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       * @default {}
       */
      MenuListProps: import_prop_types.default.object,
      /**
       * Callback fired when the component requests to be closed.
       *
       * @param {object} event The event source of the callback.
       * @param {string} reason Can be: `"escapeKeyDown"`, `"backdropClick"`, `"tabKeyDown"`.
       */
      onClose: import_prop_types.default.func,
      /**
       * If `true`, the component is shown.
       */
      open: import_prop_types.default.bool.isRequired,
      /**
       * @ignore
       */
      PaperProps: import_prop_types.default.object,
      /**
       * `classes` prop applied to the [`Popover`](https://mui.com/material-ui/api/popover/) element.
       */
      PopoverClasses: import_prop_types.default.object,
      /**
       * The props used for each slot inside.
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        backdrop: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        list: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        paper: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        root: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        transition: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      }),
      /**
       * The components used for each slot inside.
       * @default {}
       */
      slots: import_prop_types.default.shape({
        backdrop: import_prop_types.default.elementType,
        list: import_prop_types.default.elementType,
        paper: import_prop_types.default.elementType,
        root: import_prop_types.default.elementType,
        transition: import_prop_types.default.elementType,
      }),
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * The length of the transition in `ms`, or 'auto'
       * @default 'auto'
       */
      transitionDuration: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["auto"]),
        import_prop_types.default.number,
        import_prop_types.default.shape({
          appear: import_prop_types.default.number,
          enter: import_prop_types.default.number,
          exit: import_prop_types.default.number,
        }),
      ]),
      /**
       * Props applied to the transition element.
       * By default, the element is based on this [`Transition`](https://reactcommunity.org/react-transition-group/transition/) component.
       * @deprecated use the `slotProps.transition` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       * @default {}
       */
      TransitionProps: import_prop_types.default.object,
      /**
       * The variant to use. Use `menu` to prevent selected items from impacting the initial focus.
       * @default 'selectedMenu'
       */
      variant: import_prop_types.default.oneOf(["menu", "selectedMenu"]),
    })
  : void 0;
var Menu_default = Menu;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Select/Select.js
var React89 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Select/SelectInput.js
var React87 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/NativeSelect/NativeSelectInput.js
var React85 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/NativeSelect/nativeSelectClasses.js
function getNativeSelectUtilityClasses(slot) {
  return generateUtilityClass("MuiNativeSelect", slot);
}
var nativeSelectClasses = generateUtilityClasses("MuiNativeSelect", [
  "root",
  "select",
  "multiple",
  "filled",
  "outlined",
  "standard",
  "disabled",
  "icon",
  "iconOpen",
  "iconFilled",
  "iconOutlined",
  "iconStandard",
  "nativeInput",
  "error",
]);
var nativeSelectClasses_default = nativeSelectClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/NativeSelect/NativeSelectInput.js
var import_jsx_runtime63 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses32 = (ownerState) => {
  const { classes, variant, disabled, multiple, open, error } = ownerState;
  const slots = {
    select: [
      "select",
      variant,
      disabled && "disabled",
      multiple && "multiple",
      error && "error",
    ],
    icon: [
      "icon",
      `icon${capitalize_default(variant)}`,
      open && "iconOpen",
      disabled && "disabled",
    ],
  };
  return composeClasses(slots, getNativeSelectUtilityClasses, classes);
};
var StyledSelectSelect = styled_default2("select", {
  name: "MuiNativeSelect",
})(({ theme: theme2 }) => ({
  // Reset
  MozAppearance: "none",
  // Reset
  WebkitAppearance: "none",
  // When interacting quickly, the text can end up selected.
  // Native select can't be selected either.
  userSelect: "none",
  // Reset
  borderRadius: 0,
  cursor: "pointer",
  "&:focus": {
    // Reset Chrome style
    borderRadius: 0,
  },
  [`&.${nativeSelectClasses_default.disabled}`]: {
    cursor: "default",
  },
  "&[multiple]": {
    height: "auto",
  },
  "&:not([multiple]) option, &:not([multiple]) optgroup": {
    backgroundColor: (theme2.vars || theme2).palette.background.paper,
  },
  variants: [
    {
      props: ({ ownerState }) =>
        ownerState.variant !== "filled" && ownerState.variant !== "outlined",
      style: {
        // Bump specificity to allow extending custom inputs
        "&&&": {
          paddingRight: 24,
          minWidth: 16,
          // So it doesn't collapse.
        },
      },
    },
    {
      props: {
        variant: "filled",
      },
      style: {
        "&&&": {
          paddingRight: 32,
        },
      },
    },
    {
      props: {
        variant: "outlined",
      },
      style: {
        borderRadius: (theme2.vars || theme2).shape.borderRadius,
        "&:focus": {
          borderRadius: (theme2.vars || theme2).shape.borderRadius,
          // Reset the reset for Chrome style
        },
        "&&&": {
          paddingRight: 32,
        },
      },
    },
  ],
}));
var NativeSelectSelect = styled_default2(StyledSelectSelect, {
  name: "MuiNativeSelect",
  slot: "Select",
  shouldForwardProp: rootShouldForwardProp_default,
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.select,
      styles5[ownerState.variant],
      ownerState.error && styles5.error,
      {
        [`&.${nativeSelectClasses_default.multiple}`]: styles5.multiple,
      },
    ];
  },
})({});
var StyledSelectIcon = styled_default2("svg", {
  name: "MuiNativeSelect",
})(({ theme: theme2 }) => ({
  // We use a position absolute over a flexbox in order to forward the pointer events
  // to the input and to support wrapping tags..
  position: "absolute",
  right: 0,
  // Center vertically, height is 1em
  top: "calc(50% - .5em)",
  // Don't block pointer events on the select under the icon.
  pointerEvents: "none",
  color: (theme2.vars || theme2).palette.action.active,
  [`&.${nativeSelectClasses_default.disabled}`]: {
    color: (theme2.vars || theme2).palette.action.disabled,
  },
  variants: [
    {
      props: ({ ownerState }) => ownerState.open,
      style: {
        transform: "rotate(180deg)",
      },
    },
    {
      props: {
        variant: "filled",
      },
      style: {
        right: 7,
      },
    },
    {
      props: {
        variant: "outlined",
      },
      style: {
        right: 7,
      },
    },
  ],
}));
var NativeSelectIcon = styled_default2(StyledSelectIcon, {
  name: "MuiNativeSelect",
  slot: "Icon",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.icon,
      ownerState.variant &&
        styles5[`icon${capitalize_default(ownerState.variant)}`],
      ownerState.open && styles5.iconOpen,
    ];
  },
})({});
var NativeSelectInput = /* @__PURE__ */ React85.forwardRef(
  function NativeSelectInput2(props, ref) {
    const {
      className,
      disabled,
      error,
      IconComponent,
      inputRef,
      variant = "standard",
      ...other
    } = props;
    const ownerState = {
      ...props,
      disabled,
      variant,
      error,
    };
    const classes = useUtilityClasses32(ownerState);
    return /* @__PURE__ */ (0, import_jsx_runtime63.jsxs)(React85.Fragment, {
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime63.jsx)(NativeSelectSelect, {
          ownerState,
          className: clsx_default(classes.select, className),
          disabled,
          ref: inputRef || ref,
          ...other,
        }),
        props.multiple
          ? null
          : /* @__PURE__ */ (0, import_jsx_runtime63.jsx)(NativeSelectIcon, {
              as: IconComponent,
              ownerState,
              className: classes.icon,
            }),
      ],
    });
  },
);
false
  ? (NativeSelectInput.propTypes = {
      /**
       * The option elements to populate the select with.
       * Can be some `<option>` elements.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * The CSS class name of the select element.
       */
      className: import_prop_types.default.string,
      /**
       * If `true`, the select is disabled.
       */
      disabled: import_prop_types.default.bool,
      /**
       * If `true`, the `select input` will indicate an error.
       */
      error: import_prop_types.default.bool,
      /**
       * The icon that displays the arrow.
       */
      IconComponent: import_prop_types.default.elementType.isRequired,
      /**
       * Use that prop to pass a ref to the native select element.
       * @deprecated
       */
      inputRef: refType_default,
      /**
       * @ignore
       */
      multiple: import_prop_types.default.bool,
      /**
       * Name attribute of the `select` or hidden `input` element.
       */
      name: import_prop_types.default.string,
      /**
       * Callback fired when a menu item is selected.
       *
       * @param {object} event The event source of the callback.
       * You can pull out the new value by accessing `event.target.value` (string).
       */
      onChange: import_prop_types.default.func,
      /**
       * The input value.
       */
      value: import_prop_types.default.any,
      /**
       * The variant to use.
       */
      variant: import_prop_types.default.oneOf([
        "standard",
        "outlined",
        "filled",
      ]),
    })
  : void 0;
var NativeSelectInput_default = NativeSelectInput;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Select/selectClasses.js
function getSelectUtilityClasses(slot) {
  return generateUtilityClass("MuiSelect", slot);
}
var selectClasses = generateUtilityClasses("MuiSelect", [
  "root",
  "select",
  "multiple",
  "filled",
  "outlined",
  "standard",
  "disabled",
  "focused",
  "icon",
  "iconOpen",
  "iconFilled",
  "iconOutlined",
  "iconStandard",
  "nativeInput",
  "error",
]);
var selectClasses_default = selectClasses;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Select/utils/getOpenInteractionType.js
function getOpenInteractionType(event) {
  if (!event) {
    return null;
  }
  if (
    event.type === "mousedown" ||
    event.type === "pointerdown" ||
    event.type === "touchstart"
  ) {
    return "pointer";
  }
  if (
    event.type === "keydown" ||
    (event.type === "click" && event.detail === 0)
  ) {
    return "keyboard";
  }
  return null;
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Select/utils/isEmpty.js
function isEmpty3(display) {
  return display == null || (typeof display === "string" && !display.trim());
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Select/utils/areEqualValues.js
function areEqualValues(a, b) {
  if (typeof b === "object" && b !== null) {
    return a === b;
  }
  return String(a) === String(b);
}

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Select/utils/SelectFocusSourceContext.js
var React86 = __toESM(require_react(), 1);
var SelectFocusSourceContext = /* @__PURE__ */ React86.createContext(null);
if (false) {
  SelectFocusSourceContext.displayName = "SelectFocusSourceContext";
}
var SelectFocusSourceProvider = SelectFocusSourceContext.Provider;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Select/SelectInput.js
var import_jsx_runtime64 = __toESM(require_jsx_runtime(), 1);
var _span2;
var SelectSelect = styled_default2(StyledSelectSelect, {
  name: "MuiSelect",
  slot: "Select",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      // Win specificity over the input base
      {
        [`&.${selectClasses_default.select}`]: styles5.select,
      },
      {
        [`&.${selectClasses_default.select}`]: styles5[ownerState.variant],
      },
      {
        [`&.${selectClasses_default.error}`]: styles5.error,
      },
      {
        [`&.${selectClasses_default.multiple}`]: styles5.multiple,
      },
    ];
  },
})({
  // Win specificity over the input base
  [`&.${selectClasses_default.select}`]: {
    height: "auto",
    // Resets for multiple select with chips
    minHeight: "1.4375em",
    // Required for select\text-field height consistency
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
    overflow: "hidden",
  },
});
var SelectIcon = styled_default2(StyledSelectIcon, {
  name: "MuiSelect",
  slot: "Icon",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.icon,
      ownerState.variant &&
        styles5[`icon${capitalize_default(ownerState.variant)}`],
      ownerState.open && styles5.iconOpen,
    ];
  },
})({});
var SelectNativeInput = styled_default2("input", {
  shouldForwardProp: (prop) =>
    slotShouldForwardProp_default(prop) && prop !== "classes",
  name: "MuiSelect",
  slot: "NativeInput",
})({
  bottom: 0,
  left: 0,
  position: "absolute",
  opacity: 0,
  pointerEvents: "none",
  width: "100%",
  boxSizing: "border-box",
});
var useUtilityClasses33 = (ownerState) => {
  const { classes, variant, disabled, multiple, open, error } = ownerState;
  const slots = {
    select: [
      "select",
      variant,
      disabled && "disabled",
      multiple && "multiple",
      error && "error",
    ],
    icon: [
      "icon",
      `icon${capitalize_default(variant)}`,
      open && "iconOpen",
      disabled && "disabled",
    ],
    nativeInput: ["nativeInput"],
  };
  return composeClasses(slots, getSelectUtilityClasses, classes);
};
var SelectInput = /* @__PURE__ */ React87.forwardRef(
  function SelectInput2(props, ref) {
    const {
      "aria-describedby": ariaDescribedby,
      "aria-label": ariaLabel,
      autoFocus,
      autoWidth,
      children,
      className,
      defaultOpen,
      defaultValue,
      disabled,
      displayEmpty,
      error = false,
      IconComponent,
      inputRef: inputRefProp,
      labelId,
      MenuProps = {},
      multiple,
      name,
      onBlur,
      onChange,
      onClose,
      onFocus,
      // eslint-disable-next-line react/prop-types
      onKeyDown,
      // eslint-disable-next-line react/prop-types
      onMouseDown,
      onOpen,
      open: openProp,
      readOnly,
      renderValue,
      required,
      SelectDisplayProps = {},
      tabIndex: tabIndexProp,
      // catching `type` from Input which makes no sense for SelectInput
      type,
      value: valueProp,
      variant = "standard",
      ...other
    } = props;
    const [value, setValueState] = useControlled_default({
      controlled: valueProp,
      default: defaultValue,
      name: "Select",
    });
    const [openState, setOpenState] = useControlled_default({
      controlled: openProp,
      default: defaultOpen,
      name: "Select",
    });
    const inputRef = React87.useRef(null);
    const displayRef = React87.useRef(null);
    const [displayNode, setDisplayNode] = React87.useState(null);
    const { current: isOpenControlled } = React87.useRef(openProp != null);
    const [menuMinWidthState, setMenuMinWidthState] = React87.useState();
    const [openInteractionType, setOpenInteractionType] =
      React87.useState(null);
    const handleRef = useForkRef_default(ref, inputRefProp);
    const handleDisplayRef = React87.useCallback((node2) => {
      displayRef.current = node2;
      if (node2) {
        setDisplayNode(node2);
      }
    }, []);
    const anchorElement = displayNode?.parentNode;
    React87.useImperativeHandle(
      handleRef,
      () => ({
        focus: () => {
          displayRef.current.focus();
        },
        node: inputRef.current,
        value,
      }),
      [value],
    );
    const open = displayNode !== null && openState;
    React87.useEffect(() => {
      if (!open || !anchorElement || autoWidth) {
        return void 0;
      }
      if (typeof ResizeObserver === "undefined") {
        return void 0;
      }
      const observer = new ResizeObserver(() => {
        setMenuMinWidthState(anchorElement.clientWidth);
      });
      observer.observe(anchorElement);
      return () => {
        observer.disconnect();
      };
    }, [open, anchorElement, autoWidth]);
    React87.useEffect(() => {
      if (defaultOpen && openState && displayNode && !isOpenControlled) {
        setMenuMinWidthState(autoWidth ? null : anchorElement.clientWidth);
        displayRef.current.focus();
      }
    }, [displayNode, autoWidth]);
    React87.useEffect(() => {
      if (autoFocus) {
        displayRef.current.focus();
      }
    }, [autoFocus]);
    React87.useEffect(() => {
      if (!labelId) {
        return void 0;
      }
      const label = ownerDocument_default(displayRef.current).getElementById(
        labelId,
      );
      if (label) {
        const handler = () => {
          if (getSelection().isCollapsed) {
            displayRef.current.focus();
          }
        };
        label.addEventListener("click", handler);
        return () => {
          label.removeEventListener("click", handler);
        };
      }
      return void 0;
    }, [labelId]);
    const update = (openParam, event) => {
      if (openParam) {
        setOpenInteractionType(getOpenInteractionType(event));
        if (onOpen) {
          onOpen(event);
        }
      } else {
        setOpenInteractionType(null);
        if (onClose) {
          onClose(event);
        }
      }
      if (!isOpenControlled) {
        setMenuMinWidthState(autoWidth ? null : anchorElement.clientWidth);
        setOpenState(openParam);
      }
    };
    const handleMouseDown = (event) => {
      onMouseDown?.(event);
      if (event.button !== 0) {
        return;
      }
      event.preventDefault();
      displayRef.current.focus();
      update(true, event);
    };
    const handleClose = (event) => {
      update(false, event);
    };
    const childrenArray = React87.Children.toArray(children);
    const handleChange = (event) => {
      const child = childrenArray.find(
        (childItem) => childItem.props.value === event.target.value,
      );
      if (child === void 0) {
        return;
      }
      setValueState(child.props.value);
      if (onChange) {
        onChange(event, child);
      }
    };
    const handleItemClick = (child) => (event) => {
      let newValue;
      if (!event.currentTarget.hasAttribute("tabindex")) {
        return;
      }
      if (multiple) {
        newValue = Array.isArray(value) ? value.slice() : [];
        const itemIndex = value.indexOf(child.props.value);
        if (itemIndex === -1) {
          newValue.push(child.props.value);
        } else {
          newValue.splice(itemIndex, 1);
        }
      } else {
        newValue = child.props.value;
      }
      if (child.props.onClick) {
        child.props.onClick(event);
      }
      if (value !== newValue) {
        setValueState(newValue);
        if (onChange) {
          const nativeEvent = event.nativeEvent || event;
          const clonedEvent = new nativeEvent.constructor(
            nativeEvent.type,
            nativeEvent,
          );
          Object.defineProperty(clonedEvent, "target", {
            writable: true,
            value: {
              value: newValue,
              name,
            },
          });
          onChange(clonedEvent, child);
        }
      }
      if (!multiple) {
        update(false, event);
      }
    };
    const handleKeyDown = (event) => {
      if (!readOnly) {
        const validKeys = [
          " ",
          "ArrowUp",
          "ArrowDown",
          // The native select doesn't respond to enter on macOS, but it's recommended by
          // https://www.w3.org/WAI/ARIA/apg/patterns/combobox/examples/combobox-select-only/
          "Enter",
        ];
        if (validKeys.includes(event.key)) {
          event.preventDefault();
          update(true, event);
        }
        onKeyDown?.(event);
      }
    };
    const handleBlur = (event) => {
      if (!open && onBlur) {
        Object.defineProperty(event, "target", {
          writable: true,
          value: {
            value,
            name,
          },
        });
        onBlur(event);
      }
    };
    delete other["aria-invalid"];
    let display;
    let displaySingle;
    const displayMultiple = [];
    let computeDisplay = false;
    let foundMatch = false;
    if (
      isFilled({
        value,
      }) ||
      displayEmpty
    ) {
      if (renderValue) {
        display = renderValue(value);
      } else {
        computeDisplay = true;
      }
    }
    const items = childrenArray.map((child) => {
      if (!(/* @__PURE__ */ React87.isValidElement(child))) {
        return null;
      }
      if (false) {
        if ((0, import_react_is.isFragment)(child)) {
          console.error(
            [
              "MUI: The Select component doesn't accept a Fragment as a child.",
              "Consider providing an array instead.",
            ].join("\n"),
          );
        }
      }
      let selected;
      if (multiple) {
        if (!Array.isArray(value)) {
          throw new Error(
            false
              ? "MUI: The `value` prop must be an array when using the `Select` component with `multiple`."
              : formatMuiErrorMessage(2),
          );
        }
        selected = value.some((v) => areEqualValues(v, child.props.value));
        if (selected && computeDisplay) {
          displayMultiple.push(child.props.children);
        }
      } else {
        selected = areEqualValues(value, child.props.value);
        if (selected && computeDisplay) {
          displaySingle = child.props.children;
        }
      }
      if (selected) {
        foundMatch = true;
      }
      return /* @__PURE__ */ React87.cloneElement(child, {
        "aria-selected": selected ? "true" : "false",
        onClick: handleItemClick(child),
        onKeyUp: (event) => {
          if (event.key === " ") {
            event.preventDefault();
          }
          if (child.props.onKeyUp) {
            child.props.onKeyUp(event);
          }
        },
        role: "option",
        selected,
        value: void 0,
        // The value is most likely not a valid HTML attribute.
        "data-value": child.props.value,
        // Instead, we provide it as a data attribute.
      });
    });
    if (false) {
      React87.useEffect(() => {
        if (!foundMatch && !multiple && value !== "") {
          const values3 = childrenArray.map((child) => child.props.value);
          console.warn(
            [
              `MUI: You have provided an out-of-range value \`${value}\` for the select ${name ? `(name="${name}") ` : ""}component.`,
              "Consider providing a value that matches one of the available options or ''.",
              `The available values are ${
                values3
                  .filter((x) => x != null)
                  .map((x) => `\`${x}\``)
                  .join(", ") || '""'
              }.`,
            ].join("\n"),
          );
        }
      }, [foundMatch, childrenArray, multiple, name, value]);
    }
    if (computeDisplay) {
      if (multiple) {
        if (displayMultiple.length === 0) {
          display = null;
        } else {
          display = displayMultiple.reduce((output, child, index) => {
            output.push(child);
            if (index < displayMultiple.length - 1) {
              output.push(", ");
            }
            return output;
          }, []);
        }
      } else {
        display = displaySingle;
      }
    }
    let menuMinWidth = menuMinWidthState;
    if (!autoWidth && isOpenControlled && displayNode) {
      menuMinWidth = anchorElement.clientWidth;
    }
    let tabIndex;
    if (typeof tabIndexProp !== "undefined") {
      tabIndex = tabIndexProp;
    } else {
      tabIndex = disabled ? null : 0;
    }
    const buttonId =
      SelectDisplayProps.id || (name ? `mui-component-select-${name}` : void 0);
    const ownerState = {
      ...props,
      variant,
      value,
      open,
      error,
    };
    const classes = useUtilityClasses33(ownerState);
    const paperProps = {
      ...MenuProps.PaperProps,
      ...(typeof MenuProps.slotProps?.paper === "function"
        ? MenuProps.slotProps.paper(ownerState)
        : MenuProps.slotProps?.paper),
    };
    const listProps = {
      ...MenuProps.MenuListProps,
      ...(typeof MenuProps.slotProps?.list === "function"
        ? MenuProps.slotProps.list(ownerState)
        : MenuProps.slotProps?.list),
    };
    const listboxId = useId();
    return /* @__PURE__ */ (0, import_jsx_runtime64.jsxs)(React87.Fragment, {
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime64.jsx)(SelectSelect, {
          as: "div",
          ref: handleDisplayRef,
          tabIndex,
          role: "combobox",
          "aria-controls": open ? listboxId : void 0,
          "aria-disabled": disabled ? "true" : void 0,
          "aria-expanded": open ? "true" : "false",
          "aria-haspopup": "listbox",
          "aria-label": ariaLabel,
          "aria-labelledby":
            [labelId, buttonId].filter(Boolean).join(" ") || void 0,
          "aria-describedby": ariaDescribedby,
          "aria-required": required ? "true" : void 0,
          "aria-invalid": error ? "true" : void 0,
          onKeyDown: handleKeyDown,
          onMouseDown: disabled || readOnly ? null : handleMouseDown,
          onBlur: handleBlur,
          onFocus,
          ...SelectDisplayProps,
          ownerState,
          className: clsx_default(
            SelectDisplayProps.className,
            classes.select,
            className,
          ),
          id: buttonId,
          children: isEmpty3(display)
            ? // notranslate needed while Google Translate will not fix zero-width space issue
              _span2 ||
              (_span2 = /* @__PURE__ */ (0, import_jsx_runtime64.jsx)("span", {
                className: "notranslate",
                "aria-hidden": true,
                children: "\u200B",
              }))
            : display,
        }),
        /* @__PURE__ */ (0, import_jsx_runtime64.jsx)(SelectNativeInput, {
          "aria-invalid": error,
          value: Array.isArray(value) ? value.join(",") : value,
          name,
          ref: inputRef,
          "aria-hidden": true,
          onChange: handleChange,
          tabIndex: -1,
          disabled,
          className: classes.nativeInput,
          autoFocus,
          required,
          ...other,
          ownerState,
        }),
        /* @__PURE__ */ (0, import_jsx_runtime64.jsx)(SelectIcon, {
          as: IconComponent,
          className: classes.icon,
          ownerState,
        }),
        /* @__PURE__ */ (0, import_jsx_runtime64.jsx)(
          SelectFocusSourceProvider,
          {
            value: openInteractionType,
            children: /* @__PURE__ */ (0, import_jsx_runtime64.jsx)(
              Menu_default,
              {
                id: `menu-${name || ""}`,
                anchorEl: anchorElement,
                open,
                onClose: handleClose,
                anchorOrigin: {
                  vertical: "bottom",
                  horizontal: "center",
                },
                transformOrigin: {
                  vertical: "top",
                  horizontal: "center",
                },
                ...MenuProps,
                slotProps: {
                  ...MenuProps.slotProps,
                  list: {
                    "aria-labelledby": labelId,
                    role: "listbox",
                    "aria-multiselectable": multiple ? "true" : void 0,
                    disableListWrap: true,
                    id: listboxId,
                    ...listProps,
                  },
                  paper: {
                    ...paperProps,
                    style: {
                      minWidth: menuMinWidth,
                      ...(paperProps != null ? paperProps.style : null),
                    },
                  },
                },
                children: items,
              },
            ),
          },
        ),
      ],
    });
  },
);
false
  ? (SelectInput.propTypes = {
      /**
       * @ignore
       */
      "aria-describedby": import_prop_types.default.string,
      /**
       * @ignore
       */
      "aria-label": import_prop_types.default.string,
      /**
       * @ignore
       */
      autoFocus: import_prop_types.default.bool,
      /**
       * If `true`, the width of the popover will automatically be set according to the items inside the
       * menu, otherwise it will be at least the width of the select input.
       */
      autoWidth: import_prop_types.default.bool,
      /**
       * The option elements to populate the select with.
       * Can be some `<MenuItem>` elements.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * The CSS class name of the select element.
       */
      className: import_prop_types.default.string,
      /**
       * If `true`, the component is toggled on mount. Use when the component open state is not controlled.
       * You can only use it when the `native` prop is `false` (default).
       */
      defaultOpen: import_prop_types.default.bool,
      /**
       * The default value. Use when the component is not controlled.
       */
      defaultValue: import_prop_types.default.any,
      /**
       * If `true`, the select is disabled.
       */
      disabled: import_prop_types.default.bool,
      /**
       * If `true`, the selected item is displayed even if its value is empty.
       */
      displayEmpty: import_prop_types.default.bool,
      /**
       * If `true`, the `select input` will indicate an error.
       */
      error: import_prop_types.default.bool,
      /**
       * The icon that displays the arrow.
       */
      IconComponent: import_prop_types.default.elementType.isRequired,
      /**
       * Imperative handle implementing `{ value: T, node: HTMLElement, focus(): void }`
       * Equivalent to `ref`
       */
      inputRef: refType_default,
      /**
       * The ID of an element that acts as an additional label. The Select will
       * be labelled by the additional label and the selected value.
       */
      labelId: import_prop_types.default.string,
      /**
       * Props applied to the [`Menu`](/material-ui/api/menu/) element.
       */
      MenuProps: import_prop_types.default.object,
      /**
       * If `true`, `value` must be an array and the menu will support multiple selections.
       */
      multiple: import_prop_types.default.bool,
      /**
       * Name attribute of the `select` or hidden `input` element.
       */
      name: import_prop_types.default.string,
      /**
       * @ignore
       */
      onBlur: import_prop_types.default.func,
      /**
       * Callback fired when a menu item is selected.
       *
       * @param {object} event The event source of the callback.
       * You can pull out the new value by accessing `event.target.value` (any).
       * @param {object} [child] The react element that was selected.
       */
      onChange: import_prop_types.default.func,
      /**
       * Callback fired when the component requests to be closed.
       * Use in controlled mode (see open).
       *
       * @param {object} event The event source of the callback.
       */
      onClose: import_prop_types.default.func,
      /**
       * @ignore
       */
      onFocus: import_prop_types.default.func,
      /**
       * Callback fired when the component requests to be opened.
       * Use in controlled mode (see open).
       *
       * @param {object} event The event source of the callback.
       */
      onOpen: import_prop_types.default.func,
      /**
       * If `true`, the component is shown.
       */
      open: import_prop_types.default.bool,
      /**
       * @ignore
       */
      readOnly: import_prop_types.default.bool,
      /**
       * Render the selected value.
       *
       * @param {any} value The `value` provided to the component.
       * @returns {ReactNode}
       */
      renderValue: import_prop_types.default.func,
      /**
       * If `true`, the component is required.
       */
      required: import_prop_types.default.bool,
      /**
       * Props applied to the clickable div element.
       */
      SelectDisplayProps: import_prop_types.default.object,
      /**
       * @ignore
       */
      tabIndex: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * @ignore
       */
      type: import_prop_types.default.any,
      /**
       * The input value.
       */
      value: import_prop_types.default.any,
      /**
       * The variant to use.
       */
      variant: import_prop_types.default.oneOf([
        "standard",
        "outlined",
        "filled",
      ]),
    })
  : void 0;
var SelectInput_default = SelectInput;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/OutlinedInput/OutlinedInput.js
var React88 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/OutlinedInput/NotchedOutline.js
var import_jsx_runtime65 = __toESM(require_jsx_runtime(), 1);
var _span3;
var NotchedOutlineRoot = styled_default2("fieldset", {
  name: "MuiNotchedOutlined",
  shouldForwardProp: rootShouldForwardProp_default,
})({
  textAlign: "left",
  position: "absolute",
  bottom: 0,
  right: 0,
  top: -5,
  left: 0,
  margin: 0,
  padding: "0 8px",
  pointerEvents: "none",
  borderRadius: "inherit",
  borderStyle: "solid",
  borderWidth: 1,
  overflow: "hidden",
  minWidth: "0%",
});
var NotchedOutlineLegend = styled_default2("legend", {
  name: "MuiNotchedOutlined",
  shouldForwardProp: rootShouldForwardProp_default,
})(
  memoTheme_default(({ theme: theme2 }) => ({
    float: "unset",
    // Fix conflict with bootstrap
    width: "auto",
    // Fix conflict with bootstrap
    overflow: "hidden",
    // Fix Horizontal scroll when label too long
    variants: [
      {
        props: ({ ownerState }) => !ownerState.withLabel,
        style: {
          padding: 0,
          lineHeight: "11px",
          // sync with `height` in `legend` styles
          transition: theme2.transitions.create("width", {
            duration: 150,
            easing: theme2.transitions.easing.easeOut,
          }),
        },
      },
      {
        props: ({ ownerState }) => ownerState.withLabel,
        style: {
          display: "block",
          // Fix conflict with normalize.css and sanitize.css
          padding: 0,
          height: 11,
          // sync with `lineHeight` in `legend` styles
          fontSize: "0.75em",
          visibility: "hidden",
          maxWidth: 0.01,
          transition: theme2.transitions.create("max-width", {
            duration: 50,
            easing: theme2.transitions.easing.easeOut,
          }),
          whiteSpace: "nowrap",
          "& > span": {
            paddingLeft: 5,
            paddingRight: 5,
            display: "inline-block",
            opacity: 0,
            visibility: "visible",
          },
        },
      },
      {
        props: ({ ownerState }) => ownerState.withLabel && ownerState.notched,
        style: {
          maxWidth: "100%",
          transition: theme2.transitions.create("max-width", {
            duration: 100,
            easing: theme2.transitions.easing.easeOut,
            delay: 50,
          }),
        },
      },
    ],
  })),
);
function NotchedOutline(props) {
  const { children, classes, className, label, notched, ...other } = props;
  const withLabel = label != null && label !== "";
  const ownerState = {
    ...props,
    notched,
    withLabel,
  };
  return /* @__PURE__ */ (0, import_jsx_runtime65.jsx)(NotchedOutlineRoot, {
    "aria-hidden": true,
    className,
    ownerState,
    ...other,
    children: /* @__PURE__ */ (0, import_jsx_runtime65.jsx)(
      NotchedOutlineLegend,
      {
        ownerState,
        children: withLabel
          ? /* @__PURE__ */ (0, import_jsx_runtime65.jsx)("span", {
              children: label,
            })
          : // notranslate needed while Google Translate will not fix zero-width space issue
            _span3 ||
            (_span3 = /* @__PURE__ */ (0, import_jsx_runtime65.jsx)("span", {
              className: "notranslate",
              "aria-hidden": true,
              children: "\u200B",
            })),
      },
    ),
  });
}
false
  ? (NotchedOutline.propTypes = {
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The label.
       */
      label: import_prop_types.default.node,
      /**
       * If `true`, the outline is notched to accommodate the label.
       */
      notched: import_prop_types.default.bool.isRequired,
      /**
       * @ignore
       */
      style: import_prop_types.default.object,
    })
  : void 0;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/OutlinedInput/OutlinedInput.js
var import_jsx_runtime66 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses34 = (ownerState) => {
  const { classes } = ownerState;
  const slots = {
    root: ["root"],
    notchedOutline: ["notchedOutline"],
    input: ["input"],
  };
  const composedClasses = composeClasses(
    slots,
    getOutlinedInputUtilityClass,
    classes,
  );
  return {
    ...classes,
    // forward classes to the InputBase
    ...composedClasses,
  };
};
var OutlinedInputRoot = styled_default2(InputBaseRoot, {
  shouldForwardProp: (prop) =>
    rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiOutlinedInput",
  slot: "Root",
  overridesResolver: rootOverridesResolver,
})(
  memoTheme_default(({ theme: theme2 }) => {
    const borderColor2 =
      theme2.palette.mode === "light"
        ? "rgba(0, 0, 0, 0.23)"
        : "rgba(255, 255, 255, 0.23)";
    return {
      position: "relative",
      borderRadius: (theme2.vars || theme2).shape.borderRadius,
      [`&:hover .${outlinedInputClasses_default.notchedOutline}`]: {
        borderColor: (theme2.vars || theme2).palette.text.primary,
      },
      // Reset on touch devices, it doesn't add specificity
      "@media (hover: none)": {
        [`&:hover .${outlinedInputClasses_default.notchedOutline}`]: {
          borderColor: theme2.vars
            ? theme2.alpha(theme2.vars.palette.common.onBackground, 0.23)
            : borderColor2,
        },
      },
      [`&.${outlinedInputClasses_default.focused} .${outlinedInputClasses_default.notchedOutline}`]:
        {
          borderWidth: 2,
        },
      variants: [
        ...Object.entries(theme2.palette)
          .filter(createSimplePaletteValueFilter())
          .map(([color2]) => ({
            props: {
              color: color2,
            },
            style: {
              [`&.${outlinedInputClasses_default.focused} .${outlinedInputClasses_default.notchedOutline}`]:
                {
                  borderColor: (theme2.vars || theme2).palette[color2].main,
                },
            },
          })),
        {
          props: {},
          // to override the above style
          style: {
            [`&.${outlinedInputClasses_default.error} .${outlinedInputClasses_default.notchedOutline}`]:
              {
                borderColor: (theme2.vars || theme2).palette.error.main,
              },
            [`&.${outlinedInputClasses_default.disabled} .${outlinedInputClasses_default.notchedOutline}`]:
              {
                borderColor: (theme2.vars || theme2).palette.action.disabled,
              },
          },
        },
        {
          props: ({ ownerState }) => ownerState.startAdornment,
          style: {
            paddingLeft: 14,
          },
        },
        {
          props: ({ ownerState }) => ownerState.endAdornment,
          style: {
            paddingRight: 14,
          },
        },
        {
          props: ({ ownerState }) => ownerState.multiline,
          style: {
            padding: "16.5px 14px",
          },
        },
        {
          props: ({ ownerState, size }) =>
            ownerState.multiline && size === "small",
          style: {
            padding: "8.5px 14px",
          },
        },
      ],
    };
  }),
);
var NotchedOutlineRoot2 = styled_default2(NotchedOutline, {
  name: "MuiOutlinedInput",
  slot: "NotchedOutline",
})(
  memoTheme_default(({ theme: theme2 }) => {
    const borderColor2 =
      theme2.palette.mode === "light"
        ? "rgba(0, 0, 0, 0.23)"
        : "rgba(255, 255, 255, 0.23)";
    return {
      borderColor: theme2.vars
        ? theme2.alpha(theme2.vars.palette.common.onBackground, 0.23)
        : borderColor2,
    };
  }),
);
var OutlinedInputInput = styled_default2(InputBaseInput, {
  name: "MuiOutlinedInput",
  slot: "Input",
  overridesResolver: inputOverridesResolver,
})(
  memoTheme_default(({ theme: theme2 }) => ({
    padding: "16.5px 14px",
    ...(!theme2.vars && {
      "&:-webkit-autofill": {
        WebkitBoxShadow:
          theme2.palette.mode === "light" ? null : "0 0 0 100px #266798 inset",
        WebkitTextFillColor: theme2.palette.mode === "light" ? null : "#fff",
        caretColor: theme2.palette.mode === "light" ? null : "#fff",
        borderRadius: "inherit",
      },
    }),
    ...(theme2.vars && {
      "&:-webkit-autofill": {
        borderRadius: "inherit",
      },
      [theme2.getColorSchemeSelector("dark")]: {
        "&:-webkit-autofill": {
          WebkitBoxShadow: "0 0 0 100px #266798 inset",
          WebkitTextFillColor: "#fff",
          caretColor: "#fff",
        },
      },
    }),
    variants: [
      {
        props: {
          size: "small",
        },
        style: {
          padding: "8.5px 14px",
        },
      },
      {
        props: ({ ownerState }) => ownerState.multiline,
        style: {
          padding: 0,
        },
      },
      {
        props: ({ ownerState }) => ownerState.startAdornment,
        style: {
          paddingLeft: 0,
        },
      },
      {
        props: ({ ownerState }) => ownerState.endAdornment,
        style: {
          paddingRight: 0,
        },
      },
    ],
  })),
);
var OutlinedInput = /* @__PURE__ */ React88.forwardRef(
  function OutlinedInput2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiOutlinedInput",
    });
    const {
      components = {},
      fullWidth = false,
      inputComponent = "input",
      label,
      multiline = false,
      notched,
      slots = {},
      slotProps = {},
      type = "text",
      ...other
    } = props;
    const classes = useUtilityClasses34(props);
    const muiFormControl = useFormControl();
    const fcs = formControlState({
      props,
      muiFormControl,
      states: [
        "color",
        "disabled",
        "error",
        "focused",
        "hiddenLabel",
        "size",
        "required",
      ],
    });
    const ownerState = {
      ...props,
      color: fcs.color || "primary",
      disabled: fcs.disabled,
      error: fcs.error,
      focused: fcs.focused,
      formControl: muiFormControl,
      fullWidth,
      hiddenLabel: fcs.hiddenLabel,
      multiline,
      size: fcs.size,
      type,
    };
    const RootSlot = slots.root ?? components.Root ?? OutlinedInputRoot;
    const InputSlot = slots.input ?? components.Input ?? OutlinedInputInput;
    const [NotchedSlot, notchedProps] = useSlot("notchedOutline", {
      elementType: NotchedOutlineRoot2,
      className: classes.notchedOutline,
      shouldForwardComponentProp: true,
      ownerState,
      externalForwardedProps: {
        slots,
        slotProps,
      },
      additionalProps: {
        label:
          label != null && label !== "" && fcs.required
            ? /* @__PURE__ */ (0, import_jsx_runtime66.jsxs)(React88.Fragment, {
                children: [label, "\u2009", "*"],
              })
            : label,
      },
    });
    return /* @__PURE__ */ (0, import_jsx_runtime66.jsx)(InputBase_default, {
      slots: {
        root: RootSlot,
        input: InputSlot,
      },
      slotProps,
      renderSuffix: (state) =>
        /* @__PURE__ */ (0, import_jsx_runtime66.jsx)(NotchedSlot, {
          ...notchedProps,
          notched:
            typeof notched !== "undefined"
              ? notched
              : Boolean(state.startAdornment || state.filled || state.focused),
        }),
      fullWidth,
      inputComponent,
      multiline,
      ref,
      type,
      ...other,
      classes: {
        ...classes,
        notchedOutline: null,
      },
    });
  },
);
false
  ? (OutlinedInput.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * This prop helps users to fill forms faster, especially on mobile devices.
       * The name can be confusing, as it's more like an autofill.
       * You can learn more about it [following the specification](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#autofill).
       */
      autoComplete: import_prop_types.default.string,
      /**
       * If `true`, the `input` element is focused during the first mount.
       */
      autoFocus: import_prop_types.default.bool,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * The color of the component.
       * It supports both default and custom theme colors, which can be added as shown in the
       * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
       * The prop defaults to the value (`'primary'`) inherited from the parent FormControl component.
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["primary", "secondary"]),
        import_prop_types.default.string,
      ]),
      /**
       * The components used for each slot inside.
       *
       * @deprecated use the `slots` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       *
       * @default {}
       */
      components: import_prop_types.default.shape({
        Input: import_prop_types.default.elementType,
        Root: import_prop_types.default.elementType,
      }),
      /**
       * The default value. Use when the component is not controlled.
       */
      defaultValue: import_prop_types.default.any,
      /**
       * If `true`, the component is disabled.
       * The prop defaults to the value (`false`) inherited from the parent FormControl component.
       */
      disabled: import_prop_types.default.bool,
      /**
       * End `InputAdornment` for this component.
       */
      endAdornment: import_prop_types.default.node,
      /**
       * If `true`, the `input` will indicate an error.
       * The prop defaults to the value (`false`) inherited from the parent FormControl component.
       */
      error: import_prop_types.default.bool,
      /**
       * If `true`, the `input` will take up the full width of its container.
       * @default false
       */
      fullWidth: import_prop_types.default.bool,
      /**
       * The id of the `input` element.
       */
      id: import_prop_types.default.string,
      /**
       * The component used for the `input` element.
       * Either a string to use a HTML element or a component.
       * @default 'input'
       */
      inputComponent: import_prop_types.default.elementType,
      /**
       * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
       * @default {}
       */
      inputProps: import_prop_types.default.object,
      /**
       * Pass a ref to the `input` element.
       */
      inputRef: refType_default,
      /**
       * The label of the `input`. It is only used for layout. The actual labelling
       * is handled by `InputLabel`.
       */
      label: import_prop_types.default.node,
      /**
       * If `dense`, will adjust vertical spacing. This is normally obtained via context from
       * FormControl.
       * The prop defaults to the value (`'none'`) inherited from the parent FormControl component.
       */
      margin: import_prop_types.default.oneOf(["dense", "none"]),
      /**
       * Maximum number of rows to display when multiline option is set to true.
       */
      maxRows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * Minimum number of rows to display when multiline option is set to true.
       */
      minRows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * If `true`, a [TextareaAutosize](https://mui.com/material-ui/react-textarea-autosize/) element is rendered.
       * @default false
       */
      multiline: import_prop_types.default.bool,
      /**
       * Name attribute of the `input` element.
       */
      name: import_prop_types.default.string,
      /**
       * If `true`, the outline is notched to accommodate the label.
       */
      notched: import_prop_types.default.bool,
      /**
       * Callback fired when the value is changed.
       *
       * @param {React.ChangeEvent<HTMLTextAreaElement | HTMLInputElement>} event The event source of the callback.
       * You can pull out the new value by accessing `event.target.value` (string).
       */
      onChange: import_prop_types.default.func,
      /**
       * The short hint displayed in the `input` before the user enters a value.
       */
      placeholder: import_prop_types.default.string,
      /**
       * It prevents the user from changing the value of the field
       * (not from interacting with the field).
       */
      readOnly: import_prop_types.default.bool,
      /**
       * If `true`, the `input` element is required.
       * The prop defaults to the value (`false`) inherited from the parent FormControl component.
       */
      required: import_prop_types.default.bool,
      /**
       * Number of rows to display when multiline option is set to true.
       */
      rows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * The props used for each slot inside.
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        input: import_prop_types.default.object,
        notchedOutline: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        root: import_prop_types.default.object,
      }),
      /**
       * The components used for each slot inside.
       * @default {}
       */
      slots: import_prop_types.default.shape({
        input: import_prop_types.default.elementType,
        notchedOutline: import_prop_types.default.elementType,
        root: import_prop_types.default.elementType,
      }),
      /**
       * Start `InputAdornment` for this component.
       */
      startAdornment: import_prop_types.default.node,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * Type of the `input` element. It should be [a valid HTML5 input type](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#input_types).
       * @default 'text'
       */
      type: import_prop_types.default.string,
      /**
       * The value of the `input` element, required for a controlled component.
       */
      value: import_prop_types.default.any,
    })
  : void 0;
OutlinedInput.muiName = "Input";
var OutlinedInput_default = OutlinedInput;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Select/Select.js
var import_jsx_runtime67 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses35 = (ownerState) => {
  const { classes } = ownerState;
  const slots = {
    root: ["root"],
  };
  const composedClasses = composeClasses(
    slots,
    getSelectUtilityClasses,
    classes,
  );
  return {
    ...classes,
    ...composedClasses,
  };
};
var styledRootConfig = {
  name: "MuiSelect",
  slot: "Root",
  shouldForwardProp: (prop) =>
    rootShouldForwardProp_default(prop) && prop !== "variant",
};
var StyledInput = styled_default2(Input_default, styledRootConfig)("");
var StyledOutlinedInput = styled_default2(
  OutlinedInput_default,
  styledRootConfig,
)("");
var StyledFilledInput = styled_default2(
  FilledInput_default,
  styledRootConfig,
)("");
var Select = /* @__PURE__ */ React89.forwardRef(function Select2(inProps, ref) {
  const props = useDefaultProps2({
    name: "MuiSelect",
    props: inProps,
  });
  const {
    autoWidth = false,
    children,
    classes: classesProp = {},
    className,
    defaultOpen = false,
    displayEmpty = false,
    IconComponent = ArrowDropDown_default,
    id,
    input,
    inputProps,
    label,
    labelId,
    MenuProps,
    multiple = false,
    native = false,
    onClose,
    onOpen,
    open,
    renderValue,
    SelectDisplayProps,
    variant: variantProp = "outlined",
    ...other
  } = props;
  const inputComponent = native
    ? NativeSelectInput_default
    : SelectInput_default;
  const muiFormControl = useFormControl();
  const fcs = formControlState({
    props,
    muiFormControl,
    states: ["variant", "error"],
  });
  const variant = fcs.variant || variantProp;
  const ownerState = {
    ...props,
    variant,
    classes: classesProp,
  };
  const classes = useUtilityClasses35(ownerState);
  const { root, ...restOfClasses } = classes;
  const InputComponent =
    input ||
    {
      standard: /* @__PURE__ */ (0, import_jsx_runtime67.jsx)(StyledInput, {
        ownerState,
      }),
      outlined: /* @__PURE__ */ (0, import_jsx_runtime67.jsx)(
        StyledOutlinedInput,
        {
          label,
          ownerState,
        },
      ),
      filled: /* @__PURE__ */ (0, import_jsx_runtime67.jsx)(StyledFilledInput, {
        ownerState,
      }),
    }[variant];
  const inputComponentRef = useForkRef_default(
    ref,
    getReactElementRef(InputComponent),
  );
  return /* @__PURE__ */ (0, import_jsx_runtime67.jsx)(React89.Fragment, {
    children: /* @__PURE__ */ React89.cloneElement(InputComponent, {
      // Most of the logic is implemented in `SelectInput`.
      // The `Select` component is a simple API wrapper to expose something better to play with.
      inputComponent,
      inputProps: {
        children,
        error: fcs.error,
        IconComponent,
        variant,
        type: void 0,
        // We render a select. We can ignore the type provided by the `Input`.
        multiple,
        ...(native
          ? {
              id,
            }
          : {
              autoWidth,
              defaultOpen,
              displayEmpty,
              labelId,
              MenuProps,
              onClose,
              onOpen,
              open,
              renderValue,
              SelectDisplayProps: {
                id,
                ...SelectDisplayProps,
              },
            }),
        ...inputProps,
        classes: inputProps
          ? deepmerge(restOfClasses, inputProps.classes)
          : restOfClasses,
        ...(input ? input.props.inputProps : {}),
      },
      ...(((multiple && native) || displayEmpty) && variant === "outlined"
        ? {
            notched: true,
          }
        : {}),
      ref: inputComponentRef,
      className: clsx_default(
        InputComponent.props.className,
        className,
        classes.root,
      ),
      // If a custom input is provided via 'input' prop, do not allow 'variant' to be propagated to it's root element. See https://github.com/mui/material-ui/issues/33894.
      ...(!input && {
        variant,
      }),
      ...other,
    }),
  });
});
false
  ? (Select.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * If `true`, the width of the popover will automatically be set according to the items inside the
       * menu, otherwise it will be at least the width of the select input.
       * @default false
       */
      autoWidth: import_prop_types.default.bool,
      /**
       * The option elements to populate the select with.
       * Can be some `MenuItem` when `native` is false and `option` when `native` is true.
       *
       * ⚠️The `MenuItem` elements **must** be direct descendants when `native` is false.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       * @default {}
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * If `true`, the component is initially open. Use when the component open state is not controlled (i.e. the `open` prop is not defined).
       * You can only use it when the `native` prop is `false` (default).
       * @default false
       */
      defaultOpen: import_prop_types.default.bool,
      /**
       * The default value. Use when the component is not controlled.
       */
      defaultValue: import_prop_types.default.any,
      /**
       * If `true`, a value is displayed even if no items are selected.
       *
       * In order to display a meaningful value, a function can be passed to the `renderValue` prop which
       * returns the value to be displayed when no items are selected.
       *
       * ⚠️ When using this prop, make sure the label doesn't overlap with the empty displayed value.
       * The label should either be hidden or forced to a shrunk state.
       * @default false
       */
      displayEmpty: import_prop_types.default.bool,
      /**
       * The icon that displays the arrow.
       * @default ArrowDropDownIcon
       */
      IconComponent: import_prop_types.default.elementType,
      /**
       * The `id` of the wrapper element or the `select` element when `native`.
       */
      id: import_prop_types.default.string,
      /**
       * An `Input` element; does not have to be a material-ui specific `Input`.
       */
      input: import_prop_types.default.element,
      /**
       * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
       * When `native` is `true`, the attributes are applied on the `select` element.
       */
      inputProps: import_prop_types.default.object,
      /**
       * See [OutlinedInput#label](https://mui.com/material-ui/api/outlined-input/#props)
       */
      label: import_prop_types.default.node,
      /**
       * The ID of an element that acts as an additional label. The Select will
       * be labelled by the additional label and the selected value.
       */
      labelId: import_prop_types.default.string,
      /**
       * Props applied to the [`Menu`](https://mui.com/material-ui/api/menu/) element.
       */
      MenuProps: import_prop_types.default.object,
      /**
       * If `true`, `value` must be an array and the menu will support multiple selections.
       * @default false
       */
      multiple: import_prop_types.default.bool,
      /**
       * If `true`, the component uses a native `select` element.
       * @default false
       */
      native: import_prop_types.default.bool,
      /**
       * Callback fired when a menu item is selected.
       *
       * @param {SelectChangeEvent<Value>} event The event source of the callback.
       * You can pull out the new value by accessing `event.target.value` (any).
       * **Warning**: This is a generic event, not a change event, unless the change event is caused by browser autofill.
       * @param {object} [child] The react element that was selected when `native` is `false` (default).
       */
      onChange: import_prop_types.default.func,
      /**
       * Callback fired when the component requests to be closed.
       * Use it in either controlled (see the `open` prop), or uncontrolled mode (to detect when the Select collapses).
       *
       * @param {object} event The event source of the callback.
       */
      onClose: import_prop_types.default.func,
      /**
       * Callback fired when the component requests to be opened.
       * Use it in either controlled (see the `open` prop), or uncontrolled mode (to detect when the Select expands).
       *
       * @param {object} event The event source of the callback.
       */
      onOpen: import_prop_types.default.func,
      /**
       * If `true`, the component is shown.
       * You can only use it when the `native` prop is `false` (default).
       */
      open: import_prop_types.default.bool,
      /**
       * Render the selected value.
       * You can only use it when the `native` prop is `false` (default).
       *
       * @param {any} value The `value` provided to the component.
       * @returns {ReactNode}
       */
      renderValue: import_prop_types.default.func,
      /**
       * Props applied to the clickable div element.
       */
      SelectDisplayProps: import_prop_types.default.object,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * The `input` value. Providing an empty string will select no options.
       * Set to an empty string `''` if you don't want any of the available options to be selected.
       *
       * If the value is an object it must have reference equality with the option in order to be selected.
       * If the value is not an object, the string representation must match with the string representation of the option in order to be selected.
       */
      value: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([""]),
        import_prop_types.default.any,
      ]),
      /**
       * The variant to use.
       * @default 'outlined'
       */
      variant: import_prop_types.default.oneOf([
        "filled",
        "outlined",
        "standard",
      ]),
    })
  : void 0;
Select.muiName = "Select";
var Select_default = Select;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Skeleton/Skeleton.js
var React90 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Skeleton/skeletonClasses.js
function getSkeletonUtilityClass(slot) {
  return generateUtilityClass("MuiSkeleton", slot);
}
var skeletonClasses = generateUtilityClasses("MuiSkeleton", [
  "root",
  "text",
  "rectangular",
  "rounded",
  "circular",
  "pulse",
  "wave",
  "withChildren",
  "fitContent",
  "heightAuto",
]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Skeleton/Skeleton.js
var import_jsx_runtime68 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses36 = (ownerState) => {
  const {
    classes,
    variant,
    animation,
    hasChildren,
    width: width2,
    height: height2,
  } = ownerState;
  const slots = {
    root: [
      "root",
      variant,
      animation,
      hasChildren && "withChildren",
      hasChildren && !width2 && "fitContent",
      hasChildren && !height2 && "heightAuto",
    ],
  };
  return composeClasses(slots, getSkeletonUtilityClass, classes);
};
var pulseKeyframe = keyframes`
  0% {
    opacity: 1;
  }

  50% {
    opacity: 0.4;
  }

  100% {
    opacity: 1;
  }
`;
var waveKeyframe = keyframes`
  0% {
    transform: translateX(-100%);
  }

  50% {
    /* +0.5s of delay between each loop */
    transform: translateX(100%);
  }

  100% {
    transform: translateX(100%);
  }
`;
var pulseAnimation =
  typeof pulseKeyframe !== "string"
    ? css`
        animation: ${pulseKeyframe} 2s ease-in-out 0.5s infinite;
      `
    : null;
var waveAnimation =
  typeof waveKeyframe !== "string"
    ? css`
        &::after {
          animation: ${waveKeyframe} 2s linear 0.5s infinite;
        }
      `
    : null;
var SkeletonRoot = styled_default2("span", {
  name: "MuiSkeleton",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const { ownerState } = props;
    return [
      styles5.root,
      styles5[ownerState.variant],
      ownerState.animation !== false && styles5[ownerState.animation],
      ownerState.hasChildren && styles5.withChildren,
      ownerState.hasChildren && !ownerState.width && styles5.fitContent,
      ownerState.hasChildren && !ownerState.height && styles5.heightAuto,
    ];
  },
})(
  memoTheme_default(({ theme: theme2 }) => {
    const radiusUnit = getUnit(theme2.shape.borderRadius) || "px";
    const radiusValue = toUnitless(theme2.shape.borderRadius);
    return {
      display: "block",
      // Create a "on paper" color with sufficient contrast retaining the color
      backgroundColor: theme2.vars
        ? theme2.vars.palette.Skeleton.bg
        : theme2.alpha(
            theme2.palette.text.primary,
            theme2.palette.mode === "light" ? 0.11 : 0.13,
          ),
      height: "1.2em",
      variants: [
        {
          props: {
            variant: "text",
          },
          style: {
            marginTop: 0,
            marginBottom: 0,
            height: "auto",
            transformOrigin: "0 55%",
            transform: "scale(1, 0.60)",
            borderRadius: `${radiusValue}${radiusUnit}/${Math.round((radiusValue / 0.6) * 10) / 10}${radiusUnit}`,
            "&:empty:before": {
              content: '"\\00a0"',
            },
          },
        },
        {
          props: {
            variant: "circular",
          },
          style: {
            borderRadius: "50%",
          },
        },
        {
          props: {
            variant: "rounded",
          },
          style: {
            borderRadius: (theme2.vars || theme2).shape.borderRadius,
          },
        },
        {
          props: ({ ownerState }) => ownerState.hasChildren,
          style: {
            "& > *": {
              visibility: "hidden",
            },
          },
        },
        {
          props: ({ ownerState }) =>
            ownerState.hasChildren && !ownerState.width,
          style: {
            maxWidth: "fit-content",
          },
        },
        {
          props: ({ ownerState }) =>
            ownerState.hasChildren && !ownerState.height,
          style: {
            height: "auto",
          },
        },
        {
          props: {
            animation: "pulse",
          },
          style: pulseAnimation || {
            animation: `${pulseKeyframe} 2s ease-in-out 0.5s infinite`,
          },
        },
        {
          props: {
            animation: "wave",
          },
          style: {
            position: "relative",
            overflow: "hidden",
            /* Fix bug in Safari https://bugs.webkit.org/show_bug.cgi?id=68196 */
            WebkitMaskImage: "-webkit-radial-gradient(white, black)",
            "&::after": {
              background: `linear-gradient(
                90deg,
                transparent,
                ${(theme2.vars || theme2).palette.action.hover},
                transparent
              )`,
              content: '""',
              position: "absolute",
              transform: "translateX(-100%)",
              bottom: 0,
              left: 0,
              right: 0,
              top: 0,
            },
          },
        },
        {
          props: {
            animation: "wave",
          },
          style: waveAnimation || {
            "&::after": {
              animation: `${waveKeyframe} 2s linear 0.5s infinite`,
            },
          },
        },
      ],
    };
  }),
);
var Skeleton = /* @__PURE__ */ React90.forwardRef(
  function Skeleton2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiSkeleton",
    });
    const {
      animation = "pulse",
      className,
      component = "span",
      height: height2,
      style: style4,
      variant = "text",
      width: width2,
      ...other
    } = props;
    const ownerState = {
      ...props,
      animation,
      component,
      variant,
      hasChildren: Boolean(other.children),
    };
    const classes = useUtilityClasses36(ownerState);
    return /* @__PURE__ */ (0, import_jsx_runtime68.jsx)(SkeletonRoot, {
      as: component,
      ref,
      className: clsx_default(classes.root, className),
      ownerState,
      ...other,
      style: {
        width: width2,
        height: height2,
        ...style4,
      },
    });
  },
);
false
  ? (Skeleton.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The animation.
       * If `false` the animation effect is disabled.
       * @default 'pulse'
       */
      animation: import_prop_types.default.oneOf(["pulse", "wave", false]),
      /**
       * Optional children to infer width and height from.
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * Height of the skeleton.
       * Useful when you don't want to adapt the skeleton to a text element but for instance a card.
       */
      height: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * @ignore
       */
      style: import_prop_types.default.object,
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * The type of content that will be rendered.
       * @default 'text'
       */
      variant: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "circular",
          "rectangular",
          "rounded",
          "text",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * Width of the skeleton.
       * Useful when the skeleton is inside an inline element with no width of its own.
       */
      width: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
    })
  : void 0;
var Skeleton_default = Skeleton;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/Stack/Stack.js
var Stack = createStack({
  createStyledComponent: styled_default2("div", {
    name: "MuiStack",
    slot: "Root",
  }),
  useThemeProps: (inProps) =>
    useDefaultProps2({
      props: inProps,
      name: "MuiStack",
    }),
});
false
  ? (Stack.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * The content of the component.
       */
      children: import_prop_types.default.node,
      /**
       * The component used for the root node.
       * Either a string to use a HTML element or a component.
       */
      component: import_prop_types.default.elementType,
      /**
       * Defines the `flex-direction` style property.
       * It is applied for all screen sizes.
       * @default 'column'
       */
      direction: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "column-reverse",
          "column",
          "row-reverse",
          "row",
        ]),
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOf([
            "column-reverse",
            "column",
            "row-reverse",
            "row",
          ]),
        ),
        import_prop_types.default.object,
      ]),
      /**
       * Add an element between each child.
       */
      divider: import_prop_types.default.node,
      /**
       * Defines the space between immediate children.
       * @default 0
       */
      spacing: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.number,
            import_prop_types.default.string,
          ]),
        ),
        import_prop_types.default.number,
        import_prop_types.default.object,
        import_prop_types.default.string,
      ]),
      /**
       * The system prop, which allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * If `true`, the CSS flexbox `gap` is used instead of applying `margin` to children.
       *
       * While CSS `gap` removes the [known limitations](https://mui.com/joy-ui/react-stack/#limitations),
       * it is not fully supported in some browsers. We recommend checking https://caniuse.com/?search=flex%20gap before using this flag.
       *
       * To enable this flag globally, follow the [theme's default props](https://mui.com/material-ui/customization/theme-components/#default-props) configuration.
       * @default false
       */
      useFlexGap: import_prop_types.default.bool,
    })
  : void 0;
var Stack_default = Stack;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/TextField/TextField.js
var React91 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/TextField/textFieldClasses.js
function getTextFieldUtilityClass(slot) {
  return generateUtilityClass("MuiTextField", slot);
}
var textFieldClasses = generateUtilityClasses("MuiTextField", ["root"]);

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/TextField/TextField.js
var import_jsx_runtime69 = __toESM(require_jsx_runtime(), 1);
var variantComponent = {
  standard: Input_default,
  filled: FilledInput_default,
  outlined: OutlinedInput_default,
};
var useUtilityClasses37 = (ownerState) => {
  const { classes } = ownerState;
  const slots = {
    root: ["root"],
  };
  return composeClasses(slots, getTextFieldUtilityClass, classes);
};
var TextFieldRoot = styled_default2(FormControl_default, {
  name: "MuiTextField",
  slot: "Root",
})({});
var TextField = /* @__PURE__ */ React91.forwardRef(
  function TextField2(inProps, ref) {
    const props = useDefaultProps2({
      props: inProps,
      name: "MuiTextField",
    });
    const {
      autoComplete,
      autoFocus = false,
      children,
      className,
      color: color2 = "primary",
      defaultValue,
      disabled = false,
      error = false,
      FormHelperTextProps: FormHelperTextPropsProp,
      fullWidth = false,
      helperText,
      id: idOverride,
      InputLabelProps: InputLabelPropsProp,
      inputProps: inputPropsProp,
      InputProps: InputPropsProp,
      inputRef,
      label,
      maxRows,
      minRows,
      multiline = false,
      name,
      onBlur,
      onChange,
      onFocus,
      placeholder,
      required = false,
      rows,
      select = false,
      SelectProps: SelectPropsProp,
      slots = {},
      slotProps = {},
      type,
      value,
      variant = "outlined",
      ...other
    } = props;
    const ownerState = {
      ...props,
      autoFocus,
      color: color2,
      disabled,
      error,
      fullWidth,
      multiline,
      required,
      select,
      variant,
    };
    const classes = useUtilityClasses37(ownerState);
    if (false) {
      if (select && !children) {
        console.error(
          "MUI: `children` must be passed when using the `TextField` component with `select`.",
        );
      }
    }
    const id = useId(idOverride);
    const helperTextId = helperText && id ? `${id}-helper-text` : void 0;
    const inputLabelId = label && id ? `${id}-label` : void 0;
    const InputComponent = variantComponent[variant];
    const externalForwardedProps = {
      slots,
      slotProps: {
        input: InputPropsProp,
        inputLabel: InputLabelPropsProp,
        htmlInput: inputPropsProp,
        formHelperText: FormHelperTextPropsProp,
        select: SelectPropsProp,
        ...slotProps,
      },
    };
    const inputAdditionalProps = {};
    const inputLabelSlotProps = externalForwardedProps.slotProps.inputLabel;
    if (variant === "outlined") {
      if (
        inputLabelSlotProps &&
        typeof inputLabelSlotProps.shrink !== "undefined"
      ) {
        inputAdditionalProps.notched = inputLabelSlotProps.shrink;
      }
      inputAdditionalProps.label = label;
    }
    if (select) {
      if (!SelectPropsProp || !SelectPropsProp.native) {
        inputAdditionalProps.id = void 0;
      }
      inputAdditionalProps["aria-describedby"] = void 0;
    }
    const [RootSlot, rootProps] = useSlot("root", {
      elementType: TextFieldRoot,
      shouldForwardComponentProp: true,
      externalForwardedProps: {
        ...externalForwardedProps,
        ...other,
      },
      ownerState,
      className: clsx_default(classes.root, className),
      ref,
      additionalProps: {
        disabled,
        error,
        fullWidth,
        required,
        color: color2,
        variant,
      },
    });
    const [InputSlot, inputProps] = useSlot("input", {
      elementType: InputComponent,
      externalForwardedProps,
      additionalProps: inputAdditionalProps,
      ownerState,
    });
    const [InputLabelSlot, inputLabelProps] = useSlot("inputLabel", {
      elementType: InputLabel_default,
      externalForwardedProps,
      ownerState,
    });
    const [HtmlInputSlot, htmlInputProps] = useSlot("htmlInput", {
      elementType: "input",
      externalForwardedProps,
      ownerState,
    });
    const [FormHelperTextSlot, formHelperTextProps] = useSlot(
      "formHelperText",
      {
        elementType: FormHelperText_default,
        externalForwardedProps,
        ownerState,
      },
    );
    const [SelectSlot, selectProps] = useSlot("select", {
      elementType: Select_default,
      externalForwardedProps,
      ownerState,
    });
    const InputElement = /* @__PURE__ */ (0, import_jsx_runtime69.jsx)(
      InputSlot,
      {
        "aria-describedby": helperTextId,
        autoComplete,
        autoFocus,
        defaultValue,
        fullWidth,
        multiline,
        name,
        rows,
        maxRows,
        minRows,
        type,
        value,
        id,
        inputRef,
        onBlur,
        onChange,
        onFocus,
        placeholder,
        inputProps: htmlInputProps,
        slots: {
          input: slots.htmlInput ? HtmlInputSlot : void 0,
        },
        ...inputProps,
      },
    );
    return /* @__PURE__ */ (0, import_jsx_runtime69.jsxs)(RootSlot, {
      ...rootProps,
      children: [
        label != null &&
          label !== "" &&
          /* @__PURE__ */ (0, import_jsx_runtime69.jsx)(InputLabelSlot, {
            htmlFor: id,
            id: inputLabelId,
            ...inputLabelProps,
            children: label,
          }),
        select
          ? /* @__PURE__ */ (0, import_jsx_runtime69.jsx)(SelectSlot, {
              "aria-describedby": helperTextId,
              id,
              labelId: inputLabelId,
              value,
              input: InputElement,
              ...selectProps,
              children,
            })
          : InputElement,
        helperText &&
          /* @__PURE__ */ (0, import_jsx_runtime69.jsx)(FormHelperTextSlot, {
            id: helperTextId,
            ...formHelperTextProps,
            children: helperText,
          }),
      ],
    });
  },
);
false
  ? (TextField.propTypes = {
      // ┌────────────────────────────── Warning ──────────────────────────────┐
      // │ These PropTypes are generated from the TypeScript type definitions. │
      // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
      // └─────────────────────────────────────────────────────────────────────┘
      /**
       * This prop helps users to fill forms faster, especially on mobile devices.
       * The name can be confusing, as it's more like an autofill.
       * You can learn more about it [following the specification](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#autofill).
       */
      autoComplete: import_prop_types.default.string,
      /**
       * If `true`, the `input` element is focused during the first mount.
       * @default false
       */
      autoFocus: import_prop_types.default.bool,
      /**
       * @ignore
       */
      children: import_prop_types.default.node,
      /**
       * Override or extend the styles applied to the component.
       */
      classes: import_prop_types.default.object,
      /**
       * @ignore
       */
      className: import_prop_types.default.string,
      /**
       * The color of the component.
       * It supports both default and custom theme colors, which can be added as shown in the
       * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
       * @default 'primary'
       */
      color: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf([
          "primary",
          "secondary",
          "error",
          "info",
          "success",
          "warning",
        ]),
        import_prop_types.default.string,
      ]),
      /**
       * The default value. Use when the component is not controlled.
       */
      defaultValue: import_prop_types.default.any,
      /**
       * If `true`, the component is disabled.
       * @default false
       */
      disabled: import_prop_types.default.bool,
      /**
       * If `true`, the label is displayed in an error state.
       * @default false
       */
      error: import_prop_types.default.bool,
      /**
       * Props applied to the [`FormHelperText`](https://mui.com/material-ui/api/form-helper-text/) element.
       * @deprecated Use `slotProps.formHelperText` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       */
      FormHelperTextProps: import_prop_types.default.object,
      /**
       * If `true`, the input will take up the full width of its container.
       * @default false
       */
      fullWidth: import_prop_types.default.bool,
      /**
       * The helper text content.
       */
      helperText: import_prop_types.default.node,
      /**
       * The id of the `input` element.
       * Use this prop to make `label` and `helperText` accessible for screen readers.
       */
      id: import_prop_types.default.string,
      /**
       * Props applied to the [`InputLabel`](https://mui.com/material-ui/api/input-label/) element.
       * Pointer events like `onClick` are enabled if and only if `shrink` is `true`.
       * @deprecated Use `slotProps.inputLabel` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       */
      InputLabelProps: import_prop_types.default.object,
      /**
       * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
       * @deprecated Use `slotProps.htmlInput` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       */
      inputProps: import_prop_types.default.object,
      /**
       * Props applied to the Input element.
       * It will be a [`FilledInput`](https://mui.com/material-ui/api/filled-input/),
       * [`OutlinedInput`](https://mui.com/material-ui/api/outlined-input/) or [`Input`](https://mui.com/material-ui/api/input/)
       * component depending on the `variant` prop value.
       * @deprecated Use `slotProps.input` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       */
      InputProps: import_prop_types.default.object,
      /**
       * Pass a ref to the `input` element.
       */
      inputRef: refType_default,
      /**
       * The label content.
       */
      label: import_prop_types.default.node,
      /**
       * If `dense` or `normal`, will adjust vertical spacing of this and contained components.
       * @default 'none'
       */
      margin: import_prop_types.default.oneOf(["dense", "none", "normal"]),
      /**
       * Maximum number of rows to display when multiline option is set to true.
       */
      maxRows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * Minimum number of rows to display when multiline option is set to true.
       */
      minRows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * If `true`, a `textarea` element is rendered instead of an input.
       * @default false
       */
      multiline: import_prop_types.default.bool,
      /**
       * Name attribute of the `input` element.
       */
      name: import_prop_types.default.string,
      /**
       * @ignore
       */
      onBlur: import_prop_types.default.func,
      /**
       * Callback fired when the value is changed.
       *
       * @param {object} event The event source of the callback.
       * You can pull out the new value by accessing `event.target.value` (string).
       */
      onChange: import_prop_types.default.func,
      /**
       * @ignore
       */
      onFocus: import_prop_types.default.func,
      /**
       * The short hint displayed in the `input` before the user enters a value.
       */
      placeholder: import_prop_types.default.string,
      /**
       * If `true`, the label is displayed as required and the `input` element is required.
       * @default false
       */
      required: import_prop_types.default.bool,
      /**
       * Number of rows to display when multiline option is set to true.
       */
      rows: import_prop_types.default.oneOfType([
        import_prop_types.default.number,
        import_prop_types.default.string,
      ]),
      /**
       * Render a [`Select`](https://mui.com/material-ui/api/select/) element while passing the Input element to `Select` as `input` parameter.
       * If this option is set you must pass the options of the select as children.
       * @default false
       */
      select: import_prop_types.default.bool,
      /**
       * Props applied to the [`Select`](https://mui.com/material-ui/api/select/) element.
       * @deprecated Use `slotProps.select` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
       */
      SelectProps: import_prop_types.default.object,
      /**
       * The size of the component.
       * @default 'medium'
       */
      size: import_prop_types.default.oneOfType([
        import_prop_types.default.oneOf(["medium", "small"]),
        import_prop_types.default.string,
      ]),
      /**
       * The props used for each slot inside.
       * @default {}
       */
      slotProps: import_prop_types.default.shape({
        formHelperText: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        htmlInput: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        input: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        inputLabel: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
        select: import_prop_types.default.oneOfType([
          import_prop_types.default.func,
          import_prop_types.default.object,
        ]),
      }),
      /**
       * The components used for each slot inside.
       * @default {}
       */
      slots: import_prop_types.default.shape({
        formHelperText: import_prop_types.default.elementType,
        htmlInput: import_prop_types.default.elementType,
        input: import_prop_types.default.elementType,
        inputLabel: import_prop_types.default.elementType,
        root: import_prop_types.default.elementType,
        select: import_prop_types.default.elementType,
      }),
      /**
       * The system prop that allows defining system overrides as well as additional CSS styles.
       */
      sx: import_prop_types.default.oneOfType([
        import_prop_types.default.arrayOf(
          import_prop_types.default.oneOfType([
            import_prop_types.default.func,
            import_prop_types.default.object,
            import_prop_types.default.bool,
          ]),
        ),
        import_prop_types.default.func,
        import_prop_types.default.object,
      ]),
      /**
       * Type of the `input` element. It should be [a valid HTML5 input type](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#input_types).
       */
      type: import_prop_types.default.string,
      /**
       * The value of the `input` element, required for a controlled component.
       */
      value: import_prop_types.default.any,
      /**
       * The variant to use.
       * @default 'outlined'
       */
      variant: import_prop_types.default.oneOf([
        "filled",
        "outlined",
        "standard",
      ]),
    })
  : void 0;
var TextField_default = TextField;

// node_modules/.pnpm/@mui+material@7.3.11_@emoti_478642266911858e0e613223724cd82f/node_modules/@mui/material/esm/useMediaQuery/index.js
var useMediaQuery2 = unstable_createUseMediaQuery({
  themeId: identifier_default,
});
var useMediaQuery_default = useMediaQuery2;

// scripts/snapshot/fonts.ts
var font =
  (name) =>
  (options = {}) => ({
    style: { fontFamily: `'${name}', sans-serif` },
    variable: options.variable || "",
    className: "",
  });
var Fraunces = font("Fraunces");
var Figtree = font("Figtree");

// src/app/theme/fonts.ts
var fraunces = Fraunces({
  subsets: ["latin", "latin-ext"],
  weight: ["300", "400", "500", "600", "700"],
  display: "swap",
  variable: "--font-fraunces",
});
var figtree = Figtree({
  subsets: ["latin", "latin-ext"],
  weight: ["300", "400", "500", "600", "700"],
  display: "swap",
  variable: "--font-figtree",
});
var fontConfig = {
  body: figtree,
  heading: fraunces,
  slogan: figtree,
};

// src/app/theme/brand.ts
var LUMIERA = {
  mint: "#ECF5F4",
  border: "#E7E9EB",
  overlay: "#1A1A1A",
  slate: "#4C5C68",
  white: "#FFFFFF",
};
function brandFill(primary) {
  return `linear-gradient(${primary}, ${primary})`;
}
function headerPalette({ text }) {
  return {
    glass: alpha(LUMIERA.white, 0.06),
    glassBorder: LUMIERA.white,
    onImage: LUMIERA.white,
    surface: LUMIERA.white,
    border: LUMIERA.border,
    text,
  };
}
function footerPalette() {
  return {
    background: LUMIERA.slate,
    text: {
      primary: LUMIERA.white,
      secondary: alpha(LUMIERA.white, 0.85),
    },
    divider: alpha(LUMIERA.white, 0.18),
    buttonBorder: alpha(LUMIERA.white, 0.5),
  };
}
function brandSurfaces({ primary, secondary }) {
  const bgAlt = lighten(primary, 0.895);
  return {
    /** Cream section wash (bg-alt). */
    bgAlt,
    /** Lighter cream, for cards laid on white (surface). */
    surface: lighten(primary, 0.93),
    /** Fixed mint wash (accent-soft). */
    mint: LUMIERA.mint,
    /** Rose wash from the secondary colour. */
    rose: lighten(secondary, 0.8),
    border: LUMIERA.border,
    /** Stand-in shown where a photograph is missing. */
    placeholder: lighten(primary, 0.73),
    /** Laid over photographs that carry white copy — 35 %, as the photo variant of the hero. */
    scrim: alpha(LUMIERA.overlay, 0.35),
    /*
     * Ground of the glass caption card, under a 12px blur. The Figma frames record this fill
     * as solid overlay, but the card carries a background blur, which only reads on a
     * translucent fill: the opacity was lost when the paint was bound to its variable.
     */
    glass: alpha(LUMIERA.overlay, 0.45),
    onImage: LUMIERA.white,
    /** Hover and selection wash; kept under its old name for the sections not yet rebuilt. */
    tint: bgAlt,
  };
}

// src/app/theme/colors.ts
var brandDefaults = {
  primary: "#B48E5A",
  secondary: "#D9A7A0",
  text: "#1A1A1A",
};
var colorConfig = {
  primary: {
    main: brandDefaults.primary,
    contrastText: "#FFFFFF",
  },
  secondary: {
    main: brandDefaults.secondary,
    contrastText: brandDefaults.text,
  },
  background: {
    default: "#FFFFFF",
    paper: "#FFFFFF",
  },
  text: {
    primary: brandDefaults.text,
    // Figma `text-muted`.
    secondary: "#4C5C68",
  },
  header: headerPalette(brandDefaults),
  footer: footerPalette(),
};

// src/theme.ts
var bodyFont = fontConfig.body.style.fontFamily;
var headingFont = fontConfig.heading.style.fontFamily;
var sloganFont = fontConfig.slogan.style.fontFamily;
var themeOptions = {
  palette: {
    mode: "light",
    primary: colorConfig.primary,
    secondary: colorConfig.secondary,
    background: colorConfig.background,
    text: colorConfig.text,
    header: colorConfig.header,
    footer: colorConfig.footer,
    brandGradient: brandFill(brandDefaults.primary),
    surfaces: brandSurfaces(brandDefaults),
  },
  typography: {
    fontFamily: bodyFont,
    // 32 / 56 / 70
    h1: {
      fontFamily: headingFont,
      fontWeight: 400,
      fontSize: "32px",
      lineHeight: 1.15,
      "@media (min-width:600px)": { fontSize: "56px", lineHeight: 1.1 },
      "@media (min-width:900px)": { fontSize: "70px" },
    },
    // 28/36 · 36/48 · 42/56
    h2: {
      fontFamily: headingFont,
      fontWeight: 400,
      fontSize: "28px",
      lineHeight: 36 / 28,
      "@media (min-width:600px)": { fontSize: "36px", lineHeight: 48 / 36 },
      "@media (min-width:900px)": { fontSize: "42px", lineHeight: 56 / 42 },
    },
    // 26 / 28 / 32
    h3: {
      fontFamily: headingFont,
      fontWeight: 400,
      fontSize: "26px",
      lineHeight: 1.25,
      "@media (min-width:600px)": { fontSize: "28px" },
      "@media (min-width:900px)": { fontSize: "32px" },
    },
    // 22 / 24 / 26
    h4: {
      fontFamily: headingFont,
      fontWeight: 400,
      fontSize: "22px",
      lineHeight: 1.3,
      "@media (min-width:600px)": { fontSize: "24px" },
      "@media (min-width:900px)": { fontSize: "26px" },
    },
    // 20 / 20 / 22
    h5: {
      fontFamily: headingFont,
      fontWeight: 400,
      fontSize: "20px",
      lineHeight: 1.35,
      "@media (min-width:900px)": { fontSize: "22px" },
    },
    h6: {
      fontFamily: headingFont,
      fontWeight: 400,
      fontSize: "18px",
      lineHeight: 1.4,
    },
    // Body — 15/26
    body1: {
      fontFamily: bodyFont,
      fontWeight: 400,
      fontSize: "15px",
      lineHeight: 26 / 15,
    },
    body2: {
      fontFamily: bodyFont,
      fontWeight: 400,
      fontSize: "14px",
      lineHeight: 24 / 14,
    },
    // Buttons — medium 15, set solid
    button: {
      fontFamily: bodyFont,
      fontWeight: 500,
      fontSize: "15px",
      lineHeight: 1,
      textTransform: "none",
    },
    // Body medium — 15/26
    subtitle1: {
      fontFamily: bodyFont,
      fontWeight: 500,
      fontSize: "15px",
      lineHeight: 26 / 15,
    },
    subtitle2: {
      fontFamily: bodyFont,
      fontWeight: 500,
      fontSize: "14px",
      lineHeight: 24 / 14,
    },
    // Captions — 14/24
    caption: {
      fontFamily: bodyFont,
      fontWeight: 400,
      fontSize: "14px",
      lineHeight: 24 / 14,
    },
    overline: {
      fontFamily: bodyFont,
      fontWeight: 500,
      fontSize: "12px",
      letterSpacing: "1.2px",
      textTransform: "uppercase",
    },
    // Navigation — medium 15, set solid
    navLink: {
      fontFamily: bodyFont,
      fontWeight: 500,
      fontSize: "15px",
      lineHeight: 1,
    },
    // Slogan — 15/26, the body size, as the hero frames set it
    slogan: {
      fontFamily: sloganFont,
      fontWeight: 400,
      fontSize: "15px",
      lineHeight: 26 / 15,
    },
    // Logo set as type when there is no artwork — 24 / 28 / 30
    logo: {
      fontFamily: headingFont,
      fontWeight: 400,
      fontSize: "24px",
      lineHeight: 1,
      "@media (min-width:600px)": { fontSize: "28px" },
      "@media (min-width:900px)": { fontSize: "30px" },
    },
  },
  // Lumiera buttons and inputs are rounded 8.
  shape: {
    borderRadius: 8,
  },
  components: {
    /*
     * Content margins from Figma: 36 mobile, 60 tablet, 120 desktop.
     *
     * The desktop margin comes from the width cap rather than from padding: content is
     * capped at 1200, which at 1440 leaves exactly 120 a side. MUI's lg cap of 1200 includes
     * the container's own 24px padding, so on its own it capped content at 1152 and put
     * every section 144 in — ui-002 shipped that. The cap is raised by the padding so the
     * content itself is 1200.
     */
    MuiContainer: {
      styleOverrides: {
        maxWidthLg: ({ theme: theme2 }) => ({
          [theme2.breakpoints.up("lg")]: {
            maxWidth: 1200 + 2 * 24,
          },
        }),
        root: ({ theme: theme2 }) => ({
          paddingLeft: 36,
          paddingRight: 36,
          [theme2.breakpoints.up("sm")]: {
            paddingLeft: 60,
            paddingRight: 60,
          },
          [theme2.breakpoints.up("lg")]: {
            paddingLeft: 24,
            paddingRight: 24,
          },
        }),
      },
    },
    MuiPaper: {
      styleOverrides: {
        root: {
          backgroundImage: "none",
          boxShadow: "none",
        },
      },
    },
    MuiCssBaseline: {
      styleOverrides: {
        "html, body": {
          overflowX: "hidden",
        },
      },
    },
  },
};
var theme = createTheme2(themeOptions);
var theme_default = theme;

// src/app/theme/utils/createPreviewTheme.ts
var fontFamily = (font2, fallback) => {
  if (!font2) return fallback;
  return `'${font2}', ${fallback || "sans-serif"}`;
};
var normalizeThemeOptions = (input) => {
  if ("colors" in input || "fonts" in input) {
    const themeSettings = input;
    return {
      primary: themeSettings.colors?.primary ?? null,
      secondary: themeSettings.colors?.secondary ?? null,
      text: themeSettings.colors?.text ?? null,
      fontHeading: themeSettings.fonts?.heading ?? null,
      fontBody: themeSettings.fonts?.body ?? null,
      fontBanner: themeSettings.fonts?.banner ?? null,
    };
  }
  return input;
};
function createPreviewTheme(input) {
  const { primary, secondary, text, fontHeading, fontBody, fontBanner } =
    normalizeThemeOptions(input);
  const hasOverrides =
    primary || secondary || text || fontHeading || fontBody || fontBanner;
  if (!hasOverrides) {
    return theme_default;
  }
  const brand = {
    primary: primary || theme_default.palette.primary.main,
    secondary: secondary || theme_default.palette.secondary.main,
    text: text || theme_default.palette.text.primary,
  };
  const resolvedBody = fontFamily(
    fontBody,
    theme_default.typography.fontFamily,
  );
  const resolvedHeading = fontFamily(
    fontHeading,
    theme_default.typography.h1?.fontFamily,
  );
  const resolvedBanner = fontFamily(
    fontBanner,
    theme_default.typography.slogan?.fontFamily,
  );
  return createTheme2(theme_default, {
    palette: {
      ...(primary && {
        primary: {
          ...theme_default.palette.primary,
          main: primary,
        },
      }),
      ...(secondary && {
        secondary: {
          ...theme_default.palette.secondary,
          main: secondary,
        },
      }),
      ...(text && {
        text: {
          ...theme_default.palette.text,
          primary: text,
        },
      }),
      // Kept in step with src/theme.ts — see the note at the top of app/theme/brand.ts.
      header: headerPalette(brand),
      footer: footerPalette(),
      brandGradient: brandFill(brand.primary),
      surfaces: brandSurfaces(brand),
    },
    typography: {
      fontFamily: resolvedBody,
      h1: { ...theme_default.typography.h1, fontFamily: resolvedHeading },
      h2: { ...theme_default.typography.h2, fontFamily: resolvedHeading },
      h3: { ...theme_default.typography.h3, fontFamily: resolvedHeading },
      h4: { ...theme_default.typography.h4, fontFamily: resolvedHeading },
      h5: { ...theme_default.typography.h5, fontFamily: resolvedHeading },
      h6: { ...theme_default.typography.h6, fontFamily: resolvedHeading },
      body1: { ...theme_default.typography.body1, fontFamily: resolvedBody },
      body2: { ...theme_default.typography.body2, fontFamily: resolvedBody },
      button: { ...theme_default.typography.button, fontFamily: resolvedBody },
      subtitle1: {
        ...theme_default.typography.subtitle1,
        fontFamily: resolvedBody,
      },
      subtitle2: {
        ...theme_default.typography.subtitle2,
        fontFamily: resolvedBody,
      },
      caption: {
        ...theme_default.typography.caption,
        fontFamily: resolvedBody,
      },
      overline: {
        ...theme_default.typography.overline,
        fontFamily: resolvedBody,
      },
      navLink: {
        ...theme_default.typography.navLink,
        fontFamily: resolvedBody,
      },
      slogan: {
        ...theme_default.typography.slogan,
        fontFamily: resolvedBanner,
      },
      // The logo is set in the heading face, so it follows the heading override.
      logo: {
        ...theme_default.typography.logo,
        fontFamily: resolvedHeading,
      },
    },
  });
}

// scripts/snapshot/render.tsx
init_runtime2();

// scripts/snapshot/navigation.ts
var pathname = "/";
var searchParams = new URLSearchParams();
var router = {
  push() {},
  replace() {},
  refresh() {},
  back() {},
  forward() {},
  prefetch: async () => {},
};
function setSnapshotPath(path3) {
  pathname = path3;
}
function usePathname() {
  return pathname;
}
function useSearchParams() {
  return searchParams;
}
function useRouter() {
  return router;
}

// src/components/layout/PageLayout.tsx
var import_react11 = __toESM(require_react(), 1);

// src/app/theme/headerMetrics.ts
var HEADER_TOP = { xs: 16, sm: 24, md: 24 };
var HEADER_SIDE = { xs: 16, sm: 24, md: 40 };
var HEADER_BAR = { xs: 60, sm: 64, md: 72 };
var HEADER_HEIGHT = {
  xs: HEADER_TOP.xs + HEADER_BAR.xs,
  sm: HEADER_TOP.sm + HEADER_BAR.sm,
  md: HEADER_TOP.md + HEADER_BAR.md,
};
var ANCHOR_OFFSET = {
  xs: HEADER_HEIGHT.xs + 16,
  sm: HEADER_HEIGHT.sm + 16,
  md: HEADER_HEIGHT.md + 16,
};

// src/components/layout/HeaderLayout.tsx
var import_jsx_runtime71 = __toESM(require_jsx_runtime(), 1);
var px = (steps) => ({
  xs: `${steps.xs}px`,
  sm: `${steps.sm}px`,
  md: `${steps.md}px`,
});
function HeaderLayout({ children, scrolled }) {
  return /* @__PURE__ */ (0, import_jsx_runtime71.jsx)(AppBar_default, {
    position: "fixed",
    elevation: 0,
    color: "transparent",
    sx: {
      backgroundColor: "transparent",
      backgroundImage: "none",
      boxShadow: "none",
      pointerEvents: "none",
      pt: px(HEADER_TOP),
      px: px(HEADER_SIDE),
    },
    children: /* @__PURE__ */ (0, import_jsx_runtime71.jsx)(Box_default, {
      sx: (theme2) => ({
        pointerEvents: "auto",
        height: px(HEADER_BAR),
        borderRadius: "999px",
        /*
         * The same padding on both sides. Figma draws the pill lopsided — a wide inset for
         * the bare menu glyph, a narrow one for the language pill, which carries its own
         * white background and so needs less air. On the phone that put the menu 23 from
         * the border and the language pill 9, and the language pill read as though it were
         * falling off the right edge. Equal wins over optically-adjusted here.
         */
        px: { xs: "16px", sm: "18px", md: "20px" },
        display: "grid",
        /*
         * On a phone the equal outer columns cost the logo too much: the language pill is
         * twice the menu button's width, and matching it on the left left ~150px for the
         * site name. There the outer columns hug their content and the name gets the rest
         * (~200px at 390). It sits a few pixels right of true centre, as Figma draws it.
         */
        gridTemplateColumns: {
          xs: "auto minmax(0, 1fr) auto",
          sm: "1fr auto 1fr",
        },
        alignItems: "center",
        columnGap: 2,
        border: "1px solid",
        borderColor: scrolled
          ? theme2.palette.header.border
          : theme2.palette.header.glassBorder,
        backgroundColor: scrolled
          ? theme2.palette.header.surface
          : theme2.palette.header.glass,
        backdropFilter: scrolled ? "none" : "blur(16px)",
        WebkitBackdropFilter: scrolled ? "none" : "blur(16px)",
        color: scrolled
          ? theme2.palette.header.text
          : theme2.palette.header.onImage,
        "--logo-filter": scrolled ? "none" : "brightness(0) invert(1)",
        "--pill-border": scrolled
          ? theme2.palette.header.border
          : "transparent",
        transition: theme2.transitions.create(
          ["background-color", "border-color", "color"],
          { duration: theme2.transitions.duration.short },
        ),
      }),
      children,
    }),
  });
}

// src/components/layout/FooterLayout.tsx
var import_jsx_runtime72 = __toESM(require_jsx_runtime(), 1);
function FooterLayout({ children }) {
  return /* @__PURE__ */ (0, import_jsx_runtime72.jsx)(Box_default, {
    component: "footer",
    sx: (theme2) => ({
      backgroundColor: theme2.palette.footer.background,
      color: theme2.palette.footer.text.primary,
      borderTopLeftRadius: { xs: "32px", sm: "40px", md: "48px" },
      borderTopRightRadius: { xs: "32px", sm: "40px", md: "48px" },
      pt: { xs: "56px", sm: "64px", md: "80px" },
      pb: { xs: "32px", sm: "36px", md: "40px" },
      mt: "auto",
    }),
    children: /* @__PURE__ */ (0, import_jsx_runtime72.jsx)(Container_default, {
      maxWidth: "lg",
      children,
    }),
  });
}

// src/core/language-startup.ts
init_language();
init_static2();
init_language();
var languagePreference = {
  primaryLanguage,
  supportedLanguages,
  storageKey: createLanguageStorageKey(BASE_PATH),
  legacyStorageKey: "site_language",
};
var languageBootstrapScript = createLanguageBootstrapScript(languagePreference);

// src/components/layout/PageLayout.tsx
var import_jsx_runtime73 = __toESM(require_jsx_runtime(), 1);
function PageLayout({ header, footer, solidHeader = false, children }) {
  (0, import_react11.useLayoutEffect)(() => {
    markContentReady();
  }, []);
  const [scrolled, setScrolled] = (0, import_react11.useState)(false);
  (0, import_react11.useEffect)(() => {
    const handleScroll = () => setScrolled(window.scrollY > 8);
    handleScroll();
    window.addEventListener("scroll", handleScroll, { passive: true });
    return () => window.removeEventListener("scroll", handleScroll);
  }, []);
  return /* @__PURE__ */ (0, import_jsx_runtime73.jsxs)(Box_default, {
    sx: { display: "flex", flexDirection: "column", minHeight: "100dvh" },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime73.jsx)(HeaderLayout, {
        scrolled: solidHeader || scrolled,
        children: header,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime73.jsx)(Box_default, {
        id: "main",
        component: "main",
        sx: { flex: 1 },
        children,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime73.jsx)(FooterLayout, {
        children: footer,
      }),
    ],
  });
}

// src/page-content/pages/home/Home.tsx
init_static2();

// node_modules/.pnpm/@mui+icons-material@7.3.11__77544ce9cc2d00337704b0dadf8f242a/node_modules/@mui/icons-material/esm/Facebook.js
var React92 = __toESM(require_react(), 1);
var import_jsx_runtime74 = __toESM(require_jsx_runtime(), 1);
var Facebook_default = createSvgIcon(
  /* @__PURE__ */ (0, import_jsx_runtime74.jsx)("path", {
    d: "M5 3h14a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2m13 2h-2.5A3.5 3.5 0 0 0 12 8.5V11h-2v3h2v7h3v-7h3v-3h-3V9a1 1 0 0 1 1-1h2V5z",
  }),
  "Facebook",
);

// node_modules/.pnpm/@mui+icons-material@7.3.11__77544ce9cc2d00337704b0dadf8f242a/node_modules/@mui/icons-material/esm/X.js
var React93 = __toESM(require_react(), 1);
var import_jsx_runtime75 = __toESM(require_jsx_runtime(), 1);
var X_default = createSvgIcon(
  /* @__PURE__ */ (0, import_jsx_runtime75.jsx)("path", {
    d: "M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z",
  }),
  "X",
);

// node_modules/.pnpm/@mui+icons-material@7.3.11__77544ce9cc2d00337704b0dadf8f242a/node_modules/@mui/icons-material/esm/Instagram.js
var React94 = __toESM(require_react(), 1);
var import_jsx_runtime76 = __toESM(require_jsx_runtime(), 1);
var Instagram_default = createSvgIcon(
  /* @__PURE__ */ (0, import_jsx_runtime76.jsx)("path", {
    d: "M7.8 2h8.4C19.4 2 22 4.6 22 7.8v8.4a5.8 5.8 0 0 1-5.8 5.8H7.8C4.6 22 2 19.4 2 16.2V7.8A5.8 5.8 0 0 1 7.8 2m-.2 2A3.6 3.6 0 0 0 4 7.6v8.8C4 18.39 5.61 20 7.6 20h8.8a3.6 3.6 0 0 0 3.6-3.6V7.6C20 5.61 18.39 4 16.4 4H7.6m9.65 1.5a1.25 1.25 0 0 1 1.25 1.25A1.25 1.25 0 0 1 17.25 8 1.25 1.25 0 0 1 16 6.75a1.25 1.25 0 0 1 1.25-1.25M12 7a5 5 0 0 1 5 5 5 5 0 0 1-5 5 5 5 0 0 1-5-5 5 5 0 0 1 5-5m0 2a3 3 0 0 0-3 3 3 3 0 0 0 3 3 3 3 0 0 0 3-3 3 3 0 0 0-3-3z",
  }),
  "Instagram",
);

// node_modules/.pnpm/@mui+icons-material@7.3.11__77544ce9cc2d00337704b0dadf8f242a/node_modules/@mui/icons-material/esm/LinkedIn.js
var React95 = __toESM(require_react(), 1);
var import_jsx_runtime77 = __toESM(require_jsx_runtime(), 1);
var LinkedIn_default = createSvgIcon(
  /* @__PURE__ */ (0, import_jsx_runtime77.jsx)("path", {
    d: "M19 3a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h14m-.5 15.5v-5.3a3.26 3.26 0 0 0-3.26-3.26c-.85 0-1.84.52-2.32 1.3v-1.11h-2.79v8.37h2.79v-4.93c0-.77.62-1.4 1.39-1.4a1.4 1.4 0 0 1 1.4 1.4v4.93h2.79M6.88 8.56a1.68 1.68 0 0 0 1.68-1.68c0-.93-.75-1.69-1.68-1.69a1.69 1.69 0 0 0-1.69 1.69c0 .93.76 1.68 1.69 1.68m1.39 9.94v-8.37H5.5v8.37h2.77z",
  }),
  "LinkedIn",
);

// node_modules/.pnpm/@mui+icons-material@7.3.11__77544ce9cc2d00337704b0dadf8f242a/node_modules/@mui/icons-material/esm/Language.js
var import_jsx_runtime78 = __toESM(require_jsx_runtime(), 1);
var Language_default = createSvgIcon(
  /* @__PURE__ */ (0, import_jsx_runtime78.jsx)("path", {
    d: "M11.99 2C6.47 2 2 6.48 2 12s4.47 10 9.99 10C17.52 22 22 17.52 22 12S17.52 2 11.99 2m6.93 6h-2.95c-.32-1.25-.78-2.45-1.38-3.56 1.84.63 3.37 1.91 4.33 3.56M12 4.04c.83 1.2 1.48 2.53 1.91 3.96h-3.82c.43-1.43 1.08-2.76 1.91-3.96M4.26 14C4.1 13.36 4 12.69 4 12s.1-1.36.26-2h3.38c-.08.66-.14 1.32-.14 2s.06 1.34.14 2zm.82 2h2.95c.32 1.25.78 2.45 1.38 3.56-1.84-.63-3.37-1.9-4.33-3.56m2.95-8H5.08c.96-1.66 2.49-2.93 4.33-3.56C8.81 5.55 8.35 6.75 8.03 8M12 19.96c-.83-1.2-1.48-2.53-1.91-3.96h3.82c-.43 1.43-1.08 2.76-1.91 3.96M14.34 14H9.66c-.09-.66-.16-1.32-.16-2s.07-1.35.16-2h4.68c.09.65.16 1.32.16 2s-.07 1.34-.16 2m.25 5.56c.6-1.11 1.06-2.31 1.38-3.56h2.95c-.96 1.65-2.49 2.93-4.33 3.56M16.36 14c.08-.66.14-1.32.14-2s-.06-1.34-.14-2h3.38c.16.64.26 1.31.26 2s-.1 1.36-.26 2z",
  }),
  "Language",
);

// src/components/button/FooterSocials.tsx
init_runtime2();
var import_jsx_runtime79 = __toESM(require_jsx_runtime(), 1);
function TikTokIcon(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime79.jsx)(SvgIcon_default, {
    ...props,
    viewBox: "0 0 24 24",
    children: /* @__PURE__ */ (0, import_jsx_runtime79.jsx)("path", {
      d: "M21 8.5a6.5 6.5 0 0 1-4-1.36v6.12a6.26 6.26 0 1 1-6.26-6.26c.3 0 .6.03.89.08v3.18a3.11 3.11 0 1 0 2.22 2.98V2h3.19a6.47 6.47 0 0 0 4.96 4.93V8.5z",
    }),
  });
}
function FooterSocials({ size = 16, target = "_blank" }) {
  const contacts = getContacts();
  const icons = [
    {
      name: "Facebook",
      type: "FACEBOOK",
      icon: /* @__PURE__ */ (0, import_jsx_runtime79.jsx)(Facebook_default, {}),
    },
    {
      name: "Instagram",
      type: "INSTAGRAM",
      icon: /* @__PURE__ */ (0, import_jsx_runtime79.jsx)(
        Instagram_default,
        {},
      ),
    },
    {
      name: "X",
      type: "TWITTER",
      icon: /* @__PURE__ */ (0, import_jsx_runtime79.jsx)(X_default, {}),
    },
    {
      name: "LinkedIn",
      type: "LINKEDIN",
      icon: /* @__PURE__ */ (0, import_jsx_runtime79.jsx)(LinkedIn_default, {}),
    },
    {
      name: "TikTok",
      type: "TIKTOK",
      icon: /* @__PURE__ */ (0, import_jsx_runtime79.jsx)(TikTokIcon, {}),
    },
    {
      name: "Website",
      type: "WEBSITE",
      icon: /* @__PURE__ */ (0, import_jsx_runtime79.jsx)(Language_default, {}),
    },
  ]
    .map((item) => ({
      ...item,
      url: contacts.find((contact) => contact.type === item.type)?.value,
    }))
    .filter((item) => !!item.url);
  if (icons.length === 0) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime79.jsx)(Stack_default, {
    direction: "row",
    sx: {
      gap: "8px",
      mx: "-6px",
      color: "inherit",
      "& .MuiIconButton-root": { color: "currentColor", p: "6px" },
      "& .MuiSvgIcon-root": { fontSize: size },
      "& .MuiIconButton-root:hover": {
        opacity: 0.7,
        backgroundColor: "transparent",
      },
    },
    children: icons.map(({ name, url, icon }) =>
      /* @__PURE__ */ (0, import_jsx_runtime79.jsx)(
        IconButton_default,
        {
          component: "a",
          href: url,
          target,
          rel: "noopener noreferrer",
          "aria-label": name,
          children: icon,
        },
        name,
      ),
    ),
  });
}

// src/components/icons/icons.tsx
var import_jsx_runtime80 = __toESM(require_jsx_runtime(), 1);
function ChevronDownIcon({ size = 10, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("svg", {
    width: size,
    height: size * 0.6,
    viewBox: "0 0 10 6",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
      d: "M1 1l4 4 4-4",
      stroke: "currentColor",
      strokeWidth: "1.5",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
  });
}
function ChevronRightIcon({ size = 10, style: style4, ...props }) {
  const { flip: flip2, ...rest } = props;
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("svg", {
    width: size * (5.16 / 9.73),
    height: size,
    viewBox: "0 0 5.16 9.73",
    fill: "currentColor",
    "aria-hidden": true,
    style: { ...(flip2 && { transform: "scaleX(-1)" }), ...style4 },
    ...rest,
    children: /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
      d: "M0.151 0.158C0.352 -0.053 0.679 -0.053 0.88 0.158L5.005 4.48C5.207 4.691 5.207 5.033 5.005 5.244L0.88 9.565C0.679 9.776 0.352 9.776 0.151 9.565C-0.05 9.354 -0.05 9.012 0.151 8.801L3.911 4.862L0.151 0.922C-0.05 0.711 -0.05 0.369 0.151 0.158Z",
    }),
  });
}
function LongArrowIcon({ size = 14, direction = "right", ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("svg", {
    width: size,
    height: size * (10 / 14),
    viewBox: "0 0 14 10",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
      d: direction === "right" ? "M1 5h12M9 1l4 4-4 4" : "M13 5H1M5 1L1 5l4 4",
      stroke: "currentColor",
      strokeWidth: "1.5",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
  });
}
function PlayIcon({ size = 20, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("svg", {
    width: size * (16 / 20),
    height: size,
    viewBox: "0 0 16 20",
    fill: "currentColor",
    "aria-hidden": true,
    ...props,
    children: /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
      d: "M1.4.6a1 1 0 0 0-1.4.9v17a1 1 0 0 0 1.5.9l14.1-8.5a1 1 0 0 0 0-1.7L1.4.6Z",
    }),
  });
}
function DocumentIcon({ size = 17, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsxs)("svg", {
    width: size * (12 / 17),
    height: size,
    viewBox: "0 0 12 17",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
        d: "M7 .8H2.4A1.6 1.6 0 0 0 .8 2.4v12.2a1.6 1.6 0 0 0 1.6 1.6h7.2a1.6 1.6 0 0 0 1.6-1.6V4.8L7 .8Z",
        stroke: "currentColor",
        strokeWidth: "1.3",
        strokeLinejoin: "round",
      }),
      /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
        d: "M7 .8v4h4.2",
        stroke: "currentColor",
        strokeWidth: "1.3",
        strokeLinejoin: "round",
      }),
    ],
  });
}
function DownloadIcon({ size = 12, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("svg", {
    width: size * (10 / 12),
    height: size,
    viewBox: "0 0 10 12",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
      d: "M5 .8v7.4M1.6 5.4 5 8.8l3.4-3.4M.8 11.2h8.4",
      stroke: "currentColor",
      strokeWidth: "1.5",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
  });
}
function LocationIcon({ size = 20, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsxs)("svg", {
    width: size,
    height: size,
    viewBox: "0 0 20 20",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
        d: "M10 17.5s5.5-4.9 5.5-9a5.5 5.5 0 1 0-11 0c0 4.1 5.5 9 5.5 9Z",
        stroke: "currentColor",
        strokeWidth: "1.4",
        strokeLinejoin: "round",
      }),
      /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("circle", {
        cx: "10",
        cy: "8.3",
        r: "2",
        stroke: "currentColor",
        strokeWidth: "1.4",
      }),
    ],
  });
}
function PhoneIcon({ size = 20, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("svg", {
    width: size,
    height: size,
    viewBox: "0 0 20 20",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
      d: "M6.6 3.5 8.2 6.4 6.7 8c.8 1.7 2 2.9 3.7 3.7l1.6-1.5 2.9 1.6v2.8c0 .6-.5 1.1-1.1 1C7.7 15 4.9 12.2 4.4 4.6c0-.6.4-1.1 1-1.1h1.2Z",
      stroke: "currentColor",
      strokeWidth: "1.4",
      strokeLinejoin: "round",
    }),
  });
}
function MailIcon({ size = 20, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsxs)("svg", {
    width: size,
    height: size,
    viewBox: "0 0 20 20",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("rect", {
        x: "3",
        y: "5",
        width: "14",
        height: "10",
        rx: "2",
        stroke: "currentColor",
        strokeWidth: "1.4",
      }),
      /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
        d: "m3.8 6.4 6.2 4.3 6.2-4.3",
        stroke: "currentColor",
        strokeWidth: "1.4",
        strokeLinejoin: "round",
      }),
    ],
  });
}
function ClockIcon({ size = 20, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsxs)("svg", {
    width: size,
    height: size,
    viewBox: "0 0 20 20",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("circle", {
        cx: "10",
        cy: "10",
        r: "6.5",
        stroke: "currentColor",
        strokeWidth: "1.4",
      }),
      /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
        d: "M10 6.2V10l2.6 1.6",
        stroke: "currentColor",
        strokeWidth: "1.4",
        strokeLinecap: "round",
        strokeLinejoin: "round",
      }),
    ],
  });
}
function CheckIcon({ size = 10, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("svg", {
    width: size,
    height: size * 0.8,
    viewBox: "0 0 10 8",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
      d: "M1 4.2l2.8 2.8L9 1",
      stroke: "currentColor",
      strokeWidth: "1.5",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
  });
}
function ArrowOutwardIcon({ size = 9, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("svg", {
    width: size,
    height: size,
    viewBox: "0 0 9 9",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
      d: "M1 8L8 1M3 1h5v5",
      stroke: "currentColor",
      strokeWidth: "1.5",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
  });
}
function ArrowUpIcon({ size = 14, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("svg", {
    width: size * (10 / 14),
    height: size,
    viewBox: "0 0 10 14",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
      d: "M5 13V1M1 5l4-4 4 4",
      stroke: "currentColor",
      strokeWidth: "1.4",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
  });
}
function CloseIcon({ size = 14, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("svg", {
    width: size,
    height: size,
    viewBox: "0 0 14 14",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
      d: "M1 1l12 12M13 1L1 13",
      stroke: "currentColor",
      strokeWidth: "1.5",
      strokeLinecap: "round",
    }),
  });
}
function MagnifierIcon({ size = 38, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsxs)("svg", {
    width: size,
    height: size,
    viewBox: "0 0 38 38",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("circle", {
        cx: "16",
        cy: "16",
        r: "12.4",
        stroke: "currentColor",
        strokeWidth: "1.6",
      }),
      /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
        d: "m25.2 25.2 9 9",
        stroke: "currentColor",
        strokeWidth: "1.6",
        strokeLinecap: "round",
      }),
    ],
  });
}
function PlusIcon({ size = 14, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("svg", {
    width: size,
    height: size,
    viewBox: "0 0 14 14",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
      d: "M7 0v14M0 7h14",
      stroke: "currentColor",
      strokeWidth: "1.5",
      strokeLinecap: "round",
    }),
  });
}
function MinusIcon({ size = 14, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("svg", {
    width: size,
    height: size,
    viewBox: "0 0 14 14",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
      d: "M0 7h14",
      stroke: "currentColor",
      strokeWidth: "1.5",
      strokeLinecap: "round",
    }),
  });
}
function SearchIcon({ size = 18, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsxs)("svg", {
    width: size,
    height: size,
    viewBox: "0 0 18 18",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("circle", {
        cx: "7.6",
        cy: "7.6",
        r: "5.9",
        stroke: "currentColor",
        strokeWidth: "1.4",
      }),
      /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
        d: "m11.9 11.9 4.1 4.1",
        stroke: "currentColor",
        strokeWidth: "1.4",
        strokeLinecap: "round",
      }),
    ],
  });
}
function MenuIcon({ size = 18, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("svg", {
    width: size,
    height: size / 2,
    viewBox: "0 0 18 9",
    fill: "none",
    "aria-hidden": true,
    ...props,
    children: /* @__PURE__ */ (0, import_jsx_runtime80.jsx)("path", {
      d: "M0 0.75h18M0 8.25h18",
      stroke: "currentColor",
      strokeWidth: "1.5",
    }),
  });
}

// src/page-content/components/footer/Footer.tsx
init_runtime2();

// src/core/translations.ts
var TITLE_WORD = new RegExp(
  "^(\\p{Lu}\\p{Ll}*(?:[-\u2019']\\p{L}+)*)(\\p{P}*)$",
  "u",
);
function toSentenceCase(text) {
  let sentenceStart = true;
  return text
    .split(/(\s+)/)
    .map((token2) => {
      if (token2 === "" || /^\s+$/.test(token2)) return token2;
      const match2 = token2.match(TITLE_WORD);
      const result =
        !sentenceStart && match2 && match2[1] !== "I"
          ? match2[1].toLocaleLowerCase("sl") + match2[2]
          : token2;
      sentenceStart = /[.!?:]$/.test(token2) || /^[-–—]$/.test(token2);
      return result;
    })
    .join("");
}
var converted = /* @__PURE__ */ new WeakMap();
function sentenceCaseDeep(value) {
  if (typeof value === "string") return toSentenceCase(value);
  if (!value || typeof value !== "object") return value;
  const cached = converted.get(value);
  if (cached) return cached;
  const mapped = Array.isArray(value)
    ? value.map((item) => sentenceCaseDeep(item))
    : Object.fromEntries(
        Object.entries(value).map(([key, item]) => [
          key,
          sentenceCaseDeep(item),
        ]),
      );
  converted.set(value, mapped);
  return mapped;
}
var sentenceCased =
  (fn2) =>
  (...args) =>
    sentenceCaseDeep(fn2(...args));
var getAboutTranslation2 = sentenceCased(getAboutTranslation);
var getBlogTranslation2 = sentenceCased(getBlogTranslation);
var getButtonTranslation2 = sentenceCased(getButtonTranslation);
var getCallToActionTranslation2 = sentenceCased(getCallToActionTranslation);
var getCareersTranslation2 = sentenceCased(getCareersTranslation);
var getCataloguesTranslation2 = sentenceCased(getCataloguesTranslation);
var getContactTranslation2 = sentenceCased(getContactTranslation);
var getDemoPopupTranslation2 = sentenceCased(getDemoPopupTranslation);
var getEventsTranslation2 = sentenceCased(getEventsTranslation);
var getFaqTranslation2 = sentenceCased(getFaqTranslation);
var getGalleryTranslation2 = sentenceCased(getGalleryTranslation);
var getLegalTranslation2 = sentenceCased(getLegalTranslation);
var getNotFoundTranslation2 = sentenceCased(getNotFoundTranslation);
var getPortfolioTranslation2 = sentenceCased(getPortfolioTranslation);
var getPriceTranslation2 = sentenceCased(getPriceTranslation);
var getPricingStoreTranslation2 = sentenceCased(getPricingStoreTranslation);
var getPricingTranslation_packagesNoImages2 = sentenceCased(
  getPricingTranslation_packagesNoImages,
);
var getPricingTranslation_priceListNoImages2 = sentenceCased(
  getPricingTranslation_priceListNoImages,
);
var getPricingTranslation_priceListWithImages2 = sentenceCased(
  getPricingTranslation_priceListWithImages,
);
var getReviewTranslation2 = sentenceCased(getReviewTranslation);
var getServicesTranslation2 = sentenceCased(getServicesTranslation);
var getSubscriptionsTranslation2 = sentenceCased(getSubscriptionsTranslation);
var getTeamTranslation2 = sentenceCased(getTeamTranslation);
var getVideosTranslation2 = sentenceCased(getVideosTranslation);
var getWhyUsTranslation2 = sentenceCased(getWhyUsTranslation);

// src/page-content/components/footer/Footer.tsx
var import_jsx_runtime81 = __toESM(require_jsx_runtime(), 1);
function BackToTop({ label }) {
  return /* @__PURE__ */ (0, import_jsx_runtime81.jsxs)(Box_default, {
    component: "a",
    href: "#main",
    onClick: (event) => {
      event.preventDefault();
      const reduceMotion = window.matchMedia(
        "(prefers-reduced-motion: reduce)",
      ).matches;
      window.scrollTo({ top: 0, behavior: reduceMotion ? "auto" : "smooth" });
    },
    sx: (theme2) => ({
      ...theme2.typography.button,
      flexShrink: 0,
      display: "inline-flex",
      alignItems: "center",
      gap: "10px",
      px: "22px",
      py: "13px",
      borderRadius: "999px",
      border: `1px solid ${theme2.palette.footer.buttonBorder}`,
      color: "inherit",
      transition: theme2.transitions.create(
        ["background-color", "color", "border-color"],
        {
          duration: theme2.transitions.duration.short,
        },
      ),
      "&:hover": {
        backgroundColor: theme2.palette.footer.text.primary,
        borderColor: theme2.palette.footer.text.primary,
        color: theme2.palette.footer.background,
      },
    }),
    children: [
      label,
      /* @__PURE__ */ (0, import_jsx_runtime81.jsx)(ArrowUpIcon, {}),
    ],
  });
}
function Footer() {
  const { lang } = useLanguage();
  const footerTranslation = getFooterTranslation(lang);
  const buttonTranslation = getButtonTranslation2(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime81.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "32px", sm: "36px", md: "44px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime81.jsxs)(Box_default, {
        sx: {
          display: "flex",
          flexDirection: { xs: "column", sm: "row" },
          alignItems: "center",
          justifyContent: "space-between",
          gap: 3,
          textAlign: { xs: "center", sm: "left" },
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime81.jsx)(Typography_default, {
            variant: "h4",
            component: "p",
            sx: { flex: { sm: 1 }, maxWidth: { md: 760 } },
            children: footerTranslation.slogan,
          }),
          /* @__PURE__ */ (0, import_jsx_runtime81.jsx)(BackToTop, {
            label: buttonTranslation.backToTop,
          }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime81.jsx)(Box_default, {
        "aria-hidden": true,
        sx: (theme2) => ({
          height: "1px",
          backgroundColor: theme2.palette.footer.divider,
        }),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime81.jsxs)(Box_default, {
        sx: {
          display: "flex",
          flexDirection: { xs: "column-reverse", sm: "row" },
          alignItems: "center",
          justifyContent: "space-between",
          gap: { xs: "18px", sm: 3 },
          textAlign: { xs: "center", sm: "left" },
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime81.jsxs)(Typography_default, {
            component: Link_default,
            href: "https://onas.si",
            target: "_blank",
            rel: "noopener noreferrer",
            variant: "caption",
            underline: "hover",
            sx: (theme2) => ({ color: theme2.palette.footer.text.secondary }),
            children: [
              "\xA9 ",
              footerTranslation.production,
              " ",
              BUILD_YEAR,
              ".",
            ],
          }),
          /* @__PURE__ */ (0, import_jsx_runtime81.jsx)(FooterSocials, {}),
        ],
      }),
    ],
  });
}

// src/page-content/components/header/Header.tsx
init_runtime2();

// src/components/language-selector/LanguageSelector.tsx
var import_react13 = __toESM(require_react(), 1);

// src/components/common/HoverDropdown.tsx
var import_react12 = __toESM(require_react(), 1);
var import_jsx_runtime82 = __toESM(require_jsx_runtime(), 1);
function HoverDropdown({
  trigger,
  items,
  onItemClick,
  placement = "bottom-start",
}) {
  const [anchorEl, setAnchorEl] = (0, import_react12.useState)(null);
  const timeoutRef = (0, import_react12.useRef)(null);
  const openMenu = (el) => {
    if (timeoutRef.current) clearTimeout(timeoutRef.current);
    setAnchorEl(el);
  };
  const closeMenuDelayed = () => {
    timeoutRef.current = setTimeout(() => setAnchorEl(null), 150);
  };
  const cancelClose = () => {
    if (timeoutRef.current) clearTimeout(timeoutRef.current);
  };
  const closeMenu = () => {
    if (timeoutRef.current) clearTimeout(timeoutRef.current);
    setAnchorEl(null);
  };
  const opensUp = placement.startsWith("top");
  const origin = `${opensUp ? "bottom" : "top"} ${placement.endsWith("end") ? "right" : "left"}`;
  const itemSx = (active) => (theme2) => ({
    ...theme2.typography.body1,
    px: 2.5,
    py: 1,
    color: active ? theme2.palette.primary.main : theme2.palette.text.primary,
    "&:hover": {
      color: theme2.palette.primary.main,
      backgroundColor: theme2.palette.surfaces.bgAlt,
    },
  });
  return /* @__PURE__ */ (0, import_jsx_runtime82.jsxs)(Box_default, {
    onMouseEnter: (e) => openMenu(e.currentTarget),
    onMouseLeave: closeMenuDelayed,
    onClick: (e) => openMenu(e.currentTarget),
    sx: { display: "inline-block", cursor: "pointer" },
    children: [
      trigger,
      /* @__PURE__ */ (0, import_jsx_runtime82.jsx)(Popper_default, {
        open: Boolean(anchorEl),
        anchorEl,
        placement,
        transition: true,
        disablePortal: true,
        style: { zIndex: 1300 },
        children: ({ TransitionProps }) =>
          /* @__PURE__ */ (0, import_jsx_runtime82.jsx)(Grow_default, {
            ...TransitionProps,
            style: { transformOrigin: origin },
            children: /* @__PURE__ */ (0, import_jsx_runtime82.jsx)(
              Paper_default,
              {
                onMouseEnter: cancelClose,
                onMouseLeave: closeMenuDelayed,
                sx: (theme2) => ({
                  ...(opensUp ? { mb: 1.5 } : { mt: 1.5 }),
                  minWidth: 200,
                  backgroundColor: theme2.palette.background.paper,
                  border: `1px solid ${theme2.palette.surfaces.border}`,
                  borderRadius: "16px",
                  boxShadow: `0 16px 40px ${alpha(theme2.palette.text.primary, 0.08)}`,
                  overflow: "hidden",
                }),
                children: /* @__PURE__ */ (0, import_jsx_runtime82.jsx)(
                  List_default,
                  {
                    sx: { py: 1 },
                    children: items.map((item, index) =>
                      "href" in item
                        ? /* @__PURE__ */ (0, import_jsx_runtime82.jsx)(
                            ListItemButton_default,
                            {
                              component: "a",
                              href: item.href,
                              "aria-current": item.active ? "page" : void 0,
                              onClick: (event) => {
                                event.stopPropagation();
                                onItemClick?.(event, item.href);
                                closeMenu();
                              },
                              sx: itemSx(item.active),
                              children: item.label,
                            },
                            index,
                          )
                        : /* @__PURE__ */ (0, import_jsx_runtime82.jsx)(
                            ListItemButton_default,
                            {
                              onClick: (event) => {
                                event.stopPropagation();
                                item.onClick();
                                closeMenu();
                              },
                              sx: itemSx(item.active),
                              children: item.label,
                            },
                            index,
                          ),
                    ),
                  },
                ),
              },
            ),
          }),
      }),
    ],
  });
}

// src/components/language-selector/LanguageSelector.tsx
var import_jsx_runtime83 = __toESM(require_jsx_runtime(), 1);
function LanguageSelector({
  supportedLanguages: supportedLanguages2,
  defaultLanguage,
  onChange,
  variant = "bar",
}) {
  const [selectedLang, setSelectedLang] = (0, import_react13.useState)(
    defaultLanguage || supportedLanguages2[0]?.code,
  );
  const handleSelect = (code) => {
    setSelectedLang(code);
    onChange(code);
  };
  const dropdownItems = supportedLanguages2.map((lang) => ({
    label: lang.label,
    onClick: () => handleSelect(lang.code),
    active: lang.code === selectedLang,
  }));
  const inMenu = variant === "menu";
  return /* @__PURE__ */ (0, import_jsx_runtime83.jsx)(HoverDropdown, {
    placement: inMenu ? "top-start" : "bottom-end",
    items: dropdownItems,
    trigger: /* @__PURE__ */ (0, import_jsx_runtime83.jsxs)(Box_default, {
      component: "button",
      type: "button",
      "aria-haspopup": "true",
      sx: (theme2) => ({
        ...theme2.typography.navLink,
        display: "inline-flex",
        alignItems: "center",
        gap: "8px",
        cursor: "pointer",
        border: 0,
        borderRadius: "999px",
        px: inMenu ? "24px" : { xs: "16px", sm: "20px", md: "26px" },
        py: inMenu ? "14px" : { xs: "10px", sm: "12px", md: "17px" },
        backgroundColor: theme2.palette.header.surface,
        color: theme2.palette.header.text,
        boxShadow: `inset 0 0 0 1px ${inMenu ? theme2.palette.header.border : "var(--pill-border, transparent)"}`,
      }),
      children: [
        selectedLang?.toUpperCase(),
        /* @__PURE__ */ (0, import_jsx_runtime83.jsx)(ChevronDownIcon, {}),
      ],
    }),
  });
}

// src/components/navigation/HeaderNavigation.tsx
var import_react15 = __toESM(require_react(), 1);
init_runtime2();

// src/components/navigation/MobileNavDrawer.tsx
var import_react14 = __toESM(require_react(), 1);
init_static2();
init_runtime2();
var import_jsx_runtime84 = __toESM(require_jsx_runtime(), 1);
var navSx = (active) => (theme2) => ({
  ...theme2.typography.navLink,
  fontSize: 20,
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
  width: "100%",
  p: 0,
  border: 0,
  background: "none",
  cursor: "pointer",
  textAlign: "left",
  color: active ? theme2.palette.primary.main : "inherit",
});
function Rule() {
  return /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(Box_default, {
    "aria-hidden": true,
    sx: (theme2) => ({
      height: "1px",
      flexShrink: 0,
      backgroundColor: theme2.palette.surfaces.border,
    }),
  });
}
function MobileSubmenu({ item, pathname: pathname2, onLinkClick }) {
  const [expanded, setExpanded] = (0, import_react14.useState)(true);
  return /* @__PURE__ */ (0, import_jsx_runtime84.jsxs)(Box_default, {
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime84.jsxs)(Box_default, {
        component: "button",
        type: "button",
        "aria-expanded": expanded,
        onClick: () => setExpanded((value) => !value),
        sx: navSx(false),
        children: [
          item.label,
          /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(Box_default, {
            component: "span",
            sx: {
              display: "flex",
              transition: "transform 0.2s ease",
              transform: expanded ? "rotate(180deg)" : "none",
            },
            children: /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(
              ChevronDownIcon,
              { size: 12 },
            ),
          }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(Collapse_default, {
        in: expanded,
        children: /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(Box_default, {
          sx: (theme2) => ({
            mt: "20px",
            pl: "16px",
            borderLeft: `1px solid ${theme2.palette.surfaces.border}`,
            display: "flex",
            flexDirection: "column",
            gap: "8px",
          }),
          children: item.subItems.map((sub) => {
            const active = isNavActive(pathname2, sub.href);
            return /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(
              Box_default,
              {
                component: "a",
                href: sub.href,
                "aria-current": active ? "page" : void 0,
                onClick: (event) => onLinkClick(event, sub.href),
                sx: (theme2) => ({
                  ...theme2.typography.body1,
                  color: active
                    ? theme2.palette.primary.main
                    : theme2.palette.text.primary,
                }),
                children: sub.label,
              },
              sub.href,
            );
          }),
        }),
      }),
    ],
  });
}
function MobileNavDrawer({ open, onClose, items }) {
  const pathname2 = usePathname();
  const { lang, setLang, languageList } = useLanguage();
  const home = getHome(lang);
  const handleLinkClick = (event, href) => {
    if (isCurrentPath(pathname2, href)) {
      event.preventDefault();
      onClose();
    }
  };
  return /* @__PURE__ */ (0, import_jsx_runtime84.jsxs)(Drawer_default, {
    anchor: "top",
    open,
    onClose,
    slotProps: {
      paper: {
        sx: (theme2) => ({
          // dvh, not vh: on a phone the URL bar is part of vh and the last item ends up
          // under it.
          height: "100dvh",
          backgroundColor: theme2.palette.background.default,
          backgroundImage: "none",
          color: theme2.palette.text.primary,
          px: { xs: "36px", sm: "60px" },
          display: "flex",
          flexDirection: "column",
          overflowY: "auto",
        }),
      },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime84.jsxs)(Box_default, {
        sx: {
          minHeight: 76,
          flexShrink: 0,
          display: "flex",
          alignItems: "center",
          justifyContent: "space-between",
          gap: 2,
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(Box_default, {
            component: "a",
            href: withBasePath("/"),
            sx: {
              display: "flex",
              alignItems: "center",
              minWidth: 0,
              color: "inherit",
            },
            children: home.logo.image
              ? /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(Box_default, {
                  component: "img",
                  src: home.logo.image,
                  alt: `${home.name} Logo`,
                  sx: {
                    height: 32,
                    maxWidth: 180,
                    width: "auto",
                    objectFit: "contain",
                  },
                })
              : /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(
                  Typography_default,
                  {
                    variant: "logo",
                    component: "span",
                    noWrap: true,
                    children: home.name,
                  },
                ),
          }),
          /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(IconButton_default, {
            onClick: onClose,
            "aria-label": lang === "SL" ? "Zapri meni" : "Close menu",
            sx: { color: "inherit", p: "10px", mr: "-10px" },
            children: /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(
              CloseIcon,
              {},
            ),
          }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(Rule, {}),
      /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(Box_default, {
        component: "nav",
        sx: {
          display: "flex",
          flexDirection: "column",
          gap: "30px",
          py: "30px",
        },
        children: items.map((item) => {
          if (item.subItems?.length) {
            return /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(
              MobileSubmenu,
              {
                item,
                pathname: pathname2,
                onLinkClick: handleLinkClick,
              },
              item.label,
            );
          }
          if (!item.href) return null;
          const active = isNavActive(pathname2, item.href);
          return /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(
            Box_default,
            {
              component: "a",
              href: item.href,
              "aria-current": active ? "page" : void 0,
              onClick: (event) => handleLinkClick(event, item.href),
              sx: navSx(active),
              children: item.label,
            },
            item.label,
          );
        }),
      }),
      languageList.length > 1 &&
        /* @__PURE__ */ (0, import_jsx_runtime84.jsxs)(
          import_jsx_runtime84.Fragment,
          {
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(Rule, {}),
              /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(Box_default, {
                sx: { pt: "30px", pb: "36px" },
                children: /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(
                  LanguageSelector,
                  {
                    variant: "menu",
                    supportedLanguages: languageList,
                    defaultLanguage: lang,
                    onChange: (code) => setLang(code),
                  },
                ),
              }),
            ],
          },
        ),
    ],
  });
}

// src/components/navigation/DesktopNavigation.tsx
init_static2();
var import_jsx_runtime85 = __toESM(require_jsx_runtime(), 1);
var linkSx = (theme2) => ({
  ...theme2.typography.navLink,
  position: "relative",
  display: "inline-flex",
  alignItems: "center",
  gap: "8px",
  py: 1,
  p: 0,
  border: 0,
  background: "none",
  cursor: "pointer",
  color: "inherit",
  transition: theme2.transitions.create(["opacity"], {
    duration: theme2.transitions.duration.shortest,
  }),
  "&:hover": { opacity: 0.7 },
});
var activeSx = {
  "&::after": {
    content: '""',
    position: "absolute",
    left: 0,
    right: 0,
    bottom: -6,
    height: "1px",
    backgroundColor: "currentColor",
  },
};
function DesktopNavigation({ items }) {
  const pathname2 = usePathname();
  const handleLinkClick = (event, href) => {
    if (isCurrentPath(pathname2, href)) {
      event.preventDefault();
    }
  };
  return /* @__PURE__ */ (0, import_jsx_runtime85.jsx)(Box_default, {
    component: "nav",
    sx: { display: "flex", alignItems: "center", gap: "30px" },
    children: items.map((item) => {
      if (item.subItems?.length) {
        const dropdownItems = item.subItems.map((sub) => ({
          label: sub.label,
          href: sub.href,
          active: isNavActive(pathname2, sub.href),
        }));
        const active2 = dropdownItems.some((sub) => sub.active);
        return /* @__PURE__ */ (0, import_jsx_runtime85.jsx)(
          HoverDropdown,
          {
            trigger: /* @__PURE__ */ (0, import_jsx_runtime85.jsxs)(
              Box_default,
              {
                component: "button",
                type: "button",
                "aria-haspopup": "true",
                sx: [linkSx, active2 && activeSx],
                children: [
                  item.label,
                  /* @__PURE__ */ (0, import_jsx_runtime85.jsx)(
                    ChevronDownIcon,
                    {},
                  ),
                ],
              },
            ),
            items: dropdownItems,
            onItemClick: handleLinkClick,
          },
          item.label,
        );
      }
      const active = isNavActive(pathname2, item.href);
      return /* @__PURE__ */ (0, import_jsx_runtime85.jsx)(
        Box_default,
        {
          component: "a",
          href: item.href || "#",
          "aria-current": active ? "page" : void 0,
          onClick: (event) => {
            if (item.href) {
              handleLinkClick(event, item.href);
            }
          },
          sx: [linkSx, active && activeSx],
          children: item.label,
        },
        item.label,
      );
    }),
  });
}

// src/components/navigation/HeaderNavigation.tsx
var import_jsx_runtime86 = __toESM(require_jsx_runtime(), 1);
function HeaderNavigation({ items }) {
  const { lang } = useLanguage();
  const [open, setOpen] = (0, import_react15.useState)(false);
  return /* @__PURE__ */ (0, import_jsx_runtime86.jsxs)(
    import_jsx_runtime86.Fragment,
    {
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime86.jsx)(Box_default, {
          sx: { display: { xs: "none", md: "flex" } },
          children: /* @__PURE__ */ (0, import_jsx_runtime86.jsx)(
            DesktopNavigation,
            { items },
          ),
        }),
        /* @__PURE__ */ (0, import_jsx_runtime86.jsx)(Box_default, {
          sx: { display: { xs: "flex", md: "none" } },
          children: /* @__PURE__ */ (0, import_jsx_runtime86.jsx)(
            IconButton_default,
            {
              onClick: () => setOpen(true),
              "aria-label": lang === "SL" ? "Odpri meni" : "Open menu",
              "aria-expanded": open,
              sx: { color: "inherit", p: "10px", ml: "-10px" },
              children: /* @__PURE__ */ (0, import_jsx_runtime86.jsx)(
                MenuIcon,
                {},
              ),
            },
          ),
        }),
        /* @__PURE__ */ (0, import_jsx_runtime86.jsx)(MobileNavDrawer, {
          open,
          onClose: () => setOpen(false),
          items,
        }),
      ],
    },
  );
}

// src/page-content/components/header/Header.tsx
init_static2();

// src/page-content/components/header/LogoImage.tsx
init_static2();
var import_react16 = __toESM(require_react(), 1);
var import_jsx_runtime87 = __toESM(require_jsx_runtime(), 1);
function LogoImage({ imageSrc, name }) {
  return /* @__PURE__ */ (0, import_jsx_runtime87.jsx)(Box_default, {
    component: "a",
    href: withBasePath("/"),
    sx: { display: "flex", alignItems: "center" },
    children: /* @__PURE__ */ (0, import_jsx_runtime87.jsx)(Box_default, {
      component: "img",
      src: imageSrc,
      alt: `${name} Logo`,
      sx: (theme2) => ({
        height: { xs: 28, sm: 32, md: 40 },
        maxWidth: { xs: 140, sm: 180, md: 220 },
        width: "auto",
        objectFit: "contain",
        filter: "var(--logo-filter, none)",
        transition: theme2.transitions.create(["opacity", "filter"], {
          duration: theme2.transitions.duration.short,
        }),
        "&:hover": { opacity: 0.8 },
      }),
    }),
  });
}
var LogoImage_default = (0, import_react16.memo)(LogoImage);

// src/page-content/components/header/LogoText.tsx
init_static2();
var import_react17 = __toESM(require_react(), 1);
var import_jsx_runtime88 = __toESM(require_jsx_runtime(), 1);
function phoneSize(name) {
  if (name.length > 16) return "18px";
  if (name.length > 10) return "20px";
  return void 0;
}
function LogoText({ name }) {
  const size = phoneSize(name);
  return /* @__PURE__ */ (0, import_jsx_runtime88.jsx)(Box_default, {
    component: "a",
    href: withBasePath("/"),
    sx: {
      display: "flex",
      alignItems: "center",
      color: "inherit",
      minWidth: 0,
      maxWidth: "100%",
    },
    children: /* @__PURE__ */ (0, import_jsx_runtime88.jsx)(
      Typography_default,
      {
        variant: "logo",
        component: "span",
        noWrap: true,
        sx: (theme2) => ({
          display: "block",
          maxWidth: { xs: "100%", sm: 260, md: 360 },
          ...(size && { [theme2.breakpoints.down("sm")]: { fontSize: size } }),
          transition: theme2.transitions.create(["opacity"], {
            duration: theme2.transitions.duration.short,
          }),
          "&:hover": { opacity: 0.8 },
        }),
        children: name,
      },
    ),
  });
}
var LogoText_default = (0, import_react17.memo)(LogoText);

// src/page-content/components/header/Header.tsx
var import_jsx_runtime89 = __toESM(require_jsx_runtime(), 1);
function Header() {
  const { lang, setLang, languageList } = useLanguage();
  const navTranslation = getNavigationTranslation(lang);
  const home = getHome(lang);
  const navItems = [
    { label: navTranslation.home, href: withBasePath("/") },
    {
      label: navTranslation.about,
      href: getPageSlugByKeyWithBasePath("about"),
    },
    {
      label: navTranslation.more,
      subItems: [
        {
          label: navTranslation.contact,
          href: getPageSlugByKeyWithBasePath("contact"),
        },
        isSectionEnabled("services") && {
          label: navTranslation.services,
          href: getPageSlugByKeyWithBasePath("services"),
        },
        isSectionEnabled("schedule") && {
          label: navTranslation.schedule,
          href: getPageSlugByKeyWithBasePath("schedule"),
        },
        isSectionEnabled("events") && {
          label: navTranslation.events,
          href: getPageSlugByKeyWithBasePath("events"),
        },
        isSectionEnabled("faq") && {
          label: navTranslation.faq,
          href: getPageSlugByKeyWithBasePath("faq"),
        },
        isSectionEnabled("portfolio") && {
          label: navTranslation.portfolio,
          href: getPageSlugByKeyWithBasePath("portfolio"),
        },
        isSectionEnabled("gallery") && {
          label: navTranslation.gallery,
          href: getPageSlugByKeyWithBasePath("gallery"),
        },
        isSectionEnabled("blog") && {
          label: navTranslation.blog,
          href: getPageSlugByKeyWithBasePath("blog"),
        },
        isSectionEnabled("videos") && {
          label: navTranslation.videos,
          href: getPageSlugByKeyWithBasePath("videos"),
        },
        isSectionEnabled("catalogues") && {
          label: navTranslation.catalogues,
          href: getPageSlugByKeyWithBasePath("catalogues"),
        },
        isSectionEnabled("pricing") && {
          label: navTranslation.pricing,
          href: getPageSlugByKeyWithBasePath("pricing"),
        },
        isSectionEnabled("careers") && {
          label: navTranslation.careers,
          href: getPageSlugByKeyWithBasePath("careers"),
        },
      ].filter(Boolean),
    },
  ];
  return /* @__PURE__ */ (0, import_jsx_runtime89.jsxs)(
    import_jsx_runtime89.Fragment,
    {
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime89.jsx)(Box_default, {
          sx: {
            justifySelf: "start",
            display: "flex",
            alignItems: "center",
            minWidth: 0,
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime89.jsx)(
            HeaderNavigation,
            { items: navItems },
          ),
        }),
        /* @__PURE__ */ (0, import_jsx_runtime89.jsx)(Box_default, {
          sx: { justifySelf: "center", minWidth: 0, maxWidth: "100%" },
          children: home.logo.image
            ? /* @__PURE__ */ (0, import_jsx_runtime89.jsx)(LogoImage_default, {
                imageSrc: home.logo.image,
                name: home.name,
              })
            : /* @__PURE__ */ (0, import_jsx_runtime89.jsx)(LogoText_default, {
                name: home.name,
              }),
        }),
        /* @__PURE__ */ (0, import_jsx_runtime89.jsx)(Box_default, {
          sx: { justifySelf: "end", display: "flex", alignItems: "center" },
          children:
            languageList.length > 1 &&
            /* @__PURE__ */ (0, import_jsx_runtime89.jsx)(LanguageSelector, {
              supportedLanguages: languageList,
              defaultLanguage: lang,
              onChange: (code) => setLang(code),
            }),
        }),
      ],
    },
  );
}

// src/page-content/components/section/about/AboutPreviewSection.tsx
init_runtime2();
init_static2();

// scripts/snapshot/marquee.tsx
var import_react18 = __toESM(require_react(), 1);
var import_jsx_runtime90 = __toESM(require_jsx_runtime(), 1);
var StaticMarquee = (0, import_react18.forwardRef)(function StaticMarquee2(
  { children, className, style: style4, direction = "left" },
  ref,
) {
  const vertical = direction === "up" || direction === "down";
  return /* @__PURE__ */ (0, import_jsx_runtime90.jsx)("div", {
    ref,
    className,
    "data-sitegen-static-marquee": "true",
    style: {
      display: "flex",
      width: "100%",
      overflow: "hidden",
      ...style4,
    },
    children: /* @__PURE__ */ (0, import_jsx_runtime90.jsx)("div", {
      style: {
        display: "flex",
        flexDirection: vertical ? "column" : "row",
        alignItems: "center",
        flex: "0 0 auto",
        minWidth: vertical ? void 0 : "100%",
      },
      children,
    }),
  });
});
var marquee_default = StaticMarquee;

// src/components/marquee/MarqueeBand.tsx
var import_jsx_runtime91 = __toESM(require_jsx_runtime(), 1);
function MarqueeBand({ items, speed = 40, tone = "plain" }) {
  if (items.length === 0) return null;
  const cream = tone === "cream" || tone === "quiet";
  return /* @__PURE__ */ (0, import_jsx_runtime91.jsx)(Box_default, {
    sx: (theme2) => ({
      width: "100vw",
      position: "relative",
      left: "50%",
      right: "50%",
      marginLeft: "-50vw",
      marginRight: "-50vw",
      height: { xs: 56, sm: 64, md: 72 },
      display: "flex",
      alignItems: "center",
      overflow: "hidden",
      borderBottom: `1px solid ${theme2.palette.surfaces.border}`,
      "&:not(.marquee-band + .marquee-band)": {
        borderTop: `1px solid ${theme2.palette.surfaces.border}`,
      },
      backgroundColor: cream
        ? theme2.palette.surfaces.bgAlt
        : theme2.palette.background.default,
      color: theme2.palette.text.primary,
    }),
    className: "marquee-band",
    children: /* @__PURE__ */ (0, import_jsx_runtime91.jsx)(marquee_default, {
      speed,
      gradient: false,
      autoFill: true,
      children: items.map((item, index) =>
        /* @__PURE__ */ (0, import_jsx_runtime91.jsxs)(
          Box_default,
          {
            sx: {
              display: "flex",
              alignItems: "center",
              gap: { xs: "22px", md: "28px" },
              pr: { xs: "22px", md: "28px" },
              /* autoFill counts copies from the measured group width; a group that can
               * shrink asks for ever more copies. Same trap as ClientLogoSlider. */
              flexShrink: 0,
              whiteSpace: "nowrap",
            },
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime91.jsx)(Box_default, {
                "aria-hidden": true,
                sx: (theme2) => ({
                  width: 7,
                  height: 7,
                  borderRadius: "50%",
                  backgroundColor: theme2.palette.primary.main,
                }),
              }),
              /* @__PURE__ */ (0, import_jsx_runtime91.jsx)(
                Typography_default,
                { variant: "h5", component: "span", children: item },
              ),
            ],
          },
          `${item}-${index}`,
        ),
      ),
    }),
  });
}

// src/page-content/components/section/common/EstablishedAndClients.tsx
init_runtime2();
var import_jsx_runtime92 = __toESM(require_jsx_runtime(), 1);
function EstablishedAndClients() {
  const { lang } = useLanguage();
  const aboutTranslation = getAboutTranslation2(lang);
  const aboutSection = getAboutSection(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime92.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      mt: { xs: "44px", sm: "56px", md: "100px" },
    },
    children: [
      aboutSection.establishedYear &&
        /* @__PURE__ */ (0, import_jsx_runtime92.jsx)(MarqueeBand, {
          items: [
            `${aboutTranslation.establishedIn} ${aboutSection.establishedYear}`,
          ],
          speed: 45,
          tone: "plain",
        }),
      /* @__PURE__ */ (0, import_jsx_runtime92.jsx)(MarqueeBand, {
        items: [`100 % ${aboutTranslation.satisfiedClients}`],
        speed: 60,
        tone: "cream",
      }),
    ],
  });
}

// src/components/button/ArrowButton.tsx
var import_jsx_runtime93 = __toESM(require_jsx_runtime(), 1);
function toneSx(tone, theme2) {
  const { palette: palette2 } = theme2;
  const toPrimary = {
    backgroundColor: palette2.primary.main,
    color: palette2.primary.contrastText,
  };
  switch (tone) {
    case "dark":
      return {
        base: {
          backgroundColor: palette2.text.primary,
          color: palette2.background.default,
        },
        hover: toPrimary,
      };
    case "border":
      return {
        base: {
          backgroundColor: "transparent",
          color: palette2.primary.main,
          boxShadow: `inset 0 0 0 1px ${palette2.primary.main}`,
        },
        hover: toPrimary,
      };
    // The Border variant drawn in the text colour instead of primary, as the blog opening uses it.
    case "outline":
      return {
        base: {
          backgroundColor: "transparent",
          color: palette2.text.primary,
          boxShadow: `inset 0 0 0 1px ${palette2.text.primary}`,
        },
        hover: {
          backgroundColor: palette2.text.primary,
          color: palette2.background.default,
        },
      };
    case "soft":
      return {
        base: {
          backgroundColor: palette2.surfaces.bgAlt,
          color: palette2.text.primary,
        },
        hover: toPrimary,
      };
    case "white":
      return {
        base: {
          backgroundColor: palette2.surfaces.onImage,
          color: palette2.text.primary,
        },
        hover: toPrimary,
      };
    case "text":
      return {
        base: {
          backgroundColor: "transparent",
          color: palette2.text.primary,
          minHeight: 0,
          px: 0,
          py: "7px",
          borderRadius: 0,
          boxShadow: `inset 0 -1px 0 ${palette2.text.primary}`,
        },
        hover: {
          backgroundColor: "transparent",
          color: palette2.primary.main,
          boxShadow: `inset 0 -1px 0 ${palette2.primary.main}`,
        },
      };
    default:
      return {
        base: toPrimary,
        hover: {
          backgroundColor: palette2.text.primary,
          color: palette2.background.default,
        },
      };
  }
}
function ArrowButton({
  tone = "primary",
  arrow: arrow2 = true,
  endIcon,
  sx,
  ...props
}) {
  return /* @__PURE__ */ (0, import_jsx_runtime93.jsx)(Button_default, {
    disableElevation: true,
    disableRipple: true,
    endIcon:
      endIcon ??
      (arrow2
        ? /* @__PURE__ */ (0, import_jsx_runtime93.jsx)(ChevronRightIcon, {})
        : void 0),
    ...props,
    sx: [
      (theme2) => {
        const { base, hover } = toneSx(tone, theme2);
        return {
          ...theme2.typography.button,
          minHeight: 52,
          px: "30px",
          py: "10px",
          gap: "7px",
          borderRadius: `${theme2.shape.borderRadius}px`,
          transition: theme2.transitions.create(
            ["background-color", "color", "box-shadow"],
            { duration: theme2.transitions.duration.short },
          ),
          "& .MuiButton-startIcon, & .MuiButton-endIcon": { m: 0 },
          "& .MuiButton-endIcon > .MuiSvgIcon-root, & .MuiButton-startIcon > .MuiSvgIcon-root":
            {
              fontSize: 18,
            },
          ...base,
          "&:hover": hover,
          "&.Mui-disabled": {
            ...base,
            opacity: 0.5,
          },
        };
      },
      ...(Array.isArray(sx) ? sx : [sx]),
    ],
  });
}

// src/page-content/components/section/common/RichText.tsx
var import_react19 = __toESM(require_react(), 1);
var import_jsx_runtime94 = __toESM(require_jsx_runtime(), 1);
function sanitizeRichText(text, allowStyling) {
  let result = text || "";
  if (!allowStyling.bold) {
    result = result.replace(/<\/?(strong|b)>/gi, "");
  }
  if (!allowStyling.italic) {
    result = result.replace(/<\/?(em|i)>/gi, "");
  }
  if (!allowStyling.underline) {
    result = result.replace(/<\/?u>/gi, "");
  }
  if (!allowStyling.newLine) {
    result = result
      .replace(/<br\s*\/?>/gi, " ")
      .replace(/<\/p>\s*<p>/gi, " ")
      .replace(/<\/div>\s*<div>/gi, " ")
      .replace(/<\/?p>/gi, "")
      .replace(/<\/?div>/gi, "");
  }
  return result;
}
function RichText({ text, allowStyling }) {
  const html2 = sanitizeRichText(text, allowStyling);
  return /* @__PURE__ */ (0, import_jsx_runtime94.jsx)(Box_default, {
    sx: {
      "& p": {
        m: 0,
        mb: allowStyling.newLine ? 1 : 0,
      },
      "& p:last-of-type": {
        mb: 0,
      },
      "& strong, & b": {
        fontWeight: allowStyling.bold ? 700 : "inherit",
      },
      "& em, & i": {
        fontStyle: allowStyling.italic ? "italic" : "normal",
      },
      "& u": {
        textDecoration: allowStyling.underline ? "underline" : "none",
      },
    },
    dangerouslySetInnerHTML: { __html: html2 },
  });
}
var RichText_default = (0, import_react19.memo)(RichText);

// src/components/carousel/Carousel.tsx
var import_react20 = __toESM(require_react(), 1);

// src/components/button/CircleButton.tsx
var import_jsx_runtime95 = __toESM(require_jsx_runtime(), 1);
function toneSx2(tone, theme2) {
  const { palette: palette2 } = theme2;
  const toPrimary = {
    backgroundColor: palette2.primary.main,
    color: palette2.primary.contrastText,
  };
  switch (tone) {
    case "soft":
      return {
        base: {
          backgroundColor: palette2.surfaces.bgAlt,
          color: palette2.text.primary,
        },
        hover: toPrimary,
      };
    case "dark":
      return {
        base: {
          backgroundColor: palette2.text.primary,
          color: palette2.background.default,
        },
        hover: toPrimary,
      };
    case "white":
      return {
        base: {
          backgroundColor: palette2.surfaces.onImage,
          color: palette2.text.primary,
        },
        hover: toPrimary,
      };
    default:
      return {
        base: {
          backgroundColor: "transparent",
          color: palette2.text.primary,
          boxShadow: `inset 0 0 0 1px ${palette2.surfaces.border}`,
        },
        hover: { boxShadow: `inset 0 0 0 1px ${palette2.text.primary}` },
      };
  }
}
function CircleButton({ tone = "outline", size = 52, sx, children, ...props }) {
  return /* @__PURE__ */ (0, import_jsx_runtime95.jsx)(ButtonBase_default, {
    ...props,
    sx: [
      (theme2) => {
        const { base, hover } = toneSx2(tone, theme2);
        return {
          width: size,
          height: size,
          flexShrink: 0,
          borderRadius: "50%",
          transition: theme2.transitions.create(
            ["background-color", "color", "box-shadow", "opacity"],
            { duration: theme2.transitions.duration.short },
          ),
          ...base,
          "&:hover": hover,
          "&.Mui-disabled": { opacity: 0.4 },
        };
      },
      ...(Array.isArray(sx) ? sx : [sx]),
    ],
    children,
  });
}

// src/components/carousel/CarouselControls.tsx
var import_jsx_runtime96 = __toESM(require_jsx_runtime(), 1);
var ARROW = { xs: 44, sm: 48, md: 52 };
function CarouselControls({
  page,
  pageCount,
  onPrev,
  onNext,
  prevTone = "soft",
}) {
  const share = 100 / Math.max(1, pageCount);
  return /* @__PURE__ */ (0, import_jsx_runtime96.jsxs)(Box_default, {
    sx: {
      display: "flex",
      alignItems: "center",
      gap: { xs: "24px", md: "32px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime96.jsx)(Box_default, {
        "aria-hidden": true,
        sx: (theme2) => ({
          position: "relative",
          flex: 1,
          height: "2px",
          backgroundColor: theme2.palette.surfaces.border,
        }),
        children: /* @__PURE__ */ (0, import_jsx_runtime96.jsx)(Box_default, {
          sx: (theme2) => ({
            position: "absolute",
            top: 0,
            bottom: 0,
            left: `${page * share}%`,
            width: `${share}%`,
            backgroundColor: theme2.palette.text.primary,
            transition: theme2.transitions.create(["left", "width"]),
          }),
        }),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime96.jsxs)(Box_default, {
        sx: { display: "flex", gap: "12px" },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime96.jsx)(CircleButton, {
            tone: prevTone,
            onClick: onPrev,
            disabled: page === 0,
            "aria-label": "Prej\u0161nje",
            sx: { width: ARROW, height: ARROW },
            children: /* @__PURE__ */ (0, import_jsx_runtime96.jsx)(
              LongArrowIcon,
              { direction: "left" },
            ),
          }),
          /* @__PURE__ */ (0, import_jsx_runtime96.jsx)(CircleButton, {
            tone: "dark",
            onClick: onNext,
            disabled: page >= pageCount - 1,
            "aria-label": "Naslednje",
            sx: { width: ARROW, height: ARROW },
            children: /* @__PURE__ */ (0, import_jsx_runtime96.jsx)(
              LongArrowIcon,
              {},
            ),
          }),
        ],
      }),
    ],
  });
}

// src/components/carousel/Carousel.tsx
var import_jsx_runtime97 = __toESM(require_jsx_runtime(), 1);
var px2 = (value) => (typeof value === "number" ? `${value}px` : value);
var toResponsive = (value) =>
  typeof value === "object"
    ? Object.fromEntries(Object.entries(value).map(([k, v]) => [k, px2(v)]))
    : px2(value);
function Carousel({
  items,
  perView = { mobile: 1, desktop: 2 },
  gap: gap2 = 24,
  ariaLabel,
  weights,
  slideWidth,
  controlsGap = { xs: 24, sm: 28, md: 32 },
  controlsSx,
  controlsPrevTone = "soft",
  autoPlayMs,
}) {
  const theme2 = useTheme5();
  const isDesktop = useMediaQuery_default(theme2.breakpoints.up("md"));
  const visible = isDesktop ? perView.desktop : perView.mobile;
  const fixedSlides = Boolean(slideWidth);
  const gapCss = toResponsive(gap2);
  const usableWeights =
    !fixedSlides && isDesktop && weights && weights.length === visible
      ? weights
      : null;
  const track = `calc(100% - (${visible} - 1) * var(--carousel-gap))`;
  const autoColumns = fixedSlides
    ? toResponsive(slideWidth)
    : usableWeights
      ? usableWeights.map((w) => `calc(${track} * ${w})`).join(" ")
      : `calc(${track} / ${visible})`;
  const trackRef = (0, import_react20.useRef)(null);
  const [page, setPage] = (0, import_react20.useState)(0);
  const [pageCount, setPageCount] = (0, import_react20.useState)(
    fixedSlides ? items.length : Math.max(1, Math.ceil(items.length / visible)),
  );
  const stepOf = (0, import_react20.useCallback)(
    (node2) => {
      if (!fixedSlides) return node2.clientWidth || 1;
      const first = node2.firstElementChild;
      const columnGap2 = parseFloat(getComputedStyle(node2).columnGap) || 0;
      return (first?.offsetWidth || node2.clientWidth || 1) + columnGap2;
    },
    [fixedSlides],
  );
  const sync = (0, import_react20.useCallback)(() => {
    const node2 = trackRef.current;
    if (!node2) return;
    const step = stepOf(node2);
    const columnGap2 = parseFloat(getComputedStyle(node2).columnGap) || 0;
    const inView = Math.max(
      1,
      Math.floor((node2.clientWidth + columnGap2 + 1) / step),
    );
    const count = fixedSlides
      ? Math.max(1, items.length - inView + 1)
      : Math.max(1, Math.ceil(items.length / visible));
    const maxScroll = node2.scrollWidth - node2.clientWidth;
    const atEnd = node2.scrollLeft >= maxScroll - 2;
    setPageCount(count);
    setPage(
      atEnd
        ? count - 1
        : Math.min(count - 1, Math.round(node2.scrollLeft / step)),
    );
  }, [fixedSlides, items.length, stepOf, visible]);
  (0, import_react20.useEffect)(() => {
    const node2 = trackRef.current;
    if (!node2) return;
    node2.addEventListener("scroll", sync, { passive: true });
    const observer = new ResizeObserver(sync);
    observer.observe(node2);
    sync();
    return () => {
      node2.removeEventListener("scroll", sync);
      observer.disconnect();
    };
  }, [sync]);
  const goTo = (0, import_react20.useCallback)(
    (target, behavior = "smooth") => {
      const node2 = trackRef.current;
      if (!node2) return;
      const clamped = Math.min(Math.max(target, 0), pageCount - 1);
      node2.scrollTo({ left: clamped * stepOf(node2), behavior });
    },
    [pageCount, stepOf],
  );
  const [paused, setPaused] = (0, import_react20.useState)(false);
  const [stopped, setStopped] = (0, import_react20.useState)(false);
  const [reduceMotion, setReduceMotion] = (0, import_react20.useState)(false);
  const dragStartX = (0, import_react20.useRef)(null);
  (0, import_react20.useEffect)(() => {
    const query = window.matchMedia("(prefers-reduced-motion: reduce)");
    const apply = () => setReduceMotion(query.matches);
    apply();
    query.addEventListener("change", apply);
    return () => query.removeEventListener("change", apply);
  }, []);
  (0, import_react20.useEffect)(() => {
    const onVisibilityChange = () => setPaused(document.hidden);
    document.addEventListener("visibilitychange", onVisibilityChange);
    return () =>
      document.removeEventListener("visibilitychange", onVisibilityChange);
  }, []);
  (0, import_react20.useEffect)(() => {
    if (!autoPlayMs || reduceMotion || paused || stopped || pageCount <= 1)
      return;
    const timer = window.setInterval(() => {
      const next2 = page + 1 >= pageCount ? 0 : page + 1;
      goTo(next2, next2 === 0 ? "auto" : "smooth");
    }, autoPlayMs);
    return () => window.clearInterval(timer);
  }, [autoPlayMs, goTo, page, pageCount, paused, reduceMotion, stopped]);
  const takeOver = () => setStopped(true);
  if (items.length === 0) return null;
  const showControls = pageCount > 1;
  return /* @__PURE__ */ (0, import_jsx_runtime97.jsxs)(Box_default, {
    "aria-roledescription": "carousel",
    "aria-label": ariaLabel,
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime97.jsx)(Box_default, {
        ref: trackRef,
        onMouseEnter: () => setPaused(true),
        onMouseLeave: () => setPaused(false),
        onFocus: () => setPaused(true),
        onBlur: (event) => {
          if (!event.currentTarget.contains(event.relatedTarget)) {
            setPaused(false);
          }
        },
        onPointerDown: (event) => {
          dragStartX.current = event.clientX;
          setPaused(true);
        },
        onPointerMove: (event) => {
          if (
            dragStartX.current !== null &&
            Math.abs(event.clientX - dragStartX.current) > 8
          ) {
            takeOver();
          }
        },
        onPointerUp: () => {
          dragStartX.current = null;
          setPaused(false);
        },
        onPointerCancel: () => {
          dragStartX.current = null;
          setPaused(false);
        },
        sx: {
          "--carousel-gap": gapCss,
          display: "grid",
          gridAutoFlow: "column",
          gridAutoColumns: autoColumns,
          gap: "var(--carousel-gap)",
          overflowX: "auto",
          scrollSnapType: "x mandatory",
          scrollbarWidth: "none",
          msOverflowStyle: "none",
          "&::-webkit-scrollbar": { display: "none" },
          /*
           * The track is dragged, not clicked, so it says so. A slide that is itself a link
           * still shows the hand, because the child's own cursor wins over the track's.
           */
          cursor: "grab",
          "&:active": { cursor: "grabbing" },
        },
        children: items.map((item, index) =>
          /* @__PURE__ */ (0, import_jsx_runtime97.jsx)(
            Box_default,
            { sx: { scrollSnapAlign: "start", minWidth: 0 }, children: item },
            index,
          ),
        ),
      }),
      showControls &&
        /* @__PURE__ */ (0, import_jsx_runtime97.jsx)(Box_default, {
          sx: [
            { mt: toResponsive(controlsGap) },
            ...(Array.isArray(controlsSx) ? controlsSx : [controlsSx]),
          ],
          children: /* @__PURE__ */ (0, import_jsx_runtime97.jsx)(
            CarouselControls,
            {
              page,
              pageCount,
              onPrev: () => {
                takeOver();
                goTo(page - 1);
              },
              onNext: () => {
                takeOver();
                goTo(page + 1);
              },
              prevTone: controlsPrevTone,
            },
          ),
        }),
    ],
  });
}

// src/components/media/GlassCaption.tsx
var import_jsx_runtime98 = __toESM(require_jsx_runtime(), 1);
function GlassCaption({ children, sx }) {
  return /* @__PURE__ */ (0, import_jsx_runtime98.jsx)(Box_default, {
    sx: [
      (theme2) => ({
        px: { xs: "18px", md: "22px" },
        py: { xs: "14px", md: "18px" },
        borderRadius: "12px",
        backgroundColor: theme2.palette.surfaces.glass,
        backdropFilter: "blur(12px)",
        WebkitBackdropFilter: "blur(12px)",
        color: theme2.palette.surfaces.onImage,
      }),
      ...(Array.isArray(sx) ? sx : [sx]),
    ],
    children: /* @__PURE__ */ (0, import_jsx_runtime98.jsx)(
      Typography_default,
      { variant: "h6", component: "p", sx: { color: "inherit" }, children },
    ),
  });
}

// src/page-content/components/section/about/ImageCarousel.tsx
var import_jsx_runtime99 = __toESM(require_jsx_runtime(), 1);
function AboutPhoto({ item, sx }) {
  return /* @__PURE__ */ (0, import_jsx_runtime99.jsxs)(Box_default, {
    sx: [
      (theme2) => ({
        position: "relative",
        overflow: "hidden",
        width: "100%",
        borderRadius: { xs: "20px", sm: "24px" },
        backgroundColor: theme2.palette.surfaces.placeholder,
      }),
      ...(Array.isArray(sx) ? sx : [sx]),
    ],
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime99.jsx)(Box_default, {
        component: "img",
        src: item.image,
        alt: "",
        loading: "lazy",
        sx: {
          position: "absolute",
          inset: 0,
          width: "100%",
          height: "100%",
          objectFit: "cover",
        },
      }),
      item.text &&
        /* @__PURE__ */ (0, import_jsx_runtime99.jsx)(GlassCaption, {
          sx: {
            position: "absolute",
            left: { xs: 16, md: 24 },
            right: { xs: 16, md: 24 },
            bottom: { xs: 16, md: 24 },
          },
          children: item.text,
        }),
    ],
  });
}
function ImageCarousel({ items }) {
  if (items.length === 0) return null;
  const slides = items.map((item, index) =>
    /* @__PURE__ */ (0, import_jsx_runtime99.jsx)(
      AboutPhoto,
      {
        item,
        sx: {
          height: { sm: 440, md: 560 },
          aspectRatio: { xs: "1 / 1", sm: "auto" },
        },
      },
      `${item.image}-${index}`,
    ),
  );
  return /* @__PURE__ */ (0, import_jsx_runtime99.jsx)(Carousel, {
    items: slides,
    slideWidth: { xs: "100%", sm: 520, md: 560 },
    gap: { xs: 16, sm: 20, md: 24 },
    controlsGap: { xs: 24, sm: 28, md: 32 },
    controlsSx: { maxWidth: { sm: 520, md: 560 } },
    ariaLabel: "Galerija o nas",
    autoPlayMs: 6e3,
  });
}

// src/page-content/components/section/about/AboutBlock.tsx
var import_jsx_runtime100 = __toESM(require_jsx_runtime(), 1);
function AboutBlock({ title, description, items, callToAction }) {
  const count = items.length;
  const centred = count === 0;
  const text = /* @__PURE__ */ (0, import_jsx_runtime100.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      alignItems: centred ? "center" : "flex-start",
      textAlign: centred ? "center" : "left",
      gap: { xs: "24px", md: "32px" },
      ...(centred && { maxWidth: 760, mx: "auto" }),
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime100.jsx)(Typography_default, {
        variant: "h2",
        component: "h2",
        children: title,
      }),
      description &&
        /* @__PURE__ */ (0, import_jsx_runtime100.jsx)(Typography_default, {
          component: "div",
          variant: "body1",
          children: /* @__PURE__ */ (0, import_jsx_runtime100.jsx)(
            RichText_default,
            {
              text: description,
              allowStyling: {
                newLine: true,
                bold: true,
                italic: true,
                underline: true,
              },
            },
          ),
        }),
      callToAction &&
        /* @__PURE__ */ (0, import_jsx_runtime100.jsx)(ArrowButton, {
          component: "a",
          href: callToAction.href,
          sx: { width: { xs: "100%", sm: "auto" } },
          children: callToAction.label,
        }),
    ],
  });
  if (centred) return text;
  return /* @__PURE__ */ (0, import_jsx_runtime100.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: { xs: "column", md: "row" },
      alignItems: { xs: "stretch", md: "center" },
      gap: { xs: "44px", sm: "56px", md: "80px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime100.jsx)(Box_default, {
        sx: { flex: { md: `0 0 ${count === 1 ? 520 : 440}px` }, minWidth: 0 },
        children: text,
      }),
      count === 1
        ? /* @__PURE__ */ (0, import_jsx_runtime100.jsx)(Box_default, {
            sx: { flex: { md: 1 }, minWidth: 0 },
            children: /* @__PURE__ */ (0, import_jsx_runtime100.jsx)(
              AboutPhoto,
              {
                item: items[0],
                sx: {
                  aspectRatio: { xs: "1 / 1", sm: "auto", md: "1 / 1" },
                  height: { sm: 480, md: "auto" },
                },
              },
            ),
          })
        : /* @__PURE__ */ (0, import_jsx_runtime100.jsx)(Box_default, {
            sx: {
              flex: { md: 1 },
              minWidth: 0,
              /*
               * Run the track to the screen edge. The margin is a percentage of the row, which
               * is the page's content width, so this is exactly the distance from the content
               * edge to the screen edge at any width.
               */
              mr: { sm: "calc((100% - 100vw) / 2)" },
            },
            children: /* @__PURE__ */ (0, import_jsx_runtime100.jsx)(
              ImageCarousel,
              { items },
            ),
          }),
    ],
  });
}

// src/page-content/components/section/about/AboutPreviewSection.tsx
var import_jsx_runtime101 = __toESM(require_jsx_runtime(), 1);
function AboutPreviewSection() {
  const { lang } = useLanguage();
  const aboutTranslation = getAboutTranslation2(lang);
  const buttonTranslation = getButtonTranslation2(lang);
  const aboutSection = getAboutSection(lang);
  const aboutItems = getAboutItems(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime101.jsxs)(
    import_jsx_runtime101.Fragment,
    {
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime101.jsx)(AboutBlock, {
          title: aboutSection.sectionName || aboutTranslation.title,
          description: aboutSection.text,
          items: aboutItems,
          callToAction: {
            label: buttonTranslation.learnMore,
            href: getPageSlugByKeyWithBasePath("about"),
          },
        }),
        /* @__PURE__ */ (0, import_jsx_runtime101.jsx)(
          EstablishedAndClients,
          {},
        ),
      ],
    },
  );
}

// src/page-content/components/section/blog/BlogPreviewSection.tsx
init_runtime2();
init_static2();

// src/page-content/components/section/common/CenteredIntro.tsx
var import_jsx_runtime102 = __toESM(require_jsx_runtime(), 1);
function CenteredIntro({
  title,
  description,
  align = "center",
  descriptionMaxWidth = 640,
}) {
  const left2 = align === "left";
  return /* @__PURE__ */ (0, import_jsx_runtime102.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      alignItems: left2 ? "flex-start" : "center",
      textAlign: left2 ? "left" : "center",
      gap: left2 ? { xs: "18px", md: "20px" } : { xs: "20px", md: "24px" },
      ...(left2 && { maxWidth: 760 }),
    },
    children: [
      title &&
        /* @__PURE__ */ (0, import_jsx_runtime102.jsx)(Typography_default, {
          variant: "h2",
          component: "h2",
          children: title,
        }),
      description &&
        /* @__PURE__ */ (0, import_jsx_runtime102.jsx)(Typography_default, {
          component: "div",
          variant: "body1",
          sx: { maxWidth: { md: left2 ? "none" : descriptionMaxWidth } },
          children: /* @__PURE__ */ (0, import_jsx_runtime102.jsx)(
            RichText_default,
            {
              text: description,
              allowStyling: {
                newLine: true,
                bold: true,
                italic: true,
                underline: true,
              },
            },
          ),
        }),
    ],
  });
}

// src/page-content/components/section/blog/BlogGrid.tsx
init_static2();

// src/page-content/components/section/blog/BlogPreviewCard.tsx
init_runtime2();

// src/core/utils.ts
init_utils();

// src/page-content/components/section/blog/BlogPreviewCard.tsx
var import_jsx_runtime103 = __toESM(require_jsx_runtime(), 1);
function BlogPreviewCard({ href, image, title, text, author, date }) {
  const { lang } = useLanguage();
  const excerpt = stripRichText(truncateWordSafe(text, 150));
  const meta = [date ? formatEventDate(date, lang) : "", author]
    .filter(Boolean)
    .join(" \xB7 ");
  return /* @__PURE__ */ (0, import_jsx_runtime103.jsxs)(Box_default, {
    component: "a",
    href,
    sx: (theme2) => ({
      display: "flex",
      flexDirection: "column",
      gap: "20px",
      color: "inherit",
      textDecoration: "none",
      "&:hover .blog-img, &:focus-visible .blog-img": {
        transform: "scale(1.04)",
      },
      "&:hover .blog-title, &:focus-visible .blog-title": {
        color: theme2.palette.primary.main,
      },
    }),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime103.jsx)(Box_default, {
        sx: (theme2) => ({
          height: { xs: 210, sm: 220, md: 260 },
          borderRadius: "12px",
          overflow: "hidden",
          backgroundColor: theme2.palette.surfaces.placeholder,
        }),
        children: /* @__PURE__ */ (0, import_jsx_runtime103.jsx)(Box_default, {
          component: "img",
          src: image,
          alt: "",
          loading: "lazy",
          className: "blog-img",
          sx: (theme2) => ({
            width: "100%",
            height: "100%",
            objectFit: "cover",
            transition: theme2.transitions.create(["transform"]),
          }),
        }),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime103.jsxs)(Box_default, {
        sx: { display: "flex", flexDirection: "column", gap: "12px" },
        children: [
          meta &&
            /* @__PURE__ */ (0, import_jsx_runtime103.jsx)(Typography_default, {
              variant: "caption",
              sx: { color: "text.secondary" },
              children: meta,
            }),
          /* @__PURE__ */ (0, import_jsx_runtime103.jsx)(Typography_default, {
            className: "blog-title",
            variant: "h5",
            component: "h3",
            sx: (theme2) => ({
              transition: theme2.transitions.create(["color"], {
                duration: theme2.transitions.duration.short,
              }),
            }),
            children: title,
          }),
          excerpt &&
            /* @__PURE__ */ (0, import_jsx_runtime103.jsx)(Typography_default, {
              variant: "body1",
              sx: { color: "text.secondary" },
              children: excerpt,
            }),
        ],
      }),
    ],
  });
}

// src/page-content/components/section/blog/BlogGrid.tsx
var import_jsx_runtime104 = __toESM(require_jsx_runtime(), 1);
function BlogGrid({ items }) {
  if (items.length === 0) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime104.jsx)(Box_default, {
    sx: {
      display: "grid",
      gridTemplateColumns: {
        xs: "1fr",
        sm: "repeat(2, 1fr)",
        md: "repeat(3, 1fr)",
      },
      columnGap: "24px",
      rowGap: "40px",
      alignItems: "start",
    },
    children: items.map((item) =>
      /* @__PURE__ */ (0, import_jsx_runtime104.jsx)(
        BlogPreviewCard,
        {
          image: item.image,
          title: item.title,
          text: item.description,
          author: item.author,
          date: item.date,
          href: `${getPageSlugByKeyWithBasePath("blog")}/${getBlogSlugById(item.id)}`,
        },
        item.id,
      ),
    ),
  });
}

// src/page-content/components/section/blog/BlogPreviewSection.tsx
var import_jsx_runtime105 = __toESM(require_jsx_runtime(), 1);
function BlogPreviewSection() {
  const { lang } = useLanguage();
  const blogTranslation = getBlogTranslation2(lang);
  const blogSection = getBlogSection(lang);
  if (!blogSection) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime105.jsxs)(Box_default, {
    sx: { display: "flex", flexDirection: "column", gap: "48px" },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime105.jsxs)(Box_default, {
        sx: {
          display: "flex",
          flexDirection: { xs: "column", md: "row" },
          alignItems: { xs: "flex-start", md: "flex-end" },
          justifyContent: "space-between",
          gap: "24px",
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime105.jsx)(CenteredIntro, {
            align: "left",
            title: blogTranslation.title,
            description: blogSection.text,
          }),
          /* @__PURE__ */ (0, import_jsx_runtime105.jsx)(ArrowButton, {
            tone: "outline",
            component: "a",
            href: getPageSlugByKeyWithBasePath("blog"),
            sx: { flexShrink: 0, width: { xs: "100%", sm: "auto" } },
            children: blogTranslation.callToAction,
          }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime105.jsx)(BlogGrid, {
        items: getBlogItems(lang).slice(0, 3),
      }),
    ],
  });
}

// src/app/theme/utils/UseBannerImage.tsx
var useBannerImage2 = false
  ? null.useBannerImage
  : (init_useBannerImage_default(),
    __toCommonJS(useBannerImage_default_exports)).useBannerImage;

// src/page-content/components/section/call-to-action/CallToAction.tsx
init_runtime2();
var import_jsx_runtime106 = __toESM(require_jsx_runtime(), 1);
function CallToAction({ href }) {
  const { lang } = useLanguage();
  const callToActionTranslation = getCallToActionTranslation2(lang);
  const bannerImage = useBannerImage2();
  return /* @__PURE__ */ (0, import_jsx_runtime106.jsxs)(Box_default, {
    component: "section",
    sx: (theme2) => ({
      position: "relative",
      height: { xs: 420, md: 560 },
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      overflow: "hidden",
      backgroundColor: theme2.palette.surfaces.placeholder,
      backgroundImage: `url("${bannerImage}")`,
      backgroundSize: "cover",
      backgroundPosition: "center",
      color: theme2.palette.surfaces.onImage,
    }),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime106.jsx)(Box_default, {
        "aria-hidden": true,
        sx: (theme2) => ({
          position: "absolute",
          inset: 0,
          backgroundColor: theme2.palette.surfaces.scrim,
        }),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime106.jsxs)(Box_default, {
        sx: {
          position: "relative",
          width: "100%",
          /*
           * The caps count the page margin in, so the column itself is Figma's 318 / 696 /
           * 860. Capping at the content width and then adding the margin left the phone
           * button 246 wide instead of running the full column.
           */
          maxWidth: { xs: "100%", sm: 768, md: 860 },
          px: { xs: "36px", sm: "36px", md: 0 },
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          textAlign: "center",
          gap: { xs: "28px", sm: "32px", md: "40px" },
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime106.jsx)(Typography_default, {
            variant: "h1",
            component: "p",
            sx: { color: "inherit" },
            children: callToActionTranslation.title,
          }),
          /* @__PURE__ */ (0, import_jsx_runtime106.jsx)(ArrowButton, {
            tone: "white",
            component: "a",
            href,
            sx: { width: { xs: "100%", sm: "auto" } },
            children: callToActionTranslation.contactButton,
          }),
        ],
      }),
    ],
  });
}

// src/page-content/components/section/contact/ContactPreviewSection.tsx
init_runtime2();

// src/page-content/components/section/contact/ContactInfo.tsx
init_runtime2();

// src/page-content/components/section/contact/ContactInfoCard.tsx
var import_jsx_runtime107 = __toESM(require_jsx_runtime(), 1);
function ContactInfoCard({ label, icon, children }) {
  return /* @__PURE__ */ (0, import_jsx_runtime107.jsxs)(Box_default, {
    sx: { display: "flex", flexDirection: "column", gap: "12px" },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime107.jsx)(Box_default, {
        "aria-hidden": true,
        sx: (theme2) => ({
          width: 40,
          height: 40,
          borderRadius: "50%",
          display: "grid",
          placeItems: "center",
          backgroundColor: theme2.palette.surfaces.rose,
          color: theme2.palette.text.primary,
        }),
        children: icon,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime107.jsx)(Typography_default, {
        variant: "h6",
        component: "h3",
        children: label,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime107.jsx)(Box_default, {
        sx: {
          display: "flex",
          flexDirection: "column",
          gap: "4px",
          color: "text.secondary",
        },
        children,
      }),
    ],
  });
}

// src/page-content/components/section/contact/groupWorkingHours.ts
var weekdayTags = ["MONDAY", "TUESDAY", "WEDNESDAY", "THURSDAY", "FRIDAY"];
function groupWorkingHours(items) {
  const weekdays = items.filter((i) => weekdayTags.includes(i.tag));
  const weekend = items.filter((i) => !weekdayTags.includes(i.tag));
  const allWeekdaysSame =
    weekdays.every((i) => i.open) &&
    weekdays.every(
      (i) => i.from === weekdays[0].from && i.to === weekdays[0].to,
    );
  const grouped = [];
  if (allWeekdaysSame) {
    grouped.push({
      label: "Monday \u2013 Friday",
      days: weekdayTags,
      open: true,
      from: weekdays[0].from,
      to: weekdays[0].to,
    });
  } else {
    weekdays.forEach((i) =>
      grouped.push({
        label: i.tag,
        days: [i.tag],
        open: i.open,
        from: i.from,
        to: i.to,
      }),
    );
  }
  weekend.forEach((i) =>
    grouped.push({
      label: i.tag,
      days: [i.tag],
      open: i.open,
      from: i.from,
      to: i.to,
    }),
  );
  return grouped;
}

// src/page-content/components/section/contact/WorkingHours.tsx
var import_react21 = __toESM(require_react(), 1);
init_runtime2();
init_runtime2();
var import_jsx_runtime108 = __toESM(require_jsx_runtime(), 1);
var DAY_TAGS = [
  "SUNDAY",
  "MONDAY",
  "TUESDAY",
  "WEDNESDAY",
  "THURSDAY",
  "FRIDAY",
  "SATURDAY",
];
function WorkingHours() {
  const { lang } = useLanguage();
  const workingHoursTranslation = getContactTranslation2(lang).workingHours;
  const workingHours = getWorkingHours(lang);
  const grouped = (0, import_react21.useMemo)(
    () => groupWorkingHours(workingHours.items),
    [workingHours.items],
  );
  const currentDay = DAY_TAGS[/* @__PURE__ */ new Date().getDay()];
  return /* @__PURE__ */ (0, import_jsx_runtime108.jsxs)(Box_default, {
    sx: { display: "flex", flexDirection: "column", gap: 1 },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime108.jsx)(Box_default, {
        sx: { display: "flex", flexDirection: "column", gap: 0.75 },
        children: grouped.map((g, i) => {
          const isToday = g.days.includes(currentDay);
          const translated = g.days.map((d) => workingHoursTranslation.days[d]);
          const dayLabel =
            translated.length === 1
              ? translated[0]
              : `${translated[0]} \u2013 ${translated[translated.length - 1]}`;
          return /* @__PURE__ */ (0, import_jsx_runtime108.jsxs)(
            Box_default,
            {
              sx: {
                display: "flex",
                justifyContent: "space-between",
                gap: 2,
                color: isToday ? "primary.main" : "inherit",
              },
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime108.jsx)(
                  Typography_default,
                  { variant: "body1", children: dayLabel },
                ),
                /* @__PURE__ */ (0, import_jsx_runtime108.jsx)(
                  Typography_default,
                  {
                    variant: "body1",
                    sx: { opacity: isToday ? 1 : 0.7 },
                    children: g.open
                      ? `${g.from} \u2013 ${g.to}`
                      : workingHoursTranslation.closedLabel,
                  },
                ),
              ],
            },
            i,
          );
        }),
      }),
      workingHours.note &&
        /* @__PURE__ */ (0, import_jsx_runtime108.jsxs)(Typography_default, {
          variant: "caption",
          sx: { opacity: 0.6, mt: 0.5 },
          children: [
            workingHoursTranslation.noteLabel,
            ": ",
            workingHours.note,
          ],
        }),
    ],
  });
}

// src/page-content/components/section/contact/ContactInfo.tsx
var import_jsx_runtime109 = __toESM(require_jsx_runtime(), 1);
function ContactInfo() {
  const { lang } = useLanguage();
  const contactTranslation = getContactTranslation2(lang);
  const company = getCompany();
  const map = getMap();
  const contacts = getContacts();
  const workingHours = getWorkingHours();
  const email = contacts.find((c) => c.type === "EMAIL")?.value || "";
  const phone = contacts.find((c) => c.type === "PHONE")?.value || "";
  return /* @__PURE__ */ (0, import_jsx_runtime109.jsxs)(Box_default, {
    sx: {
      display: "grid",
      gridTemplateColumns: { xs: "1fr", sm: "repeat(2, minmax(0, 1fr))" },
      columnGap: "32px",
      rowGap: "36px",
      alignItems: "start",
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime109.jsxs)(ContactInfoCard, {
        label: contactTranslation.contactInfo.address.title,
        icon: /* @__PURE__ */ (0, import_jsx_runtime109.jsx)(LocationIcon, {}),
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime109.jsx)(Typography_default, {
            variant: "body1",
            children: company.address,
          }),
          /* @__PURE__ */ (0, import_jsx_runtime109.jsxs)(Typography_default, {
            variant: "body1",
            children: [company.postalCode, " ", company.postalOffice],
          }),
          map.url &&
            /* @__PURE__ */ (0, import_jsx_runtime109.jsx)(Link_default, {
              href: map.url,
              target: "_blank",
              rel: "noopener noreferrer",
              underline: "hover",
              variant: "body1",
              sx: { color: "primary.main", mt: "4px" },
              children: contactTranslation.contactInfo.address.callToAction,
            }),
        ],
      }),
      phone &&
        /* @__PURE__ */ (0, import_jsx_runtime109.jsx)(ContactInfoCard, {
          label: contactTranslation.contactInfo.phone.title,
          icon: /* @__PURE__ */ (0, import_jsx_runtime109.jsx)(PhoneIcon, {}),
          children: /* @__PURE__ */ (0, import_jsx_runtime109.jsx)(
            Typography_default,
            {
              component: Link_default,
              href: `tel:${phone.replace(/\s+/g, "")}`,
              underline: "hover",
              variant: "body1",
              color: "inherit",
              children: phone,
            },
          ),
        }),
      email &&
        /* @__PURE__ */ (0, import_jsx_runtime109.jsx)(ContactInfoCard, {
          label: contactTranslation.contactInfo.email.title,
          icon: /* @__PURE__ */ (0, import_jsx_runtime109.jsx)(MailIcon, {}),
          children: /* @__PURE__ */ (0, import_jsx_runtime109.jsx)(
            Typography_default,
            {
              component: Link_default,
              href: `mailto:${email}`,
              underline: "hover",
              variant: "body1",
              color: "inherit",
              sx: { wordBreak: "break-word" },
              children: email,
            },
          ),
        }),
      workingHours.enabled &&
        /* @__PURE__ */ (0, import_jsx_runtime109.jsx)(ContactInfoCard, {
          label: contactTranslation.workingHours.title,
          icon: /* @__PURE__ */ (0, import_jsx_runtime109.jsx)(ClockIcon, {}),
          children: /* @__PURE__ */ (0, import_jsx_runtime109.jsx)(
            WorkingHours,
            {},
          ),
        }),
    ],
  });
}

// src/page-content/components/section/contact/ContactForm.tsx
var import_react22 = __toESM(require_react(), 1);
init_runtime2();

// src/page-content/components/section/common/FormDisclaimer.tsx
init_runtime2();

// node_modules/.pnpm/@mui+icons-material@7.3.11__77544ce9cc2d00337704b0dadf8f242a/node_modules/@mui/icons-material/esm/InfoOutlined.js
var import_jsx_runtime110 = __toESM(require_jsx_runtime(), 1);
var InfoOutlined_default = createSvgIcon(
  /* @__PURE__ */ (0, import_jsx_runtime110.jsx)("path", {
    d: "M11 7h2v2h-2zm0 4h2v6h-2zm1-9C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2m0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8",
  }),
  "InfoOutlined",
);

// src/page-content/components/section/common/FormDisclaimer.tsx
init_static2();
var import_jsx_runtime111 = __toESM(require_jsx_runtime(), 1);
function FormDisclaimer({ isHighContrast = false }) {
  const { lang } = useLanguage();
  const formTranslations = getFormTranslation(lang);
  const legalPageHref = getPageSlugByKeyWithBasePath("legal");
  return /* @__PURE__ */ (0, import_jsx_runtime111.jsxs)(Typography_default, {
    color: isHighContrast ? "surfaces.onImage" : "text.primary",
    variant: "body2",
    sx: {
      display: "flex",
      alignItems: "center",
      flexWrap: "wrap",
      lineHeight: 1.3,
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime111.jsx)(InfoOutlined_default, {
        sx: { fontSize: 20, mr: 0.6 },
      }),
      formTranslations.disclaimerPrefix,
      "\xA0",
      /* @__PURE__ */ (0, import_jsx_runtime111.jsx)(Link_default, {
        href: legalPageHref,
        color: isHighContrast ? "surfaces.onImage" : "text.primary",
        underline: "always",
        sx: { fontWeight: 600 },
        children: formTranslations.termsAndConditions,
      }),
      "\xA0",
      formTranslations.disclaimerJoiner,
      "\xA0",
      /* @__PURE__ */ (0, import_jsx_runtime111.jsx)(Link_default, {
        href: legalPageHref,
        color: isHighContrast ? "surfaces.onImage" : "text.primary",
        underline: "always",
        sx: { fontWeight: 600 },
        children: formTranslations.privacyPolicy,
      }),
      ".",
    ],
  });
}

// src/page-content/components/section/contact/ContactForm.tsx
var import_jsx_runtime112 = __toESM(require_jsx_runtime(), 1);
var initialFormData = (subject) => ({
  name: "",
  email: "",
  subject: subject ?? "",
  message: "",
});
var fieldSx = (theme2) => ({
  "& .MuiOutlinedInput-root": {
    ...theme2.typography.body1,
    borderRadius: "8px",
    backgroundColor: theme2.palette.background.paper,
    "& fieldset": { borderColor: theme2.palette.surfaces.border },
    "&:hover fieldset": { borderColor: theme2.palette.text.primary },
    "&.Mui-focused fieldset": { borderColor: theme2.palette.primary.main },
  },
  "& .MuiOutlinedInput-input": {
    px: "20px",
    py: 0,
    height: 56,
    boxSizing: "border-box",
  },
  "& .MuiOutlinedInput-input::placeholder": {
    color: theme2.palette.text.secondary,
    opacity: 1,
  },
  "& .MuiInputBase-multiline": { p: 0 },
  "& .MuiInputBase-inputMultiline": { px: "20px", py: "16px", height: "auto" },
});
function ContactForm({ subject }) {
  const { lang } = useLanguage();
  const formTranslations = getFormTranslation(lang);
  const buttonTranslation = getButtonTranslation2(lang);
  const [formData, setFormData] = (0, import_react22.useState)(() =>
    initialFormData(subject),
  );
  const [errors, setErrors] = (0, import_react22.useState)({});
  const [isSubmitting, setIsSubmitting] = (0, import_react22.useState)(false);
  const [submitError, setSubmitError] = (0, import_react22.useState)("");
  const [dialogType, setDialogType] = (0, import_react22.useState)(null);
  const contactEmail = getContacts().find((c) => c.type === "EMAIL")?.value;
  const mailtoHref = `mailto:${contactEmail}?subject=${encodeURIComponent(
    formData.subject,
  )}&body=${encodeURIComponent(formData.message)}`;
  const handleChange = (field) => (event) => {
    const value = event.target.value;
    setFormData((current2) => ({ ...current2, [field]: value }));
    setErrors((current2) => ({ ...current2, [field]: void 0 }));
    setSubmitError("");
  };
  const validateForm = () => {
    const nextErrors = {};
    if (!formData.name.trim()) {
      nextErrors.name = formTranslations.name.errorMessage;
    }
    if (
      !formData.email.trim() ||
      !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(formData.email.trim())
    ) {
      nextErrors.email = formTranslations.email.errorMessage;
    }
    if (!formData.subject.trim()) {
      nextErrors.subject = formTranslations.subject.errorMessage;
    }
    if (!formData.message.trim()) {
      nextErrors.message = formTranslations.message.errorMessage;
    }
    setErrors(nextErrors);
    return Object.keys(nextErrors).length === 0;
  };
  const handleSubmit = async () => {
    if (isSubmitting || !validateForm()) {
      return;
    }
    setIsSubmitting(true);
    setSubmitError("");
    try {
      const response = await fetch(getPublicApiEndpoint("contact"), {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          name: formData.name.trim(),
          email: formData.email.trim().toLowerCase(),
          subject: formData.subject.trim(),
          message: formData.message.trim(),
          locale: lang,
        }),
      });
      if (!response.ok) {
        throw new Error("Failed to send contact message.");
      }
      setFormData(initialFormData(subject));
      setErrors({});
      setDialogType("success");
    } catch {
      if (contactEmail) {
        setDialogType("error");
      } else {
        setSubmitError(formTranslations.sendError);
      }
    } finally {
      setIsSubmitting(false);
    }
  };
  (0, import_react22.useEffect)(() => {
    setFormData((current2) => ({ ...current2, subject: subject ?? "" }));
  }, [subject]);
  return /* @__PURE__ */ (0, import_jsx_runtime112.jsxs)(
    import_jsx_runtime112.Fragment,
    {
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime112.jsxs)(Box_default, {
          component: "form",
          noValidate: true,
          autoComplete: "off",
          onSubmit: (event) => {
            event.preventDefault();
            void handleSubmit();
          },
          sx: (theme2) => ({
            display: "flex",
            flexDirection: "column",
            gap: "16px",
            p: { xs: "24px", sm: "36px", md: "48px" },
            borderRadius: "24px",
            backgroundColor: theme2.palette.surfaces.bgAlt,
          }),
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime112.jsxs)(Box_default, {
              sx: {
                display: "flex",
                flexDirection: { xs: "column", sm: "row" },
                gap: "16px",
              },
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(
                  TextField_default,
                  {
                    fullWidth: true,
                    required: true,
                    name: "name",
                    value: formData.name,
                    placeholder: formTranslations.name.placeholder,
                    error: Boolean(errors.name),
                    helperText: errors.name,
                    disabled: isSubmitting,
                    onChange: handleChange("name"),
                    slotProps: {
                      htmlInput: { "aria-label": formTranslations.name.label },
                    },
                    sx: fieldSx,
                  },
                ),
                /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(
                  TextField_default,
                  {
                    fullWidth: true,
                    required: true,
                    type: "email",
                    name: "email",
                    value: formData.email,
                    placeholder: formTranslations.email.placeholder,
                    error: Boolean(errors.email),
                    helperText: errors.email,
                    disabled: isSubmitting,
                    onChange: handleChange("email"),
                    slotProps: {
                      htmlInput: { "aria-label": formTranslations.email.label },
                    },
                    sx: fieldSx,
                  },
                ),
              ],
            }),
            /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(TextField_default, {
              fullWidth: true,
              required: true,
              name: "subject",
              value: formData.subject,
              placeholder: formTranslations.subject.placeholder,
              error: Boolean(errors.subject),
              helperText: errors.subject,
              disabled: isSubmitting,
              onChange: handleChange("subject"),
              slotProps: {
                htmlInput: { "aria-label": formTranslations.subject.label },
              },
              sx: fieldSx,
            }),
            /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(TextField_default, {
              fullWidth: true,
              required: true,
              multiline: true,
              rows: 5,
              name: "message",
              value: formData.message,
              placeholder: formTranslations.message.placeholder,
              error: Boolean(errors.message),
              helperText: errors.message,
              disabled: isSubmitting,
              onChange: handleChange("message"),
              slotProps: {
                htmlInput: { "aria-label": formTranslations.message.label },
              },
              sx: fieldSx,
            }),
            submitError &&
              /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(
                Typography_default,
                { color: "error", children: submitError },
              ),
            /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(FormDisclaimer, {
              isHighContrast: false,
            }),
            /* @__PURE__ */ (0, import_jsx_runtime112.jsxs)(Box_default, {
              sx: {
                position: "relative",
                alignSelf: { xs: "stretch", sm: "flex-start" },
                display: "flex",
                pointerEvents: isSubmitting ? "none" : "auto",
                opacity: isSubmitting ? 0.7 : 1,
              },
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(ArrowButton, {
                  type: "submit",
                  sx: { width: { xs: "100%", sm: "auto" } },
                  children: buttonTranslation.sendMessage,
                }),
                isSubmitting &&
                  /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(
                    CircularProgress_default,
                    {
                      size: 24,
                      sx: {
                        position: "absolute",
                        top: "50%",
                        left: "50%",
                        mt: "-12px",
                        ml: "-12px",
                      },
                    },
                  ),
              ],
            }),
          ],
        }),
        /* @__PURE__ */ (0, import_jsx_runtime112.jsxs)(Dialog_default, {
          open: dialogType !== null,
          onClose: () => setDialogType(null),
          fullWidth: true,
          maxWidth: "xs",
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(
              DialogTitle_default,
              {
                children:
                  dialogType === "success"
                    ? formTranslations.success.title
                    : formTranslations.sendError,
              },
            ),
            /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(
              DialogContent_default,
              {
                children:
                  dialogType === "success"
                    ? /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(
                        Typography_default,
                        { children: formTranslations.success.message },
                      )
                    : /* @__PURE__ */ (0, import_jsx_runtime112.jsxs)(
                        Typography_default,
                        {
                          children: [
                            formTranslations.contactMessage,
                            " ",
                            /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(
                              Box_default,
                              {
                                component: "a",
                                href: mailtoHref,
                                sx: { color: "inherit", fontWeight: 600 },
                                children: contactEmail,
                              },
                            ),
                            ".",
                          ],
                        },
                      ),
              },
            ),
            /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(
              DialogActions_default,
              {
                children: /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(
                  Button_default,
                  {
                    onClick: () => setDialogType(null),
                    children: formTranslations.success.close,
                  },
                ),
              },
            ),
          ],
        }),
      ],
    },
  );
}

// src/page-content/components/section/contact/CustomMap.tsx
init_runtime2();
init_runtime2();
var import_react23 = __toESM(require_react(), 1);
var import_jsx_runtime113 =
  /* Full-bleed band 400 tall, as drawn — it breaks out of the content container the
     same way the marquee bands do. */
  __toESM(require_jsx_runtime(), 1);
function CustomMap({ fadeRight }) {
  const { lang } = useLanguage();
  const [loading, setLoading] = (0, import_react23.useState)(true);
  const map = getMap();
  const company = getCompany();
  const contactTranslation = getContactTranslation2(lang);
  const mapUrl = (0, import_react23.useMemo)(() => {
    const base = formatGoogleMapsUrl(map.url);
    return lang ? `${base}&hl=${lang.toLowerCase()}` : base;
  }, [map.url, lang]);
  (0, import_react23.useEffect)(() => {
    setLoading(true);
  }, [mapUrl]);
  return /* @__PURE__ */ (0, import_jsx_runtime113.jsxs)(Box_default, {
    sx: {
      position: "relative",
      width: "100vw",
      left: "50%",
      right: "50%",
      marginLeft: "-50vw",
      marginRight: "-50vw",
      height: { xs: 300, sm: 360, md: 420 },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime113.jsx)(Fade_default, {
        in: loading,
        unmountOnExit: true,
        children: /* @__PURE__ */ (0, import_jsx_runtime113.jsx)(
          Skeleton_default,
          {
            variant: "rectangular",
            animation: "wave",
            sx: {
              position: "absolute",
              width: "100%",
              height: "100%",
            },
          },
        ),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime113.jsx)("iframe", {
        title: `${contactTranslation.contactInfo.address.title}: ${company.name}`,
        src: mapUrl,
        width: "100%",
        height: "100%",
        style: { border: 0, display: "block" },
        allowFullScreen: true,
        loading: "lazy",
        referrerPolicy: "no-referrer-when-downgrade",
        onLoad: () => setLoading(false),
      }),
      fadeRight &&
        /* @__PURE__ */ (0, import_jsx_runtime113.jsx)(Box_default, {
          sx: {
            position: "absolute",
            top: 0,
            right: 0,
            width: "20px",
            height: "100%",
            pointerEvents: "none",
            zIndex: 2,
            background: (theme2) =>
              `linear-gradient(to left, ${theme2.palette.background.default} 0%, transparent 100%)`,
          },
        }),
    ],
  });
}

// src/page-content/components/section/contact/ContactPreviewSection.tsx
var import_jsx_runtime114 = __toESM(require_jsx_runtime(), 1);
function ContactPreviewSection() {
  const { lang } = useLanguage();
  const contactTranslation = getContactTranslation2(lang);
  const map = getMap();
  return /* @__PURE__ */ (0, import_jsx_runtime114.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "72px", sm: "96px", md: "120px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime114.jsxs)(Box_default, {
        sx: {
          display: "flex",
          flexDirection: { xs: "column", md: "row" },
          alignItems: "flex-start",
          gap: { xs: "44px", md: "80px" },
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime114.jsxs)(Box_default, {
            sx: {
              flex: { md: "0 0 520px" },
              width: "100%",
              display: "flex",
              flexDirection: "column",
              gap: "40px",
            },
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime114.jsx)(
                Typography_default,
                {
                  variant: "h2",
                  component: "h2",
                  children: contactTranslation.title,
                },
              ),
              /* @__PURE__ */ (0, import_jsx_runtime114.jsx)(ContactInfo, {}),
            ],
          }),
          /* @__PURE__ */ (0, import_jsx_runtime114.jsx)(Box_default, {
            sx: { flex: { md: 1 }, width: "100%", minWidth: 0 },
            children: /* @__PURE__ */ (0, import_jsx_runtime114.jsx)(
              ContactForm,
              {},
            ),
          }),
        ],
      }),
      map.enabled &&
        /* @__PURE__ */ (0, import_jsx_runtime114.jsx)(CustomMap, {}),
    ],
  });
}

// src/page-content/components/section/gallery/GalleryPreviewSection.tsx
init_runtime2();
init_static2();

// src/page-content/components/section/common/CustomGallery.tsx
var import_react25 = __toESM(require_react(), 1);

// scripts/snapshot/gallery.tsx
function Gallery({ children }) {
  return children;
}
function Item({ children }) {
  return children({ ref: () => {}, open: () => {} });
}

// src/components/image/HoverZoomImage.tsx
var import_react24 = __toESM(require_react(), 1);
init_static2();
var import_jsx_runtime115 = __toESM(require_jsx_runtime(), 1);
var HoverZoomImage = (0, import_react24.forwardRef)(
  (
    {
      src,
      fallbackSrc = FALLBACK_IMAGE,
      alt,
      onClick,
      width: width2 = 300,
      sx,
      imgSx,
      loading = "lazy",
      zoomOnParentHover = false,
      className = "hover-zoom-image",
    },
    ref,
  ) => {
    return /* @__PURE__ */ (0, import_jsx_runtime115.jsx)(Box_default, {
      onClick,
      className,
      sx: {
        width: width2,
        overflow: "hidden",
        /* Only a picture that answers a click says so; `onClick` is optional here. */
        cursor: onClick ? "pointer" : void 0,
        display: "inline-block",
        position: "relative",
        "& img": {
          width: "100%",
          height: "100%",
          objectFit: "cover",
          // ensures cropping on zoom
          transition: "transform 0.3s ease, opacity 0.3s ease",
        },
        ...(zoomOnParentHover
          ? {
              ".zoom-image-parent:hover & img": {
                transform: "scale(1.1)",
              },
            }
          : {
              "&:hover img": {
                transform: "scale(1.1)",
              },
            }),
        ...sx,
      },
      children: /* @__PURE__ */ (0, import_jsx_runtime115.jsx)(Box_default, {
        component: "img",
        ref,
        src: src || fallbackSrc,
        alt,
        loading,
        sx: imgSx,
        onError: (e) => {
          e.currentTarget.src = fallbackSrc;
        },
      }),
    });
  },
);
HoverZoomImage.displayName = "HoverZoomImage";
var HoverZoomImage_default = HoverZoomImage;

// src/page-content/components/section/common/CustomGallery.tsx
var import_jsx_runtime116 = __toESM(require_jsx_runtime(), 1);
var LAYOUTS = {
  phone: {
    columns: 2,
    gap: 14,
    heights: [
      [210, 140, 170],
      [140, 210, 170],
    ],
  },
  tablet: {
    columns: 2,
    gap: 20,
    heights: [
      [300, 200, 240],
      [200, 300, 240],
    ],
  },
  desktop: {
    columns: 3,
    gap: 24,
    heights: [
      [380, 260],
      [260, 380],
      [320, 320],
    ],
  },
};
var CustomGallery = ({
  items,
  currentPageItems,
  variant = "mosaic",
  thumbSize = 160,
}) => {
  const theme2 = useTheme5();
  const isDesktop = useMediaQuery_default(theme2.breakpoints.up("md"));
  const isTablet = useMediaQuery_default(theme2.breakpoints.up("sm"));
  const layout = isDesktop
    ? LAYOUTS.desktop
    : isTablet
      ? LAYOUTS.tablet
      : LAYOUTS.phone;
  const [sizes, setSizes] = import_react25.default.useState({});
  import_react25.default.useEffect(() => {
    let cancelled = false;
    const missing = items.filter((src) => !sizes[src]);
    if (!missing.length) return;
    missing.forEach((src) => {
      const img = new Image();
      img.onload = () => {
        if (cancelled) return;
        setSizes((prev2) => ({
          ...prev2,
          [src]: {
            w: img.naturalWidth,
            h: img.naturalHeight,
          },
        }));
      };
      img.src = src;
    });
    return () => {
      cancelled = true;
    };
  }, [items, sizes]);
  const visibleSet = new Set(currentPageItems ?? items);
  const laidOut = items.filter((src) => visibleSet.has(src) && sizes[src]);
  const columns = Array.from({ length: layout.columns }, () => []);
  laidOut.forEach((src, index) => {
    columns[index % layout.columns].push(src);
  });
  const heightFor = (column2, position2) => {
    const pattern = layout.heights[column2 % layout.heights.length];
    return pattern[position2 % pattern.length];
  };
  if (variant === "strip") {
    return /* @__PURE__ */ (0, import_jsx_runtime116.jsx)(Gallery, {
      options: { loop: true, wheelToZoom: true },
      children: /* @__PURE__ */ (0, import_jsx_runtime116.jsx)(Box_default, {
        sx: { display: "flex", flexWrap: "wrap", gap: "12px" },
        children: laidOut.map((src, position2) => {
          const size = sizes[src];
          return /* @__PURE__ */ (0, import_jsx_runtime116.jsx)(
            Item,
            {
              original: src,
              thumbnail: src,
              width: size.w,
              height: size.h,
              children: ({ ref, open }) =>
                /* @__PURE__ */ (0, import_jsx_runtime116.jsx)(
                  HoverZoomImage_default,
                  {
                    ref,
                    src,
                    alt: `Gallery Image ${position2 + 1}`,
                    onClick: open,
                    width: thumbSize,
                    sx: {
                      height: thumbSize,
                      borderRadius: "12px",
                      display: "block",
                    },
                  },
                ),
            },
            `${src}-${position2}`,
          );
        }),
      }),
    });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime116.jsx)(Gallery, {
    options: { loop: true, wheelToZoom: true },
    children: /* @__PURE__ */ (0, import_jsx_runtime116.jsx)(Box_default, {
      sx: {
        display: "grid",
        gridTemplateColumns: `repeat(${layout.columns}, minmax(0, 1fr))`,
        gap: `${layout.gap}px`,
        alignItems: "start",
      },
      children: columns.map((column2, columnIndex) =>
        /* @__PURE__ */ (0, import_jsx_runtime116.jsx)(
          Box_default,
          {
            sx: {
              display: "flex",
              flexDirection: "column",
              gap: `${layout.gap}px`,
            },
            children: column2.map((src, position2) => {
              const size = sizes[src];
              const index = items.indexOf(src);
              return /* @__PURE__ */ (0, import_jsx_runtime116.jsx)(
                Item,
                {
                  original: src,
                  thumbnail: src,
                  width: size.w,
                  height: size.h,
                  children: ({ ref, open }) =>
                    /* @__PURE__ */ (0, import_jsx_runtime116.jsx)(
                      HoverZoomImage_default,
                      {
                        ref,
                        src,
                        alt: `Gallery Image ${index + 1}`,
                        onClick: open,
                        width: "100%",
                        sx: {
                          height: heightFor(columnIndex, position2),
                          borderRadius: "12px",
                          display: "block",
                        },
                      },
                    ),
                },
                `${src}-${position2}`,
              );
            }),
          },
          columnIndex,
        ),
      ),
    }),
  });
};

// src/page-content/components/section/gallery/GalleryPreviewSection.tsx
var import_jsx_runtime117 = __toESM(require_jsx_runtime(), 1);
function GalleryPreviewSection() {
  const { lang } = useLanguage();
  const galleryTranslation = getGalleryTranslation2(lang);
  const galleryItems = getGalleryItems().slice(0, 6);
  if (galleryItems.length === 0) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime117.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "40px", md: "56px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime117.jsx)(Typography_default, {
        variant: "h2",
        component: "h2",
        sx: { textAlign: "center" },
        children: galleryTranslation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime117.jsx)(CustomGallery, {
        items: galleryItems,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime117.jsx)(Box_default, {
        sx: {
          display: "flex",
          justifyContent: { xs: "stretch", sm: "center", md: "flex-start" },
        },
        children: /* @__PURE__ */ (0, import_jsx_runtime117.jsx)(ArrowButton, {
          component: "a",
          href: getPageSlugByKeyWithBasePath("gallery"),
          sx: { width: { xs: "100%", sm: "auto" } },
          children: galleryTranslation.callToAction,
        }),
      }),
    ],
  });
}

// src/page-content/components/section/home/HomeSection.tsx
init_runtime2();
init_static2();
var import_jsx_runtime118 = __toESM(require_jsx_runtime(), 1);
function HomeSection() {
  const bannerImage = useBannerImage2();
  const { lang } = useLanguage();
  const buttonTranslation = getButtonTranslation2(lang);
  const home = getHome(lang);
  const aboutAnchor = `#${getPageSlugByKey("about")}`;
  return /* @__PURE__ */ (0, import_jsx_runtime118.jsxs)(Box_default, {
    id: "home",
    component: "section",
    sx: (theme2) => ({
      position: "relative",
      height: "100dvh",
      minHeight: { xs: 640, sm: 720, md: 760 },
      overflow: "hidden",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      backgroundColor: theme2.palette.surfaces.placeholder,
      backgroundImage: `url("${bannerImage}")`,
      backgroundSize: "cover",
      backgroundPosition: "center",
      color: theme2.palette.surfaces.onImage,
    }),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime118.jsx)(Box_default, {
        "aria-hidden": true,
        sx: (theme2) => ({
          position: "absolute",
          inset: 0,
          backgroundColor: theme2.palette.surfaces.scrim,
        }),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime118.jsxs)(Box_default, {
        sx: {
          position: "relative",
          width: "100%",
          maxWidth: { md: 1020 },
          px: { xs: "36px", sm: "84px", md: "60px" },
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          textAlign: "center",
          gap: { xs: "24px", sm: "28px", md: "30px" },
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime118.jsx)(Typography_default, {
            variant: "h1",
            component: "h1",
            sx: { color: "inherit" },
            children: home.name,
          }),
          home.slogan &&
            /* @__PURE__ */ (0, import_jsx_runtime118.jsx)(Typography_default, {
              variant: "slogan",
              component: "p",
              sx: { color: "inherit", maxWidth: { sm: 520, md: 620 } },
              children: home.slogan,
            }),
          /* @__PURE__ */ (0, import_jsx_runtime118.jsx)(ArrowButton, {
            tone: "white",
            component: "a",
            href: aboutAnchor,
            children: buttonTranslation.discoverNow,
          }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime118.jsxs)(Box_default, {
        component: "a",
        href: aboutAnchor,
        sx: (theme2) => ({
          position: "absolute",
          left: "50%",
          transform: "translateX(-50%)",
          bottom: { xs: "36px", sm: "48px", md: "44px" },
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          gap: { xs: "12px", md: "14px" },
          color: "inherit",
          "&:hover .scroll-cue": {
            backgroundColor: theme2.palette.surfaces.onImage,
            color: theme2.palette.text.primary,
          },
        }),
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime118.jsx)(Typography_default, {
            variant: "caption",
            component: "span",
            sx: { color: "inherit", opacity: 0.85, whiteSpace: "nowrap" },
            children: buttonTranslation.scrollDown,
          }),
          /* @__PURE__ */ (0, import_jsx_runtime118.jsx)(Box_default, {
            component: "span",
            className: "scroll-cue",
            sx: (theme2) => ({
              width: { xs: 44, md: 48 },
              height: { xs: 44, md: 48 },
              borderRadius: "50%",
              border: `1px solid ${theme2.palette.surfaces.onImage}`,
              display: "flex",
              alignItems: "center",
              justifyContent: "center",
              transition: theme2.transitions.create(
                ["background-color", "color"],
                {
                  duration: theme2.transitions.duration.short,
                },
              ),
            }),
            children: /* @__PURE__ */ (0, import_jsx_runtime118.jsx)(
              ChevronDownIcon,
              { size: 12 },
            ),
          }),
        ],
      }),
    ],
  });
}

// src/page-content/components/section/portfolio/PortfolioPreviewSection.tsx
init_runtime2();
init_static2();

// src/page-content/components/section/portfolio/Portfolio.tsx
var import_react29 = __toESM(require_react(), 1);

// src/components/button/PaginationControls.tsx
var import_jsx_runtime119 = __toESM(require_jsx_runtime(), 1);
function pageList(page, count) {
  if (count <= 7) return Array.from({ length: count }, (_, i) => i + 1);
  const shown = [1, count, page - 1, page, page + 1]
    .filter((n) => n >= 1 && n <= count)
    .sort((a, b) => a - b)
    .filter((n, i, all) => all.indexOf(n) === i);
  return shown.flatMap((n, i) =>
    i > 0 && n - shown[i - 1] > 1 ? ["gap", n] : [n],
  );
}
var BUTTON = { xs: 36, sm: 40 };
function PaginationControls({ page, pageCount, onChange }) {
  if (pageCount <= 1) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime119.jsxs)(Box_default, {
    component: "nav",
    "aria-label": "Paginacija",
    sx: {
      display: "flex",
      alignItems: "center",
      flexWrap: "wrap",
      gap: "8px",
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime119.jsx)(CircleButton, {
        size: 40,
        onClick: () => onChange(page - 1),
        disabled: page <= 1,
        "aria-label": "Prej\u0161nja stran",
        sx: { width: BUTTON, height: BUTTON },
        children: /* @__PURE__ */ (0, import_jsx_runtime119.jsx)(
          LongArrowIcon,
          { direction: "left" },
        ),
      }),
      pageList(page, pageCount).map((entry, index) =>
        entry === "gap"
          ? /* @__PURE__ */ (0, import_jsx_runtime119.jsx)(
              Box_default,
              {
                component: "span",
                "aria-hidden": true,
                sx: (theme2) => ({
                  ...theme2.typography.subtitle1,
                  width: BUTTON,
                  textAlign: "center",
                }),
                children: "\u2026",
              },
              `gap-${index}`,
            )
          : /* @__PURE__ */ (0, import_jsx_runtime119.jsx)(
              Box_default,
              {
                component: "button",
                type: "button",
                "aria-label": `Stran ${entry}`,
                "aria-current": entry === page ? "page" : void 0,
                onClick: () => onChange(entry),
                sx: (theme2) => ({
                  ...theme2.typography.subtitle1,
                  lineHeight: 1,
                  width: BUTTON,
                  height: BUTTON,
                  borderRadius: "50%",
                  border: 0,
                  cursor: "pointer",
                  color: theme2.palette.text.primary,
                  backgroundColor:
                    entry === page
                      ? theme2.palette.surfaces.mint
                      : "transparent",
                  transition: theme2.transitions.create(["background-color"], {
                    duration: theme2.transitions.duration.short,
                  }),
                  "&:hover": {
                    backgroundColor:
                      entry === page
                        ? theme2.palette.surfaces.mint
                        : theme2.palette.surfaces.bgAlt,
                  },
                }),
                children: entry,
              },
              entry,
            ),
      ),
      /* @__PURE__ */ (0, import_jsx_runtime119.jsx)(CircleButton, {
        size: 40,
        onClick: () => onChange(page + 1),
        disabled: page >= pageCount,
        "aria-label": "Naslednja stran",
        sx: { width: BUTTON, height: BUTTON },
        children: /* @__PURE__ */ (0, import_jsx_runtime119.jsx)(
          LongArrowIcon,
          {},
        ),
      }),
    ],
  });
}

// src/page-content/components/section/portfolio/Portfolio.tsx
init_runtime2();

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/react/PreviousLocationContext.js
var import_jsx_runtime120 = __toESM(require_jsx_runtime(), 1);
var import_react26 = __toESM(require_react(), 1);
var PreviousLocationContext = (0, import_react26.createContext)(null);
var usePreviousLocation = () =>
  (0, import_react26.useContext)(PreviousLocationContext);

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/react/useBackToList.js
var normalizePath2 = (url) => url.split("?")[0].replace(/\/+$/, "") || "/";
function useBackToList(fallbackHref) {
  const router2 = useRouter();
  const previous = usePreviousLocation();
  return () => {
    if (previous && normalizePath2(previous) === normalizePath2(fallbackHref)) {
      router2.push(previous);
    } else {
      router2.push(fallbackHref);
    }
  };
}

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/react/useListFilters.js
var import_react27 = __toESM(require_react(), 1);
var SEARCH_DEBOUNCE = 300;
function useListFilters() {
  const router2 = useRouter();
  const pathname2 = usePathname();
  const searchParams2 = useSearchParams();
  const [selectedCategoryId, setCategoryState] = (0, import_react27.useState)(
    () => searchParams2.get("category") ?? "",
  );
  const [searchInput, setSearchInput] = (0, import_react27.useState)(
    () => searchParams2.get("q") ?? "",
  );
  const [searchQuery, setSearchQuery] = (0, import_react27.useState)(
    () => searchParams2.get("q") ?? "",
  );
  const [sortOrder, setSortState] = (0, import_react27.useState)(() =>
    searchParams2.get("sort") === "descending" ? "descending" : "ascending",
  );
  const [page, setPage] = (0, import_react27.useState)(() => {
    const parsed = parseInt(searchParams2.get("page") ?? "1", 10);
    return Number.isFinite(parsed) && parsed > 0 ? parsed : 1;
  });
  (0, import_react27.useEffect)(() => {
    if (searchInput === searchQuery) {
      return;
    }
    const timeout2 = setTimeout(() => {
      setSearchQuery(searchInput);
      setPage(1);
    }, SEARCH_DEBOUNCE);
    return () => clearTimeout(timeout2);
  }, [searchInput, searchQuery]);
  (0, import_react27.useEffect)(() => {
    const params = new URLSearchParams();
    if (searchQuery) params.set("q", searchQuery);
    if (selectedCategoryId) params.set("category", selectedCategoryId);
    if (sortOrder === "descending") params.set("sort", "descending");
    if (page > 1) params.set("page", String(page));
    const query = params.toString();
    router2.replace(query ? `${pathname2}?${query}` : pathname2, {
      scroll: false,
    });
  }, [searchQuery, selectedCategoryId, sortOrder, page, pathname2, router2]);
  const setSelectedCategoryId = (value) => {
    setCategoryState(value);
    setPage(1);
  };
  const setSortOrder = (value) => {
    setSortState(value);
    setPage(1);
  };
  return {
    searchInput,
    setSearchInput,
    searchQuery,
    selectedCategoryId,
    setSelectedCategoryId,
    sortOrder,
    setSortOrder,
    page,
    setPage,
  };
}

// node_modules/.pnpm/@ptlabTadej+sitegen-landing_c121c74d073afc1f5e4cb573b6b91796/node_modules/@ptlabTadej/sitegen-landing-core/dist/react/usePagination.js
var import_react28 = __toESM(require_react(), 1);
function usePagination(items, itemsPerPage) {
  const [page, setPage] = (0, import_react28.useState)(1);
  const pageCount = Math.ceil(items.length / itemsPerPage);
  const paginatedItems = (0, import_react28.useMemo)(() => {
    const start2 = (page - 1) * itemsPerPage;
    return items.slice(start2, start2 + itemsPerPage);
  }, [items, itemsPerPage, page]);
  const resetPage = () => setPage(1);
  return {
    page,
    setPage,
    pageCount,
    paginatedItems,
    resetPage,
  };
}

// src/page-content/components/section/portfolio/Portfolio.tsx
init_static2();

// src/components/common/FilterChips.tsx
var import_jsx_runtime121 = __toESM(require_jsx_runtime(), 1);
function FilterChips({ options, value, onChange, ariaLabel }) {
  if (options.length === 0) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime121.jsx)(Box_default, {
    role: "group",
    "aria-label": ariaLabel,
    sx: { display: "flex", flexWrap: "wrap", gap: "10px" },
    children: options.map((option) => {
      const selected = option.value === value;
      return /* @__PURE__ */ (0, import_jsx_runtime121.jsx)(
        Box_default,
        {
          component: "button",
          type: "button",
          "aria-pressed": selected,
          onClick: () => onChange(option.value),
          sx: (theme2) => ({
            ...theme2.typography.subtitle1,
            cursor: "pointer",
            border: 0,
            borderRadius: "999px",
            px: "20px",
            py: "10px",
            whiteSpace: "nowrap",
            transition: theme2.transitions.create(
              ["background-color", "box-shadow", "color"],
              { duration: theme2.transitions.duration.short },
            ),
            ...(selected
              ? {
                  backgroundColor: theme2.palette.text.primary,
                  color: theme2.palette.background.default,
                }
              : {
                  backgroundColor: "transparent",
                  color: theme2.palette.text.primary,
                  boxShadow: `inset 0 0 0 1px ${theme2.palette.surfaces.border}`,
                  "&:hover": {
                    boxShadow: `inset 0 0 0 1px ${theme2.palette.text.primary}`,
                  },
                }),
          }),
          children: option.label,
        },
        option.value,
      );
    }),
  });
}

// src/page-content/components/section/common/CategorySelector.tsx
init_runtime2();
var import_jsx_runtime122 = __toESM(require_jsx_runtime(), 1);
function CategorySelector({ categories, selectedIndex, onSelectIndex }) {
  const { lang } = useLanguage();
  const allLabel = getButtonTranslation2(lang).all;
  const options = [allLabel, ...categories];
  return /* @__PURE__ */ (0, import_jsx_runtime122.jsx)(FilterChips, {
    options: options.map((label, index) => ({ value: String(index), label })),
    value: String(selectedIndex),
    onChange: (value) => onSelectIndex(Number(value)),
    ariaLabel: allLabel,
  });
}

// src/page-content/components/section/portfolio/PortfolioPreviewCard.tsx
var import_jsx_runtime123 = __toESM(require_jsx_runtime(), 1);
function PortfolioPreviewCard({
  image,
  title,
  text = "",
  href,
  category,
  openLabel,
  variant = "full",
}) {
  const compact = variant === "compact";
  const truncated = compact ? "" : stripRichText(truncateWordSafe(text, 100));
  return /* @__PURE__ */ (0, import_jsx_runtime123.jsxs)(Box_default, {
    component: "a",
    href,
    sx: (theme2) => ({
      display: "flex",
      flexDirection: "column",
      borderRadius: "12px",
      overflow: "hidden",
      textDecoration: "none",
      color: theme2.palette.text.primary,
      backgroundColor: theme2.palette.surfaces.surface,
      "& .card-img": {
        transition: theme2.transitions.create(["transform"], {
          duration: theme2.transitions.duration.standard,
        }),
      },
      "&:hover .card-img, &:focus-visible .card-img": {
        transform: "scale(1.04)",
      },
      "@media (hover: hover)": {
        "& .card-reveal": { display: "none" },
        "& .card-open": { opacity: 0, transform: "translate(-50%, 8px)" },
        "&:hover .card-reveal, &:focus-visible .card-reveal": {
          display: "block",
        },
        "&:hover .card-open, &:focus-visible .card-open": {
          opacity: 1,
          transform: "translate(-50%, 0)",
        },
      },
    }),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime123.jsxs)(Box_default, {
        sx: (theme2) => ({
          position: "relative",
          flexShrink: 0,
          height: compact ? 200 : 300,
          overflow: "hidden",
          backgroundColor: theme2.palette.surfaces.placeholder,
        }),
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime123.jsx)(Box_default, {
            component: "img",
            src: image,
            alt: "",
            loading: "lazy",
            className: "card-img",
            sx: {
              position: "absolute",
              inset: 0,
              width: "100%",
              height: "100%",
              objectFit: "cover",
            },
          }),
          !compact &&
            category &&
            /* @__PURE__ */ (0, import_jsx_runtime123.jsx)(Box_default, {
              sx: (theme2) => ({
                ...theme2.typography.caption,
                position: "absolute",
                top: 12,
                left: 12,
                px: "10px",
                py: "5px",
                borderRadius: "6px",
                backgroundColor: theme2.palette.text.primary,
                color: theme2.palette.background.default,
              }),
              children: category,
            }),
          !compact &&
            openLabel &&
            /* @__PURE__ */ (0, import_jsx_runtime123.jsx)(Box_default, {
              className: "card-open",
              sx: (theme2) => ({
                position: "absolute",
                left: "50%",
                bottom: { xs: 20, md: 24 },
                transform: "translateX(-50%)",
                transition: theme2.transitions.create(
                  ["opacity", "transform"],
                  {
                    duration: theme2.transitions.duration.short,
                  },
                ),
              }),
              children: /* @__PURE__ */ (0, import_jsx_runtime123.jsx)(
                ArrowButton,
                {
                  tone: "dark",
                  component: "span",
                  tabIndex: -1,
                  sx: { whiteSpace: "nowrap" },
                  children: openLabel,
                },
              ),
            }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime123.jsxs)(Box_default, {
        sx: {
          display: "flex",
          flexDirection: "column",
          gap: "8px",
          ...(compact
            ? { px: "16px", pt: "14px", pb: "16px" }
            : {
                px: { xs: "20px", md: "24px" },
                pt: { xs: "18px", md: "20px" },
                pb: { xs: "22px", md: "24px" },
              }),
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime123.jsx)(Typography_default, {
            variant: compact ? "h6" : "h5",
            component: "h3",
            children: title,
          }),
          truncated &&
            /* @__PURE__ */ (0, import_jsx_runtime123.jsx)(Typography_default, {
              className: "card-reveal",
              variant: "body1",
              sx: { color: "text.secondary" },
              children: truncated,
            }),
        ],
      }),
    ],
  });
}

// src/page-content/components/section/portfolio/Portfolio.tsx
var import_jsx_runtime124 = __toESM(require_jsx_runtime(), 1);
function Portfolio({ maxCnt, callToAction }) {
  const { lang } = useLanguage();
  const buttonTranslation = getButtonTranslation2(lang);
  const portfolioSection = getPortfolioSection(lang);
  const portfolioItems = getPortfolioItems(lang);
  const categories = portfolioSection?.categories ?? [];
  const [selectedIndex, setSelectedIndex] = (0, import_react29.useState)(0);
  const selectedCategory =
    selectedIndex === 0 ? void 0 : categories[selectedIndex - 1];
  const filteredItems = selectedCategory
    ? portfolioItems.filter((item) => item.category.includes(selectedCategory))
    : portfolioItems;
  const { page, setPage, pageCount, paginatedItems, resetPage } = usePagination(
    filteredItems,
    6,
  );
  const displayedItems = maxCnt
    ? filteredItems.slice(0, maxCnt)
    : paginatedItems;
  const showPagination = !maxCnt && pageCount > 1;
  const cards = displayedItems.map((item) =>
    /* @__PURE__ */ (0, import_jsx_runtime124.jsx)(
      PortfolioPreviewCard,
      {
        title: item.title,
        text: item.text,
        image: item.images[0],
        category: item.category,
        openLabel: buttonTranslation.learnMore,
        href: withBasePath(
          `/${getPageSlugByKey("portfolio")}/${getPortfolioSlugById(item.id)}`,
        ),
      },
      item.id,
    ),
  );
  return /* @__PURE__ */ (0, import_jsx_runtime124.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "36px", md: "44px" },
    },
    children: [
      categories.length > 0 &&
        /* @__PURE__ */ (0, import_jsx_runtime124.jsx)(CategorySelector, {
          categories,
          selectedIndex,
          onSelectIndex: (index) => {
            setSelectedIndex(index);
            resetPage();
          },
        }),
      /* @__PURE__ */ (0, import_jsx_runtime124.jsx)(Box_default, {
        sx: {
          display: { xs: "none", md: "grid" },
          gridTemplateColumns: "repeat(3, 1fr)",
          gap: "24px",
          alignItems: "start",
        },
        children: cards,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime124.jsx)(Box_default, {
        sx: { display: { xs: "block", md: "none" } },
        children: /* @__PURE__ */ (0, import_jsx_runtime124.jsx)(
          Carousel,
          {
            items: cards,
            slideWidth: { xs: "100%", sm: "calc(50% - 12px)" },
            gap: 24,
            controlsGap: { xs: 24 },
          },
          `${selectedIndex}-${page}`,
        ),
      }),
      (callToAction || showPagination) &&
        /* @__PURE__ */ (0, import_jsx_runtime124.jsxs)(Box_default, {
          sx: {
            display: "flex",
            flexDirection: { xs: "column-reverse", md: "row" },
            alignItems: { xs: "stretch", sm: "flex-start", md: "center" },
            justifyContent: "space-between",
            gap: { xs: "24px", md: "32px" },
          },
          children: [
            callToAction
              ? /* @__PURE__ */ (0, import_jsx_runtime124.jsx)(ArrowButton, {
                  component: "a",
                  href: callToAction.href,
                  sx: { width: { xs: "100%", sm: "auto" } },
                  children: callToAction.label,
                })
              : /* @__PURE__ */ (0, import_jsx_runtime124.jsx)(Box_default, {}),
            showPagination &&
              /* @__PURE__ */ (0, import_jsx_runtime124.jsx)(
                PaginationControls,
                {
                  page,
                  pageCount,
                  onChange: setPage,
                },
              ),
          ],
        }),
    ],
  });
}

// src/page-content/components/section/portfolio/PortfolioPreviewSection.tsx
var import_jsx_runtime125 = __toESM(require_jsx_runtime(), 1);
function PortfolioPreviewSection() {
  const { lang } = useLanguage();
  const portfolioTranslation = getPortfolioTranslation2(lang);
  const portfolioSection = getPortfolioSection(lang);
  if (!portfolioSection) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime125.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "36px", md: "44px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime125.jsx)(CenteredIntro, {
        align: "left",
        title: portfolioSection.name || portfolioTranslation.title,
        description: portfolioSection.text,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime125.jsx)(Portfolio, {
        maxCnt: 6,
        callToAction: {
          label: portfolioTranslation.callToAction,
          href: getPageSlugByKeyWithBasePath("portfolio"),
        },
      }),
    ],
  });
}

// src/page-content/components/section/review/ReviewSection.tsx
init_runtime2();

// src/page-content/components/section/review/Reviews.tsx
init_runtime2();

// src/page-content/components/section/review/ReviewCard.tsx
var import_jsx_runtime126 = __toESM(require_jsx_runtime(), 1);
function linkLabel(href) {
  try {
    return new URL(href).hostname.replace(/^www\./, "");
  } catch {
    return href;
  }
}
function ReviewCard({ review }) {
  const { text, author, image, url } = review;
  const isLink = Boolean(url);
  return /* @__PURE__ */ (0, import_jsx_runtime126.jsxs)(Box_default, {
    ...(isLink
      ? {
          component: "a",
          href: url,
          target: "_blank",
          rel: "noopener noreferrer",
        }
      : {}),
    sx: (theme2) => ({
      height: "100%",
      display: "flex",
      flexDirection: "column",
      gap: "18px",
      px: "30px",
      py: "28px",
      borderRadius: "12px",
      backgroundColor: theme2.palette.background.paper,
      color: theme2.palette.text.primary,
      textDecoration: "none",
      ...(isLink && {
        "&:hover .review-link, &:focus-visible .review-link": {
          color: theme2.palette.primary.main,
        },
      }),
    }),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime126.jsx)(Typography_default, {
        "aria-hidden": true,
        component: "span",
        sx: (theme2) => ({
          fontFamily: theme2.typography.h1.fontFamily,
          fontSize: "56px",
          lineHeight: "36px",
          height: 36,
          color: theme2.palette.primary.main,
        }),
        children: "\u201C",
      }),
      /* @__PURE__ */ (0, import_jsx_runtime126.jsx)(Typography_default, {
        variant: "body1",
        sx: { flex: 1 },
        children: text,
      }),
      isLink &&
        /* @__PURE__ */ (0, import_jsx_runtime126.jsxs)(Box_default, {
          component: "span",
          className: "review-link",
          sx: (theme2) => ({
            ...theme2.typography.caption,
            alignSelf: "flex-start",
            display: "inline-flex",
            alignItems: "center",
            gap: "6px",
            color: theme2.palette.text.secondary,
            textDecoration: "underline",
            textUnderlineOffset: "3px",
            transition: theme2.transitions.create(["color"], {
              duration: theme2.transitions.duration.short,
            }),
          }),
          children: [
            linkLabel(url),
            /* @__PURE__ */ (0, import_jsx_runtime126.jsx)(ArrowOutwardIcon, {
              size: 7,
            }),
          ],
        }),
      /* @__PURE__ */ (0, import_jsx_runtime126.jsxs)(Box_default, {
        sx: (theme2) => ({
          display: "flex",
          alignItems: "center",
          gap: "14px",
          pt: "18px",
          borderTop: `1px solid ${theme2.palette.surfaces.border}`,
        }),
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime126.jsx)(Avatar_default, {
            src: image,
            alt: "",
            sx: { width: 50, height: 50 },
          }),
          /* @__PURE__ */ (0, import_jsx_runtime126.jsx)(Typography_default, {
            variant: "h6",
            component: "p",
            children: author,
          }),
        ],
      }),
    ],
  });
}

// src/page-content/components/section/review/Reviews.tsx
var import_jsx_runtime127 = __toESM(require_jsx_runtime(), 1);
function Reviews() {
  const { lang } = useLanguage();
  const reviewTranslation = getReviewTranslation2(lang);
  const reviewItems = getReviewItems(lang);
  if (reviewItems.length === 0) return null;
  const cards = reviewItems.map((item, index) =>
    /* @__PURE__ */ (0, import_jsx_runtime127.jsx)(
      ReviewCard,
      {
        review: {
          text: item.text,
          author: item.title,
          url: item.url,
          image: item.image,
        },
      },
      `${item.title}-${index}`,
    ),
  );
  return /* @__PURE__ */ (0, import_jsx_runtime127.jsxs)(
    import_jsx_runtime127.Fragment,
    {
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime127.jsx)(Box_default, {
          sx: {
            display: { xs: "none", md: "grid" },
            gridTemplateColumns: "repeat(3, 1fr)",
            gap: "24px",
            alignItems: "start",
          },
          children: cards,
        }),
        /* @__PURE__ */ (0, import_jsx_runtime127.jsx)(Box_default, {
          sx: { display: { xs: "block", md: "none" } },
          children: /* @__PURE__ */ (0, import_jsx_runtime127.jsx)(Carousel, {
            items: cards,
            slideWidth: { xs: "100%", sm: "calc(50% - 12px)" },
            gap: 24,
            controlsGap: { xs: 24 },
            controlsPrevTone: "white",
            ariaLabel: reviewTranslation.title,
            autoPlayMs: 6e3,
          }),
        }),
      ],
    },
  );
}

// src/page-content/components/section/review/ReviewSection.tsx
var import_jsx_runtime128 = __toESM(require_jsx_runtime(), 1);
function ReviewSection() {
  const { lang } = useLanguage();
  const reviewTranslation = getReviewTranslation2(lang);
  const reviewSection = getReviewSection(lang);
  if (!reviewSection) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime128.jsxs)(Box_default, {
    sx: { display: "flex", flexDirection: "column", gap: "48px" },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime128.jsx)(CenteredIntro, {
        title: reviewTranslation.title,
        description: reviewSection.text,
        descriptionMaxWidth: 860,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime128.jsx)(Reviews, {}),
    ],
  });
}

// src/page-content/components/section/services/ServicesPreviewSection.tsx
init_runtime2();
init_static2();

// src/page-content/components/section/services/Services.tsx
init_runtime2();

// src/page-content/components/section/services/ServiceCard.tsx
var import_jsx_runtime129 = __toESM(require_jsx_runtime(), 1);
function ServiceCard({ image, title, text, features = [], href, index = 0 }) {
  const openButton = href
    ? /* @__PURE__ */ (0, import_jsx_runtime129.jsx)(CircleButton, {
        tone: "white",
        size: 44,
        ...{ component: "a", href },
        "aria-label": title,
        children: /* @__PURE__ */ (0, import_jsx_runtime129.jsx)(
          ArrowOutwardIcon,
          {},
        ),
      })
    : null;
  return /* @__PURE__ */ (0, import_jsx_runtime129.jsxs)(Box_default, {
    sx: (theme2) => ({
      height: "100%",
      display: "flex",
      flexDirection: "column",
      borderRadius: "16px",
      overflow: "hidden",
      backgroundColor: image
        ? theme2.palette.surfaces.mint
        : index % 2 === 0
          ? theme2.palette.surfaces.rose
          : theme2.palette.surfaces.mint,
    }),
    children: [
      image &&
        /* @__PURE__ */ (0, import_jsx_runtime129.jsxs)(Box_default, {
          sx: (theme2) => ({
            position: "relative",
            flexShrink: 0,
            height: { xs: 240, md: 280 },
            backgroundColor: theme2.palette.surfaces.placeholder,
          }),
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime129.jsx)(Box_default, {
              component: "img",
              src: image,
              alt: "",
              loading: "lazy",
              sx: {
                position: "absolute",
                inset: 0,
                width: "100%",
                height: "100%",
                objectFit: "cover",
              },
            }),
            openButton &&
              /* @__PURE__ */ (0, import_jsx_runtime129.jsx)(Box_default, {
                sx: { position: "absolute", top: 16, right: 16 },
                children: openButton,
              }),
          ],
        }),
      /* @__PURE__ */ (0, import_jsx_runtime129.jsxs)(Box_default, {
        sx: {
          flex: 1,
          display: "flex",
          flexDirection: "column",
          gap: { xs: "12px", md: "14px" },
          px: { xs: "24px", md: "28px" },
          pt: { xs: "24px", md: "28px" },
          pb: { xs: "28px", md: "32px" },
        },
        children: [
          !image &&
            /* @__PURE__ */ (0, import_jsx_runtime129.jsx)(Box_default, {
              sx: {
                display: "flex",
                justifyContent: "flex-end",
                minHeight: 44,
                mb: { xs: "80px", md: "120px" },
              },
              children: openButton,
            }),
          /* @__PURE__ */ (0, import_jsx_runtime129.jsx)(Typography_default, {
            variant: "h5",
            component: "h3",
            children: title,
          }),
          text &&
            /* @__PURE__ */ (0, import_jsx_runtime129.jsx)(Typography_default, {
              variant: "body1",
              sx: { color: "text.secondary" },
              children: text,
            }),
          features.length > 0 &&
            /* @__PURE__ */ (0, import_jsx_runtime129.jsx)(Box_default, {
              component: "ul",
              sx: {
                listStyle: "none",
                display: "flex",
                flexDirection: "column",
                gap: "10px",
                mt: "6px",
              },
              children: features.map((feature, featureIndex) =>
                /* @__PURE__ */ (0, import_jsx_runtime129.jsxs)(
                  Box_default,
                  {
                    component: "li",
                    sx: {
                      display: "flex",
                      gap: "10px",
                      alignItems: "flex-start",
                    },
                    children: [
                      /* @__PURE__ */ (0, import_jsx_runtime129.jsx)(
                        Box_default,
                        {
                          "aria-hidden": true,
                          sx: (theme2) => ({
                            flexShrink: 0,
                            mt: "3px",
                            width: 18,
                            height: 18,
                            borderRadius: "50%",
                            display: "grid",
                            placeItems: "center",
                            backgroundColor: theme2.palette.background.paper,
                            color: theme2.palette.text.primary,
                          }),
                          children: /* @__PURE__ */ (0,
                          import_jsx_runtime129.jsx)(CheckIcon, { size: 9 }),
                        },
                      ),
                      /* @__PURE__ */ (0, import_jsx_runtime129.jsx)(
                        Typography_default,
                        {
                          variant: "body2",
                          sx: { color: "text.primary" },
                          children: feature,
                        },
                      ),
                    ],
                  },
                  featureIndex,
                ),
              ),
            }),
        ],
      }),
    ],
  });
}

// src/page-content/components/section/services/Services.tsx
var import_jsx_runtime130 = __toESM(require_jsx_runtime(), 1);
function Services({ maxCnt, href }) {
  const { lang } = useLanguage();
  const serviceItems = getServiceItems(lang).slice(0, maxCnt || void 0);
  if (serviceItems.length === 0) return null;
  const cards = serviceItems.map((service, index) =>
    /* @__PURE__ */ (0, import_jsx_runtime130.jsx)(
      ServiceCard,
      { index, href, ...service },
      index,
    ),
  );
  return /* @__PURE__ */ (0, import_jsx_runtime130.jsxs)(
    import_jsx_runtime130.Fragment,
    {
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime130.jsx)(Box_default, {
          sx: {
            display: { xs: "none", md: "grid" },
            gridTemplateColumns: "repeat(3, 1fr)",
            gap: "24px",
          },
          children: cards,
        }),
        /* @__PURE__ */ (0, import_jsx_runtime130.jsx)(Box_default, {
          sx: { display: { xs: "block", md: "none" } },
          children: /* @__PURE__ */ (0, import_jsx_runtime130.jsx)(Carousel, {
            items: cards,
            slideWidth: { xs: "100%", sm: "calc(50% - 12px)" },
            gap: 24,
            controlsGap: { xs: 24 },
          }),
        }),
      ],
    },
  );
}

// src/page-content/components/section/services/ServicesPreviewSection.tsx
var import_jsx_runtime131 = __toESM(require_jsx_runtime(), 1);
function ServicesPreviewSection() {
  const { lang } = useLanguage();
  const servicesTranslation = getServicesTranslation2(lang);
  const servicesSection = getServicesSection(lang);
  if (!servicesSection) {
    return null;
  }
  const servicesHref = getPageSlugByKeyWithBasePath("services");
  return /* @__PURE__ */ (0, import_jsx_runtime131.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      gap: { xs: "44px", md: "64px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime131.jsx)(CenteredIntro, {
        title: servicesTranslation.title,
        description: servicesSection.text,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime131.jsx)(Box_default, {
        sx: { width: "100%" },
        children: /* @__PURE__ */ (0, import_jsx_runtime131.jsx)(Services, {
          maxCnt: 6,
          href: servicesHref,
        }),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime131.jsx)(ArrowButton, {
        component: "a",
        href: servicesHref,
        sx: { width: { xs: "100%", sm: "auto" } },
        children: servicesTranslation.callToAction,
      }),
    ],
  });
}

// src/page-content/components/section/subscribe/SubscribeSection.tsx
var import_react31 = __toESM(require_react(), 1);
init_runtime2();
init_static2();
var import_jsx_runtime132 = __toESM(require_jsx_runtime(), 1);
var isValidEmail = (value) => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value);
function SubscribeSection() {
  const bannerImage = useBannerImage2();
  const { lang } = useLanguage();
  const subscriptionsTranslation = getSubscriptionsTranslation2(lang);
  const formTranslations = getFormTranslation(lang);
  const [email, setEmail] = (0, import_react31.useState)("");
  const [emailError, setEmailError] = (0, import_react31.useState)(null);
  const [submitStatus, setSubmitStatus] = (0, import_react31.useState)(null);
  const [isSubmitting, setIsSubmitting] = (0, import_react31.useState)(false);
  const handleSubmit = async (event) => {
    event.preventDefault();
    const normalizedEmail = email.trim().toLowerCase();
    if (!isValidEmail(normalizedEmail) || !normalizedEmail) {
      setEmailError(formTranslations.email.errorMessage);
      return;
    }
    setEmailError(null);
    setSubmitStatus(null);
    setIsSubmitting(true);
    try {
      await callPublicApi("newsletter", {
        body: {
          email: normalizedEmail,
          locale:
            lang?.toLocaleLowerCase() ?? primaryLanguage.toLocaleLowerCase(),
        },
      });
      setEmail("");
      setSubmitStatus("success");
    } catch {
      setSubmitStatus("error");
    } finally {
      setIsSubmitting(false);
    }
  };
  (0, import_react31.useEffect)(() => {
    if (!submitStatus) {
      return;
    }
    const timeout2 = window.setTimeout(() => {
      setSubmitStatus(null);
    }, 5e3);
    return () => window.clearTimeout(timeout2);
  }, [submitStatus]);
  const message =
    emailError ??
    (submitStatus === "success"
      ? subscriptionsTranslation.successMessage
      : submitStatus === "error"
        ? subscriptionsTranslation.errorMessage
        : "");
  return /* @__PURE__ */ (0, import_jsx_runtime132.jsxs)(Box_default, {
    component: "section",
    sx: (theme2) => ({
      position: "relative",
      minHeight: { xs: 437, sm: 373, md: 520 },
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      overflow: "hidden",
      backgroundColor: theme2.palette.surfaces.placeholder,
      backgroundImage: `url("${bannerImage}")`,
      backgroundSize: "cover",
      backgroundPosition: "center",
      color: theme2.palette.surfaces.onImage,
    }),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime132.jsx)(Box_default, {
        "aria-hidden": true,
        sx: (theme2) => ({
          position: "absolute",
          inset: 0,
          backgroundColor: theme2.palette.surfaces.scrim,
        }),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime132.jsxs)(Box_default, {
        component: "form",
        onSubmit: handleSubmit,
        noValidate: true,
        sx: {
          position: "relative",
          width: "100%",
          maxWidth: { xs: "100%", sm: 692, md: 720 },
          px: { xs: "36px", sm: "36px", md: 0 },
          py: { xs: "48px", md: 0 },
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          textAlign: "center",
          gap: "26px",
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime132.jsx)(Typography_default, {
            variant: "h2",
            component: "h2",
            sx: { color: "inherit" },
            children: subscriptionsTranslation.title,
          }),
          /* @__PURE__ */ (0, import_jsx_runtime132.jsxs)(Box_default, {
            sx: {
              width: "100%",
              display: "flex",
              flexDirection: "column",
              gap: "10px",
            },
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime132.jsxs)(Box_default, {
                sx: (theme2) => ({
                  width: "100%",
                  display: "flex",
                  /*
                   * The direction belongs in the breakpoint block below, not in a responsive
                   * object: both write the same `min-width:600px` media query and the later one
                   * wins the whole block, so a `{ sm: "row" }` here was dropped and the button
                   * fell out of the pill.
                   */
                  flexDirection: "column",
                  alignItems: "center",
                  gap: "12px",
                  [theme2.breakpoints.up("sm")]: {
                    flexDirection: "row",
                    height: 64,
                    pl: "26px",
                    pr: "6px",
                    py: "6px",
                    borderRadius: "999px",
                    backgroundColor: theme2.palette.background.paper,
                  },
                }),
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime132.jsx)(Box_default, {
                    sx: (theme2) => ({
                      width: "100%",
                      display: "flex",
                      alignItems: "center",
                      px: "22px",
                      py: "18px",
                      borderRadius: "999px",
                      backgroundColor: theme2.palette.background.paper,
                      boxShadow: `inset 0 0 0 1px ${theme2.palette.surfaces.border}`,
                      [theme2.breakpoints.up("sm")]: {
                        flex: 1,
                        // Back to auto: the phone's full width would keep the whole row to itself
                        // and push the button out of the pill.
                        width: "auto",
                        minWidth: 0,
                        px: 0,
                        py: 0,
                        borderRadius: 0,
                        backgroundColor: "transparent",
                        boxShadow: "none",
                      },
                    }),
                    children: /* @__PURE__ */ (0, import_jsx_runtime132.jsx)(
                      InputBase_default,
                      {
                        fullWidth: true,
                        type: "email",
                        value: email,
                        placeholder: formTranslations.email.placeholder,
                        disabled: isSubmitting,
                        inputProps: {
                          "aria-label": formTranslations.email.placeholder,
                        },
                        onChange: (event) => {
                          setEmail(event.target.value);
                          setEmailError(null);
                          setSubmitStatus(null);
                        },
                        sx: (theme2) => ({
                          ...theme2.typography.body1,
                          color: theme2.palette.text.primary,
                          "& input::placeholder": {
                            color: theme2.palette.text.secondary,
                            opacity: 1,
                          },
                        }),
                      },
                    ),
                  }),
                  /* @__PURE__ */ (0, import_jsx_runtime132.jsx)(ArrowButton, {
                    type: "submit",
                    disabled: isSubmitting,
                    sx: {
                      flexShrink: 0,
                      borderRadius: "999px",
                      width: { xs: "100%", sm: "auto" },
                    },
                    children: subscriptionsTranslation.callToAction,
                  }),
                ],
              }),
              message &&
                /* @__PURE__ */ (0, import_jsx_runtime132.jsx)(
                  Typography_default,
                  {
                    role: "status",
                    "aria-live": "polite",
                    variant: "caption",
                    sx: { color: "inherit" },
                    children: message,
                  },
                ),
            ],
          }),
          /* @__PURE__ */ (0, import_jsx_runtime132.jsx)(FormDisclaimer, {
            isHighContrast: true,
          }),
        ],
      }),
    ],
  });
}

// src/page-content/components/section-renderer/SectionRenderer.tsx
var import_react32 = __toESM(require_react(), 1);

// src/components/section/Section.tsx
var import_jsx_runtime133 = __toESM(require_jsx_runtime(), 1);
function Section({
  id,
  className,
  color: color2,
  headerColor,
  useHeaderImage = false,
  headerHeight = "100%",
  paddingY,
  children,
}) {
  const bannerImage = useBannerImage2();
  const resolvedHeaderImage = useHeaderImage ? bannerImage : void 0;
  const shouldRenderHeaderBackground =
    (headerColor || resolvedHeaderImage) && headerHeight;
  return /* @__PURE__ */ (0, import_jsx_runtime133.jsxs)(Box_default, {
    id,
    component: "section",
    className,
    sx: (theme2) => ({
      // The bar is fixed and taller than it looks; a smaller margin hides the section top.
      scrollMarginTop: {
        xs: ANCHOR_OFFSET.xs,
        sm: ANCHOR_OFFSET.sm,
        md: ANCHOR_OFFSET.md,
      },
      // Lumiera's section rhythm: 72 / 96 / 120 above and below.
      py: paddingY ?? { xs: "72px", sm: "96px", md: "120px" },
      /*
       * The section that opens a subpage is the band's own sibling in the document, and
       * the band already closes with a 24–40 rounded lip. Once the heading moved into the
       * band, the full rhythm left every subpage starting on a wide empty strip, so the
       * first section after the band keeps about 60 % of its top padding.
       *
       * One selector rather than an override in each of the eighteen page wrappers: the
       * rule then cannot be forgotten on a page added later.
       *
       * Not cut further than this: at 40 on a phone the catalogue page's first hairline
       * sat ~50 under the lip with no heading to anchor it and read as part of the band,
       * and the pages that open on a single line of text looked unmoored the same way.
       */
      "#section-header + &": {
        paddingTop: { xs: "56px", sm: "72px", md: "88px" },
      },
      backgroundColor: resolvedHeaderImage
        ? "transparent"
        : (color2 ?? "transparent"),
      position: "relative",
      color: shouldRenderHeaderBackground
        ? theme2.palette.common.white
        : void 0,
    }),
    children: [
      shouldRenderHeaderBackground &&
        /* @__PURE__ */ (0, import_jsx_runtime133.jsx)(Box_default, {
          "aria-hidden": true,
          sx: (theme2) => ({
            position: "absolute",
            inset: 0,
            height: headerHeight ?? 0,
            overflow: "hidden",
            zIndex: -1,
            backgroundColor: resolvedHeaderImage ? void 0 : headerColor,
            ...(resolvedHeaderImage && {
              backgroundImage: `url('${resolvedHeaderImage}')`,
              backgroundSize: "cover",
              backgroundPosition: "center",
              backgroundRepeat: "no-repeat",
              // Design system: photographs sit under a flat black scrim at 60 %,
              // with white copy over them. No gradient, no filters.
              "&::after": {
                content: '""',
                position: "absolute",
                inset: 0,
                backgroundColor: theme2.palette.surfaces.scrim,
              },
            }),
          }),
        }),
      /* @__PURE__ */ (0, import_jsx_runtime133.jsx)(Container_default, {
        maxWidth: "lg",
        children,
      }),
    ],
  });
}

// src/page-content/components/section-renderer/SectionRenderer.tsx
var import_jsx_runtime134 = __toESM(require_jsx_runtime(), 1);
function renderSectionsWithDividers(sections) {
  const visibleSections = sections.filter((s) => s.enabled !== false);
  return visibleSections.map((section) =>
    /* @__PURE__ */ (0, import_jsx_runtime134.jsx)(
      import_react32.default.Fragment,
      {
        children:
          section.kind === "content"
            ? /* @__PURE__ */ (0, import_jsx_runtime134.jsx)(Section, {
                id: section.sectionId,
                useHeaderImage: section.useHeaderImage,
                headerHeight: section.headerHeight,
                color: section.color,
                children: section.render,
              })
            : section.render,
      },
      section.key,
    ),
  );
}

// src/page-content/pages/home/Home.tsx
var import_jsx_runtime135 = __toESM(require_jsx_runtime(), 1);
function HomePage() {
  const sections = [
    {
      key: "about",
      kind: "content",
      sectionId: getPageSlugByKey("about"),
      render: /* @__PURE__ */ (0, import_jsx_runtime135.jsx)(
        AboutPreviewSection,
        {},
      ),
    },
    {
      key: "services",
      kind: "content",
      enabled: isSectionEnabled("services"),
      sectionId: getPageSlugByKey("services"),
      // No banner behind this section: the Figma frame is a plain background with the
      // decorative shapes only. ui-001 ran the banner image across the top 460px, which
      // cut a hard horizontal edge through the middle of the service cards.
      render: /* @__PURE__ */ (0, import_jsx_runtime135.jsx)(
        ServicesPreviewSection,
        {},
      ),
    },
    {
      key: "portfolio",
      kind: "content",
      enabled: isSectionEnabled("portfolio"),
      sectionId: getPageSlugByKey("portfolio"),
      render: /* @__PURE__ */ (0, import_jsx_runtime135.jsx)(
        PortfolioPreviewSection,
        {},
      ),
    },
    {
      key: "cta",
      kind: "cta",
      render: /* @__PURE__ */ (0, import_jsx_runtime135.jsx)(CallToAction, {
        href: `#${getPageSlugByKey("contact")}`,
      }),
    },
    {
      key: "gallery",
      kind: "content",
      enabled: isSectionEnabled("gallery"),
      sectionId: getPageSlugByKey("gallery"),
      render: /* @__PURE__ */ (0, import_jsx_runtime135.jsx)(
        GalleryPreviewSection,
        {},
      ),
    },
    {
      key: "reviews",
      kind: "content",
      enabled: isSectionEnabled("reviews"),
      sectionId: "reviews",
      // Lumiera sets the reviews on the mint wash, edge to edge.
      color: "surfaces.mint",
      render: /* @__PURE__ */ (0, import_jsx_runtime135.jsx)(ReviewSection, {}),
    },
    {
      key: "blog",
      kind: "content",
      enabled: !isSectionEnabled("reviews") && isSectionEnabled("blog"),
      sectionId: getPageSlugByKey("blog"),
      render: /* @__PURE__ */ (0, import_jsx_runtime135.jsx)(
        BlogPreviewSection,
        {},
      ),
    },
    {
      key: "subscribe",
      kind: "cta",
      enabled: isSectionEnabled("subscribe"),
      render: /* @__PURE__ */ (0, import_jsx_runtime135.jsx)(
        SubscribeSection,
        {},
      ),
    },
    {
      key: "contact",
      kind: "content",
      sectionId: getPageSlugByKey("contact"),
      render: /* @__PURE__ */ (0, import_jsx_runtime135.jsx)(
        ContactPreviewSection,
        {},
      ),
    },
  ];
  return /* @__PURE__ */ (0, import_jsx_runtime135.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime135.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime135.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime135.jsx)(HomeSection, {}),
      renderSectionsWithDividers(sections),
    ],
  });
}

// src/page-content/pages/about/About.tsx
init_runtime2();

// src/page-content/components/section/about/AboutSection.tsx
init_runtime2();
var import_jsx_runtime136 = __toESM(require_jsx_runtime(), 1);
function AboutSection() {
  const { lang } = useLanguage();
  const aboutTranslation = getAboutTranslation2(lang);
  const aboutSection = getAboutSection(lang);
  const aboutItems = getAboutItems(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime136.jsxs)(
    import_jsx_runtime136.Fragment,
    {
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime136.jsx)(AboutBlock, {
          title: aboutTranslation.subtitle,
          description: aboutSection.text,
          items: aboutItems,
        }),
        /* @__PURE__ */ (0, import_jsx_runtime136.jsx)(
          EstablishedAndClients,
          {},
        ),
      ],
    },
  );
}

// src/page-content/components/section/header/HeaderSection.tsx
var import_jsx_runtime137 = __toESM(require_jsx_runtime(), 1);
var LIP = { xs: 24, sm: 32, md: 40 };
function HeaderSection({ title, id }) {
  const bannerImage = useBannerImage2();
  return /* @__PURE__ */ (0, import_jsx_runtime137.jsx)(Box_default, {
    component: "section",
    id: "section-header",
    sx: (theme2) => ({
      position: "relative",
      overflow: "hidden",
      height: { xs: 300, md: 440 },
      pt: {
        xs: `${HEADER_HEIGHT.xs}px`,
        sm: `${HEADER_HEIGHT.sm}px`,
        md: `${HEADER_HEIGHT.md}px`,
      },
      pb: { xs: `${LIP.xs}px`, sm: `${LIP.sm}px`, md: `${LIP.md}px` },
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      backgroundColor: theme2.palette.surfaces.placeholder,
      backgroundImage: `url("${bannerImage}")`,
      backgroundSize: "cover",
      backgroundPosition: "center",
      color: theme2.palette.surfaces.onImage,
      "&::before": {
        content: '""',
        position: "absolute",
        inset: 0,
        backgroundColor: theme2.palette.surfaces.scrim,
      },
      "&::after": {
        content: '""',
        position: "absolute",
        left: 0,
        right: 0,
        bottom: 0,
        height: { xs: LIP.xs, sm: LIP.sm, md: LIP.md },
        backgroundColor: theme2.palette.background.default,
        borderTopLeftRadius: {
          xs: `${LIP.xs}px`,
          sm: `${LIP.sm}px`,
          md: `${LIP.md}px`,
        },
        borderTopRightRadius: {
          xs: `${LIP.xs}px`,
          sm: `${LIP.sm}px`,
          md: `${LIP.md}px`,
        },
      },
    }),
    children: /* @__PURE__ */ (0, import_jsx_runtime137.jsx)(Box_default, {
      id,
      sx: {
        position: "relative",
        zIndex: 1,
        width: "100%",
        maxWidth: { md: 1128 },
        mx: "auto",
        px: { xs: "36px", sm: "64px" },
        textAlign: "center",
      },
      children: /* @__PURE__ */ (0, import_jsx_runtime137.jsx)(
        Typography_default,
        {
          variant: "h2",
          component: "h1",
          sx: { color: "inherit" },
          children: title,
        },
      ),
    }),
  });
}

// src/page-content/components/section/team/TeamSection.tsx
init_runtime2();

// node_modules/.pnpm/@mui+icons-material@7.3.11__77544ce9cc2d00337704b0dadf8f242a/node_modules/@mui/icons-material/esm/AlternateEmail.js
var import_jsx_runtime138 = __toESM(require_jsx_runtime(), 1);
var AlternateEmail_default = createSvgIcon(
  /* @__PURE__ */ (0, import_jsx_runtime138.jsx)("path", {
    d: "M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10h5v-2h-5c-4.34 0-8-3.66-8-8s3.66-8 8-8 8 3.66 8 8v1.43c0 .79-.71 1.57-1.5 1.57s-1.5-.78-1.5-1.57V12c0-2.76-2.24-5-5-5s-5 2.24-5 5 2.24 5 5 5c1.38 0 2.64-.56 3.54-1.47.65.89 1.77 1.47 2.96 1.47 1.97 0 3.5-1.6 3.5-3.57V12c0-5.52-4.48-10-10-10m0 13c-1.66 0-3-1.34-3-3s1.34-3 3-3 3 1.34 3 3-1.34 3-3 3",
  }),
  "AlternateEmail",
);

// node_modules/.pnpm/@mui+icons-material@7.3.11__77544ce9cc2d00337704b0dadf8f242a/node_modules/@mui/icons-material/esm/PhoneOutlined.js
var import_jsx_runtime139 = __toESM(require_jsx_runtime(), 1);
var PhoneOutlined_default = createSvgIcon(
  /* @__PURE__ */ (0, import_jsx_runtime139.jsx)("path", {
    d: "M6.54 5c.06.89.21 1.76.45 2.59l-1.2 1.2c-.41-1.2-.67-2.47-.76-3.79zm9.86 12.02c.85.24 1.72.39 2.6.45v1.49c-1.32-.09-2.59-.35-3.8-.75zM7.5 3H4c-.55 0-1 .45-1 1 0 9.39 7.61 17 17 17 .55 0 1-.45 1-1v-3.49c0-.55-.45-1-1-1-1.24 0-2.45-.2-3.57-.57-.1-.04-.21-.05-.31-.05-.26 0-.51.1-.71.29l-2.2 2.2c-2.83-1.45-5.15-3.76-6.59-6.59l2.2-2.2c.28-.28.36-.67.25-1.02C8.7 6.45 8.5 5.25 8.5 4c0-.55-.45-1-1-1",
  }),
  "PhoneOutlined",
);

// src/page-content/components/section/team/TeamCard.tsx
var import_jsx_runtime140 = __toESM(require_jsx_runtime(), 1);
var iconMap = {
  EMAIL: /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(
    AlternateEmail_default,
    {},
  ),
  PHONE: /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(
    PhoneOutlined_default,
    {},
  ),
  LINKEDIN: /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(
    LinkedIn_default,
    {},
  ),
  INSTAGRAM: /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(
    Instagram_default,
    {},
  ),
  FACEBOOK: /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(
    Facebook_default,
    {},
  ),
  TWITTER: /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(X_default, {}),
  WEBSITE: /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(Language_default, {}),
  TIKTOK: /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(TikTokIcon, {}),
};
var hrefFor = (item) => {
  if (item.type === "EMAIL") return `mailto:${item.value}`;
  if (item.type === "PHONE") return `tel:${item.value.replace(/\s+/g, "")}`;
  return item.value;
};
function TeamCard({ name, text, image, contact = [] }) {
  return /* @__PURE__ */ (0, import_jsx_runtime140.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "16px", md: "18px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime140.jsxs)(Box_default, {
        sx: (theme2) => ({
          position: "relative",
          height: { xs: 400, sm: 380, md: 340 },
          borderRadius: "12px",
          overflow: "hidden",
          backgroundColor: theme2.palette.surfaces.mint,
        }),
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(Box_default, {
            component: "img",
            src: image,
            alt: "",
            loading: "lazy",
            sx: {
              position: "absolute",
              inset: 0,
              width: "100%",
              height: "100%",
              objectFit: "cover",
            },
          }),
          contact.length > 0 &&
            /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(Box_default, {
              role: "list",
              sx: (theme2) => ({
                position: "absolute",
                right: 12,
                bottom: 12,
                display: "flex",
                flexDirection: "column",
                gap: { xs: "6px", md: "8px" },
                p: { xs: "5px", md: "6px" },
                borderRadius: "999px",
                backgroundColor: theme2.palette.background.paper,
              }),
              children: contact.map((item) =>
                /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(
                  Box_default,
                  {
                    role: "listitem",
                    component: "a",
                    href: hrefFor(item),
                    target:
                      item.type === "EMAIL" || item.type === "PHONE"
                        ? void 0
                        : "_blank",
                    rel: "noopener noreferrer",
                    "aria-label": `${item.type}: ${name}`,
                    sx: (theme2) => ({
                      width: { xs: 30, md: 32 },
                      height: { xs: 30, md: 32 },
                      borderRadius: "50%",
                      display: "grid",
                      placeItems: "center",
                      backgroundColor: theme2.palette.surfaces.mint,
                      color: theme2.palette.text.primary,
                      "& .MuiSvgIcon-root": { fontSize: 15 },
                      transition: theme2.transitions.create(
                        ["background-color", "color"],
                        {
                          duration: theme2.transitions.duration.short,
                        },
                      ),
                      "&:hover, &:focus-visible": {
                        backgroundColor: theme2.palette.primary.main,
                        color: theme2.palette.primary.contrastText,
                      },
                    }),
                    children: iconMap[item.type],
                  },
                  `${item.type}-${item.value}`,
                ),
              ),
            }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime140.jsxs)(Box_default, {
        sx: {
          display: "flex",
          flexDirection: "column",
          gap: "8px",
          textAlign: "center",
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(Typography_default, {
            variant: "h6",
            component: "h3",
            children: name,
          }),
          text &&
            /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(Typography_default, {
              variant: "body1",
              sx: { color: "text.secondary" },
              children: text,
            }),
        ],
      }),
    ],
  });
}

// src/page-content/components/section/team/TeamSection.tsx
var import_jsx_runtime141 = __toESM(require_jsx_runtime(), 1);
function TeamSection() {
  const { lang } = useLanguage();
  const teamTranslation = getTeamTranslation2(lang);
  const teamSection = getTeamSection(lang);
  if (!teamSection) {
    return null;
  }
  const teamItems = getTeamItems(lang);
  const columns = Math.min(Math.max(teamItems.length, 1), 4);
  const cards = teamItems.map((member, index) =>
    /* @__PURE__ */ (0, import_jsx_runtime141.jsx)(
      TeamCard,
      {
        name: member.name,
        text: member.text,
        image: member.image,
        contact: member.contact,
      },
      index,
    ),
  );
  return /* @__PURE__ */ (0, import_jsx_runtime141.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "44px", md: "64px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime141.jsx)(CenteredIntro, {
        title: teamTranslation.title,
        description: teamSection.text,
      }),
      cards.length > 0 &&
        /* @__PURE__ */ (0, import_jsx_runtime141.jsxs)(
          import_jsx_runtime141.Fragment,
          {
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime141.jsx)(Box_default, {
                sx: {
                  display: { xs: "none", md: "grid" },
                  gridTemplateColumns: `repeat(${columns}, minmax(0, 282px))`,
                  justifyContent: "center",
                  gap: "24px",
                },
                children: cards,
              }),
              /* @__PURE__ */ (0, import_jsx_runtime141.jsx)(Box_default, {
                sx: { display: { xs: "block", md: "none" } },
                children: /* @__PURE__ */ (0, import_jsx_runtime141.jsx)(
                  Carousel,
                  {
                    items: cards,
                    slideWidth: { xs: "100%", sm: "calc(50% - 12px)" },
                    gap: 24,
                    controlsGap: { xs: 24 },
                    ariaLabel: teamTranslation.title,
                    autoPlayMs: 6e3,
                  },
                ),
              }),
            ],
          },
        ),
    ],
  });
}

// src/page-content/components/section/why-us/WhyUsSection.tsx
var import_react33 = __toESM(require_react(), 1);
init_runtime2();

// src/page-content/components/section/why-us/StatBox.tsx
var import_jsx_runtime142 = __toESM(require_jsx_runtime(), 1);
function StatBox({ value, label }) {
  return /* @__PURE__ */ (0, import_jsx_runtime142.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      textAlign: "center",
      gap: { xs: "10px", md: "14px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime142.jsxs)(Typography_default, {
        component: "p",
        sx: (theme2) => ({
          fontFamily: theme2.typography.h1.fontFamily,
          fontWeight: 400,
          fontSize: { xs: "52px", md: "70px" },
          lineHeight: 1.1,
          color: theme2.palette.text.primary,
        }),
        children: [
          value,
          /* @__PURE__ */ (0, import_jsx_runtime142.jsx)(Box_default, {
            component: "span",
            sx: { color: "primary.main" },
            children: "+",
          }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime142.jsx)(Typography_default, {
        variant: "body1",
        sx: { color: "text.secondary", maxWidth: 340 },
        children: label,
      }),
    ],
  });
}

// src/page-content/components/section/why-us/ExperienceItems.tsx
init_runtime2();
var import_jsx_runtime143 = __toESM(require_jsx_runtime(), 1);
function CertificateTile({ item }) {
  const isLink = Boolean(item.url);
  return /* @__PURE__ */ (0, import_jsx_runtime143.jsxs)(Box_default, {
    ...(isLink
      ? {
          component: "a",
          href: item.url,
          target: "_blank",
          rel: "noopener noreferrer",
          "aria-label": item.url,
        }
      : {}),
    sx: (theme2) => ({
      position: "relative",
      display: "grid",
      placeItems: "center",
      height: { xs: 200, sm: 180, md: 200 },
      px: "20px",
      borderRadius: "16px",
      backgroundColor: theme2.palette.background.paper,
      ...(isLink && {
        "&:hover .certificate-link": {
          backgroundColor: theme2.palette.primary.main,
          color: theme2.palette.primary.contrastText,
        },
      }),
    }),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime143.jsx)(Box_default, {
        component: "img",
        src: item.image,
        alt: "",
        loading: "lazy",
        sx: { maxWidth: "75%", maxHeight: "60%", objectFit: "contain" },
      }),
      isLink &&
        /* @__PURE__ */ (0, import_jsx_runtime143.jsx)(Box_default, {
          className: "certificate-link",
          sx: (theme2) => ({
            position: "absolute",
            top: 12,
            right: 12,
            width: 36,
            height: 36,
            borderRadius: "50%",
            display: "grid",
            placeItems: "center",
            backgroundColor: theme2.palette.surfaces.mint,
            color: theme2.palette.text.primary,
            transition: theme2.transitions.create(
              ["background-color", "color"],
              {
                duration: theme2.transitions.duration.short,
              },
            ),
          }),
          children: /* @__PURE__ */ (0, import_jsx_runtime143.jsx)(
            ArrowOutwardIcon,
            {},
          ),
        }),
    ],
  });
}
function ExperienceItems() {
  const items = getExperienceItems();
  if (items.length === 0) return null;
  const tiles = items.map((item, index) =>
    /* @__PURE__ */ (0, import_jsx_runtime143.jsx)(
      CertificateTile,
      { item },
      index,
    ),
  );
  return /* @__PURE__ */ (0, import_jsx_runtime143.jsxs)(Box_default, {
    sx: (theme2) => ({
      width: "100%",
      backgroundColor: theme2.palette.surfaces.bgAlt,
      borderRadius: { xs: "24px", sm: "28px", md: "32px" },
      px: { xs: "16px", sm: "28px", md: "40px" },
      pt: { xs: "16px", sm: "28px", md: "40px" },
      pb: { xs: "20px", sm: "28px", md: "40px" },
    }),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime143.jsx)(Box_default, {
        sx: { display: { xs: "block", sm: "none" } },
        children: /* @__PURE__ */ (0, import_jsx_runtime143.jsx)(Carousel, {
          items: tiles,
          slideWidth: { xs: "100%" },
          gap: 16,
          controlsGap: { xs: 20 },
          controlsPrevTone: "white",
          ariaLabel: "Certifikati",
        }),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime143.jsx)(Box_default, {
        sx: (theme2) => ({
          display: { xs: "none", sm: "grid" },
          gridTemplateColumns: {
            sm: `repeat(${Math.min(items.length, 2)}, 1fr)`,
            md: `repeat(${Math.min(items.length, 4)}, 1fr)`,
          },
          gap: { sm: "20px", md: "24px" },
          [theme2.breakpoints.only("sm")]: {
            "& > :last-child:nth-of-type(odd)": { gridColumn: "1 / -1" },
          },
        }),
        children: tiles,
      }),
    ],
  });
}

// src/page-content/components/section/why-us/WhyUsSection.tsx
var import_jsx_runtime144 = __toESM(require_jsx_runtime(), 1);
function formatStat(value, locale) {
  const number = Number(value);
  return Number.isFinite(number)
    ? new Intl.NumberFormat(locale).format(number)
    : String(value);
}
function WhyUsSection() {
  const { lang } = useLanguage();
  const whyUsTranslation = getWhyUsTranslation2(lang);
  const experience = getExperienceSection(lang);
  const locale = lang === "SL" ? "sl-SI" : "en-GB";
  const stats = [];
  if (experience?.clientCount) {
    stats.push({
      value: formatStat(experience.clientCount, locale),
      label: whyUsTranslation.clientCount,
    });
  }
  if (experience?.projectCount) {
    stats.push({
      value: formatStat(experience.projectCount, locale),
      label: whyUsTranslation.projectCount,
    });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime144.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      gap: { xs: "44px", sm: "56px", md: "72px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime144.jsx)(CenteredIntro, {
        title: whyUsTranslation.title,
        description: experience?.text,
      }),
      stats.length > 0 &&
        /* @__PURE__ */ (0, import_jsx_runtime144.jsx)(Box_default, {
          sx: {
            display: "flex",
            flexDirection: { xs: "column", sm: "row" },
            alignItems: "center",
            gap: { xs: "28px", sm: 0 },
            width: { xs: "100%", sm: "auto" },
          },
          children: stats.map((stat, index) =>
            /* @__PURE__ */ (0, import_jsx_runtime144.jsxs)(
              import_react33.Fragment,
              {
                children: [
                  index > 0 &&
                    /* @__PURE__ */ (0, import_jsx_runtime144.jsx)(
                      Box_default,
                      {
                        "aria-hidden": true,
                        sx: (theme2) => ({
                          flexShrink: 0,
                          backgroundColor: theme2.palette.surfaces.border,
                          width: { xs: 120, sm: "1px" },
                          height: { xs: "1px", sm: 100, md: 110 },
                        }),
                      },
                    ),
                  /* @__PURE__ */ (0, import_jsx_runtime144.jsx)(Box_default, {
                    sx: { width: { xs: "100%", sm: 300, md: 420 } },
                    children: /* @__PURE__ */ (0, import_jsx_runtime144.jsx)(
                      StatBox,
                      { value: stat.value, label: stat.label },
                    ),
                  }),
                ],
              },
              stat.label,
            ),
          ),
        }),
      /* @__PURE__ */ (0, import_jsx_runtime144.jsx)(ExperienceItems, {}),
    ],
  });
}

// src/page-content/components/section/clients/ClientLogoSlider.tsx
var import_jsx_runtime145 = __toESM(require_jsx_runtime(), 1);
function linkLabel2(href) {
  try {
    return new URL(href).hostname.replace(/^www\./, "");
  } catch {
    return href;
  }
}
function ClientLogoSlider({ clients, speed = 30 }) {
  if (clients.length === 0) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime145.jsx)(marquee_default, {
    speed,
    gradient: false,
    autoFill: true,
    pauseOnHover: true,
    children: clients.map((client, index) =>
      /* @__PURE__ */ (0, import_jsx_runtime145.jsxs)(
        Box_default,
        {
          sx: {
            width: 216,
            /*
             * Without this the entry shrinks to fit the flex row Marquee builds. autoFill
             * sizes the copies from the measured width of one group, so a shrinking group
             * asks for more copies, which shrink further: the strip grew past 32000 <img>
             * elements at 1440 and froze the page.
             */
            flexShrink: 0,
            mr: "24px",
            display: "flex",
            flexDirection: "column",
            alignItems: "center",
            gap: "14px",
          },
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime145.jsx)(Box_default, {
              ...(client.href
                ? {
                    component: "a",
                    href: client.href,
                    target: "_blank",
                    rel: "noopener noreferrer",
                    tabIndex: -1,
                    "aria-hidden": true,
                  }
                : {}),
              sx: {
                width: "100%",
                height: 110,
                borderRadius: "12px",
                display: "grid",
                placeItems: "center",
                px: "16px",
                py: "20px",
              },
              children: /* @__PURE__ */ (0, import_jsx_runtime145.jsx)(
                Box_default,
                {
                  component: "img",
                  src: client.src,
                  alt: "",
                  loading: "lazy",
                  sx: {
                    maxWidth: "100%",
                    maxHeight: "100%",
                    objectFit: "contain",
                  },
                },
              ),
            }),
            /* @__PURE__ */ (0, import_jsx_runtime145.jsx)(Box_default, {
              sx: { minHeight: 24 },
              children:
                client.href &&
                /* @__PURE__ */ (0, import_jsx_runtime145.jsx)(
                  Typography_default,
                  {
                    component: "a",
                    href: client.href,
                    target: "_blank",
                    rel: "noopener noreferrer",
                    variant: "caption",
                    sx: (theme2) => ({
                      color: theme2.palette.text.secondary,
                      textDecoration: "underline",
                      textUnderlineOffset: "3px",
                      transition: theme2.transitions.create(["color"], {
                        duration: theme2.transitions.duration.short,
                      }),
                      "&:hover": { color: theme2.palette.primary.main },
                    }),
                    children: linkLabel2(client.href),
                  },
                ),
            }),
          ],
        },
        `${client.src}-${index}`,
      ),
    ),
  });
}

// src/page-content/components/section/clients/ClientsSection.tsx
init_runtime2();
var import_jsx_runtime146 = __toESM(require_jsx_runtime(), 1);
function ClientsSection() {
  const clients = getClients();
  return /* @__PURE__ */ (0, import_jsx_runtime146.jsx)(Box_default, {
    sx: {
      width: "100vw",
      position: "relative",
      left: "50%",
      right: "50%",
      marginLeft: "-50vw",
      marginRight: "-50vw",
      display: "flex",
    },
    children: /* @__PURE__ */ (0, import_jsx_runtime146.jsx)(ClientLogoSlider, {
      clients,
      speed: 30,
    }),
  });
}

// src/page-content/pages/about/About.tsx
init_runtime2();
init_static2();
var import_jsx_runtime147 = __toESM(require_jsx_runtime(), 1);
function AboutPage() {
  const { lang } = useLanguage();
  const aboutTranslation = getAboutTranslation2(lang);
  const aboutSection = getAboutSection(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime147.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime147.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime147.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime147.jsx)(HeaderSection, {
        title: aboutSection.sectionName || aboutTranslation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime147.jsx)(Section, {
        id: "about",
        children: /* @__PURE__ */ (0, import_jsx_runtime147.jsx)(
          AboutSection,
          {},
        ),
      }),
      isSectionEnabled("experience") &&
        /* @__PURE__ */ (0, import_jsx_runtime147.jsx)(Section, {
          id: "why",
          children: /* @__PURE__ */ (0, import_jsx_runtime147.jsx)(
            WhyUsSection,
            {},
          ),
        }),
      isSectionEnabled("clients") &&
        /* @__PURE__ */ (0, import_jsx_runtime147.jsx)(Section, {
          id: "clients",
          paddingY: { xs: "56px", sm: "72px", md: "90px" },
          children: /* @__PURE__ */ (0, import_jsx_runtime147.jsx)(
            ClientsSection,
            {},
          ),
        }),
      isSectionEnabled("team") &&
        /* @__PURE__ */ (0, import_jsx_runtime147.jsx)(Section, {
          id: "team",
          children: /* @__PURE__ */ (0, import_jsx_runtime147.jsx)(
            TeamSection,
            {},
          ),
        }),
    ],
  });
}

// src/page-content/pages/contact/Contact.tsx
init_runtime2();

// src/page-content/components/section/contact/ContactSection.tsx
var import_react34 = __toESM(require_react(), 1);
init_runtime2();
var import_jsx_runtime148 = __toESM(require_jsx_runtime(), 1);
function ContactSection() {
  const searchParams2 = useSearchParams();
  const subject = searchParams2.get("subject") ?? void 0;
  const map = getMap();
  const formRef = (0, import_react34.useRef)(null);
  (0, import_react34.useEffect)(() => {
    if (!subject) return;
    const form = formRef.current;
    if (!form) return;
    const stillness = window.matchMedia(
      "(prefers-reduced-motion: reduce)",
    ).matches;
    const frame = requestAnimationFrame(() => {
      form.scrollIntoView({
        behavior: stillness ? "auto" : "smooth",
        block: "start",
      });
    });
    return () => cancelAnimationFrame(frame);
  }, [subject]);
  return /* @__PURE__ */ (0, import_jsx_runtime148.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "72px", sm: "96px", md: "120px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime148.jsxs)(Box_default, {
        sx: {
          display: "flex",
          flexDirection: { xs: "column", md: "row" },
          alignItems: "flex-start",
          gap: { xs: "44px", md: "80px" },
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime148.jsx)(Box_default, {
            sx: {
              flex: { md: "0 0 520px" },
              width: "100%",
              display: "flex",
              flexDirection: "column",
              gap: "40px",
            },
            children: /* @__PURE__ */ (0, import_jsx_runtime148.jsx)(
              ContactInfo,
              {},
            ),
          }),
          /* @__PURE__ */ (0, import_jsx_runtime148.jsx)(Box_default, {
            ref: formRef,
            sx: {
              flex: { md: 1 },
              width: "100%",
              minWidth: 0,
              /*
               * The header is fixed and taller than it looks, so a plain scroll would tuck the
               * top of the form underneath it. Same offset the section anchors use.
               */
              scrollMarginTop: {
                xs: ANCHOR_OFFSET.xs,
                sm: ANCHOR_OFFSET.sm,
                md: ANCHOR_OFFSET.md,
              },
            },
            children: /* @__PURE__ */ (0, import_jsx_runtime148.jsx)(
              ContactForm,
              { subject },
            ),
          }),
        ],
      }),
      map.enabled &&
        /* @__PURE__ */ (0, import_jsx_runtime148.jsx)(CustomMap, {}),
      /* @__PURE__ */ (0, import_jsx_runtime148.jsx)(EstablishedAndClients, {}),
    ],
  });
}

// src/page-content/pages/contact/Contact.tsx
var import_jsx_runtime149 = __toESM(require_jsx_runtime(), 1);
function ContactPage() {
  const { lang } = useLanguage();
  const contactTranslation = getContactTranslation2(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime149.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime149.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime149.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime149.jsx)(HeaderSection, {
        title: contactTranslation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime149.jsx)(Section, {
        id: "contact",
        children: /* @__PURE__ */ (0, import_jsx_runtime149.jsx)(
          ContactSection,
          {},
        ),
      }),
    ],
  });
}

// src/page-content/pages/services/Services.tsx
init_runtime2();

// src/page-content/components/section/services/ServicesSection.tsx
init_runtime2();
var import_jsx_runtime150 = __toESM(require_jsx_runtime(), 1);
function ServicesSection() {
  const { lang } = useLanguage();
  const servicesSection = getServicesSection(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime150.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "44px", md: "64px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime150.jsx)(CenteredIntro, {
        align: "left",
        description: servicesSection?.text,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime150.jsx)(Services, {}),
    ],
  });
}

// src/page-content/pages/services/Services.tsx
var import_jsx_runtime151 = __toESM(require_jsx_runtime(), 1);
function ServicesPage() {
  const { lang } = useLanguage();
  const servicesTranslation = getServicesTranslation2(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime151.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime151.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime151.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime151.jsx)(HeaderSection, {
        title: servicesTranslation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime151.jsx)(Section, {
        id: "services",
        children: /* @__PURE__ */ (0, import_jsx_runtime151.jsx)(
          ServicesSection,
          {},
        ),
      }),
    ],
  });
}

// src/page-content/pages/portfolio/Portfolio.tsx
init_runtime2();
init_runtime2();

// src/page-content/components/section/portfolio/PortfolioSection.tsx
init_runtime2();
var import_jsx_runtime152 = __toESM(require_jsx_runtime(), 1);
function PortfolioSection() {
  const { lang } = useLanguage();
  const portfolioSection = getPortfolioSection(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime152.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "36px", md: "44px" },
    },
    children: [
      portfolioSection &&
        /* @__PURE__ */ (0, import_jsx_runtime152.jsx)(CenteredIntro, {
          align: "left",
          description: portfolioSection.text,
        }),
      /* @__PURE__ */ (0, import_jsx_runtime152.jsx)(Portfolio, {}),
    ],
  });
}

// src/page-content/pages/portfolio/Portfolio.tsx
var import_jsx_runtime153 = __toESM(require_jsx_runtime(), 1);
function PortfolioPage() {
  const { lang } = useLanguage();
  const portfolioTranslation = getPortfolioTranslation2(lang);
  const portfolioSection = getPortfolioSection(lang);
  if (!portfolioSection) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime153.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime153.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime153.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime153.jsx)(HeaderSection, {
        title: portfolioSection.name || portfolioTranslation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime153.jsx)(Section, {
        id: "portfolio",
        children: /* @__PURE__ */ (0, import_jsx_runtime153.jsx)(
          PortfolioSection,
          {},
        ),
      }),
    ],
  });
}

// src/page-content/pages/portfolio/PortfolioItem.tsx
init_runtime2();
init_runtime2();

// src/components/button/BackButton.tsx
var import_jsx_runtime154 = __toESM(require_jsx_runtime(), 1);
function BackButton({ label, onClick, sx }) {
  return /* @__PURE__ */ (0, import_jsx_runtime154.jsxs)(Box_default, {
    component: "button",
    type: "button",
    onClick,
    sx: [
      (theme2) => ({
        ...theme2.typography.navLink,
        alignSelf: "flex-start",
        display: "inline-flex",
        alignItems: "center",
        gap: "10px",
        px: { xs: "20px", md: "22px" },
        py: { xs: "11px", md: "12px" },
        border: 0,
        borderRadius: "999px",
        background: "none",
        cursor: "pointer",
        color: theme2.palette.text.primary,
        boxShadow: `inset 0 0 0 1px ${theme2.palette.surfaces.border}`,
        transition: theme2.transitions.create(["box-shadow"], {
          duration: theme2.transitions.duration.short,
        }),
        "&:hover": {
          boxShadow: `inset 0 0 0 1px ${theme2.palette.text.primary}`,
        },
      }),
      ...(Array.isArray(sx) ? sx : [sx]),
    ],
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime154.jsx)(LongArrowIcon, {
        direction: "left",
      }),
      label,
    ],
  });
}

// src/page-content/components/section/portfolio/PortfolioItemSection.tsx
init_runtime2();
init_static2();

// src/page-content/components/section/common/MediaGallery.tsx
var import_react35 = __toESM(require_react(), 1);
var import_jsx_runtime155 = __toESM(require_jsx_runtime(), 1);
function useImageSizes(sources) {
  const [sizes, setSizes] = (0, import_react35.useState)({});
  (0, import_react35.useEffect)(() => {
    let cancelled = false;
    sources.forEach((src) => {
      const image = new Image();
      image.onload = () => {
        if (cancelled) return;
        setSizes((previous) => ({
          ...previous,
          [src]: { width: image.naturalWidth, height: image.naturalHeight },
        }));
      };
      image.src = src;
    });
    return () => {
      cancelled = true;
    };
  }, [sources]);
  return sizes;
}
function MediaGallery({ images, title }) {
  const [activeImage, setActiveImage] = (0, import_react35.useState)(0);
  const lightboxRef = (0, import_react35.useRef)(null);
  const sizes = useImageSizes(images);
  (0, import_react35.useEffect)(() => () => lightboxRef.current?.destroy(), []);
  if (images.length === 0) return null;
  const openLightbox = async (index) => {
    const { default: Lightbox } = await Promise.resolve().then(
      () => (init_photoswipe_lightbox_esm(), photoswipe_lightbox_esm_exports),
    );
    lightboxRef.current?.destroy();
    const lightbox = new Lightbox({
      dataSource: images.map((src) => ({
        src,
        width: sizes[src]?.width ?? 1600,
        height: sizes[src]?.height ?? 1200,
      })),
      pswpModule: () =>
        Promise.resolve().then(
          () => (init_photoswipe_esm(), photoswipe_esm_exports),
        ),
      loop: true,
      wheelToZoom: true,
    });
    lightbox.on("change", () => {
      if (lightbox.pswp) setActiveImage(lightbox.pswp.currIndex);
    });
    lightbox.init();
    lightbox.loadAndOpen(index);
    lightboxRef.current = lightbox;
  };
  return /* @__PURE__ */ (0, import_jsx_runtime155.jsxs)(Box_default, {
    sx: {
      width: "100%",
      display: "flex",
      flexDirection: "column",
      gap: { xs: "10px", md: "12px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime155.jsx)(Box_default, {
        component: "button",
        type: "button",
        onClick: () => openLightbox(activeImage),
        "aria-label": `${title} \u2014 ${activeImage + 1} / ${images.length}`,
        sx: (theme2) => ({
          display: "block",
          width: "100%",
          height: { xs: 300, sm: 480, md: 600 },
          p: 0,
          border: 0,
          borderRadius: "12px",
          overflow: "hidden",
          cursor: "zoom-in",
          backgroundColor: theme2.palette.surfaces.placeholder,
        }),
        children: /* @__PURE__ */ (0, import_jsx_runtime155.jsx)(Box_default, {
          component: "img",
          src: images[activeImage],
          alt: "",
          sx: { width: "100%", height: "100%", objectFit: "cover" },
        }),
      }),
      images.length > 1 &&
        /* @__PURE__ */ (0, import_jsx_runtime155.jsx)(Box_default, {
          sx: {
            display: "grid",
            gridTemplateColumns: "repeat(4, minmax(0, 1fr))",
            gap: { xs: "10px", md: "12px" },
          },
          children: images.map((image, index) =>
            /* @__PURE__ */ (0, import_jsx_runtime155.jsx)(
              Box_default,
              {
                component: "button",
                type: "button",
                "aria-label": `${index + 1} / ${images.length}`,
                "aria-pressed": index === activeImage,
                onClick: () => setActiveImage(index),
                sx: (theme2) => ({
                  position: "relative",
                  height: { xs: 70, sm: 110, md: 140 },
                  p: 0,
                  border: 0,
                  borderRadius: "12px",
                  overflow: "hidden",
                  cursor: "pointer",
                  backgroundColor: theme2.palette.surfaces.placeholder,
                  "&::after": {
                    content: '""',
                    position: "absolute",
                    inset: 0,
                    borderRadius: "12px",
                    boxShadow:
                      index === activeImage
                        ? `inset 0 0 0 2px ${theme2.palette.text.primary}`
                        : "none",
                  },
                }),
                children: /* @__PURE__ */ (0, import_jsx_runtime155.jsx)(
                  Box_default,
                  {
                    component: "img",
                    src: image,
                    alt: "",
                    loading: "lazy",
                    sx: { width: "100%", height: "100%", objectFit: "cover" },
                  },
                ),
              },
              `${image}-${index}`,
            ),
          ),
        }),
    ],
  });
}

// src/page-content/components/section/common/ShareActions.tsx
var import_react36 = __toESM(require_react(), 1);

// node_modules/.pnpm/@mui+icons-material@7.3.11__77544ce9cc2d00337704b0dadf8f242a/node_modules/@mui/icons-material/esm/Link.js
var import_jsx_runtime156 = __toESM(require_jsx_runtime(), 1);
var Link_default2 = createSvgIcon(
  /* @__PURE__ */ (0, import_jsx_runtime156.jsx)("path", {
    d: "M3.9 12c0-1.71 1.39-3.1 3.1-3.1h4V7H7c-2.76 0-5 2.24-5 5s2.24 5 5 5h4v-1.9H7c-1.71 0-3.1-1.39-3.1-3.1M8 13h8v-2H8zm9-6h-4v1.9h4c1.71 0 3.1 1.39 3.1 3.1s-1.39 3.1-3.1 3.1h-4V17h4c2.76 0 5-2.24 5-5s-2.24-5-5-5",
  }),
  "Link",
);

// src/page-content/components/section/common/ShareActions.tsx
init_runtime2();
var import_jsx_runtime157 = __toESM(require_jsx_runtime(), 1);
var circleSx = (theme2) => ({
  width: 32,
  height: 32,
  flexShrink: 0,
  display: "grid",
  placeItems: "center",
  p: 0,
  border: 0,
  borderRadius: "50%",
  cursor: "pointer",
  background: "none",
  color: theme2.palette.text.primary,
  boxShadow: `inset 0 0 0 1px ${theme2.palette.surfaces.border}`,
  "& .MuiSvgIcon-root": { fontSize: 15 },
  transition: theme2.transitions.create(
    ["background-color", "color", "box-shadow"],
    {
      duration: theme2.transitions.duration.short,
    },
  ),
  "&:hover, &:focus-visible": {
    backgroundColor: theme2.palette.primary.main,
    color: theme2.palette.primary.contrastText,
    boxShadow: "none",
  },
});
function ShareActions({
  url = typeof window !== "undefined" ? window.location.href : "",
  title = "",
}) {
  const { lang } = useLanguage();
  const buttonTranslations = getButtonTranslation2(lang);
  const [copied, setCopied] = (0, import_react36.useState)(false);
  const encodedUrl = encodeURIComponent(url);
  const encodedTitle = encodeURIComponent(title);
  const handleCopy = async () => {
    try {
      await navigator.clipboard.writeText(url);
      setCopied(true);
      setTimeout(() => setCopied(false), 2e3);
    } catch {}
  };
  const links = [
    {
      name: "Facebook",
      href: `https://www.facebook.com/sharer/sharer.php?u=${encodedUrl}`,
      icon: /* @__PURE__ */ (0, import_jsx_runtime157.jsx)(
        Facebook_default,
        {},
      ),
    },
    {
      name: "LinkedIn",
      href: `https://www.linkedin.com/sharing/share-offsite/?url=${encodedUrl}`,
      icon: /* @__PURE__ */ (0, import_jsx_runtime157.jsx)(
        LinkedIn_default,
        {},
      ),
    },
    {
      name: "X",
      href: `https://twitter.com/intent/tweet?url=${encodedUrl}&text=${encodedTitle}`,
      icon: /* @__PURE__ */ (0, import_jsx_runtime157.jsx)(X_default, {}),
    },
  ];
  return /* @__PURE__ */ (0, import_jsx_runtime157.jsxs)(Box_default, {
    sx: {
      display: "flex",
      alignItems: "center",
      flexWrap: "wrap",
      gap: "10px",
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime157.jsx)(Typography_default, {
        variant: "caption",
        sx: { color: "text.secondary", mr: "4px" },
        children: buttonTranslations.shareOn,
      }),
      links.map((link) =>
        /* @__PURE__ */ (0, import_jsx_runtime157.jsx)(
          Box_default,
          {
            component: "a",
            href: link.href,
            target: "_blank",
            rel: "noopener noreferrer",
            "aria-label": `${buttonTranslations.shareOn} ${link.name}`,
            sx: circleSx,
            children: link.icon,
          },
          link.name,
        ),
      ),
      /* @__PURE__ */ (0, import_jsx_runtime157.jsx)(Box_default, {
        component: "button",
        type: "button",
        onClick: handleCopy,
        "aria-label": buttonTranslations.copyLink ?? "Copy link",
        sx: circleSx,
        children: copied
          ? /* @__PURE__ */ (0, import_jsx_runtime157.jsx)(CheckIcon, {
              size: 11,
            })
          : /* @__PURE__ */ (0, import_jsx_runtime157.jsx)(Link_default2, {}),
      }),
    ],
  });
}

// src/page-content/components/section/portfolio/RelatedProjects.tsx
init_runtime2();
init_static2();
var import_jsx_runtime158 = __toESM(require_jsx_runtime(), 1);
function RelatedProjects({ items }) {
  const { lang } = useLanguage();
  const projectTranslations = getPortfolioTranslation2(lang).project;
  if (items.length === 0) return null;
  const cards = items.slice(0, 5).map((item) =>
    /* @__PURE__ */ (0, import_jsx_runtime158.jsx)(
      PortfolioPreviewCard,
      {
        variant: "compact",
        title: item.title,
        image: item.images[0],
        href: withBasePath(
          `/${getPageSlugByKey("portfolio")}/${getPortfolioSlugById(item.id)}`,
        ),
      },
      item.id,
    ),
  );
  return /* @__PURE__ */ (0, import_jsx_runtime158.jsxs)(Box_default, {
    sx: { display: "flex", flexDirection: "column", gap: "24px" },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime158.jsx)(Typography_default, {
        variant: "h4",
        component: "h2",
        children: projectTranslations.relatedProjects,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime158.jsx)(Box_default, {
        sx: {
          display: { xs: "none", md: "grid" },
          gridTemplateColumns: "repeat(5, minmax(0, 1fr))",
          gap: "20px",
          alignItems: "start",
        },
        children: cards,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime158.jsx)(Box_default, {
        sx: { display: { xs: "block", md: "none" } },
        children: /* @__PURE__ */ (0, import_jsx_runtime158.jsx)(Carousel, {
          items: cards,
          slideWidth: { xs: "calc(50% - 10px)" },
          gap: 20,
          controlsGap: { xs: 24 },
          ariaLabel: projectTranslations.relatedProjects,
        }),
      }),
    ],
  });
}

// src/page-content/components/section/portfolio/PortfolioItemSection.tsx
var import_jsx_runtime159 = __toESM(require_jsx_runtime(), 1);
function PortfolioItemSection({ id }) {
  const router2 = useRouter();
  const { lang } = useLanguage();
  const projectTranslations = getPortfolioTranslation2(lang).project;
  const allItems = getPortfolioItems(lang);
  const portfolioItem = allItems.find((item) => item.id === id);
  if (!portfolioItem) {
    return null;
  }
  const images = portfolioItem.images ?? [];
  const related = portfolioItem.category
    ? allItems.filter(
        (item) =>
          item.id !== portfolioItem.id &&
          item.category === portfolioItem.category,
      )
    : [];
  const details2 = [
    {
      label: projectTranslations.details.date,
      value: portfolioItem.date
        ? formatEventDate(portfolioItem.date, lang)
        : "",
    },
    { label: projectTranslations.details.client, value: portfolioItem.client },
    {
      label: projectTranslations.details.category,
      value: portfolioItem.category,
    },
  ].filter((detail) => Boolean(detail.value));
  return /* @__PURE__ */ (0, import_jsx_runtime159.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "36px", md: "56px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(BackButton, {
        label: projectTranslations.backToPortfolio,
        onClick: () => router2.push(`/${getPageSlugByKey("portfolio")}`),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime159.jsxs)(Box_default, {
        sx: {
          display: "flex",
          flexDirection: { xs: "column", md: "row" },
          alignItems: "flex-start",
          gap: { xs: "36px", md: "64px" },
        },
        children: [
          images.length > 0 &&
            /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(Box_default, {
              sx: { width: "100%", flex: { md: "0 0 55%" } },
              children: /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(
                MediaGallery,
                { images, title: portfolioItem.title },
              ),
            }),
          /* @__PURE__ */ (0, import_jsx_runtime159.jsxs)(Box_default, {
            sx: {
              flex: 1,
              minWidth: 0,
              width: "100%",
              display: "flex",
              flexDirection: "column",
              gap: { xs: "24px", md: "28px" },
            },
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(
                Typography_default,
                {
                  variant: "h2",
                  component: "h2",
                  children: portfolioItem.title,
                },
              ),
              portfolioItem.text &&
                /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(
                  Typography_default,
                  {
                    component: "div",
                    variant: "body1",
                    children: /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(
                      RichText_default,
                      {
                        text: portfolioItem.text,
                        allowStyling: {
                          newLine: true,
                          bold: true,
                          italic: true,
                          underline: true,
                        },
                      },
                    ),
                  },
                ),
              details2.length > 0 &&
                /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(Box_default, {
                  component: "dl",
                  "aria-label": projectTranslations.details.label,
                  sx: (theme2) => ({
                    m: 0,
                    borderTop: `1px solid ${theme2.palette.surfaces.border}`,
                  }),
                  children: details2.map((detail) =>
                    /* @__PURE__ */ (0, import_jsx_runtime159.jsxs)(
                      Box_default,
                      {
                        sx: (theme2) => ({
                          display: "flex",
                          alignItems: "center",
                          justifyContent: "space-between",
                          gap: { xs: "16px", md: "24px" },
                          py: { xs: "14px", md: "16px" },
                          borderBottom: `1px solid ${theme2.palette.surfaces.border}`,
                        }),
                        children: [
                          /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(
                            Typography_default,
                            {
                              component: "dt",
                              variant: "subtitle1",
                              children: detail.label,
                            },
                          ),
                          /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(
                            Typography_default,
                            {
                              component: "dd",
                              variant: "body1",
                              sx: {
                                m: 0,
                                color: "text.secondary",
                                textAlign: "right",
                              },
                              children: detail.value,
                            },
                          ),
                        ],
                      },
                      detail.label,
                    ),
                  ),
                }),
              /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(ShareActions, {
                title: portfolioItem.title,
              }),
            ],
          }),
        ],
      }),
      related.length > 0 &&
        /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(RelatedProjects, {
          items: related,
        }),
    ],
  });
}

// src/page-content/pages/portfolio/PortfolioItem.tsx
var import_jsx_runtime160 = __toESM(require_jsx_runtime(), 1);
function PortfolioItemPage({ slug }) {
  const { lang } = useLanguage();
  const portfolioId = parseInt(slug, 10);
  const portfolioItem = getPortfolioItems(lang).find(
    (item) => item.id === portfolioId,
  );
  if (isNaN(portfolioId) || !portfolioItem) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime160.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(HeaderSection, {
        title: portfolioItem.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(Section, {
        id: "portfolio-item",
        paddingY: { xs: "64px", sm: "80px", md: "100px" },
        children: /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(
          PortfolioItemSection,
          { id: portfolioItem.id },
        ),
      }),
    ],
  });
}

// src/page-content/pages/pricing/Pricing.tsx
init_runtime2();
init_runtime2();

// src/page-content/components/section/pricing/PricingSection.tsx
init_runtime2();

// src/page-content/components/section/pricing/store/CustomStore.tsx
var import_react37 = __toESM(require_react(), 1);

// src/components/common/ListToolbar.tsx
var import_jsx_runtime161 = __toESM(require_jsx_runtime(), 1);
function ListToolbar({
  searchValue,
  onSearchChange,
  searchLabel,
  sortValue,
  onSortChange,
  sortLabel,
  sortOptions,
  sortWidth = 220,
}) {
  return /* @__PURE__ */ (0, import_jsx_runtime161.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: { xs: "column", sm: "row" },
      alignItems: "stretch",
      gap: "12px",
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime161.jsxs)(Box_default, {
        sx: (theme2) => ({
          flex: 1,
          minWidth: 0,
          display: "flex",
          alignItems: "center",
          gap: "12px",
          height: 56,
          px: "22px",
          borderRadius: "999px",
          color: theme2.palette.text.secondary,
          boxShadow: `inset 0 0 0 1px ${theme2.palette.surfaces.border}`,
          transition: theme2.transitions.create(["box-shadow"], {
            duration: theme2.transitions.duration.short,
          }),
          "&:focus-within": {
            boxShadow: `inset 0 0 0 1px ${theme2.palette.text.primary}`,
          },
        }),
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime161.jsx)(SearchIcon, {}),
          /* @__PURE__ */ (0, import_jsx_runtime161.jsx)(Box_default, {
            component: "input",
            type: "search",
            value: searchValue,
            onChange: (event) => onSearchChange(event.target.value),
            placeholder: searchLabel,
            "aria-label": searchLabel,
            sx: (theme2) => ({
              ...theme2.typography.body1,
              flex: 1,
              minWidth: 0,
              border: 0,
              outline: "none",
              background: "none",
              color: theme2.palette.text.primary,
              "&::placeholder": {
                color: theme2.palette.text.secondary,
                opacity: 1,
              },
              "&::-webkit-search-cancel-button": { cursor: "pointer" },
            }),
          }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime161.jsxs)(Box_default, {
        sx: {
          position: "relative",
          flexShrink: 0,
          width: { xs: "100%", sm: sortWidth },
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime161.jsx)(Box_default, {
            component: "select",
            value: sortValue,
            onChange: (event) => onSortChange(event.target.value),
            "aria-label": sortLabel,
            sx: (theme2) => ({
              ...theme2.typography.subtitle1,
              appearance: "none",
              width: "100%",
              height: 56,
              pl: "22px",
              pr: "46px",
              cursor: "pointer",
              border: 0,
              borderRadius: "999px",
              background: "none",
              color: theme2.palette.text.primary,
              boxShadow: `inset 0 0 0 1px ${theme2.palette.surfaces.border}`,
              transition: theme2.transitions.create(["box-shadow"], {
                duration: theme2.transitions.duration.short,
              }),
              "&:hover, &:focus-visible": {
                boxShadow: `inset 0 0 0 1px ${theme2.palette.text.primary}`,
              },
            }),
            children: sortOptions.map((option) =>
              /* @__PURE__ */ (0, import_jsx_runtime161.jsx)(
                "option",
                { value: option.value, children: option.label },
                option.value,
              ),
            ),
          }),
          /* @__PURE__ */ (0, import_jsx_runtime161.jsx)(Box_default, {
            "aria-hidden": true,
            sx: {
              position: "absolute",
              top: "50%",
              right: 22,
              transform: "translateY(-50%)",
              display: "flex",
              pointerEvents: "none",
            },
            children: /* @__PURE__ */ (0, import_jsx_runtime161.jsx)(
              ChevronDownIcon,
              {},
            ),
          }),
        ],
      }),
    ],
  });
}

// src/page-content/components/section/pricing/store/CustomStore.tsx
init_runtime2();
init_static2();
init_runtime2();

// src/components/common/Tag.tsx
var import_jsx_runtime162 = __toESM(require_jsx_runtime(), 1);
function toneSx3(tone, theme2) {
  const { palette: palette2 } = theme2;
  switch (tone) {
    case "brand":
      return {
        backgroundColor: palette2.primary.main,
        color: palette2.primary.contrastText,
      };
    case "rose":
      return {
        backgroundColor: palette2.surfaces.rose,
        color: palette2.text.primary,
      };
    case "dark":
      return {
        backgroundColor: palette2.text.primary,
        color: palette2.background.default,
      };
    case "soft":
      return {
        backgroundColor: palette2.surfaces.bgAlt,
        color: palette2.text.secondary,
      };
    case "outline":
      return {
        boxShadow: `inset 0 0 0 1px ${palette2.surfaces.border}`,
        color: palette2.text.secondary,
      };
    default:
      return {
        backgroundColor: palette2.surfaces.mint,
        color: palette2.text.primary,
      };
  }
}
function Tag({ label, tone = "mint", sx }) {
  return /* @__PURE__ */ (0, import_jsx_runtime162.jsx)(Box_default, {
    sx: [
      (theme2) => ({
        display: "inline-flex",
        alignItems: "center",
        borderRadius: "999px",
        px: "10px",
        py: "4px",
        ...toneSx3(tone, theme2),
      }),
      ...(Array.isArray(sx) ? sx : [sx]),
    ],
    children: /* @__PURE__ */ (0, import_jsx_runtime162.jsx)(
      Typography_default,
      {
        variant: "caption",
        component: "span",
        sx: { color: "inherit" },
        children: label,
      },
    ),
  });
}

// src/page-content/components/section/pricing/common/DiscountBadge.tsx
var import_jsx_runtime163 = __toESM(require_jsx_runtime(), 1);
function DiscountBadge({ price, discountedValue }) {
  if (!price || !discountedValue) return null;
  const original = Number(price);
  const discounted = Number(discountedValue);
  if (
    Number.isNaN(original) ||
    Number.isNaN(discounted) ||
    discounted >= original
  ) {
    return null;
  }
  const discountPercent = Math.round(
    ((original - discounted) / original) * 100,
  );
  if (discountPercent <= 0) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime163.jsx)(Tag, {
    label: `- ${discountPercent} %`,
    tone: "brand",
  });
}

// src/page-content/components/section/pricing/common/PriceValue.tsx
init_runtime2();
var import_jsx_runtime164 = __toESM(require_jsx_runtime(), 1);
function PriceValue({
  value,
  currency,
  unit,
  onAgreement,
  discountedValue,
  size = "large",
}) {
  const { lang } = useLanguage();
  const priceTranslation = getPriceTranslation2(lang);
  const unitLabel = getPricingUnitTranslation(unit, lang);
  const chargedValue = discountedValue || value;
  return /* @__PURE__ */ (0, import_jsx_runtime164.jsx)(Box_default, {
    sx: {
      display: "flex",
      alignItems: "baseline",
      flexWrap: "wrap",
      columnGap: "6px",
      rowGap: "2px",
    },
    children: onAgreement
      ? /* @__PURE__ */ (0, import_jsx_runtime164.jsx)(Typography_default, {
          component: "span",
          sx: (theme2) => ({
            fontFamily: theme2.typography.h2.fontFamily,
            fontSize: size === "large" ? { xs: 22, md: 26 } : 20,
            lineHeight: 1.25,
          }),
          children: priceTranslation.onAgreement,
        })
      : /* @__PURE__ */ (0, import_jsx_runtime164.jsxs)(
          import_jsx_runtime164.Fragment,
          {
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime164.jsx)(
                Typography_default,
                {
                  component: "span",
                  sx: (theme2) => ({
                    fontFamily: theme2.typography.h2.fontFamily,
                    fontSize: size === "large" ? { xs: 28, md: 42 } : 28,
                    lineHeight: 1.15,
                  }),
                  children: chargedValue,
                },
              ),
              /* @__PURE__ */ (0, import_jsx_runtime164.jsx)(
                Typography_default,
                { component: "span", variant: "subtitle1", children: currency },
              ),
              unitLabel &&
                /* @__PURE__ */ (0, import_jsx_runtime164.jsxs)(
                  Typography_default,
                  {
                    component: "span",
                    variant: "caption",
                    sx: { color: "text.secondary" },
                    children: ["/ ", unitLabel],
                  },
                ),
              discountedValue &&
                /* @__PURE__ */ (0, import_jsx_runtime164.jsxs)(
                  Typography_default,
                  {
                    component: "span",
                    variant: "caption",
                    sx: {
                      color: "text.secondary",
                      textDecoration: "line-through",
                    },
                    children: [value, " ", currency],
                  },
                ),
            ],
          },
        ),
  });
}

// src/page-content/components/section/pricing/common/StatusBadge.tsx
init_runtime2();
var import_jsx_runtime165 = __toESM(require_jsx_runtime(), 1);
function StatusBadge({ status }) {
  const { lang } = useLanguage();
  const priceTranslation = getPriceTranslation2(lang);
  if (status === "COMING_SOON") {
    return /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(Tag, {
      label: priceTranslation.comingSoon,
      tone: "rose",
    });
  }
  if (status === "UNAVAILABLE") {
    return /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(Tag, {
      label: priceTranslation.unavailable,
      tone: "soft",
    });
  }
  return null;
}

// src/page-content/components/section/pricing/store/StoreItemCard.tsx
var import_jsx_runtime166 = __toESM(require_jsx_runtime(), 1);
function StoreItemCard({ item, href, recommendedLabel }) {
  const featureSummary = item.features
    .slice(0, 2)
    .map((feature) => `${feature.label}: ${feature.value}`)
    .join(" \xB7 ");
  const hasBadges =
    item.recommended ||
    item.status === "COMING_SOON" ||
    item.status === "UNAVAILABLE" ||
    Boolean(item.price.discountedValue);
  return /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Box_default, {
    component: "a",
    href,
    sx: (theme2) => ({
      display: "flex",
      flexDirection: "column",
      height: "100%",
      borderRadius: "12px",
      overflow: "hidden",
      textDecoration: "none",
      color: "inherit",
      backgroundColor: theme2.palette.surfaces.surface,
      "&:hover .store-image, &:focus-visible .store-image": {
        transform: "scale(1.04)",
      },
      "&:hover .store-title, &:focus-visible .store-title": {
        color: theme2.palette.primary.main,
      },
    }),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Box_default, {
        sx: (theme2) => ({
          position: "relative",
          height: { xs: 260, md: 280 },
          overflow: "hidden",
          backgroundColor: theme2.palette.surfaces.placeholder,
        }),
        children: [
          item.images[0] &&
            /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Box_default, {
              component: "img",
              className: "store-image",
              src: item.images[0],
              alt: "",
              loading: "lazy",
              sx: (theme2) => ({
                position: "absolute",
                inset: 0,
                width: "100%",
                height: "100%",
                objectFit: "cover",
                transition: theme2.transitions.create("transform"),
              }),
            }),
          hasBadges &&
            /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Box_default, {
              sx: {
                position: "absolute",
                top: 16,
                left: 16,
                right: 16,
                display: "flex",
                flexWrap: "wrap",
                gap: "6px",
              },
              children: [
                item.recommended &&
                  /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Tag, {
                    label: recommendedLabel,
                    tone: "brand",
                  }),
                /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(StatusBadge, {
                  status: item.status,
                }),
                /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(DiscountBadge, {
                  price: item.price.value,
                  discountedValue: item.price.discountedValue,
                }),
              ],
            }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Box_default, {
        sx: {
          flexGrow: 1,
          display: "flex",
          flexDirection: "column",
          gap: "8px",
          p: "18px 20px 22px",
        },
        children: [
          item.category &&
            /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Typography_default, {
              variant: "caption",
              component: "p",
              sx: { color: "text.secondary" },
              children: item.category,
            }),
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Typography_default, {
            variant: "h5",
            component: "h3",
            className: "store-title",
            children: item.title,
          }),
          item.text &&
            /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Typography_default, {
              variant: "body1",
              sx: { color: "text.secondary" },
              children: stripRichText(truncateWordSafe(item.text, 100)),
            }),
          featureSummary &&
            /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Typography_default, {
              variant: "caption",
              component: "p",
              sx: { color: "text.secondary" },
              children: featureSummary,
            }),
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Box_default, {
            sx: { mt: "auto", pt: "6px" },
            children: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(
              PriceValue,
              {
                size: "small",
                value: item.price.value,
                currency: item.price.currency,
                unit: item.price.unit,
                onAgreement: item.price.onAgreement,
                discountedValue: item.price.discountedValue,
              },
            ),
          }),
        ],
      }),
    ],
  });
}

// src/page-content/components/section/pricing/common/useVisiblePricingItems.ts
init_runtime2();
function useVisiblePricingItems() {
  const { lang } = useLanguage();
  return getPricingItems(lang).filter((item) => item.status !== "DISABLED");
}

// src/page-content/components/section/pricing/store/CustomStore.tsx
var import_jsx_runtime167 = __toESM(require_jsx_runtime(), 1);
var PER_PAGE = 9;
var parsePriceValue = (value) => {
  let normalizedValue = value.trim().replace(/\s+/g, "");
  if (!normalizedValue) {
    return null;
  }
  normalizedValue = normalizedValue.replace(/[^\d,.-]/g, "");
  const lastCommaIndex = normalizedValue.lastIndexOf(",");
  const lastDotIndex = normalizedValue.lastIndexOf(".");
  if (lastCommaIndex >= 0 && lastDotIndex >= 0) {
    normalizedValue =
      lastCommaIndex > lastDotIndex
        ? normalizedValue.replace(/\./g, "").replace(",", ".")
        : normalizedValue.replace(/,/g, "");
  } else if (lastCommaIndex >= 0) {
    const decimalPlaces = normalizedValue.length - lastCommaIndex - 1;
    normalizedValue =
      decimalPlaces > 0 && decimalPlaces <= 2
        ? normalizedValue.replace(",", ".")
        : normalizedValue.replace(/,/g, "");
  } else if (lastDotIndex >= 0) {
    const dotCount = (normalizedValue.match(/\./g) ?? []).length;
    const decimalPlaces = normalizedValue.length - lastDotIndex - 1;
    if (dotCount > 1 || decimalPlaces === 3) {
      normalizedValue = normalizedValue.replace(/\./g, "");
    }
  }
  const parsedValue = Number(normalizedValue);
  return Number.isFinite(parsedValue) ? parsedValue : null;
};
var getComparablePrice = (price) => {
  if (price.onAgreement) {
    return null;
  }
  return parsePriceValue(price.discountedValue) ?? parsePriceValue(price.value);
};
function CustomStoreInner() {
  const { lang } = useLanguage();
  const pricingSection = getPricingSection(lang);
  const pricingItems = useVisiblePricingItems();
  const categories = pricingSection?.categoryOptions ?? [];
  const pricingStoreTranslation = getPricingStoreTranslation2(lang);
  const packagesTranslation = getPricingTranslation_packagesNoImages2(lang);
  const buttonTranslation = getButtonTranslation2(lang);
  const priceTranslation = getPriceTranslation2(lang);
  const {
    searchInput,
    setSearchInput,
    searchQuery,
    selectedCategoryId,
    setSelectedCategoryId,
    sortOrder,
    setSortOrder,
    page,
    setPage,
  } = useListFilters();
  const filteredAndSortedItems = (0, import_react37.useMemo)(() => {
    const normalizedSearchQuery = normalizeSearchValue(searchQuery);
    return pricingItems
      .filter((item) => {
        if (selectedCategoryId && item.categoryId !== selectedCategoryId) {
          return false;
        }
        if (!normalizedSearchQuery) {
          return true;
        }
        const searchableValue = normalizeSearchValue(
          [
            item.title,
            stripRichText(item.text),
            item.category,
            item.price.value,
            item.price.discountedValue,
            item.price.currency,
            item.price.onAgreement ? priceTranslation.onAgreement : "",
            ...item.features.flatMap((feature) => [
              feature.label,
              feature.value,
            ]),
          ].join(" "),
        );
        return searchableValue.includes(normalizedSearchQuery);
      })
      .sort((firstItem, secondItem) => {
        const firstPrice = getComparablePrice(firstItem.price);
        const secondPrice = getComparablePrice(secondItem.price);
        if (firstPrice === null && secondPrice === null) {
          return firstItem.id - secondItem.id;
        }
        if (firstPrice === null) {
          return 1;
        }
        if (secondPrice === null) {
          return -1;
        }
        const priceDifference = firstPrice - secondPrice;
        if (priceDifference === 0) {
          return firstItem.id - secondItem.id;
        }
        return sortOrder === "ascending" ? priceDifference : -priceDifference;
      });
  }, [
    pricingItems,
    searchQuery,
    selectedCategoryId,
    sortOrder,
    priceTranslation,
  ]);
  const { pageCount, currentPage, pageItems } = paginate(
    filteredAndSortedItems,
    page,
    PER_PAGE,
  );
  return /* @__PURE__ */ (0, import_jsx_runtime167.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "28px", md: "32px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime167.jsx)(ListToolbar, {
        searchValue: searchInput,
        onSearchChange: setSearchInput,
        searchLabel: pricingStoreTranslation.search,
        sortValue: sortOrder,
        onSortChange: (value) => setSortOrder(value),
        sortLabel: pricingStoreTranslation.sortByPrice,
        sortOptions: [
          { value: "ascending", label: pricingStoreTranslation.priceAscending },
          {
            value: "descending",
            label: pricingStoreTranslation.priceDescending,
          },
        ],
      }),
      categories.length > 0 &&
        /* @__PURE__ */ (0, import_jsx_runtime167.jsx)(FilterChips, {
          ariaLabel: pricingStoreTranslation.category,
          options: [
            { value: "", label: buttonTranslation.all },
            ...categories.map((category) => ({
              value: category.id,
              label: category.name,
            })),
          ],
          value: selectedCategoryId,
          onChange: setSelectedCategoryId,
        }),
      pageItems.length > 0
        ? /* @__PURE__ */ (0, import_jsx_runtime167.jsx)(Box_default, {
            sx: {
              display: "grid",
              gap: "24px",
              gridTemplateColumns: {
                xs: "1fr",
                sm: "repeat(2, minmax(0, 1fr))",
                md: "repeat(3, minmax(0, 1fr))",
              },
              alignItems: "stretch",
            },
            children: pageItems.map((item) =>
              /* @__PURE__ */ (0, import_jsx_runtime167.jsx)(
                StoreItemCard,
                {
                  item,
                  recommendedLabel: packagesTranslation.items.recommended,
                  href: withBasePath(
                    `/${getPageSlugByKey("pricing")}/${getPricingSlugById(item.id)}`,
                  ),
                },
                item.id,
              ),
            ),
          })
        : /* @__PURE__ */ (0, import_jsx_runtime167.jsx)(Typography_default, {
            variant: "body1",
            sx: { color: "text.secondary", py: "32px" },
            children: pricingStoreTranslation.noResults,
          }),
      pageCount > 1 &&
        /* @__PURE__ */ (0, import_jsx_runtime167.jsx)(Box_default, {
          sx: {
            display: "flex",
            justifyContent: { xs: "center", md: "flex-start" },
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime167.jsx)(
            PaginationControls,
            {
              page: currentPage,
              pageCount,
              onChange: setPage,
            },
          ),
        }),
    ],
  });
}
function CustomStore() {
  return /* @__PURE__ */ (0, import_jsx_runtime167.jsx)(
    import_react37.Suspense,
    {
      fallback: null,
      children: /* @__PURE__ */ (0, import_jsx_runtime167.jsx)(
        CustomStoreInner,
        {},
      ),
    },
  );
}

// src/page-content/components/section/pricing/price-list/PriceList.tsx
var import_react39 = __toESM(require_react(), 1);
init_runtime2();

// src/page-content/components/section/pricing/common/FeatureList.tsx
var import_jsx_runtime168 = __toESM(require_jsx_runtime(), 1);
function FeatureList({ features, ring = false }) {
  if (features.length === 0) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime168.jsx)(Box_default, {
    component: "dl",
    sx: {
      m: 0,
      display: "flex",
      flexDirection: "column",
      gap: { xs: "8px", md: "10px" },
    },
    children: features.map((feature, index) =>
      /* @__PURE__ */ (0, import_jsx_runtime168.jsxs)(
        Box_default,
        {
          sx: { display: "flex", alignItems: "center", gap: "10px" },
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime168.jsx)(Box_default, {
              "aria-hidden": true,
              sx: (theme2) => ({
                flexShrink: 0,
                width: ring ? 8 : 6,
                height: ring ? 8 : 6,
                borderRadius: "50%",
                backgroundColor: ring
                  ? theme2.palette.surfaces.rose
                  : theme2.palette.primary.main,
                boxShadow: ring
                  ? `inset 0 0 0 1px ${theme2.palette.primary.main}`
                  : "none",
              }),
            }),
            /* @__PURE__ */ (0, import_jsx_runtime168.jsx)(Typography_default, {
              component: "dt",
              variant: "body1",
              sx: { flex: 1, minWidth: 0 },
              children: feature.label,
            }),
            /* @__PURE__ */ (0, import_jsx_runtime168.jsx)(Typography_default, {
              component: "dd",
              variant: "body1",
              sx: { m: 0, color: "text.secondary", textAlign: "right" },
              children: feature.value,
            }),
          ],
        },
        index,
      ),
    ),
  });
}

// src/page-content/components/section/pricing/common/PricingCtaButton.tsx
init_runtime2();

// src/page-content/components/section/common/ContactCtaButton.tsx
init_static2();
var import_jsx_runtime169 = __toESM(require_jsx_runtime(), 1);
function ContactCtaButton({
  subject,
  label,
  tone = "outline",
  fullWidth = false,
  sx,
}) {
  const href = `${withBasePath(`/${getPageSlugByKey("contact")}/`)}?subject=${encodeURIComponent(subject)}`;
  return /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(ArrowButton, {
    component: "a",
    href,
    tone,
    fullWidth,
    sx,
    children: label,
  });
}

// src/page-content/components/section/pricing/common/PricingCtaButton.tsx
var import_jsx_runtime170 = __toESM(require_jsx_runtime(), 1);
function PricingCtaButton({ title, tone = "outline", fullWidth = false, sx }) {
  const { lang } = useLanguage();
  const packagesTranslation = getPricingTranslation_packagesNoImages2(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime170.jsx)(ContactCtaButton, {
    subject: `${packagesTranslation.acquirementSubject}: ${title}`,
    label: packagesTranslation.items.callToAction,
    tone,
    fullWidth,
    sx,
  });
}

// src/page-content/components/section/pricing/price-list/PriceList.tsx
var import_jsx_runtime171 = __toESM(require_jsx_runtime(), 1);
function PriceList() {
  const { lang } = useLanguage();
  const pricingSection = getPricingSection(lang);
  const pricingItems = useVisiblePricingItems();
  const packagesTranslation = getPricingTranslation_packagesNoImages2(lang);
  const allLabel = getButtonTranslation2(lang).all;
  const categories = pricingSection?.categoryOptions ?? [];
  const [selectedCategoryId, setSelectedCategoryId] = (0,
  import_react39.useState)("");
  const items = (0, import_react39.useMemo)(
    () =>
      selectedCategoryId
        ? pricingItems.filter((item) => item.categoryId === selectedCategoryId)
        : pricingItems,
    [pricingItems, selectedCategoryId],
  );
  return /* @__PURE__ */ (0, import_jsx_runtime171.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "28px", md: "32px" },
    },
    children: [
      categories.length > 0 &&
        /* @__PURE__ */ (0, import_jsx_runtime171.jsx)(FilterChips, {
          ariaLabel: allLabel,
          options: [
            { value: "", label: allLabel },
            ...categories.map((category) => ({
              value: category.id,
              label: category.name,
            })),
          ],
          value: selectedCategoryId,
          onChange: setSelectedCategoryId,
        }),
      /* @__PURE__ */ (0, import_jsx_runtime171.jsx)(Box_default, {
        sx: {
          display: "flex",
          flexDirection: "column",
          gap: { xs: "20px", md: 0 },
        },
        children: items.map((item, index) => {
          const highlighted = item.recommended;
          const afterHighlight = index > 0 && items[index - 1].recommended;
          const hasBadges =
            highlighted ||
            item.status === "COMING_SOON" ||
            item.status === "UNAVAILABLE" ||
            Boolean(item.price.discountedValue);
          return /* @__PURE__ */ (0, import_jsx_runtime171.jsxs)(
            Box_default,
            {
              sx: (theme2) => ({
                display: "flex",
                flexDirection: { xs: "column", md: "row" },
                alignItems: { xs: "stretch", md: "center" },
                gap: { xs: "14px", md: "32px" },
                px: { xs: "20px", md: highlighted ? "28px" : 0 },
                py: { xs: "22px", md: "28px" },
                borderRadius: { xs: "16px", md: highlighted ? "16px" : 0 },
                borderStyle: "solid",
                borderColor: highlighted
                  ? theme2.palette.primary.main
                  : theme2.palette.surfaces.border,
                borderWidth: highlighted
                  ? "1px"
                  : {
                      xs: "1px",
                      md: index === 0 || afterHighlight ? 0 : "1px 0 0",
                    },
                backgroundColor: highlighted
                  ? theme2.palette.surfaces.surface
                  : "transparent",
              }),
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime171.jsxs)(Box_default, {
                  sx: {
                    display: "flex",
                    flexDirection: { xs: "column", md: "row" },
                    alignItems: { xs: "flex-start", md: "center" },
                    gap: { xs: "14px", md: "24px" },
                    width: { md: 520 },
                    flexShrink: 0,
                  },
                  children: [
                    item.images[0] &&
                      /* @__PURE__ */ (0, import_jsx_runtime171.jsx)(
                        Box_default,
                        {
                          component: "img",
                          src: item.images[0],
                          alt: "",
                          loading: "lazy",
                          sx: (theme2) => ({
                            flexShrink: 0,
                            width: { xs: 140, md: 170 },
                            height: { xs: 84, md: 100 },
                            objectFit: "cover",
                            borderRadius: "999px",
                            backgroundColor:
                              theme2.palette.surfaces.placeholder,
                            boxShadow: `0 0 0 4px ${theme2.palette.surfaces.onImage}`,
                          }),
                        },
                      ),
                    /* @__PURE__ */ (0, import_jsx_runtime171.jsxs)(
                      Box_default,
                      {
                        sx: {
                          minWidth: 0,
                          display: "flex",
                          flexDirection: "column",
                          gap: "8px",
                        },
                        children: [
                          hasBadges &&
                            /* @__PURE__ */ (0, import_jsx_runtime171.jsxs)(
                              Box_default,
                              {
                                sx: {
                                  display: "flex",
                                  flexWrap: "wrap",
                                  gap: "6px",
                                },
                                children: [
                                  highlighted &&
                                    /* @__PURE__ */ (0,
                                    import_jsx_runtime171.jsx)(Tag, {
                                      label:
                                        packagesTranslation.items.recommended,
                                      tone: "brand",
                                    }),
                                  /* @__PURE__ */ (0,
                                  import_jsx_runtime171.jsx)(StatusBadge, {
                                    status: item.status,
                                  }),
                                  /* @__PURE__ */ (0,
                                  import_jsx_runtime171.jsx)(DiscountBadge, {
                                    price: item.price.value,
                                    discountedValue: item.price.discountedValue,
                                  }),
                                ],
                              },
                            ),
                          /* @__PURE__ */ (0, import_jsx_runtime171.jsx)(
                            Typography_default,
                            {
                              variant: "h5",
                              component: "h3",
                              children: item.title,
                            },
                          ),
                          item.text &&
                            /* @__PURE__ */ (0, import_jsx_runtime171.jsx)(
                              Typography_default,
                              {
                                variant: "body1",
                                sx: { color: "text.secondary" },
                                children: stripRichText(item.text),
                              },
                            ),
                          item.category &&
                            /* @__PURE__ */ (0, import_jsx_runtime171.jsx)(
                              Typography_default,
                              {
                                variant: "caption",
                                component: "p",
                                sx: { color: "text.secondary" },
                                children: item.category,
                              },
                            ),
                        ],
                      },
                    ),
                  ],
                }),
                /* @__PURE__ */ (0, import_jsx_runtime171.jsx)(Box_default, {
                  sx: { flex: 1, minWidth: 0 },
                  children: /* @__PURE__ */ (0, import_jsx_runtime171.jsx)(
                    FeatureList,
                    { features: item.features },
                  ),
                }),
                /* @__PURE__ */ (0, import_jsx_runtime171.jsxs)(Box_default, {
                  sx: {
                    display: "flex",
                    flexDirection: "column",
                    alignItems: { xs: "flex-start", md: "flex-end" },
                    gap: "14px",
                    width: { md: 240 },
                    flexShrink: 0,
                  },
                  children: [
                    /* @__PURE__ */ (0, import_jsx_runtime171.jsx)(PriceValue, {
                      value: item.price.value,
                      currency: item.price.currency,
                      unit: item.price.unit,
                      onAgreement: item.price.onAgreement,
                      discountedValue: item.price.discountedValue,
                    }),
                    /* @__PURE__ */ (0, import_jsx_runtime171.jsx)(
                      PricingCtaButton,
                      {
                        title: item.title,
                        tone: highlighted ? "dark" : "outline",
                        sx: { width: { xs: "100%", md: "auto" } },
                      },
                    ),
                  ],
                }),
              ],
            },
            item.id,
          );
        }),
      }),
    ],
  });
}

// src/page-content/components/section/pricing/subscription/SubscriptionSection.tsx
var import_react40 = __toESM(require_react(), 1);
init_runtime2();

// src/page-content/components/section/pricing/subscription/SubscriptionCard.tsx
init_runtime2();
var import_jsx_runtime172 = __toESM(require_jsx_runtime(), 1);
function SubscriptionCard({ item }) {
  const { lang } = useLanguage();
  const packagesTranslation = getPricingTranslation_packagesNoImages2(lang);
  const highlighted = item.recommended;
  const hasBadges =
    highlighted ||
    item.status === "COMING_SOON" ||
    item.status === "UNAVAILABLE" ||
    Boolean(item.price.discountedValue);
  return /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Box_default, {
    sx: (theme2) => ({
      height: "100%",
      display: "flex",
      flexDirection: "column",
      gap: "22px",
      p: { xs: "28px 24px", md: "36px 32px" },
      borderRadius: "12px",
      backgroundColor: theme2.palette.background.default,
      borderStyle: "solid",
      borderColor: highlighted
        ? theme2.palette.text.primary
        : theme2.palette.surfaces.border,
      borderWidth: highlighted ? "1.5px" : "1px",
    }),
    children: [
      hasBadges &&
        /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Box_default, {
          sx: { display: "flex", flexWrap: "wrap", gap: "6px" },
          children: [
            highlighted &&
              /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Tag, {
                label: packagesTranslation.items.recommended,
                tone: "brand",
              }),
            /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(StatusBadge, {
              status: item.status,
            }),
            /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(DiscountBadge, {
              price: item.price.value,
              discountedValue: item.price.discountedValue,
            }),
          ],
        }),
      /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Box_default, {
        sx: { display: "flex", flexDirection: "column", gap: "10px" },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Typography_default, {
            variant: "h5",
            component: "h3",
            children: item.title,
          }),
          item.text &&
            /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Typography_default, {
              variant: "body1",
              sx: { color: "text.secondary" },
              children: stripRichText(item.text),
            }),
          item.category &&
            /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Typography_default, {
              variant: "caption",
              component: "p",
              sx: { color: "text.secondary" },
              children: item.category,
            }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Box_default, {
        children: /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(PriceValue, {
          value: item.price.value,
          currency: item.price.currency,
          unit: item.price.unit,
          onAgreement: item.price.onAgreement,
          discountedValue: item.price.discountedValue,
        }),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(PricingCtaButton, {
        title: item.title,
        tone: highlighted ? "dark" : "outline",
        fullWidth: true,
      }),
      item.features.length > 0 &&
        /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Box_default, {
          sx: (theme2) => ({
            pt: "20px",
            borderTop: `1px solid ${theme2.palette.surfaces.border}`,
          }),
          children: /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(
            FeatureList,
            { features: item.features, ring: true },
          ),
        }),
    ],
  });
}

// src/page-content/components/section/pricing/subscription/SubscriptionSection.tsx
var import_jsx_runtime173 = __toESM(require_jsx_runtime(), 1);
function SubscriptionSection() {
  const { lang } = useLanguage();
  const pricingSection = getPricingSection(lang);
  const pricingItems = useVisiblePricingItems();
  const packagesTranslation = getPricingTranslation_packagesNoImages2(lang);
  const allLabel = getButtonTranslation2(lang).all;
  const categories = pricingSection?.categoryOptions ?? [];
  const [selectedCategoryId, setSelectedCategoryId] = (0,
  import_react40.useState)("");
  const items = (0, import_react40.useMemo)(
    () =>
      selectedCategoryId
        ? pricingItems.filter((item) => item.categoryId === selectedCategoryId)
        : pricingItems,
    [pricingItems, selectedCategoryId],
  );
  const cards = items.map((item) =>
    /* @__PURE__ */ (0, import_jsx_runtime173.jsx)(
      SubscriptionCard,
      { item },
      item.id,
    ),
  );
  return /* @__PURE__ */ (0, import_jsx_runtime173.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "28px", md: "32px" },
    },
    children: [
      categories.length > 0 &&
        /* @__PURE__ */ (0, import_jsx_runtime173.jsx)(FilterChips, {
          ariaLabel: allLabel,
          options: [
            { value: "", label: allLabel },
            ...categories.map((category) => ({
              value: category.id,
              label: category.name,
            })),
          ],
          value: selectedCategoryId,
          onChange: setSelectedCategoryId,
        }),
      /* @__PURE__ */ (0, import_jsx_runtime173.jsx)(Box_default, {
        sx: {
          display: { xs: "none", sm: "grid" },
          gridTemplateColumns: {
            sm: "repeat(2, minmax(0, 1fr))",
            md: "repeat(3, minmax(0, 1fr))",
          },
          gap: "24px",
          alignItems: "stretch",
        },
        children: cards,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime173.jsx)(Box_default, {
        sx: { display: { xs: "block", sm: "none" } },
        children: /* @__PURE__ */ (0, import_jsx_runtime173.jsx)(Carousel, {
          items: cards,
          slideWidth: { xs: "100%" },
          gap: 20,
          controlsGap: { xs: 24 },
          ariaLabel: packagesTranslation.title,
        }),
      }),
    ],
  });
}

// src/page-content/components/section/pricing/PricingSection.tsx
var import_jsx_runtime174 = __toESM(require_jsx_runtime(), 1);
function PricingSection() {
  const { lang } = useLanguage();
  const pricingSection = getPricingSection(lang);
  if (!pricingSection) {
    return null;
  }
  const renderContent = () => {
    switch (pricingSection.type) {
      case "PRICING_STORE":
        return /* @__PURE__ */ (0, import_jsx_runtime174.jsx)(CustomStore, {});
      case "PRICING_LIST":
        return /* @__PURE__ */ (0, import_jsx_runtime174.jsx)(PriceList, {});
      case "PRICING_PACKAGES":
        return /* @__PURE__ */ (0, import_jsx_runtime174.jsx)(
          SubscriptionSection,
          {},
        );
      default:
        return null;
    }
  };
  return /* @__PURE__ */ (0, import_jsx_runtime174.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "36px", md: "40px" },
    },
    children: [
      (pricingSection.text || pricingSection.note) &&
        /* @__PURE__ */ (0, import_jsx_runtime174.jsxs)(Box_default, {
          sx: {
            maxWidth: 640,
            display: "flex",
            flexDirection: "column",
            gap: "14px",
          },
          children: [
            pricingSection.text &&
              /* @__PURE__ */ (0, import_jsx_runtime174.jsx)(
                Typography_default,
                { variant: "body1", children: pricingSection.text },
              ),
            pricingSection.note &&
              /* @__PURE__ */ (0, import_jsx_runtime174.jsx)(
                Typography_default,
                {
                  variant: "caption",
                  component: "p",
                  sx: { color: "text.secondary" },
                  children: pricingSection.note,
                },
              ),
          ],
        }),
      renderContent(),
    ],
  });
}

// src/page-content/pages/pricing/Pricing.tsx
var import_jsx_runtime175 = __toESM(require_jsx_runtime(), 1);
function PricingPage() {
  const { lang } = useLanguage();
  const pricingSection = getPricingSection(lang);
  if (!pricingSection) {
    return null;
  }
  const pricingTranslation_priceListWithImages =
    getPricingTranslation_priceListWithImages2(lang);
  const pricingTranslation_priceListNoImages =
    getPricingTranslation_priceListNoImages2(lang);
  const pricingTranslation_packagesNoImages =
    getPricingTranslation_packagesNoImages2(lang);
  let title = pricingSection.sectionName?.trim();
  if (!title) {
    switch (pricingSection.type) {
      case "PRICING_STORE":
        title = pricingTranslation_priceListWithImages.title;
        break;
      case "PRICING_LIST":
        title = pricingTranslation_priceListNoImages.title;
        break;
      case "PRICING_PACKAGES":
        title = pricingTranslation_packagesNoImages.title;
        break;
      default:
        title = "Pricing";
    }
  }
  return /* @__PURE__ */ (0, import_jsx_runtime175.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime175.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime175.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime175.jsx)(HeaderSection, { title }),
      /* @__PURE__ */ (0, import_jsx_runtime175.jsx)(Section, {
        id: "pricing",
        children: /* @__PURE__ */ (0, import_jsx_runtime175.jsx)(
          PricingSection,
          {},
        ),
      }),
    ],
  });
}

// src/page-content/pages/pricing/PricingItem.tsx
init_runtime2();
init_runtime2();

// src/page-content/components/section/pricing/PricingItemSection.tsx
init_runtime2();
init_static2();

// src/page-content/components/section/pricing/store/RelatedItems.tsx
init_runtime2();
init_static2();
var import_jsx_runtime176 = __toESM(require_jsx_runtime(), 1);
function RelatedItems({ items }) {
  const { lang } = useLanguage();
  const pricingTranslation = getPricingTranslation_priceListWithImages2(lang);
  if (items.length === 0) return null;
  const cards = items.slice(0, 5).map((item) =>
    /* @__PURE__ */ (0, import_jsx_runtime176.jsxs)(
      Box_default,
      {
        component: "a",
        href: withBasePath(
          `/${getPageSlugByKey("pricing")}/${getPricingSlugById(item.id)}`,
        ),
        sx: (theme2) => ({
          display: "flex",
          flexDirection: "column",
          gap: "12px",
          minWidth: 0,
          textDecoration: "none",
          color: "inherit",
          "&:hover .related-image, &:focus-visible .related-image": {
            transform: "scale(1.04)",
          },
          "&:hover .related-title, &:focus-visible .related-title": {
            color: theme2.palette.primary.main,
          },
        }),
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime176.jsx)(Box_default, {
            sx: (theme2) => ({
              position: "relative",
              height: { xs: 160, md: 180 },
              borderRadius: "12px",
              overflow: "hidden",
              backgroundColor: theme2.palette.surfaces.placeholder,
            }),
            children:
              item.images[0] &&
              /* @__PURE__ */ (0, import_jsx_runtime176.jsx)(Box_default, {
                component: "img",
                className: "related-image",
                src: item.images[0],
                alt: "",
                loading: "lazy",
                sx: (theme2) => ({
                  position: "absolute",
                  inset: 0,
                  width: "100%",
                  height: "100%",
                  objectFit: "cover",
                  transition: theme2.transitions.create("transform"),
                }),
              }),
          }),
          /* @__PURE__ */ (0, import_jsx_runtime176.jsx)(Typography_default, {
            variant: "h6",
            component: "h3",
            className: "related-title",
            children: item.title,
          }),
        ],
      },
      item.id,
    ),
  );
  return /* @__PURE__ */ (0, import_jsx_runtime176.jsxs)(Box_default, {
    sx: { display: "flex", flexDirection: "column", gap: "24px" },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime176.jsx)(Typography_default, {
        variant: "h4",
        component: "h2",
        children: pricingTranslation.relatedItemsTitle,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime176.jsx)(Box_default, {
        sx: {
          display: { xs: "none", md: "grid" },
          gridTemplateColumns: "repeat(5, minmax(0, 1fr))",
          gap: "20px",
          alignItems: "start",
        },
        children: cards,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime176.jsx)(Box_default, {
        sx: { display: { xs: "block", md: "none" } },
        children: /* @__PURE__ */ (0, import_jsx_runtime176.jsx)(Carousel, {
          items: cards,
          slideWidth: { xs: "calc(50% - 10px)" },
          gap: 20,
          controlsGap: { xs: 24 },
          ariaLabel: pricingTranslation.relatedItemsTitle,
        }),
      }),
    ],
  });
}

// src/page-content/components/section/pricing/PricingItemSection.tsx
var import_jsx_runtime177 = __toESM(require_jsx_runtime(), 1);
function PricingItemSection({ id }) {
  const { lang } = useLanguage();
  const handleBackToPricing = useBackToList(`/${getPageSlugByKey("pricing")}`);
  const pricingSection = getPricingSection(lang);
  if (!pricingSection || pricingSection.type !== "PRICING_STORE") {
    return null;
  }
  const pricingItems = getPricingItems(lang);
  const pricingItem = pricingItems.find((item) => item.id === id);
  if (!pricingItem || pricingItem.status === "DISABLED") {
    return null;
  }
  const itemTranslation =
    getPricingTranslation_priceListWithImages2(lang).items;
  const packagesTranslation = getPricingTranslation_packagesNoImages2(lang);
  const relatedItems = pricingItem.categoryId
    ? pricingItems.filter(
        (item) =>
          item.id !== pricingItem.id &&
          item.status !== "DISABLED" &&
          item.categoryId === pricingItem.categoryId,
      )
    : [];
  const hasBadges =
    pricingItem.recommended ||
    pricingItem.status === "COMING_SOON" ||
    pricingItem.status === "UNAVAILABLE" ||
    Boolean(pricingItem.price.discountedValue);
  return /* @__PURE__ */ (0, import_jsx_runtime177.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "36px", md: "56px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(BackButton, {
        label: itemTranslation.goBackButton,
        onClick: handleBackToPricing,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime177.jsxs)(Box_default, {
        sx: {
          display: "flex",
          flexDirection: { xs: "column", md: "row" },
          alignItems: "flex-start",
          gap: { xs: "36px", md: "64px" },
        },
        children: [
          pricingItem.images.length > 0 &&
            /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(Box_default, {
              sx: { width: "100%", flex: { md: "0 0 55%" } },
              children: /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(
                MediaGallery,
                {
                  images: pricingItem.images,
                  title: pricingItem.title,
                },
              ),
            }),
          /* @__PURE__ */ (0, import_jsx_runtime177.jsxs)(Box_default, {
            sx: {
              flex: 1,
              minWidth: 0,
              width: "100%",
              display: "flex",
              flexDirection: "column",
              gap: { xs: "24px", md: "28px" },
            },
            children: [
              hasBadges &&
                /* @__PURE__ */ (0, import_jsx_runtime177.jsxs)(Box_default, {
                  sx: { display: "flex", flexWrap: "wrap", gap: "6px" },
                  children: [
                    pricingItem.recommended &&
                      /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(Tag, {
                        label: packagesTranslation.items.recommended,
                        tone: "brand",
                      }),
                    /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(
                      StatusBadge,
                      { status: pricingItem.status },
                    ),
                    /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(
                      DiscountBadge,
                      {
                        price: pricingItem.price.value,
                        discountedValue: pricingItem.price.discountedValue,
                      },
                    ),
                  ],
                }),
              /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(
                Typography_default,
                { variant: "h2", component: "h2", children: pricingItem.title },
              ),
              pricingItem.text &&
                /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(
                  Typography_default,
                  {
                    component: "div",
                    variant: "body1",
                    children: /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(
                      RichText_default,
                      {
                        text: pricingItem.text,
                        allowStyling: {
                          newLine: true,
                          bold: true,
                          italic: true,
                          underline: true,
                        },
                      },
                    ),
                  },
                ),
              /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(FeatureList, {
                features: pricingItem.features,
              }),
              /* @__PURE__ */ (0, import_jsx_runtime177.jsxs)(Box_default, {
                sx: (theme2) => ({
                  display: "flex",
                  flexDirection: { xs: "column", sm: "row" },
                  alignItems: { xs: "flex-start", sm: "baseline" },
                  justifyContent: "space-between",
                  gap: { xs: "8px", sm: "24px" },
                  py: { xs: "16px", md: "20px" },
                  borderTop: `1px solid ${theme2.palette.surfaces.border}`,
                  borderBottom: `1px solid ${theme2.palette.surfaces.border}`,
                }),
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(
                    Typography_default,
                    { variant: "subtitle1", children: itemTranslation.price },
                  ),
                  /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(PriceValue, {
                    value: pricingItem.price.value,
                    currency: pricingItem.price.currency,
                    unit: pricingItem.price.unit,
                    onAgreement: pricingItem.price.onAgreement,
                    discountedValue: pricingItem.price.discountedValue,
                  }),
                ],
              }),
              /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(PricingCtaButton, {
                title: pricingItem.title,
                tone: "dark",
                sx: { alignSelf: { xs: "stretch", sm: "flex-start" } },
              }),
              /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(ShareActions, {
                title: pricingItem.title,
              }),
            ],
          }),
        ],
      }),
      relatedItems.length > 0 &&
        /* @__PURE__ */ (0, import_jsx_runtime177.jsx)(RelatedItems, {
          items: relatedItems,
        }),
    ],
  });
}

// src/page-content/pages/pricing/PricingItem.tsx
var import_jsx_runtime178 = __toESM(require_jsx_runtime(), 1);
function PricingItemPage({ slug }) {
  const { lang } = useLanguage();
  const pricingId = parseInt(slug, 10);
  const pricingItem = getPricingItems(lang).find(
    (item) => item.id === pricingId,
  );
  if (isNaN(pricingId) || !pricingItem) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime178.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime178.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime178.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime178.jsx)(HeaderSection, {
        title: pricingItem.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime178.jsx)(Section, {
        id: "pricing-item",
        children: /* @__PURE__ */ (0, import_jsx_runtime178.jsx)(
          PricingItemSection,
          { id: pricingItem.id },
        ),
      }),
    ],
  });
}

// src/page-content/pages/blog/Blog.tsx
init_runtime2();

// src/page-content/components/section/blog/BlogSection.tsx
init_runtime2();
var import_jsx_runtime179 = __toESM(require_jsx_runtime(), 1);
function BlogSection() {
  const { lang } = useLanguage();
  const blogSection = getBlogSection(lang);
  const { page, setPage, pageCount, paginatedItems } = usePagination(
    getBlogItems(lang),
    6,
  );
  return /* @__PURE__ */ (0, import_jsx_runtime179.jsxs)(Box_default, {
    sx: { display: "flex", flexDirection: "column", gap: "48px" },
    children: [
      blogSection &&
        /* @__PURE__ */ (0, import_jsx_runtime179.jsx)(CenteredIntro, {
          align: "left",
          description: blogSection.text,
        }),
      /* @__PURE__ */ (0, import_jsx_runtime179.jsx)(BlogGrid, {
        items: paginatedItems,
      }),
      pageCount > 1 &&
        /* @__PURE__ */ (0, import_jsx_runtime179.jsx)(Box_default, {
          sx: { display: "flex", justifyContent: "center" },
          children: /* @__PURE__ */ (0, import_jsx_runtime179.jsx)(
            PaginationControls,
            {
              page,
              pageCount,
              onChange: setPage,
            },
          ),
        }),
    ],
  });
}

// src/page-content/pages/blog/Blog.tsx
var import_jsx_runtime180 = __toESM(require_jsx_runtime(), 1);
function BlogPage() {
  const { lang } = useLanguage();
  const blogTranslation = getBlogTranslation2(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime180.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime180.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime180.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime180.jsx)(HeaderSection, {
        title: blogTranslation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime180.jsx)(Section, {
        id: "blog",
        children: /* @__PURE__ */ (0, import_jsx_runtime180.jsx)(
          BlogSection,
          {},
        ),
      }),
    ],
  });
}

// src/page-content/pages/blog/BlogPost.tsx
init_runtime2();
init_runtime2();

// src/page-content/components/section/blog/BlogPostSection.tsx
init_runtime2();
init_static2();

// src/page-content/components/section/blog/LatestPosts.tsx
init_runtime2();
init_static2();
var import_jsx_runtime181 = __toESM(require_jsx_runtime(), 1);
function LatestPosts({ excludeId, count = 5 }) {
  const { lang } = useLanguage();
  const blogTranslations = getBlogTranslation2(lang);
  const posts = getBlogItems(lang)
    .filter((post) => post.id !== excludeId)
    .slice(0, count);
  if (posts.length === 0) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime181.jsxs)(Box_default, {
    component: "aside",
    sx: (theme2) => ({
      display: "flex",
      flexDirection: "column",
      gap: "18px",
      px: "24px",
      py: "28px",
      borderRadius: "16px",
      backgroundColor: theme2.palette.surfaces.bgAlt,
    }),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime181.jsx)(Typography_default, {
        variant: "h5",
        component: "h2",
        children: blogTranslations.posts.latestPosts,
      }),
      posts.map((post) =>
        /* @__PURE__ */ (0, import_jsx_runtime181.jsxs)(
          Box_default,
          {
            component: "a",
            href: `${getPageSlugByKeyWithBasePath("blog")}/${getBlogSlugById(post.id)}`,
            sx: (theme2) => ({
              display: "flex",
              alignItems: "center",
              gap: "14px",
              color: "inherit",
              textDecoration: "none",
              "&:hover .latest-title, &:focus-visible .latest-title": {
                color: theme2.palette.primary.main,
              },
            }),
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime181.jsx)(Box_default, {
                sx: (theme2) => ({
                  flexShrink: 0,
                  width: 80,
                  height: 64,
                  borderRadius: "8px",
                  overflow: "hidden",
                  backgroundColor: theme2.palette.surfaces.placeholder,
                }),
                children: /* @__PURE__ */ (0, import_jsx_runtime181.jsx)(
                  Box_default,
                  {
                    component: "img",
                    src: post.image,
                    alt: "",
                    loading: "lazy",
                    sx: { width: "100%", height: "100%", objectFit: "cover" },
                  },
                ),
              }),
              /* @__PURE__ */ (0, import_jsx_runtime181.jsx)(
                Typography_default,
                {
                  className: "latest-title",
                  variant: "subtitle1",
                  sx: (theme2) => ({
                    transition: theme2.transitions.create(["color"], {
                      duration: theme2.transitions.duration.short,
                    }),
                  }),
                  children: post.title,
                },
              ),
            ],
          },
          post.id,
        ),
      ),
    ],
  });
}

// src/page-content/components/section/blog/BlogPostSection.tsx
var import_jsx_runtime182 = __toESM(require_jsx_runtime(), 1);
function BlogPostSection({ id }) {
  const router2 = useRouter();
  const { lang } = useLanguage();
  const blogTranslations = getBlogTranslation2(lang);
  const blog = getBlogItems(lang).find((item) => item.id === id);
  if (!blog) {
    return null;
  }
  const meta = [blog.date ? formatEventDate(blog.date, lang) : "", blog.author]
    .filter(Boolean)
    .join(" \xB7 ");
  return /* @__PURE__ */ (0, import_jsx_runtime182.jsxs)(Box_default, {
    sx: { display: "flex", flexDirection: "column", gap: "40px" },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime182.jsx)(BackButton, {
        label: blogTranslations.posts.backToBlogs,
        onClick: () => router2.push(`/${getPageSlugByKey("blog")}`),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime182.jsxs)(Box_default, {
        sx: {
          display: "flex",
          flexDirection: { xs: "column", md: "row" },
          alignItems: "flex-start",
          gap: { xs: "48px", md: "80px" },
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime182.jsxs)(Box_default, {
            component: "article",
            sx: {
              flex: 1,
              minWidth: 0,
              width: "100%",
              display: "flex",
              flexDirection: "column",
              gap: "24px",
            },
            children: [
              meta &&
                /* @__PURE__ */ (0, import_jsx_runtime182.jsx)(
                  Typography_default,
                  {
                    variant: "caption",
                    sx: { color: "text.secondary" },
                    children: meta,
                  },
                ),
              /* @__PURE__ */ (0, import_jsx_runtime182.jsx)(
                Typography_default,
                { variant: "h2", component: "h2", children: blog.title },
              ),
              /* @__PURE__ */ (0, import_jsx_runtime182.jsx)(Box_default, {
                sx: (theme2) => ({
                  height: { xs: 240, sm: 380, md: 440 },
                  borderRadius: "12px",
                  overflow: "hidden",
                  backgroundColor: theme2.palette.surfaces.placeholder,
                }),
                children: /* @__PURE__ */ (0, import_jsx_runtime182.jsx)(
                  Box_default,
                  {
                    component: "img",
                    src: blog.image,
                    alt: "",
                    sx: { width: "100%", height: "100%", objectFit: "cover" },
                  },
                ),
              }),
              blog.description &&
                /* @__PURE__ */ (0, import_jsx_runtime182.jsx)(
                  Typography_default,
                  {
                    component: "div",
                    variant: "body1",
                    children: /* @__PURE__ */ (0, import_jsx_runtime182.jsx)(
                      RichText_default,
                      {
                        text: blog.description,
                        allowStyling: {
                          newLine: true,
                          bold: true,
                          italic: true,
                          underline: true,
                        },
                      },
                    ),
                  },
                ),
            ],
          }),
          /* @__PURE__ */ (0, import_jsx_runtime182.jsx)(Box_default, {
            sx: { width: { xs: "100%", md: 360 }, flexShrink: 0 },
            children: /* @__PURE__ */ (0, import_jsx_runtime182.jsx)(
              LatestPosts,
              { excludeId: id, count: 5 },
            ),
          }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime182.jsx)(Box_default, {
        sx: (theme2) => ({
          pt: "24px",
          borderTop: `1px solid ${theme2.palette.surfaces.border}`,
        }),
        children: /* @__PURE__ */ (0, import_jsx_runtime182.jsx)(ShareActions, {
          title: blog.title,
        }),
      }),
    ],
  });
}

// src/page-content/pages/blog/BlogPost.tsx
var import_jsx_runtime183 = __toESM(require_jsx_runtime(), 1);
function BlogPostPage({ slug }) {
  const { lang } = useLanguage();
  const blogId = parseInt(slug, 10);
  const blog = getBlogItems(lang).find((item) => item.id === blogId);
  if (isNaN(blogId) || !blog) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime183.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime183.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime183.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime183.jsx)(HeaderSection, {
        title: blog.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime183.jsx)(Section, {
        id: "blog-post",
        paddingY: { xs: "64px", sm: "80px", md: "100px" },
        children: /* @__PURE__ */ (0, import_jsx_runtime183.jsx)(
          BlogPostSection,
          { id: blogId },
        ),
      }),
    ],
  });
}

// src/page-content/pages/careers/Careers.tsx
init_runtime2();

// src/page-content/components/section/careers/CareersSections.tsx
init_runtime2();

// src/page-content/components/section/careers/CareersList.tsx
init_runtime2();
init_static2();

// src/page-content/components/section/careers/ApplyButton.tsx
init_runtime2();
var import_jsx_runtime184 = __toESM(require_jsx_runtime(), 1);
function ApplyButton({ title, tone = "outline", fullWidth = false, sx }) {
  const { lang } = useLanguage();
  const careersTranslation = getCareersTranslation2(lang);
  const buttonTranslation = getButtonTranslation2(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime184.jsx)(ContactCtaButton, {
    subject: `${careersTranslation.applicationSubject}: ${title}`,
    label: buttonTranslation.applyNow,
    tone,
    fullWidth,
    sx,
  });
}

// src/page-content/components/section/careers/RequirementList.tsx
var import_jsx_runtime185 = __toESM(require_jsx_runtime(), 1);
function RequirementList({ requirements, label }) {
  if (requirements.length === 0) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime185.jsxs)(Box_default, {
    sx: { display: "flex", flexDirection: "column", gap: "10px" },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime185.jsx)(Typography_default, {
        variant: "caption",
        component: "p",
        sx: { color: "text.secondary" },
        children: label,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime185.jsx)(Box_default, {
        component: "ul",
        sx: {
          listStyle: "none",
          m: 0,
          p: 0,
          display: "flex",
          flexDirection: "column",
          gap: "10px",
        },
        children: requirements.map((requirement, index) =>
          /* @__PURE__ */ (0, import_jsx_runtime185.jsxs)(
            Box_default,
            {
              component: "li",
              sx: { display: "flex", alignItems: "flex-start", gap: "12px" },
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime185.jsx)(Box_default, {
                  "aria-hidden": true,
                  sx: (theme2) => ({
                    mt: "9px",
                    flexShrink: 0,
                    width: 8,
                    height: 8,
                    borderRadius: "50%",
                    backgroundColor: theme2.palette.surfaces.rose,
                    boxShadow: `inset 0 0 0 1px ${theme2.palette.primary.main}`,
                  }),
                }),
                /* @__PURE__ */ (0, import_jsx_runtime185.jsx)(
                  Typography_default,
                  { variant: "body1", children: requirement },
                ),
              ],
            },
            index,
          ),
        ),
      }),
    ],
  });
}

// src/page-content/components/section/careers/CareerPreviewCard.tsx
var import_jsx_runtime186 = __toESM(require_jsx_runtime(), 1);
function CareerPreviewCard({
  href,
  title,
  text,
  note,
  requirements,
  requirementsLabel,
}) {
  const truncatedText = stripRichText(truncateWordSafe(text, 180));
  return /* @__PURE__ */ (0, import_jsx_runtime186.jsxs)(Box_default, {
    sx: (theme2) => ({
      position: "relative",
      display: "flex",
      flexDirection: { xs: "column", md: "row" },
      alignItems: "flex-start",
      gap: { xs: "24px", md: "64px" },
      p: { xs: "32px 24px", md: "32px 40px" },
      borderRadius: "16px",
      border: `1px solid ${theme2.palette.surfaces.border}`,
      transition: theme2.transitions.create(["border-color"], {
        duration: theme2.transitions.duration.short,
      }),
      "&:hover": { borderColor: theme2.palette.text.primary },
      "&:hover .career-title": { color: theme2.palette.primary.main },
    }),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime186.jsxs)(Box_default, {
        sx: {
          flex: 1,
          minWidth: 0,
          width: "100%",
          display: "flex",
          flexDirection: "column",
          gap: "12px",
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime186.jsx)(Typography_default, {
            component: "a",
            href,
            variant: "h4",
            className: "career-title",
            sx: (theme2) => ({
              textDecoration: "none",
              color: "inherit",
              transition: theme2.transitions.create(["color"], {
                duration: theme2.transitions.duration.short,
              }),
              /* Kept alongside the parent's hover rule: the keyboard never hovers the card. */
              "&:hover, &:focus-visible": {
                color: theme2.palette.primary.main,
              },
              /* The whole card follows the name's link. */
              "&::after": { content: '""', position: "absolute", inset: 0 },
            }),
            children: title,
          }),
          truncatedText &&
            /* @__PURE__ */ (0, import_jsx_runtime186.jsx)(Typography_default, {
              variant: "body1",
              sx: { color: "text.secondary" },
              children: truncatedText,
            }),
          note &&
            /* @__PURE__ */ (0, import_jsx_runtime186.jsx)(Typography_default, {
              variant: "caption",
              component: "p",
              sx: { color: "text.secondary" },
              children: note,
            }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime186.jsxs)(Box_default, {
        sx: {
          width: { xs: "100%", md: 440 },
          flexShrink: 0,
          display: "flex",
          flexDirection: "column",
          gap: "20px",
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime186.jsx)(RequirementList, {
            requirements: requirements.slice(0, 3),
            label: requirementsLabel,
          }),
          /* @__PURE__ */ (0, import_jsx_runtime186.jsx)(Box_default, {
            sx: { position: "relative", zIndex: 1 },
            children: /* @__PURE__ */ (0, import_jsx_runtime186.jsx)(
              ApplyButton,
              { title, tone: "outline", fullWidth: true },
            ),
          }),
        ],
      }),
    ],
  });
}

// src/page-content/components/section/careers/CareersList.tsx
var import_jsx_runtime187 = __toESM(require_jsx_runtime(), 1);
function CareersList({ careers }) {
  const { lang } = useLanguage();
  const careersTranslation = getCareersTranslation2(lang);
  const careersPageSlug = getPageSlugByKey("careers");
  if (careers.length === 0) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime187.jsx)(Box_default, {
    sx: { display: "flex", flexDirection: "column", gap: "20px" },
    children: careers.map((career) =>
      /* @__PURE__ */ (0, import_jsx_runtime187.jsx)(
        CareerPreviewCard,
        {
          href: withBasePath(
            `/${careersPageSlug}/${getCareerSlugById(career.id)}`,
          ),
          title: career.title,
          text: career.text,
          note: career.note,
          requirements: career.requirements,
          requirementsLabel: careersTranslation.requirementsTitle,
        },
        career.id,
      ),
    ),
  });
}

// src/page-content/components/section/careers/CareersSections.tsx
var import_jsx_runtime188 = __toESM(require_jsx_runtime(), 1);
function CareersSection() {
  const { lang } = useLanguage();
  const careersSection = getCareersSection(lang);
  if (!careersSection) {
    return null;
  }
  const careersItems = getCareersItems(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime188.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "36px", md: "48px" },
    },
    children: [
      careersSection.text &&
        /* @__PURE__ */ (0, import_jsx_runtime188.jsx)(Typography_default, {
          variant: "body1",
          sx: { maxWidth: 640 },
          children: careersSection.text,
        }),
      /* @__PURE__ */ (0, import_jsx_runtime188.jsx)(CareersList, {
        careers: careersItems,
      }),
    ],
  });
}

// src/page-content/pages/careers/Careers.tsx
var import_jsx_runtime189 = __toESM(require_jsx_runtime(), 1);
function CareersPage() {
  const { lang } = useLanguage();
  const careersTranslation = getCareersTranslation2(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime189.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime189.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime189.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime189.jsx)(HeaderSection, {
        title: careersTranslation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime189.jsx)(Section, {
        id: "careers",
        children: /* @__PURE__ */ (0, import_jsx_runtime189.jsx)(
          CareersSection,
          {},
        ),
      }),
    ],
  });
}

// src/page-content/pages/careers/Career.tsx
init_runtime2();
init_runtime2();

// src/page-content/components/section/careers/CareerItemSection.tsx
init_runtime2();
init_static2();
var import_jsx_runtime190 = __toESM(require_jsx_runtime(), 1);
function CareerItemSection({ id }) {
  const router2 = useRouter();
  const { lang } = useLanguage();
  const careersTranslation = getCareersTranslation2(lang);
  const career = getCareersItems(lang).find((item) => item.id === id);
  if (!career) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime190.jsxs)(Box_default, {
    sx: {
      width: "100%",
      maxWidth: 880,
      display: "flex",
      flexDirection: "column",
      alignItems: "flex-start",
      gap: { xs: "24px", md: "28px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime190.jsx)(BackButton, {
        label: careersTranslation.backToCareers,
        onClick: () => router2.push(`/${getPageSlugByKey("careers")}`),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime190.jsx)(Typography_default, {
        variant: "h2",
        component: "h2",
        children: career.title,
      }),
      career.text &&
        /* @__PURE__ */ (0, import_jsx_runtime190.jsx)(Typography_default, {
          component: "div",
          variant: "body1",
          children: /* @__PURE__ */ (0, import_jsx_runtime190.jsx)(
            RichText_default,
            {
              text: career.text,
              allowStyling: {
                newLine: true,
                bold: true,
                italic: true,
                underline: true,
              },
            },
          ),
        }),
      /* @__PURE__ */ (0, import_jsx_runtime190.jsx)(RequirementList, {
        requirements: career.requirements,
        label: careersTranslation.requirementsTitle,
      }),
      career.note &&
        /* @__PURE__ */ (0, import_jsx_runtime190.jsx)(Typography_default, {
          variant: "caption",
          component: "p",
          sx: { color: "text.secondary" },
          children: career.note,
        }),
      /* @__PURE__ */ (0, import_jsx_runtime190.jsx)(ApplyButton, {
        title: career.title,
        tone: "primary",
        sx: { alignSelf: { xs: "stretch", sm: "flex-start" } },
      }),
      /* @__PURE__ */ (0, import_jsx_runtime190.jsx)(Box_default, {
        sx: (theme2) => ({
          width: "100%",
          pt: "24px",
          borderTop: `1px solid ${theme2.palette.surfaces.border}`,
        }),
        children: /* @__PURE__ */ (0, import_jsx_runtime190.jsx)(ShareActions, {
          title: career.title,
        }),
      }),
    ],
  });
}

// src/page-content/pages/careers/Career.tsx
var import_jsx_runtime191 = __toESM(require_jsx_runtime(), 1);
function CareerPage({ slug }) {
  const { lang } = useLanguage();
  const careerId = parseInt(slug, 10);
  const career = getCareersItems(lang).find((item) => item.id === careerId);
  if (isNaN(careerId) || !career) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime191.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime191.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime191.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime191.jsx)(HeaderSection, {
        id: "career",
        title: career.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime191.jsx)(Section, {
        id: "career-details",
        children: /* @__PURE__ */ (0, import_jsx_runtime191.jsx)(
          CareerItemSection,
          { id: career.id },
        ),
      }),
    ],
  });
}

// src/page-content/pages/events/Events.tsx
init_runtime2();
init_runtime2();

// src/page-content/components/section/events/EventsSection.tsx
var import_react43 = __toESM(require_react(), 1);
init_runtime2();
init_static2();

// src/page-content/components/section/events/EventPreviewCard.tsx
init_runtime2();
var import_jsx_runtime192 = __toESM(require_jsx_runtime(), 1);
function EventPreviewCard({ item, href, showApplyButton }) {
  const { lang } = useLanguage();
  const eventsTranslation = getEventsTranslation2(lang);
  const buttonTranslation = getButtonTranslation2(lang);
  const truncatedText = stripRichText(truncateWordSafe(item.text, 150));
  const formattedDate = formatEventDate(item.date, lang);
  const relativeDay = getRelativeEventDay(item.date);
  const relativeDayLabel =
    relativeDay === "today"
      ? eventsTranslation.today
      : relativeDay === "tomorrow"
        ? eventsTranslation.tomorrow
        : "";
  const dateLine = [formattedDate, item.location]
    .filter(Boolean)
    .join(" \xB7 ");
  const hasBadges = item.isCancelled || Boolean(relativeDayLabel);
  return /* @__PURE__ */ (0, import_jsx_runtime192.jsxs)(Box_default, {
    sx: (theme2) => ({
      position: "relative",
      display: "flex",
      flexDirection: "column",
      height: "100%",
      borderRadius: "12px",
      overflow: "hidden",
      backgroundColor: theme2.palette.surfaces.surface,
      opacity: item.isCancelled ? 0.86 : 1,
      "&:hover .event-image": { transform: "scale(1.04)" },
      "&:hover .event-title": { color: theme2.palette.primary.main },
    }),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime192.jsxs)(Box_default, {
        sx: (theme2) => ({
          position: "relative",
          height: { xs: 220, md: 240 },
          overflow: "hidden",
          backgroundColor: theme2.palette.surfaces.placeholder,
        }),
        children: [
          item.image &&
            /* @__PURE__ */ (0, import_jsx_runtime192.jsx)(Box_default, {
              component: "img",
              className: "event-image",
              src: item.image,
              alt: "",
              loading: "lazy",
              sx: (theme2) => ({
                position: "absolute",
                inset: 0,
                width: "100%",
                height: "100%",
                objectFit: "cover",
                transition: theme2.transitions.create("transform"),
              }),
            }),
          hasBadges &&
            /* @__PURE__ */ (0, import_jsx_runtime192.jsxs)(Box_default, {
              sx: {
                position: "absolute",
                top: 16,
                left: 16,
                right: 16,
                display: "flex",
                flexWrap: "wrap",
                gap: "6px",
              },
              children: [
                item.isCancelled &&
                  /* @__PURE__ */ (0, import_jsx_runtime192.jsx)(Tag, {
                    label: eventsTranslation.cancelled,
                    tone: "dark",
                  }),
                relativeDayLabel &&
                  /* @__PURE__ */ (0, import_jsx_runtime192.jsx)(Tag, {
                    label: relativeDayLabel,
                    tone: relativeDay === "today" ? "mint" : "rose",
                  }),
              ],
            }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime192.jsxs)(Box_default, {
        sx: {
          flexGrow: 1,
          display: "flex",
          flexDirection: "column",
          gap: "8px",
          p: "18px 20px 22px",
        },
        children: [
          dateLine &&
            /* @__PURE__ */ (0, import_jsx_runtime192.jsx)(Typography_default, {
              variant: "caption",
              component: "p",
              sx: { color: "text.secondary" },
              children: dateLine,
            }),
          /* @__PURE__ */ (0, import_jsx_runtime192.jsx)(Typography_default, {
            component: "a",
            href,
            variant: "h5",
            className: "event-title",
            sx: (theme2) => ({
              textDecoration: "none",
              color: "inherit",
              transition: theme2.transitions.create(["color"], {
                duration: theme2.transitions.duration.short,
              }),
              /* The whole card follows the name's link. */
              "&::after": { content: '""', position: "absolute", inset: 0 },
            }),
            children: item.title,
          }),
          truncatedText &&
            /* @__PURE__ */ (0, import_jsx_runtime192.jsx)(Typography_default, {
              variant: "body1",
              sx: { color: "text.secondary" },
              children: truncatedText,
            }),
          item.category &&
            /* @__PURE__ */ (0, import_jsx_runtime192.jsx)(Typography_default, {
              variant: "caption",
              component: "p",
              sx: { color: "text.secondary" },
              children: item.category,
            }),
          showApplyButton &&
            !item.isCancelled &&
            /* @__PURE__ */ (0, import_jsx_runtime192.jsx)(Box_default, {
              sx: { position: "relative", zIndex: 1, mt: "auto", pt: "8px" },
              children: /* @__PURE__ */ (0, import_jsx_runtime192.jsx)(
                ContactCtaButton,
                {
                  subject: `${eventsTranslation.applicationSubject}: ${item.title}`,
                  label: buttonTranslation.applyNow,
                  tone: "outline",
                  fullWidth: true,
                },
              ),
            }),
        ],
      }),
    ],
  });
}

// src/page-content/components/section/events/EventsSection.tsx
var import_jsx_runtime193 = __toESM(require_jsx_runtime(), 1);
var PER_PAGE2 = 6;
function EventsSectionInner() {
  const { lang } = useLanguage();
  const eventsSection = getEventsSection(lang);
  const eventItems = getEventItems(lang);
  const eventsTranslation = getEventsTranslation2(lang);
  const buttonTranslation = getButtonTranslation2(lang);
  const {
    searchInput,
    setSearchInput,
    searchQuery,
    selectedCategoryId,
    setSelectedCategoryId,
    sortOrder,
    setSortOrder,
    page,
    setPage,
  } = useListFilters();
  const filteredAndSortedEvents = (0, import_react43.useMemo)(() => {
    const normalizedSearchQuery = normalizeSearchValue(searchQuery);
    return eventItems
      .filter((event) => {
        if (selectedCategoryId && event.categoryId !== selectedCategoryId) {
          return false;
        }
        if (!normalizedSearchQuery) {
          return true;
        }
        const searchableValue = normalizeSearchValue(
          [
            event.title,
            stripRichText(event.text),
            event.location,
            event.category,
            event.date,
            formatEventDate(event.date, lang),
          ].join(" "),
        );
        return searchableValue.includes(normalizedSearchQuery);
      })
      .sort((firstEvent, secondEvent) => {
        const firstTimestamp = getEventDateTimestamp(firstEvent.date);
        const secondTimestamp = getEventDateTimestamp(secondEvent.date);
        if (firstTimestamp === null && secondTimestamp === null) {
          return firstEvent.id - secondEvent.id;
        }
        if (firstTimestamp === null) {
          return 1;
        }
        if (secondTimestamp === null) {
          return -1;
        }
        const dateDifference = firstTimestamp - secondTimestamp;
        if (dateDifference === 0) {
          return firstEvent.id - secondEvent.id;
        }
        return sortOrder === "ascending" ? dateDifference : -dateDifference;
      });
  }, [eventItems, searchQuery, selectedCategoryId, sortOrder, lang]);
  const { pageCount, currentPage, pageItems } = paginate(
    filteredAndSortedEvents,
    page,
    PER_PAGE2,
  );
  if (!eventsSection) {
    return null;
  }
  const categories = eventsSection.categories;
  const showApplyButton = Boolean(eventsSection.showApplyButton);
  const cards = pageItems.map((event) =>
    /* @__PURE__ */ (0, import_jsx_runtime193.jsx)(
      EventPreviewCard,
      {
        item: event,
        showApplyButton,
        href: withBasePath(
          `/${getPageSlugByKey("events")}/${getEventSlugById(event.id)}`,
        ),
      },
      event.id,
    ),
  );
  return /* @__PURE__ */ (0, import_jsx_runtime193.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "32px", md: "40px" },
    },
    children: [
      eventsSection.text &&
        /* @__PURE__ */ (0, import_jsx_runtime193.jsx)(Typography_default, {
          variant: "body1",
          sx: { maxWidth: 640 },
          children: eventsSection.text,
        }),
      /* @__PURE__ */ (0, import_jsx_runtime193.jsxs)(Box_default, {
        sx: { display: "flex", flexDirection: "column", gap: "14px" },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime193.jsx)(ListToolbar, {
            searchValue: searchInput,
            onSearchChange: setSearchInput,
            searchLabel: eventsTranslation.search,
            sortValue: sortOrder,
            onSortChange: (value) => setSortOrder(value),
            sortLabel: eventsTranslation.sortByDate,
            sortOptions: [
              { value: "ascending", label: eventsTranslation.dateAscending },
              { value: "descending", label: eventsTranslation.dateDescending },
            ],
            sortWidth: 257,
          }),
          categories.length > 0 &&
            /* @__PURE__ */ (0, import_jsx_runtime193.jsx)(FilterChips, {
              ariaLabel: eventsTranslation.category,
              options: [
                { value: "", label: buttonTranslation.all },
                ...categories.map((category) => ({
                  value: category.id,
                  label: category.name,
                })),
              ],
              value: selectedCategoryId,
              onChange: setSelectedCategoryId,
            }),
        ],
      }),
      cards.length > 0
        ? /* @__PURE__ */ (0, import_jsx_runtime193.jsxs)(
            import_jsx_runtime193.Fragment,
            {
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime193.jsx)(Box_default, {
                  sx: {
                    display: { xs: "none", md: "grid" },
                    gridTemplateColumns: "repeat(3, minmax(0, 1fr))",
                    gap: "24px",
                    alignItems: "stretch",
                  },
                  children: cards,
                }),
                /* @__PURE__ */ (0, import_jsx_runtime193.jsx)(Box_default, {
                  sx: { display: { xs: "block", md: "none" } },
                  children: /* @__PURE__ */ (0, import_jsx_runtime193.jsx)(
                    Carousel,
                    {
                      items: cards,
                      slideWidth: { xs: "100%", sm: "calc(50% - 10px)" },
                      gap: 20,
                      controlsGap: { xs: 24 },
                      ariaLabel: eventsTranslation.title,
                    },
                  ),
                }),
              ],
            },
          )
        : /* @__PURE__ */ (0, import_jsx_runtime193.jsx)(Typography_default, {
            variant: "body1",
            sx: { color: "text.secondary", py: "32px" },
            children: eventsTranslation.noResults,
          }),
      pageCount > 1 &&
        /* @__PURE__ */ (0, import_jsx_runtime193.jsx)(Box_default, {
          sx: {
            display: "flex",
            justifyContent: { xs: "center", md: "flex-start" },
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime193.jsx)(
            PaginationControls,
            {
              page: currentPage,
              pageCount,
              onChange: setPage,
            },
          ),
        }),
    ],
  });
}
function EventsSection() {
  return /* @__PURE__ */ (0, import_jsx_runtime193.jsx)(
    import_react43.Suspense,
    {
      fallback: null,
      children: /* @__PURE__ */ (0, import_jsx_runtime193.jsx)(
        EventsSectionInner,
        {},
      ),
    },
  );
}

// src/page-content/pages/events/Events.tsx
var import_jsx_runtime194 = __toESM(require_jsx_runtime(), 1);
function EventsPage() {
  const { lang } = useLanguage();
  const eventsSection = getEventsSection(lang);
  const eventsTranslation = getEventsTranslation2(lang);
  if (!eventsSection) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime194.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime194.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime194.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime194.jsx)(HeaderSection, {
        id: "events",
        title: eventsTranslation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime194.jsx)(Section, {
        id: "events-list",
        children: /* @__PURE__ */ (0, import_jsx_runtime194.jsx)(
          EventsSection,
          {},
        ),
      }),
    ],
  });
}

// src/page-content/pages/events/Event.tsx
init_runtime2();
init_runtime2();

// src/page-content/components/section/events/EventItemSection.tsx
init_runtime2();
init_static2();
var import_jsx_runtime195 = __toESM(require_jsx_runtime(), 1);
function EventItemSection({ id }) {
  const { lang } = useLanguage();
  const eventsTranslation = getEventsTranslation2(lang);
  const buttonTranslation = getButtonTranslation2(lang);
  const handleBackToEvents = useBackToList(`/${getPageSlugByKey("events")}`);
  const event = getEventItems(lang).find((item) => item.id === id);
  const eventsSection = getEventsSection(lang);
  if (!event) {
    return null;
  }
  const formattedDate = formatEventDate(event.date, lang, {
    includeWeekday: true,
  });
  const relativeDay = getRelativeEventDay(event.date);
  const relativeDayLabel =
    relativeDay === "today"
      ? eventsTranslation.today
      : relativeDay === "tomorrow"
        ? eventsTranslation.tomorrow
        : "";
  const dateLine = [formattedDate, event.location]
    .filter(Boolean)
    .join(" \xB7 ");
  const hasBadges = event.isCancelled || Boolean(relativeDayLabel);
  const showApplyButton =
    Boolean(eventsSection?.showApplyButton) && !event.isCancelled;
  return /* @__PURE__ */ (0, import_jsx_runtime195.jsxs)(Box_default, {
    sx: {
      width: "100%",
      maxWidth: 900,
      display: "flex",
      flexDirection: "column",
      alignItems: "flex-start",
      gap: "24px",
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime195.jsx)(BackButton, {
        label: eventsTranslation.backToEvents,
        onClick: handleBackToEvents,
      }),
      event.image &&
        /* @__PURE__ */ (0, import_jsx_runtime195.jsx)(Box_default, {
          component: "img",
          src: event.image,
          alt: "",
          sx: (theme2) => ({
            width: "100%",
            height: { xs: 240, sm: 360, md: 440 },
            objectFit: "cover",
            borderRadius: "16px",
            backgroundColor: theme2.palette.surfaces.placeholder,
          }),
        }),
      hasBadges &&
        /* @__PURE__ */ (0, import_jsx_runtime195.jsxs)(Box_default, {
          sx: { display: "flex", flexWrap: "wrap", gap: "8px" },
          children: [
            event.isCancelled &&
              /* @__PURE__ */ (0, import_jsx_runtime195.jsx)(Tag, {
                label: eventsTranslation.cancelled,
                tone: "dark",
              }),
            relativeDayLabel &&
              /* @__PURE__ */ (0, import_jsx_runtime195.jsx)(Tag, {
                label: relativeDayLabel,
                tone: relativeDay === "today" ? "mint" : "rose",
              }),
          ],
        }),
      dateLine &&
        /* @__PURE__ */ (0, import_jsx_runtime195.jsx)(Typography_default, {
          variant: "caption",
          component: "p",
          sx: { color: "text.secondary" },
          children: dateLine,
        }),
      /* @__PURE__ */ (0, import_jsx_runtime195.jsx)(Typography_default, {
        variant: "h2",
        component: "h2",
        children: event.title,
      }),
      event.text &&
        /* @__PURE__ */ (0, import_jsx_runtime195.jsx)(Typography_default, {
          component: "div",
          variant: "body1",
          children: /* @__PURE__ */ (0, import_jsx_runtime195.jsx)(
            RichText_default,
            {
              text: event.text,
              allowStyling: {
                newLine: true,
                bold: true,
                italic: true,
                underline: true,
              },
            },
          ),
        }),
      event.category &&
        /* @__PURE__ */ (0, import_jsx_runtime195.jsx)(Typography_default, {
          variant: "caption",
          component: "p",
          sx: { color: "text.secondary" },
          children: event.category,
        }),
      showApplyButton &&
        /* @__PURE__ */ (0, import_jsx_runtime195.jsx)(ContactCtaButton, {
          subject: `${eventsTranslation.applicationSubject}: ${event.title}`,
          label: buttonTranslation.applyNow,
          tone: "primary",
          sx: { alignSelf: { xs: "stretch", sm: "flex-start" } },
        }),
      /* @__PURE__ */ (0, import_jsx_runtime195.jsx)(Box_default, {
        sx: (theme2) => ({
          width: "100%",
          pt: "24px",
          borderTop: `1px solid ${theme2.palette.surfaces.border}`,
        }),
        children: /* @__PURE__ */ (0, import_jsx_runtime195.jsx)(ShareActions, {
          title: event.title,
        }),
      }),
    ],
  });
}

// src/page-content/pages/events/Event.tsx
var import_jsx_runtime196 = __toESM(require_jsx_runtime(), 1);
function EventPage({ slug }) {
  const { lang } = useLanguage();
  const eventId = parseInt(slug, 10);
  const event = getEventItems(lang).find((item) => item.id === eventId);
  if (isNaN(eventId) || !event) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime196.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime196.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime196.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime196.jsx)(HeaderSection, {
        id: "event",
        title: event.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime196.jsx)(Section, {
        id: "event-details",
        children: /* @__PURE__ */ (0, import_jsx_runtime196.jsx)(
          EventItemSection,
          { id: event.id },
        ),
      }),
    ],
  });
}

// src/page-content/pages/gallery/Gallery.tsx
init_runtime2();

// src/page-content/components/section/gallery/GallerySection.tsx
init_runtime2();
var import_jsx_runtime197 = __toESM(require_jsx_runtime(), 1);
function GallerySection() {
  const galleryItems = getGalleryItems();
  const { page, setPage, pageCount, paginatedItems } = usePagination(
    galleryItems,
    6,
  );
  return /* @__PURE__ */ (0, import_jsx_runtime197.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "40px", md: "56px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime197.jsx)(CustomGallery, {
        items: galleryItems,
        currentPageItems: paginatedItems,
      }),
      pageCount > 1 &&
        /* @__PURE__ */ (0, import_jsx_runtime197.jsx)(Box_default, {
          sx: {
            display: "flex",
            justifyContent: { xs: "center", md: "flex-end" },
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime197.jsx)(
            PaginationControls,
            {
              page,
              pageCount,
              onChange: setPage,
            },
          ),
        }),
    ],
  });
}

// src/page-content/pages/gallery/Gallery.tsx
var import_jsx_runtime198 = __toESM(require_jsx_runtime(), 1);
function GalleryPage() {
  const { lang } = useLanguage();
  const galleryTranslation = getGalleryTranslation2(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime198.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime198.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime198.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime198.jsx)(HeaderSection, {
        title: galleryTranslation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime198.jsx)(Section, {
        id: "gallery",
        children: /* @__PURE__ */ (0, import_jsx_runtime198.jsx)(
          GallerySection,
          {},
        ),
      }),
    ],
  });
}

// src/page-content/pages/legal/Legal.tsx
init_runtime2();

// src/page-content/components/section/legal/LegalSection.tsx
init_runtime2();
var import_jsx_runtime199 = __toESM(require_jsx_runtime(), 1);
function LegalSection() {
  const { lang } = useLanguage();
  const legalSection = getLegalSection(lang);
  const legalTranslation = getLegalTranslation2(lang);
  const documents = [];
  if (legalSection?.termsOfService) {
    documents.push({
      key: "termsOfService",
      title: legalTranslation.termsOfService,
      file: legalSection.termsOfService,
    });
  }
  if (legalSection?.privacyPolicy) {
    documents.push({
      key: "privacyPolicy",
      title: legalTranslation.privacyPolicy,
      file: legalSection.privacyPolicy,
    });
  }
  if (documents.length === 0) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime199.jsx)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "32px", md: "48px" },
    },
    children: /* @__PURE__ */ (0, import_jsx_runtime199.jsx)(Box_default, {
      sx: {
        display: "grid",
        gridTemplateColumns: { xs: "1fr", md: "repeat(2, minmax(0, 1fr))" },
        gap: { xs: "20px", md: "24px" },
        alignItems: "stretch",
      },
      children: documents.map((legalDocument) =>
        /* @__PURE__ */ (0, import_jsx_runtime199.jsxs)(
          Box_default,
          {
            component: "a",
            href: legalDocument.file,
            target: "_blank",
            rel: "noopener noreferrer",
            "aria-label": `${legalDocument.title}: ${legalTranslation.openDocument}`,
            sx: (theme2) => ({
              display: "flex",
              flexDirection: "column",
              gap: { xs: "16px", md: "18px" },
              p: { xs: "24px", md: "32px" },
              borderRadius: "16px",
              textDecoration: "none",
              color: "inherit",
              backgroundColor: theme2.palette.surfaces.surface,
              boxShadow: "inset 0 0 0 1px transparent",
              transition: theme2.transitions.create(["box-shadow"], {
                duration: theme2.transitions.duration.short,
              }),
              "&:hover, &:focus-visible": {
                boxShadow: `inset 0 0 0 1px ${theme2.palette.text.primary}`,
              },
              "&:hover .legal-open, &:focus-visible .legal-open": {
                color: theme2.palette.primary.main,
              },
            }),
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime199.jsx)(Box_default, {
                "aria-hidden": true,
                sx: (theme2) => ({
                  width: { xs: 44, md: 48 },
                  height: { xs: 44, md: 48 },
                  borderRadius: "50%",
                  display: "grid",
                  placeItems: "center",
                  backgroundColor: theme2.palette.surfaces.rose,
                  color: theme2.palette.text.primary,
                }),
                children: /* @__PURE__ */ (0, import_jsx_runtime199.jsx)(
                  DocumentIcon,
                  {},
                ),
              }),
              /* @__PURE__ */ (0, import_jsx_runtime199.jsx)(
                Typography_default,
                {
                  variant: "h5",
                  component: "h3",
                  children: legalDocument.title,
                },
              ),
              /* @__PURE__ */ (0, import_jsx_runtime199.jsxs)(Box_default, {
                className: "legal-open",
                sx: (theme2) => ({
                  ...theme2.typography.subtitle1,
                  mt: "auto",
                  display: "inline-flex",
                  alignItems: "center",
                  gap: "8px",
                  color: theme2.palette.text.primary,
                  transition: theme2.transitions.create(["color"], {
                    duration: theme2.transitions.duration.short,
                  }),
                }),
                children: [
                  legalTranslation.openDocument,
                  /* @__PURE__ */ (0, import_jsx_runtime199.jsx)(
                    ArrowOutwardIcon,
                    {},
                  ),
                ],
              }),
            ],
          },
          legalDocument.key,
        ),
      ),
    }),
  });
}

// src/page-content/pages/legal/Legal.tsx
var import_jsx_runtime200 = __toESM(require_jsx_runtime(), 1);
function LegalPage() {
  const { lang } = useLanguage();
  const legalTranslation = getLegalTranslation2(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime200.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime200.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime200.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime200.jsx)(HeaderSection, {
        id: "legal",
        title: legalTranslation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime200.jsx)(Section, {
        id: "legal-documents",
        children: /* @__PURE__ */ (0, import_jsx_runtime200.jsx)(
          LegalSection,
          {},
        ),
      }),
    ],
  });
}

// src/page-content/pages/videos/Videos.tsx
init_runtime2();

// src/page-content/components/section/video/VideoSection.tsx
init_runtime2();

// src/page-content/components/section/video/VideoThumbnail.tsx
var import_react47 = __toESM(require_react(), 1);
var import_jsx_runtime201 = __toESM(require_jsx_runtime(), 1);
function VideoThumbnail({ videoUrl }) {
  const [thumb, setThumb] = (0, import_react47.useState)(null);
  (0, import_react47.useEffect)(() => {
    let mounted = true;
    (async () => {
      const thumbnail = await getVideoThumbnail(videoUrl);
      if (mounted) setThumb(thumbnail);
    })();
    return () => {
      mounted = false;
    };
  }, [videoUrl]);
  return /* @__PURE__ */ (0, import_jsx_runtime201.jsxs)(Box_default, {
    component: "a",
    href: videoUrl,
    target: "_blank",
    rel: "noopener noreferrer",
    "aria-label": videoUrl,
    sx: (theme2) => ({
      position: "relative",
      display: "block",
      height: { xs: 191, sm: 188, md: 330 },
      borderRadius: "16px",
      overflow: "hidden",
      backgroundColor: theme2.palette.surfaces.placeholder,
      "&:hover .play, &:focus-visible .play": {
        transform: "translate(-50%, -50%) scale(1.08)",
      },
      "&:hover img, &:focus-visible img": { transform: "scale(1.04)" },
    }),
    children: [
      thumb &&
        /* @__PURE__ */ (0, import_jsx_runtime201.jsx)(Box_default, {
          component: "img",
          src: thumb,
          alt: "",
          loading: "lazy",
          sx: (theme2) => ({
            position: "absolute",
            inset: 0,
            width: "100%",
            height: "100%",
            objectFit: "cover",
            transition: theme2.transitions.create("transform"),
          }),
        }),
      /* @__PURE__ */ (0, import_jsx_runtime201.jsx)(Box_default, {
        "aria-hidden": true,
        sx: (theme2) => ({
          position: "absolute",
          inset: 0,
          backgroundColor: theme2.palette.surfaces.scrim,
        }),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime201.jsx)(Box_default, {
        className: "play",
        "aria-hidden": true,
        sx: (theme2) => ({
          position: "absolute",
          top: "50%",
          left: "50%",
          transform: "translate(-50%, -50%)",
          width: 72,
          height: 72,
          borderRadius: "50%",
          backgroundColor: theme2.palette.surfaces.onImage,
          color: theme2.palette.text.primary,
          display: "grid",
          placeItems: "center",
          // The triangle sits on its own centre of mass, which is left of the circle's.
          pl: "5px",
          transition: theme2.transitions.create("transform"),
        }),
        children: /* @__PURE__ */ (0, import_jsx_runtime201.jsx)(PlayIcon, {}),
      }),
    ],
  });
}

// src/page-content/components/section/video/VideoSection.tsx
var import_jsx_runtime202 = __toESM(require_jsx_runtime(), 1);
function VideoSection() {
  const { lang } = useLanguage();
  const videosTranslation = getVideosTranslation2(lang);
  const videoSection = getVideoSection();
  if (!videoSection || videoSection.items.length === 0) return null;
  const tiles = videoSection.items.map((url, index) =>
    /* @__PURE__ */ (0, import_jsx_runtime202.jsx)(
      VideoThumbnail,
      { videoUrl: url },
      index,
    ),
  );
  return /* @__PURE__ */ (0, import_jsx_runtime202.jsxs)(Box_default, {
    sx: { display: "flex", flexDirection: "column", gap: "40px" },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime202.jsx)(Box_default, {
        sx: {
          display: { xs: "none", md: "grid" },
          gridTemplateColumns: "repeat(2, 1fr)",
          gap: "32px",
        },
        children: tiles,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime202.jsx)(Box_default, {
        sx: { display: { xs: "block", md: "none" } },
        children: /* @__PURE__ */ (0, import_jsx_runtime202.jsx)(Carousel, {
          items: tiles,
          slideWidth: { xs: "100%", sm: "calc(50% - 10px)" },
          gap: 20,
          controlsGap: { xs: 24 },
          ariaLabel: videosTranslation.title,
        }),
      }),
    ],
  });
}

// src/page-content/pages/videos/Videos.tsx
var import_jsx_runtime203 = __toESM(require_jsx_runtime(), 1);
function VideosPage() {
  const { lang } = useLanguage();
  const videosTranslation = getVideosTranslation2(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime203.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime203.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime203.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime203.jsx)(HeaderSection, {
        title: videosTranslation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime203.jsx)(Section, {
        id: "videos",
        children: /* @__PURE__ */ (0, import_jsx_runtime203.jsx)(
          VideoSection,
          {},
        ),
      }),
    ],
  });
}

// src/page-content/pages/catalogues/Catalogues.tsx
init_runtime2();

// src/page-content/components/section/catalogue/CatalogueSection.tsx
init_runtime2();
var import_jsx_runtime204 = __toESM(require_jsx_runtime(), 1);
function fileName(path3) {
  return path3.split("/").pop() || path3;
}
function CatalogueSection() {
  const catalogueSection = getCatalogueSection();
  if (!catalogueSection || catalogueSection.items.length === 0) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime204.jsx)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "32px", md: "40px" },
    },
    children: /* @__PURE__ */ (0, import_jsx_runtime204.jsx)(Box_default, {
      sx: (theme2) => ({
        borderTop: `1px solid ${theme2.palette.surfaces.border}`,
      }),
      children: catalogueSection.items.map((item, index) =>
        /* @__PURE__ */ (0, import_jsx_runtime204.jsxs)(
          Box_default,
          {
            sx: (theme2) => ({
              display: "flex",
              flexDirection: { xs: "column", sm: "row" },
              alignItems: { xs: "flex-start", sm: "center" },
              justifyContent: "space-between",
              gap: { xs: "14px", sm: "24px" },
              py: { xs: "20px", sm: "22px" },
              borderBottom: `1px solid ${theme2.palette.surfaces.border}`,
            }),
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime204.jsxs)(Box_default, {
                sx: {
                  display: "flex",
                  flexDirection: { xs: "column", sm: "row" },
                  alignItems: { xs: "flex-start", sm: "center" },
                  gap: { xs: "14px", sm: "18px" },
                  minWidth: 0,
                },
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime204.jsx)(Box_default, {
                    "aria-hidden": true,
                    sx: (theme2) => ({
                      flexShrink: 0,
                      width: { xs: 44, sm: 48 },
                      height: { xs: 44, sm: 48 },
                      borderRadius: "50%",
                      display: "grid",
                      placeItems: "center",
                      backgroundColor: theme2.palette.surfaces.rose,
                      color: theme2.palette.text.primary,
                    }),
                    children: /* @__PURE__ */ (0, import_jsx_runtime204.jsx)(
                      DocumentIcon,
                      {},
                    ),
                  }),
                  /* @__PURE__ */ (0, import_jsx_runtime204.jsx)(
                    Typography_default,
                    { variant: "h5", component: "h3", children: item.name },
                  ),
                ],
              }),
              /* @__PURE__ */ (0, import_jsx_runtime204.jsxs)(Box_default, {
                component: "a",
                href: item.file,
                target: "_blank",
                rel: "noopener noreferrer",
                "aria-label": `${item.name} \u2014 ${fileName(item.file)}`,
                sx: (theme2) => ({
                  ...theme2.typography.subtitle1,
                  flexShrink: 0,
                  width: { xs: "100%", sm: "auto" },
                  display: "inline-flex",
                  alignItems: "center",
                  justifyContent: "space-between",
                  gap: "10px",
                  px: "20px",
                  py: "12px",
                  borderRadius: "999px",
                  color: theme2.palette.text.primary,
                  textDecoration: "none",
                  boxShadow: `inset 0 0 0 1px ${theme2.palette.surfaces.border}`,
                  transition: theme2.transitions.create(
                    ["box-shadow", "background-color"],
                    {
                      duration: theme2.transitions.duration.short,
                    },
                  ),
                  "&:hover, &:focus-visible": {
                    boxShadow: `inset 0 0 0 1px ${theme2.palette.text.primary}`,
                    backgroundColor: theme2.palette.surfaces.bgAlt,
                  },
                }),
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime204.jsx)(Box_default, {
                    component: "span",
                    sx: { wordBreak: "break-all" },
                    children: fileName(item.file),
                  }),
                  /* @__PURE__ */ (0, import_jsx_runtime204.jsx)(
                    DownloadIcon,
                    {},
                  ),
                ],
              }),
            ],
          },
          index,
        ),
      ),
    }),
  });
}

// src/page-content/pages/catalogues/Catalogues.tsx
var import_jsx_runtime205 = __toESM(require_jsx_runtime(), 1);
function CataloguesPage() {
  const { lang } = useLanguage();
  const cataloguesTranslation = getCataloguesTranslation2(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime205.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime205.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime205.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime205.jsx)(HeaderSection, {
        title: cataloguesTranslation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime205.jsx)(Section, {
        id: "catalogues",
        children: /* @__PURE__ */ (0, import_jsx_runtime205.jsx)(
          CatalogueSection,
          {},
        ),
      }),
    ],
  });
}

// src/page-content/pages/schedule/Schedule.tsx
init_runtime2();
init_runtime2();

// src/page-content/components/section/schedule/ScheduleSection.tsx
var import_react48 = __toESM(require_react(), 1);
init_runtime2();

// src/page-content/components/section/schedule/ScheduleTableView.tsx
var import_jsx_runtime206 = __toESM(require_jsx_runtime(), 1);
function ScheduleTableView({ title, text, categoryLabel, rows }) {
  const columnCount = rows.reduce(
    (count, row) => Math.max(count, row.length),
    0,
  );
  if (columnCount === 0) return null;
  const padded = rows.map((row) => [
    ...row,
    ...Array(columnCount - row.length).fill(""),
  ]);
  const headerCells = padded[0];
  const bodyRows = padded.slice(1);
  const cellSx = {
    px: { xs: "12px", md: "24px" },
    py: { xs: "12px", md: "16px" },
    textAlign: "left",
    verticalAlign: "top",
  };
  return /* @__PURE__ */ (0, import_jsx_runtime206.jsxs)(Box_default, {
    sx: { display: "flex", flexDirection: "column", gap: "10px" },
    children: [
      title &&
        /* @__PURE__ */ (0, import_jsx_runtime206.jsx)(Typography_default, {
          variant: "h5",
          component: "h3",
          children: title,
        }),
      text &&
        /* @__PURE__ */ (0, import_jsx_runtime206.jsx)(Typography_default, {
          variant: "body1",
          sx: { color: "text.secondary" },
          children: text,
        }),
      categoryLabel &&
        /* @__PURE__ */ (0, import_jsx_runtime206.jsx)(Typography_default, {
          variant: "caption",
          component: "p",
          sx: { color: "text.secondary" },
          children: categoryLabel,
        }),
      /* @__PURE__ */ (0, import_jsx_runtime206.jsx)(Box_default, {
        sx: (theme2) => ({
          mt: "6px",
          overflowX: "auto",
          borderRadius: "12px",
          border: `1px solid ${theme2.palette.surfaces.border}`,
        }),
        children: /* @__PURE__ */ (0, import_jsx_runtime206.jsxs)(Box_default, {
          component: "table",
          sx: {
            width: "100%",
            minWidth: { xs: 560, md: "auto" },
            borderCollapse: "collapse",
          },
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime206.jsx)(Box_default, {
              component: "thead",
              children: /* @__PURE__ */ (0, import_jsx_runtime206.jsx)(
                Box_default,
                {
                  component: "tr",
                  children: headerCells.map((cell, index) =>
                    /* @__PURE__ */ (0, import_jsx_runtime206.jsx)(
                      Box_default,
                      {
                        component: "th",
                        scope: "col",
                        sx: (theme2) => ({
                          ...theme2.typography.subtitle1,
                          ...cellSx,
                          color: theme2.palette.text.primary,
                          backgroundColor: theme2.palette.surfaces.bgAlt,
                          borderBottom: `1px solid ${theme2.palette.surfaces.border}`,
                        }),
                        children: cell,
                      },
                      index,
                    ),
                  ),
                },
              ),
            }),
            /* @__PURE__ */ (0, import_jsx_runtime206.jsx)(Box_default, {
              component: "tbody",
              children: bodyRows.map((row, rowIndex) =>
                /* @__PURE__ */ (0, import_jsx_runtime206.jsx)(
                  Box_default,
                  {
                    component: "tr",
                    sx: (theme2) => ({
                      ...(rowIndex < bodyRows.length - 1 && {
                        "& > td": {
                          borderBottom: `1px solid ${theme2.palette.surfaces.border}`,
                        },
                      }),
                    }),
                    children: row.map((cell, cellIndex) =>
                      /* @__PURE__ */ (0, import_jsx_runtime206.jsx)(
                        Box_default,
                        {
                          component: "td",
                          sx: (theme2) => ({
                            ...theme2.typography.body1,
                            ...cellSx,
                            color: theme2.palette.text.secondary,
                          }),
                          children: cell,
                        },
                        cellIndex,
                      ),
                    ),
                  },
                  rowIndex,
                ),
              ),
            }),
          ],
        }),
      }),
    ],
  });
}

// src/page-content/components/section/schedule/ScheduleSection.tsx
var import_jsx_runtime207 = __toESM(require_jsx_runtime(), 1);
function ScheduleSection() {
  const { lang } = useLanguage();
  const scheduleSection = getScheduleSection(lang);
  const scheduleTables = getScheduleTables(lang);
  const allLabel = getButtonTranslation2(lang).all;
  const [selectedCategoryId, setSelectedCategoryId] = (0,
  import_react48.useState)("");
  const categories = scheduleSection?.categories ?? [];
  const tables = (0, import_react48.useMemo)(
    () =>
      selectedCategoryId
        ? scheduleTables.filter(
            (table) => table.categoryId === selectedCategoryId,
          )
        : scheduleTables,
    [scheduleTables, selectedCategoryId],
  );
  if (!scheduleSection) {
    return null;
  }
  const categoryNameById = (categoryId) =>
    categories.find((category) => category.id === categoryId)?.name ?? "";
  return /* @__PURE__ */ (0, import_jsx_runtime207.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "36px", md: "44px" },
    },
    children: [
      scheduleSection.text &&
        /* @__PURE__ */ (0, import_jsx_runtime207.jsx)(Typography_default, {
          variant: "body1",
          sx: { maxWidth: 640 },
          children: scheduleSection.text,
        }),
      categories.length > 0 &&
        /* @__PURE__ */ (0, import_jsx_runtime207.jsx)(FilterChips, {
          ariaLabel: allLabel,
          options: [
            { value: "", label: allLabel },
            ...categories.map((category) => ({
              value: category.id,
              label: category.name,
            })),
          ],
          value: selectedCategoryId,
          onChange: setSelectedCategoryId,
        }),
      /* @__PURE__ */ (0, import_jsx_runtime207.jsx)(Box_default, {
        sx: {
          display: "flex",
          flexDirection: "column",
          gap: { xs: "40px", md: "56px" },
        },
        children: tables.map((table) =>
          /* @__PURE__ */ (0, import_jsx_runtime207.jsx)(
            ScheduleTableView,
            {
              title: table.title,
              text: table.text,
              categoryLabel:
                categoryNameById(table.categoryId) === table.title
                  ? ""
                  : categoryNameById(table.categoryId),
              rows: table.rows,
            },
            table.id,
          ),
        ),
      }),
    ],
  });
}

// src/page-content/pages/schedule/Schedule.tsx
var import_jsx_runtime208 = __toESM(require_jsx_runtime(), 1);
function SchedulePage() {
  const { lang } = useLanguage();
  const scheduleSection = getScheduleSection(lang);
  const scheduleTranslation = getScheduleTranslation(lang);
  if (!scheduleSection) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime208.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime208.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime208.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime208.jsx)(HeaderSection, {
        id: "schedule",
        title: scheduleSection.title.trim() || scheduleTranslation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime208.jsx)(Section, {
        id: "schedule-tables",
        children: /* @__PURE__ */ (0, import_jsx_runtime208.jsx)(
          ScheduleSection,
          {},
        ),
      }),
    ],
  });
}

// src/page-content/pages/faq/FaqPage.tsx
init_runtime2();

// src/page-content/components/section/faq/FaqSection.tsx
init_runtime2();

// src/page-content/components/section/faq/FaqItem.tsx
var import_react49 = __toESM(require_react(), 1);
var import_jsx_runtime209 = __toESM(require_jsx_runtime(), 1);
function FaqItem({ question, answer, defaultOpen = false }) {
  const [open, setOpen] = (0, import_react49.useState)(defaultOpen);
  const answerId = (0, import_react49.useId)();
  return /* @__PURE__ */ (0, import_jsx_runtime209.jsxs)(Box_default, {
    sx: (theme2) => ({
      borderTop: `1px solid ${open ? theme2.palette.text.primary : theme2.palette.surfaces.border}`,
      transition: theme2.transitions.create(["border-color"], {
        duration: theme2.transitions.duration.short,
      }),
    }),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime209.jsxs)(Box_default, {
        component: "button",
        type: "button",
        "aria-expanded": open,
        "aria-controls": answerId,
        onClick: () => setOpen(!open),
        sx: (theme2) => ({
          width: "100%",
          display: "flex",
          alignItems: "center",
          justifyContent: "space-between",
          gap: "20px",
          px: 0,
          py: "24px",
          border: 0,
          background: "none",
          cursor: "pointer",
          textAlign: "left",
          color: theme2.palette.text.primary,
          transition: theme2.transitions.create(["color"], {
            duration: theme2.transitions.duration.short,
          }),
          "&:hover, &:focus-visible": { color: theme2.palette.primary.main },
        }),
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime209.jsx)(Typography_default, {
            variant: "h5",
            component: "h3",
            sx: { color: "inherit" },
            children: question,
          }),
          /* @__PURE__ */ (0, import_jsx_runtime209.jsx)(Box_default, {
            "aria-hidden": true,
            sx: {
              flexShrink: 0,
              width: 20,
              height: 20,
              display: "grid",
              placeItems: "center",
            },
            children: open
              ? /* @__PURE__ */ (0, import_jsx_runtime209.jsx)(MinusIcon, {})
              : /* @__PURE__ */ (0, import_jsx_runtime209.jsx)(PlusIcon, {}),
          }),
        ],
      }),
      /* @__PURE__ */ (0, import_jsx_runtime209.jsx)(Collapse_default, {
        in: open,
        children: /* @__PURE__ */ (0, import_jsx_runtime209.jsx)(
          Typography_default,
          {
            id: answerId,
            variant: "body1",
            sx: { color: "text.secondary", pb: "24px", mt: "-8px" },
            children: answer,
          },
        ),
      }),
    ],
  });
}

// src/page-content/components/section/faq/FaqSection.tsx
var import_jsx_runtime210 = __toESM(require_jsx_runtime(), 1);
function FaqSection() {
  const { lang } = useLanguage();
  const faqSection = getFaqSection(lang);
  if (!faqSection || faqSection.items.length === 0) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime210.jsxs)(Box_default, {
    sx: {
      display: "flex",
      flexDirection: "column",
      gap: { xs: "36px", md: "48px" },
    },
    children: [
      faqSection.text &&
        /* @__PURE__ */ (0, import_jsx_runtime210.jsx)(Typography_default, {
          variant: "body1",
          sx: { maxWidth: 640 },
          children: faqSection.text,
        }),
      /* @__PURE__ */ (0, import_jsx_runtime210.jsx)(Box_default, {
        sx: (theme2) => ({
          display: "flex",
          flexDirection: "column",
          borderBottom: `1px solid ${theme2.palette.surfaces.border}`,
        }),
        children: faqSection.items.map((item, index) =>
          /* @__PURE__ */ (0, import_jsx_runtime210.jsx)(
            FaqItem,
            {
              question: item.question,
              answer: item.answer,
              defaultOpen: index === 0,
            },
            index,
          ),
        ),
      }),
    ],
  });
}

// src/page-content/pages/faq/FaqPage.tsx
var import_jsx_runtime211 = __toESM(require_jsx_runtime(), 1);
function FaqPage() {
  const { lang } = useLanguage();
  const faqTranslation = getFaqTranslation2(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime211.jsxs)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime211.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime211.jsx)(Footer, {}),
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime211.jsx)(HeaderSection, {
        title: faqTranslation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime211.jsx)(Section, {
        id: "faq",
        color: "surfaces.surface",
        children: /* @__PURE__ */ (0, import_jsx_runtime211.jsx)(
          FaqSection,
          {},
        ),
      }),
    ],
  });
}

// src/page-content/components/section/not-found/NotFoundSection.tsx
init_runtime2();
init_static2();
var import_jsx_runtime212 = __toESM(require_jsx_runtime(), 1);
function NotFoundSection() {
  const { lang } = useLanguage();
  const translation = getNotFoundTranslation2(lang);
  return /* @__PURE__ */ (0, import_jsx_runtime212.jsxs)(Box_default, {
    sx: {
      minHeight: { xs: 460, sm: 520, md: 620 },
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      textAlign: "center",
      gap: { xs: "20px", md: "28px" },
    },
    children: [
      /* @__PURE__ */ (0, import_jsx_runtime212.jsx)(Box_default, {
        "aria-hidden": true,
        sx: (theme2) => ({
          width: { xs: 88, md: 104 },
          height: { xs: 88, md: 104 },
          borderRadius: "50%",
          display: "grid",
          placeItems: "center",
          backgroundColor: theme2.palette.surfaces.rose,
          color: theme2.palette.text.primary,
        }),
        children: /* @__PURE__ */ (0, import_jsx_runtime212.jsx)(
          MagnifierIcon,
          { size: 38 },
        ),
      }),
      /* @__PURE__ */ (0, import_jsx_runtime212.jsx)(Typography_default, {
        variant: "h1",
        component: "h1",
        sx: { maxWidth: 760 },
        children: translation.title,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime212.jsx)(Typography_default, {
        variant: "body1",
        sx: { maxWidth: 520, color: "text.secondary" },
        children: translation.text,
      }),
      /* @__PURE__ */ (0, import_jsx_runtime212.jsx)(ArrowButton, {
        component: "a",
        href: withBasePath("/"),
        tone: "primary",
        children: translation.backToHome,
      }),
    ],
  });
}

// src/page-content/pages/not-found/NotFound.tsx
var import_jsx_runtime213 = __toESM(require_jsx_runtime(), 1);
function NotFoundPage() {
  return /* @__PURE__ */ (0, import_jsx_runtime213.jsx)(PageLayout, {
    header: /* @__PURE__ */ (0, import_jsx_runtime213.jsx)(Header, {}),
    footer: /* @__PURE__ */ (0, import_jsx_runtime213.jsx)(Footer, {}),
    solidHeader: true,
    children: /* @__PURE__ */ (0, import_jsx_runtime213.jsx)(Section, {
      id: "404",
      color: "surfaces.surface",
      paddingY: { xs: "96px", md: "120px" },
      children: /* @__PURE__ */ (0, import_jsx_runtime213.jsx)(
        NotFoundSection,
        {},
      ),
    }),
  });
}

// scripts/snapshot/render.tsx
var import_jsx_runtime214 = __toESM(require_jsx_runtime(), 1);
var pages = {
  home: HomePage,
  about: AboutPage,
  contact: ContactPage,
  services: ServicesPage,
  portfolio: PortfolioPage,
  pricing: PricingPage,
  blog: BlogPage,
  careers: CareersPage,
  events: EventsPage,
  gallery: GalleryPage,
  legal: LegalPage,
  videos: VideosPage,
  catalogues: CataloguesPage,
  schedule: SchedulePage,
  faq: FaqPage,
  "not-found": NotFoundPage,
};
var details = {
  portfolio: PortfolioItemPage,
  pricing: PricingItemPage,
  blog: BlogPostPage,
  careers: CareerPage,
  events: EventPage,
};
function renderPrimarySnapshot(site, route, basePath) {
  configureSnapshotRuntime(site);
  setSnapshotPath(`${basePath}${route.path}`);
  const cache = createCache({ key: "sgsnapshot" });
  const theme2 = createPreviewTheme(getThemeSettings());
  const Detail = details[route.page];
  const Page = pages[route.page];
  if (!Page) throw new Error(`Unknown snapshot page: ${route.page}`);
  const markup = (0, import_server.renderToStaticMarkup)(
    /* @__PURE__ */ (0, import_jsx_runtime214.jsx)(CacheProvider, {
      value: cache,
      children: /* @__PURE__ */ (0, import_jsx_runtime214.jsxs)(
        ThemeProvider4,
        {
          theme: theme2,
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime214.jsx)(
              CssBaseline_default,
              {},
            ),
            route.slug && Detail
              ? /* @__PURE__ */ (0, import_jsx_runtime214.jsx)(Detail, {
                  slug: route.slug,
                })
              : /* @__PURE__ */ (0, import_jsx_runtime214.jsx)(Page, {}),
          ],
        },
      ),
    }),
  );
  if (!markup.includes("<main"))
    throw new Error(`Snapshot has no main content: ${route.path}`);
  if (/<script\b/i.test(markup))
    throw new Error(`Executable script in snapshot: ${route.path}`);
  return markup;
}

// scripts/snapshot/data.ts
var import_node_fs = __toESM(require("node:fs"), 1);
var import_node_path = __toESM(require("node:path"), 1);
var import_node_crypto = require("node:crypto");
var languageCodes = /* @__PURE__ */ new Set(["SL", "EN", "DE", "IT", "HR"]);
function localizeValue(value, language, primary) {
  if (Array.isArray(value))
    return value.map((v) => localizeValue(v, language, primary));
  if (!value || typeof value !== "object") return value;
  const object = value;
  const keys = Object.keys(object);
  if (keys.length && keys.every((key) => languageCodes.has(key))) {
    return object[language] ?? object[primary] ?? "";
  }
  return Object.fromEntries(
    Object.entries(object).map(([key, child]) => [
      key,
      localizeValue(child, language, primary),
    ]),
  );
}
function atomicWrite(destination, content) {
  import_node_fs.default.mkdirSync(
    import_node_path.default.dirname(destination),
    { recursive: true },
  );
  const temporary = `${destination}.${process.pid}.${(0, import_node_crypto.randomBytes)(6).toString("hex")}.tmp`;
  try {
    import_node_fs.default.writeFileSync(temporary, content, "utf8");
    import_node_fs.default.renameSync(temporary, destination);
  } finally {
    import_node_fs.default.rmSync(temporary, { force: true });
  }
}
function prepareRuntimeData(site) {
  const primary = site.general.primaryLanguage;
  const languages = [
    .../* @__PURE__ */ new Set([primary, ...site.general.supportedLanguages]),
  ];
  const version2 = `${Date.now()}_${(0, import_node_crypto.randomBytes)(6).toString("hex")}`;
  const files = languages.map((language) => ({
    file: `website_${language.toLowerCase()}_${version2}.json`,
    content: JSON.stringify(localizeValue(site, language, primary)),
  }));
  return {
    version: version2,
    files,
    meta: JSON.stringify({ version: version2 }),
  };
}
function removeOldWebsiteJsonFiles(directory) {
  if (!import_node_fs.default.existsSync(directory)) return;
  for (const file of import_node_fs.default.readdirSync(directory)) {
    if (/^website(?:_[a-zA-Z]+)?_\d+(?:_[a-f0-9]+)?\.json$/i.test(file)) {
      import_node_fs.default.unlinkSync(
        import_node_path.default.join(directory, file),
      );
    }
  }
}

// scripts/snapshot/refresh.ts
function argsOf(argv) {
  const result = {
    out: "out",
    website: "website.json",
    manifest: import_node_path2.default.join(__dirname, "manifest.json"),
    dryRun: false,
  };
  for (let i = 0; i < argv.length; i++) {
    const key = argv[i];
    if (key === "--") continue;
    if (key === "--dry-run") {
      result.dryRun = true;
      continue;
    }
    if (
      !["--out", "--website", "--manifest"].includes(key) ||
      !argv[i + 1] ||
      argv[i + 1].startsWith("--")
    ) {
      throw new Error(
        `Unknown or missing argument: ${key}. Use --out DIR --website FILE [--manifest FILE] [--dry-run].`,
      );
    }
    result[key.slice(2)] = argv[++i];
  }
  return result;
}
function htmlFiles(directory, prefix2 = "") {
  return import_node_fs2.default
    .readdirSync(directory, { withFileTypes: true })
    .flatMap((entry) => {
      if (["_next", "data", "images", "assets"].includes(entry.name)) return [];
      if (entry.isSymbolicLink())
        throw new Error(
          `Symlink inside route tree is not supported: ${entry.name}`,
        );
      const relative = prefix2 ? `${prefix2}/${entry.name}` : entry.name;
      if (entry.isDirectory()) {
        return htmlFiles(
          import_node_path2.default.join(directory, entry.name),
          relative,
        );
      }
      return entry.name.endsWith(".html") ? [relative] : [];
    });
}
function refreshExport(argv = process.argv.slice(2)) {
  const started = import_node_perf_hooks.performance.now();
  const options = argsOf(argv);
  const output = import_node_fs2.default.realpathSync(
    import_node_path2.default.resolve(options.out),
  );
  const manifest = JSON.parse(
    import_node_fs2.default.readFileSync(options.manifest, "utf8"),
  );
  if (manifest.schema !== 1)
    throw new Error("Unknown META renderer manifest version");
  const site = JSON.parse(
    import_node_fs2.default.readFileSync(options.website, "utf8"),
  );
  assertMetaCompatible(site, manifest);
  const seed = makeSeoSeed(site, manifest);
  const css2 = import_node_fs2.default.readFileSync(
    import_node_path2.default.join(__dirname, "refresh.css"),
    "utf8",
  );
  const files = htmlFiles(output);
  for (const route of manifest.routes) {
    if (route.page !== "not-found" && !files.includes(route.file)) {
      throw new Error(
        `Missing expected exported page: ${route.file}. FULL build required.`,
      );
    }
  }
  const updates = [];
  for (const file of files) {
    let route = manifest.routes.find((route2) => route2.file === file) ?? {
      path: "/404/",
      file,
      page: "not-found",
    };
    if (
      route.id &&
      site[route.page]?.items?.[route.id - 1]?.status === "DISABLED"
    ) {
      route = { ...route, page: "not-found", slug: void 0, id: void 0 };
    }
    const markup = renderPrimarySnapshot(site, route, manifest.basePath);
    const snapshot = `<style data-sitegen-snapshot-css="true">${css2.replace(/<\/style/gi, "<\\/style")}</style>${markup}`;
    const filename = import_node_path2.default.resolve(output, file);
    const html2 = import_node_fs2.default.readFileSync(filename, "utf8");
    const metadataRoute =
      manifest.routes.find((route2) => route2.file === file) ?? route;
    updates.push({
      destination: filename,
      content: patchExportedHtml(html2, snapshot, seed, metadataRoute),
    });
  }
  const data = prepareRuntimeData(site);
  for (const file of data.files)
    updates.push({
      destination: import_node_path2.default.join(output, "data", file.file),
      content: file.content,
    });
  const urls = Object.values(seed.pages)
    .filter((meta) => !meta.noindex && meta.canonical)
    .map((meta) => meta.canonical);
  const sitemap = `<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map((url) => `<url><loc>${escapeHtml(url)}</loc></url>`).join("")}</urlset>`;
  updates.push({
    destination: import_node_path2.default.join(output, "sitemap.xml"),
    content: sitemap,
  });
  updates.push({
    destination: import_node_path2.default.join(output, "robots.txt"),
    content: `User-agent: *
Allow: /
${
  manifest.siteOrigin
    ? `Sitemap: ${manifest.siteOrigin}${manifest.basePath}/sitemap.xml
`
    : ""
}`,
  });
  updates.push({
    destination: import_node_path2.default.join(output, "data/meta.json"),
    content: data.meta,
  });
  if (!options.dryRun) {
    removeOldWebsiteJsonFiles(import_node_path2.default.join(output, "data"));
    for (const update of updates)
      atomicWrite(update.destination, update.content);
  }
  console.log(
    `${options.dryRun ? "Validated" : "Refreshed"} ${files.length} primary-language HTML files + runtime JSON in ${((import_node_perf_hooks.performance.now() - started) / 1e3).toFixed(2)}s; no Next build.`,
  );
  return { pageCount: files.length, version: data.version };
}
if (typeof require !== "undefined" && require.main === module) {
  try {
    refreshExport();
  } catch (error) {
    console.error(error instanceof Error ? error.message : error);
    process.exitCode = 1;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 &&
  (module.exports = {
    refreshExport,
  });
/*! Bundled license information:

react/cjs/react.production.js:
  (**
   * @license React
   * react.production.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react-dom/cjs/react-dom.production.js:
  (**
   * @license React
   * react-dom.production.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react-dom/cjs/react-dom-server-legacy.node.production.js:
  (**
   * @license React
   * react-dom-server-legacy.node.production.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react-dom/cjs/react-dom-server.node.production.js:
  (**
   * @license React
   * react-dom-server.node.production.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react-is/cjs/react-is.production.min.js:
  (** @license React v16.13.1
   * react-is.production.min.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react/cjs/react-jsx-runtime.production.js:
  (**
   * @license React
   * react-jsx-runtime.production.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react-is/cjs/react-is.production.js:
  (**
   * @license React
   * react-is.production.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

photoswipe/dist/photoswipe-lightbox.esm.js:
  (*!
    * PhotoSwipe Lightbox 5.4.4 - https://photoswipe.com
    * (c) 2024 Dmytro Semenov
    *)

photoswipe/dist/photoswipe.esm.js:
  (*!
    * PhotoSwipe 5.4.4 - https://photoswipe.com
    * (c) 2024 Dmytro Semenov
    *)

@mui/styled-engine/esm/index.js:
  (**
   * @mui/styled-engine v7.3.10
   *
   * @license MIT
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

@mui/private-theming/esm/index.js:
  (**
   * @mui/private-theming v7.3.11
   *
   * @license MIT
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

@mui/system/esm/index.js:
  (**
   * @mui/system v7.3.11
   *
   * @license MIT
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

@mui/material/esm/index.js:
  (**
   * @mui/material v7.3.11
   *
   * @license MIT
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)
*/
